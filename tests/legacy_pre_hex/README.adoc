= Legacy Pre-Hex Test Classification

Status: classified empty quarantine for `FTR-PREHEX-TEST-CLASSIFICATION-P3C`.

Lifecycle state: *Quarantined*.

This directory is physically separated from the active suite and is excluded
from the default `make test` run. Use `make test-prehex` to run this explicit
quarantine slice. When no tracked `test_*.py` files exist here, the target is
expected to report the documented deferred/no-tests outcome.

No tracked `test_*.py` files currently remain under `tests/legacy_pre_hex/`.

== Current Classification

[cols="2,1,4"]
|===
| Area | Classification | Rationale

| FK-helper edge cases
| promote
| Still-relevant behavior is covered in `tests/unit/domain/transformations/test_fk_edge_cases.py`.

| Validation mode behavior
| promote
| Still-relevant behavior is covered in `tests/unit/domain/validations/test_validate_modes.py`.

| Remaining historical pre-hex implementation tests
| retire
| No tracked source tests remain in this quarantine directory. Historical implementation-coupled tests must not be reintroduced without a new classification entry.
|===

== Future Additions

Any future file placed under `tests/legacy_pre_hex/` must add a classification
entry here before it is treated as intentional quarantine material.

Allowed classifications:

* `promote`: move or keep equivalent behavior in the active suite
* `rewrite`: replace historical structure with an active-suite test at the
  current abstraction level
* `retire`: keep only as documented history or remove entirely
