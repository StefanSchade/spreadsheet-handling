This suite contains pre-hexagonal (“pre-hex”) legacy tests
that exercise the old core/pack/unpack flows predating the
rendering/IR architecture.

Folder: tests/legacy_pre_hex/
Marker: @pytest.mark.legacy
Ignored by default in CI.

Planned removal: FTR-PREHEX-TEST-RETIRE (Phase 6)

