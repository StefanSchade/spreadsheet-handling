===== BUG-UNRESOLVED-HELPER-VALUE-NAN-SEMANTICS-P4A -- Unresolved FK-helper values collapse into backend-divergent, domain-colliding representations (literal `nan` on ODS, empty string on XLSX)

*Status:* Partially implemented / semantic contract still open
*Priority:* P4A
*Area:* FK helper materialization / unresolved-value contract / canonical persistence
*Release relevance:* Post-`0.2.0`. Not a runtime blocker. This is the next
dependency-ready item in the domain-hardening sequence
(`roadmap_domain_hardening_sequence.adoc` Phase C) and is named as the
next action by `CONC-DOMAIN-BOUNDARY-ROBUSTNESS` and as the sole named
non-goal exception of `DEC-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` /
`ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`.

*Revalidated:* 2026-08-07, at core HEAD `3a8d96f5716ca429c9e1c67b9cdad8d7b759e2d1`
(clean worktree). This revalidation reconciles the historical narrative with
current maintained code and the current xfail sentinel, pins the exact
current root cause, and rewrites the contract question into a decision-ready
shape. It does not implement a fix and does not choose among the recorded
options; see "Recommended direction" below for the boundary between
*recommended* and *accepted / implementation-authorized*.

*Implementation slice 1:* 2026-08-07, same-day follow-up at core HEAD
`1cecb5c7523dc29c5e9f451847f12a050b0ceb40` (the revalidation commit). Fixes
only the backend-divergent accidental serialization of the unresolved
helper's pandas missing carrier (the ODS literal `"nan"` corruption and the
XLSX/ODS divergence). Does *not* finalize the broader unresolved-reference
semantic contract -- see "Implementation slice 1 -- carrier defect fixed"
below, which does not replace "Recommended direction" but narrows what is
now fixed versus what remains open.

==== Title note

The original title ("Unresolved helper values become the literal string
`nan` in canonical JSON") is *not* retired -- current evidence
(section "Current reproduction" below) shows the literal `"nan"` collapse is
still live today on the ODS backend, for both the empty-source and the
non-empty-unresolved-key case. The title is broadened, not corrected, because
current evidence also shows a *second*, backend-divergent collapse (plain
empty string on XLSX) that the original title did not name and that is
equally a defect. Both symptoms are current facts, not merely historical
ones.

==== Historical discovery (preserved)

Reproduced at historical core HEAD `8f8b519` (2026-06-03 era), via the
worldbuilding consumer's `make export-stories-ods` + `make import-stories-ods`
recipe (recipe still present in the sibling `worldbuilding` repository's
`Makefile`; not re-run here, as this task is scoped to `core`). From
`worldbuilding/tmp/dino_stories_reimported/characters.json`:

[source,json]
----
{
  "id": "CHAR-0022",
  "name": "...",
  "home_place_id": "",
  "_places_name": "nan"
}
----

Six rows carried `"_places_name": "nan"`, one per row whose `home_place_id`
was the empty string. The historical BUG text explicitly left the root cause
"still to be pinned" and recorded three plausible carriers: FK helper
materialization producing a pandas NaN; a downstream stringification
artifact; or replay of already-`"nan"`-contaminated canonical data (the last
one was already ruled out for that specific reproduction by the historical
text itself, since the seed canonical JSON contained no `"nan"` strings).

This revalidation confirms carrier (1) -- FK helper materialization producing
a pandas NaN -- is the real cause, and additionally pins exactly *where* that
NaN turns into the literal string `"nan"` on the write path, which the
historical text did not reach (see "Root cause -- pinned" below).

==== Current reproduction (evidence, HEAD `3a8d96f5716c`)

Reproduced independently of the worldbuilding recipe, using only maintained
current APIs: the `tests/roundtrip/` fixture shape (`minimal_fk_workdir` in
`tests/roundtrip/conftest.py`) driven through the public CLI entry point
(`spreadsheet_handling.cli.apps.run.main`), via small read-only probe scripts
(no production code modified). Full matrix in the checkpoint file.

[cols="3,2,2",options="header"]
|===
| Source `entity_id` | XLSX no-cleanup reimport `_entities_name` | ODS no-cleanup reimport `_entities_name`

| `"ENT-001"` (valid) | `"Alpha"` (resolved) | `"Alpha"` (resolved)
| `""` (empty source) | `""` | *`"nan"`*
| `"ENT-MISSING"` (non-empty, unresolvable) | `""` | *`"nan"`*
| `"nan"` (literal domain string as source key, itself unresolvable) | `""` | *`"nan"`*
|===

Two further probes prove the collapse is not merely cosmetic but actively
*colliding with legitimate domain data* today, on both backends:

* A row whose FK *resolves successfully* to a target whose own field value is
  the literal domain string `"nan"` reimports as `"_entities_name": "nan"` on
  *both* XLSX and ODS -- on ODS this is byte-identical to the corrupted
  unresolved-case output above; there is no way for a canonical JSON consumer
  to tell them apart today.
* A row whose FK *resolves successfully* to a target whose own field value is
  a legitimate empty string reimports as `"_entities_name": ""` on both
  backends -- on XLSX this is byte-identical to the collapsed unresolved-case
  output; again, no way to tell them apart.

The `cleanup=drop` reimport path (`apply_derived_column_policy ...
policy: drop`) is unaffected either way on both backends: the helper column
is removed entirely before it can leak either representation. This matches
the BUG's original framing -- the twin `BUG-HELPER-CLEANUP-CONTRACT-P4A` (now
*Done*, archived to `docs/cold_storage/backlog/ftrs_done/`) governs *whether*
helpers reach canonical JSON; this BUG governs *what value they carry when
they legitimately do* (upstream/editable views, or any no-cleanup reimport
path).

==== Changed behavior since discovery

* The reproduction path changed. The historical recipe used the worldbuilding
  ODS consumer end to end; the current maintained regression sentinel
  (`tests/roundtrip/test_pathological_cell_contracts.py`) uses a synthetic
  fixture and, critically, *only exercises the XLSX backend*. That is a real
  coverage gap: the current xfail is blind to the still-live ODS `"nan"`
  collapse this revalidation reproduces.
* XLSX behavior changed (or was never the same as ODS to begin with -- the
  historical text did not distinguish backends). XLSX today collapses
  unresolved helper values to `""`, not `"nan"`. This is not a deliberate
  contract; see "Root cause -- pinned".
* `domain/fk_relations.py` has been restructured since the BUG was written
  and is now the v2 relation *policy* producer module
  (`infer_fk_relations`, `apply_v2_relations`, `build_v2_relation`) -- it is
  no longer where helper *values* are materialized. The old BUG's reference
  to `domain/fk_relations.py:160-285` as "the FK helper provenance and
  materialization shape" is stale. See "Relevant code path" below for the
  corrected pointer.
* `domain/transformations/derived_column_policy.py`'s `_norm` helper moved
  from line 386 (as the old BUG recorded) to line 605; its behavior is
  unchanged.
* The twin `BUG-HELPER-CLEANUP-CONTRACT-P4A` shipped and is archived. The
  FK-helper v1-to-v2 policy migration (`FTR-FK-HELPERS-POLICY-DRIVEN-
  PRIMITIVES-P5` and siblings) landed. Neither changed the value-
  materialization path this BUG concerns.
* `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` was accepted (Independent
  Architecture Review 002, 2026-08-07) and explicitly names this BUG as the
  one thing it does *not* decide ("the missing/unresolved/spreadsheet-error
  representation"), confirming this BUG remains the correct and only current
  owner of that question.
* `FTR-PAYLOAD-SEMANTICS-RECOVERY-P5` was drafted (exploratory, still *New*)
  using this BUG as its motivating case. It proposes a general
  classify/recover/pin registry contract and states a *recovery preference*
  (favors representations that surface meaning into data over a
  metadata-only finding) without deciding this BUG's outcome. It remains
  evidence, not authority, until it is itself accepted.

==== Exact current unresolved contract (the xfail, verbatim facts)

NOTE: this section describes the state *as revalidated*, before
"Implementation slice 1" below. The specific xfail test named here
(`test_missing_fk_key_helper_representation_without_cleanup`) has since
been retired and replaced; its underlying observation (backend-divergent
`""`/`"nan"` corruption) is now fixed. The section is preserved as
historical evidence of the pre-slice-1 state, not as the current test
inventory -- see "Implementation slice 1" for what changed and what is
still open.

`tests/roundtrip/test_pathological_cell_contracts.py::
test_missing_fk_key_helper_representation_without_cleanup`:

* *Input key used:* `entity_id` on `ITEM-003` is rewritten from the fixture's
  default `""` to the non-empty, unresolvable identifier `"ENT-MISSING"`
  before export. The fixture's own default seed row (unmodified) already
  covers the empty-string case, which this revalidation probed separately
  (table above).
* *Helper configuration:* `configure_fk_helpers` (v2, explicit) on `items` ->
  `entities`, `fk_column: entity_id`, `allowed_helpers`/`default_helpers:
  ["name"]`, `helper_prefix: "_"`; `add_fk_helpers` with `levels: 2,
  helper_value_mode: "values"` (not formula mode).
* *Backend/path exercised:* XLSX only, no-cleanup reimport
  (`apply_workbook_view_sheet_mappings` only; `apply_derived_column_policy`
  is deliberately *not* in the reverse pipeline so the helper column stays
  observable).
* *Cleanup behavior:* deliberately bypassed by test design, to keep the value
  contract observable at all.
* *Currently observed helper value:* the empty string `""` (confirmed by
  running the test individually below and by the direct probe matrix above).
* *Currently expected future contract:* explicitly left undecided by the
  test's own `reason=` text -- "null, a structured finding, a typed
  unresolved token, or another documented shape."
* *Exact xfail reason:* `"BUG-UNRESOLVED-HELPER-VALUE-NAN-SEMANTICS-P4A: the
  framework currently has no defined contract for unresolved helper values.
  In the no-cleanup sentinel path used here, a missing FK key currently
  reimports with an ordinary empty-string helper value; the final
  representation may be null, a structured finding, a typed unresolved token,
  or another documented shape once the contract decision lands."`
* *Strict or non-strict:* `strict=False`. The assertion only checks
  `helper_value not in ("nan", "")`; it does not assert a specific target
  shape.

Individual run at HEAD `3a8d96f`:

[source,console]
----
$ .venv/bin/pytest -q tests/roundtrip/test_pathological_cell_contracts.py -rA
x                                                                        [100%]
XFAIL ...test_missing_fk_key_helper_representation_without_cleanup - BUG-UNRESOLVED-HELPER-VALUE-NAN-SEMANTICS-P4A: ...
----

Where the current `""` is introduced: not at the FK-key normalization layer
(`core/fk.py:_norm_id`), which correctly returns `None` for a missing/NaN key
and the literal string `"ENT-MISSING"` or `""` unchanged for a present key.
It is introduced at the *write/read boundary of the XLSX backend*, where a
missing helper *value* (see "Root cause -- pinned") round-trips through
openpyxl as a blank cell and the XLSX parser's `str(val) if val is not None
else ""` (the accepted visible-cell string-substrate contract, per
`ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE`) turns the blank cell into `""`.
This is *not* an accepted semantic contract for "unresolved" -- it is an
accidental byproduct of one specific library's (openpyxl's) NaN-to-`None`
write behavior, which happens to compose with the string-substrate ADR to
produce `""`. The ODS backend, going through the same in-memory NaN, produces
`"nan"` instead, via an unrelated accidental defect (below). Neither backend's
current output is a deliberate representation of "unresolved."

==== Root cause -- pinned

Distinguishing *observed current fact* from *historical explanation* from
*hypothesis*, per the full trace (probes in the checkpoint file; all read via
`.venv/bin/python`, no production code modified):

. *observed current fact* -- `domain/transformations/fk_helpers/enrich.py`'s
  `enrich_helpers` calls `core/fk.py:apply_fk_helpers`
  (`src/spreadsheet_handling/core/fk.py:~327-333`). For each unresolved row,
  `value_map.get(_norm_id(rid))` returns Python `None` (dict-default
  behavior; `_norm_id` itself is correct and unchanged since the historical
  BUG -- `core/fk.py:26`).
. *observed current fact* -- assigning a Python list containing `None`
  (mixed with resolved `str` values) into a new DataFrame column
  (`core/fk.py:337`, `new_df[col_tuple] = values`) does not preserve the
  `None`. Under the project's currently active pandas string dtype, the
  column reports `dtype == "str"` but the unresolved cells are stored as an
  actual Python `float('nan')`, confirmed by direct inspection
  (`type(value) is float`, `repr(value) == 'nan'`). This is the FK-helper
  value materialization step turning "no match" into a *pandas/NumPy carrier
  missing value* -- not yet a string, and not yet `"nan"` as text.
. *observed current fact* -- `rendering/composer/layout_composer.py:353`
  (`compose_workbook`) copies `df.values.tolist()` directly into
  `TableBlock.data` with *no* NaN/None normalization at the render-IR
  boundary. The raw float NaN reaches both backend renderers unchanged.
. *observed current fact, XLSX* -- `io_backends/xlsx/openpyxl_renderer.py`
  passes the raw value straight to `ws.cell(..., value=val)`
  (`_xlsx_cell_value`/`_write_data_block`, ~line 218-235) with no NaN
  handling. A minimal openpyxl-only probe (write `float('nan')`, save,
  reload) confirms openpyxl itself silently converts a NaN-valued cell to
  `None` on save/reload -- this is accidental library behavior, not a
  decision made anywhere in this codebase. The XLSX parser then applies its
  documented, ADR-compliant `str(val) if val is not None else ""`
  (`io_backends/xlsx/parser_interpretation.py`), producing `""`.
. *observed current fact, ODS* -- `io_backends/ods/odf_renderer.py:563`
  contains the cell-emptiness check `if value in (None, ""):`. Python's `in`
  operator uses `==` comparisons, and `float('nan') == float('nan')` is
  `False` by IEEE-754 definition, so `nan in (None, "")` evaluates to
  `False`. The NaN therefore *fails* the emptiness check and falls through to
  the numeric branch (`isinstance(value, (int, float))`, line 573-579),
  which writes `valuetype="float"` with a paragraph text of `str(value)` --
  literally `"nan"` -- into the cell. This is a genuine, narrow correctness
  defect in this one emptiness check: the project already has at least three
  independent, *correct* implementations of "carrier-missing vs literal
  string" elsewhere (`core/fk.py:_norm_id`,
  `derived_column_policy.py:_norm` at line 605, and
  `domain/_cell_primitives.py:_is_empty_cell`, all of which use
  `pd.isna(...)` rather than `in (None, "")`), none of which is applied at
  this write boundary.
. *observed current fact, ODS read* -- `io_backends/ods/odf_parser.py:86`'s
  `_cell_value` reads the cell's paragraph text (`_cell_text`) *first*, before
  consulting `office:value-type`/`office:value`. Since the renderer wrote a
  non-empty paragraph text of `"nan"`, the parser returns the literal string
  `"nan"` without ever reaching the float-attribute branch.
. *hypothesis, explicitly not claimed as fact* -- whether the *historical*
  8f8b519-era reproduction went through exactly this same ODS code path is
  plausible (same architecture family, same string-substrate contract) but
  not re-verified against that historical commit; this revalidation verifies
  the *current* HEAD path only, which independently reproduces the same
  symptom via the mechanism traced above.
. *historical explanation, superseded* -- the historical BUG's "Root cause
  sketch" left open whether a downstream stringification artifact or
  contaminated replay was responsible. This revalidation rules out
  contaminated replay for the current reproduction (fresh synthetic fixture,
  no `"nan"` in seed data) and identifies the "stringification artifact" as,
  specifically, the ODS renderer's NaN-blind emptiness check -- not a generic
  or diffuse stringification problem.

==== Semantic distinctions (grounded in the current trace, not assumed)

The trace above shows the *in-memory* value materialization step already
distinguishes more than the persisted representation preserves: a
successfully resolved helper whose target value happens to be a legitimate
empty string is represented in-memory as the Python string `''`, genuinely
different from the unresolved case's `float('nan')`. The information loss is
introduced entirely at the render-write / string-substrate boundary, not in
FK resolution itself. This bounds the fix: normalization at or before the
write boundary can recover the distinction; no change to FK resolution logic
is implied.

Applying the task's six-way split to what actually happens today:

* *Missing source* -- source FK column value itself is empty/absent.
  `_norm_id("")` returns `''` (a real, distinct key, not `None`) so an empty
  source string is technically a *key that fails to match* rather than a
  `None` key -- but in practice it behaves identically to "unresolved
  reference" below because no target row has an empty-string ID. Direct-Frame
  probing shows a *programmatic* `None` or NumPy `NaN` source value
  normalizes to `_norm_id(...) -> None`, which is a genuinely distinct case
  from the empty-string source at the key level, though it collapses to the
  same downstream `nan` value carrier once the lookup misses.
* *Unresolved reference* -- a non-empty, well-formed key
  (`_norm_id` succeeds) with no matching target row (`"ENT-MISSING"`).
  Currently indistinguishable from "missing source" once the lookup misses;
  both produce the same `value_map.get(...) -> None` outcome and the same
  downstream `nan` carrier.
* *Carrier missing value* (pandas `NaN`/`NA`/`NaT`) -- confirmed as the
  actual in-memory carrier for every unresolved case today (step 2 above),
  regardless of which of the above caused the lookup miss.
* *Domain empty string* -- a legitimate resolved value of `''`. Proven
  distinct from "unresolved" *in memory* (step 2), but currently collapses to
  the *same* `""` output as an unresolved value on the XLSX no-cleanup path
  (proven collision, see reproduction table).
* *Literal domain `"nan"`* -- a legitimate resolved value that is textually
  `"nan"`. `_norm_id`/`_norm` both correctly keep this distinct from a
  carrier-missing value when acting on *keys* or *comparison values*. But as
  a resolved *helper value* it currently collapses to the *same* `"nan"`
  output as an unresolved value on the ODS no-cleanup path (proven collision,
  see reproduction table).
* *Spreadsheet/carrier error* -- not currently modeled or producible by any
  maintained automated path. Formula-mode helpers (`helper_value_mode:
  formula`) emit a generic per-row lookup formula
  (`core/formulas.py:lookup_formula`, consumed by
  `fk_helpers/formula_provider.py`) whose `missing=""` fallback is baked into
  the formula text itself, identical for every row regardless of whether that
  row's key is present, missing, or unresolved -- the actual
  resolved/unresolved distinction is deferred entirely to spreadsheet-app
  formula evaluation (Excel/LibreOffice), which no test in this repository
  executes. This is genuinely untested territory for the nan-semantics
  question, not merely unexercised by the current xfail.

The evidence does *not* support minting six separate persisted
representations. It supports: (a) a normalization step that treats
"carrier-missing" uniformly regardless of which upstream cause produced it
(missing source vs. unresolved reference collapse naturally and do not need
separate persisted tokens, since neither `_norm_id` nor any consumer
currently distinguishes them operationally); and (b) that the resulting
uniform "unresolved" representation must not equal either `""` or `"nan"`,
because both already collide with proven legitimate domain data.

==== Evaluated contract options

===== Option A -- collapse unresolved helper value to empty/missing

A bare `""` is evidenced-insufficient: this revalidation proves it already
collides with a legitimate resolved empty-string helper value on the XLSX
no-cleanup path today. JSON `null` does not have this collision (`null !=
""`) but cannot be spelled as a distinct value inside a plain string
spreadsheet cell without a reserved-token convention -- the visible-cell
typing ADR fixes the substrate as plain string and this BUG does not reopen
it. So: `null` is viable wherever the value never has to pass through an
actual visible cell (a pure in-memory or forward-only canonical view); a
plain `""` alone is not viable wherever it does, because it already loses a
real distinction. Diagnostics can be preserved separately (see Option B) but
that is additive, not a substitute for choosing a non-colliding cell value.
Compatible with the visible-cell string substrate as long as the eventual
in-cell token is still an ordinary string.

===== Option B -- visible/payload fallback plus structured diagnostic

`domain/finding_frame.py` (`Finding`, `findings_to_frame`, the shared
seven-column schema) and `derived_column_policy.py`'s
`derived_column_findings` output are an *owned, existing, reusable*
mechanism -- no new diagnostics mechanism is needed structurally. However,
today that mechanism is wired *only* for the `enrich_lookup` family's value-
mismatch checks, and it *explicitly skips* unresolved references
(`derived_column_policy.py:555`, `# unresolved reference is a separate
concern`). FK helpers are "dropped but not value-checked" in the current
slice (module docstring). Extending it to cover FK-helper unresolved
references is real, bounded, new wiring (new `rule_type`, a new value-check
entry point for the FK-helper family, which currently has none) -- not a
reuse-only change. This makes Option B a credible *complement* once
implemented, not a currently-available shortcut.

===== Option C -- spreadsheet/carrier error representation

Not viable as the sole or primary answer. It cannot cover the missing-source
or unresolved-reference case at all (no spreadsheet error token exists
without a formula being evaluated), and this project's automated tests
cannot exercise real LibreOffice/Excel evaluation to validate formula-mode
behavior. Adopting a spreadsheet error token as the *canonical* domain
representation would also cut against the spirit of
`ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` (no native typed substrate is
authorized here) and the task's own guidance against making spreadsheet error
syntax the canonical representation merely because spreadsheets support it.
A reserved ordinary *string* sentinel (still plain text, not a spreadsheet
error type) remains compatible with the string substrate and is a different,
viable idea from adopting `#N/A`-style tokens as canonical.

===== Other option considered

A dedicated reserved string sentinel, distinct from both `""` and `"nan"`,
used uniformly wherever the value must pass through a visible cell; JSON
`null` used wherever it does not. This is not one of the original three
options as literally written (the original Option 1 conflated `""` and
`null` as interchangeable) but follows directly from the current evidence:
neither `""` nor `"nan"` is safe alone, and the in-memory materialization
step already has the information needed to know "this is unresolved" before
it is ever forced through the string substrate.

==== Recommended direction

*Recommended* (not accepted / implementation-authorized):

. *Prerequisite, independent of the final token choice:* fix the ODS
  renderer's NaN-blind emptiness check
  (`io_backends/ods/odf_renderer.py:563`) so both backends behave
  *identically* for the same unresolved-helper case. This was already the
  BUG's own original acceptance criterion ("XLSX path behaves identically")
  and is currently false. Whatever the eventual sentinel is, both backends
  must produce it, not two different accidental tokens.
. *Value representation:* normalize unresolved helper values (regardless of
  whether the cause was a missing source key or an unresolved reference --
  the evidence does not support persisting that distinction as a separate
  value shape) to a reserved representation that does not collide with
  `""` or `"nan"`. Use JSON `null` on any path where the value never
  passes through a visible spreadsheet cell; use a reserved, documented
  string sentinel (exact spelling *not* decided here) wherever it does.
. *Diagnostics:* extend the existing `derived_column_findings` /
  `Finding`/`findings_to_frame` mechanism to cover FK-helper unresolved
  references as an *additive* complement (Option B), not as the primary value
  carrier. This is consistent with `FTR-PAYLOAD-SEMANTICS-RECOVERY-P5`'s
  stated recovery preference (favor surfacing meaning into data; accept a
  structured finding as a fallback/complement) even though that FTR itself
  remains unaccepted and does not authorize this BUG's decision.
. *Not recommended:* Option C as sole/primary representation, for the
  reasons above.

*Remaining open question this evidence does not settle:* the exact spelling
of the reserved string sentinel, and whether the project accepts two
different representations for "value never touches a spreadsheet cell"
(`null`) versus "value passes through a spreadsheet cell" (reserved string),
or prefers one uniform representation everywhere at the cost of an unusual-
looking canonical JSON value even on cell-free paths. That is a naming/
uniformity decision for the maintainer, not something derivable from current
code or architecture.

==== Implementation scope (updated after Implementation slice 1)

*Done (Implementation slice 1, see above):*

* ~~`domain/transformations/fk_helpers/enrich.py` / `core/fk.py:apply_fk_helpers`
  -- normalize the unresolved-value carrier before it reaches
  `rendering/composer/layout_composer.py`~~ -- fixed via an explicit `""`
  lookup-miss default in `apply_fk_helpers`; no renderer change was needed.
* ~~`tests/roundtrip/test_pathological_cell_contracts.py` -- ... add a
  parallel ODS-backend case~~ -- done; the roundtrip suite now covers both
  backends for the carrier question.

*Still open, for the eventual final-contract fix (not performed by slice 1):*

* The io_backends renderers were *not* touched. `io_backends/ods/odf_renderer.py`'s
  NaN-blind `value in (None, "")` emptiness check (line 563) is still
  present as written -- it is simply never exercised by a *lookup-miss*
  FK-helper NaN anymore, because a lookup miss in `apply_fk_helpers` no
  longer produces one. A successful lookup whose stored target payload is
  itself `None`, pandas/NumPy missing, or another maintained missing
  carrier is returned verbatim and can still reach this same renderer
  check unchanged; that case is outside this slice (see "Implementation
  slice 1" below). Whether the renderer-level predicate itself deserves an
  independent correctness fix (for that case, or for any other,
  non-FK-helper NaN that might reach it) is out of this BUG's scope; not
  evaluated here.
* Choosing and implementing the final value-representation sentinel (JSON
  `null` off the spreadsheet-cell path, a reserved string sentinel on it,
  or another shape) once the maintainer decides the open naming/uniformity
  question in "Recommended direction" above.
* Optionally, `derived_column_policy.py` -- new `rule_type` and value-check
  entry point for FK-helper unresolved references, if Option B's diagnostic
  complement is authorized alongside the value-representation change.
* `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc` and
  `docs/ai_info/concepts_fk_helpers.adoc` -- document the chosen final
  contract (not yet written; slice 1 only fixed the carrier, it did not
  document a semantic contract because none was decided).

==== Explicit non-goals

* Do not implement the fix in this task (revalidation only).
* Do not decide the exact sentinel string spelling here.
* Do not extend this BUG to cover all NaN/null normalization across the
  framework -- scope stays FK-helper-family value materialization.
* Do not attach security-policy, ingress, or sanitization work to this BUG
  (`ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` explicitly separates this
  question from its own scope and authorizes nothing here).
* Do not persist runtime Resolution as canonical `_meta` metadata to explain
  an unresolved helper -- the helper *cell value itself* is not a `_meta`
  root and is not constrained by the persistence-boundary rule the same way;
  any diagnostic frame (Option B) must stay a derived/transient artifact, not
  a new canonical `_meta` root.
* Do not reopen `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` -- any in-cell
  sentinel remains an ordinary string.
* Do not resolve the formula-mode evaluation question (untested territory
  requiring actual spreadsheet-app execution); note it as a known gap only.
* Do not create a new FTR. Current evidence shows one narrow, coherent
  defect (a backend-write-boundary NaN-handling bug) plus one narrow,
  coherent contract decision (the sentinel choice), both scoped to the
  FK-helper family and two renderer modules. This BUG can and should own the
  eventual implementation directly.

==== Acceptance

*Met by Implementation slice 1:*

* [x] The FK-helper materialization / write path produces *identical*
  behavior on XLSX and ODS for an unresolved helper value (both now `""`).
* [x] Neither backend produces the literal string `"nan"` for an unresolved
  helper value on any representative reproduction (empty source, non-empty
  unresolved key).
* [x] A resolved literal `"nan"` target value no longer collides with
  anything (the collision was only live while ODS could still corrupt an
  unresolved value into `"nan"`; that path is closed).
* [x] `tests/roundtrip/test_pathological_cell_contracts.py` gained a
  parallel ODS-backend case (closing the prior XLSX-only coverage gap); the
  prior xfail was retired rather than merely de-xfailed, since its own
  assertion no longer exercised an open question (see "Implementation
  slice 1" for why).

*Still open (not this slice):*

* [ ] The project records an explicit, accepted contract for unresolved
  helper values (informed by, but not necessarily identical to, the
  recommended direction above), documented in the FK helper concept docs.
* [ ] `""` is currently only a temporary carrier value; it still collides
  with a legitimately resolved helper whose target value is itself an
  empty string (tracked by the new xfail sentinel).

==== Focused test requirements

*Added by Implementation slice 1:*

* [x] `test_unresolved_fk_helper_carrier_is_backend_consistent` (XLSX + ODS,
  parameterized) -- replaces the former
  `test_missing_fk_key_helper_representation_without_cleanup`.
* [x] `test_missing_fk_source_helper_carrier_is_backend_consistent` (XLSX +
  ODS) -- the empty-source case, kept separate from the non-empty
  unresolved-key case above.
* [x] A resolved-value collision guard: a target whose field value is
  literally `"nan"` round-trips unchanged
  (`test_literal_domain_nan_helper_value_survives_roundtrip`, both
  backends).
* [x] A resolved-empty-string collision guard: symmetric case for `""`
  (`test_legitimate_empty_helper_value_survives_roundtrip`, both backends).
* [x] Unit-level coverage for the write-boundary normalization
  (`core/fk.py:apply_fk_helpers`) independent of the roundtrip layer
  (`TestApplyFkHelpersUnresolvedCarrier` in
  `tests/unit/domain/transformations/test_fk_helpers_enrich.py`), per the
  project's "test contracts once, not every format" convention.

*Still needed once the final semantic contract lands:*

* [ ] `test_unresolved_helper_value_distinguishable_from_legitimate_empty_string`
  transitions from xfail to a real passing assertion.
* [ ] Coverage for whatever final representation is chosen (e.g. a `null`
  path, a reserved-sentinel path, or a `derived_column_findings` entry),
  once decided.

==== Downstream dependency relationship to the internal-value-model work

`roadmap_domain_hardening_sequence.adoc` Phase C names this BUG as the
"known blocking contract" that must close before Phase D (the internal type
model) can finalize its missing/unresolved/error categories (5-7 in the
internal-value-model sketch, `trusted_ingress_and_runtime_security.adoc`
section 7). `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`'s non-goals list
names this BUG by ID as the explicit owner of "the missing/unresolved/
spreadsheet-error representation," and `CONC-DOMAIN-BOUNDARY-ROBUSTNESS`'s
`next_action` field names it as the concern's next dependency-ready step,
explicitly scoped to *not* include security-policy representation, parser/
resource limits, sink-local work, or the `run_pipeline` boundary decision --
those remain independently authorizable and must not be merged into this
BUG. Resolving this BUG unblocks only the missing/unresolved/error portion of
the later internal type model; it does not itself perform that typing work.

==== Reference: where unresolved values are produced (corrected pointers)

* `src/spreadsheet_handling/core/fk.py:26` -- `_norm_id`, FK key
  normalization. Returns `None` for `None`, NaN, or unrecognized. Correct and
  unchanged since the historical BUG; operates on keys, not helper values.
* `src/spreadsheet_handling/core/fk.py:~279-360` -- `apply_fk_helpers`, the
  actual helper-*value* materialization point. As of Implementation slice 1
  (below), `value_map.get(_norm_id(rid), "")` supplies an explicit `""`
  default on a lookup miss (line ~346); previously the implicit `None`
  default let the subsequent DataFrame column assignment coerce it into a
  pandas-carrier NaN.
* `src/spreadsheet_handling/domain/transformations/fk_helpers/enrich.py` --
  `enrich_helpers`, the pipeline-facing entry point that calls
  `apply_fk_helpers`. (Old BUG text pointed at `domain/fk_relations.py:160-
  285`, which is now the v2 relation *policy* producer module, not the value
  materialization path -- stale, corrected here.)
* `src/spreadsheet_handling/rendering/composer/layout_composer.py:353` --
  `compose_workbook`, copies `df.values.tolist()` into `TableBlock.data`
  with no NaN normalization.
* `src/spreadsheet_handling/io_backends/xlsx/openpyxl_renderer.py` --
  `_write_data_block`/`_xlsx_cell_value`, passes raw NaN to openpyxl, which
  silently drops it to a blank cell on save/reload.
* `src/spreadsheet_handling/io_backends/ods/odf_renderer.py:563` -- the
  NaN-blind `if value in (None, ""):` emptiness check; the pinned defect.
* `src/spreadsheet_handling/io_backends/ods/odf_parser.py:86` -- `_cell_value`,
  reads paragraph text before `office:value`, so the renderer's mistaken
  `"nan"` text is what round-trips.
* `src/spreadsheet_handling/domain/transformations/derived_column_policy.py:605`
  -- `_norm`, a correct NaN-vs-literal-string normalizer, but scoped to
  `enrich_lookup` mismatch checking only (line number corrected from the
  stale 386 in the original BUG text).
* `src/spreadsheet_handling/domain/_cell_primitives.py:_is_empty_cell` --
  a third correct precedent, currently unused at the defect site.
* `src/spreadsheet_handling/domain/finding_frame.py` -- the existing,
  reusable `Finding`/`findings_to_frame` diagnostics mechanism (Option B's
  carrier, if authorized).

==== Implementation slice 1 -- carrier defect fixed (2026-08-07)

*Scope of this slice:* eliminate the backend-divergent accidental
serialization of the unresolved helper's pandas missing carrier only. Does
*not* finalize the broader unresolved-reference semantic contract (see
"What remains open" below, and "Evaluated contract options" /
"Recommended direction" above, both still current).

*Normalization owner chosen:* `core/fk.py:apply_fk_helpers`
(`value_map.get(_norm_id(rid), "")`, now line ~346), the sole FK-helper
*value* materialization point -- confirmed single call site
(`domain/transformations/fk_helpers/enrich.py`), no other production caller.

Rejected alternatives, and why:

* *workbook/TableBlock projection* (`rendering/composer/layout_composer.py`
  `compose_workbook`) -- too broad. It would normalize *every* DataFrame
  cell reaching every table, not just FK-helper columns, silently changing
  behavior for any other current or future NaN-carrying payload cell (for
  example CSV-sourced missing values, which retain a real pandas NaN on the
  directory read path per the internal-value-model sketch). That is exactly
  the "arbitrary object-column values outside the owned visible-cell/
  carrier contract" this slice was told not to touch.
* *independent per-renderer emptiness-check patches* (fixing
  `io_backends/ods/odf_renderer.py:563`'s `value in (None, "")` in place,
  and/or hardening the XLSX renderer to match) -- duplicates the same
  "is this a missing carrier" predicate across two backend-specific files.
  That duplication is *how the divergence happened in the first place*
  (OpenPyXL's implicit NaN-to-`None` write behavior and the ODS renderer's
  hand-rolled check drifted independently). It also risks "ODS merely
  emulating an OpenPyXL accident" if the ODS predicate were patched to
  match XLSX's specific library-internal behavior rather than being
  correct on its own terms.

Fixing at `apply_fk_helpers` means neither renderer ever receives a raw NaN
for an FK-helper *lookup miss* -- both backends consume an already-decided,
ordinary Python string for that case. A successful lookup is unaffected by
this fix and still returns its stored target payload verbatim; if that
payload is itself a missing carrier, the column can still carry a raw NaN
into a renderer for that row (outside this slice; see "What remains open"
below). No renderer code was changed.

*Fix:* an unresolved lookup (missing source key, or a non-empty key with no
matching target row) now supplies an explicit `""` default to the
`dict.get` call, instead of relying on Python's implicit `None`. `None`
mixed into a pandas column assignment was silently coerced into a real
`float('nan')` carrier under the project's active string dtype -- confirmed
during revalidation (`type(value) is float`, `repr(value) == 'nan'`) -- and
that NaN was what let the two backend renderers diverge: OpenPyXL silently
drops a NaN-valued cell to blank on save (accidental library behavior);
the ODS renderer's NaN-blind `value in (None, "")` check falls through to
its numeric branch and writes the literal text `"nan"`. A lookup *miss* no
longer introduces a pandas missing carrier for a helper column, so neither
renderer has anything to mishandle for that miss. A successful lookup
(a dict hit) still returns its stored payload verbatim, including a stored
`None`, NumPy NaN, pandas `NA`, or another maintained missing carrier --
that case can still materialize a missing helper carrier and is outside
this slice's owned contract (precision correction per Implementation
Review 001, finding NAN-SEMANTICS-REVIEW-F1).

*Regression tests added:*

* Unit level, independent of the roundtrip layer
  (`tests/unit/domain/transformations/test_fk_helpers_enrich.py`, new
  `TestApplyFkHelpersUnresolvedCarrier`, exercising `apply_fk_helpers`
  directly): unresolved non-empty key; missing/`None` source key (kept
  separate, not conflated); literal-`"nan"`-target-value preservation on a
  resolved match; legitimate-empty-string-target-value preservation on a
  resolved match.
* Roundtrip level, parameterized XLSX/ODS
  (`tests/roundtrip/test_pathological_cell_contracts.py`):
  `test_unresolved_fk_helper_carrier_is_backend_consistent`,
  `test_missing_fk_source_helper_carrier_is_backend_consistent` (separate
  test, per the same missing-source-vs-unresolved-reference distinction),
  `test_literal_domain_nan_helper_value_survives_roundtrip`,
  `test_legitimate_empty_helper_value_survives_roundtrip`. All four are
  real, passing (non-xfail) regression tests, run through the public CLI
  entry point on both backends.

*Confirmed removed:* the ODS literal `"nan"` corruption. Pre-fix red
evidence (recorded in the checkpoint file): the two new ODS-parameterized
tests failed with `got 'nan'` for both the empty-source and the
non-empty-unresolved-key case; the two matching unit tests failed with
`['alpha', nan]`/`[None, None]` instead of `['alpha', '']`. Post-fix: all
green on both backends.

*Confirmed preserved:* a legitimate resolved helper value that is literally
`"nan"` round-trips unchanged as `"nan"` on both backends (the fix detects
the missing *carrier* via an explicit lookup-miss default, not the
spelling "nan" -- a resolved match is a dict hit, never the unresolved
default). A legitimate resolved helper value that is an empty string
round-trips unchanged as `""` on both backends (a dict hit returning the
real stored value, not the miss-default).

*What remains open:* `""` is only the current, temporary carrier value --
explicitly *not* elevated to an accepted final contract by this slice. It
is still known to collide with a legitimately resolved helper whose target
value is itself an empty string; the two are indistinguishable in
canonical JSON today. This is captured by a new xfail sentinel,
`test_unresolved_helper_value_distinguishable_from_legitimate_empty_string`,
which replaces the prior `test_missing_fk_key_helper_representation_
without_cleanup` xfail. The prior sentinel's own assertion
(`helper_value not in ("nan", "")`) is now trivially true post-fix (the
carrier defect it was guarding against is fixed) and no longer exercises
an open question, so it was retired rather than left in place as a
misleading always-green xfail; the still-open collision it never actually
tested is now the thing under xfail instead. The literal `"nan"` collision
this revalidation separately proved is *no longer live*: since every
unresolved case now uniformly produces `""` rather than sometimes
producing `"nan"` (on ODS), a legitimate resolved `"nan"` target value no
longer collides with anything.

A separate, narrower precision (Implementation Review 001, finding
NAN-SEMANTICS-REVIEW-F1): this slice normalizes only a *lookup miss*. A
*successful* lookup still returns its stored target payload verbatim --
if that stored payload is itself `None`, NumPy NaN, pandas `NA`, or another
maintained missing carrier, the helper column can still materialize a
missing helper carrier for that row, on either backend. That is not a
defect in this slice; it is simply outside the slice's owned contract (the
slice fixes the previously implicit value of a *miss*, it does not define
or normalize legitimate target payloads). The maintained
`TestMissingLabelField::test_missing_label_field_results_in_none_helper`
(`tests/unit/domain/transformations/test_fk_edge_cases.py`) exercises and
preserves exactly this successful-hit case.

The final representation decision (JSON `null` off the spreadsheet-cell
path, a reserved sentinel string on it, a `derived_column_findings`
extension as an additive diagnostic, or another shape -- see "Evaluated
contract options" and "Recommended direction" above, both unchanged by
this slice) remains the next step, to be finalized within this same BUG.

*Files changed this slice:*

* `src/spreadsheet_handling/core/fk.py` -- production fix.
* `tests/unit/domain/transformations/test_fk_helpers_enrich.py` -- new unit
  regression tests.
* `tests/roundtrip/test_pathological_cell_contracts.py` -- new
  backend-parameterized regression tests; retired/replaced xfail sentinel.
* `tests/roundtrip/conftest.py` -- `_forward_pipeline` gained an optional
  `kind` parameter (default `"xlsx"`, fully backward compatible) so the new
  tests can drive the ODS backend without duplicating pipeline plumbing.

==== Related backlog items

* `BUG-HELPER-CLEANUP-CONTRACT-P4A` -- the helper-leak twin. *Done*, archived
  to `docs/cold_storage/backlog/ftrs_done/`. Governs whether helpers reach
  canonical JSON at all; this BUG governs their value when they legitimately
  do.
* `FTR-VISIBLE-CELL-TYPING-STRATEGY-P5` -- closed by
  `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` (accepted); the visible-cell
  string substrate this BUG's eventual sentinel must respect.
* `FTR-FK-HELPER-POLICY-LIFECYCLE-P4A` -- still *New*; the likely eventual
  host for durable contract documentation of the `helper_policies.fk`
  surface.
* `FTR-ROUNDTRIP-TEST-LAYER-P4A` -- owns the regression-test home
  (`tests/roundtrip/test_pathological_cell_contracts.py`).
* `FTR-PAYLOAD-SEMANTICS-RECOVERY-P5` -- exploratory/New; provides the
  general classify/recover/pin framing this BUG's decision would be an
  instance of, and states a recovery preference consistent with the
  recommended direction above. Does not decide this BUG.
* `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` / `DEC-TRUSTED-RUNTIME-AND-
  POLICY-AUTHORITY` -- names this BUG as its explicit non-goal / next
  dependency step.
* `CONC-DOMAIN-BOUNDARY-ROBUSTNESS` -- project-memory concern whose
  `next_action` names this BUG.
* `roadmap_domain_hardening_sequence.adoc` Phase C -- sequencing context;
  this BUG is the named "known blocking contract."
