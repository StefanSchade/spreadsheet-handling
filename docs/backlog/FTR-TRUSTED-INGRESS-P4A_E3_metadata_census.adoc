= Pre-E3 Metadata Node-Shape Census -- Trusted Ingress

*Census date:* 2026-08-13

*Census type:* Fresh-context, evidence-gathering-only census of the E3
precondition in `FTR-TRUSTED-INGRESS-P4A.adoc` section 23/E3 and section 11
("Metadata contract"). This artifact does not implement E3, does not modify
runtime or tests (beyond read-only probes), does not redesign the metadata
registry, does not implement schema validation, and does not change roadmap,
Project Memory, the Semantic/Technical Model, or the Developer Guide. It
authorizes nothing by itself -- see <<independent-review>>.

*Baseline:* `a42fdbe19e1bf62405c250cc2485c2df2448967f`
(`docs(project_memory): curate Trusted Ingress E2 acceptance`), branch `main`,
clean worktree. Ancestry: E2 independently accepted at `d43de54e2b5e6781289b73c59f5a5368e37d93fe`;
roadmap marks E2 accepted at `291cec5b87da481ce92b72c1b7e8f0c8386646d6`. E3-E7
are unstarted -- no commit under `git log --oneline --all` introduces E3
runtime behavior, and `domain/ingress/structural_admission.py` (E2's own
admission module) states in its own docstring: "Reserved `_meta` content
remains opaque here for E3."

*Method:* combined re-reading of the accepted FTR contract and its review
dispositions, targeted reading of every named prior meta-FTR/registry
document, and five parallel read-only source/test traces (external ingress,
runtime-created/projected metadata, consumers, repository-wide counterexample
search, aliasing/cycles/delegates/guards). Grep was cross-checked against
direct file reads; the most consequential claims (Legend Blocks' `isinstance`
checks, `admit_ordinary_frames` caller count, `persistence_boundary.py`'s
`isinstance` gate) were independently re-verified line-by-line for this
artifact rather than trusted at face value.

[[independent-review]]
This census does not self-authorize E3. A fresh-context independent review
(planned separately as
`FTR-TRUSTED-INGRESS-P4A_E3_metadata_census_review.adoc`) must falsify it
before E3 implementation begins.

*Correction status (2026-08-13):* correction baseline `a4284c09069bf0aaaaff43f9e5fe28d56c739fb4`,
clean `main`, with E3-E7 unstarted. The independent review at that commit
rejected the original compatibility verdict through
`TRUSTED-INGRESS-E3-CENSUS-REVIEW-F1` and `-F2`. Section 17 records the narrow
correction-author disposition. Earlier factual statements are corrected where
necessary; section 14 preserves the original conclusion only as superseded
history. E3 remains blocked pending fresh-context follow-up review of this
correction.

*Second correction status (2026-08-13):* the independent follow-up review at
`ce37fd494e1b824f0a66f55328dac50f0d15d296`
(`docs/backlog/FTR-TRUSTED-INGRESS-P4A_E3_metadata_census_review.adoc` section
13) independently resolved `TRUSTED-INGRESS-E3-CENSUS-REVIEW-F1`/`-F2` and
raised exactly one correction-induced finding,
`TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1`: section 17.4's `ordinary dict keys:
exact str` clause lacked the compatibility investigation given to value
shapes. Section 18 records the narrow correction-author disposition of that
finding. It does not reopen sections 1-17. E3 remains blocked pending
fresh-context follow-up review of this second correction.

*Third correction status (2026-08-14):* the independent final follow-up
review (`docs/backlog/FTR-TRUSTED-INGRESS-P4A_E3_metadata_census_review.adoc`
section 14) found `TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1` *not resolved*:
section 18's "no maintained producer relies on a non-`str` key" claim is
falsified by the maintained public `contract_xref` dense-axis path, which
writes an E2-admitted numeric physical column label as an exact `int` `_meta`
dictionary key. Section 19 records the narrow correction-author disposition
of that specific counterexample. It does not reopen sections 1-18's other
conclusions. E3 remains blocked pending fresh-context follow-up review of
this third correction.

== 1. Exact question and preferred hypothesis under test

Per FTR section 23/E3 precondition and section 11: before E3 may treat the
`_meta` substrate as an owned, bounded, acyclic framework structure, an
independently accepted focused census must confirm or replace this preferred
hypothesis:

....
root:                    exact dict
recursive ordinary containers: exact dict, exact list
ordinary leaves:         E1-admitted Scalar (str | bool | int | float |
                         datetime.date | datetime.datetime | None, plus
                         admitted numpy/pandas carrier variants -- see
                         core/scalar_values.py:103-150)
exceptional semantics:   only explicitly named and governed delegates
....

The census must answer: what metadata node shapes must current supported
behavior admit at the E3 substrate boundary -- confirming this hypothesis, or
replacing it with a bounded alternative plus named migrations.

== 2. Reused prior evidence

None of the named prior meta-FTR/registry documents contain direct evidence
of Python node shapes at `_meta` paths. This was independently confirmed by
reading each one (not inferred from titles):

[cols="3,3,5",options="header"]
|===
|Document |Classification |What it actually provides

|`FTR-META-LIFECYCLE-INVENTORY-P5.adoc`
|Planning/backlog-only (Status: New, unexecuted)
|A *behavioral*-classification template (persisted/runtime-only/derived/
carrier/roundtrip-relevant axes). No node-shape enumeration; explicitly not
yet executed. Does not close this census's gap.

|`FTR-META-REGISTRY-P3H` / `-HARDENING-P3I` (+ reviews)
|Historical, effect still live in `registries/meta_registry.yaml`
|Established the `layer` (`meta_canonical`/`meta_rendering`) and
`classification` (`canonical_meta`/`derived_operational_view`/
`carrier_artifact`/`read_path_hint`) axes -- provenance/authority, not Python
type. `runtime_binding: forbidden` is asserted in the registry itself and by
`docs/technical_model/ch05_registries/meta_registry/01_meta_registry.adoc`.

|`FTR-META-FACET-CLASSIFICATION-P5.adoc`
|Planning/backlog-only (Status: New)
|Proposes an Intent/Resolution/Provenance facet system -- semantics, not
shape.

|`FTR-META-PERSISTENCE.adoc`
|Historical stub (Status: Done)
|Three lines; no exact Python shape grammar, but its generic acceptance that
metadata roundtrips is compatibility authority for values the maintained
sidecar currently writes and restores. It cannot select the permanent Domain
grammar by itself, but narrowing it requires an explicit migration.

|`FTR-META-SCHEMA-VALIDATION-P5.adoc`
|Planning/backlog-only (Status: Open)
|The only document that gestures at per-namespace "declared shape" -- as
*future, unimplemented* work, confirming no such field exists today.

|`FTR-META-CONTRACT-VERSIONING-P4A.adoc`
|Planning/backlog-only (Status: New)
|A whole-document `schema_version` marker proposal, not per-node shape.

|`registries/meta_registry.yaml` (34 entries, `registry_version: 2`)
|Manual review artifact, explicitly not runtime authority
|Exhaustive grep across all 34 entries for `type:`/`shape:`/`value_type:`/
`python_type:` returns zero matches. It records meaning, scope, origin,
persistence behavior, and producer/consumer references -- never a Python
container/leaf type.

|`FTR-META-CARRIER-BOUNDARY-GUARDS-P4.adoc` (Implemented)
|Historical, live effect
|A named-delegate-style exception precedent: `workbook_meta_blob` /
`carrier_artifact` (`GDR-META-CARRIER-001`). Semantic classification, not
Python-type evidence.
|===

*Conclusion:* the manual registry and every prior meta-FTR answer "what does
this metadata mean and who owns it," never "what Python type is it." This
census could not be answered from documents; it required direct source
tracing, consistent with FTR section 13's own warning that the registry
"does NOT by itself prove ... exact recursive substrate grammar."

== 3. Node-shape matrix

Grouped by structurally identical family. "Delegate?" marks whether the path
accepts a non-substrate *authoring* shape and normalizes it (FTR section 11's
delegate concept), as opposed to ordinary dict/list/scalar construction.

[cols="3,3,2,3,2,2,4",options="header"]
|===
|Path |Introduced by |Lifecycle role |Root/container shape |Leaf shapes |Delegate? |E3 consequence

|`_meta` root (XLSX/ODS, JSON-blob path)
|`openpyxl_parser.py`/`odf_parser.py` -> `workbook_projection.py`
|External
|exact `dict` (via `json.loads`, then `canonicalize_workbook_meta`)
|str/int/float/bool/None (JSON grammar)
|No
|Matches hypothesis directly.

|`_meta` root (XLSX/ODS, legacy `ast.literal_eval` blob fallback)
|`openpyxl_parser.py:149-154`, `odf_parser.py:856-862` (pre-`ADR-META-BLOB-SERIALIZATION` files)
|External (legacy read compatibility)
|`dict` (from Python-literal grammar, wider than JSON)
|str/int/float/bool/None, **and** historically writer-producible
tuple/set/bytes/complex
|No (compat fallback, not a delegate)
|Currently supported *read* path. The historical framework writer passed the
metadata dict to rendering and persisted `str(dict)`, so every listed literal
shape was mechanically writer-producible. The accepted serialization ADR also
promises legacy Python-literal workbooks remain readable. See section 17 for
the explicit migration disposition after carrier recovery.

|`_meta.yaml`/`_meta.yml` sidecar (JSON backend) and `--overrides` file
|`json_backend.py:210-215`, `yaml_overrides.py:36-48`
|External
|exact `dict` (root gated by `isinstance(meta, dict)`, `--overrides` raises
`ValueError` on non-dict root)
|Whatever `yaml.safe_load` constructs: str/int/float/bool/None/date/datetime
(in-vocabulary) plus **set** (`!!set`), **list-of-tuple** (`!!omap` and
`!!pairs`), **bytes** (`!!binary`) -- out-of-vocabulary
|No
|Set and bytes are not merely reachable: the maintained JSON-sidecar writer
emits their safe tags and its reader restores them exactly, so the broad
metadata-roundtrip acceptance applies even though no family feature gives them
semantic meaning. Omap/pairs remain parser-reachable only. Section 17 records
the intentional narrowing and its release/test consequence. E3 must validate
the full loaded tree, not just its root.

|`_meta.yaml`/`.yml` inside a `yaml_dir` input (no filename reservation yet)
|`yaml_backend.py:54-97` (`load_yaml_dir`, generic glob)
|External
|**`pd.DataFrame`**, not a dict at all
|n/a
|No
|This is precisely the "before" state the already-accepted FTR YAML decision
(Option A / F5, section 11) targets. E3 owns implementing the reserved-stem
loader conversion; this is not a new gap needing separate disposition.

|`_meta.csv` / `_meta.xml` foreign file (no filename reservation)
|`csv_backend.py:67-97`, `xml_backend.py:126-142`
|External
|**`pd.DataFrame`**, not a dict at all
|str only (XML/CSV have no native scalar typing)
|No
|Neither backend is designed to carry `_meta` (both explicitly skip it on
write). Not a supported feature; recommend a bounded follow-up bug to mirror
the YAML exclusion, not E3 scope (<<parked-concerns>>).

|Direct programmatic `_meta` (bare `run_pipeline`, or `orchestrate` with no
`legend_blocks` key present)
|Caller-constructed `frames["_meta"]`
|External
|**Unconstrained** -- any Python object
|Unconstrained
|No (no gate exists)
|`domain/ingress/structural_admission.py` explicitly documents `_meta` as
"deliberately not inspected until E3 owns that substrate," and its own
admission function (`admit_ordinary_frames`) has zero callers outside its own
unit test -- confirmed independently (`grep -rn admit_ordinary_frames`). This
is not a counterexample to the vocabulary; it is confirmation that no gate
exists yet, which is exactly E3's job.

|`_meta.legend_blocks`
|`domain/ingress/legend_blocks.py` (`normalize_legend_blocks_shape`)
|Runtime-created (ingress-boundary normalizer)
|Accepts `Mapping` **or** `list` of `Mapping` on input; always emits exact
`dict` on output
|Passthrough of block-internal values (unvalidated -- entry-value semantics
deferred to `FTR-LEGEND-BLOCKS-ENTRY-CONTRACT-P5`)
|**Yes -- the one confirmed current delegate-shaped path**
|Confirmed by the FTR text itself ("Legend Blocks migrates under those
rules"). See <<delegates>>.

|`_meta.workbook_view`, `.helper_policies`, `.constraints`, `.sparse_defaults`,
`.split_by_discriminator`, `.xref_crosstable`, `.compact_multiaxis`,
`.pipeline_cleanup`, `.derived.sheets[*].helper_columns`
|`workbook_views.py`, `helper_policies.py`, `fk_relations.py`,
`validate_columns.py`, `sparse_defaults.py`, `discriminator_split/metadata.py`,
`xref_crosstable/operation.py`, `compact_multiaxis.py`, `pipeline_cleanup.py`,
`fk_helpers/provenance.py`
|Runtime-created
|exact `dict` root, exact `dict`/`list` nesting, always freshly constructed
as dict/list literals
|str/int/float/bool/None only (every agent trace confirmed scalar-only
leaves; e.g. `provenance.py` explicitly extracts scalar fields rather than
storing source dataclass objects)
|No
|Matches hypothesis directly.

|`_meta` projected for persistence
|`pipeline/persistence_boundary.py:130-158` (`project_meta_to_persistable_contract`)
|Projected
|`dict` in, `dict` out (pass-through of unmatched keys **by reference**)
|unchanged
|No
|Matches hypothesis; see <<aliasing>> for the reference pass-through finding.

|`_meta` -> hidden-sheet carrier
|`rendering/composer/layout_composer.py:372-376` (`json.dumps(workbook_meta, ...)`)
|Projected (XLSX/ODS write)
|Hard boundary: **any non-JSON-serializable value raises `TypeError` here
today**
|JSON grammar only
|No
|This is the one real, already-enforced runtime gate confirming dict/list/
Scalar for anything that must survive an XLSX/ODS round-trip. It is a
downstream crash point, not a domain-level diagnostic -- an implementation
detail E3 should absorb, not new evidence against the hypothesis.

|`_meta` persisted via `yaml.safe_dump` (JSON backend's own `_meta.yaml`
sidecar write, and other YAML-adjacent sidecar writers)
|`json_backend.py:270`
|Projected
|`yaml.safe_dump` writes tuple as an ordinary sequence (which reloads as
`list`), writes set as `!!set`, and writes bytes as `!!binary`. Unsupported
objects such as complex, dataclass instances, Enum, and Path raise
`RepresenterError` (support for numpy scalars is type-specific and is not a
Domain contract).
|`SafeRepresenter`-supported types only (including `datetime.date`/
`datetime`, set, and bytes)
|No
|This serializer boundary is wider than the proposed Domain grammar and is
not itself grammar authority. Its maintained set/bytes roundtrip is real
compatibility evidence resolved explicitly in section 17.
|===

== 4. Substrate vs. family semantics (explicit separation)

Everything found under `docs/technical_model/ch05_registries/registry_semantics/`
(`01_data_and_meta.adoc`, `02_meta_layer.adoc`, `03_meta_classification.adoc`)
and the registry's `layer`/`classification` axes is *family-semantic*
(canonical vs. derived vs. operational-carrier; model vs. presentation vs.
workbook_identity) -- orthogonal to the substrate question this census
answers. None of it was centralized into this census's verdict. The one
family-semantic fact worth noting for scope hygiene: `_meta.protection`
straddles model/presentation semantics per `03_meta_classification.adoc`
section "Open questions" -- explicitly parked there already, not reopened
here.

== 5. Container subclasses -- exact vs. generic

*Producers:* zero custom `Mapping`/`Sequence` subclasses exist anywhere in
`src/spreadsheet_handling/` (`grep -rn "class .*(.*Mapping.*)"` /
`"class .*(.*Sequence.*)"`, excluding tests: zero hits). Every current
producer emits exact `dict`/`list`.

*Consumers:* essentially every subtree handler (`meta_update.py`'s ~15
handlers, `workbook_views.py`, `legend_blocks.py`, `persistence_boundary.py`)
gates with `isinstance(x, Mapping)`/`isinstance(x, Sequence)` -- generic ABC
checks, not `type(x) is dict`. `domain/schema_maintenance/meta_update.py`'s
`_is_sequence` (line 1036) explicitly tolerates any non-`str`/`bytes`
`Sequence`, including `tuple`, for `constraints`, `helper_columns`,
`fk.relations`, and several `workbook_view`/`lookup` fields.

This is consumer-side duck-typed *tolerance*, never producer-side *reliance*:
no current path constructs a genuine `Mapping`/`Sequence` subclass instance
and writes it into `_meta`. Per FTR section 4's own reachability principle
("a node shape that is technically constructible but has no supported
producer or consumer is not automatically part of the compatibility
contract"), this tolerance does not force the vocabulary open. E3 may
legitimately narrow to `type(x) is dict`/`type(x) is list` at ingress without
breaking any current producer -- it only tightens what a duck-typed
direct-Python caller could get away with today. One inconsistency is worth a
small parked follow-up: `domain/transformations/fk_helpers/policy.py:53`
(`resolve_v2_fk_relations`) requires `isinstance(relations, list)` **exactly**
for `_meta.helper_policies.fk.relations`, while `meta_update.py`'s validator
for the *same* path tolerates any `Sequence`. No producer ever creates a
tuple there, so this is dormant, not load-bearing (<<parked-concerns>>).

E2's own (currently unwired) admission function uses the strictest form
already -- `type(frames) is not dict` at the outer Frames level
(`structural_admission.py:117`) -- consistent with, and useful precedent
for, an exact-dict E3 boundary check.

[[aliasing]]
== 6. Aliasing and cycles

*Aliasing is the single most systemic finding of this census.* Every runtime
producer traced (`meta_bootstrap.deep_merge`, `yaml_overrides.apply_overrides`,
`legend_blocks._strip_resolved`, `workbook_views.configure_workbook_view`,
`persistence_boundary.project_meta_to_persistable_contract`,
`rendering/frame_selection.py:85-86`, `frame_keys.copy_reserved_frames`) uses a
shallow-copy/copy-along-the-mutated-path idiom: only the top-level dict or the
exact rewritten key path is freshly copied; sibling keys and untouched nested
containers retain caller-visible aliases. Two places pass `_meta` through by
direct reference with no copying (`frame_selection.py:85-86`,
`frame_keys.py:21-25`). This is implementation and compatibility evidence,
confirmed independently for Legend Blocks by executing it against HEAD:

....
entries = [{'token': 'A'}]
frames = {'_meta': {'legend_blocks': {'b': {'entries': entries, 'title': 'B'}}}}
out = normalize_legend_blocks_shape(frames)
out['_meta']['legend_blocks']['b']['entries'] is entries   # True
....

The original census incorrectly inferred blanket deep-copy permission from an
incomplete identity-test search. Maintained failure-atomicity tests in
`tests/integration/pipeline/test_domain_ingress_boundaries.py` explicitly pin
the identities of `_meta`, `sheets`, sheet metadata, `options`, and nested
`column_widths` after a failed staged operation. Other producer tests pin
untouched sibling aliases. More importantly, tests are not the whole contract:
the accepted F7 disposition explicitly excludes deep-copy ownership, alias
tracking, retained-alias isolation, concurrent isolation, and later-mutation
guarantees.

The E3 consequence is therefore precise. Ingress and governed delegates build
and validate staged candidates without mutating caller-owned input through
their own work, copy along paths they normalize or rewrite, and publish only
after successful validation. They need not recursively deep-copy every
accepted container, establish globally unique container identity, or isolate
retained aliases outside rewritten paths. Acyclic shared aliases are permitted.

*Cycles:* no maintained metadata producer constructs a self-referential or
mutually cyclic `_meta`; cycle rejection is the already-accepted acyclic E3
contract. It is not alias rejection. Traversal reaches a cycle only when an
object is encountered while that same object is active on the current ancestor
path. Encountering the same acyclic child again after another path has
completed is a shared alias and must not be rejected merely because it was
globally "seen" before. E3 owes cycle detection, not alias detection or
copying.

[[delegates]]
== 7. Delegate findings

Reconstructing FTR section 11's "Controlled metadata delegate governance"
against current code:

[cols="2,5",options="header"]
|===
|Governance requirement |Current Legend Blocks reality

|Centrally reviewed/owned; no dynamic family registration
|Sole entry in `INGRESS_RULES` (`domain/ingress/coordinator.py:46-48`);
not dynamically extensible. Compliant.

|Selected by static root contract, not caller-provided executable identity
|Selected by the literal key `"legend_blocks"`. Compliant.

|Staged candidate only, no caller-input mutation
|Docstring and code confirm: "never mutates the caller's `frames`, `_meta`,
... or any block mapping." Compliant.

|No unsafe repr/str/truthiness/equality/hashing on a *rejected* value
|Violated in spirit on *accepted* identity resolution: `_resolve_identity`
(line 103) uses `str(item.get("name") or item.get("id") or f"legend_{position}")`
-- truthiness and `str()` coercion on authored values, exactly as the FTR
text (line 1471) already flags for migration.

|Returns common substrate or named root contract; output checked before publication
|Output is always exact `dict`; entry-value contents are *not* validated
(deferred to `FTR-LEGEND-BLOCKS-ENTRY-CONTRACT-P5` by the module's own
docstring). The current implementation is therefore not yet compliant with
E3's complete recursive post-check; deferring family meaning does not defer
the common node grammar.

|Never bypasses the global bounded/acyclic postcondition
|Current nested values pass through without a common recursive check. E3 must
post-check the delegate's complete output, including every nested value and
cycle edge; retained acyclic aliases remain permitted. Delegate status cannot
preserve a carrier value that section 17 migrates/rejects.

|Owns authoring shape only, not family semantics
|Compliant -- entry-value semantics explicitly out of scope here.
|===

**Verdict: Legend Blocks is the sole current path matching the delegate
definition, and it is real, current, tested, and relied upon** (Mapping/list
polymorphism on input, aliasing of nested `entries`/`placement`). It is
**not yet a formally governed delegate** -- the word "delegate" appears
nowhere in `registries/meta_registry.yaml`, and the FTR's own text says so
directly: "The current Legend Blocks normalizer is compatibility evidence
for the authoring form, not an automatically approved delegate
implementation." This is expected, in-scope E3 work, not a census finding
that requires revising the vocabulary, provided its complete output is checked
against the common bounded substrate before publication.

No other current subtree qualifies as a delegate. `workbook_views.py`,
`meta_update.py`, `helper_policies.py`, `fk_relations.py`, and
`validate_columns.py` are either canonical-shape *builder* APIs (construct
the substrate shape from scratch, driven by explicit Python kwargs) or
*validate-and-reject* schema-maintenance code -- none of them read an
existing `_meta` subtree in an alternate authoring shape and normalize it.

== 8. YAML reserved metadata -- node-shape consequence

Current (pre-E3) behavior, confirmed independently: `yaml_backend.py`'s
`load_yaml_dir` globs `*.yml`/`*.yaml` generically with no exclusion for a
file stemmed `_meta`; such a file is parsed like any other sheet and lands
in `frames["_meta"]` as a `pd.DataFrame`. `save_yaml_dir` already skips
writing `_meta` (asymmetric convention, no round-trip promise). Duplicate
`.yaml`/`.yml` spellings are not de-duplicated on read; whichever the glob
processes last silently wins with no error.

This matches, node-shape-for-node-shape, the "before" state the FTR's
already-accepted Option A/F5 decision targets (reserve stem `_meta`,
safe-load exactly one file as a mapping, reject non-mapping/duplicate
spellings at the backend boundary, never reinterpret a DataFrame as
metadata from Domain ingress). **E3's consequence is to actually implement
that already-decided loader behavior; this census surfaces no new
YAML-shape question beyond what section 11 of the FTR already resolved.**
The one addition this census contributes: once the sidecar is safe-loaded as
a mapping, E3's substrate validation must still walk that mapping's *values*
(not just check the root). `yaml.safe_load` can produce `set` (`!!set`),
`list[tuple]` (`!!omap` and `!!pairs`), and `bytes` (`!!binary`) that the
root-is-mapping check alone would not catch. The structured JSON-sidecar writer
also currently emits set and bytes; section 17 distinguishes those
compatibility-relevant cases from syntax that is merely safe-load reachable.

== 9. Bootstrap / override / post-load paths

[cols="2,3,3,3",options="header"]
|===
|Path |Before shape |Introduced/merged shape |E3 re-establishment point

|`meta_bootstrap.bootstrap_meta`
|Parsed `_meta` (dict) or absent (`{}`)
|`deep_merge`-recursive dict merge of persisted/profile/CLI meta (dict-only
recursion; non-dict overlay values replace outright, never element-merged)
|Calls `domain_ingress.run_domain_ingress` post-merge (line ~74-79) --
currently only the `legend_blocks` rule fires; this is the FTR's own
"re-establish affected metadata" point for E3 to extend.

|`yaml_overrides.apply_overrides`
|Parsed `_meta` (dict) or absent
|Same `deep_merge` primitive against the `--overrides` YAML file's
`defaults`/`sheets` mapping (root already rejected if non-dict, line 46-47)
|Same `run_domain_ingress` call (line 93) -- same current/target gap as above.

|`run_domain_ingress` (Legend Blocks rule, `coordinator.py`)
|Whatever `_meta.legend_blocks` currently is (or absent)
|Normalizes list-or-Mapping to canonical dict (see <<delegates>>)
|This *is* the currently-existing re-establishment point; E3 generalizes it.
|===

Both bootstrap and override paths build merged candidates via a pure
function returning a new top-level dict (never mutating the caller's frames
in place at the outer level -- consistent with FTR section 11's "Bootstrap
and YAML overrides MUST build merged candidates without mutating the
published Frames graph"), then re-run the one existing ingress rule before
publishing. This structurally matches what E3 needs to generalize; no
counterexample was found in this category beyond the aliasing pattern
already recorded in <<aliasing>>.

== 10. Manual registry relationship (explicit)

`registries/meta_registry.yaml` and its Technical Model description
contribute: known namespaces/families, producer/consumer code references
(independently re-resolvable via `importlib`, as `test_meta_registry_hardening.py`
does), persistence-behavior prose, and layer/classification provenance
hints. It does **not** prove: complete runtime reachability, exact Python
node carriers at any lifecycle stage, absence of unregistered runtime-created
metadata, the recursive substrate grammar, or safe trust-boundary
classification behavior -- confirmed directly (zero `type`/`shape`/
`value_type` fields anywhere in the 34-entry document) rather than assumed.
E3 runtime behavior is not bound to the registry by this census, consistent
with FTR section 11's own statement that `meta_registry.yaml` "MUST NOT
become runtime authority without separate architecture."

== 11. Lifecycle inventory relationship

`FTR-META-LIFECYCLE-INVENTORY-P5` does *not* already establish this census's
answer. It is `Status: New`, unexecuted, and its own acceptance criteria
target *behavioral* classification (persisted-by-default, runtime-only,
user-authored, derived, carrier-specific, roundtrip-relevant, reconstructed
on read, consumed-by) -- not Python node shapes. Its scope is a template
precedent for a lifecycle census, not evidence this census could reuse
verbatim. No portion of it substitutes for the source-level tracing this
artifact performed; the gap is fully load-bearing and was closed here
directly against `src/spreadsheet_handling/`.

== 12. Architecture guards -- what they actually enforce

[cols="3,5",options="header"]
|===
|Guard |Actual scope (confirmed by reading, not by name)

|`tests/architecture/semantic_invariants/test_meta_registry*.py` (3 files)
|Validate only `registries/meta_registry.yaml`'s own document structure and
code-reference resolvability (via `importlib`). Zero runtime `_meta`
node-shape assertions.

|`tests/architecture/semantic_invariants/test_meta_carrier_boundary_guards.py`
|Registry-classification checks for known carrier keys, plus one real
runtime check: `project_meta_to_persistable_contract` prunes Resolution
facets on a captured fixture. Scoped to legend_blocks/xref_crosstable, not a
general shape/cycle guard.

|`tests/architecture/parse_contract/test_spreadsheet_parse_contract.py`
|Closest thing to a "parser canonical-write guard": asserts XLSX round-trip
`_meta` equals the canonical payload and excludes carrier-hint keys. Scoped
to named keys, not a generic substrate contract.

|`domain/ingress/structural_admission.py`
|E2's own module. `_meta` is recognized by key name and explicitly skipped
(`continue`) at line 133-134; never reaches cell-level admission. Confirmed:
"Reserved `_meta` content remains opaque here for E3" (docstring). This *is*
the deliberate E2/E3 seam, matching the independent E2 review's own finding.
|===

No existing guard enforces the dict/list/Scalar/acyclic/owned substrate
contract generically. This is expected -- it is exactly what E3 is scoped to
build -- and is not itself a compatibility finding against the vocabulary.

[[parked-concerns]]
== 13. Parked concerns (explicitly out of E3 scope)

* CSV/XML backends' missing filename exclusion for a foreign `_meta.csv`/
`_meta.xml` file (produces a `pd.DataFrame` root instead of a dict, mirroring
the pre-E3 YAML-dir gap). Neither backend is designed to carry `_meta`
(both explicitly skip it on write already). Recommend a small bounded
follow-up bug extending the same filename-reservation treatment the FTR
already decided for YAML, once E3 lands -- not itself E3 scope.
* Cross-consumer tolerance inconsistency for `_meta.helper_policies.fk.relations`
and `_meta.sheets.*.helper_columns`: `meta_update.py`'s `_is_sequence`
tolerates any non-`str` `Sequence` (including `tuple`); `fk_helpers/policy.py:53`
requires `isinstance(..., list)` exactly for the same field.
`reference_validations.py:626` explicitly accepts `(list, tuple)` on read.
No producer ever creates a tuple at these paths, so this is dormant, not
load-bearing for the census verdict. Recommend tracking under existing
carrier-helper consolidation work (`FTR-CARRIER-LEGEND-META-HELPERS-CONSOLIDATION-P4A`)
or a narrow follow-up bug, not E3.
* `FTR-META-SCHEMA-VALIDATION-P5` and `FTR-META-CONTRACT-VERSIONING-P4A`
remain independently valid future work (family-semantic schema validation
and whole-document version tagging respectively); this census does not pull
either forward.

== 14. Original compatibility verdict (superseded)

The census originally concluded `PREFERRED VOCABULARY CONFIRMED`. Independent
review rejected that conclusion as implementation-authorizing because it
missed current/historical writer-produced special values and overstated
deep-copy ownership. That original verdict is superseded in full by the
explicit compatibility and ownership dispositions in section 17. It must not
be used to authorize E3 on its own.

== 15. Lifecycle wording drift (recorded, not corrected)

Current roadmap and Project Memory point directly to "IMPLEMENT PHASE-E E3
METADATA SUBSTRATE ADMISSION." Per FTR section 23's explicit precondition,
that next-action wording remains conservatively over-eager: this corrected
census still requires fresh-context follow-up review. This is recorded here
only; roadmap and Project Memory are intentionally unchanged and may be
aligned, if appropriate, only after that review accepts the correction.

== 16. Original-census validation record

This table records the validation performed for the original `167ba29`
census. Correction-specific validation is recorded in section 17.6.

[cols="3,5",options="header"]
|===
|Check |Result

|`git status --short` (before and after this artifact)
|At original authoring time, clean except the new census artifact.

|`git diff --check`
|No whitespace errors.

|Asciidoctor render of this artifact
|Renders without ERROR-level messages (see commit for exact invocation/output).

|Relevant existing tests (read-only characterization, not modified)
|`tests/architecture/semantic_invariants/test_meta_registry.py`,
`test_meta_registry_hardening.py`, `test_meta_registry_contract_lifecycle.py`,
`test_meta_carrier_boundary_guards.py`; `tests/architecture/parse_contract/test_spreadsheet_parse_contract.py`;
`tests/unit/domain/ingress/test_legend_blocks_ingress.py`;
`tests/unit/domain/ingress/test_structural_admission.py` -- inspected as
evidence, not modified. The original inference drawn from the incomplete
identity-test search is corrected in sections 6 and 17.5.
|===

No production code, test, roadmap, Project Memory, Semantic/Technical Model,
Developer Guide, or registry file was modified by this census.

== 17. Correction disposition after independent review

This section is the correction-author response to the independent review at
`a4284c0`. It changes only the two material points raised there: special-value
compatibility (F1) and ownership/alias semantics (F2). All parked concerns in
section 13 remain parked. This correction does not claim independent
acceptance and does not authorize E3 before fresh-context follow-up review.

=== 17.1 Compatibility authority and decision principle

The corrected census distinguishes four levels of evidence that the original
text conflated:

* *mechanically writer-producible:* a serializer happened to accept the Python
  value;
* *maintained roundtrip-supported:* the maintained carrier's broad acceptance
  promises metadata roundtrips through its writer and reader;
* *intentionally semantic:* a maintained metadata family, public contract,
  example, or consumer assigns stable meaning to the value category; and
* *historically compatibility-promised:* an accepted decision preserves read
  compatibility for an older framework-produced carrier.

The Domain vocabulary is selected by maintained semantic/support contracts,
not by the accidental union of PyYAML and `ast.literal_eval`. Conversely, a
published generic roundtrip or legacy-read promise is real compatibility and
cannot be narrowed silently merely because no family-specific producer or test
uses a value.

Current sidecar set and bytes have a combination of broad carrier-roundtrip
authority and incidental serializer capability (levels two and one), but no
intentionally semantic metadata authority was found. The legacy special
literals have historical readability authority and were mechanically
writer-producible, but likewise have no family-semantic contract. The accepted
`ADR-META-BLOB-SERIALIZATION` promise means the retained reader recovers the
legacy metadata dict and its literal values; it is stronger than byte-level
carrier parseability. It does not require every Python-literal value to remain
in the canonical Domain grammar forever. Narrowing that later contract is an
explicit compatibility migration with a release consequence, selected here
under FTR section 11's pre-E3 evidence gate.

No generic normalization is authorized. In particular, set-to-list,
tuple-to-list, bytes-to-string, and complex-to-string/pair/object conversions
are not semantics-preserving without a family-specific contract. The
correction chooses targeted rejection/migration instead of adding accidental
serializer categories to the common grammar or to E1 Scalar.

=== 17.2 Exact F1 per-shape disposition

[cols="2,3,4,3,5",options="header"]
|===
|Shape |Current/legacy path |Compatibility authority |Disposition |E3 consequence

|`set`
|Current JSON-backend `_meta.yaml` sidecar write/read; also safe-loadable from
YAML-authored metadata and overrides
|`yaml.safe_dump` emits `!!set` and `safe_load` restores exact `set`.
`FTR-META-PERSISTENCE` and its maintained integration tests publish a broad
end-to-end metadata-roundtrip contract, although no test fixture or metadata
family assigns set semantics.
|*MIGRATE / NARROW*
|After safe carrier decode, E3 rejects the set node at its exact metadata path
before Domain publication. The diagnostic identifies structured-sidecar/YAML
origin, safe path, and concrete node kind without value `repr`/`str`. Release
notes identify the former PyYAML roundtrip and require an application-specific
rewrite; E3 tests pin atomic rejection.

|`bytes`
|Current JSON-backend `_meta.yaml` sidecar write/read; also `!!binary`
safe-loadable from YAML-authored metadata and overrides
|`yaml.safe_dump` emits `!!binary` and `safe_load` restores exact `bytes`.
The same broad maintained metadata-roundtrip contract applies, but no family,
consumer, example, or documented API assigns binary metadata semantics.
|*MIGRATE / NARROW*
|After safe carrier decode, E3 rejects the bytes node at its exact metadata
path before publication, with the same stable origin/path/kind diagnostic,
release note, application-specific rewrite guidance, and atomic test
obligation. Bytes is not added to E1 Scalar.

|`tuple`
|Legacy XLSX/ODS `workbook_meta_blob` produced before JSON serialization and
read today through the retained `ast.literal_eval` fallback
|The historical writer passed the metadata dict into rendering and the render
plan persisted `str(dict)`, so tuple was mechanically writer-producible. The
accepted serialization ADR promises those legacy Python-literal workbooks
remain readable. No family/test/example establishes tuple semantics. Current
YAML safe-dump is different: it writes a tuple as a sequence that reloads as
`list`, not a tuple roundtrip.
|*LEGACY MIGRATION / TARGETED REJECTION*
|The legacy parser still succeeds and recovers the dict. E3 then rejects the
tuple at its exact path with a deterministic legacy-compatibility diagnostic
before publication. Release notes direct users to a prior compatible version
or a meaning-aware manual rewrite; XLSX and ODS legacy-path tests are required.

|`set`
|Legacy XLSX/ODS Python-literal `workbook_meta_blob`
|Mechanically emitted by the historical `str(dict)` writer and restored by
the current fallback; covered by the ADR's legacy-read promise, without a
family-semantic contract.
|*LEGACY MIGRATION / TARGETED REJECTION*
|Carrier parse and root recovery remain available; E3 rejects the precise
legacy node before publication with stable path/type/origin diagnostics,
release treatment, and XLSX/ODS coverage. No unordered-to-ordered conversion
is implied.

|`bytes`
|Legacy XLSX/ODS Python-literal `workbook_meta_blob`
|Mechanically emitted by the historical `str(dict)` writer and restored by
the current fallback; covered by the ADR's legacy-read promise, without a
binary metadata semantic contract.
|*LEGACY MIGRATION / TARGETED REJECTION*
|Carrier parse and root recovery remain available; E3 rejects the precise
legacy node before publication with stable path/type/origin diagnostics,
release treatment, and XLSX/ODS coverage. Bytes is not promoted into ordinary
cell Scalar semantics.

|`complex`
|Legacy XLSX/ODS Python-literal `workbook_meta_blob`
|Mechanically emitted by the historical `str(dict)` writer and restored by
the current fallback; covered by the ADR's legacy-read promise. Current JSON
and safe YAML writers do not provide a corresponding maintained write path,
and no metadata family assigns complex-number semantics.
|*LEGACY MIGRATION / TARGETED REJECTION*
|Carrier parse and root recovery remain available; E3 rejects the precise
legacy node before publication with stable path/type/origin diagnostics,
release treatment, and XLSX/ODS coverage. No string or pair conversion is
authorized.
|===

These are E3-owned targeted migrations; no pre-E3 runtime guard is required.
Behavior remains unchanged until the corrected census receives independent
acceptance and E3 implements the boundary. E3 acceptance must characterize the
old writer/read paths, then prove deterministic late-path rejection, unchanged
caller input, no partial publication, and useful migration diagnostics for
each listed shape. Valid common-grammar metadata must continue to roundtrip.

=== 17.3 YAML safe-load inventory

[cols="2,3,3,4",options="header"]
|===
|YAML form |Constructed node |Evidence class |E3 disposition

|`!!set`
|exact `set`
|Current JSON-sidecar writer-produced and exactly restored; also safe-load
reachable from authored YAML
|Compatibility migration in section 17.2; not admitted to the Domain grammar.

|`!!binary`
|exact `bytes`
|Current JSON-sidecar writer-produced and exactly restored; also safe-load
reachable from authored YAML
|Compatibility migration in section 17.2; not E1 Scalar.

|`!!omap`
|exact `list` containing tuple pairs
|Safe-load reachable; no current framework writer, maintained fixture,
documented metadata promise, or consumer relies on this tagged form
|Rejected by the ordinary recursive post-check when it reaches the tuple.

|`!!pairs`
|exact `list` containing tuple pairs
|Safe-load reachable under the same evidence standard as `!!omap`
|Rejected by the ordinary recursive post-check when it reaches the tuple.

|YAML timestamp resolving to `datetime.date`
|E1-admitted Scalar
|Safe-load reachable and safe-dump supported
|Admitted as an ordinary metadata leaf under the common grammar.

|YAML timestamp resolving to `datetime.datetime`
|E1-admitted Scalar
|Safe-load reachable and safe-dump supported
|Admitted as an ordinary metadata leaf under the common grammar.
|===

Generic YAML syntax does not define Domain metadata grammar. Omap/pairs remain
reachable-but-unsupported, while current set/bytes receive explicit migration
rather than being mislabeled merely unreachable. Aliases and cycles are
separate graph-topology questions resolved below.

=== 17.4 Corrected common E3 substrate grammar

After the explicit migrations above, the proposed common grammar is:

....
`_meta` root:
    exact dict

recursive ordinary containers:
    exact dict
    exact list

ordinary dict keys:
    exact str -- explicit compatibility disposition in section 18

ordinary leaves:
    E1-admitted Scalar

aliases:
    acyclic shared aliases permitted

cycles:
    rejected

delegates:
    only statically governed named delegates;
    complete output subject to the common recursive post-check

special historical/carrier values:
    handled by the exact migration/rejection rules in section 17.2;
    not silently admitted into the common grammar
....

The same grammar applies to direct programmatic `_meta`. Python's ability to
pass an arbitrary object is not a public promise of arbitrary Domain node
support; nevertheless, values entering through the current maintained
sidecar/legacy contracts receive the explicit compatibility treatment above.
This establishes E3's bounded substrate contract without pre-empting E6's
later publication of programmatic entry guarantees.

Legend Blocks remains the named delegate migration only if its *complete*
normalized output is recursively checked against this common grammar before
publication. Its currently unvalidated nested values cannot become an escape
hatch: set, bytes, legacy literals, cycles, or any other out-of-substrate node
must receive the same rejection as at an ordinary metadata path. Acyclic
aliases retained outside rewritten paths remain valid.

=== 17.5 F2 ownership and graph-topology correction

The phrase "owned bounded acyclic substrate" means framework authority and
responsibility for validation, normalization, staged publication, and
sanctioned mutation. It does *not* mean recursively unique Python-object
ownership.

Trusted Ingress establishes:

* staged candidate construction and validation;
* no mutation of caller-owned input through ingress/delegate work;
* publication only after successful validation;
* copy-along-the-mutated-path where normalization or rewrite requires it; and
* an acyclic bounded substrate.

Trusted Ingress does not establish:

* complete recursive physical ownership or mandatory blanket deep copying;
* retained-alias isolation or alias tracking;
* globally unique container identity;
* concurrent isolation or protection from later out-of-band writes; or
* rollback of prior/plugin/external side effects.

A shared alias is the same acyclic child reachable through multiple completed,
non-ancestor paths. It is permitted and does not require detection or copying.
A cycle exists when traversal reaches an object already active on the current
ancestor path. E3 must reject the latter without using a global "seen once"
rule that would reject the former. This contract supplies no alias-isolation
guarantee.

In framework-managed normal execution, a parsed payload is logically handed
to Domain processing; no supported execution model assumes an independent
concurrent owner mutates the same conformant graph out of band. Mandatory deep
copy would add cost and complexity without serving that model. Programmatic or
library callers may retain references, but must not mutate conformant state
through those aliases out of band while the framework operates on it. A caller
that needs stronger isolation must establish it itself. Permanent explanatory
treatment belongs to the post-Phase-E Technical Model/Developer Guide
condensation, not this bounded correction.

=== 17.6 Correction validation

The correction re-read the accepted metadata-persistence FTR, the maintained
roundtrip tests, `ADR-META-BLOB-SERIALIZATION`, repository history immediately
before `bd27b57`, current JSON/XLSX/ODS readers and writers, the Phase-E E3
contract, the accepted F4/F7 dispositions, and maintained nested-identity
tests. Read-only probes reproduced:

* current JSON-sidecar `set`/`bytes` emission as `!!set`/`!!binary`, exact
  restoration, and whole-metadata equality;
* current XLSX fallback restoration of legacy tuple/set/bytes/complex;
* current ODS fallback restoration of the same literal values; and
* `!!omap` and `!!pairs` construction as exact `list` containing tuple pairs.

The focused persistence, carrier-boundary, parse-contract, Legend Blocks,
bootstrap/override, persistence-projection, and ingress-boundary suite passed:
128 tests, with no test modification. Asciidoctor rendered the corrected
artifact without an error, and `git diff --check` passed. The independent
review artifact, runtime/tests, roadmap, Project Memory, mega-FTR,
mega-review, registry, and all other documentation remained unchanged.

=== 17.7 Finding dispositions and revised verdict

`TRUSTED-INGRESS-E3-CENSUS-REVIEW-F1`::
    *correction-author RESOLVED.* Current sidecar set/bytes and legacy
    tuple/set/bytes/complex now have explicit compatibility authority,
    migration boundaries, diagnostics, release consequences, and test
    obligations. None is silently admitted or silently rejected.

`TRUSTED-INGRESS-E3-CENSUS-REVIEW-F2`::
    *correction-author RESOLVED.* Blanket deep-copy/unique-identity implications
    are withdrawn. The accepted F7 staged-publication, caller-non-mutation,
    retained-alias, and ancestor-path cycle model is restored.

The YAML reserved-stem Option A/F5 migration remains sound and unchanged.
CSV/XML `_meta` filename handling, dormant FK/helper tuple tolerance, schema
validation, contract versioning, registry redesign, and parser resource limits
remain independently parked and non-blocking.

This is a corrected census-author verdict, not independent acceptance. E3
remains blocked until a fresh-context follow-up review accepts the evidence,
the per-shape migrations, the common grammar, and the restored ownership model.

*PREFERRED BOUNDED SUBSTRATE CONFIRMED AFTER EXPLICIT COMPATIBILITY
DISPOSITION*

== 18. Metadata key-grammar correction (FOLLOWUP-F1 disposition)

This section is the correction-author response to
`TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1` (independent follow-up review, section
13.4): section 17.4's `ordinary dict keys: exact str` clause was published
without the compatibility investigation given to value shapes in sections
17.1-17.3. It changes only the key-type disposition. Sections 1-17 are
unmodified in substance; this section does not reopen F1 or F2.

=== 18.1 Exact question

What key types must supported canonical `_meta` dictionaries admit? The same
principle from section 17.1 applies: reachability through a carrier is not by
itself Domain admission, and a published roundtrip or legacy-read promise is
not by itself irrelevant. Desirability of a string-keyed structural document
(backend-neutral representation, stable JSON compatibility, deterministic
diagnostic paths) is a design motivation, not compatibility evidence, and is
not used here as proof.

=== 18.2 Carrier reprobe

[cols="2,3,5",options="header"]
|===
|Carrier |Key shapes tested |Result

|Current YAML sidecar (`yaml.safe_dump`/`safe_load`, `json_backend.py:270`/`210`)
|`int`, `bool`, `float`, `None`, `tuple`
|`int`/`bool`/`float`/`None` keys round-trip exactly, preserving type
(`{1: "a"}` -> `"1: a\n"` -> `{1: "a"}`). A `tuple` key *writes* successfully
(`safe_dump` emits a complex mapping-key node) but *fails to read back*:
`safe_load` raises `ConstructorError: found unhashable key`. The sidecar is
therefore already non-roundtripping for tuple keys today, independent of E3.

|Legacy Python-literal carrier (`str(dict)` -> `json.loads` fails ->
`ast.literal_eval`, `openpyxl_parser.py:_read_meta_sheet`,
`odf_parser.py:_read_meta_payload`)
|`int`, `bool`, `float`, `None`, `tuple`
|All five key shapes round-trip exactly with original type and value, by the
same mechanism (and the same ADR legacy-readability promise) already
established for legacy special *values* in section 17.2.

|Current JSON carrier (`json.dumps`/`json.loads`, used for data-sheet payload
and pre-ADR blob composition, not for the YAML sidecar)
|`int`, `bool`, `float`, `None`
|All non-string keys are silently coerced to their string form
(`1 -> "1"`, `True -> "true"`, `1.5 -> "1.5"`, `None -> "null"`). This
coercion is lossy: `{1: "int-one", "1": "str-one"}` dumps to
`{"1": "int-one", "1": "str-one"}` (duplicate JSON text key) and reloads as
`{"1": "str-one"}`, silently discarding the `1`-keyed entry.
|===

These are carrier facts only. Reachability and mechanical roundtrip support
do not by themselves decide Domain admission, matching section 17.1's
framework: current YAML-sidecar int/bool/float/None keys sit at evidence
level two (maintained roundtrip-supported); the legacy carrier's five shapes
sit at levels one and four (mechanically writer-producible, legacy
compatibility-promised); none sits at level three (intentionally semantic) --
see <<key-semantics-search>>.

[[key-semantics-search]]
=== 18.3 Search for canonical metadata key semantics

Maintained producers, consumers, tests, and declared signatures were searched
for `_meta` dictionaries whose keys are dynamically derived from domain/user
values rather than fixed schema field names:

[cols="3,4,5",options="header"]
|===
|Subtree |Key origin |Independent finding

|`_meta.sheets.<sheet>.column_widths`
|Excel column letter, dynamically derived per sheet
|`openpyxl_parser.py:_extract_column_widths` and
`odf_parser.py:_extract_ods_column_widths` both key exclusively by
`get_column_letter(...)`/`_column_letters(...)` -- always `str` by
construction, independent of any explicit type contract. Confirmed by
`test_column_width_roundtrip.py` fixtures (`widths["A"]`, `widths["B"]`).

|`_meta.workbook_view.sheet_mappings`
|N/A -- fixed-schema list
|A list of `{sheet, frame}` mappings (`workbook_views.py`), not a
dynamically-keyed dict; not evidence either way.

|FK relations (v2)
|N/A -- fixed-schema list
|`apply_v2_relations`/`build_v2_relation` write a list, not a dict keyed by
relation identity. The legacy v1 per-target dict under
`_meta.helper_policies.fk.<target>` is retired and no longer written
(`helper_policies.py` docstring at `configure_fk_helpers`).

|`_meta.helper_policies.fk` targets (`configure_fk_helpers`)
|Declared parameter, not carrier-derived
|`targets: dict[str, Any] \| None` -- the maintained signature declares `str`
target names explicitly.

|`_meta.legend_blocks` (mapping-authoring form)
|Author-supplied mapping key
|`legend_blocks.py:_normalize_mapping` copies `raw.items()` keys through
*unchanged* -- it does not currently validate or coerce key type. This is a
currently-unguarded reachable gap, not a reliance: no producer, fixture, or
test in `test_legend_blocks_ingress.py` constructs a non-`str` mapping key.
List-authoring form forces `str(...)` identity resolution
(`_resolve_identity`) regardless of input type. Legend Blocks is the named E3
delegate migration (section 17.4); its complete output -- including this gap
-- is already required to pass the common recursive post-check, which is the
correct place to close it, not a reason to widen the key grammar.

|XRef matrix-axis identities
|Data-derived (column/row values)
|Governed by a separate, already-accepted family contract requiring "unique,
non-empty strings" (`physical_labels.py` module docstring), independent of
and prior to this correction.
|===

No maintained producer, consumer, declared signature, fixture, or test
constructs, requires, or asserts a non-`str` `_meta` dictionary key anywhere
in the codebase.

=== 18.4 Physical-column-label interaction (no accidental narrowing)

`domain/tabular/physical_labels.py` governs *DataFrame physical column
labels* (`frame.columns`, the pandas Index of an ordinary data sheet) and
explicitly disclaims governing anything else: "This is deliberately distinct
from family-specific value contracts." Its accepted rule -- physical column
labels are not required to be strings; non-missing, hashable,
deterministically-comparable labels (including numeric and tuple labels) are
permitted -- is unchanged and untouched by this correction. It answers a
different question (which DataFrame column labels are addressable) from the
one this section answers (which `_meta` *dictionary* keys are canonical).

Nowhere does a raw physical column label become a `_meta` dictionary key
directly: `column_widths` re-keys by Excel column letter (a derived `str`),
and XRef axis identities are independently required to be strings by their
own family contract (section 18.3). This correction does not narrow, and is
not narrowed by, `ensure_unique_physical_column_labels` or its accepted
non-string-label authority.

=== 18.5 Structural keys vs. data-bearing map keys

Section 17.1's evidence framework, applied above, does not surface a
principled split: every data-bearing map key found in a maintained subtree
(`column_widths`, XRef axis identities, `helper_policies.fk` targets) is
already `str`-typed by construction or by an existing family contract, same
as every fixed structural key (`sheets`, `legend_blocks`, `options`,
`workbook_view`). The one reachable gap (Legend Blocks mapping-form
pass-through, section 18.3) is a currently-unvalidated delegate path, not a
second key category with its own semantics. No evidence requires a bounded
non-string key category; a single `exact str` rule for both categories is
therefore sound.

=== 18.6 Decision

*A. EXACT STRING KEYS CONFIRMED.* All maintained canonical metadata
dictionaries use `str` keys; no accepted semantic or programmatic contract
requires otherwise; the one reachable non-`str` path (Legend Blocks
mapping-form pass-through) is an unguarded gap the E3 delegate post-check
already closes, not a counterexample.

[cols="2,3,4,3,5",options="header"]
|===
|Key shape |Carrier/path |Evidence level |Disposition |E3 consequence

|`int` / `bool` / `float` / `None`
|Current YAML sidecar (`yaml.safe_dump`/`safe_load`)
|Level two -- maintained roundtrip-supported; no family/test assigns
non-`str`-key semantics
|*REJECT / NARROW*
|After safe carrier decode, E3 rejects the node at its exact metadata path
(parent-container path plus entry ordinal) before Domain publication. No
generic `str(key)` coercion. Release notes identify the former PyYAML
roundtrip; E3 tests pin atomic rejection.

|`int` / `bool` / `float` / `None` / `tuple`
|Legacy XLSX/ODS Python-literal `workbook_meta_blob`
(`str(dict)` -> `ast.literal_eval` fallback)
|Level one (mechanically writer-producible) and level four (ADR legacy-read
promise); no family/test assigns non-`str`-key semantics
|*LEGACY MIGRATION / TARGETED REJECTION*
|The legacy parser still succeeds and recovers the dict. E3 then rejects the
non-`str`-keyed node at its exact path with a deterministic legacy-
compatibility diagnostic before publication, mirroring the value-shape
treatment in section 17.2. Release notes direct users to a prior compatible
version or a meaning-aware manual rewrite; XLSX and ODS legacy-path tests are
required.

|`int` / `bool` / `float` / `None`
|Current JSON carrier (`json.dumps`/`json.loads`)
|Level zero -- carrier silently coerces to `str` today; not roundtrip-
supported (coercion is lossy under key collision, section 18.2)
|*NO ADMISSION, NO CARRIER CHANGE*
|Not an E3 boundary case: the JSON carrier already produces only `str` keys
by the time E3 would see the decoded value. No migration/test obligation
beyond noting the existing collision hazard is a pre-existing carrier
property, not an E3 compatibility break.

|Non-`str` mapping key
|Legend Blocks mapping-authoring form (`_normalize_mapping` pass-through)
|Reachable, currently unguarded; no producer/fixture/test relies on it
|*CLOSE AT DELEGATE POST-CHECK*
|No pre-E3 change. E3's required complete recursive post-check of delegate
output (section 17.4) rejects a non-`str` key here identically to an
ordinary metadata path; no separate delegate exemption.
|===

No generic normalization is authorized. In particular, `int`/`bool`/`float`/
`None` -> `str` key coercion is *not* adopted, for two independently
sufficient reasons demonstrated in section 18.2: (1) it is not
semantics-preserving without a family-specific contract, matching section
17.1's rule for values; and (2) it is demonstrably lossy under collision --
Python's own `{1: ..., True: ...}` equality (`1 == True`) already collapses
distinct-looking keys before any carrier is involved, and the current JSON
carrier's silent `int -> str` coercion independently loses data when both
`1` and `"1"` are present. Targeted rejection, not coercion, is the only
disposition consistent with the no-generic-normalization rule already
established for values.

=== 18.7 Diagnostic safety consequence

A rejected non-`str` key must be identified by structural coordinates only:

....
metadata path to the parent container
entry ordinal within that container
failure kind = unsupported_metadata_key
....

The diagnostic must not include `repr(key)`, `str(key)`, or any operation
beyond the safe classification already used elsewhere in this census
(`type(x) is dict`-style exact-type checks): an unclassified rejected key may
carry a hostile or expensive `__repr__`/`__str__`, and retaining the key
object itself for later diagnostic formatting is not required. This mirrors
the value-shape diagnostic rule already stated in section 17.2 ("without value
`repr`/`str`"), extended to keys. This section states the safety obligation;
it does not name a final error-class, which existing E3 diagnostic authority
owns elsewhere.

=== 18.8 Delegate consequence

The exact-`str`-key rule applies to *complete* delegate output, not just
directly-authored metadata. Legend Blocks (or any later governed delegate)
may not emit an out-of-contract dictionary key and escape the central
post-check by virtue of delegate status; section 17.4's "complete output
subject to the common recursive post-check" already covers this without
further change. Legend Block entry-value semantics remain out of scope here,
per section 17 and `FTR-LEGEND-BLOCKS-ENTRY-CONTRACT-P5`.

=== 18.9 Final corrected grammar (unchanged, disposition now explicit)

The section 17.4 grammar is unchanged by this correction; only its key-type
line now points here:

....
`_meta` root:
    exact dict

recursive ordinary containers:
    exact dict
    exact list

ordinary dict keys:
    exact str
    (non-str carrier/legacy/delegate keys: explicit migration/rejection
    per section 18.6, no generic coercion)

ordinary leaves:
    E1-admitted Scalar

aliases:
    acyclic shared aliases permitted

cycles:
    rejected

delegates:
    only statically governed named delegates;
    complete output subject to the common recursive post-check

special historical/carrier values and keys:
    handled by the exact migration/rejection rules in sections 17.2 and 18.6;
    not silently admitted into the common grammar, not silently coerced
....

*Superseded in part:* section 19 corrects this grammar's key-type line
further. `ordinary dict keys: exact str` is not sufficient by itself; a
named, bounded XRef Resolution-facet representation migration is also
required for `dense_axes.resolved.row_identities`. See section 19.10 for the
final grammar text.

=== 18.10 Validation

Read-only probes (no tracked file modified): current YAML-sidecar
`int`/`bool`/`float`/`None`/`tuple`-key write-read behavior; legacy
`str(dict)`-then-`ast.literal_eval` restoration of the same five key shapes;
current JSON `int`/`bool`/`float`/`None`-key coercion and collision behavior;
direct reads of `openpyxl_parser.py`, `odf_parser.py`, `json_backend.py`,
`helper_policies.py`, `workbook_views.py`, `fk_relations.py`,
`legend_blocks.py`, and `physical_labels.py`; `test_column_width_roundtrip.py`
and `test_legend_blocks_ingress.py` inspected as evidence, not modified.
Asciidoctor rendered this artifact without error; `git diff --check` passed.
The independent review artifact, runtime/tests, roadmap, Project Memory,
mega-FTR, mega-review, registry, and all other documentation remained
unchanged.

=== 18.11 Finding disposition

`TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1`::
    *correction-author RESOLVED.* The `exact str` dict-key rule now carries
    the same four-level compatibility investigation given to value shapes:
    carrier reprobe (18.2), canonical-key-semantics search (18.3),
    physical-column-label non-interaction (18.4), an explicit per-shape
    migration/rejection table with diagnostics/release-note/test obligations
    (18.6), a stated reason generic coercion is unsafe (18.6), a diagnostic
    safety rule (18.7), and a delegate-output consequence (18.8). No
    non-`str` key is silently admitted, silently coerced, or silently
    rejected without disposition.

This is a corrected census-author verdict, not independent acceptance. E3
remains blocked until a fresh-context follow-up review accepts this
disposition.

*Superseded:* the independent final follow-up review (review artifact
section 14) found this section 18.11 disposition incomplete -- it did not
account for the maintained XRef dense-axis producer. Section 19.11 is the
current `TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1` disposition; this paragraph
remains only as the (superseded) section-18 verdict.

*PREFERRED BOUNDED SUBSTRATE CONFIRMED, INCLUDING EXPLICIT KEY-COMPATIBILITY
DISPOSITION*

(Superseded by section 19 -- see section 19.12 for the current verdict.)

== 19. XRef runtime-key compatibility correction (FOLLOWUP-F1, second disposition)

This section is the correction-author response to the independent final
follow-up review's `TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1: NOT RESOLVED`
verdict (review artifact section 14): section 18's "no maintained producer
relies on a non-`str` key" conclusion is false. It addresses only the
maintained XRef dense-axis Resolution facet. Sections 1-18's other
conclusions -- special-value dispositions (F1), ownership/alias/cycle
dispositions (F2), carrier-origin non-string-key dispositions, Legend Blocks
delegate treatment, and diagnostic-safety rules -- are unmodified and are not
reopened here.

=== 19.1 Exact question

E2 permits non-missing, hashable, deterministically-comparable physical
column labels that are not required to be strings. The maintained public
XRef dense-axis path copies such a label directly into runtime `_meta` as a
dictionary key. Section 18's `ordinary dict keys: exact str` rule, applied
without qualification, would reject that maintained producer's output. This
section resolves that conflict with one exact, bounded,
implementation-authorizing disposition -- neither by narrowing E2's
physical-label authority nor by casually widening the metadata key grammar
to arbitrary hashable objects.

=== 19.2 Independent reproduction

The review's exact counterexample was reproduced against actual code, not
taken on the review's word:

....
frames = {
    "resources": pd.DataFrame({7: ["r1", "r2"]}),
    "values": pd.DataFrame([{7: "r1", "context_id": "default", "text": "Hello"}]),
}
admit_ordinary_frames(frames)   # succeeds -- E2 admits the int-7 column label
out = contract_xref(
    frames, relation="values", output="matrix",
    row_keys=[7], column_key="context_id", value="text",
    column_keys=["default", "product_a"],
    dense_axes={"rows_from": {"frame": "resources", "key": 7}},
    name="numeric_rows",
)
out["_meta"]["xref_crosstable"]["numeric_rows"]["dense_axes"]["resolved"]
    == {"row_identities": [{7: "r1"}, {7: "r2"}]}
....

The key `7` is confirmed `type(k) is int`. A bounded follow-up probe swept
`bool`, `float`, and `datetime.date` column labels through the identical
path: all three are admitted by E2 (`admit_ordinary_frames`) and all three
reach `_meta` as exact non-`str` dict keys the same way (`{True: "r1"}`,
`{3.5: "r1"}`, `{datetime.date(2024, 1, 1): "r1"}`). A tuple column label is
admitted by E2 but rejected by XRef's own family-specific rule before it can
reach `_meta` (`"Frame 'values' has MultiIndex/tuple columns; ...requires
flat column labels"`). This is not a single-shape anomaly: it is every
non-tuple physical label E2 admits.

=== 19.3 Producer and consumer trace

*Producer:* `xref_crosstable/dense_axes.py:_dense_axes_meta_payload`, lines
399-403:

....
resolved["row_identities"] = [
    {row_key: identity[index] for index, row_key in enumerate(row_keys)}
    for identity in dense_resolved["row_identities"]
]
....

`row_keys` is the caller's `row_keys` parameter to `contract_xref`/
`expand_xref` (the *relation* frame's physical row-identity column labels).
`identity` is a tuple of `_plain_axis_value`-normalized cell values read from
the *axis-source* frame (`rows_from.key`/`.keys`), in `axis_keys` order.
Field name and value therefore come from two independently-named,
positionally-paired lists (arity-checked, not name-checked): the *name*
(`row_keys[i]`, a physical label, admitted by E2 and by XRef's flat-label
rule) becomes the dict key; the resolved *value* becomes the dict value.

*Consumer (same-run fallback):* `_stored_row_identities`
(`dense_axes.py:301-320`), reached from `_row_identities_from_axis_source`
when the axis-source frame (`resources` above) is no longer present in
`frames` -- the documented "later `contract_xref` in the same run... or
hand-authored meta" scenario (`_dense_axes_meta_payload` docstring). It does
`item[row_key] for row_key in row_keys`: a genuine dict-key lookup by name,
which is why the stored form is keyed by name rather than stored
positionally -- it lets a later call supply `row_keys` in a different order
(or a differently-ordered composite) and still reassemble the correct
tuple. This same-run fallback is independently exercised by the maintained,
passing test `test_expand_contract_roundtrip_preserves_dense_shape_from_metadata`.

*Persistence:* `pipeline/persistence_boundary.py:213-214` strips
`dense_axes.resolved` (via `project_meta_to_persistable_contract`) before any
carrier write. This does not cure the E3 conflict: the accepted E3
precondition covers runtime-created and consumed metadata, and the same-run
fallback above is a consumer that reads the unstripped runtime value before
persistence projection ever runs.

*Maintained pinned test:* `test_xref_crosstable.py`'s composite-row-key test
asserts the exact current shape as part of the public `_meta` contract:
`"row_identities": [{"region": "NA", "category": "A"}, {"region": "EMEA",
"category": "B"}]`. Any representation change is therefore a compatibility
break against an existing pinned assertion, not merely an internal detail
change -- relevant to sequencing (section 19.9).

=== 19.4 Supported physical-label intersection

[cols="3,3,3,3",options="header"]
|===
|Label type |E2 (`admit_ordinary_frames`) |XRef flat-label rule (`_ensure_flat_axis_labels`) |Reaches `_meta` as a dict key today

|`str`
|Admitted
|Admitted
|Yes (unaffected -- already a `str`)

|`int` / `bool` / `float`
|Admitted
|Admitted
|Yes -- reproduced

|`datetime.date` / `datetime.datetime`
|Admitted
|Admitted
|Yes -- reproduced (`date`)

|`tuple`
|Admitted (E2 does not reject tuple column labels)
|*Rejected* -- `"...requires flat column labels"`
|No -- stopped by XRef's own family rule before reaching `_meta`
|===

The exact intersection needing disposition is: every E2-admitted,
non-missing, hashable, deterministically-comparable, *non-tuple* physical
column label -- which is precisely the set of types E1 already admits as
ordinary Scalar leaves (`str \| bool \| int \| float \| date \| datetime \|
None`, ignoring `None` which cannot be a column label). This is not a reason
to widen metadata dict keys: it is evidence that the label, once relocated
from key position to value position, needs no new value vocabulary --
<<xref-representation>> uses exactly this fact.

This finding does not touch E2's physical-label authority itself, and does
not generalize: XRef's own family rule already excludes tuples from this
path, so no other physical-label type census or admission change is implied.

=== 19.5 Sibling search

The same "raw physical/data value becomes a `_meta` dict key" pattern was
searched across `xref_crosstable`, `compact_multiaxis`, and `grouped_xref`
(the modules named in the follow-up task and the modules structurally
adjacent to dense axes):

* `xref_crosstable/operation.py:137,174,342` use the identical
  `{row_key: value for row_key in row_key_cols}` idiom, but every occurrence
  builds `pd.DataFrame` *row records* for the `relation`/`matrix` *frame
  payload* (governed by `physical_labels.py`'s DataFrame-column authority,
  which already permits non-`str` labels) or a raised exception's diagnostic
  payload -- neither is a `_meta` node. `dense_axes.py:401` is the only
  occurrence that writes into `_meta`.
* `grouped_xref/projection.py:115` already filters physical labels to
  `type(label) is str` before they reach that family's structures
  (`frozenset(label for label in physical_labels if type(label) is str)`) --
  no non-`str` key reaches `_meta` through this family.
* `compact_multiaxis.py`'s `_write_multiaxis_meta` writes a list
  (`meta[_META_KEY] = configs`), not a mapping keyed by a physical value.
* Column-identity values participating in the *matrix* axis (as opposed to
  the *row* axis) are already constrained to unique non-empty strings by an
  existing, unrelated accepted contract
  (`_ensure_column_identity_values`, cited in `contract_xref`'s own
  docstring: "Matrix column identities... must be unique, non-empty
  strings"), so `dense_axes.resolved.column_keys` was never a counterexample.

`dense_axes.resolved.row_identities` is the sole maintained `_meta` subtree
exhibiting this pattern.

=== 19.6 Semantic identity versus dict representation

The physical label does not semantically need to be a dictionary *key*. The
consumer's actual requirement (section 19.3) is an association -- "this resolved
value corresponds to this physical row-identity field" -- reassembled by
name so a later same-run call may supply `row_keys` in a different order.
That associative requirement survives unchanged if the physical label
becomes a *value* under a fixed, canonical `str` field name, and the
consumer reconstructs the same tuple by matching on that value
(`_values_equal`, already imported and used elsewhere in the same module)
instead of by native dict-key lookup. The current dict-of-names shape is
today's Python convenience representation of that association, not a
requirement that the label be hashed as a Python dict key.

[[xref-representation]]
=== 19.7 Selected resolution: bounded representation migration

*Representation migration*, scoped to `dense_axes.resolved.row_identities`
only. It is sound because it is semantically lossless (<<19-8>>), it keeps
XRef's flat, non-tuple physical-label acceptance and E2's physical-label
authority completely unchanged, it keeps every `_meta` dictionary key
`str`-typed with zero exceptions, and it does not require E3 to carry a
bounded non-`str`-key vocabulary or a path-specific grammar exception for
one feature-local facet.

The alternative -- a bounded non-`str`-key exception scoped to this one path
-- was rejected: it would make the common substrate's key rule
path-conditional (contrary to section 18.6's reasoning against ad hoc
exceptions), it is not required by anything the producer or consumer
actually needs (section 19.6), and Phase-E design does not authorize a governed
per-path grammar-exception mechanism, so inventing one here would itself be
new, undisposed architecture.

[[19-8]]
=== 19.8 Exact target representation

Each `row_identities[i]` entry -- currently a `dict[Any, Any]` keyed by
physical row-identity labels -- becomes a `list` of fixed-schema records, one
per composite field, preserving field order:

....
resolved.row_identities[i]:
    [
        {"row_key": <physical row-identity label, E1-admitted Scalar>,
         "value": <resolved identity component, E1-admitted Scalar>},
        ...  # one record per row_keys[j] / axis_keys[j] pair, same order
    ]
....

Equivalent for the two worked examples already reproduced in this census:

....
single row key (row_keys=[7]):
    [[{"row_key": 7, "value": "r1"}]]

composite row keys (row_keys=["region", "category"]):
    [
        [{"row_key": "region", "value": "NA"}, {"row_key": "category", "value": "A"}],
        [{"row_key": "region", "value": "EMEA"}, {"row_key": "category", "value": "B"}],
    ]
....

Field names `"row_key"`/`"value"` reuse the exact local variable names
already used in the producer (`dense_axes.py:400-401`) and the exact
parameter concept `row_keys`; no new generic pair/tuple abstraction is
introduced, and no sibling facet needs the same shape (section 19.5) so it stays
feature-local, not a repository-wide metadata-pair type.

*Ordering:* list order matches `row_keys` order at production time (as
today); reconstruction does not depend on list order, only on `"row_key"`
value matching (`_values_equal`), preserving the current
order-independent-by-name reassembly used by `_stored_row_identities`.

*One record per physical label; row/column-axis symmetry:* only the row
axis needs this migration. `dense_axes.resolved.column_keys` stays a plain
`list` of `str` values -- it was never dict-keyed and is already
`str`-constrained by the existing matrix-identity contract (section 19.5), so row
and column axes remain asymmetric exactly as they already are today; this
migration does not force them into a shared shape.

*Duplicate labels:* unaffected. Duplicate-row-identity rejection
(`dense_axes.py:175-179`, `_stored_row_identities:316-318`) already operates
on the *identity tuple*, not on the dict-key representation, and is
unchanged by moving the label from key to value position.

*Currently-valid non-`str` label types:* `int`/`bool`/`float`/`date`/
`datetime` all become `"row_key"` values unchanged in type (all already
E1-admitted Scalar, per section 19.4) -- no coercion, no narrowing.

*Consumer reinterpretation:* `_stored_row_identities` must reconstruct
`identity = tuple(...)` by, for each `row_key` in the caller's `row_keys`,
locating the record whose `"row_key"` value-equals (`_values_equal`) that
`row_key` and taking its `"value"`, instead of `item[row_key]`; a missing
match raises the same `KeyError` class the current "missing key(s)" branch
raises. `_row_identities_from_axis_source`'s *write* side changes
identically to <<xref-representation>>'s shape. No other consumer of
`row_identities` was found beyond these two functions and the maintained
test asserting the shape (section 19.3).

[[19-9]]
[[xref-sequencing]]
=== 19.9 Migration lifecycle ownership

This is required compatibility work *for E3*, not a prerequisite migration
before it and not a broader XRef redesign. The maintained producer, its
same-run consumer, and the pinned test in section 19.3 all currently emit and
require the *old* dict-keyed shape; E3's exact-`str`-key enforcement and this
representation migration must ship in the same implementation slice, so
there is never an execution point where enforcement is live against a
still-dict-keyed producer. E3 acceptance for this facet must therefore
include: the migrated producer, the migrated same-run consumer, an updated
`test_xref_crosstable.py` composite-row-key assertion (a release-note-worthy
public `_meta` shape change, per the release-notes checkpoint), and E3's
common recursive post-check accepting the migrated shape. No runtime or test
file is touched by this census correction itself.

Everything else about XRef -- entry-value semantics, other dense-axis
features, general refactoring -- is out of scope and not reopened.

=== 19.10 Corrected final grammar

Sections 17.4 and 18.9's grammar stands with the key-type line now fully
qualified (no remaining non-`str`-key exception of any kind):

....
`_meta` root:
    exact dict

recursive ordinary containers:
    exact dict
    exact list

ordinary dict keys:
    exact str, with zero exceptions
    -- carrier-origin non-str keys (YAML sidecar, legacy literal):
       explicit migration/rejection per section 18.6, no generic coercion
    -- maintained XRef dense-axis row-identity keys: representation
       migration per section 19.8, shipped atomically with E3
       enforcement (section 19.9); physical labels become "row_key"
       *values*, never dict keys

ordinary leaves:
    E1-admitted Scalar

aliases:
    acyclic shared aliases permitted

cycles:
    rejected

delegates:
    only statically governed named delegates;
    complete output subject to the common recursive post-check

special historical/carrier values and keys:
    handled by the exact migration/rejection rules in sections 17.2 and 18.6;
    not silently admitted into the common grammar, not silently coerced
....

=== 19.11 Preserved dispositions

Unchanged by this section: `set`/`bytes`/tuple/complex value dispositions
(F1, sections 17.1-17.3); ownership/alias/cycle dispositions (F2, section
17.5, section 6); exact dict/list ordinary containers and E1 Scalar leaves;
no generic normalization; no blanket deep copy; shared acyclic aliases
permitted; cycles rejected; staged publication/non-mutation; framework/
library caller obligations; delegate complete-output post-check; carrier-
origin non-`str`-key REJECT/MIGRATE treatment (section 18.6) -- those keys
have carrier compatibility but no maintained semantic producer, unlike the
XRef path, which has a maintained semantic producer and consumer and is
therefore resolved by representation migration, not rejection.

=== 19.12 Validation

Read-only probes (no tracked file modified): reproduced the exact reviewed
numeric-row-key `contract_xref` example end to end (`admit_ordinary_frames`
then `contract_xref`, confirming `type(k) is int`); swept `bool`/`float`/
`datetime.date` column labels through the identical path; confirmed a tuple
column label is admitted by E2 but rejected by XRef's own flat-label rule;
read `dense_axes.py`, `operation.py`, `persistence_boundary.py`,
`grouped_xref/projection.py`, and `compact_multiaxis.py` directly for the
producer/consumer/sibling trace; inspected
`test_expand_contract_roundtrip_preserves_dense_shape_from_metadata` and the
composite-row-key assertion in `test_xref_crosstable.py` as evidence, not
modified. The full XRef crosstable suite, structural-admission/ingress
suite, and the persistence/metadata suites already validated in sections
17.6/18.10 were re-run and passed with no modification. Asciidoctor rendered
this artifact without error; `git diff --check` passed. The independent
review artifact, runtime/tests, roadmap, Project Memory, mega-FTR,
mega-review, registry, and all other documentation remained unchanged.

=== 19.13 Finding disposition

`TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1`::
    *correction-author RESOLVED (second disposition).* The maintained XRef
    dense-axis counterexample independently confirmed by the final follow-up
    review is resolved by a named, bounded, meaning-preserving representation
    migration (section 19.8) that keeps every `_meta` dictionary key `str`
    with zero exceptions, keeps E2's physical-label authority and XRef's own
    flat-label rule completely unchanged, is scoped to exactly one feature-
    local facet (section 19.5's sibling search found no other occurrence),
    and is explicitly sequenced to ship atomically with E3's key-grammar
    enforcement (section 19.9) so no maintained producer state is ever
    rejected by a not-yet-migrated implementation. The sound but incomplete
    carrier-origin key disposition from section 18 is preserved unchanged
    (section 19.11).

This is a corrected census-author verdict, not independent acceptance. E3
remains blocked until a fresh-context follow-up review accepts this
disposition.

*PREFERRED BOUNDED SUBSTRATE CONFIRMED WITH XREF COMPATIBILITY DISPOSITION*
