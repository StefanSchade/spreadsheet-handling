===== Fresh Independent Readiness Review 002 -- FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A

*Review date:* 2026-08-15

*Review type:* Fresh independent pre-architecture readiness review, required by
`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc`. This is
not an architecture assessment, does not select Option A/B/C/D/E, and performs
no implementation.

*Verdict:* READY FOR ARCHITECTURE ASSESSMENT.

==== 1. Scope and current baseline

[cols="2,5"]
|===
| Item | Verified value

| Repository root
| `/home/stefan/repos/spreadsheet-handling/core`

| Branch
| `main`

| Reviewed HEAD
| `ee0496de2ecd3a9ec87f62cb77d180abeda4bffa`
  (`docs(trusted-ingress): advance lifecycle from E6 to E7`)

| Starting worktree
| Clean; `git status --short` returned no output.

| FTR authoring commit
| `b967028a7f497f4cef8bad22558ef1589e860b95`

| First readiness review commit
| `081edb4f449e990341ca13205fd631ef60991ac3`

| Correction commit
| `3a9da4c1aa2285480f6242b7a27feb5fdf68d638`
  (`docs(FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A): close readiness review
  findings`)

| FTR/review files touched since the correction
| None. `git log --oneline b967028..HEAD -- docs/backlog/FTR-PIPELINE-DOTTED-
  EXTENSION-SURFACE-P4A.adoc docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-
  SURFACE-P4A_ftr_review.adoc docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-
  SURFACE-P4A_ftr_correction_review.adoc` returns exactly the three commits
  above. The corrected FTR text read for this review is therefore identical to
  the text the correction self-review describes.
|===

Between the correction (2026-07-30) and this review (2026-08-15), Core
advanced 78+ commits including Phase-E Trusted Ingress E4 (exact producer-
transition representation), E5 (framework-managed macro wiring), and E6
(programmatic-surface/API publication, accepted at `7f1cadd`). This review
independently re-establishes currency against that advanced baseline rather
than trusting the correction's five-week-old snapshot.

This review does not overwrite `..._ftr_review.adoc` or
`..._ftr_correction_review.adoc`. It reconstructs the first review's findings
from that immutable artifact directly, not from the correction's paraphrase.

==== 2. Original finding disposition

[cols="1,4,4,2"]
|===
| Finding | Original concern (from `..._ftr_review.adoc`) | Independent verification at current HEAD | Result

| I-1
| Acceptance allowed completion via a lengthy inventory or Option-E "unresolved
  or provisional" labeling without one completed row per surface covering
  audience, stability, syntax, trust, addressability, compatibility,
  internal/consumer relationship, and follow-up decision; no criterion forced
  every deferral to be bounded.
| Current FTR "Mandatory final contract matrix" (lines ~649-702) requires
  exactly one row per each of the eight listed surfaces and forces 13 named
  decision columns per row, ending in a selected Option/hybrid, a yes/no
  follow-up-FTR decision, and a safe interim contract. "Bounded-deferral rule"
  (~704-719) requires the exact unresolved question, missing evidence, a named
  acquisition action, an owner/follow-up role, an explicit reopening trigger,
  a safe interim contract, and a follow-up-FTR decision for *any* deferred row,
  explicitly including Option E outcomes ("Option E cannot complete the
  assessment through a general 'clarify later' classification"). No path was
  found by which an assessor could skip a surface, leave trust/addressability
  unresolved, pick no option, or defer without every bounded field: the
  Mandatory assessment outputs list and Acceptance criteria items 1-3
  independently restate and gate the same requirement.
| RESOLVED

| I-2
| Acceptance required a migration plan "tested against representative adoption
  evidence" while Scope/Non-goals forbid adoption/runtime changes during
  assessment -- an assessor could either under-satisfy with prose or cross the
  no-implementation boundary.
| "Read-only migration evidence" section (~721-737) and Mandatory assessment
  outputs items 5-6 now require a read-only impact map, before/after
  configuration examples, compatibility feasibility analysis, and an
  executable *later* migration-test plan, explicitly reserving actual Core/
  adoption changes, deprecation warnings, migration execution, and rollback
  validation for a separately approved implementation FTR. Acceptance
  criterion 11 restates the same split and forbids any Core/adoption mutation
  during assessment. Non-goals still forbid modifying adoption repositories.
  No contradictory text remains.
| RESOLVED

| I-3
| Counts and repository names were given without recorded revisions,
  tracked-file scope, literal search patterns, or exclusions, making the
  census unreproducible and drift undetectable.
| "Reproducible census baseline -- 2026-07-30" section (~233-350) now pins
  exact Core/adoption commit hashes per repository, states the tracked
  `*.yaml`/`*.yml` scope, gives the three literal extended-regexp search
  patterns verbatim, and preserves the internal-tooling / adoption /
  test-and-doc / registry-representation classification split. This review
  independently re-ran the identical patterns against current HEAD in each
  sibling repository (section 4 below) and confirms they reproduce cleanly
  against the pinned revisions.
| RESOLVED

| M-1
| The "Existing surfaces" table omitted framework-owned macro flow, which the
  problem framing and later scope both rely on.
| "Existing surfaces" table (~109-168) now includes a "Framework-owned macro
  flow" row as its final entry, and "Mandatory final contract matrix" lists it
  as the eighth required row with an explicit instruction to classify fixed
  orchestrator boundary phases separately from application-owned private
  composition. The dedicated "Framework-owned macro-flow category" section
  (~410-463) still carries the fixed-boundary-phase / application-owned-
  private-composition subtype split and the final-cleanup "invocation
  ownership, not authorship" qualification the first review requested.
| RESOLVED

| M-2
| `consumer`, `expert`, `internal`, `programmatic`, `framework-owned` were used
  consistently but undefined as decision labels.
| "Operational terms" section (~79-107) now defines all five terms, explicitly
  states that expert YAML authors are consumers, and that programmatic/
  importable reachability does not by itself establish stable public-API
  status.
| RESOLVED
|===

No original finding was collapsed, and none required reopening under current
evidence: the resolution text for each is functional (forcing/gating language),
not merely descriptive, and no later section of the corrected FTR contradicts
any of the five corrections.

==== 3. Current source/resolver inventory

Independently re-inspected at `ee0496d` (not re-derived from either prior
review):

* `pipeline/dotted_paths.py` -- unchanged in shape since the FTR baseline:
  one checked resolver, `resolve_configuration_callable`, with one blocked
  namespace tuple, `("spreadsheet_handling.domain.ingress",)`.
* `pipeline/steps.py` -- `make_plugin_step`, `make_builder_target_step`,
  `make_frames_target_step` all still route string targets through
  `_resolve_target` -> `resolve_configuration_callable`; non-string targets
  (direct callables) pass through unchanged. E4's `BoundFramesTargetCall`
  wrapper (added after the FTR baseline) freezes `kwargs` and gives E4's
  classifiers an identity check; it does not add, remove, or alter a
  resolution path -- it wraps the same resolved callable in an immutable
  call object for later executable-identity verification.
* `pipeline/registry.py` -- `resolve_registration` still detects a
  colon-bearing `step` value and resolves it as a direct factory at
  registration time; short names still resolve through `REGISTRY`.
* `application/orchestrator.py` -- `orchestrate(..., steps: Iterable[BoundStep]
  | None = ..., ...)` still accepts pre-bound steps directly (direct
  `BoundStep` composition, unchanged).
* No new arbitrary string-to-callable resolver was found anywhere in
  `src/spreadsheet_handling/`. `io_backends.router`/`io_backends.__init__`
  remain source-owned lazy-import tables selected only after a short backend
  kind, not a configuration-addressable dotted resolver (matching the first
  review's finding, independently re-confirmed here). `domain.column_roles.
  resolver` and `domain.transformations.xref_axis_mapping.resolver` are
  unrelated domain-level label/role resolvers, out of this FTR's scope by its
  own explicit XRef non-goal.
* No dedicated "single resolver" architecture guard test exists specifically
  for `pipeline.dotted_paths` (the Legend Blocks Minor noted this gap as
  textual-only); independent inventory again found no second resolver to
  guard against. This remains an open architecture question the assessment
  itself may decide (FTR section 18 framing), not a readiness defect.

Conclusion: the resolver/callable topology the FTR describes is unchanged in
kind since 2026-07-30. Registry growth (below) added ordinary registered
short-name steps, not a new resolution mechanism.

==== 4. Eight-surface freshness check

Each of the eight rows in the FTR's "Existing surfaces" table and mandatory
contract matrix was independently re-verified against current source:

[cols="2,2,5"]
|===
| Surface | Current-HEAD result | Evidence

| Registered short name
| Current
| `REGISTRY` now has 44 entries (was 40): 38 `StepRegistration` (was 34, all
  string-targeted) plus 6 bare factories (unchanged: `identity`, `validate`,
  `add_fk_helpers`, `remove_fk_helpers`, `validate_fk_helpers`, `plugin`). The
  four new entries (`project_axis_labels`, `restore_axis_keys`,
  `contract_grouped_xref`, `expand_grouped_xref`, minus one removed,
  `reconstruct_grouped_matrix` also added; net +4 after `project_by_role`'s
  removal) are ordinary XRef/grouped-column registered steps from
  `FTR-XREF-AXIS-MAPPINGS-P4A2`, unrelated to dotted-extension mechanics and
  explicitly out of this FTR's scope by its own XRef non-goal.

| Plugin callable
| Current
| `make_plugin_step` mechanics unchanged; resolves at construction via
  `resolve_configuration_callable`, invokes `(frames, **args)`.

| Direct colon-style factory
| Current
| `resolve_registration`'s colon detection unchanged.

| Registry-owned string target
| Current
| All 38 (was 34) `StepRegistration` entries carry Core-owned string targets;
  same internal-representation classification applies.

| Nested configured string target
| Current
| `make_builder_target_step`/`make_frames_target_step` still accept `target`
  as string-or-callable via the same `_resolve_target` path.

| Direct Python callable target
| Current
| `_resolve_target` still returns non-string callables unchanged.

| Direct `BoundStep` composition
| Current
| `orchestrate(..., steps=...)` and `pipeline.execution.run_pipeline` both
  still accept pre-bound `BoundStep` values; E6 additionally published
  `run_pipeline` as an explicit "caller-precondition" surface (section 5
  below), consistent with, not contradicting, the FTR's classification.

| Framework-owned macro flow
| Current
| Fixed orchestrator boundary phases (ingress, `pipeline/persistence_
  boundary.py` projection, final-cleanup executor in `application/
  managed_pipeline.py`/`domain/pipeline_cleanup.py`) and application-owned
  private composition (`application/schema_maintenance.py` constructing one
  private `BoundStep`) both still exist exactly as described. E6's own census
  (`FTR-TRUSTED-INGRESS-P4A.adoc` 46.1) independently corroborates the schema-
  maintenance private-`BoundStep` classification.
|===

No ninth maintained dotted/callable surface was found. No listed surface has
disappeared. The inventory is current.

*Adoption/config census refresh (task-required, not blindly preserved):*
sibling repositories `demo`, `worldbuilding`, `ssdp`, `msg_ssdp_rollout`,
`personal`, `personal_data`, `home-lab`, `pages` are all present locally; the
FTR's exact three regexp patterns were re-run against each repository's
current tracked-YAML HEAD (not merely the pinned snapshot):

[source,text]
----
core          plugin=16 direct_colon=0 nested_target=0   (unchanged)
demo          plugin=4  direct_colon=0 nested_target=0   (unchanged, pinned rev == current)
worldbuilding plugin=6  direct_colon=0 nested_target=0   (was 3 at pinned rev; repo advanced)
ssdp          plugin=2  direct_colon=0 nested_target=0   (unchanged; working tree dirty, HEAD unchanged)
msg_ssdp_rollout plugin=2 direct_colon=0 nested_target=0 (unchanged)
personal      plugin=0  direct_colon=0 nested_target=0   (unchanged; working tree dirty, HEAD unchanged)
personal_data plugin=0  direct_colon=0 nested_target=0   (unchanged)
home-lab      plugin=0  direct_colon=0 nested_target=0   (unchanged; working tree dirty, HEAD unchanged)
pages         plugin=0  direct_colon=0 nested_target=0   (unchanged)
----

`worldbuilding` advanced from `729349291703144316e346e1724c5547565bbe3e` to
`e6ca10a9e98a728d9814a0cefc1baad4c47e59d6`; its plugin count doubled (3 -> 6)
through new `dino_insel` story-context pipelines, all classified the same way
the FTR already classifies plugin use (domain-specific active adoption). No
inspected repository shows any direct-colon or nested-target adoption use,
now or previously; the qualitative evidence direction the FTR relies on
(plugin is real active adoption; direct-colon/nested-target remain locally
unobserved) is unchanged. This is a numeric census drift, not a material
architecture-relevant change, and the FTR's own evidence-gathering plan step 3
already requires the eventual assessment to re-run this exact census rather
than trusting the printed 2026-07-30 numbers -- so this drift does not block
readiness, but the assessment must not cite the FTR's printed counts as
current without re-running them.

==== 5. E6 public-surface alignment

Read `docs/backlog/FTR-TRUSTED-INGRESS-P4A_E6_implementation_review.adoc` and
the accepted E6 slice (`FTR-TRUSTED-INGRESS-P4A.adoc` sections 46.1-46.15,
accepted at `7f1cadd`) in full, plus the resulting
`docs/ai_info/interfaces_and_gates.adoc` changes.

E6's programmatic-surface matrix (46.1) durably classifies:

* `orchestrate`/`run_app`/`run_schema_maintenance` -- framework-managed, the
  one automatic E1-E5 seam;
* `run_pipeline` -- supported, explicit caller-precondition surface, not a
  substitute for `orchestrate`;
* the six documented `pipeline.__all__` builders -- supported; building never
  establishes conformance;
* `domain.ingress` and `pipeline.execution_state` -- internal/importable-only;
* the dotted-callable plugin/extension surface (plugin, colon-style factory,
  nested target, bundled as one row) -- "STABLE protocol", executed under the
  same trusted-pipeline-description precondition as the rest of the
  configured pipeline.

This review checked specifically whether that last classification forecloses
or contradicts the Dotted-Extension FTR's still-open audience/compatibility-
tier decision for those same surfaces (task section 9's central question).
It does not:

* the "Dotted callable extension surface" bullets in `interfaces_and_gates.
  adoc` ("this protocol is part of the intentional extension surface") and
  its "Actions that require explicit approval" gate ("Any behavior change in
  documented plugin step contracts") predate the FTR itself (`git log`
  traces this text to `d4921bd`, 2026-05-12) -- it is not new evidence E6
  introduced, and the FTR's authoring/first-review already operated under it;
* E6's "STABLE protocol" language describes the *mechanical calling
  convention currently existing and not disappearing without notice* -- the
  same axis the FTR's "construction timing and invocation timing" and
  "current mechanics" columns already capture -- not a foreclosure of the
  FTR's audience/namespace/compatibility-tier decision space (Options A-E all
  operate within "documented, gated, trusted extension mechanism," none
  proposes silently withdrawing the mechanism without a migration path, which
  is what "STABLE protocol" actually guards against);
* `docs/technical_model/ch04_concepts/trusted_ingress_and_runtime_security.
  adoc` (part of the same accepted E1-E6 architecture) explicitly names all
  eight of this FTR's surfaces verbatim, states this concept "must not
  preempt" the Dotted-Extension FTR, and says its own inventory "does not
  preselect that FTR's Options A-E and defers the audience/stability/
  compatibility decisions entirely to it" -- an explicit, current,
  independently-accepted non-contradiction statement;
* the same concept document's non-guarantees section states, verbatim, "No
  containment of arbitrary Python plugins or dotted-callable targets running
  in the same process -- `pipeline/dotted_paths.py`'s one namespace exclusion
  is an ownership/architecture guard, explicitly not a security boundary,
  exactly as `FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` already states for
  that surface" -- word-for-word consistent with the FTR's own "Trust and
  safety distinctions" section.

No E6/FTR contract conflict was found. The FTR does not need correction on
this axis.

==== 6. E7 prerequisite alignment

Read `FTR-TRUSTED-INGRESS-P4A.adoc` section "E7 -- less-trusted description
authorization" (~2514-2543) and section 46.9.

E7's stated preconditions are: the extension-surface assessment is
independently accepted; a separately, independently reviewed narrow E7 design
exists; operator policy is represented for the evidenced capability kinds. Its
acceptance criteria require that supported less-trusted descriptions cannot
activate a forbidden capability, direct Python forms remain trusted-only, all
four payload/description authority combinations remain representable, and
payload conformance tests remain independent.

The Dotted-Extension FTR's own text is consistent with every one of these:
it treats plugin/factory execution as trusted Python throughout, states
namespace/addressability policy is architecture not security, requires
authorization-relevant timing (construction vs. invocation) per surface, and
explicitly excludes Python sandboxing, dependency isolation, and payload
scalar grammar from its own scope -- matching E7's exclusions verbatim.
Section 46.9 of the accepted E6 slice independently confirms "Phase E remains
Open; E7 remains unstarted and unauthorized," consistent with this FTR's own
"No broad extension implementation may begin until this corrected FTR has
passed a fresh independent readiness review, completed its architecture
assessment, and derived separately approved implementation FTRs."

*Decision-field sufficiency for E7 (task section 14):* the mandatory matrix's
13 required columns (surface name, audience, syntax, construction/invocation
timing, trust/responsibility, addressability policy, stability tier,
compatibility commitment, internal/consumer relationship, diagnostics,
selected option/hybrid, follow-up-FTR need, safe interim contract) jointly
answer seven of E7's nine listed informational needs directly. Two of E7's
nine ("what capability kind it activates," "what identifier/parameter subset
is allowed") are not literally named columns; within this FTR's own bounded
scope, however, the capability kind is uniform across the extension-surface
rows (import-and-invoke one trusted Python callable -- the FTR's own non-goals
exclude any other capability kind from its scope), and the FTR text itself
notes a *second*, separate E7 precondition ("an independently reviewed narrow
E7 design exists") that is where parameter-subset/allowlist grammar detail
belongs, not in this prerequisite. No loophole was found that would let an
assessor satisfy the matrix while leaving E7 unable to derive capability kind
or authorization timing from the required columns plus the retained "current
mechanics" evidence. This is recorded as a non-blocking clarity observation,
not a finding (see section 8).

No E6/E7 contradiction was found; this prerequisite can supply E7's stated
precondition once the architecture assessment itself is completed and
independently accepted.

==== 7. Decision-template, deferral, and derived-FTR discipline

* *Options A-E matrix:* re-read against task section 15's criteria. The five
  options remain distinct (retain-as-is-and-document, explicit-syntax,
  plugin-only, namespace-policy, clarify-only), none is a strawman (each
  carries its own falsifiable test questions), hybrids are explicitly
  permitted, and every option's test-question list ties back to the same
  evidence axes (clarity, compatibility, expert capability, internal
  layering, diagnostics, migration cost, adoption evidence, maintenance
  burden) named in the Evidence-gathering plan's decision-matrix step. No
  option was made obsolete by current-HEAD evidence gathered in this review.
* *Bounded-deferral rule:* re-read against task section 16. All six mandatory
  fields (unresolved question, missing evidence, acquisition action, owner/
  follow-up role, explicit trigger, safe interim contract) plus the
  follow-up-FTR decision are required together; nothing short of the full set
  is accepted, and the rule is explicitly bound to Option E and hybrids too.
* *Derived-FTR discipline:* re-read against task section 17. Mandatory
  assessment outputs require the derived-FTR list to include an explicit
  `none` result where applicable, and the "Read-only migration evidence"
  section requires success/rollback/compatibility criteria to accompany any
  derived implementation FTR -- an assessment cannot discharge a decision by
  merely naming a future FTR without recording the decision already made and
  the interim contract governing the gap.

No gaps were found in any of these three areas.

==== 8. Findings

*Blocking:* none.

*Important:* none.

*Minor:*

* *M-3 (non-blocking, informational):* the FTR's printed 2026-07-30 adoption
  census is now five weeks stale; `worldbuilding`'s plugin count has grown
  3 -> 6 and Core's registry has grown 40 -> 44 entries (unrelated XRef
  registered steps). The qualitative evidence direction is unchanged and the
  FTR's own evidence-gathering plan already requires the assessment to
  re-run the identical patterns rather than cite the printed numbers as
  current. No FTR text change is required; the assessment author should
  simply re-run section 4's patterns before relying on any count.
* *M-4 (non-blocking, clarity only):* the mandatory contract matrix has no
  column literally labeled "capability kind" or "identifier/parameter
  subset," two of the nine facts E7 will eventually need. Within this FTR's
  bounded single-capability-kind scope these are adequately forced by the
  existing "supported syntax" / "construction and invocation timing" /
  "trust/responsibility model" columns plus the retained "current mechanics"
  evidence, and granular allowlist/parameter grammar is explicitly deferred
  to E7's own separate, independently reviewed design precondition. No FTR
  text change is required for readiness; the eventual assessment author
  should simply cross-reference the "current mechanics" column explicitly
  when filling trust/responsibility fields so the connection is legible to
  the E7 design reader without re-deriving it.

Neither Minor item withholds the READY verdict; both are optional
improvements for the assessment author, not required FTR corrections.

==== 9. Evidence, validation, and exact results

===== Repository and history

[source,shell]
----
pwd
git rev-parse HEAD
git status --short
git log --oneline b967028..HEAD -- \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_review.adoc \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc
----

Result: clean tree; HEAD `ee0496d`; the three commits `b967028`/`081edb4`/
`3a9da4c` and no others touch those files.

===== Registry parse

[source,shell]
----
PYTHONPATH=src .venv/bin/python -c "import json; from pathlib import Path; \
from spreadsheet_handling.pipeline.registry import REGISTRY; \
from spreadsheet_handling.pipeline.types import StepRegistration; \
desc=json.loads(Path('registries/pipeline_step_registry.json').read_text(encoding='utf-8')); \
regs=[(n,e) for n,e in REGISTRY.items() if isinstance(e, StepRegistration)]; \
strings=[(n,e.target) for n,e in regs if isinstance(e.target, str)]; \
print(f'runtime_entries={len(REGISTRY)}'); print(f'step_registrations={len(regs)}'); \
print(f'string_target_registrations={len(strings)}'); \
print(f'bare_factories={len(REGISTRY)-len(regs)}'); \
print(f'descriptive_entries={len(desc[\"entries\"])}')"
----

Result:

[source,text]
----
runtime_entries=44
step_registrations=38
string_target_registrations=38
bare_factories=6
descriptive_entries=44
----

(Was `40`/`34`/`34`/`6`/`40` at the 2026-07-30 baseline; the four net new
entries are the XRef/grouped-column registered steps identified in section 4.)

===== Reproducible tracked-YAML census (current HEAD, all nine repositories)

[source,shell]
----
for repo in core demo worldbuilding ssdp msg_ssdp_rollout personal personal_data home-lab pages; do
  path="../$repo"
  rev=$(git -C "$path" rev-parse HEAD)
  plugin=$(git -C "$path" grep -h -E '^[[:space:]]*-[[:space:]]*step:[[:space:]]*plugin([[:space:]]|$)' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  direct=$(git -C "$path" grep -h -E '^[[:space:]]*-[[:space:]]*step:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  nested=$(git -C "$path" grep -h -E '^[[:space:]]*target:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  printf '%s current=%s plugin=%s direct_colon=%s nested_target=%s\n' "$repo" "$rev" "$plugin" "$direct" "$nested"
done
----

Result: see section 4's table. All nine repositories were locally available;
no repository bound needed to be documented as unavailable.

===== Focused current-surface tests

[source,shell]
----
PYTHONPATH=src .venv/bin/pytest -o addopts='' -q \
  tests/unit/pipeline/test_plugins_dotted_path.py \
  tests/unit/pipeline/test_step_registration.py \
  tests/unit/pipeline/test_public_import_surface.py \
  tests/architecture/test_domain_ingress_boundary.py \
  tests/architecture/dependency_guards/test_pipeline_step_registry.py
----

Result: `96 passed in 0.29s` (was `89 passed` at the 2026-07-30 baseline; the
increase tracks the registry/coverage growth in section 4, not a regression).

[source,shell]
----
PYTHONPATH=src .venv/bin/pytest -o addopts='' -q \
  tests/unit/pipeline/test_programmatic_surface_contract.py
----

Result: `10 passed in 0.10s` (E6's new publication-pinning suite, unrelated to
this FTR but confirming E6's classification claims are behaviorally pinned).

===== AsciiDoc and patch validation

[source,shell]
----
asciidoctor -o /tmp/.../FTR.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc
asciidoctor -o /tmp/.../review1.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_review.adoc
asciidoctor -o /tmp/.../correction.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc
git diff --check
----

Result: all three AsciiDoctor invocations exited `0` with no warnings; `git
diff --check` exited `0` with no output.

No `make test` was run; this is a documentation/readiness review and current
evidence uncovered no reason requiring the full suite.

==== 10. Release notes and lifecycle

This review adds one new documentation-only review artifact. It changes no
runtime, public/configuration surface, or release positioning, so no
release-note entry is required.

*Lifecycle cleanup check:* the reviewed FTR remains `New`/open; it is not
implemented and this is not a green implementation verdict, so no `Done`/
`Fixed`/`Superseded` transition, no `_ftrs_open.adoc` removal, and no archive
move applies. No residual scope is created by this review. The next task is
the fresh independent architecture assessment named in section 11, not
lifecycle curation.

==== 11. What this verdict does and does not mean

READY FOR ARCHITECTURE ASSESSMENT means only that
`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` may now begin its fresh
independent architecture assessment. It does not mean the FTR is accepted,
does not select Option A-E, and does not start E7. That assessment must
gather current evidence, complete all eight contract rows, complete the
Options A-E matrix, select an option/hybrid per non-deferred surface, resolve
or record bounded deferrals, decide any derived implementation FTR(s), and
define the safe interim contract -- and must itself pass independent review
before E7's prerequisite is satisfied.

E1-E6 remain independently accepted. E7 remains unstarted. Phase E remains
open.

`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` IS ready for its architecture
assessment.

DOTTED-EXTENSION-SURFACE FTR READINESS INDEPENDENTLY ACCEPTED --
READY FOR FRESH ARCHITECTURE ASSESSMENT; E7 REMAINS GATED.
