===== FTR Correction Self-Review -- FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A

*Correction date:* 2026-07-30

*Scope:* Documentation-only correction responding to independent readiness
review commit `081edb4f449e990341ca13205fd631ef60991ac3`.

*Correction verdict:* The three important and two minor findings are addressed
in the FTR text. This self-review does not perform the architecture assessment,
select Option A--E, or declare the FTR ready. A fresh independent readiness
review is required before architecture assessment begins.

==== Repository state and reviewed history

The correction started with:

[cols="2,5"]
|===
| Item | Verified value

| Repository root
| `/home/stefan/repos/spreadsheet-handling/core`

| Branch
| `main`

| Starting HEAD
| `081edb4f449e990341ca13205fd631ef60991ac3`

| Starting worktree
| Clean; `git status --short` returned no output.

| Ancestry
| Authoring commit
  `b967028a7f497f4cef8bad22558ef1589e860b95` is the direct parent of review
  commit `081edb4f449e990341ca13205fd631ef60991ac3`; divergence from the requested
  review baseline was `0 0`.
|===

The complete authoring and review patches were read before editing. The
authoring commit added the 606-line FTR and 24 index lines. The review commit
added only the 657-line immutable review artifact. The correction does not
amend either commit and does not modify the first review artifact.

==== Finding dispositions

===== Important I-1 -- mandatory complete decision output

*Disposition:* Addressed.

The corrected Purpose, Evidence-gathering plan, Mandatory assessment outputs,
Scope, Acceptance criteria, and Suggested investigation sequence now require:

* one completed contract row for each of the eight maintained surfaces;
* every decision field requested by the review;
* an Options A--E decision matrix;
* an option or explicit hybrid for each non-deferred row;
* a derived-FTR decision and safe interim contract; and
* a complete bounded-deferral record for any row that cannot be decided.

The bounded-deferral rule requires the exact unresolved question, missing
evidence, named acquisition action, responsible owner/follow-up role, explicit
reopening trigger, safe interim contract, and follow-up-FTR decision. It
applies equally to Option E and all other provisional results. Generic "more
research needed" wording cannot complete the FTR.

===== Important I-2 -- assessment-only migration evidence

*Disposition:* Addressed.

The corrected FTR replaces migration execution/testing during assessment with:

* a read-only impact map across named representative repositories/scenarios;
* before/after configuration examples;
* compatibility feasibility and expected-break analysis;
* an executable later migration-test plan; and
* later success, rollback, and compatibility criteria.

Core/adoption changes, deprecation warnings, migration execution, and rollback
validation are reserved for separately approved implementation FTRs. Optional
disposable prototypes must answer a precise question, remain temporary and
uncommitted, avoid all adoption mutation, and be removed after evidence
collection. They are not mandatory.

===== Important I-3 -- reproducible census baseline

*Disposition:* Addressed.

The FTR now records the dated Core/adoption revision pins, literal tracked-YAML
search patterns, exclusions, classification rules, registry-versus-YAML
distinction, exact counts, and the unknown-external-consumer limitation.

All requested commit objects still exist. Core had advanced from the authoring
census baseline only by the readiness-review commit; every inspected adoption
repository was still at its pinned revision.

===== Minor M-1 -- macro flow in the primary inventory

*Disposition:* Addressed.

`Framework-owned macro flow` is now an explicit primary-inventory and mandatory
contract-matrix row. The FTR distinguishes:

* fixed orchestrator boundary phases: ingress, persistence projection, and
  final-cleanup executor placement; and
* application-owned private composition: schema maintenance constructing a
  private `BoundStep` after command/use-case selection.

Final cleanup is qualified as executing explicit commands whose requests may
originate in configured transformations. Schema maintenance is not described
as an automatic orchestrator phase. No generic declaration or permission
system is required by the correction.

===== Minor M-2 -- operational terminology

*Disposition:* Addressed.

The FTR defines `consumer`, `expert`, `internal`, `programmatic`, and
`framework-owned` as operational audience/ownership values for the mandatory
matrix. Expert YAML authors are consumers; programmatic/importable mechanisms
do not automatically receive stable public-API status.

==== Direction, neutrality, and scope check

Options A--E remain open. The correction selects no policy, does not treat
absence of local direct-factory use as removal evidence, retains direct-factory
stabilization as a legitimate result, and continues to classify plugins and
factories as trusted Python rather than a sandbox.

The FTR remains one coherent architecture decision. Direct callable and direct
`BoundStep` composition remain comparison/classification rows, while any
runtime reshape of either is explicitly routed to a separate implementation
FTR. Legend Blocks Acceptance Review 4/finalization remain first and
non-blocked; XRef stays separate; YAML normalization and CLI documentation must
consume rather than pre-decide the eventual accepted decision.

==== Census verification

The revision-pinned tracked-YAML result was:

[source,text]
----
core snapshot=b967028a7f497f4cef8bad22558ef1589e860b95 current=081edb4f449e990341ca13205fd631ef60991ac3 plugin=16 direct_colon=0 nested_target=0
demo snapshot=ea9ebada8219d1c69325312e0864b6d5ee640bb1 current=ea9ebada8219d1c69325312e0864b6d5ee640bb1 plugin=4 direct_colon=0 nested_target=0
worldbuilding snapshot=729349291703144316e346e1724c5547565bbe3e current=729349291703144316e346e1724c5547565bbe3e plugin=3 direct_colon=0 nested_target=0
ssdp snapshot=b7ef1eb06a0cb8b2bba5f4f0330064f002461317 current=b7ef1eb06a0cb8b2bba5f4f0330064f002461317 plugin=2 direct_colon=0 nested_target=0
msg_ssdp_rollout snapshot=b7ef1eb06a0cb8b2bba5f4f0330064f002461317 current=b7ef1eb06a0cb8b2bba5f4f0330064f002461317 plugin=2 direct_colon=0 nested_target=0
personal snapshot=af99cace41b8d15ad4cd36da28e84c108c725406 current=af99cace41b8d15ad4cd36da28e84c108c725406 plugin=0 direct_colon=0 nested_target=0
personal_data snapshot=50785952db96f0a6ea7def0d78e9bfb88195320e current=50785952db96f0a6ea7def0d78e9bfb88195320e plugin=0 direct_colon=0 nested_target=0
home-lab snapshot=270a452b16eec4c44b86270838012530c70ce904 current=270a452b16eec4c44b86270838012530c70ce904 plugin=0 direct_colon=0 nested_target=0
pages snapshot=7c854ee1327306245e3f9f1a123eb0b24af8ff9a current=7c854ee1327306245e3f9f1a123eb0b24af8ff9a plugin=0 direct_colon=0 nested_target=0
----

Core's 16 plugin occurrences split into four domain-contract-tooling and 12
Project Memory/internal-tooling occurrences.

The registry parse returned:

[source,text]
----
runtime_entries=40
step_registrations=34
string_target_registrations=34
bare_factories=6
descriptive_entries=40
----

==== Validation commands and exact results

===== Repository and history

[source,shell]
----
pwd
git rev-parse --show-toplevel
git branch --show-current
git rev-parse HEAD
git status --short
git merge-base --is-ancestor b967028a7f497f4cef8bad22558ef1589e860b95 081edb4f449e990341ca13205fd631ef60991ac3
git merge-base --is-ancestor 081edb4f449e990341ca13205fd631ef60991ac3 HEAD
git rev-list --left-right --count 081edb4f449e990341ca13205fd631ef60991ac3...HEAD
git diff --numstat b967028a7f497f4cef8bad22558ef1589e860b95^ b967028a7f497f4cef8bad22558ef1589e860b95
git diff-tree --no-commit-id --name-status -r b967028a7f497f4cef8bad22558ef1589e860b95
git diff --numstat 081edb4f449e990341ca13205fd631ef60991ac3^ 081edb4f449e990341ca13205fd631ef60991ac3
git diff-tree --no-commit-id --name-status -r 081edb4f449e990341ca13205fd631ef60991ac3
git show --no-ext-diff --format=fuller --find-renames --find-copies b967028a7f497f4cef8bad22558ef1589e860b95 --
git show --no-ext-diff --format=fuller --find-renames --find-copies 081edb4f449e990341ca13205fd631ef60991ac3 --
----

Result: exact Core root, branch `main`, starting HEAD `081edb4f...`, clean
starting tree, both ancestry checks exited `0`, and divergence was `0 0`.
Authoring numstat was `606 0` plus `24 0`; review numstat was `657 0`.
The authoring file set was the new FTR plus modified `_ftrs_open.adoc`; the
review file set was only the new immutable review artifact. Both complete
patches were read.

===== Revision-pinned tracked-YAML census

[source,shell]
----
repos=(core demo worldbuilding ssdp msg_ssdp_rollout personal personal_data home-lab pages)
revs=(b967028a7f497f4cef8bad22558ef1589e860b95 ea9ebada8219d1c69325312e0864b6d5ee640bb1 729349291703144316e346e1724c5547565bbe3e b7ef1eb06a0cb8b2bba5f4f0330064f002461317 b7ef1eb06a0cb8b2bba5f4f0330064f002461317 af99cace41b8d15ad4cd36da28e84c108c725406 50785952db96f0a6ea7def0d78e9bfb88195320e 270a452b16eec4c44b86270838012530c70ce904 7c854ee1327306245e3f9f1a123eb0b24af8ff9a)
plugin='^[[:space:]]*-[[:space:]]*step:[[:space:]]*plugin([[:space:]]|$)'
direct='^[[:space:]]*-[[:space:]]*step:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+'
nested='^[[:space:]]*target:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+'
for i in "${!repos[@]}"; do
  repo=${repos[$i]}
  rev=${revs[$i]}
  repo_path="../$repo"
  git -C "$repo_path" cat-file -e "$rev^{commit}"
  current=$(git -C "$repo_path" rev-parse HEAD)
  plugin_count=$( { git -C "$repo_path" grep -h -E "$plugin" "$rev" -- '*.yaml' '*.yml' 2>/dev/null || true; } | wc -l)
  direct_count=$( { git -C "$repo_path" grep -h -E "$direct" "$rev" -- '*.yaml' '*.yml' 2>/dev/null || true; } | wc -l)
  nested_count=$( { git -C "$repo_path" grep -h -E "$nested" "$rev" -- '*.yaml' '*.yml' 2>/dev/null || true; } | wc -l)
  echo "$repo snapshot=$rev current=$current plugin=$plugin_count direct_colon=$direct_count nested_target=$nested_count"
done
----

Result: every commit object existed; exact per-repository results are recorded
in the preceding census section.

===== Registry parse

[source,shell]
----
PYTHONPATH=src .venv/bin/python -c "import json; from pathlib import Path; from spreadsheet_handling.pipeline.registry import REGISTRY; from spreadsheet_handling.pipeline.types import StepRegistration; desc=json.loads(Path('registries/pipeline_step_registry.json').read_text(encoding='utf-8')); regs=[(name, entry) for name, entry in REGISTRY.items() if isinstance(entry, StepRegistration)]; strings=[(name, entry.target) for name, entry in regs if isinstance(entry.target, str)]; print(f'runtime_entries={len(REGISTRY)}'); print(f'step_registrations={len(regs)}'); print(f'string_target_registrations={len(strings)}'); print(f'bare_factories={len(REGISTRY)-len(regs)}'); print(f'descriptive_entries={len(desc[\"entries\"])}')"
----

Result: 40 runtime entries, 34 registrations, 34 string targets, six bare
factories, and 40 descriptive entries.

===== Focused factual baseline

[source,shell]
----
PYTHONPATH=src .venv/bin/pytest -o addopts='' -q \
  tests/unit/pipeline/test_plugins_dotted_path.py \
  tests/unit/pipeline/test_step_registration.py \
  tests/unit/pipeline/test_public_import_surface.py \
  tests/architecture/test_domain_ingress_boundary.py \
  tests/architecture/dependency_guards/test_pipeline_step_registry.py
----

Result: `89 passed in 0.26s`.

===== AsciiDoc and patch validation

[source,shell]
----
asciidoctor -o /tmp/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.corrected.html \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc
asciidoctor -o /tmp/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.html \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc
asciidoctor -o /tmp/ftrs_open.corrected.html docs/backlog/_ftrs_open.adoc
git diff --check
----

Result: the corrected FTR and correction self-review rendered with no warning
or error. `git diff --check` exited `0` with no output.
`_ftrs_open.adoc` rendered successfully but reported one pre-existing warning
from included `FTR-XREF-AXIS-MAPPINGS-P4A2.adoc` line 56: `unterminated listing
block`. This correction does not modify that file.

==== Files and release/lifecycle scope

The correction changes:

* `docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc`;
* `docs/backlog/_ftrs_open.adoc`; and
* this correction self-review.

It changes no runtime, registry, test, adoption, public behavior, or release
positioning, so no release-note entry is required. The FTR remains `New` and
active. No lifecycle cleanup, archive move, or implementation FTR creation is
appropriate before architecture assessment and its independent reviews.

CORRECTION COMPLETE; FRESH INDEPENDENT READINESS REVIEW REQUIRED
