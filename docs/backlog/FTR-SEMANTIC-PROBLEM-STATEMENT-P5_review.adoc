= FTR-SEMANTIC-PROBLEM-STATEMENT-P5 Review

*Review date:* 2026-05-27
*Reviewer:* Codex
*Reviewed FTR:* `docs/backlog/FTR-SEMANTIC-PROBLEM-STATEMENT-P5.adoc`
*Reviewed commit:* `c0cb1057d99e50c334575e599d3c276c9122782a`
*Reviewed repo:* `/home/stefan/repos/spreadsheet-handling/core`
*Branch:* `dev`

== Verdict

Ship.

The commit implements the FTR by elaborating the semantic-model problem
statement, the human / AI / legacy ambiguity discussion, and the Ambiguity
Policy with concrete FK-helper phase examples. The edit remains documentation
only, preserves the draft-scaffold marker and stable chapter anchors, and does
not move into runtime, registry, guard, release, FTR-lifecycle, or later
semantic-model FTR implementation work.

No follow-up fix is required before keeping `c0cb105`.

== Scope Reviewed

* Commit diff against `FTR-SEMANTIC-PROBLEM-STATEMENT-P5` goal, scope, and
  out-of-scope clauses.
* Changed sections in `docs/semantic_model/semantic_model.adoc`, with focus on
  Problem Statement, Human / AI / Legacy ambiguity, and Ambiguity Policy.
* Preservation of the semantic-model draft/scaffold note.
* Preservation, uniqueness, and order of the skeleton FTR's
  `[[semantic-model-...]]` top-level chapter anchors.
* Absence of changes to later semantic-model FTR scopes: External
  Inspirations, overlapping sets / lifecycle model, registry role, adoption
  strategy, contract matrices, and semantic guards.
* Absence of runtime code, tests, registries, release machinery, and
  backlog/FTR lifecycle changes.
* Accuracy of the FK-helper, docs/demo drift, registry relocation,
  prefix-fallback, and v1 -> v2 helper-policy lifecycle examples.

== Files Inspected

* `docs/backlog/FTR-SEMANTIC-PROBLEM-STATEMENT-P5.adoc`
* `docs/semantic_model/semantic_model.adoc`
* `docs/backlog/FTR-SEMANTIC-MODEL-SKELETON-P5_review.adoc`
* `docs/backlog/FTR-FK-HELPERS-POLICY-DRIVEN-PRIMITIVES-P5_review.adoc`
* `docs/backlog/FTR-FK-HELPER-DOCS-DEMO-REALIGNMENT-P5_review.adoc`
* `docs/backlog/registry_artifact_relocation_review.adoc`
* `src/spreadsheet_handling/domain/validations/fk_helpers.py`
* `src/spreadsheet_handling/domain/transformations/fk_helpers/drop.py`
* `src/spreadsheet_handling/domain/helper_policies.py`
* `registries/meta_registry.yaml`
* `registries/pipeline_step_registry.json`
* `docs/backlog/backlog.adoc` (render validation)

== Findings By Severity

=== Blocking

None.

=== Major

None.

=== Minor

None.

=== Nits / Observations

* The new inline references to review artifacts use filenames rather than
  stable AsciiDoc xrefs. This is acceptable for this draft-scaffold phase
  because the files exist in the backlog area and the FTR asked to reference
  existing review notes rather than reconstruct history inline. A later
  documentation-hardening pass may choose to convert these to xrefs.
* The illustrative YAML block uses project namespaces such as
  `_meta.helper_policies.fk`, but it is not current runtime configuration.
  The surrounding prose frames it as named semantic modes and the document
  remains explicitly non-authoritative. This is acceptable, though a later
  hardening pass could add an "illustrative, not parsed today" note if this
  example becomes a public teaching surface.
* The Ambiguity Policy now contains early project doctrine. That is within
  this FTR's scope: it defines when ambiguity must become explicit versus
  tolerated, without adding contract matrices, guards, registry rules, or
  runtime enforcement.

== Scope Control Assessment

The commit changes one file only:

* `docs/semantic_model/semantic_model.adoc`

The diff is limited to the area from `[[semantic-model-problem-statement]]`
through the end of `[[semantic-model-ambiguity-policy]]`. Later top-level
semantic-model chapters were not edited. The unchanged later chapters still
look like scaffold material for their own FTRs, not implemented policy:

* External Inspirations remains inspirational and non-normative.
* Overlapping Semantic Sets and Lifecycle And Update Semantics remain
  candidate/question-driven.
* Registry Role still describes possible roles and warns against accidental
  runtime/rule-input promotion.
* Contract Matrices and Semantic Guards remain future/pilot-oriented.
* Adoption Strategy remains a no-big-bang scaffold.

No runtime code, tests, registries, release files, backlog includes, FTR
status, archive/cold-storage state, or guard machinery changed in the reviewed
commit.

== Example Accuracy Assessment

The project examples are concrete enough and align with the inspected review
artifacts and current runtime state.

* FK-helper missing-policy ambiguity: accurate. The
  `FTR-FK-HELPERS-POLICY-DRIVEN-PRIMITIVES-P5` review records the original
  `validate_fk_helpers` mismatch and the follow-up resolution. Current
  `validate_fk_helpers` raises a missing-policy error when neither v2 policy
  nor helper provenance is present.
* Stale registry consumer lists: accurate. The FK-helper review records stale
  derived-provenance consumers, and its follow-up review records the registry
  correction.
* Demo/docs drift after runtime contract changes: accurate. The
  docs/demo realignment review shipped with minor fixes for active stale
  doc surfaces outside the main modified set.
* Registry relocation as operational-artifact visibility signal: accurate
  enough. The relocation review confirms root-level registry artifacts, updated
  active references, and no registry-semantic rewrite.
* Prefix-fallback deprecation / migration truth: accurate. Current
  `remove_fk_helpers` uses provenance or v2 policy and retains `prefix` only
  for binding compatibility; the prior fallback history is used as a
  migration-truth example, not as a claim that prefix cleanup remains active.
* v1 -> v2 helper-policy lifecycle example: accurate enough for a motivating
  lifecycle example. Current producer code still writes the existing
  per-target `helper_policies.fk.<target>` shape and also applies v2
  `helper_policies.fk.relations`, so the lifecycle/current-vs-target
  discussion is grounded in actual project state.

No illustrative example was found to claim an unsupported current runtime
contract. The YAML semantic-mode example is aspirational/illustrative, not
presented as accepted pipeline or runtime config.

== Residual Risks

* *Inline filenames instead of stable xrefs:* acceptable for this phase.
  The risk is reader navigation, not semantic correctness or render stability.
* *Illustrative YAML could be mistaken for runtime config:* acceptable for this
  draft review because the document states it is not authoritative and the
  example is framed as semantic-mode guidance. No immediate fix required.
* *Ambiguity Policy is more doctrinal than a pure scaffold:* acceptable and
  expected for this FTR. The doctrine is narrow and does not implement later
  FTR mechanisms.

== Anchor And Structure Check

The skeleton FTR's top-level chapter structure and stable anchors are
preserved. The document still has the same 18 top-level anchors in the same
order:

. `semantic-model-management-summary`
. `semantic-model-relation-to-other-models`
. `semantic-model-problem-statement`
. `semantic-model-ambiguity-policy`
. `semantic-model-external-inspirations`
. `semantic-model-project-semantic-model`
. `semantic-model-overlapping-semantic-sets`
. `semantic-model-lifecycle-and-update-semantics`
. `semantic-model-registry-role`
. `semantic-model-roundtrip-walkthroughs`
. `semantic-model-conflict-and-precedence-questions`
. `semantic-model-contract-matrices`
. `semantic-model-semantic-guards`
. `semantic-model-versioning-and-evolution`
. `semantic-model-adoption-strategy`
. `semantic-model-planned-ftrs`
. `semantic-model-open-questions`
. `semantic-model-glossary`

No duplicate `[[semantic-model-...]]` anchors were found.

== Validation Commands And Results

[cols="2,1,4"]
|===
|Command |Result |Notes

|`git status --short`
|clean
|Clean before creating this review artifact.

|`git diff --check c0cb105^..c0cb105`
|pass
|No whitespace errors.

|`git show --stat --oneline --decorate --name-status c0cb1057d99e50c334575e599d3c276c9122782a`
|pass
|One modified file: `docs/semantic_model/semantic_model.adoc`.

|`git diff --name-only c0cb105^..c0cb105`
|pass
|Only `docs/semantic_model/semantic_model.adoc`.

|`asciidoctor --failure-level=ERROR -o /tmp/semantic_model.html docs/semantic_model/semantic_model.adoc`
|pass
|Exit 0, no output.

|`asciidoctor --safe-mode=unsafe --failure-level=ERROR -o /tmp/backlog.html docs/backlog/backlog.adoc`
|pass
|Exit 0, no output.

|`make docs-html`
|pass
|Exit 0. Rendered `docs/semantic_model/semantic_model.adoc` to
 `build/doc/semantic_model/semantic_model.html`. Existing section-level
 AsciiDoc warnings were emitted in other guides/cold-storage documents and are
 unrelated to this commit.

|`rg -n "^\\[\\[semantic-model-[^]]+\\]\\]" docs/semantic_model/semantic_model.adoc`
|pass
|18 anchors found; no duplicates detected during inspection.
|===

== Final Verdict

Ship.

`c0cb105` is ready to keep. No follow-up fix is required for this FTR before
continuing with the next semantic-model phase work.
