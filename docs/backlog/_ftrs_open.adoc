== Current Focus

This is the current near-term execution order. It supersedes older local
"recommended order" statements where they conflict.

. Close or archive any remaining review-cleanup items whose implementation has
  already landed.
. Decide whether one bounded presentation-meta roundtrip family should be
  pulled forward.
. Prefer text wrapping as the first candidate; keep borders, fills, and row
  heights outside the immediate slice.
. Evaluate model-default configuration now that the adoption result is known.
. Update the demo only after the realistic worldbuilding path is green and
  understood.

Boundary:

* Workbook-view adoption uses an existing capability and does not constitute a
  new visible-structure feature.
* The worldbuilding adoption is closed with XLSX and ODS parity and no core or
  carrier defect.
* New presentation-meta implementation remains gated by an explicit decision
  after the adoption result.
* Domain-contract registry expansion remains trace-driven; do not continue it
  without a concrete implementation or feature path.


== Phase 3d: Consumer Ergonomics (Adoption-Driven)

Purpose:

* House strategic follow-up surfaced by the worldbuilding consumer adoption
  (Adoption-001).
* Treat consumer-side workarounds as signals: promote them to platform
  capability only when the pattern recurs and the framework story benefits.
* Continue from the closed projection foundation / decision work rather than
  reopening it.

Current state:

* `FTR-PROJECTED-FRAME-COLUMN-SEMANTICS-P5` is closed.
* `FTR-DYNAMIC-FRAME-PROJECTION-P5` is closed.
* `FTR-DYNAMIC-FRAME-PROJECTION-IMPL-P5` is accepted and archived
  (`project_by_role`, the shared role resolver, and the worldbuilding YAML
  migration all landed; the migration-parity review gap is closed). Its
  `project_by_role` step was subsequently retired by
  `FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2`; the shared `resolve_column_roles`
  resolver remains.
* `FTR-DYNAMIC-VIEW-COLUMN-TARGETING-P5` and
  `FTR-DYNAMIC-VIEW-COLUMN-TARGETING-IMPL-P5` are archived as completed
  decision / implementation work.

Recommended order:

* Complete or archive remaining projection / presentation review gaps.
* `FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5` and
  `FTR-WORKBOOK-VIEW-DOCUMENTATION-AND-CONTRACT-COVERAGE-P5` completed with
  XLSX/ODS adoption evidence, a practical generic User Guide workflow, and no
  core, carrier, registry-placement, or guard gap.
* Classify surfaced findings as configuration, documentation, backend defect,
  roundtrip defect, or missing capability.
* Decide whether `FTR-TEXT-WRAP-ROUNDTRIP-P5` may be activated.
* Evaluate model-default configuration after the adoption result is known.
* Coordinate broader default expansion with Phase 4 semantic-classification
  work.

`FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2` is complete: all four slices were
independently accepted (Slice-4 acceptance review 002) and the whole FTR family
is archived under `docs/cold_storage/backlog/ftrs_done/`. The lookup/helper
family (`add_lookup_helpers` + `apply_derived_column_policy`) replaced
`project_by_role` at the ten worldbuilding sites and the step was removed; the
shared `resolve_column_roles` resolver is retained for validation targeting. See
the Phase 3d current-state note above.

include::FTR-MODEL-DEFAULT-CONFIGURATION-P5.adoc[leveloffset=-2]

'''


== Longer-Horizon Stabilization Program

The program remains the longer-horizon stabilization direction. Bounded
adoption and defect-closure slices may run before it where they exercise
already-landed capabilities without introducing a new semantic contract.

[NOTE]
====
This file is the active backlog include surface. Only open work belongs here.
Done, Fixed, Superseded, or historical workstream artifacts must leave this file
and move to cold storage with their companion artifacts.
====

Rules:

* Keep only open, new, postponed, or future FTRs and BUGs here.
* Move completed FTRs and their review artifacts to
  `docs/cold_storage/backlog/ftrs_done/`.
* Move dropped, rejected, or superseded FTRs to
  `docs/cold_storage/backlog/wont_do/`.
* Do not keep historical archaeology comments here.
* Use the project evolution chapter for past context.
* Keep includes so readers and agents can jump to active ticket details.

Current release posture (2026-06):


* `v0.2.1` is the latest released version.
* The next unreleased version is not yet numbered.

'''

== Next Program Sequence

This is the agreed near-term program ordering. It threads through the numbered
phases below (it does not renumber them): model and metadata contract first, then
the ODS capability contract, then carrier-neutral hygiene, then ODS internals,
and only then the later visible-structure features. Read this first for "what
comes before what".

Rules:

* Modeling / inventory slices carry no runtime implementation.
* Adapter stabilization is behavior-preserving and guard-backed.
* Later visible-structure features stay blocked until their model and adapter
  prerequisites land.
* `sparse_defaults` remains optional / parallel; the June 2026 family inventory
  provides the bounded need for a lightweight Functional Model catch-up FTR.
* XML adapter follow-up is out of scope for this program.

*Phase A -- Model and metadata contract stabilization* (modeling / inventory
only; lives in Phase 4 below):

. `FTR-FUNCTIONAL-MODEL-VIEW-DERIVATION-FAMILIES-P5` -- grouped view /
  projection family catch-up.
. `FTR-FUNCTIONAL-MODEL-SUPPORTING-CAPABILITY-FAMILIES-P5` -- supporting
  capability triage / catch-up.
. `FTR-META-FACET-CLASSIFICATION-P5` -- registry / docs prep; sibling of
  `FTR-PAYLOAD-SEMANTICS-RECOVERY-P5`, under the
  `FTR-META-LIFECYCLE-INVENTORY-P5` umbrella.

*Phase B -- ODS capability and parity contract* (lives in Phase 6 below):

. `FTR-ODS-CAPABILITY-PROFILE-P5` -- docs / data only.
. `FTR-ODS-FREEZE-READBACK-P3K` -- freeze-readback decision / closure
  (re-anchored existing FTR).

*Phase C -- Carrier-neutral backend hygiene* (behavior-preserving; lives in
Phase 6 below):

. `FTR-IO-BACKENDS-DOMAIN-CROSSING-GUARD-P5`
. `FTR-CARRIER-BLOB-DECODER-CONSOLIDATION-P4A`
. `FTR-CARRIER-LEGEND-META-HELPERS-CONSOLIDATION-P4A`

*Phase D -- ODS internals, behavior-preserving stabilization* (lives in Phase 6
below):

. `FTR-ODS-PRESENTATION-CELL-WALK-CONSOLIDATION-P4A`
. `FTR-ODS-PRESENTATION-SUBMODULE-EXTRACTION-P4A`
. `FTR-ODS-RENDERER-MODULARIZATION-P3K` -- behavior-preserving (re-anchored
  existing FTR).

*Phase E -- Later projection / structure features* (blocked until A--D; lives in
Phase 3b / Phase 7 below):

The XRef axis model and the grouped-XRef capability are complete:
`FTR-XREF-AXIS-MAPPINGS-P4A2` (GX-1..GX-5) is Done and archived, and the absorbed
`FTR-XREF-GROUPED-COLUMNS-P4A2` is superseded and archived to
`docs/cold_storage/backlog/wont_do/`. The remaining later features are:

. `FTR-COLUMN-GROUP-RENDER-CAPABILITY-P5` -- backend-neutral render capability.
. `FTR-NESTED-LISTS` -- last; still blocked.

'''

== Immediate Cleanup / Review Closure

Purpose:

* Close implementation-review gaps left by already-landed work.
* Keep shipped-but-not-reviewed-clean slices visible until their remaining
  evidence requirement is satisfied or explicitly descoped.

Recommended order:

* Resolve the remaining `BUG-COLUMN-LEVEL-PRESENTATION-META-READBACK-P5`
  text-orientation fallback edge, or narrow the bug to that edge explicitly.

include::BUG-COLUMN-LEVEL-PRESENTATION-META-READBACK-P5.adoc[leveloffset=-2]

'''

== Post-Legend Pipeline Extension Architecture

Purpose:

* Assess the intentional consumer, expert, internal, and programmatic dotted
  extension surfaces exposed by the pipeline.
* Preserve the toolkit responsibility model while distinguishing
  configuration addressability from importability, public API, semantic
  correctness, and security.
* Keep this architecture decision separate from the bounded Legend Blocks
  ownership correction.

Sequence:

* Acceptance Review 4 and Legend Blocks finalization proceed first and must not
  wait for this FTR.
* The corrected FTR requires a fresh independent readiness review before its
  architecture assessment begins.
* The FTR performs evidence gathering and contract assessment only.
* Any runtime, deprecation, registry, or adoption migration requires a later
  separately approved implementation slice.

include::FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc[leveloffset=-2]

'''

== Phase 3b: Helper Column Presentation

*Status:* Optional / deferrable. Phase 3a FK-helper semantics are closed.

Purpose:

* Add an optional, backend-neutral collapsible helper-column presentation
  capability so spreadsheet users can fold helper columns out of the way.
* Keep presentation strictly downstream of helper identity and provenance.
  Column grouping must never be used as a substitute for helper identity.
* Keep spreadsheet adapter concerns behind Render IR operations; FK-helper
  code must not know XLSX or ODS carrier details.

Recommended order:

* Add a backend-neutral column-group render capability (`SetColumnGroup` op,
  XLSX rendering, ODS rendering or an explicit accepted capability gap in the
  backend capability profile).
* Let FK-helper configuration emit optional helper-column grouping intent,
  consuming the policy / provenance established in Phase 3a.
* Realign demo and documentation for collapsible helper columns.

Boundary rules:

* The render capability is generic; no FK-specific terminology in the
  RenderOp.
* No cross-adapter shared carrier helper that imports XLSX assumptions into
  ODS or ODS assumptions into XLSX.
* No readback / persistence of user-expanded / collapsed UI state.

include::FTR-COLUMN-GROUP-RENDER-CAPABILITY-P5.adoc[leveloffset=-2]

'''

include::FTR-FK-HELPER-COLUMN-GROUP-META-P5.adoc[leveloffset=-2]

'''

include::FTR-FK-HELPER-GROUPING-DOCS-DEMO-P5.adoc[leveloffset=-2]

'''

== Phase 3c: Post-Beta / Exploratory

Purpose:

* Keep post-beta projection follow-up visible without letting it drive current
  work.

include::FTR-CONDITIONAL-FOLLOW-UP-PROJECTION-FLOWS-P5.adoc[leveloffset=-2]

'''

== Phase 4: Semantic and Metadata Stabilization

Purpose:

* Continue making metadata meaning explicit before and during further
  refactoring.
* Clarify authority, provenance, persistence, and reconstruction behavior.
* Consolidate already-discovered boundary rules into inventories, contract
  matrices, and review rules.
* Keep this phase small enough to unblock later maintainability work.

Recommended order:

* Edit and normalize concept documentation.
* Resolve adoption-surfaced roundtrip stabilization bugs before adding more
  projection features.
* Derived provenance lifecycle.
* Meta schema validation.
* Conflict and rename semantics.
* Broader metadata schema evolution.

Execution model:

For Phase 4 FTRs that touch existing producers, primitives, or registry roots,
use the semantic-refactor loop documented in
`docs/developer_guide/ch08_git_and_workflow/01_git_and_workflow.adoc`
("FTR Slicing for Metadata and Registry Changes"): clarify minimal semantics
-> add guard / test -> narrow refactor -> docs / demo update -> stop. A loop
is one FTR or a tightly-coupled FTR pair, not the whole phase.

include::FTR-FUNCTIONAL-MODEL-VOCABULARY-AND-CONVENTIONS.adoc[leveloffset=-2]

'''

include::FTR-PRESENTATION-DEFAULTS-SEMANTIC-CLASSIFICATION-P5.adoc[leveloffset=-2]

'''

include::FTR-CONCEPT-DOCS-CLEANUP-P5.adoc[leveloffset=-2]

'''

include::FTR-FK-HELPER-POLICY-LIFECYCLE-P4A.adoc[leveloffset=-2]

'''

include::FTR-LEGEND-BLOCKS-ENTRY-CONTRACT-P5.adoc[leveloffset=-2]

'''

include::FTR-ROUNDTRIP-TEST-LAYER-P4A.adoc[leveloffset=-2]

'''

include::FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A.adoc[leveloffset=-2]

'''

include::FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A.adoc[leveloffset=-2]

'''

include::FTR-DOMAIN-BOUNDARY-ROBUSTNESS-P4A.adoc[leveloffset=-2]

'''

include::FTR-TRUSTED-INGRESS-P4A.adoc[leveloffset=-2]

'''

[NOTE]
====
`FTR-XREF-PHYSICAL-LABEL-METADATA-AUTHORITY-P4A` was a direct, now-complete
prerequisite of `FTR-TRUSTED-INGRESS-P4A` E3. Its architecture decision
(independently accepted, `5159044`), its bounded runtime implementation
(`3f03ca2`, `fc3d288`, `f12283e`), and that implementation's independent
acceptance (`9fe6df5`) together resolved the conflict between XRef's
published non-string physical-label authority and E3's bounded metadata
substrate. The one final bounded pre-E3 census disposition this required is
also independently accepted (`5139334`): `TRUSTED-INGRESS-E3-CENSUS-FOLLOWUP-F1`
is resolved and the pre-E3 metadata census is independently accepted. E3
metadata substrate admission itself is independently accepted (`6dfef82`),
`E4 -- exact producer-transition representation and bounded
certificates` itself is independently accepted (`0f74d55`), and
`E5 -- framework-managed macro wiring` itself is now also independently
accepted (`ef84add`); `E6 -- programmatic-surface contract and API
publication` is the current next Phase-E implementation slice
(`docs/backlog/roadmap_domain_hardening_sequence.adoc`, "Phase E").
This FTR's include remains below for reference; moving it and its review
artifacts to `docs/cold_storage/backlog/ftrs_done/` and removing this include
is a separate, not-yet-performed archival follow-up.
====

include::FTR-XREF-PHYSICAL-LABEL-METADATA-AUTHORITY-P4A.adoc[leveloffset=-2]

'''

[NOTE]
====
`FTR-TRUSTED-INGRESS-DOCUMENTATION-CONDENSATION-P4A` is a future Phase-E
closure obligation, not a prerequisite placed ahead of E2-E7. After Phase E is
implemented and independently accepted, it promotes actual Trusted-Ingress
runtime truth and must close before Domain Maintainer Contract, Phase F / the
first family pilot, or D-T3 begins.
====

include::FTR-TRUSTED-INGRESS-DOCUMENTATION-CONDENSATION-P4A.adoc[leveloffset=-2]

'''

include::FTR-FORMULA-INGRESS-ROUNDTRIP-WHITELIST-P5.adoc[leveloffset=-2]

'''

include::FTR-NATIVE-CELL-VALUE-ROUNDTRIP-P5.adoc[leveloffset=-2]

'''

include::FTR-META-CONTRACT-VERSIONING-P4A.adoc[leveloffset=-2]

'''

include::FTR-META-SCHEMA-VALIDATION-P5.adoc[leveloffset=-2]

'''

include::FTR-WORKBOOK-VIEW-PAYLOAD-CONFLICT-PRECEDENCE-P6.adoc[leveloffset=-2]

'''

include::FTR-RENAME-RECONCILIATION.adoc[leveloffset=-2]

'''

include::FTR-META-SCHEMA-V2.adoc[leveloffset=-2]

'''

include::FTR-VALIDATION-COMPACT.adoc[leveloffset=-2]

'''

include::FTR-GRAPH-TOPOLOGY-VALIDATIONS-P5.adoc[leveloffset=-2]

'''

[NOTE]
====
*Next program -- Phase A (model and metadata contract before code).*
Modeling / inventory only; no runtime implementation. See the
<<_next_program_sequence,Next Program Sequence>> section above.
====

include::FTR-FUNCTIONAL-MODEL-VIEW-DERIVATION-FAMILIES-P5.adoc[leveloffset=-2]

'''

include::FTR-FUNCTIONAL-MODEL-SUPPORTING-CAPABILITY-FAMILIES-P5.adoc[leveloffset=-2]

'''

include::FTR-META-FACET-CLASSIFICATION-P5.adoc[leveloffset=-2]

'''

include::FTR-PAYLOAD-SEMANTICS-RECOVERY-P5.adoc[leveloffset=-2]

'''

== Phase 5: Maintainability and Architecture Hygiene

Purpose:

* Repay maintainability debt after semantic terms are clearer.
* Keep refactors behavior-preserving and guard-backed.
* Improve module readability for humans and AI.

Recommended order:

* Refresh guard inventory.
* Add lightweight module headers.
* Set maintainable public entry points and module budgets.
* Do targeted modularization only where semantics are already clear.

include::FTR-GUARDRAIL-AUDIT-P5.adoc[leveloffset=-2]

'''

include::FTR-REVIEW-001-MODULE-HEADERS-P5.adoc[leveloffset=-2]

'''

include::FTR-MAINTAINER-ENTRYPOINTS-AND-MODULE-BUDGETS-P5.adoc[leveloffset=-2]

'''

include::FTR-REVIEW-001-MODULARIZATION-P4A.adoc[leveloffset=-2]

'''

include::FTR-DOMAIN-CONSOLIDATION.adoc[leveloffset=-2]

'''

include::FTR-CELL-CODEC-LEGACY-CODE-ROW-RETIREMENT-P4A.adoc[leveloffset=-2]

'''

include::FTR-COMPACT-MULTIAXIS-TRACE-LIFECYCLE-COMMANDS-P5.adoc[leveloffset=-2]

'''

include::FTR-REVIEW-001-META-BOUNDARY-CLEANUP-P5.adoc[leveloffset=-2]

'''

include::FTR-IO-BACKEND-KIND-USER-INTERFACE-SMOOTHING-P5.adoc[leveloffset=-2]

'''

include::FTR-CLI-STEPS-DEPRECATION-P3G.adoc[leveloffset=-2]

'''

include::FTR-VALIDATION-MODE-ALIASES-P3G.adoc[leveloffset=-2]

'''

include::FTR-YAML-SURFACE-NORMALIZATION-P5.adoc[leveloffset=-2]

'''

include::FTR-LOCAL-DEV-ENVIRONMENT-ERGONOMICS-P5.adoc[leveloffset=-2]

'''

include::FTR-META-LIFECYCLE-INVENTORY-P5.adoc[leveloffset=-2]

'''

[NOTE]
====
*Per-family lifecycle slices under the inventory umbrella.*

Each transformation family is recovered as its own slice following the
process documented in
`docs/developer_guide/ch10_project_evolution/03_domain_comprehension_and_semantic_refactoring_wave.adoc`.
First execution: `FTR-FK-HELPER-POLICY-LIFECYCLE-P4A` (above). Each slice
carries an `_inventory.adoc` and, on implementation, an `_impl_review.adoc`
and the mandated `_review.adoc`.
====

== Phase 6: Backend and Packaging Hardening

Purpose:

* Finish known backend hardening after semantics are stable.
* Keep backend capabilities explicit instead of forcing false parity.
* Decide optional dependency and distribution posture.

Recommended order:

* ODS renderer structure.
* ODS freeze readback.
* Optional backend dependencies.
* Packaging experiments.

[NOTE]
====
`FTR-XLSX-PARSER-RESOURCE-LIMITS-P4A` is independent serialized-input
hardening work. It is not a Phase-E Trusted Ingress slice and does not block E2;
Phase E may proceed independently. Complete public/untrusted XLSX upload
readiness must not be claimed while this FTR remains open.
====

include::FTR-XLSX-PARSER-RESOURCE-LIMITS-P4A.adoc[leveloffset=-2]

'''

include::FTR-ODS-CAPABILITY-PROFILE-P5.adoc[leveloffset=-2]

'''

include::FTR-ODS-FREEZE-READBACK-P3K.adoc[leveloffset=-2]

'''

include::FTR-IO-BACKENDS-DOMAIN-CROSSING-GUARD-P5.adoc[leveloffset=-2]

'''

include::FTR-CARRIER-BLOB-DECODER-CONSOLIDATION-P4A.adoc[leveloffset=-2]

'''

include::FTR-CARRIER-LEGEND-META-HELPERS-CONSOLIDATION-P4A.adoc[leveloffset=-2]

'''

include::FTR-REVIEW-001-MULTIHEADER-READBACK-P5.adoc[leveloffset=-2]

'''

include::FTR-ODS-PRESENTATION-CELL-WALK-CONSOLIDATION-P4A.adoc[leveloffset=-2]

'''

include::FTR-ODS-PRESENTATION-SUBMODULE-EXTRACTION-P4A.adoc[leveloffset=-2]

'''

include::FTR-ODS-RENDERER-MODULARIZATION-P3K.adoc[leveloffset=-2]

'''

include::FTR-OPTIONAL-BACKEND-DEPS-P3K.adoc[leveloffset=-2]

'''

include::FTR-DISTRIBUTION-PEX.adoc[leveloffset=-2]

'''

== Phase 7: Transformation Extensions

Purpose:

* Add transformation capabilities deferred from beta work.
* Keep primitive relation transforms separate from composite workbook
  workflows.
* Revisit ordering before implementation.

Recommended order:

* XRef axis mapping and grouped columns are complete (see the Phase E note
  above); the absorbed `FTR-XREF-GROUPED-COLUMNS-P4A2` is superseded and archived.
* Compact XRef usage slice.
* Partition-by-domain and nested JSON work only after the simpler axis work is
  clear.

include::FTR-COMPACT-XREF-USE-CASE-SLICE-P4A2.adoc[leveloffset=-2]

'''

include::FTR-PARTITION-BY-DOMAIN.adoc[leveloffset=-2]

'''

include::FTR-JSON-BUNDLER.adoc[leveloffset=-2]

'''

include::FTR-NESTED-LISTS.adoc[leveloffset=-2]

'''

include::FTR-ORDERED-LISTS.adoc[leveloffset=-2]

'''

include::FTR-JSON-SHARDING-BY-PARTITION.adoc[leveloffset=-2]

'''

== Phase 8: Release Automation and Public Surfaces

Purpose:

* Improve release mechanics after the manual post-release lifecycle has proven
  stable.
* Keep publication work separate from core feature work.

Recommended order:

* Clarify documentation surface architecture: which artifacts are
  release-versioned vs latest-oriented
  (`FTR-USER-FACING-DEMO-AND-INTEGRATION-DOCUMENTATION-P5`).
* Decide release automation / Pages archive policy.
* Demo automation.
* Public-page and messaging work only after documentation surfaces are stable.

include::FTR-USER-FACING-DEMO-AND-INTEGRATION-DOCUMENTATION-P5.adoc[leveloffset=+2]

'''

include::FTR-RELEASE-PROCESS-AUTOMATION-P5.adoc[leveloffset=+2]

'''

include::FTR-AUTORELEASE-POLICY.adoc[leveloffset=-2]

'''

include::FTR-DEMO-AUTOSYNC.adoc[leveloffset=-2]

'''

include::FTR-PROJECT-MARKETING.adoc[leveloffset=-2]

'''

== Phase 9: Later Ideas

Purpose:

* Keep speculative ideas visible without letting them drive current work.
* Revisit after semantic stabilization and adoption evidence.

include::FTR-CONSUMER-WORKFLOW-MANAGEMENT-UI.adoc[leveloffset=-2]

'''

include::FTR-PERF-LIMITS.adoc[leveloffset=-2]

'''

include::FTR-QUALITY-DASHBOARD-SONAR-PILOT.adoc[leveloffset=-2]

'''

include::FTR-EXCEL-ADDIN.adoc[leveloffset=-2]

'''

include::FTR-WEB-SERVICE.adoc[leveloffset=-2]
