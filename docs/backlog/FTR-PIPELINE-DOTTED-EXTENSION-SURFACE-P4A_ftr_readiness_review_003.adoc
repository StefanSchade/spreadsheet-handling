===== Fresh Independent Readiness Review 003 -- FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A

*Review date:* 2026-08-15

*Review type:* Fresh independent pre-architecture readiness review. This is
not an architecture assessment, does not select Option A/B/C/D/E, and
performs no implementation.

*Verdict:* READY FOR ARCHITECTURE ASSESSMENT.

==== 0. Process note -- pre-existing review artifact found at task start

The review task was framed as starting from HEAD
`ee0496de2ecd3a9ec87f62cb77d180abeda4bffa`
(`docs(trusted-ingress): advance lifecycle from E6 to E7`), with "no fresh
independent review" yet performed.

At the actual start of this review, repository HEAD was already one commit
ahead: `8a6249e422cfa73825f2e06f7c50df960bd3b934`
(`docs(FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A): accept assessment
readiness`), which had already added
`docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_readiness_review_002.adoc`
with a self-declared `READY FOR ARCHITECTURE ASSESSMENT` verdict, dated
2026-08-15.

This review does not treat that pre-existing artifact as authority, for the
same reason the task instructs this review not to trust the correction
self-review's claims: a document declaring itself an independent,
already-accepted result is not evidence of independence merely by asserting
it. Consistent with the task's own methodology (sections 3-5), every factual
claim in `..._readiness_review_002.adoc` that this review could check was
independently re-derived from source, tests, and git history *before* being
compared against that document's text, not copied from it. Where this
review's own commands reproduced the same numbers, that is stated as
independent corroboration, not as trust extended to the prior artifact.

No evidence was found that `..._readiness_review_002.adoc` is
factually inaccurate (see sections 2-7 below); this review's disagreement, if
any, would have been recorded as a finding rather than silently reconciled.
Because `_002` is already the sibling-review filename, and per this task's
own naming fallback ("if repository naming convention strongly suggests a
different unambiguous suffix, use that and report it"), this artifact is
filed as `_readiness_review_003`, matching the repo's existing `_00N`
incrementing convention (e.g.
`trusted_ingress_and_runtime_security_review_001/002/003.adoc`).

This process note is a repository-state observation for audit purposes, not
a finding against the FTR.

==== 1. Scope and current baseline

[cols="2,5"]
|===
| Item | Verified value

| Repository root
| `/home/stefan/repos/spreadsheet-handling/core`

| Branch
| `main`

| HEAD at review start
| `8a6249e422cfa73825f2e06f7c50df960bd3b934`
  (includes the pre-existing `_readiness_review_002.adoc` commit; see
  section 0)

| Starting worktree
| Clean; `git status --short` returned no output.

| FTR authoring commit
| `b967028a7f497f4cef8bad22558ef1589e860b95` -- confirmed ancestor of HEAD.

| First independent readiness review commit
| `081edb4f449e990341ca13205fd631ef60991ac3` -- confirmed ancestor of HEAD.

| Correction commit
| `3a9da4c1aa2285480f6242b7a27feb5fdf68d638`
  (`docs(FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A): close readiness review
  findings`) -- confirmed ancestor of HEAD.

| FTR/review files touched since the correction
| None. `git log --oneline b967028..HEAD -- docs/backlog/FTR-PIPELINE-DOTTED-
  EXTENSION-SURFACE-P4A.adoc docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-
  SURFACE-P4A_ftr_review.adoc docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-
  SURFACE-P4A_ftr_correction_review.adoc` returns exactly `3a9da4c` and
  `081edb4`, in that order, and nothing else. The FTR text this review reads
  is therefore byte-identical to the text the correction self-review
  describes; `..._readiness_review_002.adoc`'s addition did not touch any of
  the three files.
|===

Between the correction (2026-07-30) and this review (2026-08-15), Core
advanced through Phase-E Trusted Ingress E4 (exact producer-transition
representation), E5 (framework-managed macro wiring), and E6
(programmatic-surface/API publication, accepted at `7f1cadd`), plus the
`_readiness_review_002.adoc` commit itself. This review independently
re-establishes currency against that advanced baseline; it does not assume
the five-week-old correction snapshot, or the `_002` artifact's snapshot, is
still accurate.

`..._ftr_review.adoc` and `..._ftr_correction_review.adoc` are not
overwritten. Section 2 below reconstructs the first review's five findings
from that immutable artifact's own wording, independently of both the
correction's paraphrase and the `_002` artifact's paraphrase.

==== 2. Original finding disposition

[cols="1,4,4,2"]
|===
| Finding | Original concern (`..._ftr_review.adoc`, verbatim wording) | Independent verification against current FTR text | Result

| I-1
| "No criterion mandates one completed row per surface with intended
  audience, stability tier, supported syntax, trust/responsibility model,
  configuration-addressability policy, internal/consumer notation
  relationship, compatibility posture, and implementation-follow-up
  decision." Option E could complete via unresolved/provisional labeling
  without a bounded gate.
| Read directly from the current FTR (not from either later review): the
  "Mandatory final contract matrix" section requires exactly one row for
  each of the eight listed surfaces and names 13 mandatory decision columns
  per row (audience, syntax, construction/invocation timing, trust model,
  addressability policy, stability tier, compatibility commitment,
  internal/consumer relationship, diagnostics, selected Option/hybrid,
  follow-up-FTR yes/no, safe interim contract). The "Bounded-deferral rule"
  section requires, for *any* deferred row including Option E outcomes: the
  exact unresolved question, missing evidence, a named acquisition action,
  an owner/follow-up role, an explicit reopening trigger, the safe interim
  contract, and the follow-up-FTR decision -- and states explicitly "Option E
  cannot complete the assessment through a general 'clarify later'
  classification." "Mandatory assessment outputs" and "Acceptance criteria"
  items 1-3 restate and gate the identical requirement a third and fourth
  time. No path exists to skip a surface, leave a field unresolved, pick no
  option, or defer without every bounded field.
| RESOLVED

| I-2
| Acceptance required a migration plan "tested against representative
  adoption evidence" while Scope/Non-goals forbid adoption/runtime changes
  during assessment.
| "Read-only migration evidence" and "Mandatory assessment outputs" items 5-6
  require a read-only impact map, before/after configuration examples,
  compatibility feasibility analysis, and an executable *later*
  migration-test plan, explicitly reserving Core/adoption mutation,
  deprecation warnings, migration execution, and rollback validation for a
  separately approved implementation FTR. Acceptance criterion 11 restates
  this split and forbids Core/adoption mutation during assessment. Non-goals
  still forbid modifying adoption repositories. No contradictory text
  remains anywhere else in the FTR.
| RESOLVED

| I-3
| Counts and repository names were given without recorded revisions,
  tracked-file scope, literal search patterns, or exclusions.
| "Reproducible census baseline -- 2026-07-30" pins exact Core/adoption
  commit hashes per repository, states the `*.yaml`/`*.yml` tracked-file
  scope, gives the three literal extended-regexp patterns verbatim, and
  preserves the internal-tooling / adoption / test-and-doc /
  registry-representation classification split. Section 3 below
  independently re-ran the identical three patterns against current HEAD in
  all nine repositories and confirms they still reproduce the pinned-revision
  numbers exactly (see also the M-3-equivalent freshness note in section 4).
| RESOLVED

| M-1
| The "Existing surfaces" table omitted framework-owned macro flow.
| "Existing surfaces" (current FTR, final row) lists "Framework-owned macro
  flow" as an eighth row; "Mandatory final contract matrix" lists it as the
  eighth required row with an explicit instruction to classify fixed
  orchestrator boundary phases separately from application-owned private
  composition; the dedicated "Framework-owned macro-flow category" section
  keeps the subtype split and the final-cleanup "invocation ownership, not
  authorship" qualification.
| RESOLVED

| M-2
| `consumer`, `expert`, `internal`, `programmatic`, `framework-owned` were
  used consistently but undefined as decision labels.
| "Operational terms" defines all five terms, states explicitly that expert
  YAML authors are consumers, and that programmatic/importable reachability
  does not by itself establish stable public-API status.
| RESOLVED
|===

No original finding was collapsed or reinterpreted to force a match; each
result above was checked against the *first* review's own wording, reproduced
in this table, not against the correction's or `_002`'s paraphrase of it. No
later FTR section contradicts any of the five corrections.

==== 3. Current source/resolver inventory

Independently re-read at HEAD `8a6249e` (source files read directly, not
taken from either prior review's description):

* `pipeline/dotted_paths.py` -- one checked resolver,
  `resolve_configuration_callable`, gated by
  `_ensure_module_addressable` against exactly one blocked namespace tuple,
  `("spreadsheet_handling.domain.ingress",)`. `_split_reference` accepts
  `module:function` or `module.function` form.
* `pipeline/steps.py` -- `_resolve_target` calls
  `resolve_configuration_callable` only for string targets and returns
  non-string (direct callable) targets unchanged; `make_plugin_step`,
  `make_builder_target_step`, and `make_frames_target_step` all route through
  it. `make_frames_target_step` wraps the resolved callable in
  `types.BoundFramesTargetCall` (an E4-era addition) for exact-identity
  classification; this wraps the same resolved callable, it does not add a
  second resolution path.
* `pipeline/registry.py` -- `resolve_registration` detects a colon-bearing
  `step_id` and resolves it via `resolve_configuration_callable`; short names
  still resolve through `REGISTRY`.
* Whole-tree search: `grep -rn "importlib.import_module\|import_module(" src/`
  finds exactly four call sites: one in `pipeline/dotted_paths.py` (the
  pipeline resolver) and three in `io_backends/__init__.py` /
  `io_backends/router.py` (lazy backend-module loading, selected only after a
  user picks a fixed short backend kind such as `ods`/`xlsx`, not an
  arbitrary configuration-addressable dotted resolver). No other dynamic
  string-to-callable resolution mechanism exists anywhere under
  `src/spreadsheet_handling/`. This independently confirms no ninth maintained
  surface exists via an unaccounted resolver.
* No test file matching `*single*resolver*` or a dedicated "one resolver"
  architecture guard for `pipeline.dotted_paths` was found;
  `tests/architecture/dependency_guards/test_pipeline_step_registry.py`
  guards the step-registry contract shape, not resolver singularity. This
  matches the first review's finding and the gap the Legend Blocks Minor
  noted as textual-only; independent inventory again found no second
  resolver for such a guard to protect against. This remains an open
  question the architecture assessment itself may decide (FTR's own
  discussion of shared-vs-separate resolution), not a readiness defect.

Conclusion: the resolver/callable topology is unchanged in kind since
2026-07-30; no new resolution mechanism was introduced by E4/E5/E6.

==== 4. Eight-surface freshness check

Each row was independently re-verified against current source and a fresh
census, not copied from either prior review:

[cols="2,2,5"]
|===
| Surface | Result | Evidence (this review's own commands)

| Registered short name
| Current
| `REGISTRY` parse (section 6) returns 44 entries (38 `StepRegistration`,
  all string-targeted, plus 6 unchanged bare factories:
  `identity`, `validate`, `add_fk_helpers`, `remove_fk_helpers`,
  `validate_fk_helpers`, `plugin`). Growth from 40/34 is XRef/grouped-column
  registered steps, unrelated to dotted-extension mechanics and out of this
  FTR's own XRef non-goal.

| Plugin callable
| Current
| `make_plugin_step` mechanics unchanged; construction-time resolution via
  `resolve_configuration_callable`, execution calls `(frames, **args)`.

| Direct colon-style factory
| Current
| `resolve_registration`'s colon detection unchanged (source read, section
  3).

| Registry-owned string target
| Current
| All 38 `StepRegistration` entries carry string targets (parse result,
  section 6).

| Nested configured string target
| Current
| `make_builder_target_step`/`make_frames_target_step` still accept `target`
  as string-or-callable via the same `_resolve_target` path.

| Direct Python callable target
| Current
| `_resolve_target` still returns non-string callables unchanged.

| Direct `BoundStep` composition
| Current
| `orchestrate(..., steps=...)` and `run_pipeline` both still accept
  pre-bound `BoundStep` values (confirmed via `FTR-TRUSTED-INGRESS-P4A.adoc`
  46.1 programmatic-surface census, cross-read against source signatures).

| Framework-owned macro flow
| Current
| Fixed boundary phases (ingress, persistence projection, final-cleanup
  executor) and application-owned private composition (schema maintenance's
  private `BoundStep`) both still exist as described; independently
  cross-checked against `FTR-TRUSTED-INGRESS-P4A.adoc` 46.1's own schema-
  maintenance classification, which agrees.
|===

No ninth surface was found (section 3); no listed surface has disappeared.

*Adoption/config census re-run (this review's own execution, all nine
repositories present locally):*

[source,text]
----
core             plugin=16 direct_colon=0 nested_target=0  (unchanged)
demo             plugin=4  direct_colon=0 nested_target=0  (unchanged; pinned rev == current)
worldbuilding    plugin=6  direct_colon=0 nested_target=0  (was 3 at pinned rev; repo advanced to e6ca10a)
ssdp             plugin=2  direct_colon=0 nested_target=0  (unchanged; working tree has local, uncommitted changes -- tracked HEAD unchanged)
msg_ssdp_rollout plugin=2  direct_colon=0 nested_target=0  (unchanged)
personal         plugin=0  direct_colon=0 nested_target=0  (unchanged; working tree has local, uncommitted changes -- tracked HEAD unchanged)
personal_data    plugin=0  direct_colon=0 nested_target=0  (unchanged)
home-lab         plugin=0  direct_colon=0 nested_target=0  (unchanged; working tree has local, uncommitted changes -- tracked HEAD unchanged)
pages            plugin=0  direct_colon=0 nested_target=0  (unchanged)
----

`worldbuilding` doubled its plugin count (3 -> 6, new `dino_insel`
story-context pipelines) since the pinned census; no repository shows any
direct-colon or nested-target adoption use, now or previously. This is
numeric drift in the same qualitative direction the FTR already relies on
(plugin is real active adoption; direct-colon/nested-target remain locally
unobserved everywhere inspected); it does not change which architecture
option is plausible, and the FTR's own evidence-gathering plan already
requires the eventual assessment to re-run this exact census rather than
cite the printed 2026-07-30 numbers. Recorded as Minor below, not a
readiness blocker.

==== 5. E6/E7 alignment

Read in full (not summarized secondhand):
`docs/backlog/FTR-TRUSTED-INGRESS-P4A_E6_implementation_review.adoc`;
`FTR-TRUSTED-INGRESS-P4A.adoc` sections 46.1-46.15 and the "E7 -- less-trusted
description authorization" section; `docs/ai_info/interfaces_and_gates.adoc`;
`docs/technical_model/ch04_concepts/trusted_ingress_and_runtime_security.adoc`.

Independently confirmed by direct quotation from source, not from either
prior review:

* `FTR-TRUSTED-INGRESS-P4A.adoc` line ~7061-7072 (section 46.1) classifies
  the dotted-callable plugin/extension surface as: "Executes under the same
  trusted-pipeline-description precondition as the rest of the configured
  pipeline. STABLE protocol; `domain.ingress` is the one namespace this
  resolver refuses to address." "STABLE" here is read in context as "this
  mechanical calling convention exists now and will not be silently
  withdrawn without notice" -- the same axis the FTR's own "current
  mechanics" and "construction/invocation timing" columns already capture --
  not a foreclosure of the FTR's still-open audience/namespace/
  compatibility-tier decision. Every one of Options A-E keeps the mechanism
  as a documented, gated, trusted extension surface; none proposes silent
  unannounced withdrawal.
* `docs/ai_info/interfaces_and_gates.adoc` lines 32-35 state "this protocol
  is part of the intentional extension surface" and line 101 gates "Any
  behavior change in documented plugin step contracts" behind explicit
  approval. `git log --follow` traces this text to commit `d4921bd`
  (2026-05-12), which predates the FTR's own authoring commit (`b967028`,
  2026-07-30) -- this is not new E6 evidence contradicting the FTR; the FTR's
  authoring and first review already operated under it.
* `docs/technical_model/ch04_concepts/trusted_ingress_and_runtime_security.adoc`
  line 146-149 states this concept "must not preempt
  `docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc`", and line
  157 states its own inventory "does not preselect that FTR's Options A-E".
  Lines 1167-1170 state, verbatim: "No containment of arbitrary Python
  plugins or dotted-callable targets running in the same process --
  `pipeline/dotted_paths.py`'s one namespace exclusion is an
  ownership/architecture guard, explicitly not a security boundary, exactly
  as `FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` already states." This is
  independently-accepted architecture text agreeing with, not contradicting,
  the FTR's own "Trust and safety distinctions" section.
* `FTR-TRUSTED-INGRESS-P4A.adoc` section 46.9 states: "Phase E remains
  *Open*; E7 remains unstarted and unauthorized by this slice." The
  "E7 -- less-trusted description authorization" section's stated
  precondition is exactly "the still-open extension-surface assessment is
  independently accepted" plus "an independently reviewed narrow E7 design
  exists" plus "operator policy is represented for the evidenced capability
  kinds"; its acceptance criteria require forbidden-capability rejection
  before activation, direct-Python-forms-remain-trusted-only, and no Python
  sandbox claim -- all consistent with the Dotted-Extension FTR's own
  Non-goals ("make plugin/factory execution safe for untrusted Python",
  "create a Python sandbox... or dependency isolation mechanism").

No E6/E7 contract conflict was found. Both documents describe the same
current mechanics from different angles (compatibility-of-mechanism vs.
audience/ownership-of-mechanism) without contradicting each other.

*Decision-field sufficiency for E7:* the mandatory matrix's 13 columns
jointly cover seven of the nine facts E7 will eventually need per this
task's own list. Two -- "capability kind" and "identifier/parameter subset" --
have no literally-named column. Within this FTR's own bounded scope the
capability kind is uniform across all eight rows (import-and-invoke one
trusted Python callable; the FTR's Non-goals exclude any other capability
kind), and granular parameter/allowlist grammar is explicitly assigned to
E7's own separate "independently reviewed narrow E7 design" precondition,
not to this prerequisite. This is recorded as Minor M-4 below (clarity, not a
gap that would let an assessor satisfy the matrix while leaving E7 unable to
derive authorization timing or capability kind).

==== 6. Decision-template, deferral, and derived-FTR discipline

* *Options A-E matrix:* the five options (retain-and-document,
  explicit-syntax, plugin-only, namespace-policy, clarify-only) remain
  distinct, none is a strawman (each carries falsifiable test questions),
  hybrids are explicitly permitted, and every option's questions tie back to
  the same evidence axes named in the Evidence-gathering plan. No option was
  made obsolete by any current-HEAD evidence found in this review.
* *Bounded-deferral rule:* all six mandatory fields (unresolved question,
  missing evidence, acquisition action, owner/follow-up role, explicit
  trigger, safe interim contract) plus the follow-up-FTR decision are
  required together for every deferred row, explicitly including Option E
  and hybrid outcomes; nothing short of the full set is accepted.
* *Derived-FTR discipline:* "Mandatory assessment outputs" requires the
  derived-FTR list to include an explicit `none` result where applicable,
  and "Read-only migration evidence" requires success/rollback/compatibility
  criteria for any derived implementation FTR -- an assessment cannot
  discharge a decision by naming a future FTR without recording the decision
  already made and the interim contract governing the gap.

No gap was found in any of these three areas.

==== 7. Evidence and validation -- exact commands and results (this review)

===== Repository, ancestry, and file-touch history

[source,shell]
----
pwd
git rev-parse HEAD
git status --short
git merge-base --is-ancestor b967028a7f497f4cef8bad22558ef1589e860b95 HEAD
git merge-base --is-ancestor 081edb4f449e990341ca13205fd631ef60991ac3 HEAD
git merge-base --is-ancestor 3a9da4c1aa2285480f6242b7a27feb5fdf68d638 HEAD
git log --oneline b967028..HEAD -- \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_review.adoc \
  docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc
----

Result: root `/home/stefan/repos/spreadsheet-handling/core`, HEAD `8a6249e`,
clean tree, all three ancestry checks exit `0`; the file-touch log returns
exactly `3a9da4c` and `081edb4`.

===== Registry parse

[source,shell]
----
PYTHONPATH=src .venv/bin/python -c "import json; from pathlib import Path; \
from spreadsheet_handling.pipeline.registry import REGISTRY; \
from spreadsheet_handling.pipeline.types import StepRegistration; \
desc=json.loads(Path('registries/pipeline_step_registry.json').read_text(encoding='utf-8')); \
regs=[(n,e) for n,e in REGISTRY.items() if isinstance(e, StepRegistration)]; \
strings=[(n,e.target) for n,e in regs if isinstance(e.target, str)]; \
print(f'runtime_entries={len(REGISTRY)}'); print(f'step_registrations={len(regs)}'); \
print(f'string_target_registrations={len(strings)}'); \
print(f'bare_factories={len(REGISTRY)-len(regs)}'); \
print(f'descriptive_entries={len(desc[\"entries\"])}')"
----

Result: `runtime_entries=44`, `step_registrations=38`,
`string_target_registrations=38`, `bare_factories=6`, `descriptive_entries=44`.
Bare-factory names: `add_fk_helpers`, `identity`, `plugin`,
`remove_fk_helpers`, `validate`, `validate_fk_helpers`.

===== Reproducible tracked-YAML census (this review's own re-run, all nine repositories)

[source,shell]
----
for repo in core demo worldbuilding ssdp msg_ssdp_rollout personal personal_data home-lab pages; do
  path="../$repo"
  rev=$(git -C "$path" rev-parse HEAD)
  plugin=$(git -C "$path" grep -h -E '^[[:space:]]*-[[:space:]]*step:[[:space:]]*plugin([[:space:]]|$)' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  direct=$(git -C "$path" grep -h -E '^[[:space:]]*-[[:space:]]*step:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  nested=$(git -C "$path" grep -h -E '^[[:space:]]*target:[[:space:]]*[^[:space:]#]+:[^[:space:]#]+' -- '*.yaml' '*.yml' 2>/dev/null | wc -l)
  printf '%s current=%s plugin=%s direct_colon=%s nested_target=%s\n' "$repo" "$rev" "$plugin" "$direct" "$nested"
done
----

Result: see section 4's table. All nine repositories were present locally; no
repository bound needed to be documented as unavailable.

===== Focused current-surface tests

[source,shell]
----
PYTHONPATH=src .venv/bin/pytest -o addopts='' -q \
  tests/unit/pipeline/test_plugins_dotted_path.py \
  tests/unit/pipeline/test_step_registration.py \
  tests/unit/pipeline/test_public_import_surface.py \
  tests/architecture/test_domain_ingress_boundary.py \
  tests/architecture/dependency_guards/test_pipeline_step_registry.py
----

Result: `96 passed in 0.29s`.

[source,shell]
----
PYTHONPATH=src .venv/bin/pytest -o addopts='' -q \
  tests/unit/pipeline/test_programmatic_surface_contract.py
----

Result: `10 passed in 0.10s` (E6's programmatic-surface publication suite;
unrelated to this FTR, run to confirm E6's classification claims in section 5
are behaviorally pinned, not merely prose).

===== Resolver-inventory search

[source,shell]
----
grep -rn "importlib.import_module\|import_module(" src/spreadsheet_handling/ --include=*.py
----

Result: exactly four call sites -- one in `pipeline/dotted_paths.py`, three
in `io_backends/__init__.py`/`io_backends/router.py` (lazy backend loading).
No other dynamic-resolution mechanism exists in `src/`.

===== AsciiDoc and patch validation

[source,shell]
----
asciidoctor -o /tmp/.../FTR.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc
asciidoctor -o /tmp/.../review1.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_review.adoc
asciidoctor -o /tmp/.../correction.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_correction_review.adoc
asciidoctor -o /tmp/.../review002.html docs/backlog/FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A_ftr_readiness_review_002.adoc
git diff --check
----

Result: all four AsciiDoctor invocations exit `0` with no warnings; `git diff
--check` exits `0` with no output.

No `make test` was run; this is a documentation/readiness review and no
current evidence uncovered a reason requiring the full suite.

==== 8. Findings

*Blocking:* none.

*Important:* none.

*Minor:*

* *M-3 (non-blocking, informational):* the FTR's printed 2026-07-30 adoption
  census is now stale; `worldbuilding`'s plugin count has grown 3 -> 6 and
  Core's registry has grown 40 -> 44 entries (unrelated XRef/grouped-column
  registered steps). The qualitative evidence direction is unchanged (plugin
  is real active adoption everywhere it appears; direct-colon and
  nested-target remain unobserved in every inspected repository, then and
  now). No FTR text change is required; the FTR's own evidence-gathering
  plan already requires the assessment to re-run the census rather than cite
  the printed numbers, and this review's section 4/7 confirms the re-run
  patterns still work exactly as specified.
* *M-4 (non-blocking, clarity only):* the mandatory contract matrix has no
  column literally named "capability kind" or "identifier/parameter
  subset" -- two of the nine facts E7 will eventually need per this task's
  own list. Within this FTR's bounded single-capability-kind scope
  (import-and-invoke one trusted Python callable; no other capability kind is
  in scope per Non-goals) these are adequately forced by the existing
  "supported syntax" / "construction and invocation timing" /
  "trust/responsibility model" columns, and granular parameter/allowlist
  grammar is explicitly deferred to E7's own separate design precondition.
  No FTR text change is required for readiness; the assessment author should
  simply cross-reference "current mechanics" explicitly when filling
  trust/responsibility fields so the connection is legible to the future E7
  design reader.

Neither Minor item withholds the READY verdict; both are optional
improvements for the assessment author, not required FTR corrections.

*Process observation (not a Minor finding against the FTR):* see section 0.
The pre-existing `_readiness_review_002.adoc` artifact makes an identical
READY claim reached, on its face, through an equivalent independent method.
This review's own re-derivation from source, tests, and git history
corroborates its factual claims wherever checked; no discrepancy was found.
Two independently-dated "fresh independent readiness review" artifacts for
the same gate now exist on `main`. This does not change the verdict, but a
maintainer should be aware that this review could not itself observe how
`_002` was produced and has treated it as an unverified prior claim
throughout, per this task's own required method.

==== 9. Verdict

0 Blocking, 0 Important. All five original findings (I-1, I-2, I-3, M-1,
M-2) are independently RESOLVED, verified against the first review's own
wording and the current FTR text directly. The current-repository freshness
check (Phase-E E4/E5/E6, sections 3-5) finds no material unrepresented
surface, no ninth resolver, and no E6/E7 contract conflict. The two
non-blocking Minor items are informational only.

`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` IS ready for its architecture
assessment.

READY FOR ARCHITECTURE ASSESSMENT means only that the FTR may now begin its
fresh independent architecture assessment. It does not mean the FTR is
accepted, does not select Option A-E, and does not start E7. That assessment
must gather current evidence, complete all eight contract rows, complete the
Options A-E matrix, select an option/hybrid per non-deferred surface, resolve
or record bounded deferrals, decide any derived implementation FTR(s), and
define the safe interim contract -- and must itself pass independent review
before E7's prerequisite is satisfied.

E1-E6 remain independently accepted. E7 remains unstarted. Phase E remains
open.

==== 10. Release notes and lifecycle

This review adds one new documentation-only review artifact. It changes no
runtime, public/configuration surface, or release positioning, so no
release-note entry is required.

*Lifecycle cleanup check:* the reviewed FTR remains `New`/open; it is not
implemented and this is not a green implementation verdict, so no
`Done`/`Fixed`/`Superseded` transition, no `_ftrs_open.adoc` removal, and no
archive move applies. No residual scope is created by this review. The next
task is the fresh independent architecture assessment, not lifecycle
curation.

DOTTED-EXTENSION-SURFACE FTR READINESS INDEPENDENTLY ACCEPTED --
READY FOR FRESH ARCHITECTURE ASSESSMENT; E7 REMAINS GATED.
