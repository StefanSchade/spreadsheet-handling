= Independent Review -- Pre-E3 Metadata Node-Shape Census

*Review date:* 2026-08-13

*Review type:* Fresh-context independent census review only. This artifact
reviews `FTR-TRUSTED-INGRESS-P4A_E3_metadata_census.adoc`; it does not
implement E3, correct the census, change runtime/tests, alter lifecycle
documents, or reopen metadata architecture.

*Overall verdict:* *C. CHANGES REQUIRED -- COMPATIBILITY EVIDENCE
INCOMPLETE.*

== 1. Baseline and bounded scope

The review began on clean `main` at
`167ba2932192d012737baf3b695d767ec0e54393`
(`docs(trusted-ingress): record pre-E3 metadata node-shape census`). `main`
was ahead of `origin/main`; nothing was fetched, rebased, or pushed.

`git diff-tree --no-commit-id --name-status -r 167ba29` and
`git diff --name-status 167ba29^ 167ba29` both show exactly one addition:

....
A  docs/backlog/FTR-TRUSTED-INGRESS-P4A_E3_metadata_census.adoc
....

The parent and census commit were inspected as the exact before/after pair.
Every census conclusion was treated as a claim to falsify. The only question
decided here is whether current supported metadata behavior justifies E3
enforcing the proposed bounded node vocabulary without an unrecorded
compatibility break.

== 2. Accepted authority reconstructed independently

`TRUSTED-INGRESS-DESIGN-REVIEW-F4` originally found that the proposed exact
dict/list/scalar grammar preceded the necessary metadata inventory and that an
unconstrained root delegate could bypass both grammar and atomicity. Its
accepted disposition changed the exact vocabulary into a preferred hypothesis
and required a focused, independently accepted pre-E3 census. It also made
delegates central, static, staged, non-mutating, safe-inspection constrained,
bounded in output, and subject to a central post-check. Legend Blocks is named
as compatibility evidence to migrate, not as an automatically approved
delegate.

The exact E3 precondition is an independently accepted census of *external,
persisted, runtime-created, projected, and consumed* metadata node shapes. The
evidence must accept dict/list/scalar or provide an explicit bounded
alternative and migration treatment. Acceptance requires supported metadata
to survive in an owned bounded substrate; invalid roots, opaque values, and
cycles reject atomically; delegate output is post-checked; YAML Option A is
implemented at the backend boundary; and family semantics remain local.

The accepted F4 correction therefore confirms all of the following:

* dict/list/scalar was a hypothesis, not authority;
* supported existing shapes cannot become silent rejection;
* a Legend Blocks or later delegate cannot bypass the common postcondition;
* `meta_registry.yaml` is a manual review artifact, not runtime grammar; and
* census evidence may revise E3's vocabulary or require migration.

The accepted F5 disposition selected YAML Option A: reserve stem `_meta`,
safe-load at most one `_meta.yaml`/`_meta.yml` mapping, reject duplicates and
non-mapping roots at the parse boundary, never reinterpret a DataFrame in
Domain ingress, and provide release-note/migration treatment for the lost
ordinary sheet name.

The accepted F7 disposition is equally load-bearing here. A conformance
interval is not an alias tracker or isolation mechanism. Atomicity means that
ingress/delegates do not mutate caller input through their own work and publish
no staged candidate before success. It explicitly excludes deep-copy
ownership, retained-alias guarantees/tracking, concurrent isolation, later
mutation, and rollback of prior/plugin side effects.

== 3. Independent lifecycle coverage

The census materially touches all five required lifecycle classes, and source
spot checks found the following representative paths:

[cols="2,4,5",options="header"]
|===
|Class |Independent source evidence |Assessment

|External
|`json_backend.py` sidecar safe-load; XLSX/ODS hidden-blob readers;
`yaml_backend.py`; `yaml_overrides.py`; direct programmatic Frames
|Covered. The JSON/YAML and legacy-literal shape dispositions are incorrect or
incomplete, as recorded below.

|Persisted
|`JSONBackend.write_multi` uses `yaml.safe_dump`; XLSX/ODS composition uses
deterministic `json.dumps`; pre-ADR workbook composition used `str(dict)`
|Covered as a class, but the census incorrectly says the YAML writer rejects
set/bytes and incorrectly says no historical writer emitted wider literals.

|Runtime-created
|Workbook views, helper policies/FK relations, constraints, presentation
metadata, sparse defaults, discriminator split, XRef, Compact Multiaxis,
derived provenance, and cleanup commands
|Materially covered. Maintained family producers use exact dict/list
containers. Several accept `Any` configuration/cell values, but accepted E1
ordinary-payload authority and their documented examples supply no separate
non-Scalar metadata promise.

|Projected
|`project_meta_to_persistable_contract`, workbook composition, hidden-blob
serialization, and structured-sidecar serialization
|Covered. Projection is where the missed current/historical writer evidence is
load-bearing.

|Consumed
|Rendering passes/composer, schema maintenance, helper primitives, Workbook
View/XRef/Sparse/Discriminator consumers, and persistence projection
|Covered. Generic `Mapping`/`Sequence` checks are consumer tolerance, not on
their own producer or compatibility authority.
|===

The matrix is representative rather than exhaustive: it compresses parser-
produced presentation metadata and some transformation trace families. Direct
source checks found no omitted family requiring a different root kind. That
qualification does not cure the missed persistence-carrier evidence.

== 4. Prior-document conclusion

The census is sound that the named registry/lifecycle/facet/schema documents
do not establish an exact recursive Python node grammar:

* `meta_registry.yaml` has `runtime_binding: forbidden`; its lifecycle
  `shape` tokens describe an entry-local v1/v2 policy transition, not Python
  runtime node types. No runtime module loads the registry as grammar.
* `FTR-META-LIFECYCLE-INVENTORY-P5` is `Status: New` and specifies a future
  behavioral/lifecycle inventory, not completed node-shape evidence.
* META-REGISTRY/HARDENING establish meaning, provenance, producer/consumer,
  and lifecycle documentation. META-FACET and META-SCHEMA-VALIDATION remain
  future semantic/schema work. META-PERSISTENCE does not enumerate exact
  types.

There is nevertheless stronger *compatibility* authority than the census
uses. `FTR-META-PERSISTENCE` and its maintained roundtrip tests state that
metadata roundtrips end to end. `ADR-META-BLOB-SERIALIZATION` says pre-JSON
Python-literal workbooks remain readable and that the canonical `_meta`
structure was unchanged by the carrier rewrite. These artifacts do not select
the final E3 vocabulary, but they prevent writer-produced shapes from being
dismissed merely because no family-specific producer or shape test was found.

== 5. Counterexample classification

[cols="3,2,6",options="header"]
|===
|Shape/path |Class |Concrete evidence

|JSON-sidecar `set` and `bytes`
|A -- SUPPORTED COMPATIBILITY REQUIREMENT
|The maintained JSON metadata writer serializes them as `!!set` and
`!!binary`; the maintained reader restores exact `set`/`bytes`; a direct
write/read probe preserved both values and equality. This is the exact
metadata sidecar covered by the generic end-to-end metadata roundtrip
acceptance. E3 must admit them or explicitly migrate/narrow that promise.

|Legacy workbook tuple/set/bytes/complex
|A -- SUPPORTED COMPATIBILITY REQUIREMENT
|The pre-`ADR-META-BLOB-SERIALIZATION` writer put the metadata dict into the
carrier through `str(dict)`. Current `ast.literal_eval` fallback restores all
four shapes, confirmed by an XLSX read probe. The accepted ADR promises legacy
Python-literal workbooks remain readable. E3 must admit them or name a legacy
migration/rejection boundary and release consequence.

|YAML-dir `_meta` DataFrame
|B -- ALREADY-DECIDED E3 MIGRATION
|F5/Option A explicitly replaces the accidental ordinary-sheet behavior with
reserved mapping-sidecar behavior and requires release notes.

|Metadata cycle
|B -- ALREADY-DECIDED E3 MIGRATION
|PyYAML can write and reload a self-cycle, but the accepted Phase-E contract
already requires an acyclic substrate and E3 cycle tests. The census should
call this a decided migration/rejection, not prove compatibility from absence
of a family producer.

|Legend Blocks Mapping/list authoring
|B -- ALREADY-DECIDED E3 MIGRATION
|F4 and section 11 explicitly name Legend Blocks for delegate migration. Its
canonical output root is an exact dict and must undergo the common recursive
post-check.

|`!!omap` and `!!pairs` list-of-tuples
|C -- REACHABLE BUT NOT SUPPORTED CONTRACT
|Both are PyYAML safe-load shapes. No maintained writer preserves tuple
identity through the structured sidecar, no test/example uses the tags, and no
metadata-specific contract names them. (`!!pairs` was omitted by the census.)

|Custom Mapping/Sequence subclasses and generic tuples accepted by readers
|C -- REACHABLE BUT NOT SUPPORTED CONTRACT
|Representative consumers use ABC checks, but no maintained metadata producer,
fixture, public example, or explicit direct-Python contract supplies these
containers. `isinstance` tolerance alone is not authority.

|Arbitrary direct-Python object under `_meta`
|C -- REACHABLE BUT NOT SUPPORTED CONTRACT
|`run_pipeline` and current E2 can physically pass any object, but neither
promises arbitrary metadata nodes. The accepted design deliberately leaves
publication wiring to E6 and requires lower-level ordinary consumers to state
their local conformance precondition. This does not erase the narrower
writer/roundtrip evidence classified A above.

|IR-only metadata carrier objects before canonical projection
|D -- TRANSIENT / NOT AN `_meta` NODE
|`SheetIR`, render operations, DataFrames, and carrier-local structures that
do not become canonical `Frames["_meta"]` are outside the E3 node walk.
|===

== 6. Aliasing, cycles, and ownership

Aliasing and cycles are distinct. A shared child can be reached from two paths
without any path reaching an ancestor. Such a graph is acyclic. A cycle exists
only when traversal reaches an active ancestor/self.

The accepted contract forbids cycles. It does not forbid acyclic aliases,
require global alias detection, require aliases to be copied, or promise
retained-alias isolation. Identity-based cycle detection should therefore be
path-active/ancestor detection, not a global “seen twice means cycle” rule.

The census's ownership consequence is not sound. In particular:

* F7 expressly excludes deep-copy ownership and alias tracking from ingress
  atomicity.
* Current code commonly copies only the mutated path and retains unrelated
  aliases. Existing metadata ownership work names
  `copy-along-the-mutated-path`, not blanket recursive copying.
* Maintained tests do assert nested `_meta` identity in failure-atomicity paths
  (`test_domain_ingress_boundaries.py` preserves `sheets`, sheet metadata,
  `options`, and `column_widths` identities). Other tests assert direct
  `_meta` pass-through. Even where a success-path identity is not asserted,
  absence of an assertion is not compatibility authority.

“Owned bounded acyclic substrate” therefore requires ingress-owned staging,
validation, and publication; no mutation through ingress/delegate work; and
copying along paths that ingress/delegates actually rewrite. Retained aliases
outside mutated paths may remain. It does *not* require every accepted
container to be recursively deep-copied or to have unique identity throughout
the graph.

== 7. YAML, programmatic, and delegate dispositions

PyYAML safe-load reachability was independently confirmed for `!!set`,
`!!omap`, `!!pairs`, `!!binary`, date, and datetime. Date/datetime are E1
Scalar categories. The census correctly recognizes `!!set`, `!!omap`, and
`!!binary` as reachable but incorrectly treats all of them as unsupported on
the rationale that no writer produces them. The maintained structured
metadata writer produces and roundtrips set and bytes. Omap/pairs remain
reachability-only on current evidence.

Direct programmatic `_meta` has no blanket “arbitrary Python object graph”
promise. Exact-container E3 is the accepted establishment of a bounded
substrate, not an accidental change merely because Python can pass an object.
That conclusion cannot be used to discard an explicit carrier roundtrip or
legacy-read promise.

Legend Blocks is correctly classified as the anticipated E3 delegate
migration rather than evidence that the common vocabulary must be dynamically
extensible. Current normalization accepts Mapping/list, emits an exact dict,
shallowly retains nested values, and performs unsafe truthiness/`str` identity
resolution. Its block-internal values are not currently recursively checked.
E3 may authorize the delegate only if the complete post-delegate output is
walked against the accepted common substrate (or an explicitly named bounded
root contract). Delegate status cannot let tuple/set/bytes/opaque values pass
unchecked.

== 8. Backend and parked concerns

The YAML reserved-stem treatment is sound and already decided by F5/Option A:
at most one spelling, mapping root at the backend boundary, no DataFrame
reinterpretation, Domain E3 validation of the resulting values, and release
notes for the reserved-name migration.

CSV/XML are not documented metadata carriers and both writers deliberately
omit `_meta`. A foreign `_meta.csv`/`_meta.xml` currently becomes a DataFrame;
E3 invalid-root rejection safely prevents it becoming metadata. Cross-backend
filename reservation would improve ergonomics/consistency but is not an E3
vocabulary prerequisite. Parking it is sound.

The FK/helper tuple-tolerance inconsistency is dormant. The v2 FK producer
writes an exact list, the primitive requires a list, and searches found no
maintained producer, fixture, documented example, or supported programmatic
contract supplying a tuple at the affected paths. Generic Sequence or
list/tuple consumer tolerance does not make tuple input load-bearing.

== 9. Required verdict matrix

[cols="3,2,7",options="header"]
|===
|Area |Verdict |Independent evidence

|Lifecycle coverage completeness
|SOUND WITH QUALIFICATION
|All five classes are materially represented, but persisted writer behavior
was mischaracterized and some producer families were compressed rather than
enumerated.

|Prior metadata-document reuse
|SOUND WITH QUALIFICATION
|No prior artifact supplies exact runtime grammar; META-PERSISTENCE and the
serialization ADR nevertheless carry compatibility promises relevant to
writer-produced/current legacy shapes.

|Exact dict root
|SOUND
|Maintained metadata loaders/producers yield dict roots; YAML DataFrame root is
an accepted migration; CSV/XML roots are accidental non-carriers.

|Exact dict/list recursive containers
|REJECT
|Current JSON sidecar roundtrips set; legacy writer/reader roundtrips tuple and
set. Admission or migration must be decided first.

|E1 Scalar leaves
|REJECT
|Current JSON sidecar roundtrips bytes; legacy writer/reader roundtrips bytes
and complex. Admission or migration must be decided first.

|Mapping/Sequence subclass compatibility
|SOUND WITH QUALIFICATION
|Consumer tolerance is real, but no supported producer/API use was found.
Legend Blocks Mapping acceptance is a named delegate migration.

|YAML safe-load special shapes
|REJECT
|Set/bytes have maintained writer/readback evidence; omap/pairs remain
reachable-only; dates/datetimes are E1 Scalar.

|Legacy literal-eval special shapes
|REJECT
|Historical framework writer and current fallback reproduce the shapes, and
the accepted ADR promises legacy readability.

|Direct programmatic `_meta`
|SOUND WITH QUALIFICATION
|Arbitrary physical reachability is not a blanket promise, but explicit
backend roundtrip compatibility cannot be dismissed under that rule.

|Legend Blocks delegate classification
|SOUND WITH QUALIFICATION
|Correct named migration; authorization requires a complete bounded output
post-check and safe identity handling.

|YAML reserved-stem migration
|SOUND
|Matches accepted F5/Option A including backend ownership and release notes.

|Aliasing compatibility
|REJECT
|The claim that full deep-copy ownership is compatible/possibly required is
unsupported and contradicts accepted F7 boundaries.

|Cycle rejection compatibility
|SOUND WITH QUALIFICATION
|Cycles are currently YAML-writable/reloadable, but acyclicity is an already-
accepted E3 migration. Alias sharing must not be mistaken for a cycle.

|“Owned substrate” interpretation
|REJECT
|Accepted authority requires staged non-mutating ingress publication, not
blanket recursive deep copying or retained-alias isolation.

|CSV/XML parked concern
|SOUND
|Not metadata carriers; writer omission plus E3 invalid-root rejection keeps
the filename issue independent.

|Tuple-tolerance parked concern
|SOUND
|No supported affected producer/fixture/API was found; generic reader
tolerance is non-load-bearing.

|Overall preferred-vocabulary verdict
|REJECT
|The target vocabulary may still be the eventual result, but current and
legacy writer-produced special shapes lack the required admission/migration
disposition. E3 is not authorized.
|===

== 10. Findings

=== TRUSTED-INGRESS-E3-CENSUS-REVIEW-F1 -- Blocking -- current and historical metadata writers falsify the “reachable only/no producer” basis for rejecting special shapes

*Evidence:* Current `yaml.safe_dump` writes set and bytes and the JSON metadata
reader restores them exactly. The pre-JSON workbook writer used `str(dict)`,
and the retained literal-eval fallback restores tuple, set, bytes, and complex.
Both paths were reproduced. The generic metadata persistence acceptance says
metadata roundtrips end to end; the accepted serialization ADR promises legacy
Python-literal workbooks remain readable.

*Impact:* Exact dict/list/E1-Scalar enforcement would reject or change these
values without the admission/migration decision F4 requires. The census's
claim that safe dumping rejects set/tuple/bytes is factually wrong (tuple is
serialized as a sequence; set and bytes use safe standard tags), and its “no
known producer ever wrote” legacy claim is contradicted by repository history.

*Required correction:* Reopen only the bounded compatibility disposition for
current structured-sidecar set/bytes and legacy literal tuple/set/bytes/complex.
For each, either retain it in an explicit bounded vocabulary or name the
migration/rejection boundary, legacy handling, tests, and release consequence.
Also include `!!pairs` in the reachable safe-load inventory. This review does
not choose or implement that correction.

=== TRUSTED-INGRESS-E3-CENSUS-REVIEW-F2 -- Important -- ownership is incorrectly converted into blanket deep-copy permission/possible requirement

*Evidence:* The accepted F7 disposition explicitly excludes deep-copy
ownership, alias tracking, retained-alias isolation, and concurrent/later
mutation guarantees. Phase E requires staged non-mutating ingress/delegate
work and publication only after success. Current metadata contracts use
copy-along-the-mutated-path, and maintained tests do pin nested identity in
failure-atomicity paths.

*Impact:* This does not by itself widen the node vocabulary, but it would send
E3 implementation toward a stronger and potentially incompatible ownership
architecture not authorized by the accepted design.

*Required correction:* State that acyclic aliasing is permitted; cycle
detection is ancestor/path-active rather than global alias rejection; ingress
copies paths it rewrites and otherwise owes caller-input non-mutation and
staged publication, not universal recursive deep copying.

== 11. Overall verdict and lifecycle consequence

*C. CHANGES REQUIRED -- COMPATIBILITY EVIDENCE INCOMPLETE.*

The census successfully reconstructs the need for a central bounded substrate,
correctly keeps the registry out of runtime grammar, and correctly classifies
Legend Blocks and YAML Option A at an architectural level. It does not yet
authorize the preferred exact dict/list/E1-Scalar vocabulary because current
and historical framework writers supply material compatibility evidence for
out-of-vocabulary nodes that the census incorrectly denies.

E3 remains blocked until F1's exact compatibility issue is independently
resolved. If the resolution chooses migration rather than vocabulary widening,
the preferred bounded substrate may remain the final E3 target. F2 must also be
corrected so E3 does not infer blanket deep-copy/alias elimination from
“owned.” No E3 implementation, lifecycle alignment, E4+ work, or correction is
performed by this review.

== 12. Validation

The following existing tests were run without modification and passed (121
tests):

* metadata registry, hardening, lifecycle, and carrier-boundary architecture
  guards;
* spreadsheet parse-contract metadata tests;
* Legend Blocks ingress and maintained ingress-boundary integration tests;
* YAML override tests;
* persistence-boundary tests; and
* metadata persistence roundtrip tests.

Read-only probes confirmed:

* YAML set/bytes write-read equality;
* legacy XLSX literal tuple/set/bytes/complex restoration;
* `!!omap` and `!!pairs` list-of-tuple construction;
* safe-load/safe-dump alias preservation; and
* safe-load/safe-dump self-cycle preservation.

The review artifact was rendered with Asciidoctor, checked with
`git diff --check`, and committed alone. No runtime, test, roadmap, Project
Memory, mega-FTR, mega-review, parser-resource, registry, or other documentation
file was modified.
