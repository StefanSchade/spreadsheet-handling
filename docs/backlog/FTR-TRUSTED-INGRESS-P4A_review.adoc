===== Independent Architecture Review -- FTR-TRUSTED-INGRESS-P4A

*Review date:* 2026-08-11

*Review type:* Fresh-context, independent architecture review. This is not an
implementation review and does not authorize E1-E7.

*Reviewed design commit:* `11c85fcbeb9d62e2ab5caecf3ae341a77c885390`

*Lifecycle baseline:* `2bb95ba09889333dede73076a744333c632ac263`

*Verdict:* *CHANGES REQUIRED -- PHASE-E CONTRACT MUST BE MATERIALLY REDESIGNED
BEFORE IMPLEMENTATION.*

Two Blocking and seven Important findings are recorded. The six-category
scalar vocabulary, validate-only scalar treatment, framework-managed
application boundary, and separation of payload and description authorization
are sound. The proposal is not implementation-ready because its closed ordinary
cell postcondition contradicts the maintained formula-helper output contract,
and because E4's preservation claim is neither configuration-granular nor
bounded short of a broad producer/family audit.

==== 1. Scope and exact baseline

[cols="2,5",options="header"]
|===
|Item |Independent verification

|Repository
|`/home/stefan/repos/spreadsheet-handling/core`

|Branch
|`main`; no branch was created or changed.

|Starting HEAD
|`11c85fcbeb9d62e2ab5caecf3ae341a77c885390`.

|Design parent
|`git rev-parse 11c85fcb^` returned exactly
`2bb95ba09889333dede73076a744333c632ac263`.

|Starting worktree
|Clean; `git status --short` returned no output before review work began.

|Design diff
|One new 1,739-line
`docs/backlog/FTR-TRUSTED-INGRESS-P4A.adoc` and four added lines in
`docs/backlog/_ftrs_open.adoc`. No runtime, test, ADR, Technical Model, Project
Memory, roadmap implementation, or other file changed.

|Accepted factual basis
|Census `7a8f460`, Independent Review 001 `7c2152ec`, corrections `4151eaea`,
and final Follow-up Review 002 `57c0451`.
|===

The complete design diff and the FTR were read in full before conclusions were
formed. The FTR was treated as a proposal. Project Memory was not used as
architecture proof. No runtime or test file was modified, no ADR/TM/PM/roadmap
document was modified, no guard or Pydantic dependency was added, no D-T3 or
family pilot was started, and nothing was pushed.

Primary authority read for this review:

* `ADR-DOMAIN-BOUNDARY-ROBUSTNESS`;
* `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`;
* `ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB`;
* `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE`;
* Technical Model ch06 Architectural Layers;
* Technical Model ch08 Internal Scalar Value Model, Pipeline Semantics,
  Spreadsheet Parse Contract, spreadsheet backend capability profiles, and
  relevant persistence/reconstruction sections;
* `roadmap_domain_hardening_sequence.adoc`, especially Phase E, the Domain
  Maintainer Contract, Phase F, and D-T3;
* accepted Phase-D Minimal Internal Value Model Slice 1 and Date/Time D-T1/D-T2
  evidence; and
* the accepted Phase-E census and both independent census reviews.

The ch04 Trusted Ingress concept was used only to locate historical questions
and counterexamples. It was not treated as normative authority.

==== 2. Independent authority reconstruction

The governing chain is:

. The robustness ADR requires deliberate, proportionate validation at owned
  boundaries but explicitly does not promise a sandbox against same-process
  Python.
. The runtime/policy ADR fixes the trusted-operator precondition, keeps payload
  authorship and description authorship independent, places policy with the
  operator, combines restrictions per kind, requires rejection before
  capability activation, and keeps sink capability separate from payload
  shape.
. The validation/writer ADR puts Domain semantics and validity in the Domain;
  persistence writers translate already-decided intent and remain dumb.
. The visible-cell ADR and accepted Phase-D Technical Model establish the six
  scalar categories, exact carrier precedence, and classification without
  normalization. `FormulaSpec` is explicitly framework-authored output Intent,
  not a payload scalar.
. Phase E must establish a mandatory, documented boundary before ordinary
  Domain work, but may not absorb family invariants, reconstruction,
  persistence projection, or sink interpretation.

Payload authority and payload conformance are different. The ADR asks who
authored the payload; Phase E asks whether the current payload state conforms
to the Domain's bounded grammar. A trusted author does not make an unsupported
object part of the global Domain grammar. Conversely, applying the same global
grammar to both authority classes does not erase the authority dimension:
origin still selects the operator-owned policy context and the four
payload/description combinations remain representable.

===== Does Phase E need a new ADR?

[cols="4,3,5",options="header"]
|===
|Decision area |Classification |Reason

|Fixed trusted runtime; independent payload/description authority; operator
policy; rejection before activation; sink separation
|DERIVED FROM EXISTING ADR/TM
|These are direct decisions of `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`.

|Six categories, carrier order/exclusions, no string-shape guessing
|DERIVED FROM EXISTING ADR/TM
|Accepted Phase-D Technical Model and classifier already own them.

|Validate-only scalar ingress
|PHASE-E DESIGN CHOICE WITHIN EXISTING AUTHORITY
|Phase D intentionally left normalization to Phase E and does not require
coercion.

|Framework-managed guarantee, `run_pipeline` caller precondition, direct-API
boundary
|PHASE-E DESIGN CHOICE WITHIN EXISTING AUTHORITY
|The ADR explicitly left ingress mechanism and `run_pipeline` disposition open.

|Exact axis and metadata substrate grammars
|PHASE-E DESIGN CHOICE WITHIN EXISTING AUTHORITY
|These specialize the robustness boundary. They require compatibility evidence,
not an ADR merely because they are durable.

|Preserving producers, re-establishment, and a contractual trust epoch
|PHASE-E DESIGN CHOICE WITHIN EXISTING AUTHORITY
|They are candidate mechanisms for maintaining an already-authorized
postcondition. They do not add a new trust dimension.

|Coordinated payload and description subcontracts
|DERIVED FROM EXISTING ADR/TM
|Separate ownership and pre-import authorization follow directly from the ADR.

|E1-E7 ordering
|PHASE-E DESIGN CHOICE WITHIN EXISTING AUTHORITY
|This is lifecycle planning, not a new architectural principle.
|===

*ADR verdict:* *No new ADR is required.* None of the challenged items creates a
genuinely new durable axis not already permitted or required by existing
authority. The Blocking findings therefore require FTR correction, not an ADR.

==== 3. Proposed architecture in one view

The intended layering is understandable and mostly well separated:

[source,text]
----
trusted operator context
        |
description parse -> authorize capability -> bind/import       [E7]
        |                         (trusted-description precondition before E7)
        v
loader / parser conversion
        |
payload ingress: structural dispatch + scalar admission + metadata delegates
        |
ordinary Domain producer
        |\
        | reviewed exact invocation -> contractual preservation
        |
        + uncertified return -> full re-establishment
        v
final Domain cleanup -> persistence projection -> saver / sink

run_pipeline: caller supplies and maintains the conformance precondition;
it owns no loader, policy, automatic ingress, cleanup, projection, or saver.
----

This small diagram also exposes the two missing seams. First, framework-created
render Intent such as `LookupFormulaSpec` exists between a Domain producer and
the saver but is neither admitted ordinary payload nor a declared carrier.
Second, “reviewed producer” must be attached to an exact behavior/configuration,
not inferred once for a callable with open parameters.

==== 4. Independent review of all 15 design questions

[cols="1,3,3,3,2,3",options="header"]
|===
|# |Design question / proposed decision |Authority support |Census/runtime support
|Verdict |Residual risk or correction

|1
|Mandatory coverage: select hybrid Model D across router-backed entries,
post-custom returns, `run_pipeline`, and supported direct APIs.
|Phase E requires an owned application boundary; the ADR does not require
universal Python-call coverage.
|The six named entry forms converge on `orchestrate`; custom returns and
`run_pipeline` bypass initial-only ingress.
|REVISE
|The overall split is appropriate, but formula Intent and configuration-scoped
producer behavior make the selected transition model incomplete (F1, F2).

|2
|Define framework-managed by ownership of loader, ingress, cleanup, projection,
and saver, not by function names or plugin internals.
|Matches ch06 application/Domain ownership and the trusted-runtime ADR.
|All named high-level paths use the fixed `orchestrate` macro; plugin internals
do not.
|ACCEPT
|Keep the definition lifecycle-based and avoid implying API stability for
package-public `run_schema_maintenance`.

|3
|Publish an explicit guarantee matrix for every high-level entry.
|A public boundary must state its postcondition; description trust is
orthogonal.
|The CLI, `run_app`, `orchestrate`, shim, and schema use case share the macro.
|ACCEPT WITH QUALIFICATION
|The guarantee is only truthful after F1/F2 and only under the trusted-
description precondition until E7.

|4
|Retain `run_pipeline` as step-only trusted programmatic composition with a
caller conformance/preservation precondition.
|Technical Model Pipeline Semantics already assigns this role.
|Runtime is a plain assignment loop and has supported direct consumers.
|ACCEPT WITH QUALIFICATION
|E6 must distinguish current behavior from the final post-E4 vocabulary and
must not imply automatic behavior or deprecation.

|5
|Supported direct APIs keep local contracts; importable internals get no entry
guarantee.
|Consistent with the small public surface and Domain-owned invariants.
|The census found mixed aggregate reachability and specific supported roots.
|ACCEPT
|A later Maintainer Contract may standardize wording; Phase E must not promote
all imports.

|6
|Traverse every DataFrame data cell and every `ExactTable.data` cell with closed
carrier dispatch and scalar policy admission.
|Required to establish a complete ordinary-payload postcondition.
|Current ingress has no general traversal; object-dtype and YAML cells can carry
rich values.
|ACCEPT WITH QUALIFICATION
|It proves exclusion of opaque ordinary input, not uniform scalar operations;
framework-produced Intent needs a separate lifecycle (F1).

|7
|Make `_meta` an owned exact-dict/list/scalar acyclic substrate plus explicit
root delegates.
|Robustness and metadata/payload separation support a bounded substrate.
|The census found multiple origins and only one current delegate; the complete
metadata lifecycle inventory is explicitly still open.
|REVISE
|The precise vocabulary is evidence-gated, delegate governance is incomplete,
and YAML `_meta.yaml` needs a migration decision (F4, F5).

|8
|Order initial ingress after parsing; re-establish bootstrap/override affected
metadata; validate ExactTable generically before local reconstruction.
|Preserves parser, Domain, reconstruction, and persistence ownership.
|Matches current macro and current post-load metadata introduction sites.
|ACCEPT WITH QUALIFICATION
|“Affected state” must still produce the complete declared postcondition and
delegates must be staged/pure.

|9
|Use reviewed preserving producers; re-establish every uncertified returned
candidate before later macro consumption.
|A postcondition must survive replacement; trusted Python authorship is not
payload conformance.
|Plugins and BoundSteps replace Frames. Registry targets have open configuration
and can emit non-grammar values.
|REVISE
|Certification must be exact-invocation/behavior scoped and the initial
certified set must be bounded without pulling Phase F into E4 (F1, F2).

|10
|Use explicit DataFrame/ExactTable dispatch; keep grouped/canonical carriers
local and trusted by construction.
|Carrier identity and family reconstruction are distinct from scalar ingress.
|ExactTable and GroupedMatrix are real current carriers with local factories.
|REVISE
|Axis grammar is premature, and “trusted by construction” proves structural
pairing only unless constructors also preserve payload conformance (F2, F3).

|11
|Validate only; preserve every admitted scalar carrier identity; normalize only
owned metadata shapes.
|Phase D classification is explicitly non-coercing and the string substrate is
fixed.
|No repository-wide downstream need justifies precision/timezone/Missing loss.
|ACCEPT WITH QUALIFICATION
|The safety gain is removal of arbitrary opaque Python, not uniform equality,
hashing, sorting, serialization, Missing, or timezone behavior.

|12
|Introduce immutable operator payload policy over cell categories/variants,
metadata leaves, and structural carrier kinds; keep description policy
separate.
|The ADR requires operator ownership and per-kind narrowing.
|The census found no current payload policy object, transport, or operator use
case for these four dimensions.
|DEFER
|Retain only dimensions justified by an implementation use site; carrier-
variant and independent metadata-leaf controls are premature (F8).

|13
|Coordinate separate payload and description subcontracts; reject forbidden
less-trusted capabilities before import; direct callables remain trusted-only.
|Directly required by the runtime/policy ADR.
|Current binding imports before payload load; all configured strings use the
central resolver.
|ACCEPT WITH QUALIFICATION
|E7 depends on an unresolved extension assessment and Phase-E completion state
must be explicit (F9).

|14
|Use typed boundary failures, safe ordinal paths, fail-fast behavior, and staged
publication atomicity.
|Robustness ADR and validating-transformation rules support deliberate failure.
|Current Legend Blocks ingress is staged but invokes `str`/truthiness and
shallowly retains aliases.
|REVISE
|Narrow atomicity to actions of ingress and make delegate purity/output checks
normative; trust epochs cannot provide isolation (F4, F7).

|15
|Keep an empty `DeclaredComposite` grammar seam with future named, bounded,
policy-controlled adapters.
|Phase D explicitly leaves bounded `List[Scalar]` open without admitting
`List[Object]`.
|No current external composite classifies as a scalar.
|ACCEPT WITH QUALIFICATION
|The seam is useful, but it does not solve current framework-authored formula
Intent and must not imply every future structured value belongs in a cell (F1).
|===

==== 5. Coverage-model review

[cols="1,2,2,2,2,2",options="header"]
|===
|Model |Complexity / audit |Bypass and enforcement |API / explainability
|Performance |Repository fit

|A -- universal ingress at every supported API
|Highest wiring and policy-plumbing breadth; every direct surface must be
classified.
|Low bypass only if “supported” is exhaustively maintained; importability makes
the boundary unstable.
|Disrupts low-level composition and obscures local preconditions.
|Repeated work depends on call graph.
|Too broad for this repository.

|B -- framework external guarantee; all later output by producer contract
|Low central complexity but high reliance on every producer contract.
|Fails after a custom replacement because cleanup/projection/saver still consume
the candidate.
|Simple until plugins are considered; then the advertised guarantee is false.
|One initial O(total cells) pass.
|Necessary foundation, insufficient alone.

|C -- explicit wrapper/token/type state
|Mechanically clearest state, but large pandas/Frames retrofit and plugin adapter
surface.
|Best visible transition enforcement; still cannot prevent retained aliases to
mutable DataFrames.
|High API disruption and plugin friction.
|Can avoid redundant traversals once state is sound.
|Not proportionate for the current API.

|D -- selected hybrid
|One initial pass plus producer classification; audit is substantial unless
certification is exact and defaults closed.
|Good containment of returned plugins, but preservation metadata and retained
aliases are contractual, not mechanically proven.
|Most compatible and explainable once its producer/Intent seams are corrected.
|Initial O(total cells), plus only uncertified returns.
|Best long-term fit, not implementable as currently specified.

|E -- re-establish after every macro step
|Lowest certification audit and easiest enforcement.
|Lowest accidental bypass after returns; still cannot constrain what occurred
inside a callable.
|Very simple rule and plugin story; no new API type.
|O(total cells times steps), pathologically wasteful for large workbooks and
long built-in pipelines.
|Useful safe migration default, not a desirable permanent universal rule.
|===

*Model D verdict:* *SOUND WITH CORRECTION.* It is the best target for this
repository only if its optimization is staged: unknown and configuration-open
producer forms remain uncertified, certification describes an exact bound
behavior (not a name or callable in the abstract), and current framework-owned
non-scalar Intent gets an explicit lifecycle. A safe initial implementation may
re-establish after more steps than the final optimized model; that is still
Model D's closed default, not a claim that all built-ins preserve.

Plugin support is strongest under corrected Model D: the framework makes no
claim about operations or side effects inside the plugin, but validates a
recognized returned candidate before any subsequent framework-owned consumer.
Plugins that return `None` through the maintained plugin/generic binders use the
mutated input as the candidate; a raw `BoundStep` passed to `run_pipeline` has
no such automatic convention and remains its caller's responsibility.

==== 6. Scalar grammar, normalization, and Missing

The FTR reproduces the accepted scalar grammar exactly:

* String: `str`, including `numpy.str_` by subclass behavior;
* Boolean: `bool` and `numpy.bool_`, dispatched before Number;
* Number: Python and NumPy integer/floating variants, after explicit
  `numpy.timedelta64` exclusion;
* Missing: `None`, empty string, supported float/NumPy NaN, `pandas.NA`,
  `pandas.NaT`, and `numpy.datetime64('NaT')`, but not
  `numpy.timedelta64('NaT')`;
* DateTime: `datetime.datetime`, `pandas.Timestamp`, and every non-NaT
  `numpy.datetime64`, dispatched before Date and without unit inference; and
* Date: `datetime.date` after DateTime.

Timezone-aware datetime/Timestamp carriers are admitted without timezone
stripping. No carrier was silently added or removed. The focused classifier
probe and accepted Phase-D tests confirmed bool-before-number,
DateTime-before-Date, `numpy.timedelta64` rejection, all listed Missing
carriers, `numpy.str_`, Timestamp, aware datetime, and NumPy datetime units.

===== Validate-only result

Validate-only is the least lossy decision and is architecturally sound. Its
precise safety benefit is:

[source,text]
----
ordinary input cell is a member of a small, known semantic carrier vocabulary
and is not arbitrary opaque Python
----

It does *not* prove uniform Python scalar semantics. Repository operations use
sets, dictionary keys, pandas merge/groupby/comparison, sorting, arithmetic,
string methods, serialization, and datetime comparison. Accepted carriers can
still differ materially:

* `pandas.NA` has tri-state comparison/boolean behavior;
* NaN remains non-reflexive and has surprising equality/hash behavior;
* aware and naive datetimes are both valid but may not be mutually orderable;
* NumPy numeric and datetime precision/overflow semantics remain carrier-owned;
* pandas/Timestamp and native datetime conversions can differ by backend; and
* no admitted-category cross-product is promised sortable or serializable.

Section 18's non-guarantees correctly preserve these facts. Family code may
remove generic arbitrary-object/container/callable defenses after a real
conformance precondition, but it may not remove family type constraints,
Missing handling, compatibility checks, comparison/sort rules, or sink
translation.

===== Missing preservation

Preserving all admitted Missing carriers is consistent with Phase D. Generic
code must use `core.scalar_values.is_missing_carrier`, not ordinary equality or
truthiness. That predicate is the Technical Model's stable shared classifier
primitive even though it is not re-exported from `core.__init__`; the FTR's
“may use” wording is therefore supportable. Empty string remains both a
legitimate String substrate value in source-specific contexts and a Missing
carrier under the accepted generic classifier; Phase E does not resolve that
family semantic ambiguity.

===== Framework-authored FormulaSpec conflict

The accepted Technical Model says `FormulaSpec` is framework-authored output
Intent, not a payload scalar. That does not mean it is absent from runtime
DataFrames: documented `add_fk_helpers` / `add_lookup_helpers`
`helper_value_mode: formula` creates `LookupFormulaSpec` cells and the XLSX/ODS
renderers deliberately consume them. The registry exposes `add_lookup_helpers`
as a normal configured step. A full post-step ingress pass would reject this
maintained path. Section 19's empty `DeclaredComposite` branch cannot repair
the gap because FormulaSpec is Intent, not a future `List[Scalar]`. See F1.

==== 7. Structural carriers, axes, and ExactTable

===== DataFrame traversal and axes

Positional traversal of every data cell is feasible and avoids promoting row
indexes to data. The current axis evidence includes:

* flat string labels from most tabular backends;
* supported numeric physical column labels;
* supported hashable tuple labels and MultiIndex roundtrip paths;
* default and non-monotonic numeric row indexes;
* family-specific rejection of MultiIndex in operations that require flat
  fields; and
* an existing physical column contract that accepts any non-missing-like,
  hashable, self-comparable, unique label, rather than a closed six-category
  vocabulary.

The proposed `AxisAtom`/finite-tuple grammar is coherent, and it admits known
numeric/MultiIndex examples, but it is a new tightening. No completed census
shows that all framework-generated and supported consumer-reachable labels fit
it. Row-index restriction is especially weakly motivated because ingress uses
positions and the index is not promoted to payload. Section 10 both fixes the
grammar and says the implementation must census before “tightening”; those
statements conflict. The exact grammar must be evidence-gated before it becomes
normative (F3).

“Deterministically comparable” has a narrow existing meaning in
`domain.tabular.physical_labels`: self-equality yields an unambiguous Boolean.
It is not an ordering promise. Labels satisfying the proposed grammar can
still be mutually non-orderable, and tuple labels can contain Missing atoms.
The FTR's non-guarantee for sorting/comparison is therefore load-bearing.

===== ExactTable

The proposed checks are generic carrier self-consistency, not grouped
reconstruction:

* exact `ExactTable` identity;
* exact non-negative integer `header_rows` and `n_cols`;
* `header_rows == len(header_grid)`;
* header/data row widths equal `n_cols`;
* string header cells; and
* accepted scalar data cells.

Current `ExactTable.__post_init__` owns container conversion but does not enforce
geometry: a probe constructed `header_rows=99`, `n_cols=-1`, and mismatched row
width without failure. Parser/workbook projection and grouped projection
construct consistent instances, so central checking is meaningful. Row keys,
header-depth intent, tuple uniqueness, mapping/bijection, and reconstruction
remain correctly local. *ExactTable verdict: SOUND.*

===== Grouped/canonical carrier trust

`GroupedMatrix` rejects direct raw construction and its public factory proves a
frame/header pair against a live mapping. That proves structural pairing, not
the ordinary scalar grammar of its contained mutable DataFrame. The public
factory can receive a validly headed DataFrame containing arbitrary values, and
`contract_grouped_xref` can propagate a caller-supplied `fill_value`. Therefore
“trusted by construction” must always name the exact property established.
Such carriers may extend a conformance interval only when their producing
invocation also preserves payload conformance; a dataclass/factory identity is
not a payload bypass.

==== 8. Metadata substrate review

The distinction between external/persisted metadata and runtime metadata is
correct. Current producers/consumers predominantly use mapping/list/string/
Boolean/number leaves under roots such as `sheets`, `constraints`,
`helper_policies`, `workbook_view`, `legend_blocks`, `xref_crosstable`,
`compact_multiaxis`, `split_by_discriminator`, `pipeline_cleanup`, and
`derived`. Persistence strips known runtime roots/facets. However,
`FTR-META-LIFECYCLE-INVENTORY-P5` explicitly records that the complete root and
sub-root lifecycle inventory does not yet exist.

Exact owned built-in dict/list nodes, string keys, scalar leaves, identity-based
cycle detection, and no arbitrary recursive attribute walking form a plausible
substrate. They are not yet proven compatible. Current Legend Blocks ingress:

* accepts general `Mapping` input;
* calls truthiness and `str` while resolving list identities;
* shallow-copies block mappings; and
* retains nested aliases.

E3 can replace those mechanics, but the design must first state delegate
governance. A root delegate must be centrally reviewed/owned, operate only on a
staged candidate, avoid side effects and unsafe rejected-value inspection, and
either return the common substrate or an explicitly named root-specific output
contract that central ingress checks. Arbitrary family registration must not
become a hidden plugin system. Without those rules a delegate can bypass the
grammar and atomicity (F4).

===== YAML `_meta.yaml`

The current `yaml_dir` loader treats every YAML filename as a sheet. A direct
probe of `_meta.yaml` containing a mapping returned
`frames["_meta"]` as a DataFrame; the YAML saver then skips `_meta`. This behavior
is explicitly documented in the accepted census and Minimal Internal Value
Model, and `yaml_dir` is a documented CLI backend. Declaring this DataFrame an
invalid metadata root is a defensible hardening decision, but it changes a
user-reachable reserved-name behavior. The design does not choose among:

. teaching the YAML backend to produce a metadata mapping;
. rejecting the reserved input with an explicit compatibility error; or
. preserving a deliberately reinterpreted ordinary sheet.

E3 cannot discover that release/migration decision after the contract has
already declared the input invalid. The option, compatibility statement, and
release-note consequence must be decided before implementation (F5).

==== 9. Producers, trust epochs, and re-establishment

===== What can actually be certified

The runtime registry currently contains 44 entries: 38 `StepRegistration`
entries with fixed targets and six bare factories (`identity`, `validate`,
`add_fk_helpers`, `remove_fk_helpers`, `validate_fk_helpers`, `plugin`).
`BoundStep` contains only `name`, mutable `config`, and `fn`; there is no current
producer-state representation.

A preservation proof cannot generally attach once to a callable or family:

* `contract_xref` with an otherwise conforming input and an opaque configured
  `fill_value` placed that exact object in a missing matrix cell in a direct
  probe;
* `validate_references` on a conforming DataFrame with a tuple/MultiIndex row
  label emitted that tuple into the ordinary `validation_findings.row_index`
  cell;
* formula-mode lookup-helper configuration emits `LookupFormulaSpec` cells;
* grouped producers hold mutable DataFrames and accept configuration-dependent
  fill values; and
* wrappers/factories may close over callbacks or mutable configuration even
  when the outer callable is built in.

The certified unit must therefore be the exact bound behavior/configuration
whose output contract is closed. Callable/family review may establish
eligibility, but it is not the certificate. Any unreviewed option, nested
callback, mutable post-binding configuration, or wrapper composition returns
the invocation to the uncertified default. The FTR need not choose the metadata
field/API now, but this granularity is contract-load-bearing.

E4's present requirement to inventory all registered producers and provide
payload/metadata output tests for every preserving declaration is not a small
infrastructure slice. Current counterexamples show it entails behavior-mode
and family-output analysis across 44 entries. That pulls Phase-F compatibility
work ahead of the Domain Maintainer Contract unless E4 starts from a very small
closed certification set or conservatively re-establishes after unproven steps.
See F2.

===== Trust epoch enforceability

Without a wrapper/token, deep copy, ownership-enforcing carrier, or mutation
tracking, a trust epoch is explanatory state. The framework can know that an
ingress/re-establishment pass succeeded and that the next producer was marked
for a reviewed bound behavior. It cannot know that a caller, closure, plugin,
thread, retained DataFrame alias, retained `_meta` alias, or in-place operation
will not mutate the graph later. Many current transformations shallow-copy the
top-level Frames mapping and retain unchanged DataFrames.

This is acceptable under the trusted-runtime ADR only if stated as a
caller/producer obligation, not as mechanically certified isolation.
“Must not retain an out-of-band mutation path” is not an ingress guarantee; it
is a reviewable producer obligation with the same-process threat-model limit.
The macro can advertise conformance only while these obligations hold (F7).

===== Uncertified-return boundary

Full re-establishment after an uncertified return is sufficient to protect the
*next framework-owned consumer* if the returned candidate is a recognized
Frames mapping and if all intended downstream internal carriers are modeled.
It does not protect:

* ordinary Domain calls made inside the custom callable;
* a sink side effect performed by that callable;
* in-place mutation before returning `None` or the same mapping;
* effects performed before a non-Frames return is rejected; or
* later mutation through an alias retained by the callable.

The FTR states most of these limits correctly. Maintained plugin/generic
binders translate `None` to the mutated input and therefore give the macro a
candidate to check. A raw `BoundStep` in `run_pipeline` does not; that surface
remains caller-owned. Re-establishment must reject a non-Frames return before
cleanup, projection, or save. It cannot roll back custom side effects.

==== 10. Programmatic surfaces and framework matrix

[cols="3,3,3,4",options="header"]
|===
|Surface |Payload/description source |Lifecycle ownership |Verdict

|`sheets-run`
|Router-loaded external payload; YAML/CLI description binds before load.
|CLI delegates to `orchestrate`; macro owns ingress, steps, cleanup, projection,
and saver.
|Framework-managed. Trusted-description precondition through E1-E6.

|`run_app`
|`AppConfig` I/O and pipeline specs; binding occurs before delegated load.
|Thin compatibility adapter over `orchestrate`.
|Framework-managed; same guarantee, no separate boundary.

|`orchestrate`
|Router-loaded payload; caller supplies already-bound steps.
|Owns the full macro and is the convergence seam.
|Framework-managed; supplied BoundSteps are trusted capability but
uncertified output by default.

|Compatibility shim `spreadsheet_handling.orchestrator`
|Exact alias of application `orchestrate`.
|Adds no lifecycle.
|Inherits the identical guarantee; not a bypass.

|`sheets-schema-maintain`
|Router-loaded payload; CLI constructs a fixed request, not a general pipeline.
|Calls package application use case, which builds one private BoundStep through
`orchestrate`.
|Framework-managed fixed-description path.

|`run_schema_maintenance`
|Package-public request plus router input/output descriptors.
|Uses the same fixed private step and macro.
|Framework-managed application use case, but this does not promote it to a
general consumer API or promise stability beyond its package contract.

|`run_pipeline`
|Caller-provided Frames and already-bound Step objects.
|Owns only sequential calls; no load, policy, ingress, cleanup, projection, or
saver.
|Trusted-programmatic, caller-precondition surface. No hidden automatic change.

|Supported direct Domain/backend API
|Caller-provided values/configuration.
|Only its documented local operation.
|Local contract; ordinary payload consumers require the conformance
precondition. Importable-only internals get no promise.
|===

`run_pipeline` is usable if E6 publishes where the conformance operation and
producer classification vocabulary live. Before E4 exists, documentation must
describe the *current* plain step runner, not demand knowledge represented by no
API. The final contract may require a caller to call an explicit establishment
function or use only documented preserving invocations, but `run_pipeline`
itself must not acquire implicit policy or transition behavior.

No current YAML/JSON path constructs a callable object from payload data.
Configured string identifiers are centrally resolved during binding. Future
less-trusted descriptions must accept declarative identifiers only and must not
deserialize callable objects through YAML/JSON/pickle-like mechanisms. Direct
callables and BoundSteps remain trusted-programmatic capability and cannot be
made less-trusted by a flag.

==== 11. Description plane and E7 lifecycle

The two-plane split is correct:

* E1-E6 may establish a payload-conformance guarantee for entries whose
  description is trusted by precondition.
* Less-trusted configured descriptions require structural parsing and
  operator authorization before import/binding/acquisition; module import is
  capability activation.
* Payload admission cannot compensate for an already activated forbidden
  callable.

The stated dependency exists:
`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A.adoc` is still an open architecture
assessment. Its original readiness review found it not ready; a correction
self-review exists, but no accepted architecture selection yet supplies E7's
per-surface policy metadata.

The design says E7 is a Phase-E sub-slice and required before advertising
less-trusted descriptions, but does not define whether “Phase E complete” means
E1-E6 or E1-E7. The roadmap's Phase E and accepted ADR require the two authority
questions to remain representable; they do not require payload implementation
to wait for E7. The lifecycle must therefore name an intermediate state, for
example an accepted payload milestone under a trusted-description precondition,
while reserving Phase-E completion for E7 -- or explicitly move E7 to another
lifecycle feature. Current CLI/user documentation must not imply that
operator-untrusted pipeline YAML is supported during that intermediate state.
See F9.

D-T3 still does not follow E1-E6 or this review. The roadmap gates it on
completed Phase E and the Domain Maintainer Contract, and no family pilot is
selected here.

==== 12. Atomicity, diagnostics, and security

===== Atomicity and aliasing

Read-only DataFrame traversal plus a staged top-level mapping and fully owned
metadata dict/list graph can ensure that ingress performs no mutation and
publishes no candidate on failure. It cannot ensure isolation against concurrent
or later writes by external aliases. DataFrames are intentionally not deep
copied; existing metadata normalizers retain nested aliases; delegates could
have side effects unless forbidden.

The contract should promise:

[source,text]
----
ingress and every approved delegate perform no mutation of caller-owned input;
no staged candidate is published before all checks succeed
----

It should not be read as proof that the object graph remained unchanged by all
actors during the attempt. Post-custom-step re-establishment rejects the
returned state before later macro consumption but cannot roll back earlier
mutation or side effects.

===== Diagnostic feasibility

Safe diagnostics are feasible with closed dispatch:

* exact type checks precede arbitrary value inspection;
* DataFrame cells are located by frame and positional row/column ordinals;
* axis tuples are recursively type-checked before physical-label equality/hash;
* exact dict keys are type-checked while traversing the container contract;
* cycle detection uses `id(container)`, not container hashing; and
* rejected values need only a bounded carrier/type category, never `repr` or
  `str`.

The prohibition on equality/hash/iteration applies to the rejected unsupported
*value*, not to iteration of an already-admitted exact dict/list/DataFrame
container. Existing Legend Blocks `str`/truthiness behavior must be replaced or
bounded during E3; it cannot simply be grandfathered as a delegate.

===== Hostile/malformed input matrix

[cols="3,3,4",options="header"]
|===
|Input |Containment point |Qualification

|Custom cell object with hostile `repr`/equality/hash or NumPy protocol
|Initial scalar dispatch or post-uncertified re-establishment rejects by bounded
type before protocols.
|No sandbox; object construction/import already occurred in trusted runtime.

|Generator, file handle, callable, class, or module in a cell
|Same scalar rejection.
|Diagnostic must use ordinal/type category only.

|`numpy.timedelta64`, including NaT
|Classifier rejects before Number.
|Exact Phase-D rule.

|`pandas.NA`, NaN, `pandas.NaT`, NumPy datetime NaT
|Accepted as Missing.
|Downstream must use missing predicate, not equality/truthiness.

|`pandas.Timestamp` or aware datetime
|Accepted as DateTime.
|No mutual comparison, timezone normalization, or serialization promise.

|Nested metadata cycle
|Metadata traversal rejects using active identity set.
|No arbitrary container hashing is needed.

|Mapping subclass / tuple / set in external metadata
|Exact substrate rejects before arbitrary recursive traversal, unless an
explicit reviewed root delegate owns that authoring form.
|Delegate output and purity rules are currently missing (F4).

|Huge finite nested list
|Structurally accepted/traversed if otherwise valid.
|Resource ceilings are expressly out of Phase E; same-process memory/CPU DoS is
not contained.

|Plugin replacement containing an opaque object
|Full returned-candidate re-establishment before next operation/cleanup/save.
|Only after F1 models legitimate internal Intent.

|Plugin invokes Domain or a sink before returning
|Not contained by ingress.
|Trusted-programmatic obligation and explicit non-guarantee.

|Plugin mutates shared DataFrame and returns same mapping or `None`
|Maintained wrapper turns `None` into candidate, which is checked.
|Prior mutation is not rolled back; retained aliases remain outside mechanical
knowledge.

|Forbidden dotted module in less-trusted description
|E7 authorization must reject before import.
|E1-E6 support only trusted descriptions.
|===

==== 13. Compatibility and migration inventory

[cols="4,3,5",options="header"]
|===
|Proposed rejection/change |Classification |Disposition

|Opaque objects, callables, generators, files, arbitrary collections in
externally loaded ordinary cells
|INTENDED HARDENING
|Core purpose of Phase E; release documentation should describe the new
boundary.

|Unsupported Time/Duration/`numpy.timedelta64` carriers
|EXISTING CONTRACT ALREADY FORBIDS
|Already excluded by accepted Phase-D internal value model, though runtime did
not yet enforce at every entry.

|YAML nested rich cell values and binary values
|INTENDED HARDENING
|The accepted census documents reachability but no Domain guarantee; provide
safe location diagnostics.

|Framework-created `LookupFormulaSpec` in helper cells
|CURRENTLY SUPPORTED -- MIGRATION OR EXPLICIT LIFECYCLE NEEDED
|Documented, registered, renderer-consumed behavior conflicts directly with
the proposed postcondition (F1).

|YAML `_meta.yaml` becoming a DataFrame
|CURRENTLY SUPPORTED -- MIGRATION NEEDED
|Choose backend conversion, explicit reserved-name rejection, or another
supported interpretation before E3 (F5).

|Metadata tuple/set/Mapping subclasses, aliases, special root objects
|UNKNOWN -- MUST CENSUS BEFORE IMPLEMENTATION
|Complete root/sub-root lifecycle inventory is not available (F4).

|Custom/hashable axis labels outside six scalars; extension index types
|UNKNOWN -- MUST CENSUS BEFORE IMPLEMENTATION
|Existing physical-label contract is broader; exact axis grammar is gated
(F3).

|Numeric and tuple/MultiIndex axes
|CURRENTLY SUPPORTED
|The proposed grammar appears to admit known examples, subject to policy and
Missing-tuple clarification.

|Duplicate/missing-like/unhashable/ambiguous physical columns
|EXISTING CONTRACT ALREADY FORBIDS
|Current shared physical-label boundary rejects these for ordinary Domain
transformations.

|Invalid ExactTable geometry accepted by raw dataclass constructor
|INTENDED HARDENING
|Parser/framework producers already construct consistent shapes; generic
ingress adds a meaningful check.

|Operator-selected policy narrowing
|INTENDED HARDENING
|Only implement dimensions with an evidenced operator use site (F8).

|Custom step non-Frames or opaque return in framework macro
|INTENDED HARDENING
|Reject before subsequent framework-owned consumption; side effects remain.

|`run_pipeline` caller-provided arbitrary Frames
|NO AUTOMATIC BEHAVIOR CHANGE
|The final published contract adds a caller precondition, not hidden ingress.
|===

Formula Intent, YAML reserved metadata, and any axis rule that removes a
confirmed supported label require migration/release-note treatment. Ordinary
garbage rejection and explicit operator narrowing are intended hardening, not
backward-compatibility promises.

==== 14. Implementation slicing, performance, and design detail

===== E1-E7 dependency review

[cols="1,3,3,4",options="header"]
|===
|Slice |Independent shape |Dependency/ordering verdict |Correction

|E1
|Classifier composition, minimal policy, and safe errors can be tested without
traversal.
|Implementable independently after policy dimensions are narrowed.
|Do not build unused carrier-variant/metadata-policy machinery merely because
the FTR listed it.

|E2
|Frames/DataFrame/ExactTable dispatch and read-only traversal are separable.
|GATED by accepted axis compatibility evidence and F1's treatment of internal
Intent.
|Do not make the precise axis grammar normative before its census; state which
payload states are initial-ingress versus producer-internal.

|E3
|Metadata substrate/delegates can be isolated from family semantics.
|GATED by the complete relevant node-shape inventory, delegate governance, and
YAML migration decision.
|The current proposed precondition is not merely implementation discovery; it
can change the design.

|E4
|Producer transition representation is conceptually separate.
|TOO LARGE / INCORRECTLY BOUNDED as written.
|Certify exact bound behavior, start from a narrow closed set, and avoid
requiring all-family proof before Phase E. Resolve current FormulaSpec and
other non-grammar outputs first.

|E5
|Macro wiring is coherent only after transition rules exist.
|Blocked by E2-E4 corrections. It need not wait for public docs, but behavior
must not ship with an unknowable contract.
|Conservatively re-establish after all unproven producer returns; preserve the
trusted-description support boundary.

|E6
|Documentation/public contract publication is separately reviewable.
|Not necessarily docs-only: a supported explicit establishment API/export or
producer introspection surface may be required for `run_pipeline` callers.
|Publish current versus final behavior precisely; do not imply deprecation or
automatic ingress.

|E7
|Description authorization is a distinct security feature.
|Correctly depends on an independently accepted extension-surface assessment,
but Phase-E lifecycle is undefined.
|Name the E1-E6 intermediate state and whether Phase E completes only at E7.
|===

The repository must remain coherent between slices. E1-E3 can remain unwired;
E4 can establish conservative metadata; E5 is the first user-visible
enforcement point and therefore the point at which compatibility/release
decisions must already be settled. E6 should ship with E5 behavior. E7 can land
later only if every E1-E6 surface explicitly requires trusted descriptions.

===== E4 scope explosion

Auditing 44 registry forms is not automatically Phase F, but proving every
parameter combination's payload, axis, metadata, internal-carrier, and policy
output is. The `LookupFormulaSpec`, opaque `fill_value`, and tuple-finding
examples show this is not theoretical. The narrowest sequence-compatible rule
is a closed uncertified default plus a small initial set of exact invocations
whose preservation is already implied by infrastructure-level behavior. Model
E-style validation after all other steps may be a temporary migration strategy;
it must not be advertised as proof that current outputs all satisfy the
proposed grammar.

===== Performance sanity

Initial ingress is O(total DataFrame and ExactTable data cells plus metadata
nodes). Each uncertified return repeats that cost. This is reasonable once at
framework ingress and after occasional plugins. Re-establishing after every one
of a long 44-step built-in pipeline can multiply full-workbook traversal and is
unattractive for large object-dtype workbooks. Corrected Model D therefore has a
real performance advantage over permanent Model E, but only after bounded
preservation evidence exists. No benchmark is needed to decide the contract.

===== Can family defensive code actually be removed?

[cols="4,3,4",options="header"]
|===
|Representative path |After corrected Phase E |Reason

|Discriminator-split checks rejecting mapping/list/set/tuple ordinary values
|Generic arbitrary-container part can become redundant.
|A conforming ordinary cell excludes those carriers; family discriminator
rules still remain.

|Repeated shared physical-column addressability checks in XRef/Cell Codec
|Can be removed only where the API documents the conformance precondition.
|Family flat-string/role/identity requirements remain; direct unsupported calls
have no automatic guarantee.

|FK/validation missing recognition
|Must remain.
|Validate-only preserves `pandas.NA`, NaN, NaT, and empty string differences.

|Join/groupby/set/sort protections
|Must remain where family semantics require them.
|Ingress does not prove hash/order/comparison compatibility.

|Formula helper and renderer handling
|Must remain under an explicit internal-Intent contract.
|FormulaSpec is deliberately not an ordinary scalar (F1).
|===

Phase E can provide meaningful simplification, but only the first two bounded
classes of generic defenses are candidates. It cannot become a reason to delete
semantic, Missing, comparison, reconstruction, persistence, or sink checks.

===== Composite seam and Pydantic

The empty `DeclaredComposite` branch is a useful reminder that the scalar
grammar is not permanently closed. Depth, cardinality, codec, schema, operator
capability, and diagnostics are appropriate entry criteria for a future
bounded composite, not an implementation today. The seam must not imply that
all future structured/nested values are cell payload; child frames and trusted
structural carriers remain separate.

No concrete Phase-E payload use site justifies Pydantic. Scalar carrier
classification and pandas/ExactTable traversal are project-owned semantic
code. Metadata substrate walking is simple and its schemas are not inventoried.
E7 may later present a declarative description-parsing use site, but the
extension surface and policy metadata are unsettled; technology selection is
future E7 design input, not a payload blocker or prohibition. *No Pydantic ADR
is warranted now.*

===== Contract versus mechanism

[cols="4,3,4",options="header"]
|===
|Statement |Classification |Review

|Six categories, full ordinary-cell traversal, no scalar coercion,
postcondition/non-guarantees
|CONTRACT-LOAD-BEARING
|Keep, subject to internal-Intent correction.

|Exact built-in metadata dict/list vocabulary
|CONTRACT-LOAD-BEARING BUT EVIDENCE-GATED
|Do not fix before compatibility inventory proves it or records migrations.

|Exact axis grammar
|PREMATURE MECHANISM PRESENTED NORMATIVELY
|Retain existing physical-label authority until the census accepts a tighter
contract.

|Typed boundary failure kinds and safe structural locations
|CONTRACT-LOAD-BEARING
|Exact public class names/field layout may remain implementation detail.

|Staged top-level mapping and owned metadata copies
|USEFUL IMPLEMENTATION GUIDANCE
|Supports atomicity; equivalent pure staging could satisfy the contract.

|Immutable effective context
|USEFUL IMPLEMENTATION GUIDANCE
|One stable policy per macro is load-bearing; public dataclass/API is not.

|Preserving producer notion
|CONTRACT-LOAD-BEARING
|Exact-invocation granularity is missing; registry/BoundStep field is mechanism.

|E1-E7 exact API/export implications
|USEFUL IMPLEMENTATION GUIDANCE
|Some E6 API work may be discovered; do not precommit a public policy class.
|===

==== 15. Findings

===== TRUSTED-INGRESS-DESIGN-REVIEW-F1 -- Blocking -- the closed ordinary-cell postcondition rejects the repository's maintained framework-authored formula Intent path

*Exact FTR sections:* 7 (Payload grammar), 10 (Structural-carrier contract), 12
(Trust propagation), 17 (Downstream postconditions), 19 (Bounded-composite
seam), 23/E2-E5, and 24 (formula sink policy defer).

*Primary authority:* Technical Model ch08 Internal Scalar Value Model explicitly
classifies FormulaSpec as framework-authored output Intent, not a payload
scalar; spreadsheet backend capability profiles and the validation/writer ADR
keep backend-neutral intent distinct from sink translation.

*Runtime/evidence:* `pipeline.registry` exposes `add_lookup_helpers`;
`domain.transformations.enrich_lookup` documents and implements
`helper_value_mode="formula"` by storing `LookupFormulaSpec` in DataFrame cells;
the User Guide documents that mode; XLSX/ODS renderers consume it. Focused tests
confirm the output. The FTR explicitly excludes FormulaSpec from every ordinary
cell and declares no Phase-E `DeclaredComposite` variant.

*Architectural impact:* A preserving declaration is impossible for the formula
mode, while an uncertified return followed by required full re-establishment
rejects it before cleanup/save. E5 would break a supported framework-managed
workflow. Deferring generic formula *ingress/roundtrip* policy does not resolve
the already-supported framework-authored output-Intent lifecycle.

*Smallest correction:* Before E2/E4, add an explicit, bounded lifecycle for
framework-created FormulaSpec Intent (or explicitly migrate/remove the current
mode with compatibility/release treatment). State whether it is a special
producer-created cell Intent, a dedicated structural carrier, or moved out of
ordinary DataFrames; define which consumers may see it and how re-establishment
recognizes it. Do not admit arbitrary external formula objects and do not use
the future `List[Scalar]` seam as a false substitute.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F2 -- Blocking -- producer preservation is scoped too broadly and E4 is not bounded independently of family hardening

*Exact FTR sections:* 4 questions 1/9/10, 5 (Model D), 12 (Trust propagation),
17 (Postconditions), and 23/E4-E5.

*Primary authority:* The robustness ADR requires checkable boundary contracts;
the roadmap orders Phase E before the Domain Maintainer Contract and Phase-F
family hardening. The trusted-runtime ADR does not make built-in or registered
code payload-preserving.

*Runtime/evidence:* There are 44 registry forms and no producer metadata on
`BoundStep`. A direct probe showed `contract_xref(fill_value=object())` emitting
that object into a cell. `validate_references` emitted tuple row-index labels
into finding cells. Formula mode and grouped configurations add further
configuration-dependent outputs. Factories, wrappers, mutable configs, and
nested callbacks prevent a once-per-callable proof.

*Architectural impact:* E4's “inventory registered producers” plus output tests
either certifies too broadly and makes the downstream postcondition false, or
requires broad parameter/family proof before E5 and accidentally pulls Phase F
ahead of its roadmap gate. Model D's claimed implementation advantage is not
currently enforceable.

*Smallest correction:* Define preservation at exact bound behavior/invocation
granularity; callable/family review supplies eligibility only. Keep every
unknown option/callback/wrapper uncertified. Bound E4 to a small closed initial
certificate set and permit conservative re-establishment for all other steps.
Inventory current non-grammar outputs as compatibility/design work rather than
silently treating them as producer test failures.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F3 -- Important -- the exact DataFrame axis grammar is normative before its required compatibility evidence and is narrower than existing physical-label authority

*Exact FTR sections:* 7 (Axis grammar), 10 (DataFrame contract), 17
(Postconditions), and 23/E2.

*Primary authority:* Existing shared physical-label authority permits
non-missing-like, hashable, deterministically self-comparable, unique labels and
explicitly is not string-only; parser/reconstruction and family-specific axis
contracts remain separate.

*Runtime/evidence:* Numeric and tuple/MultiIndex labels are supported. The
existing physical contract is open to other safe hashable labels. No accepted
inventory covers all framework-generated and consumer-reachable column/index
extension types. Row indexes are traversed positionally and need not be Domain
data.

*Architectural impact:* E2 could reject currently supported labels for no
demonstrated downstream safety gain. Section 10's compatibility prerequisite
cannot repair a design that already fixes the narrower grammar and claims it is
not a new universal column-label type system.

*Smallest correction:* Make the exact axis vocabulary provisional. Complete
and accept the E2 axis census first; either confirm the scalar/tuple grammar
with named migrations, or retain the existing physical-column contract and add
only the minimal safe row-index rule. Define “deterministically comparable” as
self-comparability, not ordering.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F4 -- Important -- metadata vocabulary and delegate governance are fixed before the relevant lifecycle inventory and permit a grammar/atomicity bypass

*Exact FTR sections:* 11 (Metadata contract), 15 (Failure atomicity), 16
(Diagnostics), and 23/E3.

*Primary authority:* ch06 separates metadata structure from family semantics;
the robustness ADR permits narrow owned representations; the persistence model
and `FTR-META-LIFECYCLE-INVENTORY-P5` say the complete runtime/persisted root
inventory is not yet established.

*Runtime/evidence:* Current metadata is mostly mapping/list/scalar, but current
Legend Blocks accepts general Mapping, invokes truthiness/`str`, shallow-copies,
and retains nested aliases. Runtime and persistence have many roots/facets and
the manual registry is not runtime authority.

*Architectural impact:* Unsupported existing shapes could be rejected without
migration, and an unconstrained root delegate could introduce opaque values,
mutate aliases, inspect hostile values, or become a hidden extension registry.
The central owned-substrate and atomicity postconditions would then be false.

*Smallest correction:* Treat the exact dict/list grammar as acceptance-gated by
a focused external/runtime metadata node census. Normatively require delegates
to be centrally reviewed, staged/pure, non-extensible by arbitrary family code,
and to return either the common substrate or a named explicit root contract
that central ingress rechecks.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F5 -- Important -- YAML `_meta.yaml` rejection lacks a compatibility and migration decision

*Exact FTR sections:* 4 question 7, 11 (Metadata contract), 23/E3, and 35-like
compatibility consequences implicit in the design.

*Primary authority:* Spreadsheet Parse Contract keeps backend conversion
backend-owned; the accepted Minimal Internal Value Model and census explicitly
document YAML's current `_meta.yaml`-as-DataFrame behavior. `yaml_dir` is a
documented CLI backend.

*Runtime/evidence:* A real `load_yaml_dir` probe returned a DataFrame at the
reserved `_meta` key; `save_yaml_dir` skips that key. The FTR declares it invalid
but does not choose backend conversion, reserved-name rejection, or a preserved
interpretation.

*Architectural impact:* E3 changes a user-reachable backend behavior without a
decided error/migration/release contract. It may also silently remove a
currently addressable sheet named `_meta`.

*Smallest correction:* Select and document one explicit YAML behavior before
E3, identify whether existing input needs migration, and require release notes
for the observable change. Keep the conversion in the backend rather than
teaching Domain ingress to reinterpret a DataFrame as metadata.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F6 -- Important -- “Trusted Domain Payload” materially collides with the ADR's payload-trust authorship dimension

*Exact FTR sections:* 2 (Vocabulary), 6 (Surface guarantees), 13 (Operator
policy), and every downstream normative sentence using the term.

*Primary authority:* `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` defines “Is the
payload trusted?” as an external authorship/authority question independent of
pipeline-description trust.

*Runtime/evidence:* The FTR defines “Trusted Domain Payload” as conformance, then
later refers to a “trusted-payload authority assessment.” All four combinations
must remain understandable. The same adjective now denotes two different axes
in one contract.

*Architectural impact:* A reader can plausibly infer that conforming payload
came from a trusted author or that less-trusted payload cannot become
conforming. This is security-significant vocabulary ambiguity, not cosmetic
wording.

*Smallest correction:* Rename the conformance state to a term such as
“Ingress-Conformant Domain Payload” or “Conformant Domain Payload,” and reserve
trusted/less-trusted payload for the ADR's authorship dimension. Preserve the
rule that neither authority class bypasses global grammar.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F7 -- Important -- trust epoch and atomicity wording must be explicitly contractual rather than mechanically enforceable isolation

*Exact FTR sections:* 2 (Vocabulary), 12 (Trust propagation), 15 (Failure
atomicity), and 17 (Downstream postconditions).

*Primary authority:* The robustness and trusted-runtime ADRs disclaim protection
against hostile same-process code; pandas DataFrames are mutable; no wrapper or
copy-based ownership system is selected.

*Runtime/evidence:* Current transformations frequently shallow-copy Frames and
reuse DataFrames; current metadata normalization retains nested aliases. A
plugin/closure can retain or mutate a reference after re-establishment, and the
framework cannot detect that without new state/copying architecture.

*Architectural impact:* “Must not retain an out-of-band mutation path” cannot be
mechanically certified as an ingress guarantee. Downstream entitlement is
conditional on caller/producer obligations; failure atomicity cannot assert
concurrent isolation.

*Smallest correction:* State that epoch is an explanatory contract interval,
not runtime state. Make no-out-of-band-mutation a producer/caller obligation.
Narrow atomicity to “ingress/delegates do not mutate caller input and do not
publish staged output before success,” explicitly excluding concurrent or
later external alias mutation.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F8 -- Important -- concrete payload-policy dimensions are premature without an operator use site

*Exact FTR sections:* 4 question 12, 13 (Operator payload policy), 21 (API-shape
implications), and 23/E1.

*Primary authority:* The policy ADR requires operator ownership and per-kind
combination, but deliberately does not choose policy kinds, profile/API
transport, or ingress mechanism.

*Runtime/evidence:* The accepted census and FTR acknowledge no current payload
policy object or transport. No operator case demonstrates a need to distinguish
NumPy versus Python carriers within one semantic category or to narrow metadata
leaves independently of valid framework metadata.

*Architectural impact:* E1 would create generic security knobs and combination
semantics before their effects on backends, framework metadata, or users are
known. Carrier-variant policy can reject semantically admitted values without
improving the ordinary Domain postcondition.

*Smallest correction:* Keep the ADR-derived rule that any policy is operator-
owned, narrowing, immutable per macro, and per kind. Defer carrier-variant and
independent metadata-leaf dimensions until a concrete operator use site defines
them; implement only the minimum evidenced category/structural restrictions.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F9 -- Important -- E7's dependency is real but the Phase-E completion state is undefined

*Exact FTR sections:* 4 question 13, 14 (Description trust), 23/E7, 24 (scope
matrix), and 26 (Acceptance criteria).

*Primary authority:* The runtime/policy ADR requires independent authority
dimensions and pre-activation rejection; the roadmap gates later work on Phase
E; D-T3 also requires the Domain Maintainer Contract.

*Runtime/evidence:* Current configured binding imports before payload load.
`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` exists but has not completed its
architecture assessment. The design says E1-E6 support trusted descriptions
and E7 is a Phase-E sub-slice, without naming whether E1-E6 can close Phase E.

*Architectural impact:* The project could mark Phase E complete while one of its
declared sub-slices and less-trusted-description authority combinations remains
unimplemented, incorrectly unblocking the Maintainer Contract/D-T3; or it could
unnecessarily block a valid payload milestone.

*Smallest correction:* Define lifecycle explicitly: E1-E6 may be accepted as a
payload-conformance milestone under a mandatory trusted-description
precondition, while Phase E remains open until E7, or move E7 to a separately
named lifecycle feature and update roadmap dependencies before implementation.
Publish the interim CLI support boundary.

==== 16. Downstream contract audit and major-contract verdicts

===== Claimed postconditions

[cols="4,3,5",options="header"]
|===
|Claim |Proof class |Review

|Recognized top-level structural carriers
|PROVEN BY INGRESS
|Closed dispatch can prove it, after legitimate internal Intent/carriers are
modeled.

|Physical columns non-missing-like, hashable, self-comparable, unique
|EXISTING STRUCTURAL AUTHORITY / PROVEN BY INGRESS
|Feasible if safe axis classification precedes equality/hash. This is not
sorting compatibility.

|Every column/index label belongs to exact axis grammar
|NOT YET GUARANTEED
|Implementation could enforce it, but compatibility authority is missing (F3).

|Every ordinary DataFrame/ExactTable data cell is admitted scalar/policy
|PROVEN BY INGRESS FOR INITIAL CANDIDATE
|True for initial/re-established ordinary payload; false as a universal runtime
claim while formula Intent exists (F1).

|No opaque objects/collections/callables in ordinary cells
|PROVEN BY INGRESS FOR INITIAL CANDIDATE
|Conditional on corrected internal Intent and producer transitions.

|Scalar carrier identity preserved
|PROVEN BY INGRESS
|Read-only validation proves no ingress coercion, not persistence identity.

|`_meta` is owned valid substrate
|PROVEN BY INGRESS AFTER CORRECTION
|Requires complete copying, evidence-backed grammar, and closed delegates (F4).

|Feature-owned metadata sugar normalized
|DELEGATE CONTRACT ONLY
|Central ingress can prove only if every relevant delegate is run and its output
is checked.

|ExactTable generic self-consistency
|PROVEN BY INGRESS
|Proposed checks are feasible and distinct from reconstruction.

|Grouped/canonical carrier validity
|EXISTING STRUCTURAL AUTHORITY / PRODUCER CONTRACT ONLY
|Constructor proves named local invariants, not scalar conformance.

|Producer chain keeps facts true until next operation
|PRODUCER/CALLER CONTRACT ONLY
|Not mechanically known without wrapper/copy/mutation tracking (F2, F7).
|===

The proposed non-guarantees are strong and correctly exclude family schemas,
FK/key/geometry/reconstruction semantics, sorting/comparison/hash/
serialization support, semantic metadata validity, cleanup/canonical output,
sink safety, persistence identity, description authorization, arbitrary Python
behavior, and sandboxing. Two clarifications should remain explicit during
correction: ingress does not prove resource boundedness, and it does not prove
concurrent/alias isolation. It also does not prove framework-created render
Intent is ordinary scalar payload.

===== Wrong-conclusion test

[cols="4,4",options="header"]
|===
|Potential wrong conclusion |Result

|Conforming payload means trusted author
|Plausible because of the selected name; fails F6.

|Every supported Python API automatically ingresses
|Not derivable; surface matrix is clear.

|`run_pipeline` is unsafe/deprecated
|Not derivable; it remains supported trusted-programmatic composition.

|All built-ins automatically preserve
|Explicitly denied, but certification granularity remains unsafe (F2).

|Plugin output is malicious by definition
|Not derivable; it is merely uncertified.

|Ingress guarantees cross-carrier comparison/sorting
|Not derivable; section 18 explicitly denies it.

|`pandas.NA` behaves like `None`
|Not derivable; carrier identity/Missing caveats remain.

|Every `_meta` value is semantically valid
|Not derivable; family semantics remain local.

|Every framework dataclass is conforming payload
|Risk remains in “trusted by construction”; F2/section 7 correction required.

|Every DataFrame axis is ordinary cell payload
|Not derivable; separate axis grammar is explicit.

|ExactTable reconstruction is centrally validated
|Not derivable; explicit non-guarantee is clear.

|Pydantic is forbidden
|Not derivable; it is an evidence-based nondecision.

|E1-E6 make less-trusted YAML safe
|Not derivable in the FTR, but interim public lifecycle wording is required
(F9).

|Phase E can finish without deciding E7 lifecycle
|Currently plausible; fails F9.

|D-T3 follows immediately
|Explicitly denied; roadmap gate remains.

|Phase E requires broad family hardening before completion
|Currently plausible through E4; fails F2.
|===

===== Major-contract verdict matrix

[cols="4,3,5",options="header"]
|===
|Major contract |Verdict |Qualification/correction

|Model D hybrid coverage
|SOUND WITH CORRECTION
|Correct target; fix internal Intent and exact-invocation certification.

|Framework-managed definition
|SOUND
|Lifecycle property, not public-stability claim.

|Trusted Domain Payload term
|REJECT
|Rename to avoid ADR authorship-axis collision (F6).

|Trust epoch
|SOUND WITH CORRECTION
|Contractual/explanatory only; no isolation claim (F7).

|Preserving producer concept
|SOUND WITH CORRECTION
|Certify exact bound behavior, not name/callable/family (F2).

|Uncertified producer re-establishment
|SOUND WITH CORRECTION
|Protects subsequent macro consumption only; model legitimate Intent (F1).

|`run_pipeline` caller contract
|SOUND
|No automatic transition; publish current/final distinction.

|Direct Domain contract
|SOUND
|Supported APIs get local caller preconditions; internals do not.

|Six-category grammar
|SOUND
|Exact match to accepted Phase D for ordinary scalar payload.

|Validate-only normalization
|SOUND
|Excludes opaque Python; does not unify carrier semantics.

|Missing preservation
|SOUND
|Shared predicate remains required.

|DataFrame traversal
|SOUND
|Complete positional data-cell traversal.

|Axis grammar
|DEFERRED/GATED
|Compatibility evidence must precede exact narrowing (F3).

|ExactTable ingress
|SOUND
|Generic consistency only; reconstruction remains local.

|Metadata substrate
|DEFERRED/GATED
|Node census and YAML decision required (F4, F5).

|Metadata delegates
|SOUND WITH CORRECTION
|Closed ownership, purity, staging, and output recheck required (F4).

|Payload policy
|DEFERRED/GATED
|Retain ADR rules; implement only evidenced dimensions (F8).

|Description-plane split
|SOUND
|Separate owners/timing and pre-import authorization are correct.

|E7 lifecycle
|SOUND WITH CORRECTION
|Define intermediate milestone versus Phase-E completion (F9).

|Atomicity
|SOUND WITH CORRECTION
|Ingress nonmutation/nonpublication, not alias/concurrency isolation (F7).

|Diagnostics
|SOUND WITH CORRECTION
|Feasible; delegates must obey the same safe-inspection rules.

|Downstream postconditions
|REJECT
|Overclaim FormulaSpec absence and producer-chain proof (F1, F2, F7).

|Downstream non-guarantees
|SOUND WITH CORRECTION
|Add resource and alias/isolation clarity; retain all listed separations.

|Composite seam
|SOUND WITH CORRECTION
|Useful future seam; not a solution for current framework Intent.

|Pydantic nondecision
|SOUND
|No current payload use site; E7 can reconsider later.

|E1-E7 slicing
|REJECT
|E2/E3 are evidence-gated, E4 is overbroad, E6 may need API work, E7 lifecycle
is undefined.
|===

==== 17. Required verdict matrices and final readiness verdict

===== Fifteen-question summary

[cols="1,4,3,5",options="header"]
|===
|# |Question |Decision verdict |Correction needed?

|1 |Mandatory coverage model |REVISE |Yes -- F1/F2; corrected staged Model D.
|2 |Meaning of framework-managed |ACCEPT |No; preserve lifecycle meaning.
|3 |High-level entry guarantees |ACCEPT WITH QUALIFICATION |Yes -- trusted-description and corrected transition conditions.
|4 |`run_pipeline` future contract |ACCEPT WITH QUALIFICATION |Publish current/final representation; no hidden ingress.
|5 |Supported direct APIs vs internals |ACCEPT |No material correction.
|6 |Scalar traversal/classification |ACCEPT WITH QUALIFICATION |Separate ordinary scalar input from framework Intent.
|7 |Metadata roots/carriers |REVISE |Yes -- F4/F5 inventory, delegate, YAML migration.
|8 |Ordering around load/bootstrap/reconstruction |ACCEPT WITH QUALIFICATION |Complete affected-state postcondition and staged delegates.
|9 |Later producer-created/replaced payload |REVISE |Yes -- F1/F2 exact invocation and bounded E4.
|10 |Structural carrier dispatch |REVISE |Yes -- F2/F3; property-specific construction trust.
|11 |Normalization ownership |ACCEPT WITH QUALIFICATION |State the limited safety benefit precisely.
|12 |Operator policy dimensions |DEFER |Implement only evidence-backed kinds (F8).
|13 |Description authorization coordination |ACCEPT WITH QUALIFICATION |Resolve E7 dependency/lifecycle (F9).
|14 |Rejection, diagnostics, atomicity |REVISE |Delegate safety and no isolation overclaim (F4/F7).
|15 |Bounded-composite extension seam |ACCEPT WITH QUALIFICATION |Do not use it to hide FormulaSpec or all structured carriers.
|===

===== Validation evidence

The review used read-only probes and existing tests only; no tracked runtime or
test file was changed.

[cols="3,5",options="header"]
|===
|Validation |Result

|Focused tests
|`tests/unit/core/test_scalar_values.py`,
`tests/unit/domain/tabular/test_physical_labels.py`, the formula-mode producer
test, and `tests/unit/io_backends/test_quick_wins.py`: 99 passed.

|Registry probe
|44 total entries: 38 `StepRegistration`, six bare factories.

|Producer probes
|`validate_references` preserved tuple row indexes in ordinary finding cells;
`contract_xref` preserved an opaque configured fill object in an ordinary
matrix cell; formula-mode tests produced `LookupFormulaSpec` cells.

|Carrier/backend probes
|The accepted scalar matrix classified exactly as Phase D specifies; a raw
invalid-geometry ExactTable constructed successfully; real `load_yaml_dir`
projected `_meta.yaml` to a DataFrame.

|Standalone rendering
|The FTR and this review both rendered with Asciidoctor at failure level ERROR
with exit status 0.

|Containing backlog rendering
|`docs/backlog/backlog.adoc` produced HTML with exit status 0 under the
repository's normal Asciidoctor invocation. It also emitted pre-existing
section-level diagnostics from unrelated included FTRs and the already-authored
Trusted Ingress include; strict failure-level rendering therefore does not pass
at the reviewed baseline.

|Patch hygiene
|`git diff --check` exited 0 with no output before commit.
|===

===== Overall architecture verdict

*CHANGES REQUIRED -- PHASE-E CONTRACT MUST BE MATERIALLY REDESIGNED BEFORE
IMPLEMENTATION.*

The central direction is valuable: mandatory framework ingress, a closed
ordinary scalar grammar, validate-only admission, explicit carrier dispatch,
post-custom-return checking, caller-owned `run_pipeline`, separate description
authorization, and bounded downstream non-guarantees form a coherent
architecture. It is not ready for slicing because the currently claimed
postcondition cannot coexist with a maintained framework FormulaSpec workflow,
and E4 cannot establish preservation at the proposed scope without either
false certification or premature family hardening. Axis, metadata, YAML,
policy, naming, aliasing, and lifecycle corrections are bounded but must be
resolved in the design before implementation starts.

No new ADR is required. No Pydantic decision is required. Phase E does not
authorize D-T3, a family pilot, broad family hardening, or less-trusted
configured execution before E7.

PHASE-E TRUSTED INGRESS CONTRACT
REQUIRES CORRECTION BEFORE IMPLEMENTATION.

==== Review 001 Disposition

This disposition records the design-only response to Review 001. It does not
alter the review's original findings, matrices, evidence, or verdict above;
fresh-context Follow-up Review 002 decides acceptance.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F1 -- RESOLVED

*FTR sections changed:* 2, 4 questions 1/3/6/9-11/15, 5, 7, 9-10, 12,
17-20, 23-30.

*Architectural resolution:* the corrected contract separates
Ingress-Conformant Domain Payload (ordinary Scalar cells only) from Conformant
Framework Execution Payload with explicitly declared framework-owned Intent
roles. The exact reviewed
`add_lookup_helpers(helper_value_mode="formula")` behavior may introduce
`LookupFormulaSpec` only in its named helper role; only declared
Intent-aware/transparent consumers and FormulaSpec-capable XLSX/ODS output
adaptation may receive it. External and uncertified-return FormulaSpec rejects,
and FormulaSpec is neither Scalar nor `DeclaredComposite`.

*Gate/migration consequence:* current formula-helper behavior is classified
CURRENTLY SUPPORTED -- PRESERVE and receives focused E4-E5 lifecycle evidence;
external formula ingress/roundtrip and sink capability remain separately owned.
No runtime, test, ADR, Technical Model, Project Memory, or implementation change
was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F2 -- RESOLVED

*FTR sections changed:* 2, 4 questions 1/3/9-10, 5, 12, 17, 20-21, 23-30.

*Architectural resolution:* preservation is scoped to one exact reviewed bound
behavior and effective configuration. Callable/family review establishes only
eligibility; unknown options, nested callbacks, mutable post-bind state, and
wrapper/factory composition remain uncertified unless explicitly covered. E4
uses a closed uncertified default and a small, possibly zero, initial preserving
set. E5 conservatively re-establishes every still-uncertified relevant return.

*Gate/migration consequence:* Phase E does not audit all 44 registry forms or
pull family hardening forward. Later exact certificates are performance
optimizations, not milestone prerequisites. No runtime, test, ADR, Technical
Model, Project Memory, or implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F3 -- RESOLVED

*FTR sections changed:* 4 questions 6/10, 7, 9-10, 17-18, 23-29.

*Architectural resolution:* the proposed closed Scalar/tuple axis grammar is no
longer normative. Existing physical-column authority remains: labels are
non-missing-like, hashable, unambiguously self-comparable, and unique where
physical addressing requires it. Self-comparability is not total ordering.
Row indexes are positional locations and receive no closed payload grammar.

*Gate/migration consequence:* an independently accepted E2 compatibility census
and design gate is required before any narrower vocabulary; supported behavior
cannot be tightened silently. No runtime, test, ADR, Technical Model, Project
Memory, or implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F4 -- RESOLVED

*FTR sections changed:* 4 questions 7-8/14, 9, 11, 15-16, 17-18, 23-29.

*Architectural resolution:* Phase E now accepts only the abstract requirement
for an owned, bounded, acyclic metadata substrate without opaque graphs. The
exact dict/list/scalar vocabulary is a preferred hypothesis, not accepted fact.
Delegates are centrally reviewed/owned, statically selected, staged,
non-mutating, safe-inspection constrained, output-bounded, post-checked, and
unable to absorb family semantics or bypass the global structural contract.
Legend Blocks must migrate under those rules.

*Gate/migration consequence:* a focused, independently accepted pre-E3 metadata
lifecycle census must confirm the hypothesis or record a bounded alternative
and migrations. No runtime, test, ADR, Technical Model, Project Memory, or
implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F5 -- RESOLVED

*FTR sections changed:* 4 questions 7-8, 11, 23-26.

*Architectural resolution:* YAML option A is selected. The YAML backend reserves
stem `_meta`, safe-loads at most one `_meta.yaml`/`_meta.yml` mapping before
Domain ingress, and rejects duplicate spellings or a non-mapping root at the
parse boundary. Domain ingress never reinterprets a metadata DataFrame.

*Gate/migration consequence:* an ordinary YAML sheet named `_meta` must be
renamed; intended metadata must satisfy the E3-accepted substrate. E3 owns the
loader behavior and release-note obligation. The saver may continue omitting
metadata, so no round-trip promise is added. No runtime, test, ADR, Technical
Model, Project Memory, or implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F6 -- RESOLVED

*FTR sections changed:* 2, 4-6, 8-9, 12-14, 17, 20-21, 23-30.

*Architectural resolution:* the conformance state is renamed
Ingress-Conformant Domain Payload, with Conformant Framework Execution Payload
for declared internal Intent states. “Trusted/less-trusted payload” is reserved
for the ADR authorship dimension: either authorship class may conform or fail,
and neither bypasses the global grammar. Description authority remains
independent.

*Gate/migration consequence:* terminology changes are design-only and carry no
runtime migration. No runtime, test, ADR, Technical Model, Project Memory, or
implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F7 -- RESOLVED

*FTR sections changed:* 2, 4 questions 9/14, 12, 15, 17-18, 20-21, 26-30.

*Architectural resolution:* “conformance interval” is explicitly
explanatory/contractual state, not a token, mutation detector, alias tracker, or
isolation guarantee. No-out-of-band mutation is a caller/producer obligation.
Atomicity promises only that ingress and approved delegates do not mutate caller
input through their own work and publish no staged candidate before success.

*Gate/migration consequence:* concurrency, retained aliases, later mutation,
plugin effects, prior steps, and external side effects are excluded; no deep
DataFrame copy is mandated. No runtime, test, ADR, Technical Model, Project
Memory, or implementation change was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F8 -- RESOLVED

*FTR sections changed:* 4 question 12, 13, 16, 20-21, 23, 25-29.

*Architectural resolution:* E1 implements the global grammar with no
configurable cell-category, carrier-variant, metadata-leaf, or structural-kind
policy matrix. The contract preserves only the ADR architecture for a future
evidenced per-kind narrowing use site: trusted-operator supplied, fixed for its
interval, narrowing-only, and unable to admit unsupported values.

*Gate/migration consequence:* no policy knob or transport is implemented now;
description capability authorization remains the evidenced E7 policy use site.
No runtime, test, ADR, Technical Model, Project Memory, or implementation change
was introduced.

===== TRUSTED-INGRESS-DESIGN-REVIEW-F9 -- RESOLVED

*FTR sections changed:* 4 question 13, 14, 23, 25-30.

*Architectural resolution:* lifecycle option A is selected. E1-E6 may reach an
independently accepted Phase-E Payload-Conformance Milestone only under a
mandatory trusted-description precondition. Phase E remains Open until E7 is
independently accepted; the Domain Maintainer Contract, Phase F, and D-T3 remain
blocked. Less-trusted configured YAML cannot be advertised as protected during
the intermediate state.

*Gate/migration consequence:*
`FTR-PIPELINE-DOTTED-EXTENSION-SURFACE-P4A` remains a still-open prerequisite
whose accepted output is required before E7. No runtime, test, ADR, Technical
Model, Project Memory, roadmap, or implementation change was introduced.
