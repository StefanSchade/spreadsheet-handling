===== FTR-AGENT-WORKFLOW-COORDINATOR-P5 -- Coordinate bounded repository-native agent workflows

*Lifecycle:* Open

*Status:* *ACCEPTED AS V1 IMPLEMENTATION AUTHORITY -- IMPLEMENTATION SLICES
1-3 IMPLEMENTED, INDEPENDENTLY VERIFIED, AND MAINTAINER-ACCEPTED; SLICE-4A
IMPLEMENTATION (S4A-R1..R9 VERIFIED CLOSED) MAINTAINER-ACCEPTED; THE EXACTLY-ONE
DISPOSABLE SLICE-4 N=1 REPLACEMENT PROOF IS CONSUMED (REVIEW ca31a8e0); THE
DETERMINISTIC PROOF HARNESS IS IMPLEMENTED AND ACCEPTED; THE PRIOR N=1 LIVE
HAPPY-PATH PROOF EXPOSED S4A-R10, WHICH IS VERIFIED CLOSED (FIX
6bfad5bb/41ff4e35, EVIDENCE d1e01b94, RE-REVIEW af933f7d); THE CORRECTED N=1 LIVE
HAPPY-PATH PROOF IS ALSO CONSUMED AND REACHED THE PROVIDER, BUT FAILED ON A SECOND
CONCRETE STRUCTURED-OUTPUT-SCHEMA COMPATIBILITY ERROR (EVIDENCE
agent_workflow_coordinator_slice4a_corrected_live_schema_failure_evidence_2026-08-31);
HAPPY-PATH WFC-OUTPUT LIVE PROOF REMAINS REQUIRED AND UNPROVEN; ARCHITECTURE
VERDICT A AND THE CORRECTED LEVEL-3 SMOKE RUNNER ARE MAINTAINER-ACCEPTED; THE
ONE-SHOT LEVEL-3 AUTHORIZATION IS CONSUMED BY A FAILED LOCAL CLI
WORKSPACE/TRUSTED-DIRECTORY PREREQUISITE; ONE BOUNDED DETERMINISTIC
SMOKE-WORKSPACE CORRECTION IS AUTHORIZED, BUT NO SECOND LEVEL-3 INVOCATION OR
LEVEL-4 COORDINATOR E2E IS AUTHORIZED; ROUTINE SLICE-4 USE, SLICE 4B, SLICE 5,
AND AUTONOMOUS MULTI-HOP REMAIN NOT AUTHORIZED*

*Priority:* P5

*Type:* Internal developer tooling / AI-assisted engineering workflow

*Review history:* Draft `902d777`; broad independent adversarial readiness review
`82fcf12` returned `MATERIAL REVISION REQUIRED` (finding authority
`docs/warm_storage/global_reviews/agent_workflow_coordinator_readiness_review_2026-08-25.adoc`);
bounded correction `7a4f979` addressed `R1 + R2..R8`; independent bounded
re-review `bb9154c` returned `ACCEPT -- READINESS CORRECTION VERIFIED`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_readiness_correction_review_2026-08-26.adoc`).
R1 and R2..R8 are closed; R10/R11 remain known non-blocking minors.
The independently re-reviewed Slice-4 readiness findings S4-READY-R1..R7 are
closed. The maintainer selected the external trusted checkout-associated state
reconciliation for WFC-PERSIST-02.
Independent persistence authority review `421bc71` returned `ACCEPT --
WFC-PERSIST-02 AUTHORITY CORRECTION VERIFIED`; the maintainer now authorizes
one disposable Slice-4 `N=1` proof only.

*Acceptance:* The readiness gate is satisfied. By maintainer decision on
2026-08-27, the proposed architecture and this FTR are accepted as V1
implementation authority. Implementation Slice 1 (reducer/model/replay,
section 11.1) is implemented and maintainer-accepted following the compact
implementation/review chain `9097ceb` through `36bccde`. Implementation Slice 2
(section 11.2) is independently verified and maintainer-accepted following the
implementation/review chain `32e322a` through `a014e3d`. Slice 3 (section
11.3) is independently verified and maintainer-accepted following `fb2c012`
through independent bounded re-review `403ff88`; S3-R1 and S3-R2 are closed and
S3-R3 remains deferred/non-blocking for future correction-routing/evidence-fail
semantics. Slice 4 is authorized only for the disposable proof in section 11.4;
routine real-repository use, Slice 5, autonomous multi-Hop execution, DMC Family
Cycle 2, a DMC work item, Trusted-Ingress work, and all deferred capabilities
remain unauthorized and separately gated. S1-R3 remains a deferred non-blocking
observation for future Slice-3/real-agent-result hardening.

[IMPORTANT]
====
This FTR is accepted as V1 implementation authority. Implementation Slices 1-3
are implemented, independently verified, and maintainer-accepted. The Slice-4A
implementation is maintainer-accepted following the bounded correction review
chain `5b09ea01` through `0f2c50fe` (findings S4A-R1 through S4A-R9 verified
closed). Both disposable proof attempts of `WFC-S4-PROOF` -- the historical first
Run and the authorized replacement Run
`RUN-a9cc83d29171494795c0789f97571288`
(`invocation_id=INV-683fbba33b634246b25a798a4b8fc11f`) -- remain immutable
historical evidence: neither is resumed or retried. The replacement proof
authorization is *consumed*. Independent review `ca31a8e0` is accepted as the
maintainer's factual basis: the replacement proof is accepted as valid real
failure-path evidence (started/unresolved invocation, one conservative Hop charge,
independent Git observation, unchanged Git reality, dispatch marker retained,
`reconcile_required`, no retry); no Slice-4A implementation defect was exposed; and
the real happy-path `WFC-OUTPUT` proof required by section 11.4 remains *unproven*.
That acceptance criterion is not weakened or rewritten ex post. The deterministic
proof harness (`scripts/workflow_coordinator/proof_harness.py`) is now implemented
and independently accepted (review `8cea13df`); truthfulness findings S4H-R1 and
S4H-R2 are closed following correction `0b950dc4` / evidence `eb5a5037` and bounded
re-review `2ea964a2`, and S4H-R3/R4/R5 remain non-blocking WATCH residuals that
need no correction before the proof. On that basis the maintainer now authorizes
exactly ONE new disposable `N=1` live happy-path proof of `WFC-S4-PROOF`, launched
manually from the ordinary non-nested host through that harness (section 11.4.1);
until that single invocation establishes it, the real happy-path `WFC-OUTPUT`
criterion remains *unproven* and is not weakened. No other live invocation, resume,
retry, or second proof is authorized, and no broader authority is granted. Routine
real-repository use, Slice 4B, Slice 5, and autonomous multi-Hop execution remain
unauthorized, as do DMC Family Cycle 2, a DMC work item, and Trusted-Ingress
work. This remains an active backlog item (not `Done`) and is not moved to cold
storage; the hypothesis paper, accepted architecture, and readiness/implementation
review material remain active engineering inputs. End-of-cycle WFC-LIFE closure
occurs only after V1 implementation and closure are accepted.
====

==== 1. Purpose and design inputs

V1 addresses the repeated human orchestration around bounded engineering-agent
work: preparing tasks, invoking roles, establishing actual Git truth, collecting
deterministic evidence, validating structured results, selecting only permitted
next actions, coordinating reviews/corrections, stopping for human semantic
authority, and preserving ex-post observability across autonomous Hops.

The empirical and architectural reasoning is not repeated here. Inputs are:

* `docs/warm_storage/concepts/agent_workflow_coordinator_concept_2026-08-25.adoc`
  -- non-binding evidence/hypothesis;
* `docs/warm_storage/concepts/agent_workflow_coordinator_proposed_architecture_2026-08-25.adoc`
  -- accepted formal architecture design projected into this contract;
* `docs/warm_storage/global_reviews/agent_workflow_coordinator_readiness_review_2026-08-25.adoc`
  -- finding authority for the bounded correction, not implementation authority.

If this FTR conflicts with the proposed design, readiness review must record the
conflict; implementation must not silently choose a third contract.

==== 2. V1 scope and planned placement

===== 2.1 Required V1 capability

V1 is a local, repository-native, Git-centered, policy-bounded coordinator with:

* one coordinator process, repository, worktree, active Run, and agent
  invocation at a time;
* WorkItem semantic input plus Run, Hop, and Finding operational records;
* finite validated workflow profiles and deterministic route reduction;
* one configured agent adapter plus an optional, policy-bounded supervisor role;
* Git reconciliation, fixed trusted evidence callables, structured results,
  finding convergence, autonomy budget, checkpoints, interruption/resume, and
  explicit human authority stops;
* a first routine profile, a non-production non-DMC bug-workflow fixture that
  falsifies complex-profile overfit, and, only in Slice 5, a DMC-shaped profile
  without DMC vocabulary in generic mechanics.

Planned implementation placement follows the proposed architecture:

* repository-local executable mechanics: `scripts/workflow_coordinator/`;
* versioned profiles, repository policy, and prompt components:
  `docs/ai_info/workflows/`;
* focused unit/integration/architecture tests under the existing `tests/`
  topology.

No coordinator code belongs under `src/spreadsheet_handling/`; it is developer
tooling, not product/runtime API. No new dependency is justified by V1 unless
the readiness review or a separately reviewed slice demonstrates necessity.

===== 2.2 Deferred capabilities

Deferred without V1 scaffolding: coordinator-created or mixed semantic commit
ownership; mandatory trailers/hooks; multiple agent providers/models and
provider-diversity policy; extraction into `tools/` or a package; automated
warm/cold run archival; quantitative trust scoring; parallel worktrees/runs;
remote/distributed execution; SonarQube implementation; semantic/vector
retrieval; web UI; autonomous/preauthorized child Runs; and `continuing`
conversation context unless a separately reviewed real adapter declares and
proves it as an optional capability.

===== 2.3 Explicit non-goals

* No workflow DSL, executable profile code, graph engine, database, event store,
  plugin manager, DI framework, queue, or distributed fault tolerance.
* No universal agent abstraction, general sandbox/security product, automatic
  workflow invention, automatic semantic acceptance, or Project Memory run
  store.
* No Domain, FK/Lookup, Trusted-Ingress, public CLI/YAML, spreadsheet, demo,
  release, or consumer behavior change.
* No real Run/Hop manifests, Git hooks, enforced trailers, live workflow agent,
  Sonar service, vector index, or external infrastructure in this drafting task.

===== 2.4 FTR sizing preflight

This is a justified FTR because it introduces architectural developer tooling
with acceptance contracts. It does not touch spreadsheet roundtrip, `_meta`,
Domain registries, XLSX/ODS parity, or current public product surfaces. The
workflow profile schema is an internal coordinator input, not the existing
public pipeline YAML surface. The five slices separate the state/reducer,
repository transaction, adapter, live invocation, and multi-Hop risks.

==== 3. Binding V1 system and model contracts

These contracts become binding only after independent FTR acceptance.

===== 3.1 System boundary

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-SYS-01
|V1 shall execute serially for exactly one configured repository/worktree and
at most one active Run and one agent invocation. A suspended parent is not
active while its separate child Run executes.

|WFC-SYS-02
|Coordinator mechanics, workflow profile, repository engineering policy,
operator/Run policy, and task payload shall remain distinguishable inputs even
when implemented by a small number of modules/files.

|WFC-SYS-03
|The generic coordinator shall contain no DMC, family, cycle, slice, or
Trusted-Ingress E-phase primitive. Profiles and WorkItem labels may contain
those terms.

|WFC-SYS-04
|V1 shall remain useful with `max_autonomous_hops=1` and shall require no
future-only service or infrastructure to run.

|WFC-SYS-05
|The implementation shall stay repository-local under the planned placement
unless readiness review explicitly corrects a demonstrated placement conflict.
|===

===== 3.2 WorkItem input, Run, Hop, and Finding

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-MODEL-01
|WorkItem shall be semantic input, not a mutable operational entity. Its compact
specification shall carry stable `work_item_id`, task/authority source, profile
reference, repository/scope, initial phase, `max_autonomous_hops`, and optional
profile labels.

|WFC-MODEL-02
|A Run shall identify one execution attempt with `run_id`, schema version,
WorkItem reference, pinned profile/policy revision, repository identity,
baseline/current observed HEAD, current phase/status, budget used/limit, compact
Hop summaries, Findings, child/suspension reference, reconcile reason/anchor,
and stop reason. Finite next route keys shall be derived from pinned profile,
current phase, and preemption state rather than persisted as independent state.

|WFC-MODEL-03
|Run state shall not duplicate full diffs, Git log, changed-file contents, or
other facts reconstructable from stored commit IDs and Git.

|WFC-MODEL-04
|A Hop shall identify one adapter-accepted invocation through run-local
`hop_id`/sequence and record role/context, start/end, independently observed
base/end commits, actual commit range, invocation/result status, Finding delta,
evidence refs, route/stop, and concise attributed rationale.

|WFC-MODEL-05
|A Finding shall have stable run-local ID, concise invariant/problem statement,
blocking flag, state, introducing/last-changing Hop, evidence refs, concise
disposition, and optional successor/external WorkItem reference. It shall not
grow assignees, due dates, comments, or general issue-tracker workflow.

|WFC-MODEL-06
|Review descriptors, observations, transition decisions, prerequisite links,
and checkpoints shall remain nested ordinary data or generated views rather
than additional operational aggregate roots.
|===

==== 4. Workflow profile, route, and authority contracts

===== 4.1 Profile data and route effects

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-PROFILE-01
|Human-authored WorkItem specifications, profiles, and structured repository
policy shall use strict validated YAML; natural-language prompt components are
versioned text referenced by that YAML. Machine-written transient Run state
shall use JSON. Unknown fields fail closed, and no equivalent second
human-authored serialization is provided. Phase keys are opaque to generic code.

|WFC-PROFILE-02
|Each phase shall declare role/prompt policy, explicit role/modifier and
repository-policy component selections, `durable_artifact` policy, permitted
route keys, evidence requirements, authorized scope/actions, human gates, and a
review descriptor when the phase is a review.

|WFC-PROFILE-03
|Profiles shall express only five route effects: `goto`, `complete`,
`await_human`, `suspend_for_prerequisite`, and `stop`.

|WFC-PROFILE-04
|`goto` selects one declared target phase; `complete` ends the Run without
claiming authority beyond profile policy; `await_human` records a concrete
question; `suspend_for_prerequisite` records an explicitly human-authorized child
and clears stale next action; `stop` ends autonomous progress without claiming
completion.

|WFC-PROFILE-05
|Profile data shall not contain executable code, arbitrary conditions, computed
graphs, fan-out, or user-defined loops. A loop is a finite declared route edge.

|WFC-PROFILE-06
|A profile or pinned policy mutation during a Run shall be an anomaly requiring
stop and common reconcile/resume; an agent result cannot authorize its own
profile change.
|===

===== 4.2 Routing precedence and authority

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-AUTH-01
|Routing precedence shall be: mechanical preemption, deterministic policy
reduction, optional declared supervisor judgment, then human semantic authority.

|WFC-AUTH-02
|Mechanical preemption shall stop on malformed state/result, Git anomaly,
policy mutation, budget prohibition, required evidence-provider error,
`requires_human=true`, or any non-null unresolved escalation; no supervisor may
override it. Human/escalation preemption applies regardless of outcome or valid
requested route, including `outcome=completed` with `goto`/`complete`.

|WFC-AUTH-03
|Only after mechanical preemption clears, deterministic reduction shall use the
validated routing envelope, current review authority/blocking policy, Finding
state, evidence state, budget, and current finite route table. It shall route
automatically only when policy determines a single permitted result.

|WFC-AUTH-04
|A supervisor may be invoked only at a profile-declared judgment point, shall be
read-only, consumes a Hop, receives one bounded question plus finite route keys
and compact history, and may classify/recommend, choose only one already
authorized key, or request a human.

|WFC-AUTH-05
|A supervisor shall not invent routes, mutate profile/policy, enlarge scope,
close or reopen a Finding, downgrade blocking status, accept a residual,
supersede/reclassify a Finding as an authoritative disposition, create semantic
ownership, approve public/API choices, activate any child/work item, raise review
authority, or claim project/Project-Memory decisions. Profile delegation cannot
grant these powers; the supervisor may only route to the declared gate/human.

|WFC-AUTH-06
|Human escalation shall be mandatory for new/conflicting semantic ownership or
authority, plausible public/API/compatibility choices, trust/security redesign,
capability deletion, workflow/profile redesign, unauthorized scope, unapproved
nested work, roadmap/Project Memory governance, and any undelegated decision. AI
may discover, propose, and escalate these questions, but cannot self-authorize
their disposition.
|===

==== 5. Review, Finding, and convergence contracts

===== 5.1 Review dimensions and anti-ceremony rule

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-REVIEW-01
|Every review phase shall independently declare breadth/search scope, authority,
and context policy. Breadth selects the falsification surface and grants no
authority.

|WFC-REVIEW-02
|Review authority is `advisory` or `gate`. Advisory results may add evidence or
Findings but cannot advance/complete. A gate authorizes only its declared route
under its blocking policy.

|WFC-REVIEW-03
|`fresh` context shall not reuse author conversation history and is the required
V1 baseline. `continuing` is not a V1 engine guarantee; a profile may require it
only after a separately reviewed adapter declares/proves bounded-thread support.
Context does not establish independence or authority by itself.

|WFC-REVIEW-04
|A review stage shall exist only where the profile states a distinct epistemic,
falsification, risk, or authority purpose plus trigger, blocking policy, and
exit. Availability shall never imply mandatory execution.

|WFC-REVIEW-05
|After broad review establishes a Finding set, correction/delta review normally
checks known Findings and direct regressions. New material evidence may escape
the bound; unrelated new minors shall not automatically reopen the whole work.
A resolved Finding may reopen only through WFC-FIND-03 authority and new material
evidence.
|===

===== 5.2 Finding lifecycle and convergence

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-FIND-01
|Finding state shall be exactly `open`, `resolved`, `residual`, or `superseded`.
`residual` means genuine and explicitly accepted non-blocking;
`superseded` requires successor/reclassification reference.

|WFC-FIND-02
|Agents may cite an existing ID or propose an unassigned Finding. The
coordinator allocates new IDs and rejects silent invariant/identity rewrites.

|WFC-FIND-03
|Only the current declared gate with the applicable phase/blocking-policy
authority, or human authority, may validate closure, `resolved -> open`
reopening, blocking downgrade, residual acceptance, or
supersession/reclassification. Reopening additionally requires new material
evidence; agent/supervisor reassertion alone does not change authoritative
disposition.

|WFC-FIND-04
|The coordinator shall deterministically expose Finding transitions/reopens,
commit/path and evidence changes, regression state, new material-Finding count,
route repetition, and scope/policy/baseline changes.

|WFC-FIND-05
|Material mechanism change, semantic narrowing, productive progress, goalpost
drift, lack of progress, or wrong owner/workflow are semantic classifications
for delegated supervisor or human judgment. A supervisor classification is only
a routing recommendation and never an authoritative Finding disposition. V1
shall implement no numerical convergence score.

|WFC-FIND-06
|V1 shall permit productive correction beyond three loops, stop non-progressing
loops before budget where judged, and route semantic reclassification out of
local correction. No fixed low `max_review_iterations` may be the primary rule.
|===

==== 6. Bounded autonomy and Git contracts

===== 6.1 Autonomy budget

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-AUTO-01
|Every Run shall carry positive `max_autonomous_hops=N`; architecture shall
support empirical N=1 -> 2 -> 5 -> 10 changes without model/schema redesign.

|WFC-AUTO-02
|Every invocation accepted by the agent adapter consumes one Hop whether it
succeeds, fails, or yields malformed output. Author, reviewer, correction,
re-entry assessor, curator, and supervisor invocations all count. Mechanical
reconcile/resume work does not consume a Hop. After crash recovery, absence of a
dispatch marker proves no Hop consumption; once the atomic dispatch marker
exists, acceptance that cannot be disproved is conservatively charged as one Hop
and never replayed.

|WFC-AUTO-03
|Local failure before adapter/provider acceptance, Git/evidence inspection,
prompt assembly, schema validation, deterministic routing, state/checkpoint
writing, and human actions do not consume Hops. V1 has no hidden retry.

|WFC-AUTO-04
|The coordinator shall stop before the next invocation on early-stop conditions.
At exhaustion it completes safe deterministic post-Hop work, records
`awaiting_human_budget`, renders a checkpoint, and invokes no further agent.

|WFC-AUTO-05
|Resume requires explicit maintainer action, resolved gates, verified current
HEAD/clean state, and a limit not below used Hops. Used Hops never reset within
the Run.
|===

===== 6.2 Git transaction and prospective identity

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-GIT-01
|Every Hop begins at an independently observed Git commit. Before invocation,
the coordinator verifies repository identity, expected ancestry, current HEAD,
and no tracked or non-ignored untracked changes.

|WFC-GIT-02
|After invocation, the coordinator independently observes HEAD/worktree and
computes the actual `base..head` commit range. Agent-claimed commits are hints,
never factual authority.

|WFC-GIT-03
|A successful mutating Hop shall produce 1..n atomic agent-created commits and
end clean before another Hop. Observation-only or failed Hops may have
`base==head`. The coordinator shall not synthesize semantic commits in V1.

|WFC-GIT-04
|The next Hop is prohibited after rewritten/divergent ancestry, HEAD movement
outside the invocation window, forbidden merge commit, dirty tracked/non-ignored
state, out-of-scope path, pinned policy/prompt mutation, commit safety-cap breach,
or unresolved repository/submodule/evidence identity.

|WFC-GIT-05
|Claimed/actual mismatch shall be reported and actual Git used. Mismatch becomes
blocking when coupled to hidden, divergent, out-of-scope, or policy-forbidden
change.

|WFC-GIT-06
|Ignored scratch affects cleanliness only through tracked trusted repository
policy: it may be tolerated in approved roots but must not retain secrets. A
local `.git/info/exclude` entry alone shall not establish the configured
coordinator-state-root invariant. V1 does not promise whole-filesystem
cleanliness.

|WFC-GIT-07
|WorkItem ID is semantic identity; Run ID is one attempt; Hop ID is invocation
identity within Run. Run evidence carries all three. No historical Hop IDs are
invented.

|WFC-GIT-08
|Workflow-produced commit subjects shall project WorkItem/Hop identity in
human-readable `git log --oneline` form compatible with Conventional Commits.
Trailers/hooks remain optional/deferred; any subject validation is coordinator
postcondition, not a Git hook.
|===

==== 7. Prompt, agent output, and repository-policy contracts

===== 7.1 Prompt composition

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-PROMPT-01
|Reusable prompt material shall be repository-local, versioned, and pinned by
path plus Git revision/content digest for the Run. Each phase shall explicitly
select the role/modifier and repository-policy components it requires; the
coordinator shall not inject all repository guidance automatically.

|WFC-PROMPT-02
|Expanded prompt order shall be: generated identity/Git/authority/output header;
selected role/persona/modifiers; selected repository engineering-policy
components; workflow/profile/phase policy; task-specific semantic payload;
bounded evidence/context; result/output contract.

|WFC-PROMPT-03
|Identity, provenance, scope, routes, process, evidence refs, and output contract
shall be structured; semantic task/reasoning remains natural-language prose.

|WFC-PROMPT-04
|V1 shall have no general template language or user-defined conditional/include
engine. What deterministic software can reliably enforce shall not exist only
as an LLM instruction. Evidence/context omitted by a bound shall be listed in an
omission/reference manifest with its retrieval path; silent truncation is
forbidden. Numeric size/diff thresholds may follow Slice-2 measurement.
|===

===== 7.2 Three output surfaces

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-OUTPUT-01
|Every accepted invocation shall produce or fail validation against a
machine-readable routing envelope containing schema version, outcome, requested
profile route, `scope_changed`, `requires_human`, optional escalation, Finding
deltas, claimed commits, evidence refs, and short summary.

|WFC-OUTPUT-02
|Outcome vocabulary shall be `completed`, `changes_required`, `blocked`, or
`failed`. Escalation classes shall include `scope`, `semantic_authority`,
`public_surface`, `trust_boundary`, `prerequisite`, and `operational` with a
concise question.

|WFC-OUTPUT-03
|A Finding delta shall contain existing/null ID, invariant, blocking flag,
proposed state, and evidence refs. Free-form prose cannot supply missing
routing-critical fields.

|WFC-OUTPUT-04
|Every phase shall declare `durable_artifact` as `required`, `optional`, or
`none`. For `required`, result validation demands an in-scope versioned artifact
reference and a successful mutating Hop commits it; for `optional`, the artifact
is permitted but not a gate; `none` creates no coordinator artifact obligation.
The agent cannot weaken the declared value. Phases with durable semantic purpose
(normally architecture/design, broad assessment, material formal review,
closure assessment, or lifecycle closure) use `required`; tiny implementation,
mechanical observation, and supervisor routing normally use `none`.

|WFC-OUTPUT-05
|Terminal output shall be concise and navigational: identities, outcome/stop,
commit/check status, human question, and artifact/checkpoint locations. Material
analysis belongs in repository artifacts, not a long terminal transcript.
|===

===== 7.3 Repository engineering policy boundary

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-POLICY-01
|Coordinator core shall encode execution mechanics, not code-quality or Domain
semantics. Repository rules are versioned inputs to prompts and deterministic
guards.

|WFC-POLICY-02
|Repository policy may reference current architecture, coding/testing/docs,
commit, AI/developer, and Human Readability guidance, including
`docs/developer_guide/ch06_code_conventions/03_human_readability_review.adoc`;
none becomes generic workflow vocabulary.

|WFC-POLICY-03
|Operator/Run policy controls repository, adapter, autonomy, authorized scope and
actions; profile controls workflow semantics; task payload controls concrete
work. No layer may silently grant authority owned by another.
|===

==== 8. Evidence, persistence, Project Memory, and checkpoint contracts

===== 8.1 Evidence providers

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-EVID-01
|V1 shall use a fixed trusted in-code registry of simple evidence callables; no
runtime discovery or plugin lifecycle.

|WFC-EVID-02
|An observation shall identify provider, one of `pass`, `fail`, `error`, or
`not_run`; exact command/version when applicable; concise summary; artifact ref;
and digest.

|WFC-EVID-03
|Initial categories are mandatory Git facts and profile-selected tests,
architecture guards, source/style checks, and diff hygiene. Commands/argv and
working directory originate in trusted policy, not agent output.

|WFC-EVID-04
|Providers return observations and never semantic acceptance. A future
Sonar/static-analysis provider may implement the same shape through explicit
registration without any Sonar-specific V1 machinery.
|===

===== 8.2 Persistence and artifact lifecycle

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-PERSIST-01
|V1 shall persist enough compact local state for same-checkout crash/resume:
an atomically replaced Run snapshot and atomically published correlated
dispatch/in-progress marker with base HEAD and budget reservation are the only
trusted resume anchors. Compact
Hop/result metadata, Finding state, evidence-manifest refs/digests, and generated
checkpoint remain referenced state, not repository authority.

|WFC-PERSIST-02
|Operational state shall live on persistent local storage, be unambiguously
associated with the concrete repository checkout, remain coordinator-controlled,
not contaminate repository Git state, and have its storage/correlation boundary
verified fail-closed before the first Run write and each Hop. A
repository-local root is permitted where the boundary-crossing process is
trusted not to mutate coordinator state; it must be repository-specific, ignored
by tracked repository policy, and pass Git ignore-resolution preflight (a local
exclude alone is insufficient). An external trusted checkout-associated root is
required for the writable real-agent Slice-4 boundary: it shall be
outside the agent-writable workspace, not re-exposed through writable additional
directories, and correlate coordinator-controlled state through a durable opaque
checkout identifier and trusted current checkout/Git facts. On first
registration, the coordinator records the concrete checkout's canonical path
and Git common-dir/git-dir fact as appropriate with one opaque checkout UUID;
path or association mismatch fails closed pending explicit maintainer
intervention. The UUID is not a security proof: authority also requires current
fail-closed Git/checkout and Run/Hop/HEAD correlation. No universal path,
database, repository registry service, event store, or append-only event log is
permitted.

|WFC-PERSIST-03
|Git stores durable repository history; committed design/review/assessment
artifacts store durable semantics. Run state references commits/artifacts rather
than copying full diffs/content.

|WFC-PERSIST-04
|Expanded prompts, raw stdout/stderr, full logs, large diffs, and copied files
are transient by default, may be redacted/promoted selectively, and may be
deleted. Secrets are never eligible for promotion.

|WFC-PERSIST-05
|A maintainer may deliberately promote one compact active/paused/completed Run
report to warm storage for durable handoff. Later cold-storage movement or
deletion is governed separately; V1 automates neither retention nor archival.

|WFC-PERSIST-06
|Every coordinator-owned structured result or observation manifest whose
presence may advance resume shall be atomically published; partial temporary
files are ignored. On restart every result is schema-revalidated, correlated to
the expected Run/Hop/invocation, and reconciled with Git/evidence. Invalid,
truncated, or ambiguous data fails closed to human. A valid unreduced result may
be reduced only after explicit resume. The promise ends if local state is lost:
Git remains intact, but recovery requires a maintainer-created fresh Run/re-entry.
|===

===== 8.3 Project Memory boundary

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-PM-01
|Project Memory remembers what matters; the coordinator records what happened.
Project Memory shall never be operational Run/Hop storage.

|WFC-PM-02
|Ordinary `complete` ends a Run. `Accepted completion` additionally requires a
satisfied declared formal gate or explicit human semantic acceptance that
remains valid at completion. Only then may the coordinator suggest `none` or
`update_existing` for the originating signal with WorkItem, accepted HEAD,
durable decision/residual, and evidence refs. It never creates canonical
completion events; separately governed human curation may create one coarse
outcome event.

|WFC-PM-03
|Individual Hops shall not become routine Project Memory events. A semantically
exceptional Hop may be referenced inside a coarse curated outcome, not promoted
as execution state. Future Run/Hop references are prospective only; vector
retrieval remains out of scope.
|===

===== 8.4 Checkpoint and terminal observability

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-OBS-01
|Every autonomous stop/completion and each explicit operator checkpoint shall
render a compact report from Run state and evidence manifests.

|WFC-OBS-02
|Checkpoint shall contain WorkItem/Run, profile/policy revision,
baseline/current HEAD, phase/status, budget used/remaining, one-line Hop
summaries and ranges, Finding state/origins, evidence state, anomalies, stop
reason, reconcile reason, human questions, and finite next permitted routes.
Routes shall be recomputed from pinned profile, phase, and preemption state, not
trusted from a persisted route-key cache.

|WFC-OBS-03
|Git facts/counts/route validity/provider state are deterministic. Semantic
summaries or progress classifications shall be attributed to agent, supervisor,
or human.

|WFC-OBS-04
|Progressive disclosure shall be `checkpoint -> Hop/commit -> review/result ->
evidence/diff`; reports and terminal output link rather than duplicate full
prompts, prose, logs, or diffs.
|===

===== 8.5 End-of-cycle lifecycle closure

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-LIFE-01
|At final accepted V1 closure, repository governance shall explicitly set FTR
lifecycle/status, remove it from the active/open backlog, move the FTR and
companion material to the repository-standard cold-storage destination, and
dispose the hypothesis, proposed design, readiness/correction, and slice-review
warm artifacts so none presents as current truth. Canonical documentation shall
be updated from implemented reality; separately authorized Project Memory
curation shall record durable outcome/residual/next direction; retained and
transient coordinator/Run material shall be deliberately handled.

|WFC-LIFE-02
|The final lifecycle step shall receive independent closure verification where
repository precedent warrants it. This is a future closure obligation, moves no
artifact during readiness, and does not require archiving every trivial
temporary file: implemented truth becomes canonical and working evidence becomes
clearly historical.
|===

==== 9. Failure, suspension, resume, and trust contracts

===== 9.1 Failure and resume

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-FAIL-01
|Invocation failure or malformed structured result after adapter acceptance
consumes a Hop; coordinator reconciles Git/evidence, records diagnostic, and
stops without hidden retry or inferred route.

|WFC-FAIL-02
|A failed deterministic guard is recorded. A profile-declared failed gate may
route to correction; evidence-provider error or hard invariant stops.

|WFC-FAIL-03
|Dirty/divergent/unexpected Git state shall stop without reset, cleanup,
rollback, or guessed ownership and shall report exact state.

|WFC-FAIL-04
|Before invocation, the coordinator atomically publishes the correlated
invocation marker with base HEAD. Interruption or other uncertain state enters
one common `reconcile_required` mechanism: validate Git/evidence/governing
inputs, discard stale next action, derive current continuations, checkpoint, and
require applicable explicit resume/human authority; it never replays
automatically.

|WFC-FAIL-05
|Budget exhaustion, human stop, or unclassifiable route persists
state/checkpoint and invokes no next agent.

|WFC-FAIL-06
|Every V1 prerequisite activation requires explicit maintainer authority,
creates a separate child Run, and leaves parent `suspended` with
child/suspension HEAD and no stale next action. Autonomous/preauthorized child
creation is deferred.

|WFC-FAIL-07
|Nested return shall enter the same `reconcile_required` mechanism with
`prerequisite_return` provenance, compare current Git/governing inputs/Findings
against the suspension anchor, discard stale next action, and derive currently
valid continuations. Nested work never restores stale next action; no
authoritative rebaseline counter/state machine exists.
|===

===== 9.2 Trust and security

[cols="1,5",options="header"]
|===
|ID |Requirement

|WFC-SEC-01
|V1 trusts the maintainer-selected repository, pinned profile/policy, configured
adapter, Git/filesystem/process observations, and existing OS/tool permission
boundary; it is not a hostile-code sandbox.

|WFC-SEC-02
|Agent prose, factual claims, claimed commits, requested routes, and Finding
proposals are untrusted input. Repository facts are observed independently.

|WFC-SEC-03
|Agents/supervisors cannot request arbitrary workflow mutation or evidence
commands; only validated profile routes and trusted policy command definitions
are executable.

|WFC-SEC-04
|Coordinator invocation shall grant no new agent permission. Supervisor shall
not increase its own semantic/review authority.

|WFC-SEC-05
|Secrets, model credentials, environment dumps, and credential-bearing output
shall not be persisted in workflow artifacts; raw material is deleted/redacted
before deliberate promotion.
|===

==== 10. Historical replay and correction acceptance matrix

Slice 1 shall make historical A--H plus correction regressions I--J executable
as reducer/model/replay tests. Later slices replace fixture assumptions with
Git/adapter integration where noted.

[cols="1,3,4",options="header"]
|===
|Case |Contracts exercised |Required acceptance

|A -- Trusted Ingress E4
|WFC-REVIEW-05, WFC-FIND-04..06, WFC-AUTO-01/04
|Stable Finding survives >3 productive correction loops; resolved Findings stay
closed; bounded review continues while budget remains; no review-count stop.

|B -- Trusted Ingress E3/XRef
|WFC-AUTH-05/06, WFC-FIND-05/06, WFC-FAIL-06/07
|Conflicting contracts escape local correction, require human/prerequisite,
suspend parent, and use common reconcile/resume before return.

|C -- DMC F-002 Ultrareview
|WFC-REVIEW-01..04, WFC-AUTH-03..05
|Broad/fresh adversarial review remains advisory, returns evidence to a separate
formal gate, and cannot accept implementation.

|D -- DMC Cell Codec
|WFC-REVIEW-04, WFC-FIND-01/03/06
|Empty known-open set does not generically close a Run; a profile-selected
closure search can introduce a new blocker and route correction/prerequisite.

|E -- DMC deletion authority
|WFC-AUTH-06, WFC-FAIL-06/07, WFC-PM-01
|Human authorizes independent child lifecycle; parent is suspended and safely
reconciled through the common mechanism; Project Memory is not used for nested
execution state.

|F -- tiny local change
|WFC-SYS-04, WFC-REVIEW-04, WFC-AUTO-01, WFC-PM-02
|One authorized implementation Hop plus passing mechanics can complete without
design/readiness/adversarial/integration/closure/lifecycle review chain. This is
ordinary completion and creates no Project Memory promotion suggestion.

|G -- accepted non-blocking minor
|WFC-FIND-01/03, WFC-REVIEW-02
|Authorized declared gate, never supervisor, records `residual` and
advances/completes without a forced correction loop.

|H -- `max_autonomous_hops=1`
|WFC-AUTO-01..05, WFC-OBS-01..04
|One invocation still yields Git/evidence/result processing and useful
checkpoint; next invocation is preempted unless deterministic completion ends
the Run.

|I -- completed result escalates (R1 regression)
|WFC-AUTH-01..03/06, WFC-OUTPUT-01/02, WFC-FAIL-05
|Given `outcome=completed`, requested `complete` or `goto`,
`requires_human=true`, and non-null `semantic_authority` escalation, mechanical
preemption produces a human stop and executes no autonomous route. The case may
not use `outcome=blocked` as an alternate reason to stop.

|J -- non-DMC complex profile (R7 falsification)
|WFC-SYS-03, WFC-PROFILE-01..05, WFC-REVIEW-01..05, WFC-FIND-01..06
|A non-production `bug_investigation -> fix -> independent_review -> correction
-> rereview -> complete` profile fixture preserves one Finding across correction
and bounded rereview and converges without DMC/Trusted-Ingress vocabulary or new
engine behavior.
|===

==== 11. Implementation slices and slice acceptance

Slices 1-3 are implemented, independently verified, and maintainer-accepted.
Slice 4 is authorized only for one disposable `N=1` proof; implementation and
live proof are not yet completed. Routine real-repository Slice-4 proof and
Slice 5 remain unauthorized.

===== 11.1 Slice 1 -- reducer / model / replay proof

Status:: *IMPLEMENTED AND MAINTAINER-ACCEPTED.* The implementation/review chain
is recorded compactly in section 1; S1-R1, S1-R2, S1-R4, and S1-R5 are closed.
S1-R3 remains deferred non-blocking for Slice-3/real-agent-result hardening.

Scope:: Minimal typed WorkItem input, Run/Hop/Finding records; strict YAML
human-input/profile validator and JSON transient-state validator; five
route effects; deterministic reducer; Finding lifecycle; autonomy accounting;
common reconcile/resume; atomic anchors/checkpoint; A--J tests; no real agent
invocation.

Acceptance::

. WFC-SYS, WFC-MODEL, WFC-PROFILE, WFC-AUTH deterministic path, WFC-REVIEW,
  WFC-FIND, WFC-AUTO, WFC-PERSIST, WFC-PM, and WFC-OBS contracts have executable
  focused tests where behavior exists.
. A--J matrix tests demonstrate every required state/evidence/route outcome,
  including >3 loops, advisory non-authority, gate-only residual completion,
  N=1, R1 escalation preemption, and the non-DMC complex profile.
. R1's test uses `outcome=completed` plus an otherwise valid `goto`/`complete`,
  `requires_human=true`, and a non-null escalation; it asserts no autonomous
  route or supervisor invocation, so `outcome=blocked` cannot mask failure.
. Invalid phase/route/effect/profile/result data fail closed with deliberate
  diagnostics; unknown YAML/JSON fields fail closed and no DSL/executable or
  duplicate human-authored serialization is introduced.
. Atomic write/interrupted-write tests cover Run snapshot and invocation marker;
  crash before dispatch marker preserves the prior snapshot and consumes no Hop;
  crash after the marker but before provider completion charges the reserved Hop
  conservatively and enters common reconcile without replay; lost local state
  exposes only the documented fresh-Run boundary.
. Test implementation uses no real LLM/provider/network and changes no product
  runtime under `src/spreadsheet_handling/`.

===== 11.2 Slice 2 -- Git transaction / evidence proof

Status:: *IMPLEMENTED; INDEPENDENTLY VERIFIED; MAINTAINER-ACCEPTED.* The
implementation/review chain `32e322a` through `a014e3d` records the accepted
proof and closure of S2-R1 through S2-R5. Slice 3 is independently verified and
maintainer-accepted.
WFC-GIT-08 proof is maintainer-approved for Slice 3 because workflow-produced
commit subjects first exist at that prompt/fake-adapter vertical boundary.

Scope:: Disposable fixture repositories; pre/post Git facts; base/head/range,
ancestry, scope, linearity, clean-state and policy mutation checks; actual vs
claimed commits; configured persistent-state-root preflight; fixed evidence
callables; crash reconciliation.

Acceptance::

. WFC-GIT-01..07 and WFC-EVID-01..04 are exercised against real temporary Git
  repositories, including 1..n commits and allowed `base==head`.
. Fixture repositories prove a root ignored by tracked policy passes, an
  unignored root fails before first state write, and `.git/info/exclude` alone
  does not satisfy WFC-PERSIST-02; diagnostics identify the tracked ignore fix.
. Dirty tracked/non-ignored, divergent history, forbidden merge, out-of-scope
  path, pinned-policy change, unexpected commit, and claim mismatch cases yield
  the specified stop/diagnostic without cleanup or reset.
. Git observation is authoritative even when a fixture agent result lies about
  commits.
. Provider `pass/fail/error/not_run` behavior and trusted command selection are
  tested; no plugin discovery or Sonar-specific code appears.
. Crash while marked `invoking`, including provider commits before coordinator
  failure, reconciles actual fixture HEAD and requires explicit resume without
  replay or stale-route restoration.

===== 11.3 Slice 3 -- N=1 vertical proof with fake adapter

Status:: *IMPLEMENTED; INDEPENDENTLY VERIFIED; MAINTAINER-ACCEPTED.* The
implementation/review chain `fb2c012` through `403ff88` records acceptance;
S3-R1 and S3-R2 are closed, while S3-R3 remains deferred/non-blocking for future
correction-routing/evidence-fail semantics. Slice 4 is authorized only for the
single disposable proof specified in section 11.4.

Scope:: Per-phase selected prompt composition; bounded context/omission policy;
typed durable-artifact policy; structured-result validation; fake/subprocess
adapter; complete one-Hop routine workflow; checkpoint and concise terminal
projection; WFC-GIT-08 workflow-produced commit-subject projection.

Acceptance::

. WFC-PROMPT-01..04, WFC-OUTPUT-01..05, and WFC-OBS-01..04 are proven in an
  end-to-end fixture Run with `max_autonomous_hops=1`.
. WFC-GIT-08 proves that workflow-produced commit subjects project WorkItem/Hop
  identity in human-readable `git log --oneline` form; validation is a
  coordinator postcondition, with neither a Git hook nor trailers required.
. Selected component paths/revisions/digests and prompt order are deterministic;
  unselected repository guidance is absent, and routing-critical data missing
  only from prose fails validation.
. Completed, failed, malformed, scope-change, and human-escalation fake results
  produce the required route/stop/budget state.
. Fixtures prove `durable_artifact=required` rejects a missing/out-of-scope
  artifact and accepts a committed in-scope artifact; `none` permits a tiny Hop
  without a document; terminal output stays concise.
. Prompt/diff bounds never silently truncate: every omission appears in the
  manifest with a usable reference. V1 applies the RRQ-01-calibrated
  omission/reference thresholds: 65,536 bytes per selected component, 196,608
  bytes total bounded context/evidence, and 65,536 diff bytes or 12 changed
  files for inline diff evidence.
. Truncated/invalid/identity-mismatched results fail closed after resume. A valid
  correlated result present before reduction is revalidated and may reduce only
  after explicit resume plus successful Git/evidence reconciliation.

===== 11.4 Slice 4 -- real local adapter at N=1

Status:: *SLICE-4A IMPLEMENTATION MAINTAINER-ACCEPTED (S4A-R1..R9 CLOSED); THE
EXACTLY-ONE DISPOSABLE `N=1` REPLACEMENT PROOF IS CONSUMED AND ACCEPTED AS VALID
REAL FAILURE-PATH EVIDENCE (REVIEW `ca31a8e0`); THE DETERMINISTIC PROOF HARNESS IS
IMPLEMENTED AND INDEPENDENTLY ACCEPTED (SECTION 11.4.1; S4H-R1/R2 CLOSED,
S4H-R3/R4/R5 WATCH); THE PRIOR `N=1` LIVE HAPPY-PATH PROOF (SECTION 11.4.2) IS
CONSUMED AND EXPOSED `S4A-R10`, WHICH IS NOW VERIFIED CLOSED (SECTION 11.4.3; FIX
`6bfad5bb`/`41ff4e35`, EVIDENCE `d1e01b94`, RE-REVIEW `af933f7d`) AND WHOSE BOUNDED
CORRECTION AUTHORIZATION IS CONSUMED; HAPPY-PATH `WFC-OUTPUT` LIVE PROOF STILL
REQUIRED AND NOT YET PROVEN; THE CORRECTED DISPOSABLE `N=1` LIVE HAPPY-PATH PROOF
(SECTION 11.4.4) IS CONSUMED, REACHED THE PROVIDER, AND FAILED on a second concrete
structured-output-schema compatibility error; NO LIVE AUTHORIZATION OR IMMEDIATE
FIELD-LEVEL RETRY REMAINS, AND NEXT WORK REQUIRES MAINTAINER DISPOSITION/DESIGN OF
THE PROVIDER-INTEGRATION BOUNDARY.*
Routine real-repository use remains unauthorized.

Scope:: One selected configured local agent invocation boundary, first in a
disposable/low-risk repository, then in a separately authorized routine
documentation/tooling task. No new semantic Domain work.

Authorization:: The first disposable proof attempt -- a prior Run of
`WFC-S4-PROOF`, recorded in
`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4_disposable_proof_evidence_2026-08-30.adoc`
-- is immutable historical evidence; it is not resumed or retried. Its single live
invocation failed before repository mutation in a non-representative nested-Codex
environment, its failure-path containment was verified, and the corrected
Slice-4A implementation is now maintainer-accepted (S4A-R1..R9 closed). On that
basis the maintainer authorizes exactly ONE replacement real Codex invocation
against a freshly initialized, no-remote, disposable repository containing only
tracked `PROOF.md`, as a NEW Run of WorkItem `WFC-S4-PROOF` with a new `run_id`
and new `invocation_id`, Hop `H001`, and `max_autonomous_hops=1`. The replacement
proof shall be launched only from the ordinary non-nested host/SSH environment --
never from inside Claude/Codex or another agent sandbox. The agent may edit that
repository and create the one required workflow commit under Git ownership Option
A; workspace-write thereby acknowledges effectively full writable `.git` authority
only for this disposable proof. It is not accepted for `core`, any routine
repository, or later multi-Hop work. Coordinator-created semantic commits remain
unauthorized. The proof stops after its single invocation regardless of outcome;
no retry or second replacement invocation is authorized.

Authorization -- consumed:: The replacement invocation has occurred as Run
`RUN-a9cc83d29171494795c0789f97571288`
(`invocation_id=INV-683fbba33b634246b25a798a4b8fc11f`, `hop_id=H001`), and this
authorization is *consumed*. Independent review `ca31a8e0`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4a_replacement_proof_review_2026-08-30.adoc`),
accepted as the maintainer's factual basis, confirms a started/unresolved
invocation (process exit `1`, no timeout, zero-byte structured result) that the
corrected driver contained correctly: one conservative Hop charged, independent
Git observation, unchanged Git reality (`HEAD` at baseline, `PROOF.md` pending, no
agent commit), dispatch marker retained, `reconcile_required`, explicit resume
required, no retry. No Slice-4A implementation defect was exposed. The real
happy-path `WFC-OUTPUT` proof below is *unproven*; that criterion is not weakened
or rewritten ex post. No further live invocation, resume, or retry of either
historical Run is authorized, and no live authorization remains.

Persistence boundary:: Trusted Run snapshot, dispatch marker, validated/pending
result state, result schema/transport, checkpoints/resume anchors, and other
coordinator operational state use the external trusted checkout-associated root
of WFC-PERSIST-02. The real agent receives no writable exposure to that root.

Safety/readiness acceptance::

. The adapter/API, credentials boundary, permissions, cancellation/failure
  mapping, structured-result transport, and fresh-context evidence have been
  independently reviewed before any live invocation. `Continuing` context is
  optional and requires a separately proven adapter capability.
. Disposable-repository runs prove WFC-AUTO, WFC-GIT, WFC-OUTPUT, WFC-FAIL, and
  WFC-SEC without hidden retries, extra permissions, secret retention, or
  coordinator-created semantic commits.
. `max_autonomous_hops=1` cannot cause a second live invocation; human stop and
  malformed/failed results stop safely.
. A maintainer separately authorizes the first real repository task and its
  scope. The accepted checkpoint/commit/artifact reconstruct the Run without
  raw transcript review.
. No Domain, public product surface, DMC Cycle 2, or Trusted-Ingress task is used
  as first dogfood.

===== 11.4.1 Slice 4A -- deterministic proof-harness hardening (implemented and accepted)

Status:: *IMPLEMENTED, INDEPENDENTLY VERIFIED, AND MAINTAINER-ACCEPTED.* The
harness (`scripts/workflow_coordinator/proof_harness.py`, tests
`tests/unit/workflow_coordinator/test_proof_harness.py`) was implemented under
`fc53be37` / evidence `367ee166`, independently reviewed at verdict A under
`8cea13df`, corrected for truthfulness under `0b950dc4` / evidence `eb5a5037`, and
its S4H-R1 (character-vs-byte diagnostic bound) and S4H-R2 (operator exit status
for `reconcile_required`) findings were verified closed by bounded re-review
`2ea964a2`. S4H-R3/R4/R5 remain non-blocking WATCH residuals requiring no
correction before the one-shot live proof. This subsection's authorization for
deterministic implementation/testing is *consumed*; it authorized no live
invocation. The next step is the single live happy-path proof of section 11.4.2.

Motivation:: Review `ca31a8e0` exposed two concrete evidence-hygiene residuals in
the disposable proof harness (not in Slice-4A runtime): the wrapper persisted
`returncode`/`timed_out` but not the bounded `CodexOutcome` stdout/stderr tail, so
a failed live process was undiagnosable after terminal loss; and no durable
execution provenance distinguished an ordinary host/operator launch from an
agent-nested launch. The maintainer's mosh/terminal environment also proved
fragile, so terminal scrollback must not be required for evidence.

Authorization:: The maintainer authorizes implementation and deterministic testing
of the smallest disposable Slice-4 proof harness / operator runner that provides
these properties. It may compose the already accepted Slice-4A APIs and prepare a
fresh disposable repo, fresh external state, fresh Run/invocation IDs, the fixed
`WFC-S4-PROOF` task, a durable proof-summary, bounded stdout/stderr tails, bounded
host/ancestor provenance, and a Git/result/state inspection summary. It must be
runnable through a small repository-controlled operator runner / file-based
launcher whose durable artifacts are authoritative.

Constraints::
. It must not implement a second coordinator.
. It must not change Slice-4A runtime semantics unless a direct contradiction is
  found.
. No live Codex/provider invocation is authorized while implementing or testing
  the harness; tests use fake executables only.
. Provenance is bounded evidence to distinguish host vs agent-nested launch; it is
  not, and must not pretend to be, cryptographic attestation.

Not authorized by this section:: Another live proof; resume/retry of either
historical Run; routine repository use; Slice 4B; `bwrap`; commit broker; Slice 5;
autonomous multi-Hop. A future live happy-path proof remains a separate maintainer
gate after the harness is implemented and independently reviewed.

Authorization-consumption residual (future, not implemented here):: Run-level Hop
budget is mechanically persisted and consumed, but the current "exactly one
maintainer-authorized Run" permission remains governance rather than a centrally,
atomically consumed authorization capability. This is not solved in the
proof-harness task; it is relevant before autonomous N>1 / Slice 5 design.

===== 11.4.2 Slice 4A -- live happy-path proof (authorized, one-shot)

Status:: *CONSUMED. THE ONE AUTHORIZED LIVE HAPPY-PATH PROOF WAS EXECUTED (RUN
`RUN-c2fe2ab13f224bbeb37f8cd9519ce0d1`, INVOCATION
`INV-1c8e44c2a7e444aa9e2ff7dfa2ff2a06`, `H001`) AND REACHED THE PROVIDER BOUNDARY;
IT DID NOT PROVE THE HAPPY PATH. THE PROVIDER REJECTED THE STRUCTURED-OUTPUT SCHEMA
(HTTP 400 `invalid_json_schema`), EXPOSING FINDING `S4A-R10` (BLOCKING; REVIEW
`75c63d4f`, EVIDENCE `ae5d36c3`). THE ONE-SHOT AUTHORIZATION IS CONSUMED. NO OTHER
LIVE INVOCATION, RESUME, RETRY, OR SECOND PROOF IS AUTHORIZED. THE NEXT LIVE PROOF
REMAINS A SEPARATE MAINTAINER GATE AFTER THE S4A-R10 CORRECTION (SECTION 11.4.3)
AND INDEPENDENT RE-REVIEW.*

Basis:: The maintainer accepts as factual basis the independently verified and
now-accepted deterministic proof harness (review `8cea13df`, correction
`0b950dc4`, correction evidence `eb5a5037`, bounded re-review `2ea964a2`): the
Slice-4A implementation remains accepted; the harness is independently verified;
S4H-R1 and S4H-R2 are closed; S4H-R3/R4/R5 remain non-blocking WATCH residuals;
and the real happy-path `WFC-OUTPUT` proof remains required and unproven.

Authorization:: The maintainer authorizes exactly ONE new disposable `N=1` live
proof, executed through the reviewed repository-controlled harness
`scripts/workflow_coordinator/proof_harness.py`, as a NEW Run of WorkItem
`WFC-S4-PROOF` with `H001`, `max_autonomous_hops=1`, a fresh `run_id`, a fresh
`invocation_id`, a fresh proof root, a fresh disposable no-remote repository, and
fresh external trusted state. It must be launched manually from the ordinary
non-nested maintainer host/SSH environment -- never from inside Claude, a Codex
implementation-agent context, or another model-controlled sandbox.

Operator contract:: The intended interface is the file-based harness, not a
heredoc and not terminal scrollback:
+
[source,text]
----
.venv/bin/python scripts/workflow_coordinator/proof_harness.py \
  --proof-root <NEW DURABLE PROOF ROOT>
----
+
The maintainer selects the exact proof-root path at launch. The durable files are
authoritative: `proof-summary.json`, `launch-provenance.json`, `trusted-state/`,
and the disposable repository Git state; shell output is convenience only. The
runner is foreground and does not claim session-disconnect survival; no
`nohup`/`tmux`/daemon behavior is added in this lifecycle step.

One-shot rule:: Exactly one real provider invocation is authorized. Regardless of
outcome -- success, uncertain, pre-spawn failure, provider failure, timeout,
malformed result, or operator/session incident -- the authorization is *consumed*
once the real invocation boundary is reached. No retry, no resume, no second
replacement proof, and no automatic recovery invocation are authorized. If
execution does not reach the provider boundary, factual state may be inspected
later before deciding whether the authorization was consumed; consumption is not
guessed.

Expected happy path:: A successful proof should establish exactly one real Codex
invocation, the expected `PROOF.md` mutation, exactly one workflow commit with
subject `docs(workflow): WFC-S4-PROOF H001 mark fixture`, a valid correlated
structured result, `acceptance=established`, a completed Run, `hop_used=1`,
authoritative Git observation, a retired dispatch marker, exit code `0`, bounded
stdout/stderr persisted, bounded launch provenance persisted, no Git locks, no
unexpected mutation, and a clean disposable worktree. If any expectation fails,
the proof must record the actual facts.

Not authorized by this section:: Routine real-repository use; Slice 4B; `bwrap`;
commit broker; Slice 5; autonomous multi-Hop; DMC Family Cycle 2; central atomic
authorization-consumption machinery; and any second live invocation. The WATCH
residuals S4H-R3/R4/R5 remain recorded and need no correction before this
one-shot proof.

===== 11.4.3 Slice 4A -- S4A-R10 structured-output schema correction (authorized, one hop)

Status:: *CONSUMED AND VERIFIED CLOSED. THE BOUNDED CORRECTION HOP WAS EXECUTED
(FIX `6bfad5bb`, STRICTNESS-WALKER CORRECTION `41ff4e35`, EVIDENCE `d1e01b94`) AND
INDEPENDENTLY VERIFIED (RE-REVIEW `af933f7d`, VERDICT A): the opaque provider-facing
`result:{"type":"object"}` defect is removed, Slice-4A emits the canonical strict
structured-result envelope from a single source shared with the validator, every
provider-schema object is strict and complete, and the corrected strictness test
detects reintroduction of the exact S4A-R10 defect. `S4A-R10` is VERIFIED CLOSED and
this correction authorization is consumed. NO LIVE INVOCATION WAS AUTHORIZED OR
PERFORMED BY THIS CORRECTION.*

Basis:: The maintainer accepts independent review `75c63d4f`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4a_live_schema_failure_review_2026-08-31.adoc`)
and its evidence `ae5d36c3` as the factual and technical basis. `S4A-R10` is a new
BLOCKING Slice-4A implementation defect: the real provider-facing structured-output
schema built in `invoke_codex()` declares the inner `result` as an opaque
`{"type":"object"}`, which strict Structured Outputs rejects (`additionalProperties`
must be supplied and `false` on every object) and which does not describe the
accepted `RoutingResult` contract. The internal Python `RoutingResult` validation
contract is not implicated; the outer correlation envelope remains sound; a
one-line `additionalProperties:false` patch is not sufficient; a complete strict
inner `RoutingResult` schema is required. The models-cache
`supports_parallel_tool_calls` warning remains unrelated/WATCH and is out of
correction scope. The consumed live proof remains immutable evidence and is neither
resumed nor retried.

Authorization:: The maintainer authorizes exactly ONE bounded implementation hop to
correct `S4A-R10`, following section 10 of review `75c63d4f`. Expected production
scope is `scripts/workflow_coordinator/serialization.py` and
`scripts/workflow_coordinator/slice4.py`, plus focused workflow-coordinator tests
and correction evidence. The hop shall:

. introduce a canonical provider-facing strict `RoutingResult` JSON-schema builder
  co-located with the existing serialization/validation contract, and a strict
  structured-result correlation envelope built around it;
. replace Slice-4A's inline opaque `result:{"type":"object"}` transport schema with
  the canonical strict envelope builder, preserving the outer envelope exactly
  (`schema_version`, `run_id`, `hop_id`, `invocation_id`, `result`; strict key set;
  existing Python-side correlation checks);
. represent the accepted `RoutingResult` semantics completely, including nested
  `escalation` and `FindingDelta` structures, with the review's nullable semantics
  (`escalation`: null or strict object; `finding_id`: string or null;
  `successor_ref`: string or null; `new_material_evidence`: required non-nullable
  boolean);
. keep non-empty-string validation Python-side where Structured Outputs cannot
  express the existing rule; and
. add no general schema framework or code generator.

Deterministic tests required:: Tests that would have caught the exact live defect
without a provider call: a recursive strictness traversal asserting every object
schema has `additionalProperties == false` and `set(required) == set(properties)`
across `properties`, array `items`, and `anyOf` branches; the emitted
`structured-result.schema.json` exactly equals the canonical builder; the inner
`result` required keys equal the accepted `RoutingResult` contract; the
`FindingDelta` schema stays aligned with its accepted key contract; a known-good
correlated `RoutingResult` envelope both passes Python `validated_result` and
satisfies the locally checked provider-schema shape; and the existing
fake-executable vertical tests remain green. No `jsonschema` runtime dependency is
added for this correction.

Not authorized by this section:: Any live Codex/provider invocation; another live
proof; resume/retry of any consumed Run; routine real-repository use; Slice 4B;
`bwrap`; commit broker; Slice 5; autonomous multi-Hop; work on the models-cache
warning; and S4H-R3/R4/R5 cleanup. The next live happy-path proof remains a
separate maintainer gate after this correction lands and is independently
re-reviewed.

===== 11.4.4 Slice 4A -- corrected live happy-path proof (consumed, schema failure)

Status:: *CONSUMED. THE ONE AUTHORIZED NEW DISPOSABLE `N=1` LIVE HAPPY-PATH PROOF
WAS EXECUTED AS RUN `RUN-2aa11937d22d48fb80d160378bb337eb` (INVOCATION
`INV-f74422b833f94eaf88a16f1caf81d7cf`, `H001`) AND REACHED THE PROVIDER BOUNDARY.
IT DID NOT PROVE THE HAPPY PATH: THE PROVIDER REJECTED THE NESTED
`result.schema_version` SCHEMA NODE FOR LACKING A `type` KEY (HTTP 400
`invalid_json_schema`). THE ONE-SHOT AUTHORIZATION IS CONSUMED. NO LIVE
INVOCATION, RESUME, RETRY, SECOND PROOF, OR IMMEDIATE FIELD-LEVEL SCHEMA PATCH IS
AUTHORIZED; NEXT WORK REQUIRES MAINTAINER DISPOSITION/DESIGN OF THE
PROVIDER-INTEGRATION BOUNDARY.*

Basis:: `S4A-R10` is verified closed (section 11.4.3; fix `6bfad5bb`/`41ff4e35`,
evidence `d1e01b94`, re-review `af933f7d`, verdict A). The retained corrected proof
records `process_returncode=1`, `timed_out=false`, `acceptance=uncertain`,
`vertical_success=false`, `hop_used=1`, and `final_run_status=reconcile_required`.
Git reality is unchanged: no workflow commit, `PROOF.md` remains pending, the
disposable worktree is clean with no remotes or Git locks, and the dispatch marker
is retained. Its bounded host provenance is consistent with the required ordinary
non-nested maintainer mosh launch. The real `WFC-OUTPUT` happy path remains required
and unproven; that criterion is not weakened.

Recorded provider diagnostic:: The retained stdout contains HTTP 400,
`invalid_request_error`, code `invalid_json_schema`, parameter
`text.format.schema`, and context `('properties', 'result', 'properties',
'schema_version')`: `schema must have a 'type' key`. The accepted code emitted the
equivalent nested node `"schema_version": {"const": 1}`. This does not recur the
prior R10 failure, which rejected an under-specified `properties.result` object;
the provider advanced beyond that corrected object and rejected the later nested
node. The factual architectural signal is that end-to-end coordinator proof is
currently discovering provider/CLI structured-output compatibility rules
incrementally. Provider-conformance discovery must be examined at a lower
integration layer before another live coordinator proof; this record authorizes no
solution design or implementation.

One-shot rule:: Exactly one real provider invocation is authorized. Once the real
provider boundary is reached, the authorization is consumed regardless of outcome
-- success, provider rejection, uncertain result, timeout, malformed result, agent
failure, or operator/session incident. No retry, resume, automatic second attempt,
or recovery invocation is authorized. If execution fails locally before the
provider boundary, later disposition is based on retained facts; consumption is not
assumed without evidence.

Expected happy path:: A successful proof should establish facts consistent with:
exactly one real Codex invocation; the strict structured-output schema accepted by
the provider; the agent executing the task; `PROOF.md` reading `WFC-S4-PROOF:
completed marker`; exactly one workflow commit with subject `docs(workflow):
WFC-S4-PROOF H001 mark fixture`; a valid correlated structured result;
`acceptance=established`; `vertical_success=true`; `final_run_status=completed`;
`hop_used=1`; a retired dispatch marker; `process_returncode=0`; a clean disposable
worktree; no remotes; no Git lock files; no unexpected mutation/anomaly facts; and
bounded stdout/stderr and launch provenance persisted. If reality differs, the
harness must record reality rather than forcing these expectations.

Not authorized by this section:: A retry, resume, new proof, provider invocation,
field-level `type=integer` patch, adapter refactor, port, Claude support,
conformance harness, architecture redesign, routine real-repository use, Slice 4B,
`bwrap`, commit broker, Slice 5, autonomous multi-Hop, DMC Family Cycle 2, central
atomic authorization-consumption machinery, cleanup of S4H-R3/R4/R5, work on the
models-cache `supports_parallel_tool_calls` warning, or any second live invocation.
Any later authorization remains a separate maintainer decision.

===== 11.4.5 Slice 4A-C -- provider-boundary / adapter-conformance hardening (authorized)

Status:: *SLICE 4A-C AND THE LEVEL-3 SMOKE RUNNER ARE MAINTAINER-ACCEPTED.
LEVEL 1 LOCAL BOUNDARY CONFORMANCE IS REQUIRED / ACCEPTED; LEVEL 2 IS NOT
REQUIRED / NOT PURSUED BY DESIGN; THE FIRST AND ONLY AUTHORIZED LEVEL-3
REAL-PROVIDER CODEX-ADAPTER SMOKE IS CONSUMED AND FAILED ON A LOCAL CLI
WORKSPACE/TRUSTED-DIRECTORY PREREQUISITE; ONE BOUNDED DETERMINISTIC
SMOKE-WORKSPACE CORRECTION IS AUTHORIZED, BUT NO SECOND LEVEL-3 INVOCATION OR
LEVEL-4 COORDINATOR E2E IS AUTHORIZED.* This bounded sub-slice
follows the consumed proof of section 11.4.4 and precedes every later real-provider
proof. Implementation evidence is
`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4ac_implementation_evidence_2026-09-01.adoc`.

Basis and direction:: The maintainer accepts verdict A of independent architecture
review `55c01bb4` (recorded by `75275663`): the coordinator semantic/application
core depends on a provider-neutral `AgentExecutionPort`, which is implemented for
the current path by `CodexCliAdapter`, which alone depends on Codex/OpenAI
transport. The port request is deliberately narrow: invocation identity, assembled
prompt, repository/workspace, canonical result-contract identity/reference, and
timeout (with exact typing adjusted only where current code requires it). A generic
`ExecutionPolicy`, capabilities, provider routing, registry, discovery, plugin
surface, or Claude implementation is not authorized; Codex-specific configuration
may remain adapter construction/configuration state.

Independent-review disposition and bounded correction:: The maintainer accepts
verdict *B -- BOUNDED CORRECTION REQUIRED* of independent review `5454d6ca`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4ac_independent_review_2026-09-01.adoc`).
The accepted 4A-C architecture direction remains valid. One bounded correction
hop is authorized for S4AC-R1 (BLOCKING): restore a sound pre-provider versus
provider-may-have-executed distinction without restoring mutable `adapter.outcome`;
marker deletion is allowed only for a proven pre-execution failure, and otherwise
uncertain dispatch must fail closed to marker-retaining, reconcile-required or
explicit-human-resume semantics. Its deterministic regression coverage must prove
a provider/fake-agent post-execution mutation or commit followed by an injected
application-stage failure retains the dispatch marker and cannot automatically
replay; existing proven pre-execution cleanup/unconsumed-Hop coverage remains
required. S4AC-R2 (IMPORTANT) is also authorized: resolve the `const` disposition
offline from current primary OpenAI Structured Outputs documentation/source; record
an explicit documented basis if `const` remains, or use the smallest unequivocally
documented representation. No provider call is authorized. S4AC-R3 and S4AC-R5
may be closed only when trivial, local consequences in the same touched code;
S4AC-R4 is deferred as WATCH, and no IO-utility or dependency-purity cleanup is
authorized.

Correction state and acceptance:: The implementation records S4AC-R1 fail-closed
dispatch handling and the S4AC-R2 documented single-value enum disposition in
`docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4ac_correction_evidence_2026-09-01.adoc`.
Bounded independent re-review `1943bf10` returned verdict *A*. The maintainer
therefore accepts Slice 4A-C: S4AC-R1, S4AC-R2, S4AC-R3, and S4AC-R5 are *CLOSED*;
S4AC-R4 remains *DEFERRED WATCH*. No further 4A-C correction is required before
Level 3.

Accepted architecture and next gate:: The provider-neutral semantic/application
core -> `AgentExecutionPort` -> `CodexCliAdapter` boundary remains accepted;
semantic parsing, validation, and correlation remain provider-neutral, and
transport outcome remains distinct from semantic acceptance. This acceptance does
not prove real Codex/OpenAI compatibility or the Slice-4A N=1 coordinator happy
path. The next empirical gate is Level 3: a tiny real-provider Codex-adapter
smoke, materially smaller than Level 4.

Level-3 preparation authorization:: A later deterministic implementation hop may
create and test the smallest practical repository-controlled smoke runner. It may
exercise `AgentExecutionRequest` -> `CodexCliAdapter.execute()` ->
`AgentExecutionOutcome` -> existing provider-neutral `validated_result()`, but
must not invoke `run_one_hop()`, `run_real_one_hop()`, a reducer, Run/Hop
persistence, Git workflow, or `proof_harness.py`. The runner must require a fresh
durable smoke-output root and fail before provider invocation if that root already
exists; it must be testable with a fake executable or equivalent adapter seam,
without a provider. This authorizes preparation only. The one-shot actual
real-provider Level-3 invocation requires separate maintainer authorization.

Level-3 smoke-runner correction authorization:: The maintainer accepts verdict
*B -- BOUNDED CORRECTION REQUIRED* of independent smoke-runner review
`17b63af0`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_level3_smoke_runner_review_2026-09-01.adoc`).
One bounded, documentation-authorized correction hop is permitted. *L3SR-R1*
and *L3SR-R3* are required corrections; *L3SR-R2* and *L3SR-R4* are authorized
as small direct evidence-truthfulness consequences. The correction must ensure
that `invocation_attempted=false` and `local_pre_execution_error` are recorded
only when execution is positively known not to have begun. Once execution may
have begun, it must record an executed-or-indeterminate failure classification,
fail the smoke, and not retry; a small adapter exception-typing change is allowed
only if needed for this truthful distinction. After the smoke root exists,
post/maybe-execution exceptions must make a best-effort durable
`smoke-summary.json` whenever persistence remains possible; failure to write that
summary may propagate honestly. The durable
`real_provider_compatibility_established` overclaim must be replaced by a
mechanically true runner-local field such as
`configured_executable_smoke_succeeded`; executable injection remains required.
The summary must also record `timeout_seconds` alongside `executable`, `model`,
and `reasoning_effort`. *W1* and *W3* remain deferred WATCH items: this hop must
not redesign sandboxing, and non-`FileExistsError` smoke-root creation failures
may remain uncaught as provably pre-execution. This authorization neither accepts
the runner nor authorizes a provider invocation, Level 3, or Level 4. It permits
no runtime architecture expansion, retries, generic smoke framework, Project
Memory update, or concern event. L3SR-R1 and L3SR-R3 require bounded independent
re-review using the existing review context; that review must also verify the
R2/R4 disposition and direct regressions. Only after a successful correction
re-review may a separate maintainer authorization prescribe exactly one Level-3
invocation with a fresh smoke root and pinned `executable`, `model`,
`reasoning_effort`, and `timeout_seconds`.

Runner maintainer disposition:: The maintainer accepts verdict *A -- ACCEPT
CORRECTION* of bounded re-review `82a94eac`
(`docs/warm_storage/global_reviews/agent_workflow_coordinator_level3_smoke_runner_correction_rereview_2026-09-02.adoc`), on the correction evidence
`docs/warm_storage/global_reviews/agent_workflow_coordinator_level3_smoke_runner_correction_evidence_2026-09-02.adoc` and original review `17b63af0`.
The Level-3 smoke runner is *MAINTAINER-ACCEPTED*. *L3SR-R1*, *L3SR-R2*,
*L3SR-R3*, and *L3SR-R4* are *CLOSED*. *W1* and *W3* remain *DEFERRED WATCH*.
Under R2, retain the non-blocking WATCH that, on a `post_execution_error` without
an `AgentExecutionOutcome`, `timed_out=false` and
`candidate_result_present=false` may mean unknown rather than known-false. This
does not reopen R2 and does not authorize a nullable/generalized evidence state.

Authorization:: Implement and deterministically test only the following:

. Introduce/reshape the explicit provider-neutral `AgentExecutionPort` with typed
  request and outcome sufficient for the current real `N=1` path. The outcome
  carries candidate structured result/transport output and bounded transport facts,
  eliminating mutable `adapter.outcome` smuggling.
. Establish `CodexCliAdapter` behind that port. It owns the Codex executable and
  argv, model/reasoning and sandbox configuration, output-schema/last-message and
  JSON/event-stream handling, process-group timeout/termination mapping, bounded
  diagnostics, and the Codex/OpenAI strict structured-output projection.
. Retain the canonical provider-neutral `RoutingResult` contract and its Python
  validation/correlation gate as semantic authority. The adapter projection derives
  from that canonical contract; it must not duplicate an independent semantic
  contract or establish semantic acceptance in Codex-specific code.
. Remove provider-specific orchestration duplication in `slice4.py` only where the
  accepted provider-neutral vertical already owns the semantics. Do not broadly
  refactor model/Run/Hop/Finding, reducer, persistence, Git facts, evidence,
  prompt assembly, reconcile, routing, or correlation semantics.
. Add a named offline Codex/OpenAI adapter-conformance layer. From primary
  documentation or locally available primary CLI/help material, encode the full
  documented strict Structured Outputs subset relied on by the current projection:
  every object has `additionalProperties:false`; every object has
  `required == properties`; and every applicable schema node has explicit `type`,
  including const-bearing nodes, properties, array items, and `anyOf` branches.
  The exact schema supplied to Codex must pass those checks.

Acceptance:: This sub-slice is implementation-complete only with deterministic
evidence that the port and concrete Codex adapter exist; no mutable outcome
side-channel remains; adapter-owned transport/dialect knowledge is separated from
provider-neutral semantic contract and validation/correlation; projection derives
from the canonical contract; the complete documented strict subset is covered by
offline conformance tests and passes for the emitted Codex schema; provider-neutral
`N=1` orchestration semantics and existing coordinator tests remain green; no real
provider invocation occurred; and no Slice-4A happy-path claim is made. The
required independent implementation and bounded smoke-runner reviews have since
been completed and accepted in the lifecycle disposition below.

Conformance ladder and lifecycle::

* *LEVEL 1 -- REQUIRED / ACCEPTED:* local boundary conformance: Coordinator ->
  `AgentExecutionPort` doubles, and `CodexCliAdapter` -> deterministic fake CLI
  executable.
* *LEVEL 2 -- NOT REQUIRED / NOT PURSUED BY DESIGN:* simulation of
  provider-internal transport behind the CLI. The configured agent CLI is the
  external-system boundary: our system is Coordinator -> `AgentExecutionPort` ->
  `CodexCliAdapter`; provider transport/API/auth/retry and related behavior behind
  the Codex CLI belong to the external system. Level 1 tests our public neighbor
  at that boundary. Reproducing HTTP/API protocol behind the real CLI would couple
  tests to foreign implementation details and is not part of this conformance
  ladder. If a provider exposes a documented and supported local test/mock
  endpoint, test-provider mode, endpoint override, or equivalent public
  conformance facility, it may be used as optional additional evidence. Its
  absence is not a gap, not technical debt, and not a blocker for Level 3.
* *LEVEL 3 -- REQUIRED:* one tiny real contract smoke:
  `CodexCliAdapter` -> real Codex CLI -> real provider. It is authorized exactly
  once below and remains not yet executed.
* *LEVEL 4 -- REQUIRED:* full Coordinator E2E. It remains separately governed
  and is not authorized now.

Level-3 live authorization:: Exactly one later real Level-3 invocation is
authorized, and only with this operator command (the `cd` is preparation):

[source,bash]
----
cd /home/stefan/repos/spreadsheet-handling/core

.venv/bin/python scripts/workflow_coordinator/codex_adapter_smoke.py \
    --smoke-root /home/stefan/wfc-proofs/level3-codex-smoke-20260902-01 \
    --executable codex \
    --model gpt-5.6-terra \
    --reasoning-effort medium \
    --timeout-seconds 120
----

The fixed smoke root `/home/stefan/wfc-proofs/level3-codex-smoke-20260902-01`
must not exist before the operator runs the command. If it exists, stop: do not
invoke the provider, do not select a replacement root, and require a new
maintainer authorization with a new fixed root. The Python command may cause
exactly one adapter/provider invocation. There is no retry, resume, second smoke
root, alternate executable/model/reasoning effort/timeout, or interactive
recovery. Any failure consumes the authorization: preserve and evaluate available
evidence, do not retry, and require a new maintainer disposition/authorization.

First Level-3 live disposition:: The authorization in `029f3ff3` is *CONSUMED*.
Its exactly one attempt used smoke root
`/home/stefan/wfc-proofs/level3-codex-smoke-20260902-01` and failed with
`smoke_status=transport_failure`, `invocation_attempted=true`, `returncode=1`,
`timed_out=false`, no candidate result, and no semantic validation. The emitted
schema is present; `result-message.json` exists but is empty. The material stderr
is `Not inside a trusted directory and --skip-git-repo-check was not specified.`
The real Codex CLI process boundary was entered, but neither provider reachability
nor remote structured-schema acceptance is established. This is a *local Codex
CLI workspace/trusted-directory prerequisite*, not an OpenAI schema,
`RoutingResult`, semantic-validation, Coordinator Run/Hop, Git-authority, or
Level-4 failure. The truthful runner classification is accepted as evidence that
its transport/no-candidate machinery worked. The durable evidence record is
`docs/warm_storage/global_reviews/agent_workflow_coordinator_level3_first_live_smoke_failure_evidence_2026-09-02.adoc`.

Bounded smoke-workspace correction authorization:: One later deterministic
implementation hop may correct only the Level-3 runner's fresh disposable
workspace setup: initialize that already-created workspace as a fresh local Git
repository before `adapter.execute()`, using the normal local Git CLI (for
example, `git init`) if that is the smallest robust implementation. The `.git/`
directory is environmental scaffolding for the external Codex CLI only. The
correction must not add files, stage, commit, inspect or compare `HEAD`, assert
commits, use coordinator Git facts/helpers, dispatch/reconcile, or add Git evidence
semantics. It must deterministically prove a fresh real `.git` structure before
fake execution, exactly one fake execution, existing-root blocking before Git
initialization/execution, no coordinator Git semantics, and the existing R1--R4
truthfulness behavior; where deterministic and inexpensive, it must also prove a
Git-initialization failure is pre-execution and durably reported where possible.
No installed real `codex` runs in those tests. Do not add
`--skip-git-repo-check` to the production adapter. If `git init` does not satisfy
the supported CLI prerequisite, stop and report rather than broaden this
authorization. This authorization permits no provider invocation, retry, new smoke
root, ad-hoc command-line retry, or Level 4. A later retry requires this bounded
correction, deterministic tests, bounded review, and a new maintainer one-shot
authorization.

Level-3 scope and limits:: This smoke tests only `AgentExecutionRequest` ->
`CodexCliAdapter` -> real Codex CLI -> real provider ->
`AgentExecutionOutcome` -> `validated_result()`. A success establishes only that
the current CLI launches through the adapter, accepts the emitted strict
structured-output schema, returns a result carrier, and is accepted by existing
provider-neutral correlation/validation for one tiny real adapter-contract
invocation. It does not establish Coordinator Run/Hop lifecycle, Git mutation,
reducer behavior, dispatch/reconcile, Finding convergence, Slice-4A coordinator
happy path, routine repository use, Slice 4B, or Slice 5. Slice-4A coordinator
happy path remains *NOT YET PROVEN*.

W1 operator note:: The Codex adapter still uses `--sandbox workspace-write` and
`--ask-for-approval never`. The prompt requests no repository, shell/tool, or Git
work, but that is requested model behavior, not a hard capability prohibition.
Level 3 exercises *no Coordinator Git workflow or Git-authority semantics*.
A disposable local Git repository may exist solely to satisfy the external CLI
working-directory prerequisite; this does not make Level 3 a Git E2E. No stronger
enforcement is claimed and no sandbox redesign is authorized.

After live execution:: On success, preserve `smoke-summary.json` and result
artifacts, record Level-3 evidence, independently/boundedly review the result as
appropriate, and then obtain a maintainer lifecycle disposition before any Level
4 decision. On failure, preserve evidence, classify the failure, and do not retry
without new authorization. Level 4 is not pre-authorized.

Not authorized by this section:: A second actual real-provider invocation; Level-4
E2E; another smoke root; retry, resume, alternate live configuration, ad-hoc
command-line retry, `--skip-git-repo-check`, or interactive recovery; a field-only
schema patch; Claude support; provider
routing/registry/discovery/plugins/capability framework; repository/package
carve-out; Slice 4B; Slice 5; DMC scope expansion; work on S4AC-R4, S4H-R3/R4/R5,
or the models-cache warning; or Project Memory updates. This section creates no
tactical Project Memory event and no self-commit-hash backfill.

===== 11.5 Slice 5 -- bounded multi-Hop / DMC-profile dogfood

Status:: *NOT AUTHORIZED.* DMC Family Cycle 2 is also not authorized or started.

Scope:: N>1; optional supervisor; correction/delta loops; prerequisite
suspension/common reconcile; integrated non-DMC complex fixture; DMC-shaped
profile; separately authorized dogfood task.

Acceptance::

. N>1 uses the unchanged Run/Hop/Finding/budget model and stops exactly at the
  configured outer budget.
. Optional supervisor is invoked only at declared points, selects only finite
  routes, performs no authoritative Finding disposition, and fails closed to
  human on forbidden/ambiguous authority.
. Productive >3 correction replay and non-progress/reclassification paths work
  against the integrated coordinator, not only Slice-1 reducer fixtures.
. Parent/child Runs prove suspension and common current-state reconcile without
  stale route restoration or separate rebaseline counter/state machine.
. The non-DMC bug-workflow fixture passes integrated review/correction/rereview
  with stable Finding identity using the same engine; it adds no production
  workflow and no generic special case.
. DMC profile represents assess/design/readiness/implement/review/correction/
  closure/lifecycle plus optional advisory/prerequisite/reconcile routes while
  generic code contains none of those names.
. Maintainer separately authorizes the dogfood WorkItem and budget. This FTR does
  not implicitly authorize DMC Family Cycle 2.

===== 11.6 Complete V1 acceptance

V1 is complete only when all five slices are independently reviewed and their
findings resolved; A--J remain green at integrated level where applicable;
relevant focused, architecture, and full repository tests pass; documentation
and release-note checkpoint are applied; no deferred infrastructure leaked into
V1; and WFC-LIFE-01/02 closure is independently verified under repository
governance.

==== 12. Readiness dispositions and bounded-correction traceability

WFC-ODI-01 -- RESOLVED: one serialization per role::
Human-authored WorkItem/profile/policy configuration is strict YAML;
machine-written transient Run state is JSON; unknown fields reject fail-closed.
Natural-language components remain referenced versioned text. V1 has no second
equivalent human-authored surface.

WFC-RRQ-01 -- CONTRACT RESOLVED; numeric thresholds calibrated::
Per-phase explicit component selection bounds relevance. Silent truncation is
forbidden, and every bounded omission carries a manifest/reference path. The
empirically calibrated V1 omission/reference thresholds are 65,536 bytes per
selected component, 196,608 bytes total bounded context/evidence, and 65,536
diff bytes or 12 changed files for inline diff evidence. They are context/diff hygiene
bounds, not semantic rejection or provider-capacity limits; routing-critical
structured data is never omitted by these bounds. Calibration evidence is
`docs/warm_storage/concepts/agent_workflow_coordinator_slice3_rrq01_calibration_2026-08-28.adoc`.

WFC-RRQ-02 -- V1 BASELINE RESOLVED; real adapter still gated::
`fresh` is the V1 context baseline. `continuing` is optional and unavailable
unless a selected real adapter later declares and proves it. The first live
adapter/API, permissions, cancellation, and result transport remain subject to
Slice-4 independent acceptance; no adapter-generalization begins earlier.

===== 12.1 R1--R8 correspondence

[cols="1,3,3,4",options="header"]
|===
|Finding |Repaired invariant |Architecture correction |FTR and acceptance pin

|R1
|Human escalation mechanically preempts autonomy.
|Sections 5.2, 9.2, and Replay I.
|WFC-AUTH-02/03/06; matrix I; explicit Slice-1 completed-result regression.

|R2
|Supervisor never performs authoritative Finding disposition.
|Sections 4.2, 5.2, and Replay G.
|WFC-AUTH-04/05, WFC-FIND-03; matrix G; Slice-5 supervisor test.

|R3
|Repository-local state satisfies clean-Git portability before write; writable
real-agent state remains outside the agent-writable namespace.
|Sections 8 and 10.1.
|WFC-GIT-06, WFC-PERSIST-02; tracked-ignore/local-exclude Slice-2 fixtures and
external checkout-association boundary validation.

|R4
|Crash and nested return share one reconcile/resume mechanism.
|Sections 4, 10.3, 11, and Replays B/E.
|WFC-MODEL-02, WFC-OBS-02, WFC-FAIL-04/07; matrix B/E; Slices 1, 2, and 5.

|R5
|Durable artifact obligation is typed profile policy.
|Sections 3 and 5.1.
|WFC-PROFILE-02, WFC-OUTPUT-04; required/none Slice-3 fixtures.

|R6
|Prompt composition bounds relevance and makes omissions visible.
|Sections 5.1 and 9.1.
|WFC-PROMPT-01/02/04; WFC-RRQ-01 disposition; Slice-3 selection/omission tests.

|R7
|Complex-profile genericity is falsified outside DMC.
|Section 13 non-DMC fixture.
|WFC-SYS-03 plus matrix J; reducer proof in Slice 1 and integrated proof in
Slice 5.

|R8
|Resume has explicit atomic anchors and revalidation boundary.
|Sections 10.1 and 11.
|WFC-PERSIST-01/02/06 and WFC-FAIL-04; crash/loss/result cases across Slices 1--3.
|===

==== 13. Readiness, review, authorization, and lifecycle plan

The next action is Slice-4 implementation followed by the single authorized
disposable `N=1` proof. It must remain within the reviewed boundary:

* repository-local tracked-ignore preflight, external checkout-association
  fail-closed validation, and atomic reconcile/resume correctness;
* one disposable no-remote repository, one Hop, one invocation, and no retry;
* Option-A agent-owned commit only in that disposable repository; no
  coordinator-created semantic commit;
* Git/result/evidence inspection after the Hop, with any anomaly stopping
  continuation; a stale Git lock is inspected by a human and never auto-deleted;
* no routine-repository use, Slice 5, or autonomous multi-Hop authorization.

The hypothesis is evidence, not an artifact requiring architecture acceptance.
The proof stops after its single invocation regardless of outcome. Any later
authorization remains a separate maintainer decision.

After implemented V1 passes implementation/closure review, realized contracts
shall be condensed into the appropriate canonical AI/developer/technical
documentation. Final distribution is chosen from implemented reality, not
prematurely by this draft. Hypothesis/design/FTR remain working or historical
engineering artifacts rather than automatically becoming canonical truth.

==== 14. Validation contract

Draft/readiness validation::

* render this FTR, the proposed architecture, and the active backlog include;
* run `git diff --check` and validate any Project Memory JSON/context change;
* confirm the hypothesis and readiness-review authority remain intact;
* confirm only architecture/FTR/backlog and administrative Project Memory
  documentation changes in the correction commit;
* validate contract-ID uniqueness, R1's completed-result preemption case, A--H
  preservation, matrix J, WFC-LIFE closure, and R1--R8 correspondence
  mechanically;
* inspect status/authorization wording and the requirements/replay/slice
  traceability.

Future implementation validation::

* every slice runs its named focused tests and relevant architecture guards;
* new/modified test files carry `pytest.mark.ftr("FTR-AGENT-WORKFLOW-COORDINATOR-P5")`;
* Slice 4/5 live validation is explicitly authorized, bounded, and reported;
* complete V1 runs the full active repository suite and source-style guards;
* implementation review records the release-notes checkpoint. Internal-only
  tooling may need no release note, but the review must say why.

==== 15. References and protected surfaces

Primary design inputs::

* `docs/warm_storage/concepts/agent_workflow_coordinator_concept_2026-08-25.adoc`
* `docs/warm_storage/concepts/agent_workflow_coordinator_proposed_architecture_2026-08-25.adoc`

Repository governance inputs::

* `docs/ai_info/project_context.adoc`
* `docs/ai_info/conventions.adoc`
* `docs/ai_info/git_and_workflow.adoc`
* `docs/ai_info/agent_rules.adoc`
* `docs/ai_info/review_instructions.adoc`
* `docs/ai_info/project_memory_workflow.adoc`
* `docs/developer_guide/ch08_git_and_workflow/01_git_and_workflow.adoc`
* `docs/developer_guide/ch06_code_conventions/03_human_readability_review.adoc`

Protected surfaces::

Product runtime under `src/spreadsheet_handling/`; Domain/FK/Lookup and Trusted
Ingress; existing CLI/YAML/plugin/import surfaces; spreadsheet backends and
registries; demo/consumer repositories; release infrastructure; DMC Family
Cycle 2; Project Memory schema/automation; Git hooks; external quality/retrieval
infrastructure.
