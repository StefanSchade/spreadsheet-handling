= Confirmation Review 001: XRef Lookup/Helper Substitution (FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2)
:toc:
:toclevels: 2

Review date:: 2026-08-02
Author:: Claude (Opus 4.8; self-review of the confirmation)
Inputs:: assessment 001, FTR, confirmation 001

== Scope

Independent re-check of whether the confirmed contract is supported by the
recorded evidence and is bounded.

== Findings by review dimension

Evidence for A::
Outbound and inbound equivalence proven on real data for all five surfaces;
dynamic ordering shown unnecessary. B (ordering retained) is factually excluded;
C (unsuitable) is excluded because behaviour is preserved. *Supported.*

All five surfaces::
story_group (compact-multiaxis inverse) and the four cast surfaces (xref
inverse) each verified for outbound cols+values and inbound inverse input.
*Supported.*

Actual workbook persistence::
Real XLSX export + readback used. `_meta.derived` confirmed stripped;
`_meta.sheets` confirmed persisted but keyed by sheet name — the confirmation's
durable-binding requirement follows directly. *Supported.*

Helper provenance::
Two carriers distinguished (transient `enrich_lookup`, durable workbook-view);
the confirmation binds removal to the durable one. *Supported.*

Dynamic-axis behaviour::
Two differing matrix-column fixtures roundtrip exactly; candidate is generic.
*Supported.*

Read-only helper semantics::
`title` kept read-only; drop-mode discard proven; no writeback introduced.
*Supported.*

Accidental genericity::
The asymmetric-key affordance is scoped as opt-in `source_key`/`lookup_key` on
`enrich_lookup` only; a stop condition fires if it grows into a join redesign.
*Bounded.*

Ordering responsibility::
Confirmed eliminated; FTR non-goals forbid a new reorder step and static column
enumeration. *Supported.*

Dogfood completeness::
`domain-contracts-check`, full `pytest`, and worldbuilding `check-reimport` all
green and clean; no Core/PM pipeline invokes `project_by_role`. *Supported.*

Project Memory historical/current distinction::
PM only names the FTR; current-state PM updates deferred to a separate gated
change; historical FTR records retained. *Supported.*

Registry / domain-contract migration::
Step-registry and implementation-unit current-state records identified for
update; generated tables rebuilt; historical records retained. *Supported.*

Grouped-XRef protection::
No grouped-XRef path touched; listed as protected surface. *Supported.*

Implementation boundedness::
Four slices; enabling capability (Slice 1) and inbound binding (Slice 2) are
additive; removal (Slice 4) gated on migration (Slice 3). Stop/reconfirmation
conditions cover the two real risks (asymmetric-key scope creep,
persistence-boundary provenance addressing). *Bounded.*

== Residual risks accepted

* The inbound binding depends on `configure_workbook_view` declaring the display
  helper; the FTR requires a guard for this.
* If a backend-neutral Core capability ships in Slice 1, ODS parity becomes an
  explicit acceptance criterion (already stated).

== Disposition

The confirmation is internally consistent with the assessment evidence, bounded,
and free of the rejected static-column / large-wrapper / encapsulation routes.

'''

XREF LOOKUP HELPER SUBSTITUTION CONFIRMATION ACCEPTED IN REVIEW 001
