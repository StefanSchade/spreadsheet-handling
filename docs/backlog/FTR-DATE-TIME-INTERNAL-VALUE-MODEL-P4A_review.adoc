===== FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A -- Independent Architecture Review 001

*Reviewed FTR:* `docs/backlog/FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A.adoc`
*Review date:* 2026-08-09
*Review type:* independent architecture/design review, read-only, fresh
context (does not reuse the design agent's own reasoning except as claims to
verify)
*Reviewed commit:* `770d4dc091fcf9da0b68f51f41c1b18016815444`
("docs(backlog): define Date/Time internal value model scope")
*Parent commit:* `423da311481b322ec0a310f75031c00b8efeeaad`
("docs(project_memory): record Phase D Slice 1 independent acceptance, point
next_action to Date/Time")
*Diff reviewed:* `423da31..770d4dc` (3 files, 1379 insertions, 1 deletion --
the new FTR, a two-line discoverability pointer in
`FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A.adoc`, and a four-line
`docs/backlog/_ftrs_open.adoc` include registration)
*Initial repository status:* clean
*Final repository status:* clean except this review artifact; no runtime,
test, ADR, or Project Memory file touched; nothing pushed; remained on `main`
for the entire review.

==== Verdict

*ACCEPTED WITH FOLLOW-UP -- DATE/TIME CONTRACT REQUIRES NARROW CORRECTIONS
BEFORE IMPLEMENTATION.*

* Blocking findings: 0.
* Important findings: 2.
* Minor findings: 1.
* The FTR's central recommendation -- accept `Date` and `DateTime` as two
  distinct Phase-D MVP categories, native `datetime.date`/`datetime.datetime`
  as conceptual canonical carriers with `pandas.Timestamp`/`numpy.datetime64`
  as classifiable-not-narrowed carrier variants, no wrapper hierarchy, Time
  and Duration deferred, timezone bounded-open -- is well evidenced and does
  not misrepresent `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` or Slice 1's
  accepted contract. Neither Important finding below invalidates that
  recommendation; both must be corrected before the specific implementation
  slices they touch (D-T1, D-T2) proceed.

==== Baseline and reviewed scope

Read in full, independently, before evaluating the FTR's own claims:
`docs/ai_info/review_instructions.adoc`; the FTR under review (1373 lines,
full text); `FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A.adoc` (parent FTR, full
text including section 17's Slice-1 implementation record);
`FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A_review.adoc` (Independent Review 001,
its follow-up disposition, and Review Pass 002's independent follow-up
verification, including IVM-REVIEW-F7's own text); `FTR-FORMULA-INGRESS-ROUNDTRIP-WHITELIST-P5.adoc`;
`roadmap_domain_hardening_sequence.adoc` (Phase D/E in full);
`ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE`; `ADR-DOMAIN-BOUNDARY-ROBUSTNESS`;
`ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`; and the
`CONC-DOMAIN-BOUNDARY-ROBUSTNESS` entry in
`docs_generated/project_memory/current_context.adoc`. The Trusted Ingress
concept document was not separately re-read in full; the parent FTR's and
this FTR's own already-verified quotations from it were treated as sufficient
for this review's scope (concept content is explicitly non-normative per
`ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY`).

Per this review's own instructions, none of the FTR's "confirmed by probe"
claims were trusted on the design agent's word. Every claim below marked
"independently reproduced" was re-executed from a fresh Python process
against current `core` HEAD (`770d4dc`), not copied from the FTR's own
Validation section.

==== Central review question -- is this the smallest coherent extension of Slice 1?

*Finding: yes, for the category-set and carrier-model recommendation itself.*
Every one of the six challenge questions in section 1 of the review task was
independently checked:

* *Is Date semantically distinct from midnight DateTime?* Yes --
  independently reproduced: `datetime.date(2026,8,9) < datetime.datetime(2026,8,9)`
  raises `TypeError` at the Python level; a `date`+`None` DataFrame column
  stays `object` dtype with a plain `None` missing cell, while a
  `datetime`+`None` column is silently promoted by pandas itself to
  `datetime64[us]` with `pandas.NaT` -- the two categories are not
  interchangeable even at the pandas-ergonomics level, not merely the
  conceptual level.
* *Does current maintained data exercise Date? DateTime?* Yes, both,
  independently reproduced: `yaml.safe_load` on an unquoted `2026-08-09`
  scalar produces a real `datetime.date`; on `2026-08-09T10:30:00` produces a
  real `datetime.datetime`; on `2026-08-09T10:30:00+02:00` produces an aware
  `datetime.datetime`. This is PyYAML's own `SafeLoader` resolver, already
  reachable through `yaml_backend.load_yaml_dir`'s unmodified `yaml.safe_load`
  call, not a hypothetical.
* *Would collapsing either direction lose semantics?* Yes, independently
  confirmed both directions are lossy (day-vs-midnight ambiguity one way,
  dropped time-of-day the other), matching the FTR's own section 7/8
  analysis.
* *Does pandas coercion undermine the distinction at carrier level?*
  Partially, and the FTR states this correctly, not overclaiming: a pure
  `date` column stays `object`/`datetime.date` (undisturbed); a `datetime`
  column is promoted to `datetime64`/`Timestamp` (disturbed in carrier shape
  only, not in semantic category -- a promoted `Timestamp` is still
  conceptually DateTime, never reclassified as Date by the FTR's own
  recommendation).

No evidence found to challenge the two-category recommendation itself. The
concerns this review does raise are narrower: two specific claims inside the
FTR's own evidence base, and one slice-sequencing self-consistency question.

==== 1. numpy.datetime64 unit semantics -- independently probed, decision-ready

Ran the task's own representative probes
(`np.datetime64("2026-08-09","D")`, `np.datetime64("2026-08-09T12:34","m")`,
an `"ns"`-unit value) plus one additional probe the FTR itself did not run: a
`Timestamp` known to be a genuine midnight *DateTime* value, cast to `[D]`
unit, compared against a genuine day-only value at the same unit.

Result: `np.datetime64(pd.Timestamp("2026-08-09 00:00:00"), "D") == np.datetime64("2026-08-09", "D")`
is `True` -- the two are *bit-identical* once stored at day precision. Unit
alone cannot distinguish "this was authored as a date" from "this was a
midnight datetime that got truncated/resampled to day granularity," because
no information about intent survives the cast. Separately confirmed: pandas
does not even preserve the unit distinction on ordinary `Series` construction
-- both `datetime64[D]` and `datetime64[m]` source arrays produce a
`datetime64[s]`-dtype `Series` of `Timestamp` scalars in the installed pandas
version, discarding the original unit before any framework code runs.

*Assessment:* this independently reproduces and strengthens, rather than
contradicts, the FTR's own recommendation (section 8, open decision 1 in
section 24) not to infer Date-vs-DateTime from `numpy.datetime64` unit. The
evidence is now stronger than what the FTR itself presented -- the FTR
asserted the unit "is not used" as a matter of policy preference; this review
adds that the unit *could not reliably be used* even if a future slice wanted
to, because day-precision storage is genuinely ambiguous between "authored as
a date" and "datetime truncated to a date." This closes decision-ready
evidence gap 1 in the FTR's own favor: *uniform DateTime classification for
all `numpy.datetime64`, ignoring unit, is the only sound default*; a future
slice would need an out-of-band schema/producer-supplied hint (not carrier
inspection) if day-precision `Date` recognition from raw NumPy is ever
wanted.

==== 2. Timezone-aware DateTime -- independently confirmed reachable, correctly left open

Independently reproduced: aware `datetime.datetime`/`Timestamp` reachable via
the same YAML ingress path as naive values; resulting column dtype is a
genuine pandas extension dtype (`datetime64[us, tz]`-shaped in this pandas
version); naive-vs-aware comparison raises `TypeError`
(`"can't compare offset-naive and offset-aware datetimes"`); the YAML
write-side `Timestamp` `RepresenterError` (section 3 below) reproduces
identically for naive and aware values, so timezone-awareness introduces no
*additional* serialization failure mode beyond the already-latent `Timestamp`
gap. The FTR's three-option framing (accept-as-is / reject-pending-policy /
normalize-to-UTC, with C correctly excluded as unevidenced) is accurate and
appropriately left open -- no current evidence forces a choice between A and
B, and the FTR does not manufacture one.

==== 3. Missing/NaT and IVM-REVIEW-F7 -- decision sound for the cases it covers, but its own evidence base has a material factual error

The FTR's section 6 decision (pd.NaT stays Missing unconditionally;
`numpy.datetime64('NaT')` and `numpy.timedelta64('NaT')` should each require
an explicit, category-specific Missing branch rather than the current
`np.generic`-bounded-dispatch side effect; both currently misclassify as
Missing via that side effect, correctly diagnosed) was independently
reproduced exactly as claimed:

[source,pycon]
----
>>> is_missing_carrier(pd.NaT)                     # True, by identity
>>> scalar_category(np.datetime64("NaT"))           # "missing" (F7, as claimed)
>>> scalar_category(np.timedelta64("NaT"))          # "missing" (F7, as claimed)
>>> scalar_category(np.complex128(complex(float("nan"), 0)))  # "missing" (precedent case, as claimed)
>>> scalar_category(complex(float("nan"), 0))       # UnsupportedScalarError('complex') -- correctly rejected, not missing
----

The `complex128` analogy the FTR leans on is valid *for the NaT-shaped case*.
It does not hold for the non-NaT case -- see finding DTVM-REVIEW-F1, which is
the central finding of this review.

==== 4. Native-Python-vs-wrapper recommendation -- sound

Independently reproduced the pandas-ergonomics table's key rows (pure `date`
list stays `object`/`date`; pure `datetime` list promotes to
`datetime64[us]`/`Timestamp`) and the comparison-semantics table (section 7
below). No evidence found that a wrapper would close a gap native
classification does not already close. Endorsed without reservation.

==== 5. Serialization implications -- independently reproduced, all accurate

* `yaml.safe_dump`/`yaml_backend.save_yaml_dir` on a pure `datetime.date`
  DataFrame: round-trips correctly, independently confirmed via a real
  `save_yaml_dir`/`load_yaml_dir` call (not merely stdlib `yaml`).
* `yaml.safe_dump`/`save_yaml_dir` on a `Timestamp`-backed (i.e. any
  full-datetime YAML-sourced) DataFrame: independently reproduced the exact
  claimed crash --
  `yaml.representer.RepresenterError: ('cannot represent an object', Timestamp(...))`
  -- via a direct call to the repository's own `save_yaml_dir`, not a
  synthetic probe. This is a real, currently reachable, currently broken
  round trip: read a full-timestamp YAML directory, do nothing else, write it
  back, crash.
* `json.dumps` on `date`/`datetime`/`time`: all three raise `TypeError`
  directly, as claimed; no encoder exists anywhere in `json_backend.py`
  (independently grepped).
* `yaml.safe_dump` on `datetime.time`: raises `RepresenterError`, as claimed
  -- confirming Time is *more* expensive to support than Date/DateTime, not
  merely equally deferred.

No correction needed here. This section's "an incomplete runtime slice would
result if Date/DateTime landed before D-T3" framing is accurate and
appropriately not implemented by this FTR.

==== 6. Spreadsheet ingress/egress -- independently reproduced, ADR correctly undisturbed

* XLSX parser: independently grepped 8 `str(val)`-shaped call sites in
  `parser_interpretation.py`, matching the FTR's count exactly.
* ODS parser: independently read `odf_parser.py:_cell_value` -- confirmed the
  claimed branch order (paragraph text first, then `value-type`-keyed
  fallback attributes including separate `date`/`time` branches, each
  immediately `str(...)`-wrapped).
* openpyxl round trip: independently reproduced -- a pure `date` value
  written via `ws.cell(value=date(...))` reads back as
  `datetime.datetime(2026, 8, 9, 0, 0)`, `data_type == "d"`, raw XML shows
  `t="n"` with Excel serial value `46243` and a date `number_format` -- the
  date-vs-midnight-datetime distinction is lost one layer below the
  framework's own parser, exactly as claimed.
* odfpy round trip via the framework's own `_cell_value` (not a
  reimplementation): a genuine ODS date cell *with* paragraph text (the
  normal LibreOffice/Excel-authored case) returns the paragraph text
  (`"08/09/26"`); the same cell *without* paragraph text returns the
  `date-value` fallback (`"2026-08-09"`) -- both independently reproduced
  exactly as claimed.
* Egress asymmetry (section 16): independently reproduced end-to-end through
  the framework's own `save_xlsx`/`save_ods`, not raw openpyxl/odfpy calls --
  a native `datetime.date` payload value in an ordinary DataFrame becomes a
  real Excel date cell (`data_type == "d"`) on XLSX and an ordinary
  `office:value-type="string"` text cell (`"2026-08-09"`) on ODS. The
  divergence is real, current, and reachable exactly as the FTR describes it.

`ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` is not reopened by anything in
this FTR; its own migration-strategy and revisit-criteria sections remain
accurate and unmet. No successor/amendment ADR is required by this FTR's
content, confirmed independently, not merely accepted on the FTR's own
say-so.

==== 7. Comparison semantics -- independently reproduced

`date < datetime` raises `TypeError`; `Timestamp < datetime`/`Timestamp == datetime`
both succeed (subclass relationship); naive-vs-aware comparison raises
`TypeError`; `Series.sort_values()` on a mixed `date`/`datetime`/`None`
object column raises the same `TypeError`. All four independently
reproduced exactly as claimed. `_where_predicates.py` independently
re-grepped: exactly `{"equals", "in", "non_empty", "is_null", "not_null"}`,
no ordering predicate exists -- the FTR's claim that no current consumer
constrains this recommendation is accurate.

==== Findings

===== DTVM-REVIEW-F1 -- section 3's inventory table and section 10 both misstate current `numpy.timedelta64` (non-NaT) classification behavior; it is not currently rejected, it silently misclassifies as `"number"`

*Classification:* Important.

*File/symbol:* FTR section 3 (inventory table, `numpy.timedelta64` (non-`NaT`)
row); FTR section 10 ("`scalar_category` already correctly rejects
`datetime.timedelta`, `pandas.Timedelta`, and non-`NaT` `numpy.timedelta64`
(`UnsupportedScalarError`, confirmed by probe) -- unaffected by this FTR.");
runtime: `src/spreadsheet_handling/core/scalar_values.py:202-208`
(`scalar_category`'s Boolean/Number branches).

*Evidence:* independently reproduced from a fresh process against current
`core` HEAD:

[source,pycon]
----
>>> import numpy as np
>>> from spreadsheet_handling.core.scalar_values import scalar_category
>>> v = np.timedelta64(1, "D")
>>> type(v).__mro__
(<class 'numpy.timedelta64'>, <class 'numpy.signedinteger'>,
 <class 'numpy.integer'>, <class 'numpy.number'>, <class 'numpy.generic'>,
 <class 'object'>)
>>> scalar_category(v)
'number'
----

`numpy.timedelta64` subclasses `numpy.signedinteger`/`numpy.integer` (unlike
`numpy.datetime64`, independently confirmed *not* to subclass `np.integer` or
`np.floating` -- its `__mro__` is only
`(numpy.datetime64, numpy.generic, object)`). `scalar_category`'s Number
branch is `isinstance(value, (int, float, np.integer, np.floating))`
(`scalar_values.py:206`); a non-`NaT` `numpy.timedelta64` satisfies this
before the function ever reaches its trailing `raise UnsupportedScalarError`.
The FTR's own claim that this "already correctly rejects... same path as
`datetime64` above" is false: `datetime64` correctly falls through to the
trailing raise (`np.generic` but not `np.integer`/`np.floating`); `timedelta64`
does not (it *is* `np.integer`), and the two carriers reach materially
different, not "same-path," outcomes. `tests/unit/core/test_scalar_values.py`
independently grepped: contains zero `timedelta64` test cases (neither NaT
nor non-NaT), so nothing in the existing suite would have caught this.

*Why it matters:* this is not a Date/Time-specific defect -- it predates this
FTR and is inherited unchanged from Slice 1 -- but it is exactly the "carrier
silently becoming domain semantics" defect class the entire value-model
program exists to close, and it is currently live in the accepted,
independently-reviewed `scalar_values.py` module today. A `numpy.timedelta64`
value (e.g. the natural result of subtracting two `datetime64` columns, a
completely ordinary pandas operation) reaching `scalar_category` today is
silently accepted as an ordinary `Number` -- its integer nanosecond/day count,
stripped of unit -- rather than rejected as an unsupported Duration carrier.
This directly undermines section 10's own reasoning for deferring Duration
("already correctly rejects... unaffected by this FTR") and weakens section
6's `complex128` analogy: `complex128`'s precedent holds because *every*
`complex128` instance, NaN or not, is uniformly rejected today; `timedelta64`
does not have that property today -- only its `NaT` form is (mis)handled via
Missing; its ordinary form is silently absorbed into Number, a strictly
worse defect (semantic corruption, not merely an inconsistent-Missing
classification) than the one section 6 is built to fix. Per
`ADR-DOMAIN-BOUNDARY-ROBUSTNESS`, this is exactly the class of "cheap,
simplifying, local hardening" the ADR calls desirable -- narrowing the
accepted vocabulary to what the category actually intends, not new defensive
introspection.

*Smallest correction:* (1) correct FTR section 3's inventory-table row and
section 10's claim to state the accurate current behavior (misclassifies as
`"number"`, not rejected); (2) fold the fix into D-T1's own scope -- D-T1
already touches the identical bounded-dispatch mechanism for the NaT case, so
adding an explicit `np.timedelta64` exclusion to (or ordering fix around) the
Number branch is a natural, narrow extension of work D-T1 is already doing,
not a new slice. A minimal fix shape: check for `np.timedelta64` (e.g. via
`isinstance(value, np.timedelta64)`) before the Number branch and route it to
`UnsupportedScalarError` unconditionally (consistent with the FTR's own
Duration-stays-deferred recommendation), mirroring the Boolean-before-Number
subclass-ordering pattern already established in this module.

*Disposition:* non-blocking for accepting this FTR's Date/DateTime category
recommendation (the recommendation does not depend on this claim being
correct). Should be corrected in the FTR text before D-T1 is treated as
implementation-ready, and the underlying runtime misclassification should be
fixed as part of D-T1 (or, if the maintainer prefers not to widen D-T1's
scope, filed as its own narrow BUG referencing this finding) before Phase E
begins relying on `scalar_category`'s Number classification as trustworthy.

===== DTVM-REVIEW-F2 -- D-T2 ("canonical Date/DateTime normalization primitive") contradicts this FTR's own section 12 posture and Slice 1's established Phase-E-owns-normalization precedent, without acknowledging the conflict

*Classification:* Important.

*File/symbol:* FTR section 12 ("Producing an actual canonical value from an
accepted carrier variant remains Phase E's future 'value normalization and
type resolution' stage, per Slice 1's own precedent (IVM-REVIEW-F2's
resolution) -- this FTR does not build that primitive either."); FTR section
20, D-T2 ("Adds the `normalize_scalar()`-shaped primitive Slice 1 and this
FTR both deliberately did not build (section 12) -- narrowing an accepted
carrier variant ... to its native-Python canonical form ... for whichever
future consumer needs one canonical representation rather than mere
classification.").

*Evidence:* `roadmap_domain_hardening_sequence.adoc`'s Phase E boundary shape
explicitly names "value normalization and type resolution" as a Phase E
stage, downstream of Phase D's "establish the internal type model."
`FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A_review.adoc`'s Independent Review 001
answers its own sub-question 4 ("Does Phase E need a normalization primitive
not provided here?") with "Yes," and its resolution of IVM-REVIEW-F2 states
explicitly: "Phase E's 'value normalization and type resolution' boundary
stage still owns adding a normalization primitive on top of these
classifiers." This FTR's own section 12 restates that precedent correctly and
applies it to Date/Time ("this FTR does not build that primitive either").
Section 20's D-T2 then proposes building exactly that primitive -- narrowing
an accepted carrier variant to a canonical native form -- as a *Phase D*
slice, under a `D-T` label, without flagging that this reassigns ownership
away from the precedent section 12 itself just restated two sections earlier
in the same document.

*Why it matters:* the task that produced this FTR explicitly named this a
"major architecture consistency check," and the FTR's own section 24 open
decision 4 asks the maintainer to "confirm... the D-T1..D-T5 slice sequence
and dependency ordering is the right shape" -- but that framing presents the
sequence as needing generic confirmation, not as containing a specific,
already-detectable self-contradiction against the document's own section 12.
A future implementer skimming section 20 in isolation (exactly the failure
mode Independent Review 001 warned about for `ScalarValue`'s
"Native Python representation" framing, IVM-REVIEW-F2) could reasonably
start building a general `normalize_scalar()` primitive inside Phase D,
re-litigating a boundary the project already spent one full review cycle
establishing. D-T3's own text compounds the ambiguity: it lists "a
`normalize_scalar()` call from D-T2" as one option for closing the YAML
write-side gap, treating D-T2's primitive as available Phase-D-internal
infrastructure rather than flagging that its existence there is itself the
open question.

*Smallest correction:* either (a) reframe D-T2 narrowly -- scope it to only
the local `Timestamp`/`numpy.datetime64` -> native-`date`/`datetime`
coercion D-T3's persistence fix concretely needs (a bounded, D-T3-owned
helper, not a general-purpose Phase-E-shaped `normalize_scalar()` contract),
and rename it to make that scope explicit; or (b) if the maintainer decides
Phase D should own the general primitive after all (a legitimate choice --
building it once, early, and having Phase E reuse it is defensible), state
that decision explicitly as an amendment to Slice 1's established boundary,
with its own rationale, rather than silently proposing it under a `D-T`
label two sections after restating the opposite precedent. Either
correction is a documentation-only change to this FTR; no runtime work is
implied by making the correction itself.

*Disposition:* non-blocking for accepting this FTR's Date/DateTime category
recommendation or D-T1. Must be resolved before D-T2 is treated as
implementation-ready; D-T1 does not depend on D-T2 and may proceed once
DTVM-REVIEW-F1 is addressed, independent of this finding's resolution.

===== DTVM-REVIEW-F3 -- section 11's "no `tzinfo` anywhere in either backend's native date-cell round trip" claim was not independently re-probed by this review beyond the already-established fact that both formats lack a timezone concept by design

*Classification:* Minor.

*File/symbol:* FTR section 11.

*Evidence:* this review independently confirmed XLSX/ODS date cells carry no
timezone concept via the openpyxl/odfpy round-trip probes already run for
findings under "Spreadsheet ingress/egress" above (neither showed any
`tzinfo`-bearing structure), which is corroborating evidence but not a
dedicated adversarial probe (e.g. deliberately trying to force a
timezone-tagged native XLSX date cell via an unusual `number_format` or
locale setting).

*Disposition:* not a defect. The claim is well-supported by the format
specifications themselves (neither ECMA-376 nor ODF define a per-cell
timezone attribute for date/time values) and by every probe this review did
run. Recorded only because this review's own validation did not go further
than corroboration; a future slice touching spreadsheet timezone handling
specifically should treat this as re-confirmable-but-not-exhaustively-probed,
not as a gap requiring action now.

==== Human decisions -- decision-readiness assessment

. *numpy.datetime64 unit-aware disambiguation.* Decision-ready, and this
  review's own probe (section 1 above) strengthens the FTR's recommendation:
  treat all `numpy.datetime64` uniformly as DateTime; do not infer Date from
  unit. Evidence now shows this is not merely a stylistic preference but the
  only sound default, since day-precision storage is genuinely ambiguous
  between authored-as-date and truncated-from-datetime. *Recommended
  choice:* uniform DateTime classification, confirmed. *Must decide before
  implementation:* no -- D-T1 can proceed with the FTR's own default; only a
  future narrower Date-from-`[D]`-unit recognition (not currently proposed)
  would need a new decision. *Owning slice:* D-T1 as scoped.
. *Timezone-aware DateTime: accept-as-is vs.\ reject-pending-policy.*
  Genuinely open; both this review's and the FTR's own evidence support
  leaving it open rather than forcing a premature choice. *Recommended
  choice:* none forced by evidence; the FTR's own refusal to choose is
  correct. *Must decide before implementation:* yes, before D-T2 (the
  slice whose own acceptance criteria the FTR assigns this resolution to).
  *Owning slice:* D-T2 (contingent on DTVM-REVIEW-F2's resolution of what
  D-T2 actually is).
. *XLSX/ODS egress parity direction.* Genuinely open; three legitimate
  options with no forcing evidence, correctly not decided here.
  *Recommended choice:* none forced by evidence. *Must decide before
  implementation:* no -- explicitly gated behind D-T5, itself gated behind a
  future ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE reopening that has not
  happened and is not proposed by this FTR. *Owning slice:* D-T5, if/when
  authorized.
. *D-T1..D-T5 slice sequence and ordering.* Not yet decision-ready as
  written -- see DTVM-REVIEW-F2. The sequence's *dependency* ordering (D-T1
  before D-T2 before D-T3, D-T4/D-T5 gated appropriately) is sound; D-T2's
  own *scope and ownership* is not yet internally consistent with the rest
  of the document. *Recommended choice:* apply DTVM-REVIEW-F2's smallest
  correction before treating this sequence as confirmed. *Must decide before
  implementation:* yes, before D-T2 specifically; D-T1 is unaffected.
  *Owning slice:* the maintainer, at the point D-T2 is scheduled.

==== Quality of the FTR itself

* *Factual accuracy:* high, with one material exception (DTVM-REVIEW-F1).
  Every other independently-checked FACT-tagged claim in this review
  reproduced exactly as stated, including several this review verified with
  probes the FTR's own Validation section did not explicitly describe running
  (the `[D]`-unit ambiguity probe, the framework's own `save_xlsx`/`save_ods`
  egress probe rather than raw library calls).
* *Observed fact vs.\ recommendation vs.\ prior architecture labeling:*
  consistently and correctly tagged throughout (`[FACT]`/`[RECOMMENDATION]`/
  `[ACCEPTED]`/`[OPEN]`), matching the parent FTR's own convention. No
  instance found of a recommendation smuggled in as fact, other than the
  factual claims corrected by DTVM-REVIEW-F1.
* *Duplicated material:* minimal; the document appropriately restates rather
  than re-derives Slice 1's accepted contract (sections 2, 12, 13) and cites
  rather than repeats the Formula FTR's constraints (throughout).
* *Length:* 1373 lines, comparable to the parent FTR's own 1836-line Slice-1
  design document -- consistent with this repository's established
  heavy-evidence FTR convention, not excessive by that standard.
* *Implementation-ready claims vs.\ evidence:* mostly proportionate. Section
  20's slice list is explicitly and repeatedly marked "not authorized for
  implementation," correctly. DTVM-REVIEW-F2 is the one place an
  implementation-shaped claim (D-T2's own scope) outruns the document's own
  stated posture.
* *Acceptance criteria testability:* section 21's criteria are checkable and
  were independently checked where feasible for a docs-only review (diff
  scope, `git diff --check`, no source/test/ADR/Project-Memory changes, both
  `.adoc` files render cleanly under `asciidoctor`, all independently
  confirmed).
* *Non-goals:* adequately strong; section 23 correctly excludes Formula, any
  wrapper class, any parser/renderer change, any Phase E implementation, and
  any new ADR.
* *AsciiDoc structure:* title at `=====`, owned sections at `====`, matching
  the dominant, currently-live convention this repository's own Slice-1
  Review Pass 002 already surveyed and confirmed (28 of 34 backlog files with
  sub-headings use this shape). Both `FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A.adoc`
  and `_ftrs_open.adoc` independently confirmed to render cleanly
  (`asciidoctor -o /dev/null`, exit 0, no warnings, this review's own run).

==== Lifecycle cleanup check

This review does not declare any FTR/BUG workstream green or complete.

* `FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A` status should *not* change. It
  remains what it already declares itself to be -- a decision-ready inventory
  and contract-design document, not an implemented slice -- pending the two
  Important findings' correction.
* No removal from `docs/backlog/_ftrs_open.adoc` is appropriate; the FTR (and
  its new registration entry, independently confirmed present and correctly
  placed) correctly stays included.
* No companion-artifact move to `docs/cold_storage/backlog/ftrs_done/` is
  appropriate -- nothing here is Done.
* Residual scope: DTVM-REVIEW-F1's underlying runtime misclassification is
  live, independent evidence that predates this FTR and is not fixed by
  anything in this diff. Whether it becomes its own narrow BUG or is folded
  into D-T1's own acceptance criteria is a maintainer decision this review
  does not make (see DTVM-REVIEW-F1's disposition); either path is a
  legitimate follow-up, neither is performed here.
* This review's own two Important findings are, per
  `project_memory_workflow.adoc`'s bounded-growth rule, plausibly worth a
  coarse Concern-event record on `CONC-DOMAIN-BOUNDARY-ROBUSTNESS` once the
  FTR corrections land and a follow-up review confirms them -- recorded here
  as a handoff note, not performed by this review (Project Memory was not
  updated, per this task's own explicit instruction).

==== Validation

Baseline and diff inspection:

[source,console]
----
cd ~/repos/spreadsheet-handling/core
git status --short
git rev-parse HEAD
git log --oneline --decorate -25
git rev-parse 770d4dc^
git diff --stat 423da31..770d4dc
git diff --check 423da31..770d4dc
asciidoctor -o /dev/null docs/backlog/FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A.adoc
asciidoctor -o /dev/null docs/backlog/_ftrs_open.adoc
----

Result: clean tree throughout; HEAD `770d4dc` matches the expected commit;
parent `423da31`; three files changed (1379 insertions, 1 deletion); no
whitespace errors; both `.adoc` files render cleanly (exit 0, no warnings).

Source re-reads (independent, not copied from the FTR's own citations):
`core/scalar_values.py` (full module); `xlsx/parser_interpretation.py`
(`str(val)` call-site grep, 8 confirmed); `ods/odf_parser.py` (`_cell_value`,
full branch structure); `ods/odf_renderer.py` (`_is_missing_carrier`, branch
order); `xlsx/openpyxl_renderer.py` (`_xlsx_cell_value`, confirmed no
date-specific branch); `json_backend.py`, `csv_backend.py` (`dtype=str`/
`convert_dates=False`/`keep_default_na=False`/`na_values=[]` call sites);
`core/fk.py:_norm_id`; `domain/_where_predicates.py` (full supported-predicate
set); `xlsx_backend.py:save_xlsx`, `ods_backend.py:save_ods` (used directly
for the egress-asymmetry probe, not raw library calls);
`tests/unit/core/test_scalar_values.py` (grepped for `timedelta64` coverage:
none found).

Independent live probes (read-only, scratchpad-only, nothing left in the
repository):

* Full re-execution of `is_missing_carrier`/`scalar_category` against every
  case in the FTR's section 3 table, plus `numpy.timedelta64(1, "D")`
  (DTVM-REVIEW-F1) and its `__mro__`.
* YAML native timestamp resolution (date/datetime/aware-datetime) and its
  effect once assembled into a `pandas.DataFrame`, mirroring
  `yaml_backend.load_yaml_dir`'s own construction shape.
* `pandas.DataFrame` construction/dtype/scalar-type inspection for pure date,
  pure datetime, date+`None`, datetime+`None` columns.
* Comparison-semantics probes: `date < date`, `date < datetime`,
  `Timestamp < datetime`, `Timestamp == datetime`, aware-vs-naive comparison,
  `Series.sort_values()` on a mixed date/datetime/`None` object column.
* `yaml.safe_dump`/`json.dumps` on native `date`/`datetime`/`time`, and on
  `pandas.Timestamp`; a direct `save_yaml_dir`/`load_yaml_dir` round trip on
  a pure-`date` DataFrame (succeeded) and a `Timestamp`-backed DataFrame
  (reproduced the claimed `RepresenterError` crash).
* OpenPyXL round-trip probe: native `date` value written via
  `ws.cell(value=...)`, reloaded with `data_only=True`, `cell.value`/
  `cell.data_type`/`cell.number_format` inspected; raw
  `xl/worksheets/sheet1.xml` inspected to confirm native Excel serial-date
  storage.
* odfpy round-trip probe via the framework's own `odf_parser._cell_value`
  directly: a genuine ODS date cell with and without paragraph text.
* Egress-asymmetry probe through the framework's own `save_xlsx`/`save_ods`
  (not raw openpyxl/odfpy): a native `datetime.date` payload value produces a
  real Excel date cell on XLSX and a `office:value-type="string"` text cell
  on ODS.
* `numpy.datetime64` unit probe: `"D"`, `"m"`, `"ns"` units; a `Timestamp`
  known to be a genuine midnight value cast to `[D]` unit, confirmed
  bit-identical to a genuine day-only value at the same unit; pandas
  `Series` construction from `datetime64[D]`/`datetime64[m]` source arrays,
  confirmed both collapse to `datetime64[s]`/`Timestamp` uniformly.
* `numpy.complex128` NaN and non-NaN classification, and plain `complex` NaN
  classification, re-confirming the precedent section 6 relies on.

No production or test file was modified by any probe. `git diff --check`
shows only this new review artifact as an addition; no existing file was
altered by this review.

==== Conclusion

The Date/Time Internal Value Model contract is architecturally sound at its
center: `Date` and `DateTime` as two distinct MVP categories, native
`datetime.date`/`datetime.datetime` as conceptual canonical carriers with
`pandas.Timestamp`/`numpy.datetime64` accepted as classified-not-narrowed
carrier variants, no wrapper hierarchy, Time and Duration correctly deferred
(and independently confirmed unreachable in any maintained producer/consumer
path), and timezone handling correctly left open rather than resolved by
assumption. `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` is not reopened and
does not need to be. No successor/amendment ADR is required by this FTR's
content.

Two Important findings require correction before their respective slices
proceed. DTVM-REVIEW-F1 is a factual error in the FTR's own inventory and
Duration-deferral reasoning: `numpy.timedelta64` (non-`NaT`) is not currently
rejected, as both section 3 and section 10 claim -- it silently misclassifies
as `"number"` today, a live, pre-existing, evidenced defect this review
discovered independently and that the existing test suite does not cover.
DTVM-REVIEW-F2 is a slice-sequencing self-consistency gap: D-T2 as currently
scoped proposes building the general normalization primitive that this FTR's
own section 12 -- two sections earlier in the same document -- correctly
states remains Phase E's job per Slice 1's established precedent. Neither
finding invalidates the FTR's central category recommendation or blocks D-T1
from proceeding once DTVM-REVIEW-F1 is corrected; DTVM-REVIEW-F2 blocks only
D-T2, not D-T1.

[source,text]
----
ACCEPTED WITH FOLLOW-UP -- DATE/TIME CONTRACT REQUIRES NARROW CORRECTIONS
BEFORE IMPLEMENTATION.
----
