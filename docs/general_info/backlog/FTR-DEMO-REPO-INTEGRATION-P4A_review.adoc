= Review: FTR-DEMO-REPO-INTEGRATION-P4A

Date: 2026-05-17

Status: *Self-documentation -- pending independent review.* Written by the
implementing agent for the partial delivery (realignment + verification
slice). Not an accepted verdict until a second pass confirms it. The FTR
remains *Open*.

== Scope of this pass

Implementation of the demo-repo realignment driven by the prototype consumer
finding. Multi-repo: documentation/status in core
(`spreadsheet-handling`), runnable changes in the consumer demo
(`spreadsheet-handling-demo`, branch `dev`).

== What changed (demo repo, focused commits on `dev`)

* `9174437` -- realign maintained pipelines to canonical FK step names
  (`apply_fks` -> `add_fk_helpers`, `reorder_helpers` -> `reorder_fk_helpers`;
  same `defaults` block). Authoritative mapping taken from the core step
  registry (`add_fk_helpers` `replaces: [apply_fks]`, `reorder_fk_helpers`
  `replaces: [reorder_helpers]`, both `status: current`). No core aliases.
* `29ad0c1` -- realign the product-business-slice walkthrough to the canonical
  steps; rework the README into a guided curated journey (run / outputs /
  observe / idea) without dumping internals.
* `3d37f83` -- optional, clearly-secondary, bounded Storyforge first slice
  (characters/species FK helper -> readable workbook) + one smoke test.

== Verification

* Local binding: demo `.venv` editable-bound to the local core checkout
  `0.1.0b6.dev191` (via the demo's own `make setup-lib-local`).
* Flows run clean (exit 0): product business slice forward, workbook
  reimport, fk_helpers, Storyforge.
* Demo smoke suite: `.venv/bin/pytest -q` -> 9 passed (was 6 passed / 2
  failed on the unknown `apply_fks` step before realignment). Existing checks
  are already meaningful (named-range presence, roundtrip edit preservation);
  no golden-output dumps were added.
* Core: no `src/`/test changes were made, so no core verification was
  required for this pass; the core's own suites are unaffected.

== Deferred / intentionally not implemented

* Full ACME beta-slice tutorial buildout (XRef/matrix + its reimport
  tutorial, resource fallback/override, key-value resource writer, artifact
  manifest, explicit XLSX/ODS maintained-path tutorial). Not built, not
  implied as done.
* `DEMO-*` backlog, demo release/tag plan, Pages publishing -- not in scope
  here; `spreadsheet-handling-pages` untouched (future/planned).
* Deferred ACME patterns A-F -- not implemented or implied.

== Verdict (provisional, self-assessed)

PARTIAL. The realignment objective is fully met and verified: the maintained
demo journey no longer drifts from the canonical core surface, runs against
the beta-candidate local checkout, and the public entrypoint guides a new
user. The optional Worldbuilding trail is present, secondary, and bounded.
The broader ACME beta-slice journey, the `DEMO-*` backlog, and the release/tag
plan remain open, so the FTR stays *Open*. `_ftrs_open.adoc` is intentionally
unchanged (no lifecycle transition, no new FTR).

== Recommended next steps for an independent reviewer

* Confirm the `apply_fks`/`reorder_helpers` -> canonical rename is the
  intended `0.1.0b6` interface (registry `replaces` + `status: current`
  indicate yes) and that treating it as demo realignment (no core alias) is
  correct.
* Decide whether the next slice builds out the ACME XRef/matrix +
  resource-writer/manifest tutorials, and whether the `DEMO-*` backlog and
  release/tag plan should be a separate follow-up rather than blocking this
  FTR indefinitely.
* Confirm the Storyforge seed stays bounded (no matrix/reimport/lore) in
  future passes.
