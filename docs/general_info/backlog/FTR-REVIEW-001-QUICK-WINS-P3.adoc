===== FTR-REVIEW-001-QUICK-WINS-P3 — Bundle of self-contained quick fixes from REVIEW-001

*Status:* Open
*Priority:* P3
*Area:* io_backends / rendering / pipeline / domain
*Kind:* Bugfix / Hygiene
*Review source:* REVIEW-001, Quick Wins section, Finding IDs: A-002, A-003, A-006,
A-007, D-007, IR-007

==== Purpose

Six self-contained fixes from the review are each under 30 minutes and have no
dependencies on other clusters. Bundling them in a single PR minimises context-switch
overhead while delivering immediate correctness and hygiene value. Each item is
independently verifiable. One item (A-002) touches a gated CLI flag and requires
explicit developer approval before execution.

==== Checklist

- [ ] *A-006* — Delete `io_backends/_interfaces.py`.
  Dead 7-line legacy re-export; imported by nothing in `src/` or `tests/`.
  - File to delete: `src/spreadsheet_handling/io_backends/_interfaces.py`
  - Acceptance: `grep -r '_interfaces' src/ tests/` returns no matches after deletion;
    full test suite passes.

- [ ] *A-007* — Fix `coerce_backend_options(None)` return value.
  Change `return cast(BackendOptions, {})` → `return BackendOptions()`.
  - File: `src/spreadsheet_handling/io_backends/` (locate with
    `grep -r 'coerce_backend_options' src/`)
  - Acceptance: `coerce_backend_options(None)` returns a `BackendOptions` instance (not
    a plain `dict`); `isinstance(coerce_backend_options(None), BackendOptions)` is
    `True`.

- [ ] *A-003 (partial)* — Skip `_meta` in `XMLBackend` and `save_yaml_dir` writers.
  Add the same `if k == "_meta": continue` guard used in `JSONBackend` /
  `save_csv_dir`.
  - Files: `src/spreadsheet_handling/io_backends/xml_backend.py:152` (approximately),
    `src/spreadsheet_handling/io_backends/yaml_backend.py:76` (approximately).
  - Acceptance: `sheets-run --out-kind yaml_dir` and `sheets-run --out-kind xml_dir`
    no longer raise `AttributeError` on a meta-producing pipeline; test added or
    existing test updated to exercise `_meta`-skip path.

- [ ] *IR-007* — Delete unused step factories.
  `make_flatten_headers_step` and `make_unflatten_headers_step` are not imported
  anywhere outside `tests/` (if they appear in tests, migrate those tests to use
  `build_steps_from_config` instead).
  - Files: locate with
    `grep -r 'make_flatten_headers_step\|make_unflatten_headers_step' src/ tests/`
  - Acceptance: `grep -r 'make_flatten_headers_step\|make_unflatten_headers_step' src/`
    returns no matches; full test suite passes.

- [ ] *D-007* — Make `_deep_merge`, `_get_meta`, `_set_meta` public in
  `meta_bootstrap.py`.
  Rename to `deep_merge`, `get_meta`, `set_meta` (remove leading underscore); update
  the one known consumer `yaml_overrides.py` to use the public names.
  - Files: `src/spreadsheet_handling/domain/meta_bootstrap.py`,
    `src/spreadsheet_handling/domain/yaml_overrides.py`.
  - Acceptance: `grep '_deep_merge\|_get_meta\|_set_meta' src/` returns no matches;
    `grep 'deep_merge\|get_meta\|set_meta' src/spreadsheet_handling/domain/
    meta_bootstrap.py` confirms public definitions; full test suite passes.

- [ ] *A-002 (minimal fix)* — Remove `csv_dir` from `sheets-pack --input-kind` choices.
  ⚠ *This item touches a gated CLI surface listed in `interfaces_and_gates.adoc`.*
  Requires explicit developer approval before execution. Do not execute without
  confirmation.
  The minimal fix removes `csv_dir` from the `choices` list in the `sheets-pack`
  argument parser so the option is no longer advertised as valid. The full fix
  (adding a `csv_dir` branch to `orchestrate()`) is tracked in
  FTR-REVIEW-001-BACKEND-DISPATCH-P4A.
  - File: `src/spreadsheet_handling/cli/` (locate the `sheets-pack` argument parser
    with `grep -r 'csv_dir' src/spreadsheet_handling/cli/`)
  - Acceptance: `sheets-pack --help` no longer lists `csv_dir` as a valid
    `--input-kind` choice; `sheets-pack --input-kind csv_dir` produces a clear
    argparse error rather than an internal `ValueError`; full test suite passes.

==== Non-Goals

* Do not implement the full `orchestrate()` CSV branch here — that is C5
  (FTR-REVIEW-001-BACKEND-DISPATCH-P4A).
* Do not add a YAML/XML `_meta` sidecar output — the A-003 partial fix is skip-only;
  the sidecar question is a separate design decision.
* Do not rename or restructure modules beyond the specific symbols named above.

==== Dependencies

None. All items are independent of each other and of other clusters.

==== Risk

Low overall. The A-002 minimal fix touches a public CLI flag (low functional risk; high
visibility risk — requires approval). All other items are internal-only with no
consumer-visible change.

==== Acceptance

All six items checked off above; full test suite passes in one combined run.
