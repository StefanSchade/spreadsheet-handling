= Review: FTR-PROTOTYPE-CONSUMER-INTEGRATION-P4A

Date: 2026-05-17

Status: *Self-documentation — pending independent review.* This note records
the pre-beta prototype-consumer integration pass performed for this gate FTR.
It is written by the executing agent and has *not* yet been independently
reviewed; it must not be treated as an accepted verdict until a second pass
confirms it.

== Scope

Gate FTR, not a feature. The pass:

* (correction) initially assumed no consumer repo was provisioned; the
  developer then designated `spreadsheet-handling-demo` (on `dev`, `main`
  merged, clean) as the consumer integration surface. The real run below
  supersedes the earlier environment-blocker conclusion.
* bound the demo consumer to the local `spreadsheet-handling` checkout via the
  demo's own non-invasive `make setup-lib-local`
  (`pip install -e ../spreadsheet-handling`; only the gitignored `.venv`
  changed);
* ran the maintained demo flow (`make run` product business slice) and the
  demo smoke tests against the local `0.1.0b6.dev` core;
* re-ran the three FTR-named in-core validation targets as the core
  beta-readiness proxy;
* classified the failure and assigned its owning FTR; made no core
  `src/`/test changes and no tracked demo changes.

== Findings

* *Binding: PASS.* Demo venv bound to local core
  `0.1.0b6.dev191+g1431e7ff0` from
  `/home/stefan/repos/spreadsheet-handling/src`. No tracked demo file touched.
* *Demo smoke tests: 6 passed, 2 failed* (`.FF.....`). Non-FK journeys
  (multiheader, transforms, verify-steps) pass against the expanded core.
* *Maintained product-business-slice flow: FAIL.* `make run` and the two
  failing smoke tests share one root cause:
  `KeyError: Unknown step 'apply_fks'`. The demo's checked-in
  `demo_product_business_slice.yaml` uses pre-`0.1.0b6` step IDs `apply_fks`
  and `reorder_helpers`; the core registry renamed these to
  `add_fk_helpers` / `configure_fk_helpers` / `reorder_fk_helpers` during the
  `0.1.0b6` expansion (capability verified present under new names).
* *In-core proxy: PASS.* `test_configured_workbook_view_reimport_roundtrip.py`,
  `test_ods_xlsx_parity.py`, `test_lazy_optional_imports.py` => 15 passed.
* *Classification:* consumer-specific config drift from a core-side rename of
  consumer-facing step keys between the demo's `0.1.0b5` pin and the local
  `0.1.0b6.dev` checkout — *not* a missing generic core capability. This is
  the intended pre-beta signal (demo missed the `0.1.0b6` expansion).
* *Boundaries respected.* One-way dependency held; no consumer data copied to
  core; no core `src/`/test changes; no tracked demo changes (demo YAML fix
  deliberately not applied — it is owned by `FTR-DEMO-REPO-INTEGRATION-P4A`);
  no push/pull; both working trees clean.

== Verdict (provisional, self-assessed)

PARTIAL PASS. The acceptance OR clause is satisfied: the selected consumer
flow runs against the local checkout (clean binding + 6/8 maintained flows
pass + in-core proxy green) and the residual blocker is documented with a
concrete next owner — the already-open `FTR-DEMO-REPO-INTEGRATION-P4A`.

Lifecycle status remains *Open*. `_ftrs_open.adoc` is intentionally unchanged:
no lifecycle transition and no new follow-up FTR (the owning demo-realignment
FTR already exists and is open).

== Recommended next step for an independent reviewer

* Confirm the `apply_fks` / `reorder_helpers` -> `add_fk_helpers` /
  `configure_fk_helpers` / `reorder_fk_helpers` rename is an intended,
  documented `0.1.0b6` interface change (then it is purely demo-realignment
  work for `FTR-DEMO-REPO-INTEGRATION-P4A`, not a core regression).
* Confirm no other generic capability gap is masked behind the single
  blocking step error (6/8 demo flows + the in-core proxy already pass, which
  is corroborating evidence).
* Decide whether this gate can move to Done once `FTR-DEMO-REPO-INTEGRATION-P4A`
  realigns the demo configs, or whether a fresh consumer pass is required after
  that realignment.
