= Review: FTR-PROTOTYPE-CONSUMER-INTEGRATION-P4A

Date: 2026-05-17

Status: *Self-documentation — pending independent review.* This note records
the pre-beta prototype-consumer integration pass performed for this gate FTR.
It is written by the executing agent and has *not* yet been independently
reviewed; it must not be treated as an accepted verdict until a second pass
confirms it.

== Scope

Gate FTR, not a feature. The pass:

* identified the maintained workbook flow representing beta readiness
  (configured workbook view reimport roundtrip, derived from the prototype
  consumer discussion captured in
  `by_use_case/business_calculation_workbook_roundtrip.adoc`);
* bound the flow to the local `spreadsheet-handling` checkout via its public
  pipeline API and console entrypoints (no vendoring/copying);
* ran the three FTR-named validation targets as the in-core beta-readiness
  proxy;
* classified the inability to run an external consumer repository as an
  environment/access blocker rather than a library capability gap;
* recorded the result in the FTR and made no core `src/`/test changes.

== Findings

* *In-core proxy: PASS.* `pytest` over
  `test_configured_workbook_view_reimport_roundtrip.py`,
  `test_ods_xlsx_parity.py`, and `test_lazy_optional_imports.py` => 15 passed
  in 1.33s. The configured workbook view reimport roundtrip runs end to end
  against the local checkout across XLSX and ODS, including renamed-sheet
  remap and the no-edit identity roundtrip.
* *External consumer repo: BLOCKED (environment/access).* No separate
  prototype consumer repository exists or is accessible in this environment.
  Only the core repo and `spreadsheet-handling-demo` (out of scope for this
  FTR) are present; no other consumer repo was ever cloned on this VM. The
  prototype consumer was a design discussion that already produced the 16
  derived FTRs, `REVIEW-prototype-consumer-core-gaps-P4A.adoc`, and the two
  `by_use_case` concept notes.
* *No generic gap.* The expected pre-beta capability set is already
  implemented per the closed/archived P4A FTRs and the
  `FTR-ODS-CONSUMER-FEATURE-PARITY-P4A` parity matrix. No missing generic
  spreadsheet-handling behavior surfaced; no follow-up FTR created. Payload
  conflict/precedence stays deferred to
  `FTR-WORKBOOK-VIEW-PAYLOAD-CONFLICT-PRECEDENCE-P6`.
* *Boundaries respected.* No consumer data copied into core (none exists to
  copy); dependency direction one-way; demo repo untouched; no push/pull; both
  working trees clean; proxy writes only to pytest `tmp_path`.

== Verdict (provisional, self-assessed)

PARTIAL PASS. The acceptance OR clause is satisfied: the selected flow runs
against the local checkout (in-core proxy passes) and the external-repo step
is documented as an environment blocker with no generic next FTR required.

Lifecycle status remains *Open*: a controlled integration against a real,
separate consumer repository has not been physically performed in this
environment. `_ftrs_open.adoc` is intentionally left unchanged (no lifecycle
status transition, no new follow-up FTR).

== Recommended next step for an independent reviewer

* Confirm the in-core proxy genuinely represents the prototype-consumer
  workbook flow and is sufficient for the beta gate, or require provisioning a
  real external consumer repository before this FTR can move to Done.
* Confirm no generic capability gap was missed by the proxy-only run.
