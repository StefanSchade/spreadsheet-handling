= Review: FTR-ODS-CONSUMER-FEATURE-PARITY-P4A

Date: 2026-05-17

Status: *Independent review accepted.* This note records the audit performed
for this cross-cutting tracking FTR and the independent review of commit
`c2e5a7b`.

Reviewed commit:

* `c2e5a7b` --
  `docs(FTR-ODS-CONSUMER-FEATURE-PARITY-P4A): document pre-beta ODS parity`

== Scope

This is a cross-cutting tracking FTR, not a feature. The audit:

* recorded an explicit ODS disposition for every listed pre-beta
  prototype-consumer-derived workbook feature;
* added the missing in-FTR disposition notes (key-value resource writer,
  generated artifact manifest, resource fallback/override semantics);
* built the compact parity matrix in the tracking FTR and cross-referenced it
  from the existing adapter capability profile;
* verified the post-wave invariants without changing adapter behavior.

== Verdict

ACCEPT. The audit documentation is truthful, complete enough for the beta
gate, and not misleading. Acceptance criteria are met:

* Every listed feature has an explicit disposition (implemented / not
  applicable / accepted gap) in its owning FTR and in the parity matrix.
* The maintained declarative workbook path is exercised on ODS by existing
  integration tests
  (`test_configured_workbook_view_renders_portably_across_xlsx_and_ods`,
  `test_workbook_view_reverse_mapping_roundtrips_portably_across_xlsx_and_ods`,
  `test_configured_reimport_roundtrip_across_xlsx_and_ods`).
* Accepted gaps (protection, column-width, freeze readback) are documented in
  their FTRs and in `spreadsheet_backend_capability_profiles.adoc`; none are
  simulated.
* No new generic workbook feature carries only implicit XLSX behavior.

`c2e5a7b` is safe as the ODS parity beta gate.

== Architectural Assessment

No production code changed and no new tests were added. This is deliberate:

* The *implemented* dispositions are each already proven by an existing
  ODS+XLSX test (see the matrix evidence column). Duplicating those would
  violate the FTR non-goal "do not duplicate every XLSX integration test
  one-to-one".
* The *not applicable* items (resource fallback/override, key-value writer,
  artifact manifest, derived-column policy slice 1) are backend-neutral by
  construction: they are domain/in-pipeline or text/JSON-writer steps with no
  `WorkbookIR` / `RenderPlan` / spreadsheet-parse path. Forcing an ODS parity
  test there would be fake parity, which is an explicit hard boundary.
* The *accepted gaps* reflect materially different backend models and are
  documented rather than hidden or simulated.

ODS parity here functioned as an architecture pressure test: the audit found
the post-REVIEW-001 router centralization already routes new spreadsheet
features through both backends, so no XLSX-only drift was discovered.

== Post-Wave Invariants

* Backend dispatch remains centralized through `io_backends/router.py`; no
  parallel XLSX-only dispatch was introduced (no code changed).
* The lazy-optional-import guard
  `test_package_import_does_not_load_spreadsheet_backend_modules` stays green.

== Findings

No defects. One observation, no action required: the freeze readback gap is
already separately tracked by `FTR-ODS-FREEZE-READBACK-P3K`; it remains an
accepted gap for the pre-beta parity claim and is not in scope to close here.

One evidence note, also no action required: the matrix entry for "validation
dropdowns from legend metadata" is supported by the existing chain of
backend-neutral `from_legend` validation resolution, ODS list-validation
renderer/parser coverage, and ODS legend-block metadata roundtrip coverage.
The audit does not need a duplicated end-to-end ODS-only legend dropdown test
to satisfy the beta gate.

== Checks Run

----
source .venv/bin/activate && python -m pytest tests/integration/io_backends/test_ods_xlsx_parity.py
source .venv/bin/activate && python -m pytest tests/integration/pipeline/test_configured_workbook_view_reimport_roundtrip.py
source .venv/bin/activate && python -m pytest tests/unit/io_backends/test_lazy_optional_imports.py
source .venv/bin/activate && python -m pytest tests/unit/io_backends/ods
source .venv/bin/activate && python -m pytest tests/architecture/pipeline_semantics \
  tests/architecture/semantic_invariants/test_meta_registry.py \
  tests/architecture/semantic_invariants/test_meta_registry_hardening.py
----

Independent review result:

* `tests/integration/io_backends/test_ods_xlsx_parity.py` -- passed
  (`11 passed`)
* `tests/integration/pipeline/test_configured_workbook_view_reimport_roundtrip.py`
  -- passed (`2 passed`)
* `tests/unit/io_backends/test_lazy_optional_imports.py` -- passed
  (`2 passed`)
* `tests/unit/io_backends/ods` -- passed (`17 passed`)
* `tests/architecture/pipeline_semantics` plus
  `tests/architecture/semantic_invariants/test_meta_registry.py` and
  `tests/architecture/semantic_invariants/test_meta_registry_hardening.py` --
  passed (`15 passed`)

== Follow-up Disposition

Acceptance is satisfied by documentation alone and independent review accepted
the audit. The FTR remains in the active backlog as an implemented tracking
FTR until the enclosing pre-beta backlog phase is closed. No
conflict/precedence work, recomposition semantics, adapter behavior change,
`_meta.derived` change, or heuristic mapping was introduced.
