===== FTR-DOMAIN-CONSOLIDATION — Merge remaining `core/` into `domain/`, coverage gate, deprecation shims

// Extracted → FTR-REVIEW-001-BACKEND-DISPATCH-P4A (2026-05-11):
//   dual pipeline-runner consolidation (run_pipeline(AppConfig) → run_app).
// Extracted → FTR-REVIEW-001-MODULARIZATION-P4A (2026-05-11):
//   public domain API surface / internal marking (stable __all__ per sub-package).

NOTE: Phase 3b prerequisite complete. FK-specific migration is in the REVIEW-001
Supplement below (C2) and should be executed first. Pipeline-runner and API-surface
concerns extracted 2026-05-11 (see comments above). Remaining scope: coverage gate,
non-FK `core/` cleanup after C2 lands, deprecation shims.

*Purpose:*::
Complete the `core/ → domain/` migration (non-FK remainder) after the FK work (C2)
lands, and add a domain coverage gate to protect the layer during and after refactoring.

*Scope:*::
Domain-only; no adapter behaviour changes.
Execute after FTR-REVIEW-001-META-REGISTRY-DERIVED-P3 and the FK work (C2 /
REVIEW-001 Supplement) are complete. Verify whether `pk/` still exists before starting.

*Solution:*

- Establish a **coverage floor** (e.g., 80% domain, 70% overall) and add a CI gate.
- Merge/relocate remaining non-FK `core/` and `pk/` symbols into `domain/` subpackages.
- Provide deprecation shims (`from domain.old import X` → warnings) for any moved
  public symbols, for one release.

*Acceptance:*

- CI passes with new coverage gate.
- Imports from moved packages emit a deprecation warning and still work for one cycle.
- Docs show the new domain layout; internal modules are not referenced from examples.
- No adapter/router changes needed; roundtrip tests remain green.
- Non-FK `core/` symbols are in `domain/`; `pk/` is gone or has an explicit domain role.

'''

====== REVIEW-001 Supplement (2026-05-11) — FK-Konsolidierung specifics

REVIEW-001 Cluster C2 (findings D-002, D-003, D-009, IR-011) maps onto this FTR.
When executing the `core/ → domain/` migration, incorporate the following:

*Target sub-package structure for FK code:*

* `domain/fk/columns.py` — FK naming convention (`FK_PATTERN`), detection
  (`detect_fk_columns`).
* `domain/fk/helpers.py` — column enrichment / provenance
  (`build_registry`, `apply_fk_helpers`).
* `domain/fk/checks.py` — FK validations (content currently in
  `domain/validations/fk_helpers.py`).
* Dissolve `core/fk.py` entirely; trim `core/__init__.py` accordingly.
* Remove backward-compat remnants: the legacy dict-input path in
  `core/fk.py::apply_fk_helpers` and the bare `except Exception` in
  `core/refs.py::add_helper_columns`. (D-009)

*Name alignment (IR-011):*
After the FK sub-package lands, align FK-step factory names to Registry keys:
`make_apply_fks_step` → matches key `add_fk_helpers`; `make_drop_helpers_step` →
`remove_fk_helpers`; `make_check_fk_helpers_step` → `validate_fk_helpers`.

*Guard update:*
The FK architecture guard currently references several symbols by name. Update it to
point to the new `domain/fk/` paths.

*Prerequisite:*
Complete FTR-REVIEW-001-META-REGISTRY-DERIVED-P3 first — `fk_helpers.py` is a
`derived`-channel producer, and the contract should be on record before the file is
moved.
