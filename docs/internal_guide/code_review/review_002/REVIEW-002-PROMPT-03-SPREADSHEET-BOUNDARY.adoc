= Prompt — REVIEW-002 Pass 3 (Adapter / Spreadsheet Boundary Focus)

Context:
You are continuing REVIEW-002 for the spreadsheet-handling repository.

Pass 1 covered:
* broad architectural duplication / misplaced responsibilities
* lifecycle of previous refactor slices
* consolidation opportunities
* major hotspot identification.

Pass 2 covered:
* domain package split candidacy
* behavior-preserving package split feasibility
* duplication clusters and consolidation priorities
* future FTR extraction candidates.

Pass 3 should now focus specifically on:

* spreadsheet adapter boundaries
* parser / renderer seams
* RenderPlan / WorkbookIR contract integrity
* backend-neutrality drift
* adapter-local vs generic logic separation
* read/write path asymmetry
* XLSX/ODS divergence risk
* contract leakage into domain/pipeline layers
* current guard coverage adequacy.

Important:
* This is NOT an implementation task.
* This is NOT a refactoring task.
* Do NOT change code.
* Do NOT propose broad rewrites.
* Work strictly as an architecture reviewer.

Your task:

Perform a focused architectural review of the spreadsheet adapter and spreadsheet contract area.

Analyze at least:

* src/spreadsheet_handling/rendering/
* src/spreadsheet_handling/io_backends/
* src/spreadsheet_handling/spreadsheet_contract/
* parser modules
* renderer modules
* workbook projection modules
* RenderPlan flow
* WorkbookIR ownership
* adapter facade boundaries
* spreadsheet-related architecture guards/tests.

Evaluate specifically:

1. Whether generic spreadsheet semantics remain centralized in IR / contract layers
2. Whether adapter-specific logic is leaking upward
3. Whether parser/renderer responsibilities are cleanly separated
4. Whether current abstractions are stable or accidental
5. Whether XLSX currently acts as de-facto canonical backend
6. Whether ODS parity is structurally sustainable
7. Whether current contract facades are too thin / too broad / correctly sized
8. Whether current guard coverage matches the actually risky seams
9. Whether current module/package structure suggests future split pressure
10. Whether there are hidden semantic seams not yet protected.

Very important constraints:

* Distinguish carefully between:
** real architectural debt
** deliberate pragmatism
** acceptable asymmetry
** temporary migration state.

* Do NOT recommend abstraction for abstraction’s sake.
* Prefer narrow seam hardening over framework expansion.
* Respect ADR-001 pragmatic hexagonal stance.
* Respect ADR-003 IR ownership.
* Respect existing spreadsheet-contract architecture direction.
* Do NOT propose generic backend abstraction unless clearly justified.

Output requirements:

Produce:

* concrete findings
* severity assessment
* risk analysis
* guard-gap analysis
* package split candidacy observations (only if truly justified)
* explicit “do NOT split” observations where appropriate
* candidate future FTRs only where strongly justified.

Strongly avoid:

* speculative framework ideas
* abstract architecture evangelism
* large future visions disconnected from current code.

Expected style:
* critical
* concrete
* architecture-review style
* grounded in actual repository structure and tests.

Suggested output file:

docs/internal_guide/architecture/reviews/review_002_pass3_adapter_boundaries.adoc
