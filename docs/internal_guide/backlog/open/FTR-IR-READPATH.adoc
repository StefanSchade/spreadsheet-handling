* FTR-IR-READPATH — XLSX read via IR
** Implement the mirror of the write pipeline: Excel→IR→Frames(+Meta).
** Acceptance: roundtrip JSON→XLSX→JSON equality (ignoring presentation-only meta).