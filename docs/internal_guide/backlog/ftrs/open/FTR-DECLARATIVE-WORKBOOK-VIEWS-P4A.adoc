===== FTR-DECLARATIVE-WORKBOOK-VIEWS-P4A -- Configure workbook views without consumer plugins

*Status:* Open
*Priority:* P4A
*Area:* Pipeline / workbook view composition

==== Purpose

Make consumer workbook shapes configurable with generic pipeline steps instead
of requiring consumer-local `workbook_views` plugins for ordinary view
composition.

Plugins should remain available for domain-specific extraction, validation, or
generation logic. They should not be required just to select which prepared
frames are visible, order sheets, rename visible sheets, and assemble an
editable workbook view from generic frames.

==== Consumer Trigger

A prototypical consumer workbook currently uses a local `workbook_views` plugin
to create friendly entity, axis, matrix, mapping, legend, and open-question
tabs.

Some of that logic is genuinely consumer-specific naming and presentation
choice, but the shape is not domain-specific: it is a declarative view layer on
top of normalized frames.

==== Composition Principle

Workbook views are consumers of frames, not producers of new data relations.
They compose already-available frames into visible workbook sheets and attach
view, lifecycle, and render metadata. Non-trivial data reshaping -- joins,
pivots, aggregations, and relation-changing transformations -- must happen in
preceding pipeline steps such as `extract_frame`, tabular view operations, or
join views. Those steps produce explicit named frames before workbook-view
materialization.

A workbook view step may perform visibility-near presentation work: sheet
selection, sheet naming, sheet ordering, column subset selection for display,
column label overrides, editability/protection markers, and lifecycle
annotations. It must not perform semantic data joins or row-producing
operations.

==== Goals

* Add one or more generic steps for declaring workbook-visible views.
* Support sheet renaming and output sheet order.
* Build on the existing `select_render_frames()` behavior,
  `_meta.workbook_view`, and `_meta.frame_lifecycle` instead of inventing a
  second render-selection mechanism.
* Allow prepared view frames from `extract_frame`, tabular view operations, or
  existing transforms to be exposed as sheets.
* Preserve frame lifecycle metadata so canonical sources, derived views,
  editable projections, and omitted intermediates remain explicit.
* Keep ODS and XLSX behavior aligned at the adapter contract level.

==== Non-goals

* Do not implement a SQL engine in this slice.
* Do not move consumer-specific generation logic into the core library.
* Do not require the generic view layer to express every possible pandas
  transformation.
* Do not implement column projection, row filtering, sorting, joins, or pivots;
  those belong to frame extraction and tabular view preparation steps.
* Do not remove plugin support.

Parameterized joins, pivots, or richer query-like view definitions can become
later FTRs if repeated consumers need them.

==== Acceptance

* A consumer can replace simple `workbook_views` plugin cases with
  declarative YAML for view selection, sheet naming, and sheet ordering.
* The selected view frames render in the configured order.
* View frames carry lifecycle metadata describing whether they are canonical,
  derived, editable, visible, or omitted.
* The implementation uses or extends the existing `workbook_view` policy and
  `select_render_frames()` path rather than bypassing them.
* Tests cover at least one XLSX and one ODS render/read smoke path for the
  configured workbook views.
* A prototypical consumer workbook can use the generic view path for the parts
  that are not domain-specific.
