= Review: FTR-GRAPH-REFERENCE-VALIDATIONS-P4A
:author: Review Session 2026-05-07

== Verdict: Akzeptiert

Alle Acceptance-Kriterien erfüllt. Sauber begrenzter Scope (nur
`endpoints_exist` und `unique_edges`), Topologie-Checks korrekt auf
FTR-GRAPH-TOPOLOGY-VALIDATIONS-P5 verschoben.

'''

== Commit

* `ca03d86 feat(validation): add graph reference checks`
* 9 Dateien, 726 Insertions, 5 Deletions

'''

== Acceptance-Prüfung

[cols="1,1,3"]
|===
| Kriterium | Status | Evidenz

| Pipeline validates simple node/edge network
| ✓
| `validate_graph` Step registriert, Config-addressable Test bestätigt

| Tests: endpoint existence
| ✓
| `test_validate_graph_reports_missing_edge_endpoints` — source + target missing

| Tests: duplicate edges
| ✓
| `test_validate_graph_reports_duplicate_edges_when_unique_is_enabled`

| Tests: composite edge keys
| ✓
| `test_validate_graph_supports_composite_edge_keys` — (operation_id, slot_id) als Source-Key

| Findings include frame/row/key context
| ✓
| Findings enthalten `frame`, `row_index`, `value`, `target_frame`, `target_columns`, `message` mit Graph/Edge/Node-Namen

| Composes with standard reference validations
| ✓
| Importiert `ReferenceFinding` und `findings_to_frame` aus `reference_validations` — gleiche Finding-Shape, gleicher Frame-Output
|===

'''

== Implementation Quality

=== Positiv

* **Eigenes Modul**: `graph_validations.py` (446 Zeilen) — sauber getrennt von
  `reference_validations.py`, aber importiert deren `ReferenceFinding` +
  `findings_to_frame` für Shape-Kompatibilität
* **Richtige Schnittstelle**: Reused `ReferenceFinding` Dataclass statt eigene
  Graph-Finding-Klasse → gleiche Findings-Frame-Shape, gleiche `FINDING_COLUMNS`
* **Defensive Graph-Config-Validierung**: `_validate_graph_config` prüft
  Node-Referenzen, Column-Existenz, und Key-Arity-Match bevor Checks laufen
* **Flexible Key-Spec**: `key`/`keys` singular/plural Support für Nodes;
  `source_column`/`source_columns` und `target_column`/`target_columns` für Edges
* **Configurable unique_columns**: Default ist `[*source_columns, *target_columns]`,
  kann aber explizit konfiguriert werden für partial-key Uniqueness
* **Selective Checks**: `checks: ["endpoints_exist"]` erlaubt nur Endpoint-Check
  ohne Duplicate-Prüfung (getestet in `test_validate_graph_can_run_selected_checks_only`)
* **Finding-Messages klar**: Graph-Name, Edge-Name, Node-Name, und Endpoint-Role
  (source/target) in jeder Message
* **Scope korrekt begrenzt**: Topology-Checks (cycle, reachability) explizit
  in Non-Goals und auf Phase 5 FTR verwiesen

=== Anmerkungen (kein Blocker)

* [A1] **`unique_columns` Default-Deduplication**: `dict.fromkeys([*source_columns, *target_columns])`
  dedupliziert bei Source==Target Column-Name (gut), aber die Reihenfolge hängt
  von der Einfüge-Reihenfolge ab. Für die Findings-Darstellung kein Problem,
  aber bei ordered Composite-Key-Semantik beachtenswert.

* [A2] **Kein `when`/`enabled_when` Support**: Im Gegensatz zu
  `validate_references` unterstützt `validate_graph` keine Condition-Gating.
  Das ist für den aktuellen Scope richtig (Graph-Validierungen gelten
  typischerweise komplett oder gar nicht), sollte aber bei späterem Bedarf
  bewusst nachgezogen werden.

* [A3] **Empty-Cell-Handling bei Endpoints**: Ein leerer Source/Target-Key wird als
  Missing-Endpoint gemeldet (Zeile 96: `any(_is_empty_cell(value) for value in key)`).
  Das ist strenger als bei `validate_references` FK (wo `allow_empty: true`
  default ist). Für Graph-Edges ist das korrekt — ein Edge ohne Endpoint
  ist per Definition broken.

* [A4] **Performance**: Wie bei `reference_validations` — `iterrows()`-basiert.
  Für Konfigurationsdaten mit typisch <10k Rows kein Problem.

* [A5] **`_VALID_CHECKS` erweiterbar**: Set-basiert, neue Checks können durch
  einfache Erweiterung hinzugefügt werden, ohne die bestehende API zu brechen.
  Gute Vorbereitung für den Topology-FTR.

'''

== Architektur-Einordnung

Die Graph-Validation steht als eigenständiges Modul neben der
Reference-Validation. Die Komposition ist korrekt:

* **Shared Shape**: `ReferenceFinding` + `FINDING_COLUMNS` werden geteilt →
  ein Consumer kann beide Finding-Frames mit dem gleichen Report-Tool lesen
* **Shared Utility-Logik**: `_row_keys`, `_key_token`, `_plain_value`,
  `_is_empty_cell` sind im neuen Modul dupliziert statt importiert. Das ist
  für den aktuellen Scope akzeptabel (unabhängige Module), aber bei Wachstum
  könnte ein shared `_validation_utils.py` sinnvoll werden.
* **Kein FK-Reuse für Endpoints**: Die Endpoint-Prüfung reimplementiert die
  FK-Logik leicht vereinfacht (kein `allow_empty`, kein partial-key skip).
  Das ist bewusst einfacher als ein generischer Delegation-Path.

'''

== Deferred / Follow-up Items

* Topology-Checks (cycle detection, reachability) → FTR-GRAPH-TOPOLOGY-VALIDATIONS-P5
* `when`/`enabled_when` Support → bei Bedarf nachziehen
* Shared validation utility extraction → bei weiterem Wachstum evaluieren

'''

== Fazit

Gut abgegrenzte, minimal-viable Graph-Validation. Der Scope ist bewusst auf
die beiden häufigsten Graph-Checks limitiert (Endpoint-Existenz und
Edge-Uniqueness) — das reicht für Consumer-Konfigurationsmodelle. Die
Finding-Shape ist kompatibel mit der Standard-Reference-Validation, und
Topology-Checks sind sauber auf einen späteren FTR deferred. Kein Blocker.
