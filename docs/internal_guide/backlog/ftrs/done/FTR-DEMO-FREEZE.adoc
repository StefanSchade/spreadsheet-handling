===== FTR-DEMO-FREEZE — Freeze consumer repo and add smoke guard

* *Status* Done
* *Context* Core library is in active refactoring (Phase 3: IR parity). The demo repo should be frozen to avoid redundant sync work.
* *Goal* Freeze spreadsheet-handling-demo with a version pin and add a consumer smoke test in the main repo to detect interface breakage early.
* *Acceptance*
- Demo README includes freeze statement and version pin (`spreadsheet-handling==0.1.0b4`).
- Main repo runs 3 consumer smoke tests: import surface, JSON roundtrip, XLSX roundtrip.
- Failures in the smoke test signal breaking changes in the core interface.

*Delivery scope*

[cols="1,3"]
|===
| Demo README | Freeze notice with version pin and rationale
| Import surface test | `test_consumer_import_surface` — verifies `load_app_config`, `run_pipeline`, `build_steps_from_config`, `orchestrate` resolve
| JSON roundtrip test | `test_consumer_roundtrip_json_to_json` — JSON in → no steps → JSON out, verifies frames
| XLSX roundtrip test | `test_consumer_roundtrip_json_to_xlsx` — JSON in → XLSX out, verifies file exists
| Test file | `tests/integration/test_consumer_smoke.py` — 3 tests, marked `FTR-DEMO-FREEZE`
|===
