== Review: FTR-TEST-SUITE-TOPOLOGY-P3C

Reviewed diff: `diff.txt`.

=== Erledigt

* Die leeren Artefaktdateien wurden entfernt.
* Der `spit`/`split`-Tippfehler ist dadurch erledigt.
* Alte Pfad-Kommentare in verschobenen Testdateien wurden entfernt.
* Die Verschiebung wurde laut Rueckkommentar gegen Collection und aktiven Testlauf geprueft.
* Der Scope-Overlap mit `FTR-ARCHITECTURE-TEST-LAYER-P3C` ist transparent dokumentiert.

=== Gesamturteil

Die Umsetzung geht klar in die richtige Richtung und trifft den Kern des FTR: Die aktive Testsuite ist jetzt physisch nach Verantwortung gegliedert, `tests/architecture/` ist sichtbar, Roundtrip-/Backend-/Pipeline-Tests liegen plausibler unter `integration`, und `tests/README.adoc` gibt eine brauchbare Platzierungsregel.

Ich wuerde den Stand aber nicht ohne kleine Nacharbeiten schliessen. Es gibt ein paar konkrete Hygiene- und Scope-Probleme, die jetzt billig zu korrigieren sind.

=== Positiv

* Die neue Topologie `tests/unit/`, `tests/integration/`, `tests/architecture/` ist grundsaetzlich passend.
* `tests/README.adoc` ist knapp, verstaendlich und trifft die wichtigste Regel: Verantwortung vor Implementierungsnaehe.
* Architektur-/Contract-/Invariant-Tests sind jetzt deutlich auffindbarer.
* Offensichtliche Integrationstests wie Roundtrips, Backend-Pfade und Orchestrator-Flows wurden aus der Unit-Ecke herausgezogen.
* Marker-/Make-Normalisierung wurde bewusst nicht vermischt. Das ist gut; das gehoert ins naechste FTR.
* Der aktive Lauf wurde laut Codex mit `pytest -q --ignore=tests/legacy_pre_hex tests` sowie dem bisherigen `integ`-Slice geprueft.

=== Kritische Punkte vor Abschluss

==== 1. und 2.

betrifft die backlog arbeiten die wir parallel durchfuehren - ignorieren

==== 3. Leere Dateien wirken wie Artefakte

Im Diff erscheinen leere Dateien:

* `tests/integration/pipeline/fixtures/sheets_minimal.yaml`
* `tests/integration/pipeline/fixtures/sheets_spit_join.yaml`
* `tests/integration/pipeline/test_run_split_join_pipeline.py`

`sheets_spit_join.yaml` sieht zusaetzlich nach Tippfehler aus (`spit` statt `split`).

Empfehlung: Entfernen, falls sie nicht aktiv gebraucht werden. Wenn sie absichtlich Platzhalter sind, dann mit minimalem Inhalt und erklaerendem Kommentar versehen. Leere Testdateien sollten nicht committed werden.

==== 4. Einige verschobene Dateien enthalten alte Pfad-Kommentare

Mehrere Tests tragen noch Kommentare wie:

* `# tests/unit/io_backends/test_json_roundtrip.py`
* `# tests/unit/io_backends/test_meta_persistence.py`
* `# tests/unit/io_backends/xlsx/test_ir_readpath.py`

Das widerspricht dem Zweck dieses FTR, weil die Datei selbst noch die alte Einordnung suggeriert.

Empfehlung: Alte Pfad-Kommentare entfernen oder aktualisieren.

==== 5. Der Diff sieht eher nach Kopieren als nach Umziehen aus

Im bereitgestellten Diff sind sehr viele neue Dateien sichtbar, aber keine entsprechenden Loeschungen alter Testdateien. Falls das nur ein Diff-Ausschnitt ist, ist das unkritisch. Falls die alten Dateien weiterhin existieren, entsteht doppelte Testabdeckung und potenziell divergierende Wahrheit.

Empfehlung: Vor Abschluss pruefen:

[source,bash]
----
find tests -name 'test_*.py' | sort
pytest --collect-only -q --ignore=tests/legacy_pre_hex tests
----

Ziel: keine versehentlich doppelt vorhandenen alten/neuen Testdateien.

==== 6. Der Scope greift leicht in das naechste FTR ueber

Die neue Architektur-Teststruktur ist bereits stark befuellt. Das ist sachlich nuetzlich, aber streng genommen teilweise schon `FTR-ARCHITECTURE-TEST-LAYER-P3C`, nicht nur Topologie.

Ich wuerde das nicht zurueckdrehen. Aber im Review sollte klar festgehalten werden: Dieses FTR hat mehr getan als nur Ordner anzulegen; es hat bereits erste Architekturtests sichtbar einsortiert. Das ist akzeptabel, solange das naechste FTR dann nicht dieselbe Arbeit noch einmal behauptet.

=== Kleinere Hinweise

* Einige Testdateien haben noch uneinheitlichen Stil bei Quotes und Formatierung. Kein Blocker fuer dieses FTR.
* Manche Architekturtests sind recht spezifisch auf aktuelle Modulnamen geschnitten. Das ist als Guardrail okay, sollte aber spaeter im Architecture-Test-Layer-FTR bewusst als Current-State vs. Durable-Invariant eingeordnet werden.
* `tests/README.adoc` ist gut, koennte spaeter noch um Marker-/Make-Regeln ergaenzt werden; das gehoert aber ins naechste FTR.

=== Abschlussbewertung

Ich wuerde `FTR-TEST-SUITE-TOPOLOGY-P3C` nach kleinen Nacharbeiten als erfolgreich umgesetzt ansehen.

Blockierend vor Abschluss sind aus meiner Sicht nur:

. deutsche Arbeitsnotizen in `_ftrs_open.adoc` entfernen,
. leere Artefaktdateien klaeren oder loeschen,
. pruefen, dass alte Testdateien nicht zusaetzlich weiterbestehen,
. moegliche fehlende Include-Dateien entweder anlegen oder die Includes zurueckstellen.

Danach ist der Schnitt tragfaehig und eine gute Basis fuer `FTR-ARCHITECTURE-TEST-LAYER-P3C` und `FTR-TEST-MARKERS-AND-MAKE-TARGETS-P3C`.

=== Rueckkommentar / Nachbearbeitung (2026-04-24)

==== Zu 1. und 2.

Wie im Review schon vermerkt: bewusst nicht bearbeitet, da das parallele Backlog-Arbeit im Diff war und nicht Teil dieser Nacharbeit ist.

==== Zu 3. Leere Dateien wirken wie Artefakte

Erledigt.

Die drei unreferenzierten Zero-Byte-Artefakte wurden entfernt:

* `tests/integration/pipeline/fixtures/sheets_minimal.yaml`
* `tests/integration/pipeline/fixtures/sheets_spit_join.yaml`
* `tests/integration/pipeline/test_run_split_join_pipeline.py`

Der vermutete `spit`/`split`-Tippfehler ist damit ebenfalls gegenstandslos, weil die Datei nicht aktiv verwendet wurde.

==== Zu 4. Einige verschobene Dateien enthalten alte Pfad-Kommentare

Erledigt.

Veraltete Altpfad-Kommentare wurden aus den verschobenen aktiven Dateien entfernt:

* `tests/integration/io_backends/json/test_json_roundtrip.py`
* `tests/integration/roundtrip/test_meta_persistence.py`
* `tests/integration/spreadsheet_contract/test_ir_readpath.py`
* `tests/integration/pipeline/test_run_minimal_roundtrip_pipeline.py`

==== Zu 5. Der Diff sieht eher nach Kopieren als nach Umziehen aus

Geprueft.

Ausgefuehrte Checks:

[source,bash]
----
rg --files tests -g 'test_*.py' | sort
pytest -q --collect-only --ignore=tests/legacy_pre_hex tests
pytest -q --ignore=tests/legacy_pre_hex tests
----

Ergebnis:

* Die Collection laeuft erfolgreich durch.
* Die aktive Suite bleibt nach der Nacharbeit gruen.
* Die vormals verschobenen Dateien bestehen nicht zusaetzlich parallel weiter, sondern liegen im Worktree als Loeschungen an den alten Pfaden und als neue Dateien an den Zielpfaden vor.

Der Eindruck eines reinen Kopier-Diffs kam hier durch den bereitgestellten Diff-Ausschnitt zustande; im aktuellen Arbeitsstand ist die Verschiebung konsistent.

==== Zu 6. Der Scope greift leicht in das naechste FTR ueber

Einverstanden, aber bewusst nicht zurueckgedreht.

Das FTR hat faktisch mehr getan als nur Ordner anzulegen: Es hat bereits bestehende Guardrail-/Contract-/Invariant-Tests als erste Architektur-Schicht sichtbar einsortiert. Ich halte das fuer akzeptabel, solange das Folge-FTR `FTR-ARCHITECTURE-TEST-LAYER-P3C` darauf aufsetzt und dann Taxonomie, Abgrenzung `current-state` vs. `durable`, sowie eventuelle Ausbauarbeit behandelt statt dieselbe Sichtbarmachung noch einmal zu reklamieren.
