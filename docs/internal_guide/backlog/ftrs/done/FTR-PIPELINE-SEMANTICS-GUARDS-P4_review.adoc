===== Review: FTR-PIPELINE-SEMANTICS-GUARDS-P4

*Review-Datum:* 2026-05-06
*Reviewer:* Codex (post-hoc review)
*Verdict:* Akzeptiert; keine offenen Findings

'''

====== Findings

Keine offenen funktionalen oder architektonischen Findings fuer den
umgesetzten Delivery-Scope.

'''

====== Positiv bestaetigt

* Dedizierter Guard-Slice vorhanden:
  `tests/architecture/pipeline_semantics/test_pipeline_semantics_guards.py`
* Persistente `meta_contract`-Roots werden gegen `meta_registry.yaml` auf
  `meta_canonical/canonical_meta` erzwungen.
* `current_state_gap`-Eintraege sind strukturiert und reviewbar
  (`reason`, `follow_up_ftr`, `review_condition`).
* `auto`-Semantik bleibt im Guard modelliert als configuration-owned und wird
  fuer nicht-Configuration-Steps abgewehrt.
* Composite-Vertraege werden auf `wrapped_steps`, interne Frames und
  `composite_metadata_policy` geprueft.
* Plugin-Vertrag bleibt explizit erweiterbar (`plugin`-Entry,
  `may_change_frame_set`, Lifecycle-Verantwortung).
* Pipeline-Orchestrierung wird auf Nicht-Routing ueber Payload-Meta-Tokens
  (`_meta`, `frame_lifecycle`, `workbook_view`) geprueft.
* Guard-Inventar dokumentiert die neue Guard-Familie unter
  `tests/architecture/pipeline_semantics/`.

'''

====== Testsignal

In dieser Umgebung ausgefuehrt und gruen:

* `tests/architecture/pipeline_semantics/test_pipeline_semantics_guards.py`
* `tests/architecture/dependency_guards/test_pipeline_step_registry.py`

'''

====== Rest-Risiken / Deferred

Die im FTR dokumentierten spaeteren Guard-Tiers bleiben bewusst
aktivierungsgebunden und sind als solche nachvollziehbar dokumentiert:

* Lifecycle/View-Render-Guards erst nach stabiler Einfuehrung von
  `FTR-FRAME-LIFECYCLE-AND-WORKBOOK-VIEWS-P4`.
* Tiefere Inline-vs-Policy-Verhaltensguards erst im naechsten stabilen
  Helper-Policy-Behavior-Slice.

Das ist im aktuellen Stand konsistent mit den Acceptance-Kriterien
("exist ... or activation condition documented").

'''

====== Fazit

Der FTR ist sauber umgesetzt: Die Guards sind schmal, erklaerbar und auf
dokumentierte Invarianten ausgerichtet, ohne Plugin-Extensibility zu
verhindern oder die JSON-Registry zur Runtime-Authority zu machen.