===== Review: FTR-TEST-NAMING-AND-CONVENTIONS-P3C

*Review-Datum:* 2026-04-30
*Reviewer:* (post-hoc review)
*Verdict:* Umgesetzt — leichtgewichtige Konvention etabliert

'''

====== Zusammenfassung

Das FTR definiert minimale Namens- und Strukturkonventionen für Tests und wurde
als „Done" markiert. Die nachfolgende Überprüfung bestätigt, dass die im
Delivery-Scope genannten Punkte tatsächlich umgesetzt sind.

'''

====== Prüfpunkte

[cols="1,3,1", options="header"]
|===
| # | Kriterium | Status

| 1
| Dateinamen-Renames durchgeführt (`test_validate_columns_xlsx_effect.py`,
  `test_run_select_io_config_unknown_profile.py`,
  `test_carrier_boundary_guards.py`)
| ✓ vorhanden

| 2
| Modul-Docstrings bei Architecture-Tests
  (z.B. `test_spreadsheet_boundary_guards.py`,
  `test_carrier_boundary_guards.py`)
| ✓ vorhanden

| 3
| Modul-Docstring bei Integration-Slices
  (z.B. `test_legend_blocks.py`)
| ✓ vorhanden

| 4
| FTR-Marker `pytest.mark.ftr("FTR-TEST-NAMING-AND-CONVENTIONS-P3C")`
  an bearbeiteten Dateien
| ✓ vorhanden (legend_blocks, carrier_boundary_guards)

| 5
| Testfunktionsnamen beschreiben Verhalten/Invariante
  (Stichprobe Architecture-Tests)
| ✓ konsistent

| 6
| Kein schweres Tooling / Linter eingeführt
| ✓ wie beabsichtigt
|===

'''

====== Offene Beobachtungen (informell, kein Blocker)

* Nicht alle Architecture- und Integration-Tests tragen den FTR-Marker — das
  ist konsistent mit dem Non-Goal „kein flächendeckender kosmetischer Rename".
* Einige ältere Unit-Tests haben noch keine Module-Docstrings; das ist
  explizit im Scope ausgeschlossen (nur Architecture + Integration erwartet).
* Konventionen sind als Wissen im FTR-Dokument hinterlegt, aber es gibt kein
  separates `CONTRIBUTING.md`- oder Developer-Guide-Kapitel, das die Regeln
  dauerhaft referenzierbar macht. Falls gewünscht, könnte das in einem späteren
  Schritt ergänzt werden.

'''

====== Fazit

Das FTR ist inhaltlich abgeschlossen. Der Lieferumfang entspricht dem
definierten Scope: leichtgewichtige Konvention, gezielte Renames, Docstrings
wo nötig, keine übermäßige Churn. Kein weiterer Handlungsbedarf.
