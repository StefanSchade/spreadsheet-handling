== Release History

=== Releasenotes 0.1.0b4 — 2025-10-06

*CI, tests, and packaging*

- Hardened release workflow: full fetch depth for tag-based versioning; test selection aligned.
- Test targets cleaned up (markers/filters); legacy tests excluded by default.
- Package metadata and discovery tuned in pyproject.

*Architecture & CLI*

- Documentation and structure aligned toward a single orchestrator (run) with thin CLI wrappers.
- sheets-run refined: clearer profile/pipeline selection and inline I/O in --pipeline-yaml supported.
- sheets-pack / sheets-unpack: simplified to build config and delegate into the common execution path.

*Examples & docs*

- Added example pipeline.yml and sheets.yaml to demonstrate profile-based runs.

=== Releasenotes 0.1.0b3 — 2025-09-29

*Excel user experience*

- XLSX export sets AutoFilter and applies gray/bold header styling (OpenPyXL post-processing).

*Design groundwork*

- Specified sheets-run with pipelines and I/O backends; introduced profile concept and dotted factories.
- Captured requirement for AutoFilter and outlined cross-tab → xref transform.
- Sketched per-sheet configuration (overrides).
- Described helper columns via FK denormalization.

*Housekeeping*

- Expanded .gitignore (build artifacts, generated _version.py, temp files).
- Hardened Makefile shell flags and targets.

=== Releasenotes 0.1.0b2 — 2025-09-24

*Release setup*

- Initial release automation added, generated _version.py ignored.


=== 0.1.0b2 — 2025-09-24

*Important Notes*

- 0.1.0b1 was tagged but never published to PyPI / functionally it is equivalent to 0.1.0b2.
