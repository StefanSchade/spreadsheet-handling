= `{project-name}` Documentation
:revnumber: {project-version}
:revdate: {build-date}
:revremark: {build-rev}
:toc:
:icons: font

// Header attributes such as `{project-name}`, `{project-version}`,
// `{build-date}`, and `{build-rev}` are supplied by
// `scripts/build_docs_pages.sh` via Asciidoctor CLI `-a` attributes during
// `make docs-pages`. A direct single-file `asciidoctor` invocation without
// those CLI attributes will leave the placeholders literally unresolved in
// the rendered output.

include::../attributes.adoc[]

`spreadsheet-handling` is a beta Python toolkit for spreadsheet-oriented data
workflows. The demo repository remains the best starting point for users, while
this site publishes release-bound framework documentation built from the core
repository.

== Start Here

* link:user-guide/[User Guide] -- current framework-oriented user guide.
* link:release-notes/[Release Notes] -- minimal release trail for published
  core versions.
* link:https://github.com/StefanSchade/spreadsheet-handling-demo[Demo
  repository] -- runnable prototype integration and main user entry point.

== Scope

This first publishing slice contains normal AsciiDoc HTML generated from the
core repository. Demo walkthroughs and presentation decks are intentionally
deferred to a later publishing slice.
