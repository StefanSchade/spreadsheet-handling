= `{project-name}` -- Published Documentation
:revnumber: {project-version}
:revdate: {build-date}
:revremark: {build-rev}
:icons: font

// Source for the root navigation page of the `spreadsheet-handling-pages`
// artifact repository served at {pages-url}.
//
// Header attributes such as `{project-version}`, `{build-date}`,
// `{build-rev}`, `{project-url}`, `{demo-url}`, and `{pages-url}` are
// provided by `scripts/build_docs_pages.sh` via Asciidoctor CLI `-a`
// attributes. Direct single-file `asciidoctor` invocations without those
// attributes will leave the placeholders unresolved.
//
// This page is a stable navigation entry point, not a redirect. PyPI keeps
// old README/long-description rendering online indefinitely, so the bare
// root URL must remain a meaningful landing page that links forward to the
// current state of the documentation and back to the per-release archive.
// It is regenerated on every release tag and only ever links to
// release-bound paths.

include::../attributes.adoc[]

This is the published documentation site for `spreadsheet-handling`. Source
code lives in the link:{project-url}[core repository]; the consumer
walkthrough lives in the link:{demo-url}[demo repository].

== Latest release: `{project-version}`

* link:latest/core/user-guide/[User guide] -- current framework-oriented user guide.
* link:latest/core/release-notes/[Release notes] -- minimal release trail for published core versions.
* link:latest/demo/slides/product_business_slice.html[Demo visual walkthrough] -- the maintained product business slice as a Reveal.js deck.

[NOTE]
====
Installed a different release? Pick the matching version in *Browse by
version* below; per-release documentation is preserved at stable paths so
links from old PyPI release pages keep resolving.
====

== Browse by version

Each published release writes its documentation into a stable
`versions/<tag>/` path. Demo slide decks tagged `v<core-tag>-demo` are merged
into the matching core release row.

[cols="1,4", options="header"]
|===
| Release | Documentation

| `v0.1.0b6` (current)
| link:versions/v0.1.0b6/core/user-guide/[User guide],
  link:versions/v0.1.0b6/core/release-notes/[Release notes],
  link:versions/v0.1.0b6-demo/demo/slides/product_business_slice.html[Demo walkthrough deck]
|===

== Where source-of-truth lives

Developer documentation, the FTR backlog, AI agent instructions, and
architecture material live in the link:{project-url}[core source
repository] and are not published here by design. This site carries only
user-facing artifacts (user guide, release notes, demo walkthrough decks).

== Related resources

* link:{project-url}[`spreadsheet-handling` source repository]
* link:{demo-url}[`spreadsheet-handling-demo` consumer walkthrough]
* link:https://pypi.org/project/spreadsheet-handling/[`spreadsheet-handling` on PyPI]
