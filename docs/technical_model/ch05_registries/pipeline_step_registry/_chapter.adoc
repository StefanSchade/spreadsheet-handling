== Pipeline Step Registry

The pipeline step registry documents the public pipeline step surface.

It mirrors the runtime `REGISTRY` as a manual review artifact.
Architecture tests verify that the documented entries stay aligned with the
runtime registry.

Registry artifact:

* link:pipeline_step_registry.json[pipeline_step_registry.json]
