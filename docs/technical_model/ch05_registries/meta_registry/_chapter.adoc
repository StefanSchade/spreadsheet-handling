== Meta Registry

The meta registry documents known spreadsheet-relevant metadata entries,
their layer, classification, producers, consumers, persistence behavior, and
roundtrip relevance.

Registry artifact (root-level):

* `registries/meta_registry.yaml`

The YAML artifact lives at the repository root under `registries/`; this
chapter is the explanatory documentation around it.

include::01_meta_registry.adoc[leveloffset=+1]
