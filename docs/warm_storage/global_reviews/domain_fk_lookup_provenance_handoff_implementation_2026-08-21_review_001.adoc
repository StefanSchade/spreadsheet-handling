= FK / Lookup Provenance Handoff (H5): Implementation Review 001
:toc:
:toclevels: 3
:sectnums:

Status: final independent implementation review; sole finding closed by direct
verification of correction commit `f7eadc1`.

Review date:: 2026-08-21

Repository:: `/home/stefan/repos/spreadsheet-handling/core` only

Reviewed commit:: `c93355148b5dc345755c4416a779c773a83c776e`
(`refactor(domain): separate FK and Lookup provenance ownership`)

Observed branch state before review:: `main...origin/main [ahead 8]`, clean
worktree. The observed HEAD and subject matched the requested baseline exactly.

== 1. Verdict

Overall implementation verdict:: *ACCEPT*

Slice-closure verdict:: *SLICE COMPLETE*

The runtime handoff is correctly owned, narrow, failure-safe, and compatible for
valid states. Lookup now owns the H5 truthfulness predicate; FK retains its own
cleanup sequencing and copy-on-write publication mechanics; no FK relation or
policy enters Lookup. FIND-001 is resolved coherently.

The original review identified one MINOR architecture-guard defect: the guard
did not inspect imported names and therefore missed the equivalent executable
spelling `from ...enrich_lookup import provenance`. Correction commit `f7eadc1`
closes that bypass without changing runtime source or broadening the rule. See
<<finding-closure>>.

Historical finding count:: 0 BLOCKER, 0 MAJOR, 0 IMPORTANT, 1 MINOR, 0 NOTE.

Open finding count:: *zero*; IMPL-REV-001 is *CLOSED*.

== 2. Review method and evidence

The review followed the required order and read completely:

. `docs/developer_guide/ch06_code_conventions/05_domain_maintainer_contract.adoc`;
. `domain_fk_reference_helper_cycle0_assessment_2026-08-21.adoc`;
. `domain_fk_reference_helper_cycle0_assessment_2026-08-21_review_001.adoc`;
. `domain_fk_lookup_provenance_handoff_implementation_2026-08-21.adoc`;
. the complete diff for commit `c933551`.

The Human Readability Review Guide was also read completely. The review then
inspected the touched runtime and tests directly, traced `drop_helpers` through
metadata detachment and H5 reconciliation, compared the changed function with
its parent-commit implementation, checked the relevant supported-interface
guidance, searched the bounded import graph, and verified the exact commit file
set. Surrounding code was used only where needed to verify the handoff.

An independent focused run covered the changed tests plus the adjacent FK,
Lookup, and derived-cleanup behavior:

[source,text]
----
.venv/bin/python -m pytest -o addopts='' -q \
  tests/unit/domain/transformations/test_derived_provenance_nested_ownership.py \
  tests/unit/domain/transformations/test_enrich_lookup_provenance_reconciliation.py \
  tests/unit/domain/transformations/test_derived_column_policy.py \
  tests/unit/domain/transformations/test_fk_helpers_domain.py \
  tests/unit/domain/transformations/test_enrich_lookup.py \
  tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py
----

Result:: *200 passed in 0.58s*.

The implementation report's larger 294 + 150 + 2847 green runs were not
repeated in full. The focused result, exact diff, and unchanged surrounding
registries/consumers provide no reason to doubt them.

== 3. Commit-scope verification

The commit contains exactly the six reported runtime/test files plus the
implementation report:

* `src/spreadsheet_handling/domain/transformations/enrich_lookup/provenance.py`
  (new);
* `src/spreadsheet_handling/domain/transformations/enrich_lookup/__init__.py`;
* `src/spreadsheet_handling/domain/transformations/fk_helpers/provenance.py`;
* `tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py`;
* `tests/unit/domain/transformations/test_derived_provenance_nested_ownership.py`;
* `tests/unit/domain/transformations/test_enrich_lookup_provenance_reconciliation.py`
  (new);
* `docs/warm_storage/global_reviews/domain_fk_lookup_provenance_handoff_implementation_2026-08-21.adoc`
  (new).

There is no opportunistic runtime or documentation refactor. In particular,
the commit does not change `derived_column_policy.py`, FK formula mode, Trusted
Ingress, schema maintenance, Workbook View, the pipeline or metadata registries,
Core placement, package-wide topology, compatibility parameters, stale
Lookup-output policy, or the Cycle-0/DMC artifacts.

== 4. Ownership separation and H5 handoff

=== 4.1 FK side

`fk_helpers/provenance.py::_clean_helper_provenance` no longer reads
`enrich_lookup.helper_columns` or any other Lookup record field. Its local work
is now reconstructable as:

. identify and remove FK-owned `helper_columns` provenance;
. observe the post-FK frame column labels;
. ask the Lookup facade whether its sibling record remains truthful;
. apply the returned retain/remove decision while owning the existing
  copy-on-write and structural-pruning sequence.

The remaining literal `enrich_lookup` key in FK is necessary routing and
mutation knowledge for the explicit cross-family handoff; it is not residual
interpretation of Lookup's shape. FK passes the record opaquely and supplies
only the post-mutation observation. It passes no relation, target, key, value
field, or policy object.

The delegation test is intentionally architectural: replacing the facade
predicate with one that always returns `False` causes FK to remove a record
whose helper remains present. That establishes that FK does not recompute or
override Lookup's answer.

=== 4.2 Lookup side

`enrich_lookup/provenance.py::reconcile_enrich_lookup_provenance` reads only:

* Lookup's own `record.helper_columns`; and
* `present_columns`, the post-mutation frame observation.

`frame_name` exists only to make validation diagnostics path-specific. The
function contains no FK relation, fallback, target, policy, formula, or cleanup
mode semantics. Its docstring names FK as the motivating H5 caller while also
stating the general owner-local invariant: the function does not know or care
which transformation removed columns. That is useful non-local rationale, not
reverse semantic coupling.

The semantic knowledge was therefore moved to the correct owner rather than
to a neutral mechanism or another cross-cutting module.

=== 4.3 API/facade design

The facade is narrow and coherent:

* `record` is the smallest coherent Lookup-owned value. Scalarizing its one
  currently-used field into an FK-side extraction would restore the ownership
  leak this slice removes.
* `present_columns` is the necessary and sufficient post-mutation observation.
  Passing the whole frame would be broader; passing the removed FK columns
  would make the predicate FK-shaped.
* a Boolean retain decision leaves mutation, copy detachment, sibling
  preservation, and parent pruning with the caller that already owns the
  composite cleanup publication sequence;
* the name states the Lookup provenance invariant rather than advertising a
  generic metadata cleanup protocol. Although the function itself is a
  predicate, the explicit `retained = ...` call site and return annotation make
  that behavior locally clear.

The facade does not become a generic cleanup API. It operates on exactly one
Lookup provenance record and one frame observation.

== 5. FIND-001 malformed-state review

FIND-001 is resolved coherently and safely.

Validation is Lookup-owned. A mapping record whose `helper_columns` is present
but is not a `list` or `tuple` raises `ValueError` with the specific path
`_meta.derived.sheets[<frame>].enrich_lookup.helper_columns`. This matches the
nearby `_validated_enrich_spec` behavior in `derived_column_policy.py` and
removes the old type-dependent string/dict iteration or opaque `TypeError`.

Accepting both lists and tuples is intentional and consistent with that nearby
validator. Lookup's writer emits a list; accepting tuples preserves the former
iterable behavior without accepting arbitrary strings, mappings, or scalars.
Element stringification and empty/missing `helper_columns` behavior remain the
same as before, so weird-but-previously-valid iterable contents are not newly
rejected.

The apparently asymmetric malformed-state rule is acceptable bounded scope:

* malformed field inside a mapping record -> fail clearly;
* non-mapping record itself -> retain/pass through.

The parent implementation evaluated the predicate only for `dict` records, so
a non-mapping record was retained. Review-001 FIND-001 targeted the malformed
`helper_columns` field, not wholesale validation of every shared-carrier shape.
Preserving non-mapping pass-through therefore avoids an unrelated failure-
contract change and does not make H5's supported valid-state invariant
misleading. A later owner-local validation decision could address the outer
record shape, but it is not needed for this slice.

== 6. Copy, mutation, and failure atomicity

The caller-owned `frames` mapping, DataFrames, and nested metadata remain
unmodified.

`drop_helpers` constructs `out`, detaches the top-level metadata mapping, and
constructs replacement DataFrames before reconciliation. Within
`_clean_helper_provenance`, `derived` and `derived_sheets` remain aliases only
until the first mutation; `copy_derived_path()` detaches both before changing a
sheet entry, and the affected sheet entry is then copied before either producer
subkey is removed. Unchanged sibling entries and a retained Lookup record keep
their prior object identities.

On malformed Lookup provenance, reconciliation occurs before the mutation block
for that sheet. If an earlier sheet has already been cleaned in the local
candidate, all mutations are still confined to detached `out`/`meta` state and
no result is returned. The input mappings and frames remain untouched. Thus
failure cannot partially publish output or mutate caller-owned input.

The pre-existing nested-ownership tests continue to protect copy-on-write,
unchanged sibling identity, retained Lookup-record identity, empty-parent
pruning, and repeated cleanup. The new malformed end-to-end test pins the
exception but does not itself snapshot the caller after failure; direct code
tracing establishes the guarantee, and the neighboring copy tests exercise the
same detachment path. This is adequate and is not a finding.

Repeated invocation, partial overlap, full absence, and total container pruning
remain safe and idempotent at the semantic level.

== 7. Valid-state compatibility

[cols="3,1,5",options="header"]
|===
| Case | Result | Evidence

| A. A declared Lookup helper remains
| Pass
| The facade returns `True`; the pre-existing composition test retains the
  record and its identity.

| B. Some declared Lookup helpers remain
| Pass
| The independent Lookup test covers partial overlap and retains provenance.

| C. No declared Lookup helper remains
| Pass
| The facade returns `False`; composition removes the subkey and prunes empty
  parents as before.

| D. FK provenance removal
| Pass
| `helper_columns` removal is unchanged except that the sibling predicate is a
  facade call.

| E. Unrelated sibling provenance
| Pass
| Existing identity assertions remain unchanged and green.

| F. Empty-container pruning
| Pass
| The structural mutation/pruning block is unchanged.

| G. Replay/repeat cleanup
| Pass
| Existing repeat tests and new predicate idempotence test are green.

| H. Registered/YAML/metadata shape
| Pass
| No registry, pipeline binding, writer shape, or persisted metadata file was
  changed. The writer still emits the same list-valued record.

| I. Consumer migration
| Pass
| No supported call or carrier shape changed; no consumer sequencing changes.
|===

For valid writer-produced state, the old and new predicates are equivalent:
both stringify declared list/tuple elements, compare them with visible
post-drop labels, retain on any overlap, and remove on no overlap.

== 8. Dependency direction and import safety

The runtime dependency direction is correct:

* the only new semantic family edge is
  `fk_helpers.provenance -> enrich_lookup` package facade;
* FK does not import `operation`, `policy`, or `provenance` directly;
* Lookup imports neither FK nor `derived_column_policy`;
* the handoff does not route through the derived-column executor;
* package import succeeds and the focused tests expose no import-time cycle.

Importing the Lookup facade loads its existing `operation` export as well as
the new provenance symbol. That is ordinary package-facade behavior here and
introduces no reverse edge or import-time side effect. There is no circular
dependency.

The architecture guard's family-root exemption correctly allows imports among
Lookup's own modules and its global scan correctly checks other transformation
modules. The current internal-prefix set exactly covers the package's three
implementation modules. The new FK import is correctly permitted because its
resolved module is the package facade.

At reviewed implementation commit `c933551`, the guard nevertheless had the
loophole recorded in <<impl-rev-001>>. Correction commit `f7eadc1` closes it;
see <<finding-closure>>.

=== Public versus internal Python surface

Adding the symbol to `enrich_lookup.__all__` creates an importable package
facade for the architectural handoff, but not an unintended supported external
API. `docs/ai_info/interfaces_and_gates.adoc` explicitly says not to treat every
internal module or barrel export as public API and says direct Domain
transformation functions carry no general public-import promise. The top-level
`spreadsheet_handling` package does not re-export this function; the pipeline
registry still targets only `enrich_lookup:enrich_lookup`; no user or YAML
documentation exposes the reconciliation function.

The package's `__all__` is a sufficient local supported-symbol list for the
architectural facade. No additional public-API registry or new guard
infrastructure is justified. The existing AST guard only needs to recognize
all equivalent direct-submodule import spellings.

== 9. Local Human Readability

Verdict:: *improved*.

Before, a reader in FK provenance cleanup encountered
`enrich.get("helper_columns")`, had to infer that this was a Lookup-owned shape,
and had to reconstruct from tests or history why an FK-private function decided
the truthfulness of a sibling producer's record.

After, the FK sequence is top-down and semantically explicit: remove FK-owned
provenance, observe post-removal columns, invoke the named Lookup reconciliation
boundary, then publish copied metadata. The call and ownership comment explain
the non-local reason without requiring the reader to open Lookup internals.

Within Lookup, the predicate, malformed-shape rule, and diagnostic path now sit
together in a short owner-coherent module. Opening that module is needed only
when changing or auditing Lookup's invariant, not to understand FK's algorithm.
The added helper hop therefore represents a real semantic boundary and removes
cross-owner reconstruction rather than fragmenting a coherent operation.

No formerly coherent sequence was unnecessarily split. The comments state
ownership, inputs, failure rationale, and compatibility boundaries rather than
restating syntax. The optional H5 historical label is accompanied by a complete
current-semantic explanation and is not required to understand the code.

== 10. Test-quality review

The new Lookup test module exercises the owner-local invariant independently:
presence, partial overlap, total absence, empty/missing declaration, repetition,
non-mapping pass-through, multiple malformed shapes, and frame-specific error
paths. These tests assert semantic retain/remove and failure behavior rather
than private helper structure.

The FK delegation test is deliberately a boundary test. Its monkeypatch is not
mere implementation pinning: it proves FK accepts Lookup's decision even when
that decision differs from the old inline presence answer. FK cleanup remains
covered independently by the pre-existing nested-ownership tests, and the
composition path remains covered end-to-end.

All pre-existing assertions were preserved; the commit only added tests. Copy
identity, sibling preservation, pruning, overlap, repetition, and malformed
failure are proportionately protected.

The original architecture test was meaningful for the import forms it
recognized and correctly included all current Lookup internal modules, but its
parser omission left FIND-003 incomplete. Correction commit `f7eadc1` adds the
missing `from <family> import <internal_module>` resolution and focused
regression coverage; independent closure verification is recorded in
<<finding-closure>>.

== 11. Deferred FIND-002

`derived_column_policy.py` is unchanged. The implementation report explicitly
names its larger independent Lookup-shape interpretation and states that this
slice does not solve it. The report therefore does not overclaim broader
ownership cleanup.

The new facade is not made unusably FK-specific for later evidence: its signature
contains only a Lookup record, diagnostic frame name, and post-mutation column
observation, and its implementation says it does not care who performed the
removal. A later bounded cycle may reuse it, compose with other owner-local
functions, or choose a different Lookup boundary. No decision is forced here.

== 12. Implementation-report accuracy

The implementation report is accurate about commit scope, runtime ownership,
FIND-001, copy behavior, compatibility, public/YAML/metadata stability, and
deferred FIND-002.

At implementation commit `c933551`, its FIND-003 statement was too strong:
lines 93-94 said the guard failed if *any* cross-family caller reached past the
facade, while the equivalent `from ...enrich_lookup import provenance` form
succeeded without being reported. Correction commit `f7eadc1` makes that
statement accurate without requiring a separate implementation-report rewrite.

== 13. Finding

[[impl-rev-001]]
=== IMPL-REV-001 -- MINOR -- Import guard misses `from <family> import <internal_module>`

Affected files/symbols::

* `tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py::_resolved_imports`;
* `GUARDED_INTERNAL_PREFIXES` consumption in
  `test_transformations_do_not_import_other_families_internal_modules`;
* the FIND-003 claim in
  `domain_fk_lookup_provenance_handoff_implementation_2026-08-21.adoc`.

Evidence:: For `ast.ImportFrom`, `_resolved_imports` appends only
`resolve_name(node.module, package_context)` and ignores `node.names`. For the
executable statement
`from spreadsheet_handling.domain.transformations.enrich_lookup import provenance`,
the guard therefore records only
`spreadsheet_handling.domain.transformations.enrich_lookup`, which is not one of
the three internal prefixes. An independent probe printed that package root and
`detected=False`; importing the statement then produced the actual module
`spreadsheet_handling.domain.transformations.enrich_lookup.provenance`.

Concrete failure mode:: A future transformation-family caller can import
`operation`, `policy`, or `provenance` through the package-plus-imported-name
spelling and couple directly to that internal module while the architecture
test remains green. The narrow facade may then widen silently, defeating the
specific FIND-003 protection this slice claims.

Smallest correction:: Extend `ImportFrom` resolution to consider the qualified
candidate formed by the resolved package plus each imported name when that
candidate matches a guarded internal prefix. Add a focused parser/guard
regression for at least
`from spreadsheet_handling.domain.transformations.enrich_lookup import provenance`
(and preferably parameterize the three guarded Lookup module names). Preserve
the current exemption for within-family imports and the permitted FK import of
`reconcile_enrich_lookup_provenance` from the package facade.

Blocks slice closure:: *Originally yes; now resolved.* This was not a runtime
ownership or compatibility defect, but FIND-003 was an explicit required
correction for the authorized first slice. Correction commit `f7eadc1` closes
it; see <<finding-closure>>.

== 14. Required conclusions

=== A. Ownership

*Yes.* Lookup provenance semantics at H5 are genuinely Lookup-owned.

=== B. FK cleanup

*Yes.* FK cleanup contains FK-owned provenance semantics plus the explicit
cross-family facade call and the caller-owned mutation/pruning mechanics. It no
longer interprets Lookup fields.

=== C. Malformed state

*Yes.* FIND-001 is resolved coherently and safely. Mapping internals fail
clearly before publication; non-mapping pass-through deliberately preserves
the bounded compatibility baseline.

=== D. Import boundary

*Yes.* The runtime edge uses the facade correctly, dependency direction is
sound, and correction commit `f7eadc1` closes FIND-003's equivalent-import
loophole while preserving facade and within-family imports.

=== E. Behavior

*Yes.* Valid-state behavior, registered/YAML surfaces, metadata shapes, replay,
pruning, and consumer sequencing are preserved.

=== F. Failure/copy

*Yes.* Failure atomicity and caller ownership are preserved; sibling identity
is maintained where the existing contract requires it.

=== G. Readability

*Improved.* Bounded reconstruction cost is lower on both sides of the explicit
ownership seam.

=== H. Public surface

*No unintended external API was created.* The new `__all__` entry is an
internal architectural package facade, not a documented or registered product
surface.

=== I. Scope

*Yes.* The implementation stayed within the accepted slice; deferred FIND-002
remains explicit and untouched.

=== J. Slice closure

*Yes.* The first hardening slice is complete. The single MINOR guard correction
in <<impl-rev-001>> is verified and closed; no further correction is required
for this slice.

== 15. Closure prerequisite disposition

The former prerequisite was to correct and regression-test the `ImportFrom`
alias-module loophole in the existing cross-family guard. Commit `f7eadc1`
does exactly that and changes no runtime source. The prerequisite is satisfied;
deferred FIND-002 remains outside this completed slice.

[[finding-closure]]
== 16. Finding Closure

=== IMPL-REV-001 -- CLOSED

Correction reviewed:: `f7eadc12589ef5d5f9b8c97dd81b968e7ea61dbc`
(`test(architecture): close Lookup import guard bypass`).

Scope verification:: The correction changes only
`tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py`.
No runtime source, package facade, registry, metadata shape, or architectural
prefix set changed.

Direct verification:: `_resolved_imports` now emits both the resolved
`ImportFrom` module and `<resolved_module>.<alias.name>` for every imported
name. It uses `alias.name`, not the local `as` name, so bare and aliased imports
of `operation`, `policy`, and `provenance` all resolve to the existing guarded
internal paths. Existing `import ...enrich_lookup.<internal>` and
`from ...enrich_lookup.<internal> import ...` forms remain detected. The
legitimate facade symbol resolves to
`enrich_lookup.reconcile_enrich_lookup_provenance`, which is not an internal
prefix and remains allowed. The existing importer-family exemption is
unchanged, so imports from within `enrich_lookup` remain allowed.

Rule-scope verification:: The correction completes parsing for the already
established rule -- other transformation families must use a guarded family's
facade -- without adding a family, internal prefix, caller category, or new
restriction. Current facade exports do not collide with guarded module names.
The architectural rule is therefore not broadened beyond FIND-003.

Independent tests::

[source,text]
----
.venv/bin/python -m pytest -o addopts='' -q \
  tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py
# 6 passed in 0.09s
----

An additional independent 23-case probe passed. It covered all three Lookup
internals in bare, aliased, direct `import`, and direct `from <internal> import`
forms; aliased and unaliased facade imports; and within-family imports of all
three internal modules.

Finding closure verdict:: *CLOSED*.

Final implementation verdict:: *ACCEPT*.

Final slice-closure verdict:: *SLICE COMPLETE*.

The first bounded FK/Lookup provenance-handoff hardening slice under the
canonical Domain Maintainer Contract is complete.
