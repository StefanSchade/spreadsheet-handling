= Agent Workflow Coordinator -- Slice 4A Dispatch Marker Lifecycle Correction Evidence

Status:: *S4A-R8/R9 CORRECTED; BOUNDED RE-REVIEW PENDING.*
Date:: 2026-08-30
Review baseline:: `69bd46e2`.
Correction implementation:: `4aa0859c` -- `fix(workflow): correct slice 4a dispatch lifecycle`.

== Dispositions

* S4A-R8 -- CLOSED. The bounded real driver handles exceptions by factual `adapter.outcome` state. When it is `None`, no process outcome exists, `dispatch.json` is removed and the local failure is re-raised without charging a Hop. If an outcome exists, non-validation exceptions retain the marker and fail closed.
* S4A-R9 -- CLOSED. On successful vertical reduction, the driver atomically writes the resulting `run.json`, then `checkpoint.json`, and only then removes `dispatch.json`.

== Three-state deterministic lifecycle

* Pre-spawn local prompt-component failure through `run_real_one_hop()`: fake executable count remains zero, no Git mutation occurs, `hop_used=0`, and `dispatch.json` is absent.
* Post-spawn nonzero/no-result uncertainty: exactly one fake execution, `hop_used=1`, `reconcile_required`, and `dispatch.json` remains as the trusted unresolved anchor.
* Successful fake-executable reduction: expected committed Git mutation is observed, completed Run and checkpoint are persisted, and `dispatch.json` is absent.

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator
  passed
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator
  All checks passed
git diff --check
  exit 0
----

REAL CODEX/PROVIDER INVOCATIONS PERFORMED: 0

No replacement disposable proof, routine real-repository use, Slice 5, or autonomous multi-Hop work is authorized by this correction.
