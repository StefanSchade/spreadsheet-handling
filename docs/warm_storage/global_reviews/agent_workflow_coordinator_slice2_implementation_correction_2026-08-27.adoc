= Agent Workflow Coordinator -- Slice 2 Bounded Correction Evidence

Status:: *IMPLEMENTED; NOT INDEPENDENTLY ACCEPTED*
Date:: 2026-08-27
Authority:: maintainer-approved bounded correction following independent review
`82f18d4`

== Maintainer allocation decision

S2-R1 is resolved by formally allocating WFC-GIT-08 implementation proof to
Slice 3. Slice 2 has no invocation, prompt, or adapter boundary and therefore
does not create workflow-produced commit subjects. The FTR now requires
WFC-GIT-01..07 in Slice 2 and requires WFC-GIT-08 in Slice 3's N=1 fake-adapter
vertical proof. WFC-GIT-08 itself remains unchanged: WorkItem/Hop identity must
project into human-readable `git log --oneline` subjects and the coordinator
validates it as a postcondition, without a Git hook or mandatory trailers.

== Finding dispositions

* S2-R1 IMPORTANT: CLOSED. The accepted FTR and original Slice-2 evidence now
  state the bounded allocation precisely; no subject-validation implementation
  was introduced.
* S2-R2 MINOR: CLOSED. Evidence now says WFC-EVID-01/02/04 are verified and
  WFC-EVID-03's trusted mechanism is proven, while profile-selected category
  population belongs to Slice 3. Mandatory Git facts remain direct Git facts.
* S2-R3 MINOR: CLOSED. `observe_hop()` first uses `git cat-file -e
  <base>^{commit}`. Unknown and blob anchors yield `unresolved base anchor`;
  valid non-ancestor history still yields `divergent or rewritten ancestry`.
* S2-R4 MINOR: CLOSED. The authoritative ignore source must be tracked and
  clean relative to `HEAD`; staged and unstaged edits fail closed before any
  state-root write.
* S2-R5 MINOR: CLOSED. The duplicate package string literal was removed.

== Validation and boundaries

All workflow-coordinator unit tests and A--J replays pass (74 tests). Ruff,
compileall, and `git diff --check` pass. The correction adds no Slice-3 prompt,
adapter, commit-subject, or autonomous-loop implementation; no product-runtime
`src/spreadsheet_handling/` code or DMC Family Cycle 2 work changed.

Slice 2 remains *NOT independently accepted*. Independent bounded re-review is
the next lifecycle step.
