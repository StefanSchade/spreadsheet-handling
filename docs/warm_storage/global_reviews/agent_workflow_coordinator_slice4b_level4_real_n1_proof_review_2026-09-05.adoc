= Agent Workflow Coordinator -- Slice-4B Level-4 Real N=1 Happy-Path Proof Review
Bounded empirical closure review, 2026-09-05
:toc: macro
:toclevels: 2

toc::[]

== 1. Verdict

*VERDICT: A -- ACCEPT LEVEL-4 / SLICE-4B N=1 HAPPY-PATH PROOF.*

The single consumed real-provider proof under
`/home/stefan/wfc-proofs/slice4b-level4-20260905-01` establishes the intended
end-to-end Slice-4B property of section 11.4.8 with no blocking or important
contradiction. A real Codex invocation performed the semantic worktree mutation
of `PROOF.md`, returned a non-null agent-owned `commit_intent` with empty
`claimed_commits`, and the authoritative post-base commit was produced by the
mechanical Coordinator identity and re-observed factually from Git. Every
recorded Git hash, range, subject, and path was independently reproduced from
the disposable repository rather than trusted from `proof-summary.json`.

This is an empirical closure review of the current Slice-4B happy path only. It
is not a new architecture review, not a repeat trusted-Git security review, not
a Slice-5 review, and authorizes nothing beyond recording that this one N=1 path
completed as required. Carried residuals `S4B-R3`, `S4B-R4`, the `S4B-R5`
compatibility residual, `L4-R1`, and `L4-R2` remain as previously dispositioned;
`S4B-R1`/`S4B-R2` remain CLOSED; the `S4B-R5` provider-conformance sub-obligation
remains SATISFIED. No new findings are raised.

== 2. Proof authority and consumption

The core repository baseline is exactly the authorized commit
`f829c1d2 docs(workflow): authorize slice 4b level 4 proof`; work performed on
`main`, no branch. The proof root matches the section 11.4.8 fixed root exactly.

One-shot consumption is corroborated across independent carriers:

* `proof-summary.json`: `hop_used = 1`, `max_autonomous_hops = 1`,
  `process_returncode = 0`, `timed_out = false`, `invocation_id =
  INV-3ade0182cef14452acab2381e4343d9e`, `run_id =
  RUN-3ef67c90cbf048dc8b9e6163e708aff7`.
* `run.json`: `hop_limit = 1`, `hop_used = 1`, exactly one Hop `H001`.
* `checkpoint.json`: `budget.used = 1`, `budget.remaining = 0`,
  `next_permitted_route_keys = []`.
* Disposable-repo reflog (`.git/logs/HEAD`): exactly two events -- the initial
  baseline commit and the single `H001` mark-fixture commit. No third event, no
  amend, no retry ref.
* `launch-provenance.json`: bounded host/operator launch context (python under
  bash under `mosh-server`), self-described as "not cryptographic attestation";
  `environment_presence.codex_related = false` reflects launch-time env capture
  only and is not treated as evidence about provider execution.

Identities are consistent across all carriers: `WorkItem = WFC-S4-PROOF`, `Hop =
H001`, `run_id`/`invocation_id` as above. No evidence of a second provider
attempt exists.

== 3. Real provider / result evidence

The persisted provider transcript (`proof-summary.json.stdout_tail`) shows a real
Codex thread with `thread.started` / `turn.started` / `item.completed` /
`turn.completed` events. The agent's visible command transcript consists of
`sed -n '1,80p' PROOF.md && git status --short`, a `file_change` update to
`PROOF.md`, and `git diff --check` / `git diff` / `git status`. The transcript
contains *no* `git commit`, `git add`, or `.git`-writing command. Final result
carrier (`result-message.json`) fields:

* `outcome = completed`, `requested_route = complete`, `requires_human = false`,
  `scope_changed = false`, `escalation = null`, `findings = []`.
* `schema_version = 1`; correlation `run_id` / `hop_id` / `invocation_id` all
  match `run.json` and `proof-summary.json`.

`process_returncode = 0`, `timed_out = false`, `stop_reason = null`.

== 4. CommitIntent vs claimed_commits (core Slice-4B distinction)

The durable result carrier records:

* `claimed_commits = []` -- the agent claims no authoritative commit hash.
* `commit_intent = { paths: ["PROOF.md"], subject: "docs(workflow): WFC-S4-PROOF
  H001 mark fixture" }` -- non-null, exact path set, exact required subject.

The `structured-result.schema.json` for this proof requires `commit_intent` as a
strict nullable object (`anyOf: [null, {paths[], subject}]` with
`additionalProperties: false` and both fields required), and lists
`commit_intent` and `claimed_commits` as separately required keys. The two are
therefore held semantically distinct by the schema, and the agent supplied
semantic intent without an authoritative hash -- exactly the intended contract.

*Agent-owned direct commit required for success: NO.* Evidence: empty
`claimed_commits`; non-null `commit_intent`; agent transcript containing no
commit command; and the authoritative commit produced under the mechanical
identity (section 5). This demonstrates that the historical Slice-4A happy-path
dependency on agent `.git` write access is removed from this N=1 path. Per the
prompt and section 11.4.8, this does *not* claim the agent was cryptographically
incapable of every possible `.git` write; that stronger claim is neither made nor
required.

== 5. Actual Git reality (independently reproduced)

Reproduced directly against `…/repo/.git` (not copied from the summary):

* `HEAD = 0e872eda8508334467096c44b8ab3bcf7f28642a`.
* Baseline `833eccfadb2a6df71b428ceee53bbdfd12ae5770` exists
  (`git cat-file -t` = commit) and is an ancestor of HEAD
  (`git merge-base --is-ancestor` rc=0).
* `git rev-list --count baseline..HEAD = 1`; the single hash equals the final
  HEAD.
* `git rev-list --parents -n1 HEAD` shows one parent (`…833eccf…`): non-merge.
* Subject exactly `docs(workflow): WFC-S4-PROOF H001 mark fixture`.
* `git diff --name-only baseline HEAD = PROOF.md` (sole changed path).
* `PROOF.md`: baseline `WFC-S4-PROOF: pending marker` -> HEAD
  `WFC-S4-PROOF: completed marker`.
* `git status --porcelain` empty: clean worktree/index; no unrelated tracked or
  untracked mutation.
* Commit author and committer both `Slice 4 proof <proof@example.invalid>` --
  the mechanical Coordinator identity from repo config, not an agent identity.
* One ref only (`refs/heads/master`); no remotes; no `*.lock` residue under
  `.git`.

All observed Git facts match the recorded hashes in `proof-summary.json`,
`run.json`, and `checkpoint.json`. The authoritative hash was taken from Git, and
the model result correctly did not carry it (`claimed_commits = []`).

== 6. Durable Run / Checkpoint reconstruction

`run.json`: `status = completed`, `stop_reason = null`, `anomalies = []`,
`findings = []`, `baseline_head = 833eccf…`, `current_head = 0e872ed…`,
`hop_limit = 1`, `hop_used = 1`. Exactly one Hop:

* `H001`, `outcome = completed`, `applied_route = complete`, `base_head =
  baseline`, `end_head = 0e872ed…`, `actual_commits = ["0e872ed…"]`,
  `stop_reason = null`, `role = worker`.

The Hop `attribution = "agent"` denotes semantic authorship of the change; it is
not a claim that the agent created the Git commit. `actual_commits` is populated
from factual Git re-observation, consistent with the reproduced range.

`checkpoint.json` projects the Run consistently: `budget.used = 1`,
`budget.remaining = 0`, heads matching `run.json` and Git, `status = completed`,
`anomalies = []`, `findings = []`, `human_question = null`, `stop_reason = null`,
`next_permitted_route_keys = []`, and Hop `H001` `range = 833eccf…..0e872ed…`
equal to the actual Git range. An embedded `git_version` evidence entry records a
passing `git version 2.47.3` provider check. Semantic consistency holds; no
byte-for-byte duplication is required.

== 7. Checkout association and provenance

`checkout-association.json`: `checkout_id =
6bafecf6-e080-4024-9751-06ecf0cb95fe`, `canonical_path =
…/slice4b-level4-20260905-01/repo`, `git_dir`/`common_dir` = that repo's `.git`.
The `checkout_id` matches the durable state directory
`trusted-state/checkouts/6bafecf6-…/` and the `checkout_id` in
`proof-summary.json`. The persisted association therefore points at exactly the
disposable proof repository exercised by the production composition path, giving
useful confirming evidence that the registered checkout association is exercised
in the production happy path. This does not resolve the broader low-level API
footgun behind `S4B-R3`, which remains WATCH.

== 8. Residual dispositions

* `S4B-R1` -- *CLOSED.* No proof-only bypass or alternate Git path is evident;
  the production trusted-commit path was used. Not re-reviewed here.
* `S4B-R2` -- *CLOSED.* As above.
* `S4B-R3` -- *WATCH.* Confirmed exercised in the happy path, but the optional
  low-level API validator footgun is not eliminated by a single success.
* `S4B-R4` -- *WATCH.* Not contradicted by this proof.
* `S4B-R5` provider-conformance sub-obligation -- *SATISFIED.* This live proof
  again exercises the current required-nullable `commit_intent` schema.
* `S4B-R5` old-artifact / schema-version compatibility residual -- *WATCH.*
  `schema_version` remains `1`; no migration framework; unchanged.
* `L4-R1` -- *WATCH.* This success path did not exercise the
  mechanical-preemption ledger-ordering case; not closed.
* `L4-R2` -- *MINOR.* Intended and committed path sets coincide here
  (`["PROOF.md"]`); coincidence does not close the finding.

No new findings. No `L4B-R*` finding is manufactured for documented carried
residuals.

== 9. Token-efficiency observation

The persisted `turn.completed` usage is `input_tokens = 60700`,
`cached_input_tokens = 43264`, `cache_write_input_tokens = 0`, `output_tokens =
690`, `reasoning_output_tokens = 62`, matching the figures cited in the review
authority. Input size is large relative to a one-line fixture edit. Recorded as a
token-efficiency *WATCH* / empirical observation only -- not an acceptance
blocker, and not investigated or optimized in this review.

== 10. Scope of what is proven

Proven for this N=1 path: real agent -> semantic worktree mutation -> non-null
agent-owned `CommitIntent` -> no agent-owned commit requirement ->
Coordinator-owned trusted mechanical Git commit -> factual Git re-observation ->
durable Run/Checkpoint state -> completed terminal state, preserving the intended
authority boundary.

Explicitly *not* established by this proof: routine valuable-repository
execution; multi-Hop; Slice 5; DMC dogfooding; machine-readable MaintainerGrant;
generic provider portability; Coordinator-owned outer bubblewrap/harness
isolation; OS-level isolation or provider internals; arbitrary Git-oriented agent
work; and old result-message schema-migration compatibility.

== 11. Readiness

The consumed one-shot authorization is correctly spent, the happy path is
proven, and the artifacts are sufficient to reconstruct execution. The proof is
ready for maintainer proof disposition. Whether to authorize routine N=1 / DMC
dogfooding is a separate maintainer lifecycle decision; this review authorizes
routine repository use, Slice 5, and DMC dogfooding: *none*. No implementation,
tests, FTR, or Project Memory were modified.

=== Validation commands

[source,text]
----
# core baseline / cleanliness
git -C core log --oneline -3
git -C core status --porcelain

# independent Git reproduction in the disposable repo
GIT_DIR=…/repo/.git GIT_WORK_TREE=…/repo git rev-parse HEAD
… git merge-base --is-ancestor 833eccf… HEAD; echo $?
… git rev-list --count 833eccf…..HEAD
… git rev-list --parents -n1 HEAD
… git log -1 --format=%s HEAD
… git diff --name-only 833eccf… HEAD
… git status --porcelain
… git show HEAD:PROOF.md ; git show 833eccf…:PROOF.md
cat …/repo/.git/logs/HEAD

# durable carriers
jq . …/proof-summary.json …/launch-provenance.json \
   …/trusted-state/checkout-association.json \
   …/trusted-state/checkouts/6bafecf6-…/{result-message,run,checkpoint,structured-result.schema}.json

# artifact render check
git -C core diff --check
----

No provider was invoked during this review (real Codex/provider invocations: 0).
