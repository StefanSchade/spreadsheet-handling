= Agent Workflow Coordinator -- Slice 4A Disposable Proof Evidence

Status:: *SLICE 4A IMPLEMENTED / DISPOSABLE PROOF EXECUTED; INDEPENDENT REVIEW PENDING.*
Date:: 2026-08-30
Core implementation:: `60c6bf9f` -- `feat(workflow): implement slice 4 real adapter`.
Authorization baseline:: `fbfc00e8`.

This records one attempted disposable real-agent proof. It does not accept Slice 4 or authorize routine use, Slice 5, or autonomous multi-Hop work.

== Deterministic validation before the live boundary

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator
  85 passed
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator
  All checks passed
git diff --check
  exit 0
----

The focused suite covers external checkout registration and fail-closed renamed checkout validation, state-root exclusion, and local process-group timeout.

== Preflight facts

* Core HEAD: `60c6bf9f3ed347cf9c613eeea25ae333c75263e7`.
* Codex: `codex-cli 0.147.0`; top-level approval syntax parsed and login status reported a logged-in ChatGPT session.
* Disposable no-remote checkout safe identifier: `wfc-s4-proof.UBAOCk`. Initial/pre-invocation HEAD: `b1fdd4fdae4c6531003d478c6cdab9454e43a2c8`. It was clean and contained exactly tracked `PROOF.md` in its initial commit.
* External trusted state safe identifier: `wfc-s4-state.NlGwY4`, not supplied as an additional writable directory. Checkout UUID: `0614ecab-d1b8-4205-afc2-d32a7f0e4ee9`; canonical path and Git-dir/common-dir facts were recorded and validated.
* `work_item_id=WFC-S4-PROOF`, `hop_id=H001`, and `max_autonomous_hops=1`; no prior live invocation existed.

The command used top-level `--ask-for-approval never`, then `exec --ephemeral --json --color never --sandbox workspace-write --model gpt-5.6-terra -c model_reasoning_effort=medium --cd <checkout> --output-schema <external path> --output-last-message <external path> -`. Prompt input used stdin; it was started in a new session and deadline handling targets the process group.

== Invocation and observation

Exactly one Codex process was started. It exited with status `1`, without a timeout and without a structured result. Its bounded stderr contained:

[source,text]
----
Error: failed to initialize in-process app-server client: Read-only file system (os error 30)
----

Acceptance was not positively established and could not be disproved from the supported CLI contract after process start. The Hop is therefore conservatively charged as `uncertain`; no retry occurred.

* Final HEAD was unchanged at `b1fdd4fdae4c6531003d478c6cdab9454e43a2c8`; the expected one-commit range was empty and changed paths were empty.
* The expected agent commit did not exist; expected subject was `docs(workflow): WFC-S4-PROOF H001 mark fixture`.
* `PROOF.md` remains `WFC-S4-PROOF: pending marker`. The worktree was clean, the repository had no remote, and the initial one-commit history remained intact.
* The external result transport file was empty, so structured-result validation was not attempted.
* Dispatch marker and Run snapshot were external. Conservative recovery charged H001 and checkpointed `status=reconcile_required`, `hop_used=1`, and `stop_reason=reconcile_requires_explicit_resume`.
* The broad `.git/**/*.lock` scan found no locks. No lock was removed.
* No unexpected Git mutation was observed.

No raw JSONL transcript is retained. External state contains bounded diagnostic tails and compact machine state only. No second invocation was performed.

== Scope conclusion

This is an observed failed/uncertain N=1 proof attempt, not a successful proof commit. Slice 4 maintainer acceptance, routine real-repository use, Slice 5, and autonomous multi-Hop work remain unauthorized pending independent review.
