= Agent Workflow Coordinator -- Slice 4A Correction Evidence

Status:: *CORRECTION IMPLEMENTED; BOUNDED RE-REVIEW PENDING.*
Date:: 2026-08-30
Review baseline:: `5b09ea01`.
Correction implementation:: `ecc133ca` -- `fix(workflow): correct slice 4a review findings`.

No Codex CLI, provider, or model invocation was performed for this correction.

== Finding dispositions

* S4A-R1 -- CLOSED. `_require_external_state_root()` resolves paths and rejects equality, a state root at/below the checkout, and a checkout at/below the state root. It runs before every association write, at validation entry, and per Hop. Tests confirm rejected registration leaves neither mapping nor checkout directory.
* S4A-R2 -- CLOSED. `invoke_codex()` uses the accepted `adapter.validated_result()` with expected `Invocation`; only a valid identity-correlated `structured_result_v1` establishes the result path. Missing, malformed, or uncorrelated transport stays post-spawn `uncertain`.
* S4A-R3 -- CLOSED. `run_real_one_hop()` plugs the concrete process boundary into `vertical.run_one_hop()`, reusing strict validation, Git observation, WFC-GIT-08/durable checks, evidence, `record_hop`, reduction, and checkpoint projection.
* S4A-R4 -- CLOSED. Deterministic tests cover containment, malformed associations, linked-worktree Git facts, malformed/non-correlated results, nonzero/no-result uncertainty, command construction, fake-executable vertical composition, and descendant timeout termination.
* S4A-R5 -- CLOSED. `test_slice4.py` now has the required FTR module marker.
* S4A-R6 -- CLOSED. The intentionally broad transport schema is unchanged; the existing strict `validated_result()` authority now gates semantic/correlation acceptance, proven by malformed and uncorrelated transport tests.
* S4A-R7 -- CLOSED. The shared containment helper rejects both nesting directions without extra sandbox or filesystem machinery.

== Deterministic validation

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator
  passed
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator
  All checks passed
git diff --check
  exit 0
----

The fake executable is a local test fixture only. It receives the reviewed argv, records it under external state, locates `--cd` and `--output-last-message`, makes the fixture commit, writes a valid correlated envelope, and exits successfully. The test verifies the existing vertical end state and checkpoint; it does not manually compose a second workflow path.

REAL CODEX INVOCATIONS PERFORMED: 0

The previously consumed disposable proof remains unchanged. This correction does not authorize a replacement proof, routine real-repository use, Slice 5, or autonomous multi-Hop operation.
