= FK / Reference-Helper Family: F-002 Implementation-Readiness / Design
:toc:
:toclevels: 3
:sectnums:

Status: bounded implementation-readiness / design step. Non-binding
architecture/evidence-based design. Authorizes no source, test, registry,
model, Trusted-Ingress, consumer, or topology change. Independent review of
this artifact is required before any source change begins.

Date: 2026-08-24.

Repository baseline: `51d9cb5` (`docs(domain): review FK helper cycle 1
re-entry`), branch `main`, worktree clean before this task,
`main...origin/main [ahead 25]`.

*Correction pass (2026-08-24, same day).* This design record has been
updated in place to apply F-CORR-2 through F-CORR-6 from the independent
readiness review (`c7f205c`,
`domain_fk_reference_helper_f002_implementation_readiness_2026-08-24_review_001.adoc`).
F-CORR-1 was resolved directly by Slice 1's implementation and is not
reopened here. Slice 1 is CLOSED and ACCEPTED (`6f15026` implementation,
`9b0c978` independent review, verdict ACCEPT WITH MINOR CORRECTIONS); its
accepted seam -- `InterpretedEnrichLookupProvenance` /
`interpret_written_enrich_lookup_provenance` /
`validated_enrich_lookup_helper_columns` in
`enrich_lookup/provenance.py`, and `validated_helper_columns` in
`fk_helpers/policy.py` -- is the actual owner-local input Slice 2's design
below now targets; it is not redesigned or reopened here. Every correction
is marked inline at its owning section below, following this repository's
existing in-place-correction convention (see, for precedent,
`docs/warm_storage/concepts/lookup_projection_capability_notes_2026-08-23.adoc`'s
own corrected sections, `3077b12`). Original historical reasoning is
preserved wherever it still holds; only what F-CORR-2 through F-CORR-6
require is changed. See <<sec18>> for the closing status of this correction
pass.

== 1. Status / gate

This task is the explicitly authorized next step after:

* `7393438` -- `docs(domain): reassess FK helper cycle 1 re-entry`
  (`domain_fk_reference_helper_cycle1_reentry_assessment_2026-08-23.adoc`):
  recommended next slice F-002, implementation *NOT YET AUTHORIZED*.
* `51d9cb5` -- `docs(domain): review FK helper cycle 1 re-entry`
  (`..._review_001.adoc`): verdict *ACCEPT WITH MINOR CORRECTIONS*; **F-002
  selection: AUTHORIZED**; **F-002 implementation: NOT YET AUTHORIZED**,
  pending exactly this bounded implementation-readiness/design step, with two
  MATERIAL findings (FIND-C1-001 formula-mode defect, FIND-C1-002 FK-side
  sibling) that this document must resolve.

This document does not reopen whether F-002 exists or is the next Cycle-1
work item -- both gates are closed by the artifacts above. Its job is to
decide exactly how to implement F-002 safely, including the two mandatory
findings, and to reach an explicit implementation-ready/not-ready verdict.
It implements nothing.

== 2. Evidence

=== 2.1 Canonical guidance read in full

`docs/developer_guide/ch06_code_conventions/05_domain_maintainer_contract.adoc`
(DMC), `docs/backlog/roadmap_domain_hardening_sequence.adoc` (Family cycle 1
scope, Phase F status), `docs/ai_info/{agent_rules,conventions,git_and_workflow,
architecture_primer,concepts_fk_helpers,interfaces_and_gates}.adoc`.

=== 2.2 Program-state artifacts read in full

* `domain_fk_reference_helper_cycle1_reentry_assessment_2026-08-23.adoc` and
  its independent `..._review_001.adoc` (ACCEPT WITH MINOR CORRECTIONS;
  authorizes F-002 selection; findings FIND-C1-001/002/003).
* `dmc_hardening/provenance_handoff/domain_fk_lookup_provenance_handoff_implementation_2026-08-21.adoc`
  -- the accepted H5 pattern this design mirrors one level up in size
  (package-facade delegation, fail-clear hardening, explicit non-goals
  naming F-002 as untouched).
* `dmc_hardening/deletion_authority/sequence_summary.adoc` -- confirms the
  deletion-authority subroutine is closed and that F-002 is the named
  return-address item; confirms nothing in that subroutine touched
  value-mismatch or provenance-shape interpretation.

=== 2.3 Current runtime read in full (not accepted from prose)

`domain/transformations/derived_column_policy.py` (829 lines);
`domain/transformations/enrich_lookup/{__init__,policy,operation,provenance}.py`;
`domain/transformations/fk_helpers/{__init__,policy,provenance,drop,enrich}.py`;
`domain/validations/fk_helpers.py` (first 120 lines plus targeted reads);
`core/formulas.py`; `tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py`;
`tests/unit/domain/transformations/test_derived_column_policy.py` (all test
names enumerated; malformed-provenance, duplicate-key, and asymmetric test
bodies read in full).

=== 2.4 Independent empirical reproduction of FIND-C1-001 [[sec2-4]]

The formula-mode defect was independently reproduced in this task, not
merely accepted from Review 001's Area 9, via a read-only script through the
installed package (no source/test file touched):

[source,text]
----
enrich_lookup(source="matrix", lookup="stories", output="matrix_view",
              source_key="story_id", lookup_key="id",
              helpers={"fields": ["title"]},
              helper_value_mode="formula", missing="empty")
  -> cell value: LookupFormulaSpec(source_key_column='story_id',
     lookup_sheet='stories', lookup_key_column='id',
     lookup_value_column='title', missing='')

apply_derived_column_policy(source="matrix_view", policy="warn_on_mismatch")
  -> 1 finding: rule_type='derived_value_mismatch', columns='title',
     row_index='0, 1', message="2 row(s) differ from canonical lookup
     'stories' for helper column 'title'." (both rows are correctly
     enriched; the "mismatch" is spurious)

apply_derived_column_policy(source="matrix_view", policy="fail_on_mismatch")
  -> raises ValueError: "Derived column policy failed:
       - derived_value_mismatch: 2 row(s) differ from canonical lookup
       'stories' for helper column 'title'."
----

*Current fact, independently confirmed:* root cause is `_norm()`
(`derived_column_policy.py:820-828`), which calls `str(value).strip()` on
whatever cell value it receives. `LookupFormulaSpec` is a frozen dataclass
with no custom `__str__`, so `str()` yields its dataclass-generated `repr()`,
which will essentially never equal `_norm()` of the plain scalar canonical
value pulled from the lookup frame. `_write_provenance`
(`enrich_lookup/operation.py:495-565`) writes the identical provenance record
regardless of `helper_value_mode` -- the provenance shape carries no
`helper_value_mode` field, so DCP cannot know from provenance alone that it
is looking at formula-mode cells; it can only know from the cell value's own
Python type at check time.

Repository-wide grep confirms zero existing tests exercise this
combination. This finding is independently CONFIRMED, not merely inherited.

== 3. Responsibility decomposition

[cols="1,4,2",options="header"]
|===
| # | Responsibility | Classification

| 1
| Lookup provenance-record shape validation (`_validated_enrich_spec`)
| Lookup-owned primitive semantics

| 2
| Symmetric/asymmetric key-form interpretation from the *written* record
  (`_validated_mismatch_keys`, `_validated_symmetric_on`, `_validated_single_key`)
| Lookup-owned primitive semantics

| 3
| Lookup key resolution from written provenance (selecting `payload_keys`/
  `lookup_keys` per the shape above)
| Lookup-owned primitive semantics

| 4
| Target-value / canonical-map construction from the current lookup frame
  (`_canonical_value_map`)
| Lookup-owned primitive semantics

| 5
| Duplicate-key semantics (current: DCP-invented "first occurrence wins";
  correct: reuse Lookup's own duplicate-key contract)
| Lookup-owned primitive semantics (currently a DCP-invented, unowned policy
  -- see <<sec6>>)

| 6
| Missing-key semantics (unresolved payload rows against the lookup frame)
| DCP correctly declines to reproduce Lookup's `missing` contract at all
  (see <<sec6>>) -- classified *not applicable* to this delegation, not a
  gap to close

| 7
| Cell-value normalization/comparison, including formula-mode cell values
| Lookup-owned primitive semantics ("what does a mismatch mean" -- DMC
  composite/primitive contract)

| 8
| Mismatch-row calculation (iterating payload rows against the canonical map)
| Lookup-owned primitive semantics, invoked once per DCP call

| 9
| Finding construction (`DerivedColumnFinding`/`Finding` objects, `rule_type`,
  `severity`, `message` wording)
| DCP-owned composition/policy -- `Finding` is DCP's own findings-frame
  contract (`domain/finding_frame.py`, tied to `FINDING_COLUMNS`/
  `findings_to_frame`), not a generic cross-domain type (confirmed distinct
  from `domain/validations/findings.py::Finding`, a different dataclass)

| 10
| Severity / warn / fail behavior (which policy triggers what)
| DCP-owned composition/policy

| 11
| DCP sequencing/atomicity (`_publish_with_lifecycle`, two-physical-
  publication invalidation, `fk_deletion_unauthorized` diagnostic)
| DCP-owned composition/policy -- explicitly *not* touched by F-002 (already
  confirmed correct by Review 001 Area 8)

| 12
| FK transient-provenance shape validation sibling (`_validated_fk_helper_names`
  reading `_meta.derived.sheets[frame].helper_columns`)
| FK-owned primitive semantics (mirror-image of #1-3, smaller scale,
  FIND-C1-002) -- see <<sec7>>
|===

Item 6 is listed for completeness per the task's own enumeration; it is
resolved as *not applicable* (DCP does not, and should not, attempt to
reproduce it) rather than as a responsibility to relocate. No item was moved
merely because it is physically adjacent to another; each classification
above is grounded in the DMC's composite/primitive contract ("composite owns
composition; dependencies retain primitive semantics") applied to the actual
current code, matching Review 001 Area 8's independently-verified table.

== 4. Desired owner boundary [[sec4]]

*Design decision.* Task 2's Option C -- "one small immutable
interpreted-provenance value object plus a mismatch evaluator" -- is
selected over Option A (one big function) and Option B (three separate
free functions). Rationale:

* An interpreted-provenance value object (shape/key-form already resolved)
  is reusable by both the shape/key-interpretation concern and the
  mismatch-evaluation concern, and it gives the two slices (<<sec8>>) a
  clean, typed interface boundary instead of a wide parameter list.
+
*Correction (2026-08-24, opportunistic, Slice-1 FINDING-001).* This bullet
originally continued "...without re-parsing the raw record twice in one
call." Slice 1's accepted implementation
(`domain_fk_reference_helper_f002_slice1_implementation_2026-08-24.adoc`,
Section 6) diverged from this document's literal one-function sketch: it
exposes two Lookup entry points -- `validated_enrich_lookup_helper_columns`
(shape-only, called unconditionally by `_resolve_derived_identity` for
every policy including `drop`) and
`interpret_written_enrich_lookup_provenance` (full interpretation, called
only under `warn_on_mismatch`/`fail_on_mismatch`) -- because folding both
into one eager call would have collapsed `drop`'s test-pinned
value-blindness (`test_malformed_key_form_drop_is_value_blind`). Both
entry points share the same shape-validation step internally, but under
`warn_on_mismatch`/`fail_on_mismatch` the record's *shape* is in fact
validated twice in one call, once per entry point -- harmlessly, since the
second validation is idempotent (Slice 1's independent implementation
review, FINDING-001, confirms this and finds no material risk). The
original claim is corrected accordingly: the value object avoids
re-parsing *join-key form* twice (genuinely single-sourced, inside
`interpret_written_enrich_lookup_provenance` alone), not shape validation
generally.
* A single mismatch evaluator (rather than three-plus free functions
  mirroring every private helper 1:1) matches the actual cohesion of the
  current code: shape validation, key resolution, canonical-map
  construction, and row comparison are one behavior unit today
  (`_check_enrich_lookup_values` already calls all of them internally) and
  splitting them into separately-exported functions would expose internal
  sequencing as public API for no consumer benefit.
* This avoids inventing a larger framework: no "projection engine", no new
  global provenance schema, no generic domain-validation framework, no
  producer IDs/ledgers, no registry change, no new public pipeline step --
  matching the task's explicit constraints and the roadmap's own "new
  infrastructure is justified only when it removes repeated complexity from
  supported paths" principle.

*Concrete shape:*

* `enrich_lookup/provenance.py` (existing module, already the package's
  provenance-interpretation home since H5) gains a new function, working
  name `interpret_written_enrich_lookup_provenance(record, *, frame_name) ->
  InterpretedEnrichLookupProvenance`. It performs exactly what
  `_validated_enrich_spec` + `_validated_mismatch_keys` +
  `_validated_symmetric_on` + `_validated_single_key` do today: validate the
  record shape, resolve the symmetric/asymmetric key form, and return an
  immutable value object exposing `lookup_name: str`, `payload_keys: tuple[str, ...]`,
  `lookup_keys: tuple[str, ...]`, `helper_columns: tuple[str, ...]`. Raises
  the identical `ValueError` messages/paths the current DCP code raises
  (compatibility, <<sec9>>).
* A new sibling module `enrich_lookup/mismatch.py` (kept separate from
  `provenance.py` to avoid overloading the write/reconcile module with
  ~150-200 lines of comparison logic, and separate from the already-large
  `operation.py`, 566 lines) exposes
  `evaluate_lookup_mismatches(payload, *, interpreted, lookup_frames) ->
  LookupMismatchReport` -- the owner-local mismatch evaluator. It performs
  what `_check_enrich_lookup_values` + `_canonical_value_map` +
  `_column_mismatch_indices` do today, corrected per <<sec5>> and <<sec6>>.
  `LookupMismatchReport` is a small immutable value carrying enough
  structured information for DCP to build its own findings (never a
  DCP `Finding`/`DerivedColumnFinding` itself -- see <<sec10>>).
* Both are re-exported through `enrich_lookup/__init__.py`'s `__all__`,
  alongside the existing `enrich_lookup` and
  `reconcile_enrich_lookup_provenance` exports -- identical mechanism to H5.
* FK side (<<sec7>>): `fk_helpers/policy.py` gains a new function, working
  name `validated_helper_columns_by_sheet(frames, *, frame_name) ->
  tuple[dict, ...]`, re-exported through `fk_helpers/__init__.py`'s
  `__all__` alongside the existing `derived_helper_columns_by_sheet`.

`derived_column_policy.py` imports only these package-facade symbols from
`enrich_lookup` and `fk_helpers`, never their internal modules -- the
identical discipline H5 already established and the existing architecture
guard already enforces (<<sec11>>).

== 5. Formula-mode defect and semantic decision [[sec5]]

*Existing defect, confirmed (FIND-C1-001, <<sec2-4,Section 2.4>>).*

*Design decision on the mandatory A/B/C/D:* **(B) explicitly correct
formula-mode mismatch semantics as part of F-002.** Not (A): silently
carrying a confirmed, empirically-reproduced, zero-coverage defect forward
into the new Lookup-owned owner contract would formalize a bug as the
newly-canonical semantics -- exactly the risk Review 001 names. Not (C):
splitting the fix into a separate prerequisite slice means writing the new
Lookup-owned mismatch evaluator twice (once preserving the bug, to satisfy a
"preserve exactly" instruction, then again to fix it) for no coherence
benefit, since the evaluator is being newly authored in Slice 2 regardless
(<<sec8>>) -- Review 001 Area 16 independently reached the same conclusion
("better handled as the first explicit sub-step... than as a whole separate
Cycle-1 slice").

*Semantic decision (Task 4's A-E), reasoned from the questions posed:*

* *What is being validated?* Not "current displayed value" -- formula-mode
  helper cells never carry a copied scalar inside the Domain layer; they
  carry a `LookupFormulaSpec` *intent* object that a spreadsheet backend
  later renders as a live formula (e.g. XLOOKUP). The evaluated result
  exists only after a spreadsheet application recalculates the opened
  workbook -- a stage the Domain pipeline never reaches and does not control.
* *Is scalar comparison fundamentally unsuitable?* Yes. Even within one
  supported backend (XLSX), the Domain-layer `Frames` never hold a
  recalculated value for a formula cell. Across backends (ODS may not
  auto-calculate on open, and no backend recalculates during Domain
  pipeline execution), there is no pipeline stage at which a "current
  evaluated scalar" for a formula-mode helper is reliably available.
  Candidate (C) -- evaluate against cached/calculated scalars -- is
  therefore rejected as architecturally unsupportable, not merely
  inconvenient.
* *Is `LookupFormulaSpec` already sufficient to validate formula intent?*
  Yes, precisely. Its four identity fields --
  `source_key_column`, `lookup_sheet`, `lookup_key_column`,
  `lookup_value_column` -- are exactly the four facts DCP's own already-
  resolved provenance interpretation independently carries: `payload_keys[0]`,
  `lookup_name`, `lookup_keys[0]`, and the helper column name being checked.
  (Formula mode is structurally single-key only:
  `enrich_lookup/operation.py::_build_formula_enrichment` uses
  `source_keys[0]`/`lookup_keys[0]` regardless of how many symmetric keys
  were configured -- an existing, unrelated `enrich_lookup` characteristic
  this design does not change, and reuses as-is for the structural
  comparison's key-index selection.)
* *Should validation check structural intent rather than evaluated result?*
  Yes -- Candidate A/E. `warn_on_mismatch`/`fail_on_mismatch` for a
  formula-mode helper cell means: does this cell's `LookupFormulaSpec`
  still point at the *declared* lookup relationship (same lookup frame,
  same source/lookup key columns, same value column)? A structural mismatch
  means the formula was built against, or hand-edited into, a different
  relationship than provenance declares -- exactly analogous in spirit to
  what values-mode detects (a cell whose content no longer matches what
  the declared relationship would produce), but evaluated on the intent
  object instead of an unavailable evaluated scalar.
* *Does provenance encode `helper_value_mode`? Should it?* No, and no --
  rejected. Adding a metadata field is an out-of-scope, unjustified
  metadata-shape change (the task's own explicit caution). The existing
  cell value already carries sufficient type information:
  `isinstance(cell_value, LookupFormulaSpec)` unambiguously identifies a
  formula-mode cell; a plain scalar unambiguously identifies a values-mode
  cell. This satisfies the task's own preference ("prefer a small semantic
  rule over new metadata if current cell values already carry enough type
  information").
* Candidate (B) (skip value-mismatch checking entirely for formula-mode
  helpers) is rejected: it would make `fail_on_mismatch` a silent no-op
  guarantee for every formula-mode helper column, removing verification
  entirely rather than correcting it, and it discards information the cell
  value already carries for free.
* Candidate (D) (reject formula-mode mismatch policies as unsupported) is
  rejected: no currently-passing pipeline relies on this combination (it
  has never worked correctly), so rejecting it outright would remove a
  currently-reachable, intended combination of two independently
  documented, registered features (`helper_value_mode="formula"` and
  `policy="warn_on_mismatch"`/`"fail_on_mismatch"`) rather than making it
  correct.

*Concrete rule (per-cell, not per-column), corrected 2026-08-24 per
F-CORR-2:* for each row's helper-column cell, branch on the cell's own
runtime type -- not on any new provenance field, not on a whole-column mode
assumption:

* If the cell value is a `LookupFormulaSpec` *and* the interpreted
  provenance is single-key (`len(interpreted.payload_keys) == 1` and
  `len(interpreted.lookup_keys) == 1`): compare the 4-tuple
  `(lookup_sheet, source_key_column, lookup_key_column, lookup_value_column)`
  against the expected tuple derived from the interpreted provenance
  `(lookup_name, payload_keys[0], lookup_keys[0], helper_col)`. Unequal ->
  structural mismatch (`derived_formula_mismatch`).
* If the cell value is a `LookupFormulaSpec` *and* the interpreted
  provenance is multi-key (`len(interpreted.payload_keys) != 1` or
  `len(interpreted.lookup_keys) != 1` -- the two lengths are always equal
  for a validly-interpreted symmetric/asymmetric record, so either
  condition alone is sufficient and equivalent): do **not** index `[0]` and
  compare. Report as unverifiable (`unverifiable_enrich_lookup`), never as
  structurally valid or invalid. See the multi-key sub-section immediately
  below for the full reasoning and required disposition.
* Otherwise (a plain scalar, the values-mode case): unchanged existing
  behavior -- compare against the canonical scalar value from the lookup
  frame.

Branching per-cell rather than per-column is deliberate: it requires no new
metadata, degrades gracefully for a hand-mixed column (e.g. one formula
cell manually overwritten with a literal value), and costs nothing extra
over the existing per-row iteration the new `evaluate_lookup_mismatches`
performs (successor to `_column_mismatch_indices`, <<sec4>>).

*Multi-key formula-mode carve-out (F-CORR-2, MATERIAL, resolves the
independent review's Area 9 finding, the most important correction in this
pass).* The independent readiness review empirically established, and this
correction pass independently re-confirmed by a fresh read-only repro
against the current (post-Slice-1) codebase (no source/test file touched):

[source,text]
----
enrich_lookup(frames, source="matrix", lookup="stories", output="matrix_view",
              keys=["id", "part"], helpers={"fields": ["title"]},
              helper_value_mode="formula", missing="empty")
  -> succeeds; both rows receive the IDENTICAL LookupFormulaSpec
     (source_key_column='id', lookup_sheet='stories', lookup_key_column='id',
     lookup_value_column='title', missing='') -- `part` silently dropped
     from the formula's identity entirely

interpret_written_enrich_lookup_provenance(record, frame_name="matrix_view")
  -> InterpretedEnrichLookupProvenance(payload_keys=('id', 'part'),
     lookup_keys=('id', 'part'), ...) -- the interpreted provenance, unlike
     the formula cell, correctly retains BOTH keys
----

`enrich_lookup(..., keys=["id", "part"], helper_value_mode="formula")` is
currently accepted by every registered surface -- two independently
documented, registered features composed with no validation-time error --
but `_build_formula_enrichment` (`enrich_lookup/operation.py:461-462`)
generates the formula using only `source_keys[0]`/`lookup_keys[0]`, silently
dropping every key after the first. This is a pre-existing `enrich_lookup`
write-path defect, independent of F-002.

The original 4-tuple structural-comparison design above compared:

    cell:     (lookup_sheet, source_key_column, lookup_key_column,
               lookup_value_column)
    expected: (lookup_name, payload_keys[0], lookup_keys[0], helper_column)

Both sides independently truncate to index `[0]` using the *same* logic
`_build_formula_enrichment` already (defectively) uses. For the reproduced
multi-key case above the two tuples are therefore equal, so the original
design's validator would report **no finding** under `warn_on_mismatch` and
**no raise** under `fail_on_mismatch` for a cell that does not, in fact,
faithfully encode the declared two-key relationship. This is unsound: the
proposed validator would *certify a broken multi-key formula as
structurally correct*, actively legitimizing a pre-existing defect that
today is simply never checked by any registered validation path (today's
defective `str(LookupFormulaSpec)` comparison at least fails closed by
accident, reporting a spurious `derived_value_mismatch`; a naive
`[0]`-only structural validator would fail *open* instead, which is
strictly worse).

*Required design correction, applied here.* Do NOT fix multi-key formula
generation in F-002 -- that write-path defect
(`_build_formula_enrichment`'s own `[0]`-only truncation) is outside F-002's
ownership/mismatch-checking slice; it is a different, larger,
`enrich_lookup`-write-path change. Instead, the Slice-2 mismatch evaluator
must fail closed for structural formula verification whenever the
interpreted provenance is multi-key -- approximately:

[source,python]
----
if isinstance(cell, LookupFormulaSpec):
    if len(interpreted.payload_keys) != 1 or len(interpreted.lookup_keys) != 1:
        # cannot structurally verify a multi-key formula relationship
        # against a single-key LookupFormulaSpec representation
        ...  # routed to unverifiable_reason, not value_/formula_mismatches
    else:
        ...  # single-key structural 4-tuple comparison, as designed above
----

(illustrative, not literal production code; do NOT index `[0]` first and
silently discard additional keys).

The DCP-facing semantic result must allow DCP to emit the existing
`unverifiable_enrich_lookup` finding category for this case -- no new rule
type is introduced for it (unlike the single-key structural-mismatch case,
which correctly keeps its own new `derived_formula_mismatch` rule type,
independently confirmed on its own merits by the review's Area 11.1). The
reason string must state, in substance: multi-key formula-mode
`enrich_lookup` cannot be structurally verified by the current single-key
`LookupFormulaSpec` representation.

Explicitly recorded, not silently implied: the underlying formula-generation
truncation (`_build_formula_enrichment`'s `[0]`-only key selection) remains
a pre-existing defect, outside F-002 Slice 2, and is **not** fixed or
normalized by this work -- multi-key formula-mode `enrich_lookup` remains as
functionally broken after Slice 2 as before it; Slice 2 only stops
*certifying* that brokenness as structurally correct.

*`LookupFormulaSpec.missing` disposition (F-CORR-3, MINOR, required before
Slice 2).* `core/formulas.py::LookupFormulaSpec` declares five fields, not
four: `source_key_column`, `lookup_sheet`, `lookup_key_column`,
`lookup_value_column`, and `missing` (default `""`). The 4-tuple structural
comparison above omits `missing`. Re-confirmed against current source:
formula mode rejects `missing="fail"` before formula-building
(`"missing='fail' cannot be enforced with helper_value_mode='formula'"`,
raised at `enrich_lookup/operation.py:164-168`); `_build_formula_enrichment`
then hardcodes `missing=""` on every `LookupFormulaSpec` it constructs
(`operation.py:469`), regardless of its own `missing_mode` parameter. The
practical result: today, every formula-mode `LookupFormulaSpec` the one
existing producer ever writes carries the identical literal `missing=""` --
the 4-tuple's omission has zero currently-reachable failure case.
+
*Design decision (preferred minimal posture, chosen):* compare the four
relationship-identity fields only; `missing` is deliberately excluded from
the structural relationship comparison because its currently-reachable
producer value is invariant (`""`) and provenance does not independently
encode an expected formula-mode `missing` policy -- the same gap Section 6.2
below already dispositions for values-mode `missing`, for the identical
reason (`_write_provenance` never records the `missing` mode used at
enrichment time). This is a deliberate, partial structural contract, not an
oversight: if `_build_formula_enrichment` ever gains the ability to emit a
formula-mode `LookupFormulaSpec` with a non-empty `missing` value (e.g. a
future write-path change loosening today's `missing="fail"` rejection),
this assumption must be revisited. F-002 does not add `missing` to
provenance, and this design does not infer the original `missing` policy
for formula-mode cells any more than Section 6.2 infers it for values-mode
cells.

*New finding vocabulary, explicit compatibility decision:* structural
formula-mode mismatches are reported under a **new** `rule_type`,
`derived_formula_mismatch`, distinct from `derived_value_mismatch`. Keeping
it distinct avoids conflating two different mismatch kinds (content
divergence vs. formula-intent divergence) under one label and keeps
`derived_value_mismatch`'s existing meaning and message wording completely
unchanged for the values-mode path it was always about. No canonical
documentation currently enumerates DCP `rule_type` strings (grepped;
none found outside the review artifact itself), so this addition requires
no registry/model update -- test coverage is the compatibility record.
+
*Correction (2026-08-24, F-CORR-4).* This paragraph originally continued:
"This is additive, not breaking: the combination has never produced a true
positive under the current defect (100% false-positive rate), so no
consumer can be relying on any specific `rule_type` for this case." That is
an overclaim, corrected per the independent review (`c7f205c`, Section
13.2): a consumer *can* depend on defective behavior -- for example, code
that filters/suppresses `derived_value_mismatch` findings specifically for
formula-mode columns as a workaround, or code that treats the unconditional
`fail_on_mismatch` raise as an implicit "this combination is unsupported"
signal. The correct framing, already used by Section 9.2's own table and
unchanged by this correction: existing behavior is evidence, not an
automatic compatibility obligation. The formula-mode behavior change is an
intentional, evidenced bug fix; it changes observable behavior; the old
behavior is known defective and independently reproduced (Section 2.4 here;
independently re-derived by `c7f205c` Section 11); the project deliberately
chooses not to preserve it; downstream consumers could theoretically have
depended on the defect regardless; the change is therefore treated as an
explicit compatibility-impacting bug fix, not declared risk-free. This does
not reopen the accepted rule-type choice itself: structurally incorrect
formula cells still use `derived_formula_mismatch`, independently confirmed
correct on its own merits by the review's Area 11.1 verdict, separately
from the wording this correction fixes.

*Open question, original framing -- superseded by the multi-key carve-out
above (history preserved, correction noted 2026-08-24).* This paragraph
originally read (kept for record): "`_build_formula_enrichment` only ever
uses index `[0]` of `source_keys`/`lookup_keys`, silently ignoring any
additional symmetric keys if a caller configured `keys: [a, b]` together
with `helper_value_mode='formula'`. This is a pre-existing `enrich_lookup`
characteristic, not something F-002 introduces or is asked to fix; it is
noted here only so a future maintainer does not mistake the single-key
structural comparison above for a new limitation this design invented."
The independent readiness review (`c7f205c`, Area 9) found this framing
incomplete: leaving the *production* defect alone is correctly out of
scope (unchanged by this correction), but the original design's own
proposed *validator* would have gone further and actively *certified* the
resulting broken multi-key cells as structurally correct -- a new,
Slice-2-introduced false-confidence signal that does not exist today (today
the combination is simply never checked by any registered validation
path). That gap is now closed by the multi-key carve-out above: the
production defect itself is still untouched and still out of scope; only
the new validator's willingness to silently endorse it is corrected.

== 6. Duplicate/missing-key semantics [[sec6]]

=== 6.1 Duplicate-key semantics

*Existing defect, independently confirmed.* `_canonical_value_map`
(`derived_column_policy.py:776-790`) resolves duplicate lookup keys with
"first occurrence wins" -- a DCP-invented tie-break with no counterpart in
Lookup's own contract. `enrich_lookup/operation.py::_check_duplicate_lookup_keys`
instead *rejects* a lookup frame with duplicate keys outright at enrichment
time (`lookup_df.duplicated(subset=join_keys, keep=False).any()` ->
`ValueError`). A currently-*pinned* test,
`test_asymmetric_duplicate_lookup_keys_first_occurrence_wins`
(`test_derived_column_policy.py:727-746`), asserts the permissive DCP
behavior today.

*Design decision:* DCP must not invent a more permissive duplicate-key
contract than Lookup. Reasoning through the task's own questions: a lookup
frame can legitimately have unique keys at enrichment time (Lookup's own
gate enforces this) and later carry duplicate keys by the time a
`warn_on_mismatch`/`fail_on_mismatch` pass runs against current pipeline
state (a later step replaced or edited the lookup frame). At that point,
"first occurrence wins" *silently certifies* a value as canonical when the
current data cannot actually support that certification -- structurally the
same unsoundness DCP's own existing "cannot verify" pattern already refuses
to accept for missing key *columns* (`_check_enrich_lookup_values`'s
`missing_payload`/`missing_lookup` branch, "certifying zero mismatches...
would be unsound").

*Resolution:* extend that same fail-closed pattern to duplicate lookup
keys, reusing Lookup's own duplicate-detection logic (a non-raising
variant of the boolean condition `_check_duplicate_lookup_keys` already
computes) inside the new `evaluate_lookup_mismatches` evaluator. When the
current lookup frame has duplicate values on `lookup_keys`, the whole
check for that call is reported through the *existing*
`unverifiable_enrich_lookup` `rule_type` (no new vocabulary needed here,
unlike <<sec5>>) with an added reason string ("lookup frame `{lookup}`
contains duplicate keys on `{lookup_keys}`; cannot verify enrich_lookup
helpers"), following the exact severity-scaled behavior the missing-key-
column path already has: a warning under `warn_on_mismatch`, a failure
that raises under `fail_on_mismatch`.

*Compatibility consequence, explicit:* this changes observable behavior for
the one pinned test above. `test_asymmetric_duplicate_lookup_keys_first_occurrence_wins`
must be renamed and its assertion corrected to expect an
`unverifiable_enrich_lookup` finding (or, under `fail_on_mismatch`, a raise)
instead of zero findings, as part of Slice 2 (<<sec8>>). This is recorded
here as a deliberate, reviewed compatibility change, not an incidental
side effect. Per F-CORR-6 (<<sec9>>), this is release-note-worthy on the
same explicit basis as the formula-mode change -- see Section 9.2's table.

*Shared duplicate-key primitive (design detail, recorded per the
independent review's non-blocking Area 14.2 recommendation; not implemented
here).* `_check_duplicate_lookup_keys` (`enrich_lookup/operation.py:227-233`)
is private to `operation.py`, computing the raising check as
`lookup_df.duplicated(subset=join_keys, keep=False).any()`. Slice 2's new,
non-raising `evaluate_lookup_mismatches` (<<sec4>>) needs the identical
boolean predicate. Two options are rejected: duplicating the boolean
expression in two files (drifts silently over time), and having
`mismatch.py` call `operation.py`'s raising function directly and catch the
resulting `ValueError` (abuses a raising function as a boolean predicate, a
pattern this codebase does not otherwise use).
+
Preferred implementation, recorded for Slice 2 to apply: extract the
boolean predicate into a small, same-family, non-raising private helper in
`enrich_lookup/policy.py` (not `operation.py`) -- the smallest owner-local
location consistent with existing package convention. `operation.py`
already imports underscore-prefixed private names directly from `.policy`
(`_FORMULA_MODES`, `_ResolvedKeys`, `_resolve_allowed`, `_resolve_fields`,
`_resolve_join_keys`, `_resolve_missing`, `_resolve_order`,
`_resolve_value_mode`), so `policy.py` is already this package's
established home for shared private helpers consumed by `operation.py`;
`mismatch.py`, as a sibling module in the same family, may import the same
private name the identical way (same-package private imports across
`enrich_lookup`'s own submodules are the codebase's established idiom, not
a boundary violation -- the architecture guard only blocks cross-family
imports). Both `operation.py`'s existing raising check and `mismatch.py`'s
new non-raising check then call the one shared predicate: one source of
truth for "does this lookup frame currently have duplicate keys," with
`operation.py` wrapping it in its existing raise and `mismatch.py` wrapping
it in `LookupMismatchReport.unverifiable_reason`. This is a design detail,
not a new public facade -- the extracted helper is never exported through
`enrich_lookup/__init__.py`. Implementing this correctly places
`enrich_lookup/operation.py` in Slice 2's file scope (<<sec13>>) for this
one small edit -- same-family deduplication supporting one semantic
contract, not a public-behavior redesign; recorded explicitly here as a
scope note, not performed in this design-only task.

=== 6.2 Missing-key semantics

*Current fact, confirmed correct -- no change.* `_column_mismatch_indices`
treats an unresolved payload row (`canonical_row is None`, i.e. the
payload's join-key value has no match in the current canonical map) as
"a separate concern" and skips it entirely -- it neither enforces
Lookup's `missing="fail"` contract (would require raising) nor its
`missing="empty"` contract (would require asserting the helper cell is
blank). Tracing why: `_write_provenance` never records the `missing` mode
used at enrichment time -- the provenance shape carries only
`lookup`/key-form/`helper_columns`, so DCP structurally cannot know which
contract was in force even if it wanted to reproduce it.

Per the task's explicit instruction not to invent hidden inference and to
say plainly when mismatch checking cannot fully reproduce the original
enrichment contract: it cannot, for `missing`, because the required policy
is absent from provenance by design, and adding it would be an unjustified
metadata-shape change out of this slice's scope (same reasoning as
<<sec5>>'s rejection of a new `helper_value_mode` field). DCP's current
behavior -- silently declining to check unresolved rows rather than
guessing which `missing` contract applied -- is the only sound option given
that gap, and is independently judged correct, not a gap to close. This
responsibility is *not* moved to Lookup and is *not* changed by F-002.

== 7. FK-side sibling disposition [[sec7]]

*Existing defect, confirmed (FIND-C1-002).*
`_validated_fk_helper_names` (`derived_column_policy.py:286-303`)
independently parses and validates `_meta.derived.sheets[frame].helper_columns`
-- FK's own writer's own shape (`fk_helpers/provenance.py::_write_helper_provenance`)
-- with zero delegation to any FK-owned reader, mirroring F-002's larger
pattern at roughly 18 lines. FK's package already exports a reader for this
exact metadata root, `fk_helpers/policy.py::derived_helper_columns_by_sheet`
(re-exported via `fk_helpers/__init__.py`), but it is deliberately *lenient*
-- it silently skips malformed entries (`if not isinstance(sheet_entry, dict):
continue`) rather than raising.

*Would reusing the existing lenient reader break current callers?* This
question was checked directly, not assumed: `derived_helper_columns_by_sheet`
has six existing callers beyond DCP --
`domain/transformations/helpers.py::reorder_helpers_next_to_fk`,
`fk_helpers/drop.py::drop_helpers`, `domain/column_roles/resolver.py`, and
`domain/validations/fk_helpers.py` (three call sites:
`validate_fk_helpers`'s own guard, `_expected_helper_columns_by_sheet`, and
`_target_id_fields`).
+
*Correction (2026-08-24, opportunistic, Slice-1 FINDING-003).* This count
originally read "five... (twice)," inherited unchanged from this document
through the readiness review, the Slice-1 implementation record, and the
Slice-1 ultrareview. Slice 1's independent implementation review (`9b0c978`,
FINDING-003) directly grepped and read `domain/validations/fk_helpers.py`
and found three call sites (lines 302, 358, 479), not two -- corrected here
to six/three. This does not affect the decision the count supports: all six
callers use the unchanged, untouched lenient
`derived_helper_columns_by_sheet`; Slice 1 did not touch any of them, and
the rejection below holds identically whether the true count is five or
six. Retrofitting fail-clear behavior onto the existing function would be a
materially larger, riskier change touching reorder/drop/column-role-
resolution/validation behavior all at once -- explicitly rejected. DCP's own fail-clear contract, in turn, is
directly test-pinned
(`test_malformed_helper_columns_entry_raises_clear_value_error`) and must
not be silently loosened to match the lenient reader either.

*Design decision:* two owner-local readers for two genuinely distinct
contracts (Task 7's third listed option). Add a new function,
`fk_helpers/policy.py::validated_helper_columns_by_sheet(frames, *,
frame_name)`, that performs exactly what DCP's `_validated_fk_helper_names`
does today (fail-clear, identical `ValueError` message/path), re-exported
through `fk_helpers/__init__.py`'s `__all__`. `derived_helper_columns_by_sheet`
is left completely unchanged for its six existing lenient callers.

*Disposition (Task 7's required A/B/C choice):* **INCLUDE IN SAME
IMPLEMENTATION** -- specifically folded into Slice 1 (<<sec8>>), not its
own follow-up slice and not deferred. Rationale: it is the same seam
(FK-owned shape parsed inside DCP), the same low-risk/formula-agnostic
profile as Slice 1's other content, the same package-facade mechanism, and
Review 001 itself calls it "naturally absorbable as a companion item within
F-002's own implementation-readiness scope" and warns that leaving it
silently unaddressed risks a milder recurrence of the exact "reader
mistakes a smaller fix for the whole problem" risk Section 7 of the
Cycle-1 assessment already names for H5. Bundling it into Slice 1 (rather
than deferring) closes that risk instead of reproducing it a third time.

== 8. Slice decomposition [[sec8]]

*Design decision: TWO SLICES* (Task 3's Option B, refined with the formula-
mode and duplicate-key decisions resolved explicitly here rather than left
open at implementation time).

[cols="1,4,4",options="header"]
|===
| Criterion | Slice 1 (shape/key interpretation) | Slice 2 (mismatch delegation)

| Semantic coherence
| Shape validation + key-form resolution is one coherent, formula-agnostic
  concern; folding in the FK-side sibling is the same concern at FK's
  provenance shape
| Value comparison, duplicate-key handling, and formula-mode structural
  comparison are one coherent "what does a mismatch mean" concern

| Reversibility
| High -- pure relocation behind a facade, byte-identical error paths
| Medium -- two intentional, separately tested behavior corrections bundled
  with the ownership move; each is independently revertable in principle
  but authored together since the function is rewritten once, not twice

| Reviewability
| Small, mechanical, easy to diff against current private helpers
| Larger, requires reviewing two explicit behavior-change decisions plus
  the ownership move together, matching how the review itself reasoned
  about the defect

| Blast radius
| `derived_column_policy.py`, `enrich_lookup/provenance.py`,
  `fk_helpers/policy.py`, both `__init__.py` facades
| `derived_column_policy.py`, new `enrich_lookup/mismatch.py`, one pinned
  test rename/behavior update, new formula-mode tests

| Risk of mixing bugfix with refactor
| None -- no behavior change intended in this slice at all
| Present but explicit and reviewed: bundling the formula-mode and
  duplicate-key corrections into the same slice that moves ownership is
  the deliberate choice (see rationale below), not an accident

| Preserving current behavior intentionally
| Fully preserved (see <<sec9>>)
| Two named exceptions, both explicit and separately tested (<<sec9>>)

| Risk of leaving duplicated semantics half-fixed
| Low on its own; would be *complete* only once Slice 2 lands
| Closes the pattern F-002 targets; after both slices, zero Lookup-owned
  interpretation remains reimplemented in DCP
|===

*Why not Option A (one slice)?* A single slice moving both shape
interpretation and mismatch evaluation together is architecturally
coherent, but review-sizing evidence (Review 001 Area 10/16) shows the two
halves carry materially different risk: shape/key-form resolution is
formula-agnostic and low-risk; value comparison is exactly where the
empirically-confirmed defect lives and where two explicit behavior
corrections must be reviewed on their own merits. Splitting lets a reviewer
accept the low-risk relocation quickly and spend its attention on the
higher-risk behavior corrections in Slice 2, mirroring H5's own discipline
of keeping FIND-001-class hardening decisions visible and separately
reviewable.

*Why not Option C (formula decision/fix first, entirely separate from the
ownership move)?* Because the formula-mode and duplicate-key fixes are
naturally expressed *inside* the new Lookup-owned mismatch evaluator being
authored in Slice 2 regardless. Fixing them first inside the old DCP
private functions, then deleting those functions and re-authoring the
corrected logic again inside the new evaluator, means writing the same
correction twice for no benefit -- exactly the "wasted churn" risk Review
001's own gate decision and Area 16 independently flagged. Resolving the
semantics explicitly in *this* document (<<sec5>>, <<sec6>>) and
implementing them once, at the point the evaluator is newly authored,
achieves the review's real requirement (an explicit, first-class, reviewed
decision, not a silent side effect) without the double-authoring cost.

== 9. Compatibility contract [[sec9]]

=== 9.1 Preserved unless explicitly listed as changed below

* Registered pipeline step name (`apply_derived_column_policy` is unchanged;
  no new registered step is added).
* YAML parameters of `apply_derived_column_policy`
  (`source`/`output`/`policy`/`findings`/`name`) and of `enrich_lookup`.
* All existing `_meta` shapes: `_meta.derived.sheets.*.enrich_lookup`,
  `_meta.derived.sheets.*.helper_columns`, `_meta.helper_policies.fk`,
  `_meta.sheets[*].helper_columns`. No new metadata field is introduced
  (explicitly rejected in <<sec5>> and <<sec6>>).
* Finding `rule_type` values `missing_lookup_frame`, `unverifiable_enrich_lookup`
  (values-mode uses unchanged), `derived_value_mismatch` (values-mode uses
  unchanged), `fk_deletion_unauthorized` -- unchanged meaning and message
  wording for every path except the two named below.
* `DerivedColumnFinding`/`Finding` shape (`domain/finding_frame.py`) --
  unchanged; Lookup's new evaluator does not construct these (<<sec10>>).
* DCP's severity/warn/fail policy wiring, `_publish_with_lifecycle`
  two-physical-publication invalidation, and the `fk_deletion_unauthorized`
  diagnostic -- entirely untouched (DCP-owned per Section 3, item 11).
* Safe/non-safe failure boundaries: malformed-provenance `ValueError`s
  remain raised unconditionally (never fail open under any policy),
  matching the existing `_validated_mismatch_keys` discipline.
* H5/deletion-authority behavior -- untouched; F-002 does not read or write
  anything H5/deletion-authority already owns.
* `enforce_derived_column_policy_frame`'s pure-function signature and
  return shape.
* Error message text and `_meta` path wording for every existing
  `ValueError` raised by shape/key-form validation (Slice 1 is a pure
  relocation).
* No public rename, no config-schema addition, no new registered step, no
  surface-consolidation decision -- matching the Cycle-1 assessment's
  explicit non-goals for this slice.

=== 9.2 Intentional behavior changes

[cols="2,3,3,2",options="header"]
|===
| # | Old behavior | New behavior | Why / tests

| 1 (<<sec5>>)
| Formula-mode helper cells are compared via `str(LookupFormulaSpec)` vs.
  the plain canonical scalar -- always mismatches, spuriously, under both
  `warn_on_mismatch` (false finding) and `fail_on_mismatch` (unconditional
  raise)
| Formula-mode cells are compared structurally: the cell's
  `(lookup_sheet, source_key_column, lookup_key_column, lookup_value_column)`
  tuple against the expected tuple from provenance. A structural
  divergence is reported as a **new** `rule_type`,
  `derived_formula_mismatch`; a structurally-correct formula cell produces
  no finding
| Corrects an empirically-confirmed, zero-coverage defect (FIND-C1-001); no
  release note required in the H5 sense (no valid/supported prior state
  relied on the old behavior -- it never worked), but this correction is
  release-note-worthy as a genuine bug fix per the git-and-workflow
  release-notes checkpoint (user-facing: `apply_derived_column_policy`
  behavior for formula-mode Lookup helpers). New tests: valid/invalid
  formula-mode cells under both `warn_on_mismatch` and `fail_on_mismatch`,
  plus the multi-key carve-out (F-CORR-2): multi-key symmetric formula
  provenance + `LookupFormulaSpec` cell -> `unverifiable_enrich_lookup`
  under `warn_on_mismatch`, failure at the DCP orchestration boundary under
  `fail_on_mismatch` (<<sec12>>)

| 2 (<<sec6>>)
| Duplicate lookup keys on the current lookup frame: DCP's own
  `_canonical_value_map` silently picks "first occurrence wins" and
  certifies a canonical value from it
| Duplicate lookup keys are treated as unverifiable, reusing the existing
  `unverifiable_enrich_lookup` `rule_type` with an added reason string
  (warns under `warn_on_mismatch`; raises under `fail_on_mismatch`)
| Removes a DCP-invented, unowned tie-break that has already
  independently drifted from Lookup's own duplicate-key contract
  (Review 001's own corroborating evidence). One existing pinned test,
  `test_asymmetric_duplicate_lookup_keys_first_occurrence_wins`, is renamed
  and its assertion updated as part of Slice 2. *Correction (2026-08-24,
  F-CORR-6):* this row was not previously given the same explicit
  release-note disposition as Row 1, despite being an equally
  user-observable behavior change under `warn_on_mismatch`/
  `fail_on_mismatch` (a previously-silent pass now warns or raises).
  Corrected: this change is release-note-worthy on the same explicit basis
  as Row 1's formula-mode fix, per the git-and-workflow release-notes
  checkpoint -- Slice 2's implementer must either update
  `docs/release_notes/release_notes.adoc` for both named changes or
  explicitly state in the Slice-2 review artifact why no release note is
  needed for either, symmetric treatment for both (release notes are owned
  by the implementing change, not written here)
|===

No other behavior is authorized to change. Both changes above are
implemented, tested, and reviewable as explicit line items inside Slice 2 --
never silently, never as an incidental side effect of the ownership move.

== 10. Error/finding ownership [[sec10]]

*Design decision, per Task 9:* Lookup's `evaluate_lookup_mismatches`
returns a small immutable `LookupMismatchReport` value -- owner-local
semantic information, not `Finding`/`DerivedColumnFinding` objects. Its
exact shape (non-binding on naming, binding on the information it must
carry):

* `missing_lookup_frame: bool` (the named lookup frame is absent from
  `lookup_frames`);
* `unverifiable_reason: str | None` (a combined reason string covering
  missing payload/lookup key columns and, per <<sec6>>, duplicate lookup
  keys -- reusing the existing `reasons` list-and-join pattern
  `_check_enrich_lookup_values` already uses, extended with the new
  duplicate-key reason);
* `value_mismatches: Mapping[str, tuple[RowIndex, ...]]` (per helper
  column, row indices whose value diverges from canonical -- values-mode
  path, unchanged semantics);
* `formula_mismatches: Mapping[str, tuple[RowIndex, ...]]` (per helper
  column, row indices whose `LookupFormulaSpec` structurally diverges --
  the new formula-mode path, <<sec5>>).

*Multi-key formula-mode disposition, confirmed to fit the existing shape
(correction pass, 2026-08-24, per F-CORR-2).* The multi-key formula
carve-out (<<sec5>>) reports through the existing
`unverifiable_reason: str | None` field -- the same field and reason-join
mechanism the duplicate-key correction (<<sec6>>) already uses -- rather
than requiring a new report field. No concrete reason was found requiring a
richer shape: `unverifiable_reason` already needs to combine multiple,
independent unverifiable causes in one call today (missing payload/lookup
key columns, duplicate lookup keys, and now multi-key formula provenance
can all arise on different helper columns of the same call), and DCP
already owns 100% of message construction and `rule_type` selection from
this one field, so no per-column reason detail needs to survive into the
report structure itself. No new `LookupMismatchReport` field is added for
this case.

*`unverifiable_reason` shape reconsidered and left unchanged.* The
independent review separately suggested, non-blockingly, that a
`tuple[str, ...]` of structured reason strings (left unjoined, formatting
deferred entirely to DCP) may be marginally cleaner than one pre-joined
string. Considered and declined for Slice 2: the existing
`_check_enrich_lookup_values` already assembles an unjoined
`reasons: list[str]` and joins it itself today
(`derived_column_policy.py:594-614`), so a joined `str | None` relocates an
existing responsibility rather than inventing a new one, and expanding to a
structured tuple is an aesthetic simplification with no concrete Slice-2
consumer need -- the smallest sufficient shape is kept, not expanded merely
for aesthetics.

DCP (`_check_enrich_lookup_values`'s replacement body) translates this
report into `DerivedColumnFinding`s exactly as it does today: it supplies
`severity` (from `policy`), constructs `missing_lookup_frame`/
`unverifiable_enrich_lookup`/`derived_value_mismatch` findings with their
existing message templates for the first three report fields, and
constructs the new `derived_formula_mismatch` finding for the fourth.
Nothing about `rule_type` selection, severity, or `fail_on_mismatch`
raise/collect behavior moves into Lookup -- DCP retains 100% of that
policy, matching the DMC's composite/primitive contract (Section 3, items
9-11) and Review 001 Area 8's independently-verified table.

Where current exact error wording is user-visible/test-pinned (every
`ValueError` message in `_validated_mismatch_keys`/`_validated_symmetric_on`/
`_validated_single_key`/`_validated_enrich_spec`, and the `missing_lookup_frame`/
`unverifiable_enrich_lookup`/`derived_value_mismatch` finding `message`
strings), Slice 1 and the unchanged paths of Slice 2 preserve it verbatim;
only the two named changes in <<sec9>> introduce new wording, and only for
cases that previously had no sound wording at all (formula-mode) or an
unowned wording (duplicate-key "first occurrence wins" was never wording,
it was silent).

== 11. Package/import boundary [[sec11]]

*Current fact, independently confirmed.* `GUARDED_INTERNAL_PREFIXES` in
`tests/architecture/domain_transformations/test_no_cross_family_internal_imports.py`
already includes both `enrich_lookup` (`operation`, `policy`, `provenance`)
and `fk_helpers` (`drop`, `enrich`, `formula_provider`, `policy`,
`provenance`) as guarded families, added by H5 and earlier package-split
work respectively. The guard scans every module under
`domain/transformations/`, which already includes `derived_column_policy.py`
as an importer -- so DCP-as-caller is structurally covered today, with **no
new guard infrastructure required** for either seam this design adds.

* `enrich_lookup/mismatch.py` (new) requires an explicit
  `GUARDED_INTERNAL_PREFIXES` edit in Slice 2 -- this is an operational
  requirement, not an optional clarity improvement.
+
*Correction (2026-08-24, F-CORR-5).* This bullet originally claimed the
guard is directory-scanning for guarded-target modules and therefore needs
no edit for a new module, calling the `GUARDED_INTERNAL_PREFIXES` addition
"not strictly required... a one-line, low-risk clarity improvement." That
is self-contradicting against the guard's actual implementation, correctly
identified by the independent review (`c7f205c`, Section 16.3):
`_python_modules(TRANSFORMATIONS_ROOT)` directory-scans to determine the
*importer* side of the check (so a new importer such as
`derived_column_policy.py`, already covered today, needs no new entry) --
but `GUARDED_INTERNAL_PREFIXES` itself is a fixed, explicitly-enumerated
tuple per family root defining the *guarded internal target* side, not
directory-derived at all (`enrich_lookup`'s guarded tuple today is exactly
`(operation, policy, provenance)`, confirmed directly against
`test_no_cross_family_internal_imports.py` -- three names, not "every
submodule"). If `enrich_lookup/mismatch.py` is added without adding
`enrich_lookup.mismatch` to that tuple, a future bare-module import
bypassing the facade (`from ...enrich_lookup import mismatch`, structurally
identical to the `provenance`/`policy` bypass case the guard's own
regression tests already exist to catch) would **not** be flagged -- a real
enforcement gap, not a documentation nicety. The planned action itself does
not change: add `enrich_lookup.mismatch` to `GUARDED_INTERNAL_PREFIXES` in
Slice 2, exactly as already planned in Section 12.4/13 below; only its
justification is corrected.
* `derived_column_policy.py` continues to import only
  `enrich_lookup.{interpret_written_enrich_lookup_provenance,
  evaluate_lookup_mismatches}` and `fk_helpers.{resolve_v2_fk_relations,
  validated_helper_columns_by_sheet}` -- all package-facade symbols, never
  an internal submodule path.

No new guard, registry, or cross-family coordination mechanism is
introduced. This exactly repeats H5's own accepted pattern for both seams.

== 12. Test plan [[sec12]]

=== 12.1 Lookup -- values mode (moved/adapted from existing DCP tests)

Existing coverage in `test_derived_column_policy.py` (symmetric and
asymmetric variants of: unchanged/edited helper under warn and fail;
malformed key forms fail closed; missing payload/lookup key columns;
malformed sheet-level/derived-container provenance) is retained and
re-targeted to exercise `derived_column_policy.py` calling the new
`enrich_lookup`-owned functions, preserving assertions on `apply_derived_column_policy`'s
observable output (findings frame contents, raised messages) unchanged.
New/updated:

* duplicate lookup keys -> `unverifiable_enrich_lookup` under
  `warn_on_mismatch`, raise under `fail_on_mismatch` (replaces
  `test_asymmetric_duplicate_lookup_keys_first_occurrence_wins`).

=== 12.2 Lookup -- formula mode (new; zero prior coverage)

* valid formula-mode helper (structurally correct `LookupFormulaSpec`)
  under `warn_on_mismatch` -> no finding.
* valid formula-mode helper under `fail_on_mismatch` -> no raise.
* structurally incorrect formula spec (e.g. `lookup_value_column` naming
  the wrong field) under `warn_on_mismatch` -> `derived_formula_mismatch`
  finding.
* structurally incorrect formula spec under `fail_on_mismatch` -> raises,
  naming `derived_formula_mismatch`.
* formula targeting the wrong lookup sheet/key column -> same as above,
  distinct assertion per field to pin exactly which structural facet is
  compared.
* missing lookup target combined with formula-mode cells ->
  `missing_lookup_frame` (unchanged path, formula-mode-agnostic).
* duplicate lookup keys combined with formula-mode cells ->
  `unverifiable_enrich_lookup` (unchanged path, formula-mode-agnostic).
* mixed column (one row formula, one row plain scalar, e.g. after a manual
  edit) -> each row evaluated per its own type; no cross-row leakage.
* explicit no-spurious-mismatch regression test reproducing the exact
  scenario from <<sec2-4,Section 2.4>>'s empirical repro, asserting zero
  findings.

*Multi-key formula-mode tests, mandatory (added 2026-08-24, F-CORR-2 --
zero prior coverage, this document's own original test list had none):*

* symmetric multi-key provenance (e.g. `keys=["id", "part"]`) + a
  `LookupFormulaSpec` cell, under `warn_on_mismatch` ->
  `unverifiable_enrich_lookup` finding, never `derived_formula_mismatch`
  and never zero findings.
* the identical scenario under `fail_on_mismatch` -> failure at the
  existing DCP orchestration boundary (`apply_derived_column_policy`, not
  inside the evaluator), matching the existing `unverifiable_enrich_lookup`
  raise/collect behavior for other unverifiable causes.
* an explicit assertion that the evaluator does not certify an `[0]`-only
  truncated comparison as valid for a multi-key formula cell -- i.e. that
  the reproduced defect from Section 2.4's multi-key repro above is never
  reported as structurally correct.
* an explicit assertion that no `derived_formula_mismatch` is emitted
  merely because multi-key structural comparison is impossible -- the
  multi-key case is unverifiable, not "wrong."

=== 12.3 DCP policy

* `warn_on_mismatch` emits the expected finding set (values-mode,
  formula-mode, unverifiable) with unchanged `rule_type`/severity for
  every preserved path.
* `fail_on_mismatch` raises at the same orchestration boundary
  (`apply_derived_column_policy`, not inside the evaluator).
* Severity/`rule_type` compatibility for every preserved path (regression
  assertions against current test expectations, unchanged).
* Atomicity/publication (`_publish_with_lifecycle`) behavior unchanged --
  rerun existing publication-lifecycle tests (`test_payload_decoupled_*`,
  `test_findings_publication_*`, `test_decoupled_payload_invalidation_*`)
  unmodified.

=== 12.4 Architecture

* `test_no_cross_family_internal_imports.py` stays green; add
  `enrich_lookup.mismatch` to `GUARDED_INTERNAL_PREFIXES` -- operationally
  required, not optional (<<sec11>>) -- and confirm the new
  `derived_column_policy -> enrich_lookup`/`fk_helpers` edges resolve to
  package-facade imports only.
* No new guard infrastructure test is needed.

=== 12.5 FK sibling

* malformed transient FK provenance (`helper_columns` not a list; an
  entry not a mapping) retains fail-clear behavior through
  `validated_helper_columns_by_sheet`, with the identical message/path
  DCP raises today (regression-preserving rename of
  `test_malformed_helper_columns_entry_raises_clear_value_error`'s
  target, not its assertion).
* valid provenance returns the same helper names DCP resolves today.
* DCP no longer parses `_meta.derived.sheets[frame].helper_columns` itself
  -- verified by removing `_validated_fk_helper_names` and confirming the
  test suite above still passes unchanged.
* `derived_helper_columns_by_sheet`'s six existing callers (corrected
  2026-08-24, Slice-1 FINDING-003; see <<sec7>>) and their existing tests
  are untouched and rerun unmodified as a regression check that the new
  strict reader did not alter the lenient one.

Private-implementation-detail tests (e.g. asserting the internal call
sequence inside the evaluator) are not added; tests target observable
`apply_derived_column_policy` behavior, matching the task's own instruction
and existing test-file style.

== 13. Implementation sequence [[sec13]]

*Status note (2026-08-24): Slice 1 as planned below is CLOSED and
ACCEPTED* (`6f15026` implementation, `9b0c978` independent review, verdict
ACCEPT WITH MINOR CORRECTIONS). Retained below verbatim as the historical
plan Slice 1 actually followed, with the F-CORR-1 signature correction and
the two-entry-point deviation recorded in the Slice-1 implementation
record's own Section 6 -- neither reopened here. Slice 2 below is
corrected per F-CORR-2 through F-CORR-6 (<<sec18>>) and remains NOT
AUTHORIZED.

*Slice 1 -- owner-local shape/key interpretation (Lookup) + FK sibling*

* Semantic purpose: relocate provenance-shape validation and key-form
  resolution to their respective owners; zero behavior change.
* Files: `enrich_lookup/provenance.py` (new function),
  `enrich_lookup/__init__.py` (facade export), `fk_helpers/policy.py` (new
  function), `fk_helpers/__init__.py` (facade export),
  `derived_column_policy.py` (call sites updated, `_validated_enrich_spec`/
  `_validated_mismatch_keys`/`_validated_symmetric_on`/`_validated_single_key`/
  `_validated_fk_helper_names` removed).
* Behavior preserved: every `ValueError` message/path, every resolved key
  tuple, `enforce_derived_column_policy_frame`'s return shape.
* Behavior intentionally changed: none.
* Tests: <<sec12>> 12.1 (shape/malformed-provenance subset, re-targeted
  unmodified in assertion), 12.4, 12.5.
* Review gate: independent review confirming byte-identical observable
  behavior (a diff-only relocation), architecture guard green, full active
  suite green.

*Slice 2 -- Lookup-owned mismatch evaluation, formula-mode correction,
duplicate-key correction*

* Semantic purpose: transfer ownership of "what a Lookup mismatch means"
  to Lookup, including the two named, explicit, reviewed corrections.
* Files (updated 2026-08-24, F-CORR corrections): new
  `enrich_lookup/mismatch.py`; `enrich_lookup/__init__.py` (facade export);
  `enrich_lookup/policy.py` (new small private, non-raising duplicate-key
  predicate, per the shared-primitive design in <<sec6>> -- not exported);
  `enrich_lookup/operation.py` (its existing raising duplicate-key check
  updated to call the shared predicate instead of its own inline boolean
  expression -- **scope expansion, recorded 2026-08-24**: same-family
  deduplication supporting one semantic contract, not a public-behavior
  redesign; the original readiness design did not include this file);
  `derived_column_policy.py` (`_check_enrich_lookup_values`/
  `_canonical_value_map`/`_column_mismatch_indices` removed, replaced by a
  call to `evaluate_lookup_mismatches` plus DCP's own Finding-construction
  translation); `test_no_cross_family_internal_imports.py`
  (`GUARDED_INTERNAL_PREFIXES` gains `enrich_lookup.mismatch`, operationally
  required per F-CORR-5, <<sec11>>).
* Behavior preserved: values-mode comparison semantics, `missing_lookup_frame`/
  `unverifiable_enrich_lookup`/`derived_value_mismatch` message wording for
  every path except the changes below, DCP severity/policy wiring,
  publication lifecycle.
* Behavior intentionally changed: formula-mode structural comparison
  replacing spurious scalar comparison (new `derived_formula_mismatch`
  `rule_type`, single-key only); multi-key formula-mode reported as
  `unverifiable_enrich_lookup` rather than certified-or-rejected structural
  intent (F-CORR-2, <<sec5>>); duplicate-lookup-key fail-closed behavior
  replacing "first occurrence wins" (reuses `unverifiable_enrich_lookup`).
  Both the formula-mode and duplicate-key changes carry the same explicit
  release-note disposition (F-CORR-6, Section 9.2).
* Tests: <<sec12>> 12.1 (duplicate-key subset), 12.2 (all formula-mode,
  new, including the mandatory multi-key cases), 12.3, 12.4.
* Review gate: independent review explicitly evaluating all named behavior
  changes on their own merits (not merely "does the suite pass"),
  confirming the empirical repro from <<sec2-4,Section 2.4>> now produces
  zero spurious findings, confirming the multi-key formula repro above
  (F-CORR-2) now produces `unverifiable_enrich_lookup` rather than a false
  structural pass, confirming
  `test_asymmetric_duplicate_lookup_keys_first_occurrence_wins`'s
  replacement is correctly reasoned, full active suite green.

Each slice is its own Conventional Commit naming this readiness artifact's
governing assessment (F-002), consistent with `git_and_workflow.adoc`'s
"one logical change per commit" and the H5 precedent's commit granularity.

== 14. Exit criteria

* DCP no longer independently interprets Lookup's provenance/key shape
  (Slice 1) or independently defines Lookup mismatch semantics (Slice 2).
* Lookup owns its primitive semantics (shape, key-form, canonical-map
  construction, mismatch evaluation including formula-mode) behind its
  package facade; FK owns its transient-provenance shape validation behind
  its package facade (<<sec7>>).
* DCP retains only composition/policy/severity/atomicity -- unchanged from
  today (Section 3, items 9-11).
* Formula-mode behavior is explicitly corrected, not merely dispositioned
  (<<sec5>>) -- FIND-C1-001 fully closed, not deferred.
* Duplicate-key semantics no longer drift between DCP and Lookup (<<sec6>>).
* FK-side sibling (FIND-C1-002) is closed within the same implementation
  sequence (<<sec7>>), not left as an unnamed residual.
* Architecture guard (`test_no_cross_family_internal_imports.py`) passes,
  with `enrich_lookup.mismatch` named explicitly.
* Full active suite (`make test`) green after each slice.
* No public-surface consolidation decision is made -- `add_fk_helpers` vs.
  `add_lookup_helpers` vs. any merged name remains entirely undecided by
  this work, per <<sec15>>.

*Family-Cycle-1 closure consequence:* completing both slices as scoped
closes the roadmap's own named blocker for this family's Family-cycle-1
exit criterion ("remove remaining ontology... hidden cross-family
knowledge") for the FK/Lookup pair specifically. It does **not** itself
declare Family cycle 1 closed for the whole family (Review 001 Area 20
already makes this distinction) and does **not** trigger a separate
Family-Cycle-1 closure assessment automatically -- that remains a
deliberate next step, to be requested explicitly once Slice 2 is
independently accepted, matching the roadmap's own "Cycle-1 exit itself
should receive its own closing assessment when reached, not be
self-declared inside the F-002 implementation."

== 15. Explicit non-goals / non-decisions [[sec15]]

This document does not decide, and none of the above should be read as
deciding:

* whether `add_fk_helpers` or `add_lookup_helpers` (or a merged name)
  survives any future surface consolidation -- entirely out of scope,
  per the task's explicit instruction and the still-open
  `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5`;
* the non-binding Lookup Projection concept note or its findings -- not
  read, not used, not reopened;
* any config-schema addition (output aliasing, a `validate_lookup_helpers`
  registered step, `helper_value_mode` in provenance) -- all considered
  and explicitly rejected above as out of scope for this slice, not
  silently dropped;
* Trusted-Ingress FK-formula certification, or any change to
  `LookupFormulaSpecRole`/the controlled-role model in
  `pipeline/execution_state/` -- untouched; this design's formula-mode
  comparison reads `LookupFormulaSpec`'s own declared fields, the same
  fields Trusted Ingress's own role model already treats as the certified
  shape, and introduces no new admission or certification logic;
* `core/refs.py` disposition, FK `defaults` config-surface cleanup, or any
  other item in the Cycle-1 assessment's Section 9 inventory beyond F-002
  and its FK-side sibling;
* whether Lookup Enrichment should gain a formal "family" label -- untouched;
* Family cycles 2-7 for this family -- untouched, remain entirely
  unstarted.

== 16. Implementation authorization recommendation

Both mandatory findings (FIND-C1-001, FIND-C1-002) are resolved above with
explicit, reasoned, independently-reviewable decisions:

* formula-mode: **explicitly corrected** (Decision B), not preserved, not
  silently fixed, structural-intent semantics decided and specified
  (<<sec5>>);
* FK-side sibling: **included in the same implementation sequence**,
  folded into Slice 1 (<<sec7>>);
* duplicate-key drift, raised by Task 5 as a required decision even though
  not itself a numbered FIND-C1 finding: **explicitly corrected** to a
  fail-closed contract, bundled into Slice 2 with its own compatibility
  record (<<sec6>>).

The owner boundary (<<sec4>>), package/import mechanism (<<sec11>>), test
plan (<<sec12>>), and two-slice implementation sequence (<<sec8>>,
<<sec13>>) are concrete and bounded: no new framework, registry, public
surface, or metadata shape is introduced; the mechanism repeats the
already-twice-accepted H5 package-facade pattern one level up in size,
exactly as the Cycle-1 assessment and its review anticipated.

== 17. Independent review gate

**IMPLEMENTATION READY -- INDEPENDENT REVIEW REQUIRED**

First implementation slice, if this readiness design is independently
accepted: *Slice 1 -- owner-local shape/key interpretation (Lookup) + FK
sibling*, exactly as scoped in <<sec13>>. It is allowed to change only:

* `enrich_lookup/provenance.py` (add
  `interpret_written_enrich_lookup_provenance`);
* `enrich_lookup/__init__.py` (facade export);
* `fk_helpers/policy.py` (add `validated_helper_columns_by_sheet`);
* `fk_helpers/__init__.py` (facade export);
* `derived_column_policy.py` (remove `_validated_enrich_spec`,
  `_validated_mismatch_keys`, `_validated_symmetric_on`,
  `_validated_single_key`, `_validated_fk_helper_names`; call the two new
  facade functions instead; no other change to this module in this slice);
* the corresponding test files named in <<sec12>>.

It must not touch `_check_enrich_lookup_values`, `_canonical_value_map`,
`_column_mismatch_indices`, or any formula-mode/duplicate-key behavior --
that is Slice 2's exclusive scope, gated on its own independent review
after Slice 1 is accepted.

This artifact does not itself authorize implementation to begin.
Independent review of this readiness design is required first, per the
governing gate in `domain_fk_reference_helper_cycle1_reentry_assessment_2026-08-23_review_001.adoc`
Section 21.

*Status update (2026-08-24): Slice 1 as scoped above is CLOSED and
ACCEPTED* (`6f15026` implementation; `9b0c978` independent review, verdict
ACCEPT WITH MINOR CORRECTIONS), on the condition recorded by `c7f205c`
F-CORR-1 (the FK reader takes the already-extracted per-sheet value, not
`frames` -- followed; see the Slice-1 implementation record's own Section
3). This section's original text is retained verbatim above as the
historical gate Slice 1 actually passed through. See <<sec18>> for Slice
2's corrected status.

== 18. Slice-2 correction record and current status [[sec18]]

*Correction pass, 2026-08-24.* This section records the disposition of
`c7f205c`'s F-CORR-2 through F-CORR-6 against this document, applied
in-place at each owning section above. F-CORR-1 was resolved directly by
Slice 1's implementation (`6f15026`, Section 3 of
`domain_fk_reference_helper_f002_slice1_implementation_2026-08-24.adoc`)
and independently confirmed accepted (`9b0c978`); it is not reopened here.
Slice 1's own three MINOR findings (FINDING-001 through FINDING-003) are
opportunistically corrected in this document where they recur (FINDING-001
at <<sec4>>; FINDING-003's caller-count inaccuracy at <<sec7>> and Section
12.5) -- their own governing artifacts are left unmodified, per this task's
instruction not to rewrite historical review artifacts merely to fix a
count. FINDING-002 (isolated-unit-test coverage gaps) does not recur in
this document and is left for Slice 2 implementation, as instructed.

[cols="1,4",options="header"]
|===
| Finding | Resolution in this document

| F-CORR-2 (MATERIAL)
| Multi-key + formula-mode carve-out added to <<sec5>>'s concrete rule:
  `len(interpreted.payload_keys) != 1` (equivalently `lookup_keys`) combined
  with a `LookupFormulaSpec` cell is now explicit `unverifiable_enrich_lookup`,
  never index-`[0]` structural comparison. Independently re-confirmed by a
  fresh read-only repro against the current (post-Slice-1) codebase, which
  also confirms `InterpretedEnrichLookupProvenance.payload_keys`/
  `lookup_keys` already carry the full, untruncated key tuples, so the
  `len(...) != 1` guard is directly computable from the accepted Slice-1
  seam with no further Lookup-side change needed. New mandatory tests added
  to Section 12.2.

| F-CORR-3 (MINOR)
| <<sec5>> now explicitly dispositions `LookupFormulaSpec.missing`: the
  4-tuple structural comparison deliberately excludes it, reasoned
  symmetric with Section 6.2's values-mode `missing` disposition, with the
  producer invariant (`missing=""` on every reachable formula-mode write)
  stated as the basis and a revisit trigger named.

| F-CORR-4 (MINOR)
| <<sec5>>'s compatibility justification reworded: "no consumer can be
  relying on any specific `rule_type`" is corrected to "existing behavior
  is evidence, not an automatic compatibility obligation" -- the
  formula-mode change remains an intentional, evidenced bug fix, not a
  claim of zero compatibility risk. The accepted `derived_formula_mismatch`
  rule-type choice itself is unchanged and not reopened.

| F-CORR-5 (MINOR)
| <<sec11>> reworded: adding `enrich_lookup.mismatch` to
  `GUARDED_INTERNAL_PREFIXES` is now stated as operationally necessary to
  close a real facade-bypass enforcement gap, not an optional clarity
  nicety. The planned action itself (add the entry) is unchanged.

| F-CORR-6 (MINOR)
| Section 9.2's table (Row 2, duplicate-key) now carries the same explicit
  release-note disposition already given to Row 1 (formula-mode): both are
  equally user-observable `warn_on_mismatch`/`fail_on_mismatch` behavior
  changes, and Slice 2's implementer must either update
  `docs/release_notes/release_notes.adoc` for both or explicitly record in
  the Slice-2 review artifact why no release note is needed for either --
  symmetric treatment, not written here (release notes are owned by the
  implementing change, per `docs/ai_info/review_instructions.adoc`).
|===

*Duplicate-key shared primitive (design detail, recorded per the
independent review's non-blocking Area 14.2 recommendation).* Recorded in
full at <<sec6>>: a small, non-raising, same-family private predicate in
`enrich_lookup/policy.py`, called by both `operation.py`'s existing raising
check and `mismatch.py`'s new non-raising check. This adds
`enrich_lookup/operation.py` to Slice 2's file scope (<<sec13>>), recorded
explicitly as same-family deduplication, not a public-behavior redesign.

*`LookupMismatchReport` shape, reconfirmed.* No new field is added for the
multi-key formula case (<<sec10>>): it fits the existing
`unverifiable_reason: str | None` field, alongside missing-lookup-frame,
missing-key-column, and duplicate-lookup-key reasons already routed through
the same field. The optional structured-tuple-of-reasons simplification
remains declined, as a non-required aesthetic change.

*Slice 2 remains one implementation slice.* None of F-CORR-2 through
F-CORR-6 reveal a coherence reason to split Slice 2 further: the multi-key
carve-out is a small, localized branch inside the same
`evaluate_lookup_mismatches` function already being newly authored; the
`missing`-field and compatibility-wording corrections are
documentation-only; the architecture-guard and release-note corrections are
unchanged action items with corrected justification only; the
duplicate-key shared-primitive extraction is a small, same-family addition
to `operation.py` authored in the same commit. Section 13's expected file
scope, updated: new `enrich_lookup/mismatch.py`; `enrich_lookup/__init__.py`
facade export; `enrich_lookup/policy.py` (new private predicate);
`enrich_lookup/operation.py` (call the shared predicate);
`derived_column_policy.py`; the corresponding test files (Section 12.1
duplicate-key subset, 12.2 all formula-mode including the new multi-key
cases, 12.3, 12.4); the architecture guard; a release-note artifact if
Slice 2's own implementer determines one is required per Section 9.2; the
Slice-2 implementation record.

*Implementation authorization.* This correction task does not itself
authorize Slice 2.

Slice 2 design: CORRECTED

Slice 2 implementation: NOT AUTHORIZED

Targeted independent finding closure: REQUIRED

Only after a new independent review closes F-CORR-2 through F-CORR-6 may
Slice 2 implementation begin, in addition to the pre-existing requirement
(`c7f205c` Section 19) that Slice 2 remains gated on Slice 1's own
independent implementation review -- already satisfied (`9b0c978`, verdict
ACCEPT WITH MINOR CORRECTIONS, Slice 1 CLOSED).
