= Derived Artifact / FK Helper Deletion Authority -- Slice 6 Documentation Closure Review
2026-08-23

== 1. Scope / reviewed commit

Independent, bounded documentation review of Slice 6 of the accepted
Derived Artifact / FK Helper Deletion Authority workstream.

Reviewed commit::
`daf27bcdd7d6fbe7d881370d1ec2bd6242e2b6ce` -- "docs(domain): close helper
deletion authority docs"

Baseline::
`683c16a` -- "docs(domain): review deletion authority integration"
(`derived_artifact_deletion_authority_slices1_5_integration_review_2026-08-23.adoc`,
verdict ACCEPT; Slices 1-5 runtime INTEGRATED, Slice 6 doc closure
AUTHORIZED).

This review is read-only: no runtime, test, registry, or documentation
edits were made other than this artifact. No Project Memory / roadmap
closure and no DMC FK-helper hardening work was started, per the task
boundary.

Files touched by `daf27bc` (confirmed via `git show --stat`, matches the
expected-file list exactly -- no scope expansion):

* `docs/ai_info/concepts_fk_helpers.adoc`
* `docs/functional_model/ch04_use_case_concepts/fk_helpers.adoc`
* `docs/release_notes/release_notes.adoc`
* `docs/semantic_model/12_contract_matrices.adoc`
* `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`
* `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`
* `registries/pipeline_step_registry.json`

`git diff --check` on the commit is clean. No file under
`docs/cold_storage/`, `docs/warm_storage/global_reviews/`, or
`docs/backlog/` was touched; no accepted design/review artifact was
rewritten; no runtime Python or test file changed.

== 2. Authoritative runtime contract (independently re-verified)

Re-derived directly from source, not from the commit message or prior
reviews:

* `src/spreadsheet_handling/domain/transformations/fk_helpers/drop.py`
  (`drop_helpers`, registered `remove_fk_helpers`): deletion candidates
  come only from `derived_helper_columns_by_sheet(frames)`
  (`_columns_to_drop_by_sheet`, lines 84-96). The v2 relation list
  (`resolve_v2_fk_relations`) is never read into the drop set; it is used
  only for (a) the precondition `relations is None and not
  provenance_by_sheet` (line 54, raises `MissingFkRelationPolicyError`)
  and (b) `_log_durable_policy_only_survivors`, a non-raising diagnostic.
* `src/spreadsheet_handling/domain/transformations/fk_helpers/policy.py`:
  `resolve_v2_fk_relations` returns `None`/a list for the *whole*
  `_meta.helper_policies.fk` block (not per sheet);
  `derived_helper_columns_by_sheet` returns a dict keyed by *every* sheet
  that currently carries provenance. The `drop_helpers` precondition at
  line 54 therefore evaluates once, over the whole `frames` argument --
  it is not evaluated per individual frame/sheet. See Finding 3.
* `src/spreadsheet_handling/domain/transformations/derived_column_policy.py`
  (`apply_derived_column_policy`, registered `apply_derived_column_policy`):
  operates on a single `source` frame per call. It never raises a
  policy/provenance-precondition error -- there is no
  `MissingFkRelationPolicyError`-equivalent call anywhere in this module
  (confirmed by `grep -n raise`). The only FK-policy-related behavior is
  the non-raising `fk_deletion_unauthorized` diagnostic
  (`_fk_deletion_unauthorized_findings`, lines 410-474), which never
  raises under any policy, including `fail_on_mismatch` (line 441-442).
  See Finding 2.
* `src/spreadsheet_handling/pipeline/persistence_boundary.py`: top-level
  `derived` is in `RUNTIME_ONLY_TOP_LEVEL_META_KEYS` (line 112-114) and is
  unconditionally pruned before persistence; `helper_policies` (v2
  relations) is explicitly preserved (lines 84-91). This confirms the
  central claim: a durable FK relation surviving reimport carries no
  transient provenance with it.
* `enrich_lookup` (`operation.py`) writes its own
  `_meta.derived.sheets.<frame>.enrich_lookup` record independently of the
  FK relation model, confirming Lookup-owned identity is a genuinely
  distinct authority, not derived from FK relation policy.

This matches the runtime contract stated in the task prompt; no
contradiction found between the prompt's contract and the code.

== 3. Canonical-doc review

=== 3.1 `docs/ai_info/concepts_fk_helpers.adoc` (AI-info FK helper contract)

The rewritten bullet (lines 60-74) correctly states: current truthful
transient provenance is the only source of FK-attributed deletion
candidates; durable v2 policy expresses configuration/materialization
intent and does not independently authorize deletion; there is no prefix
fallback; `reorder_fk_helpers` is explicitly called out as unaffected and
`FK_PATTERN` is not consulted by either primitive. This closes the item the
integration review classified `MUST UPDATE IN SLICE 6` (lines 60-61
pre-change).

One imprecision: "raises only when policy and provenance are both absent
*for a frame*" (line 68) mischaracterizes the precondition's granularity.
See Finding 3.

*Verdict: correct on the central authority claim; one scope-precision
imprecision (Finding 3, MINOR).*

=== 3.2 `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc` (three instances)

All three instances named in the commit message were independently
located and verified stale pre-change, and now correctly state the
non-fallback rule:

* Lines 130-133 (pre-change) / 128-144 (post-change) -- "Producer/consumer"
  paragraph. Fixed correctly; reproduces the same "for a frame" imprecision
  (line 136). Finding 3.
* Lines 184-188 (pre-change) / 190-203 (post-change) -- "Cleanup direction"
  paragraph. Fixed correctly; same "for a frame" imprecision (line 197).
  Finding 3.
* Lines 397-403 (pre-change, "Concrete state" section) / 412-418
  (post-change) -- third instance, *not* named in the integration review's
  Section 11 inventory (which listed only the first two). Legitimately
  discovered during implementation; correctly fixed; same "for a frame"
  imprecision (line 415-416). Finding 3.

The already-correct Workbook-View reimport paragraph (now lines 150-157,
"Reimport cleanup: `apply_derived_column_policy` reads the rich
`_meta.derived...` provenance...") was **not** touched and remains
semantically correct -- matches the integration review's `ALREADY CURRENT`
classification.

Validation/reorder semantics (`validate_fk_helpers`, `reorder_fk_helpers`,
`bootstrap_meta`) are not touched by this diff and are not misrepresented
as changed by this workstream. Historical evolution text (FK Helper Slice 2
v1-retirement narrative) is untouched and remains historical.

*Verdict: all three instances genuinely stale, all now match runtime on
the central claim; recurring "for a frame" imprecision (Finding 3, MINOR).*

=== 3.3 `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`

This is genuinely current, living architecture documentation (ch06 trace
of the domain-transformation layer), not a historical artifact. The
updated passage (lines 943-950) matches `fk_helpers/drop.py`'s actual
behavior: "removes columns identified only by current derived provenance;
the v2 policy never independently authorizes deletion. Policy is consulted
only to raise a precondition error when both policy and provenance are
absent for a frame ... and to log a non-raising diagnostic ...". It
correctly preserves policy's relevance to the configuration/precondition
and diagnostic role (does not claim policy is irrelevant to
`remove_fk_helpers` altogether), and explicitly states `reorder_fk_helpers`
is unaffected (untouched lines immediately following). Same "for a frame"
imprecision as 3.1/3.2 (Finding 3).

*Verdict: correct disposition as a real MUST-UPDATE instance beyond the
integration review's checklist; same recurring imprecision (Finding 3).*

=== 3.4 `docs/functional_model/ch04_use_case_concepts/fk_helpers.adoc`

The "Drop FK Helpers" concept (lines 89-118) now correctly distinguishes
declaration/materialization intent from physical deletion evidence:
runtime provenance is "the only source that authorizes FK-attributed
deletion"; the durable marker (Workbook-View) is described as "a
structurally distinct, independently authorized cleanup declaration ...
not a fallback reconstruction of FK provenance"; FK relation declaration
is explicitly "never consulted for deletion identity" and no longer
"last-resort reconstruction". No implication remains that absence of
authority should cause guessing ("the system does not guess silently").
Terminology fits the Functional Model level and does not leak excessive
implementation mechanics (it does not, for instance, quote the Python
precondition expression the way the technical-model doc does).

The wording "current and truthful for this run (or a still-valid earlier
run)" appears twice (lines 90-92, 96-98). This phrase is not supported by
the accepted design vocabulary and is flagged as Finding 4 (MINOR,
ambiguity risk).

*Verdict: correct on declaration-vs-evidence distinction and on Workbook-View
being a distinct, not FK-derived, authority; one unsupported temporal
qualifier (Finding 4).*

== 4. Registry review

`registries/pipeline_step_registry.json`, `apply_derived_column_policy`
entry (lines 1769, 1824):

* `purpose` (line 1769): truthfully states the accepted deletion
  identities (derived provenance, durable workbook-view declarations) and
  correctly demotes durable FK policy to "alone never authorizes
  deletion". Matches runtime.
* `meta_contract.reads[].notes` for `helper_policies` (line 1824): **does
  not** match the actual DCP implementation. It claims DCP is "read only
  to raise a precondition error when both policy and transient derived
  provenance are absent for a frame" -- but `apply_derived_column_policy`
  has no such raise path anywhere (verified by exhaustive `grep -n raise`
  over `derived_column_policy.py`; every raise site is either the
  `fail_on_mismatch` value-mismatch path or an unrelated config-validation
  `ValueError`). That precondition/raise exists only in
  `drop_helpers`/`remove_fk_helpers` (`policy.py:missing_fk_policy_error`,
  `drop.py:54-55`). This is exactly the failure mode the review brief
  flagged as the high-value attack area: direct-executor
  (`remove_fk_helpers`) precondition semantics copied into the DCP
  registry entry, where DCP does not implement them. **Finding 1,
  MATERIAL.**
* `meta_contract.writes: []` for `apply_derived_column_policy`: unchanged
  by this commit, remains the known pre-existing descriptive gap the
  integration review classified `OPTIONAL CLARIFICATION` (not required for
  correctness, not worsened here).
* JSON parses cleanly (`python3 -m json.load`); no unrelated field
  changed (diff is exactly the two `purpose`/`notes` string edits).

A second, more severe registry problem was found in the **same file**,
in an entry Slice 6 did *not* touch: the `remove_fk_helpers` step entry
(lines 246-325) still carries the pre-Slice-4 durable-policy-fallback
claim verbatim:

* `purpose` (line 254): "...**When provenance is absent on a sheet, the
  v2 relation policy at `_meta.helper_policies.fk` supplies the declared
  helper columns to remove.**..." -- this is the exact stale claim Slice 6
  was created to eliminate, still present, contradicting
  `drop.py:_columns_to_drop_by_sheet` (deletion candidates come only from
  `provenance_by_sheet`; `relations` is never read into the drop set).
* `meta_contract.reads[1].notes` (line 279): "v2 relation entries used as
  the cleanup fallback when per-sheet provenance is absent." -- same stale
  claim, for the same step.

This entry sits in the identical file, describing the identical step
(`remove_fk_helpers`/`drop_helpers`), whose stale claim in five *other*
canonical docs this very commit corrected. It now directly contradicts
`docs/ai_info/concepts_fk_helpers.adoc`,
`docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`,
`docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`,
and the corrected `apply_derived_column_policy` entry two thousand lines
below it in the same JSON file. This was not in the integration review's
Section 11 inventory (which audited `pipeline_step_registry.json` but only
flagged the `apply_derived_column_policy` `writes: []` gap) and was missed
by the Slice 6 implementation pass. **Finding 5, MATERIAL.**

== 5. Release-note review

New bullet under `== unknown -- planned` (confirmed via `grep -n "^== "`:
the bullet sits between line 17's `== unknown -- planned` and line 568's
`== 0.2.1 (released)`; placement correct per the release-notes-checkpoint
placement rule). Released sections (`0.2.1`, `0.2.0`) untouched.

Content check:

* OLD behavior described accurately: "a durable relation that merely
  *named* a column ... could cause that column to be dropped even without
  current evidence that this pipeline run ... actually produced it" --
  matches the pre-Slice-4 behavior the accepted design's repros
  (same-run collision, durable-policy-only) documented.
* NEW behavior accurately stated and matches Section 2's re-verified
  runtime contract.
* Safety motivation (risk of "deletion of a same-label foreign column a
  durable relation happened to also name") is supported by the accepted
  design's Repro 1/4 (same-run collision / same-run staleness via
  decoupled `enrich_lookup` rebind).
* Migration/persistence consequence is accurate and consistent with
  `persistence_boundary.py`'s unconditional pruning of `derived`.
* Workbook-View migration route (`configure_workbook_view` →
  `_meta.sheets[*].helper_columns`) correctly identified as unaffected.
* No claim implies transient provenance survives generic persistence --
  the bullet is explicit that it does not.

AsciiDoc rendering check: built the changed file with `asciidoctor 2.0.23`
(no warnings/errors) and inspected the generated HTML. Both
`_meta.derived.sheets.\*.helper_columns` and `_meta.sheets[*].helper_columns`
render as literal `<code>` text (confirmed the escaped `\*` prevents the
asterisk from being parsed as emphasis markup); no accidental bold/italic
introduced, no path text altered for readers.

*Verdict: ACCEPT -- accurate, correctly placed, correctly escaped.*

== 6. Persistence / authority consistency

Cross-checked all persistence/reimport statements in the diff against
`persistence_boundary.py` (Section 2). None of the touched docs claim:
all deletion authority disappears across persistence; Workbook-View
`helper_columns` is transient; the FK policy itself disappears at
persistence; or that fresh post-reimport materialization could never
re-establish new transient provenance. The narrowing documented is exactly
the accepted one: durable policy surviving reimport cannot *by itself*
regain deletion authority, because `_meta.derived` (not
`_meta.helper_policies` or `_meta.sheets[*].helper_columns`) is what gets
pruned.

Across the diff, no passage collapses DCP's authority model into "only FK
provenance may ever delete helpers" -- the `apply_derived_column_policy`
registry `purpose` field and the functional-model doc both explicitly keep
Workbook-View declarations as a second legitimate identity source, and
`enrich_lookup`'s own provenance (Section 2) is untouched and undisputed
elsewhere. `remove_fk_helpers`-focused passages correctly do not attempt
to enumerate DCP's other authorities (not required, per the review brief).

*Verdict: ACCEPT for the touched docs.* (Finding 5's stale
`remove_fk_helpers` registry entry is a persistence-adjacent
authority-claim defect, but it is a pre-existing untouched entry, not a
new persistence-language regression introduced by this diff -- tracked
separately under Section 4/Finding 5.)

== 7. Current-doc stale-text search (Area 11)

Searched `docs/ai_info/`, `docs/developer_guide/`, `docs/functional_model/`,
`docs/semantic_model/`, `docs/technical_model/`, `docs/user_guide/`,
`docs/release_notes/`, `registries/` (excluding `docs/cold_storage/` and
`docs/warm_storage/global_reviews/`) for the four listed stale-claim
patterns.

Results:

* `registries/pipeline_step_registry.json`'s `remove_fk_helpers` entry --
  genuine, live "durable policy supplies deletion candidates" claim.
  Reported as Finding 5 (Section 4).
* `docs/semantic_model/12_contract_matrices.adoc` `reorder_fk_helpers` rows
  and `docs/semantic_model/04_ambiguity_policy.adoc` /
  `05_external_inspirations.adoc` prefix-fallback mentions: these describe
  the unrelated, already-known, already-retired *prefix* fallback (a
  distinct, previously-closed concern), not the "durable policy authorizes
  deletion" claim this workstream addresses. Correctly out of scope, per
  the integration review's own `HISTORICAL ARTIFACT` / unrelated
  classification for `reorder_fk_helpers`.
* `docs/user_guide/ch03_transformation_catalog/03_supporting_steps.adoc`:
  re-confirmed clean (no durable-fallback claim for either
  `remove_fk_helpers` or `apply_derived_column_policy`), matching the
  integration review's `ALREADY CURRENT`.
* `registries/meta_registry.yaml` `helper_policies` entry
  (`precedence_notes`, lines 426-433): re-confirmed it only says
  FK-helper primitives "consume ... the v2 relation policy ... (plus
  derived helper provenance ...)" -- a true, non-directional statement
  that does not claim policy independently authorizes deletion. Correctly
  left unchanged (Section 8 / Area 14).
* `registries/domain_contracts/{canonical,staging}/transformations.json`
  `drop_helpers` entries: pre-v2-era draft/seed content (empty
  `meta_output_content`/`meta_output_loss_risks` fields for `drop_helpers`
  itself); does not assert the durable-fallback claim and is not part of
  the reviewed living-contract surface named in the integration review's
  Section 11 inventory. Not flagged as a Slice-6 finding.
* One pre-existing, **not introduced by `daf27bc`**, stale sentence:
  `docs/ai_info/concepts_fk_helpers.adoc:18-19` (untouched by this commit):
  "A pipeline that uses any FK-helper primitive must therefore run a
  producer step earlier in the same pipeline (**or rely on persisted
  provenance written by an earlier run, as the reimport pipeline does**)."
  "Provenance" here is the same term the preceding sentence uses for
  `_meta.derived.sheets.*.helper_columns` derived provenance, which
  `persistence_boundary.py` unconditionally prunes -- it cannot be
  "persisted" or survive "an earlier run" via reimport. This reads as a
  residual instance of the same conflation this workstream exists to
  remove, sitting eleven lines above the passage Slice 6 fixed in the same
  file. Reported as **Finding 6 (MINOR-leaning-MATERIAL, pre-existing, not
  a Slice-6 regression)** -- recommend a fast follow-up correction, not a
  reopening of Slice 6.

== 8. Historical-artifact preservation

Confirmed via `git show --stat` and targeted diffs that no file under
`docs/cold_storage/`, `docs/warm_storage/global_reviews/`, or
`docs/backlog/` (open or done) was touched by `daf27bc`. The accepted
design, both design reviews, and all Slice 1-5 implementation/integration
review artifacts are untouched. No FTR/BUG historical record was rewritten.
Historical evolution narrative inside `fk_helper_metadata.adoc` (FK Helper
Slice 2 v1-retirement passage) is untouched and remains clearly historical.

*Verdict: ACCEPT -- no historical truth was rewritten.*

== 9. Validation

* `asciidoctor -o ... <file>` on all six changed `.adoc` files: 0
  warnings, 0 errors.
* `python3 -c "import json; json.load(open('registries/pipeline_step_registry.json'))"`:
  valid JSON.
* `git diff --check daf27bc~1 daf27bc`: clean, no whitespace errors.
* Targeted stale-text `grep` sweeps across `docs/` and `registries/`
  (Section 7).
* Full runtime/architecture test suite was not run (not required for a
  documentation-only diff per the review brief); the registry-JSON
  structural change is a value-only string edit within an existing schema
  shape, so no registry-architecture-test regression is expected from this
  diff itself -- this was not independently executed and is not claimed as
  verified.

== 10. Findings

[cols="1,6", options="header"]
|===
| # | Finding

| 1 (MATERIAL)
| `registries/pipeline_step_registry.json:1824` -- the `apply_derived_column_policy`
  entry's `helper_policies` read `notes` field attributes `remove_fk_helpers`'s
  own precondition/raise behavior ("raise a precondition error when both
  policy and transient derived provenance are absent") to
  `apply_derived_column_policy`, which implements no such raise anywhere
  (verified against `derived_column_policy.py`). *Risk:* a future reader
  or agent trusting this registry entry could believe DCP fails closed on
  missing configuration the way `remove_fk_helpers` does; it does not --
  DCP silently proceeds with no deletion candidates and only logs/finds a
  non-raising `fk_deletion_unauthorized` diagnostic. *Recommended action:*
  rewrite the note to describe only the non-raising
  `fk_deletion_unauthorized` diagnostic path; remove the "raise a
  precondition error" clause entirely for this entry.

| 2 (MATERIAL)
| `registries/pipeline_step_registry.json:254,279` -- the **untouched**
  `remove_fk_helpers` entry's `purpose` and `meta_contract.reads[1].notes`
  still state the pre-Slice-4 durable-policy-fallback claim ("the v2
  relation policy ... supplies the declared helper columns to remove" /
  "used as the cleanup fallback when per-sheet provenance is absent"),
  directly contradicting `drop.py` (Section 2) and every other doc this
  commit fixed. *Risk:* the same JSON file now asserts both the correct
  and the stale rule for sibling entries describing the same underlying
  step family, actively misleading a reader who checks `remove_fk_helpers`
  instead of `apply_derived_column_policy`. *Recommended action:* apply
  the same correction pattern used for `apply_derived_column_policy` to
  this entry as an immediate follow-up (not a Slice 6 reopening --
  Slice 6's own diff is otherwise sound; this is a missed sibling entry in
  the same file).

| 3 (MINOR)
| `docs/ai_info/concepts_fk_helpers.adoc:68`,
  `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc:136,197,415-416`,
  `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc:947`,
  `registries/pipeline_step_registry.json:1824` -- all describe the
  `drop_helpers` precondition (`relations is None and not
  provenance_by_sheet`) as raising "when policy and provenance are both
  absent *for a frame*". The check (Section 2) is evaluated once over the
  whole `frames` argument -- `relations` covers the entire
  `_meta.helper_policies.fk` block and `provenance_by_sheet` covers every
  sheet at once -- not per individual frame/sheet. A reader could
  misinterpret this as a per-sheet guard that raises whenever one
  particular frame lacks both, which is not how the code behaves (a frame
  with no provenance simply drops nothing, silently, as long as some other
  frame or the global policy exists). Does not invert the authority
  direction. *Recommended action:* replace "for a frame" with "for the
  whole run" / "workbook-wide" in all five locations.

| 4 (MINOR)
| `docs/functional_model/ch04_use_case_concepts/fk_helpers.adoc:91-92,97-99` --
  "current and truthful for this run (or a still-valid earlier run)".
  The accepted design's central claim (Section F, and the integration
  review) is consistently phrased as strictly *same-run* truthful
  provenance; "still-valid earlier run" is new vocabulary not present in
  the accepted design, either design review, or the integration review,
  and is undefined (what makes an earlier run "still valid"?). It does not
  contradict the persistence rule outright (it does not claim survival
  across a save/reimport cycle), but it is a documentation-only expansion
  of the temporal scope beyond what the accepted design established, and
  risks being read as licensing broader provenance validity than the
  runtime model supports. *Recommended action:* drop the parenthetical, or
  replace with the design's own vocabulary (bind it explicitly to
  "same in-memory pipeline run, no persistence boundary crossed").

| 5 (pre-existing, not introduced by `daf27bc`)
| `docs/ai_info/concepts_fk_helpers.adoc:18-19` (untouched by this commit)
  -- "...or rely on persisted provenance written by an earlier run, as the
  reimport pipeline does." Contradicts `persistence_boundary.py`'s
  unconditional pruning of `_meta.derived` (Section 2); derived provenance
  is never persisted and cannot be "written by an earlier run" that a
  later reimport observes. *Risk:* sits eleven lines above the passage
  Slice 6 corrected in the same file, describing the identical channel.
  *Recommended action:* fast follow-up correction; does not block Slice 6
  acceptance since Slice 6 did not touch or introduce this line, but
  should not be left open indefinitely given its proximity to the just-
  corrected passage.
|===

== 11. Final verdict

*MATERIAL CORRECTION REQUIRED*

Rationale: Slice 6's own diff correctly closes every `MUST UPDATE IN
SLICE 6` item from the integration review's Section 11 inventory, plus two
legitimately self-discovered additional instances
(`architectural_layers.adoc` and the third `fk_helper_metadata.adoc`
passage), without touching runtime, tests, or historical artifacts, and
with clean AsciiDoc/JSON validation. However, Finding 1 is a direct
instance of the review brief's own named high-value attack pattern
("registry text attributes direct-drop precondition behavior to DCP that
DCP does not actually implement") and Finding 2 leaves a live,
un-corrected instance of the exact stale claim this workstream exists to
remove, in the same registry file, for the sibling step. Both are
current-documentation contract-matrix-adjacent surfaces
(`registries/pipeline_step_registry.json`), not historical artifacts, and
both create a genuine, discoverable contradiction between docs, registry,
and runtime -- squarely a MATERIAL verdict condition per the review brief.

Findings 3, 4, and 6 are MINOR / pre-existing and do not by themselves
change the verdict, but should be corrected in the same follow-up pass for
efficiency.

== 12. Next-step authorization

Slice 6 documentation closure: **NOT YET ACCEPTED** -- pending a small,
targeted follow-up correcting Findings 1 and 2 in
`registries/pipeline_step_registry.json` (and, ideally, Findings 3/4/6 in
the same pass, since they are localized string edits in already-identified
locations).

Deletion-authority workstream: **NOT YET READY FOR PROJECT-MEMORY/ROADMAP
CLOSURE.** Do not start Project Memory closure and do not resume the
interrupted DMC FK-helper hardening cycle until a follow-up documentation
correction closes Findings 1 and 2 and this artifact (or a successor) is
updated to ACCEPT / ACCEPT WITH MINOR.

== 13. Finding-closure review (correction commit `4b4aeb9`)

Targeted, bounded closure review. Does not redo the full Slice-6 review;
re-examines only the corrected passages plus the minimum collateral surface
needed to confirm no new contradiction was introduced.

Correction commit::
`4b4aeb94511ac6452572d31920aa75e4b09e5fdc` -- "docs(domain): correct
deletion authority closure findings".

=== 13.1 Scope check

`git show --stat 4b4aeb9` touches exactly:

* `registries/pipeline_step_registry.json`
* `docs/ai_info/concepts_fk_helpers.adoc`
* `docs/functional_model/ch04_use_case_concepts/fk_helpers.adoc`
* `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`
* `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`
* `docs/release_notes/release_notes.adoc`

No other file changed. No runtime Python, test, historical/backlog, or
Project Memory artifact touched. Matches the expected file set exactly.

=== 13.2 FIND-1 -- DCP registry precondition

`registries/pipeline_step_registry.json:1824`
(`apply_derived_column_policy` -> `helper_policies` read note), new text:

> "Durable FK helper policy never independently authorizes deletion.
> `apply_derived_column_policy` reads it only to identify durable-policy-named
> physical columns that remain after the accepted deletion authorities
> (derived provenance, workbook-view declarations) have run; those may
> surface as the non-raising `fk_deletion_unauthorized` diagnostic. This
> entry implements no missing-policy/provenance precondition or
> exception -- that raise belongs only to `remove_fk_helpers`."

Re-traced against `derived_column_policy.py`:

* Exhaustive `grep -n raise` over the module confirms no
  `MissingFkRelationPolicyError`-equivalent raise exists in DCP; every raise
  site is either the `fail_on_mismatch` value-mismatch path or an unrelated
  config-validation `ValueError`. The note's closing clause is now
  accurate.
* `_fk_deletion_unauthorized_candidates` (lines 477-507) checks durable
  policy names against `cleaned` -- the frame *after*
  `enforce_derived_column_policy_frame`'s own drop -- so "columns that
  remain after the accepted deletion authorities ... have run" is a
  precise paraphrase of the function's own docstring ("Presence is checked
  against the post-drop `cleaned` frame ... any column a different,
  currently accepted deletion authority ... has already removed is
  physically gone from `cleaned`").
* "may surface as the non-raising `fk_deletion_unauthorized` diagnostic" is
  accurate: `warn_on_mismatch` gets a findings-frame entry;
  `drop`/`fail_on_mismatch` get a log warning only; it never raises under
  any policy (`_fk_deletion_unauthorized_findings`, lines 441-442).

No missing-policy/provenance precondition is attributed to DCP anywhere in
the corrected note. *FIND-1: CLOSED.*

=== 13.3 FIND-2 -- `remove_fk_helpers` registry fallback claim

`registries/pipeline_step_registry.json:254` (`purpose`) and `:279`
(`helper_policies.fk.relations` read note), both rewritten. Re-traced
against `drop_helpers`/`_columns_to_drop_by_sheet`
(`fk_helpers/drop.py:37-96`):

* New `purpose`: "deletion candidates come only from this transient,
  per-sheet provenance -- the v2 relation policy ... never independently
  supplies columns to remove." Matches
  `_columns_to_drop_by_sheet(provenance_by_sheet)` exactly -- `relations`
  is never read into the drop set.
* "The precondition check runs once across the whole cleanup invocation:
  if durable policy is entirely absent and no sheet carries provenance, a
  clear error is raised" -- matches `relations is None and not
  provenance_by_sheet` (line 54), evaluated once over the whole `frames`
  argument, not per sheet.
* "Where an individual sheet lacks provenance but durable policy still
  names a column present on that sheet, the column is left in place and a
  non-raising diagnostic warning is logged" -- matches
  `_log_durable_policy_only_survivors` (lines 99-132) exactly, including
  the "only fires when the named column is actually physically present"
  behavior.
* New `helper_policies.fk.relations` note restates the same three facts
  (never independently supplies candidates; gates the whole-invocation
  precondition; drives the non-raising survivor diagnostic) without
  reintroducing "fallback" language.

No remaining "durable policy supplies/fallback" claim in either field.
*FIND-2: CLOSED.*

=== 13.4 FIND-3 -- precondition granularity ("for a frame")

Re-checked all four named locations plus the corrected registry text:

* `docs/ai_info/concepts_fk_helpers.adoc:71-74` (renumbered by the earlier
  edit in the same commit): "is evaluated once across the whole cleanup
  invocation (the complete frames input), not per sheet; it raises only
  when policy and provenance are both entirely absent".
* `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc` -- all
  three corrected passages (producer/consumer paragraph, cleanup-direction
  paragraph, and the "Concrete state" bullet) now use "evaluated once
  across the whole cleanup invocation, not per sheet" / "entirely absent"
  consistently.
* `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc:945-949`:
  same "evaluated once across the whole cleanup invocation, not per sheet"
  phrasing.
* `registries/pipeline_step_registry.json:254,279`: covered in 13.3, uses
  the same whole-invocation framing.

None of the five locations overcorrects into the three rejected failure
modes: none implies a per-sheet raise, none implies the global policy
block is itself per-frame, and none implies a single provenance-less
sheet triggers failure while another sheet or the global policy carries
authority -- each explicitly ties the raise to *both* policy and
provenance being *entirely* absent, evaluated once. *FIND-3: CLOSED.*

=== 13.5 FIND-4 -- temporal provenance vocabulary

`grep -rniE "still-valid|earlier run|earlier one"` across
`docs/ai_info/`, `docs/developer_guide/`, `docs/functional_model/`,
`docs/semantic_model/`, `docs/technical_model/`, `docs/user_guide/`,
`docs/release_notes/`, `registries/` returns zero remaining instances of
the old claim. The only hits are the corrected sentences themselves,
asserting the opposite (provenance "cannot be reused from an earlier run"
/ "cannot carry forward from an earlier run") and one unrelated hit in
`developer_guide/ch10_project_evolution/01_project_evolution.adoc:1008`
("builds on the earlier runtime-metadata persistence boundary" -- a
different sense of "earlier", unrelated to FK provenance lineage).

Functional-model doc (`fk_helpers.adoc:90-99`) now reads "current, truthful
transient producer provenance for this same in-memory pipeline run. It is
never persisted (generic persistence prunes `_meta.derived`), so it cannot
carry forward from an earlier run" and drops "(or a still-valid earlier
one)" from the FK-relation-declaration bullet. Release-notes bullet
(`release_notes.adoc:552-553`) makes the matching edit; diffed against the
prior text, only the "(or a still-valid earlier one)" clause was removed --
no other release-note content changed (confirmed by the minimal diff hunk
and by re-checking placement under `== unknown -- planned`, still line 17,
unaffected).

No stronger cross-run lineage contract was introduced; both edits bind
provenance validity strictly to the current in-memory run and correctly
cite the persistence-boundary pruning rule. *FIND-4: CLOSED.*

=== 13.6 FIND-5 -- persisted-provenance claim (high-value check)

`docs/ai_info/concepts_fk_helpers.adoc:15-24`, new text:

> "...run a producer step earlier in the same pipeline to obtain transient
> derived provenance for this run; that provenance is never persisted (the
> persistence boundary unconditionally prunes `_meta.derived`) and cannot
> be reused from an earlier run. What *does* survive persistence and
> reimport is durable metadata -- the v2 relation policy under
> `_meta.helper_policies.fk` and the Workbook-View marker under
> `_meta.sheets[*].helper_columns` -- but neither independently restores
> FK-attributed deletion authority; see the `remove_fk_helpers` rule
> below."

This was flagged as the highest-risk closure in the task brief: does
grouping the FK policy and the Workbook-View marker under one "neither
independently restores FK-attributed deletion authority" clause wrongly
deny Workbook-View's own independent authority (confirmed real, via DCP,
in Section 3.4/6 of the original review and re-confirmed against
`derived_column_policy.py` here)?

Read in context rather than in isolation, the sentence does not make that
error:

* The whole paragraph is explicitly scoped to "the *primitive* steps
  (`add_fk_helpers`, `reorder_fk_helpers`, `validate_fk_helpers`,
  `remove_fk_helpers`)" -- a closed, named list that structurally excludes
  `apply_derived_column_policy`. The claim "neither independently restores
  FK-attributed deletion authority" is made about *this* primitive family,
  and the sentence's own closing clause -- "see the `remove_fk_helpers`
  rule below" -- points the reader at the specific rule it is talking
  about, not a general claim spanning DCP.
* Within that family, the claim is literally true for both carriers:
  `remove_fk_helpers`/`drop_helpers` never reads Workbook-View
  `_meta.sheets[*].helper_columns` at all (confirmed -- `drop.py` has no
  reference to `sheets[*].helper_columns` or any Workbook-View symbol), so
  Workbook-View cannot "restore" an authority it was never a source of
  within this primitive; and durable FK policy is confirmed (Section 2)
  to never independently supply deletion candidates either.
* The sentence does not assert, anywhere, that Workbook-View lacks
  deletion authority in general -- it uses the qualified term
  "FK-attributed deletion authority" (an established term of art in this
  workstream's runtime docstrings, e.g. `derived_column_policy.py`:
  "only truthful, current transient FK provenance ... authorizes deleting
  an FK-attributed column"), not an unqualified "deletion authority".

Residual observation (not a new finding, does not reopen FIND-5): taken
completely in isolation from the rest of the documentation set, a reader
of only this one sentence could still momentarily wonder whether
Workbook-View has *any* independent authority, since this file does not
positively restate DCP's Workbook-View authority anywhere itself. This is
a stylistic completeness point, not a semantic error -- the claim as
written is accurate and appropriately scoped, and the file's very next,
already-verified bullet (Section 3.1) plus the functional-model doc
(Section 3.4) both correctly and explicitly state Workbook-View's
independent authority elsewhere in the same living-doc set. Not required
to reopen closure; noted for optional future tightening only.

The TRANSIENT/DURABLE distinction requested by the closure brief is
present and correct: transient derived provenance is bound to "this run"
and "never persisted"; durable FK policy and the Workbook-View marker are
both correctly described as surviving persistence, with FK policy's
non-authority stated accurately and Workbook-View's authority not denied
(only appropriately scoped away from this specific paragraph's subject).

*FIND-5: CLOSED.*

=== 13.7 Collateral consistency check

Targeted `grep` sweep for all six original stale patterns across
`docs/ai_info/`, `docs/developer_guide/`, `docs/functional_model/`,
`docs/semantic_model/`, `docs/technical_model/`, `docs/user_guide/`,
`docs/release_notes/`, `registries/` (cold-storage/warm-storage/backlog
excluded):

* "durable FK policy supplies helper columns to remove" -- zero hits.
* "cleanup fallback ... provenance is absent" -- zero hits.
* DCP raising on missing policy/provenance -- zero hits; the one
  registry passage using "raise" near `helper_policies`/`fk` in this
  context is the corrected `apply_derived_column_policy` note, which now
  explicitly *denies* the raise for DCP.
* "for a frame" near the drop precondition -- zero hits (the two
  remaining unrelated "for a frame" occurrences are in
  `xref_crosstable.adoc`, about axis-frame subsets, and in the pre-existing
  draft `registries/domain_contracts/{canonical,staging}/transformations.json`
  seed content about `enrich_fk_helpers` config shorthand -- neither is
  about the `drop_helpers` precondition and neither is part of the
  reviewed living-contract surface).
* Transient `_meta.derived` surviving persistence -- zero hits; all
  "earlier run" hits are the corrected sentences asserting non-survival
  (Section 13.5).
* "still-valid earlier run/earlier one" -- zero hits (Section 13.5).

No new collateral contradiction found. `reorder_fk_helpers`'s unrelated,
historical prefix-fallback language (`12_contract_matrices.adoc`,
`04_ambiguity_policy.adoc`, `05_external_inspirations.adoc`) is untouched
and was correctly not conflated with the deletion-authority fallback this
correction addresses.

=== 13.8 Validation

* `python3 -c "import json; json.load(open('registries/pipeline_step_registry.json'))"`:
  valid JSON.
* `asciidoctor` build of all five changed `.adoc` files: 0 warnings, 0
  errors.
* `git diff --check daf27bc 4b4aeb9`: clean.
* `.venv/bin/pytest tests/architecture/dependency_guards/test_pipeline_step_registry.py -q`:
  17 passed.
* Collateral stale-text sweep: Section 13.7, clean.

=== 13.9 Finding status

[cols="1,3", options="header"]
|===
| Finding | Status

| FIND-1 (MATERIAL -- DCP registry precondition)
| CLOSED

| FIND-2 (MATERIAL -- `remove_fk_helpers` registry fallback)
| CLOSED

| FIND-3 (MINOR -- precondition granularity)
| CLOSED

| FIND-4 (MINOR -- temporal provenance vocabulary)
| CLOSED

| FIND-5 (pre-existing -- persisted-provenance claim)
| CLOSED
|===

=== 13.10 Final Slice-6 verdict

*ACCEPT*

All five findings from the original Slice-6 documentation review are
truthfully closed against current runtime source, with no new material
contradiction introduced and no collateral regression in the surrounding,
already-accepted documentation surface. Scope of the correction commit is
exactly the six files it claims to touch; no runtime, test, or historical
artifact was changed.

=== 13.11 Next-step authorization

Slice 6 documentation closure: **ACCEPTED.**

Deletion-authority workstream: **READY FOR PROJECT-MEMORY/ROADMAP
CLOSURE.**

Project Memory / roadmap closure: **AUTHORIZED** as the next step -- not
performed in this review. Do not resume the interrupted DMC FK-helper
hardening cycle directly; the next step remains the small Project Memory /
roadmap closure recording completion of this subroutine, the DMC-hardening
return address, and the reconstructed `add_fk_helpers` /
`add_lookup_helpers` historical-genealogy and consolidation constraints, as
specified by the task brief.
