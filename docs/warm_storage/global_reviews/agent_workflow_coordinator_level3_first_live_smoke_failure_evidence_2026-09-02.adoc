= Agent Workflow Coordinator -- First Level-3 Live Smoke Failure Evidence
:toc:
:toclevels: 2

Status:: *FIRST LEVEL-3 AUTHORIZATION CONSUMED; SMOKE FAILED ON A LOCAL CLI
WORKSPACE/TRUSTED-DIRECTORY PREREQUISITE. NO RETRY IS AUTHORIZED. ONE BOUNDED
DETERMINISTIC SMOKE-WORKSPACE CORRECTION IS AUTHORIZED. LEVEL 4 IS NOT AUTHORIZED.*
Date:: 2026-09-02
Authorization:: `029f3ff3` -- `docs(workflow): accept smoke runner and authorize level 3`
Smoke root:: `/home/stefan/wfc-proofs/level3-codex-smoke-20260902-01`

== Consumed one-shot attempt

The authorization was consumed by exactly one authorized attempt. The real Codex
CLI process boundary was entered (`invocation_attempted=true`), but the attempt
failed with `smoke_status=transport_failure` and return code `1`. It did not time
out. The runner truthfully recorded no candidate result and no semantic validation:
`candidate_result_present=false` and `semantic_validation_succeeded=false`.

The durable `smoke-summary.json` records the material stderr:

[source,text]
----
Not inside a trusted directory and --skip-git-repo-check was not specified.
----

The result directory contains the emitted `structured-result.schema.json`.
`result-message.json` exists but is empty. No result carrier was obtained.

== Classification and limits of the evidence

This failure is a *local Codex CLI workspace/trusted-directory prerequisite*:
the disposable smoke workspace did not meet the CLI's Git/trusted working-directory
precondition. It is a Level-3 harness/environment compatibility discovery.

It is not evidence of OpenAI schema incompatibility, `RoutingResult`
incompatibility, semantic-validation failure, a Coordinator Run/Hop failure,
Git-authority failure, or a Level-4 E2E failure. The provider was *NOT
ESTABLISHED* as reached, and remote acceptance of the emitted structured-output
schema was *NOT ESTABLISHED*. Provider compatibility and real schema compatibility
therefore remain unproven. Slice-4A's coordinator happy path remains unproven.

The runner's `transport_failure` / invocation-attempted / no-candidate reporting
is truthful for this observed failure and does not reopen L3SR-R1 through L3SR-R4.
The accepted Level-2 decision is unchanged: Level 2 is *NOT REQUIRED / NOT
PURSUED BY DESIGN*; HTTP/API/provider implementation mocking behind the configured
CLI remains outside this system boundary and is neither a gap nor technical debt.

== Disposition and bounded correction authority

No retry, replacement smoke root, `--skip-git-repo-check`, ad-hoc command-line
retry, or second Level-3 invocation is authorized. Level 4 is not authorized.

One deterministic correction is authorized for the smoke runner only: after it
creates the fresh disposable smoke workspace and before `adapter.execute()`, it may
initialize that workspace as a fresh local Git repository (for example, with
`git init`). This is minimum environmental scaffolding for the external CLI, not
Coordinator Git workflow or Git-authority behavior. The correction must not add,
stage, or commit files; inspect or compare `HEAD`; use coordinator Git facts or
helpers; dispatch/reconcile; or add Git evidence semantics.

The later correction hop must use deterministic fake-executable coverage for the
initialized `.git` workspace, exactly one execution, existing-root pre-execution
blocking, absence of Coordinator Git semantics, and retained R1--R4 truthfulness.
Where deterministic and inexpensive, it must also cover Git-initialization failure
before execution. No real provider or installed real `codex` invocation is
authorized in that hop. If initialization does not satisfy the supported CLI
prerequisite, the implementation must stop and report; it may not broaden the
adapter by adding `--skip-git-repo-check`.

A subsequent live retry requires the correction, deterministic tests, bounded
review, and a new maintainer one-shot authorization.

== Required status

FIRST LEVEL-3 AUTHORIZATION CONSUMED:: YES
FIRST LEVEL-3 SMOKE:: FAILED
FAILURE CLASS:: LOCAL CLI WORKSPACE / TRUSTED-DIRECTORY PREREQUISITE
INVOCATION ATTEMPTED:: YES
RETURN CODE:: 1
CANDIDATE RESULT:: ABSENT
RESULT MESSAGE:: EMPTY
SCHEMA EMITTED:: YES
PROVIDER REACHED:: NOT ESTABLISHED
REAL SCHEMA ACCEPTANCE TESTED:: NOT ESTABLISHED
RETRY AUTHORIZED:: NO
BOUNDED DISPOSABLE-GIT-WORKSPACE CORRECTION AUTHORIZED:: YES
PRODUCTION `--skip-git-repo-check` CHANGE AUTHORIZED:: NO
SECOND LEVEL-3 INVOCATION AUTHORIZED:: NO
LEVEL 1:: REQUIRED / ACCEPTED
LEVEL 2:: NOT REQUIRED / NOT PURSUED BY DESIGN
LEVEL-2 ABSENCE TECHNICAL DEBT:: NO
LEVEL 4 AUTHORIZED:: NO
SLICE-4A HAPPY PATH PROVEN:: NO
PROJECT MEMORY UPDATED:: NO
REAL PROVIDER INVOCATIONS IN THIS DOCUMENTATION HOP:: 0
