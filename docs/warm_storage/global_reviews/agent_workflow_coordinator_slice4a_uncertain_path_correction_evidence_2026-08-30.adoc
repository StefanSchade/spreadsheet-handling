= Agent Workflow Coordinator -- Slice 4A Uncertain-Path Correction Evidence

Status:: *REMAINING S4A-R3/R4 CORRECTION IMPLEMENTED; BOUNDED RE-REVIEW PENDING.*
Date:: 2026-08-30
Review baseline:: `5daeaa46`.
Correction implementation:: `dd3d73d1` -- `fix(workflow): wire slice 4a uncertain recovery`.

== Remaining correction

`run_real_one_hop()` now atomically publishes the existing external Run snapshot and existing correlated `DispatchMarker` before the real subprocess boundary. It preserves the successful path by delegating to the sole `vertical.run_one_hop()` composition.

When the process started but produces no valid correlated result, the driver observes actual Git state with existing `observe_hop()`, calls existing `recover_uncertain_dispatch()`, receives existing `require_reconcile()` semantics, and atomically writes the resulting Run snapshot and checkpoint. `RealHopRun` truthfully represents either a successful `VerticalRun` or an unreduced `reconcile_required` recovery; no fake routing result is created.

An executable launch failure is caught as local pre-acceptance failure: the provisional dispatch marker is removed, the error remains fail-closed, and no Hop is charged.

== Deterministic evidence

The end-to-end fake-executable test calls `run_real_one_hop()` and exits 7 without writing a result or mutating the fixture. It proves exactly one execution, one charged Hop, unchanged HEAD and worktree content, external `dispatch.json`/`run.json`/`checkpoint.json`, `status=reconcile_required`, and `stop_reason=reconcile_requires_explicit_resume`.

The pre-spawn regression test drives an unlaunchable executable through the real driver and proves `hop_used=0` with no residual dispatch marker. Existing fake-executable success composition remains green.

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator
  passed
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator
  All checks passed
git diff --check
  exit 0
----

REAL CODEX/PROVIDER INVOCATIONS PERFORMED: 0

This correction authorizes neither a new disposable proof nor routine real-repository use, Slice 5, or autonomous multi-Hop execution.
