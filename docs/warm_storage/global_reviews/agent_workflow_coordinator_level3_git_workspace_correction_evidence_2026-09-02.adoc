= Agent Workflow Coordinator -- Level-3 Disposable Git Workspace Correction Evidence
:toc:
:toclevels: 2

Status:: *WORKSPACE CORRECTION IMPLEMENTED; AWAITING BOUNDED INDEPENDENT REVIEW.
FIRST LIVE AUTHORIZATION REMAINS CONSUMED. SECOND LEVEL-3 INVOCATION AND LEVEL 4
ARE NOT AUTHORIZED.*
Date:: 2026-09-02
Authorization baseline:: `212d7f05` -- `docs(workflow): record level 3 workspace failure`

== Trigger and bounded correction

The first and only authorized Level-3 live smoke failed at the external Codex CLI
working-directory prerequisite: `Not inside a trusted directory and
--skip-git-repo-check was not specified.` The durable triggering record is
`agent_workflow_coordinator_level3_first_live_smoke_failure_evidence_2026-09-02.adoc`.

The runner now initializes its freshly created disposable `workspace/` with the
normal local Git executable using `git init`, before `CodexCliAdapter`
construction or `adapter.execute()`. The disposable Git repository exists solely
as external CLI environmental scaffolding. No files are added, staged, or
committed, and no Coordinator Git workflow or Git-authority semantics are
exercised.

The production `CodexCliAdapter` is unchanged. `--skip-git-repo-check` was not
added to production argv. No real Codex CLI or provider was invoked.

== Deterministic coverage

Focused fake-executable coverage confirms:

* the workspace is a real Git repository before fake execution, using local
  `git rev-parse --git-dir`;
* the successful path invokes the deterministic fake exactly once;
* no commit exists at `HEAD` after setup and fake execution;
* an existing smoke root blocks before workspace creation, Git initialization,
  and adapter execution;
* a simulated Git-initialization failure blocks adapter/fake execution, records
  `invocation_attempted=false`,
  `configured_executable_smoke_succeeded=false`, and
  `smoke_status=local_pre_execution_error`, and writes a durable summary; and
* the existing R1--R4 truthfulness regressions remain covered.

Only local Git and deterministic fake executables are subprocesses in these tests.
Installed real `codex` was not executed.

== Required status

WORKSPACE CORRECTION IMPLEMENTED:: YES
FIRST LIVE AUTHORIZATION REMAINS CONSUMED:: YES
SECOND LEVEL-3 INVOCATION NOT AUTHORIZED:: YES
LEVEL-3 PROVIDER/SCHEMA COMPATIBILITY STILL UNPROVEN:: YES
LEVEL-4 NOT AUTHORIZED:: YES
LEVEL 2:: NOT REQUIRED / NOT PURSUED BY DESIGN
COORDINATOR GIT SEMANTICS:: NONE
COMMITS CREATED IN SMOKE WORKSPACE:: NONE
REAL CODEX INVOCATIONS:: 0
REAL PROVIDER INVOCATIONS:: 0

== Validation

`.venv/bin/pytest -q tests/unit/workflow_coordinator/test_codex_adapter_smoke.py`:: PASS -- 10 passed

`.venv/bin/pytest -q tests/unit/workflow_coordinator`:: PASS -- 122 passed

`.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator`:: PASS

Strict AsciiDoc rendering with `asciidoctor --failure-level=ERROR` for this
artifact and the changed FTR:: PASS

`git diff --check`:: PASS

This artifact deliberately does not include its own future commit hash.
