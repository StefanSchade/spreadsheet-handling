= Agent Workflow Coordinator -- Slice 3 Implementation Evidence
:toc:

Status:: *IMPLEMENTED; INDEPENDENT IMPLEMENTATION REVIEW NEXT.*
Date:: 2026-08-28
Baseline:: `4171a52` -- `docs(workflow): authorize coordinator slice 3`
Implementation commit:: `fb2c012` -- `feat(workflow): implement coordinator slice 3`
Authority:: Accepted FTR section 11.3, accepted architecture, and RRQ-01 calibration.

== Authorized Slice-3 scope implemented

This adds only the N=1 fake/subprocess boundary under `scripts/workflow_coordinator/`: phase-selected prompt components, bounded omission manifests, correlated structured result validation, independently observed Git and selected fixed evidence, existing reducer application, and existing compact checkpoint projection. The adapter cannot select routes, alter profile/policy, or supply Git facts.

There is no live OpenAI/Codex/Claude invocation, authentication, provider/model routing, session continuation, quota logic, or autonomous multi-Hop execution. `fresh` is the only context value used by this seam.

== Contract coverage and evidence

`tests/unit/workflow_coordinator/test_slice3_vertical.py` proves selected role/modifier/policy ordering; generated identity/scope/route/evidence/output contract; unselected-guidance absence; malformed/truncated/correlation-mismatched result rejection; fake and local subprocess seams; N=1 reduction/checkpoint; and profile-selected fixed evidence observation. `durable_artifact=required` rejects a missing artifact and accepts an in-scope committed artifact; `none` permits a tiny fake Hop without a document.

[source,bash]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator tests/architecture/semantic_invariants/test_workflow_coordinator_replays.py -rA
----

This passed 90 tests. Ruff passed for new Slice-3 files. Black could not run because its local executable's interpreter is absent; this is an environment observation, not a test waiver.

== RRQ-01 boundary proof

The calibrated values are exact: 65,536 bytes per selected component, 196,608 bytes for optional bounded content, and 65,536 diff bytes or more than 12 changed files for inline diff evidence. Tests cover just below/above each byte boundary and 12/13 changed files. Every excluded component, context item, or diff has a path/artifact/Git retrieval reference, size, and reason in the omission manifest. Nothing is silently truncated; generated routing-critical structured data lies outside the optional pool.

== WFC-GIT-08 proof

After adapter return, the coordinator reads every actual Git commit subject and requires a Conventional-Commit-shaped subject containing the WorkItem and Hop ID. Fixtures accept `feat(workflow): WI-1 H001 tiny change` and reject a subject without those identities. This is a coordinator postcondition; no hook or trailer was added.

== S1-R3 and residual observations

S1-R3 is addressed only at this fake/subprocess result boundary: raw results are strict JSON envelopes, revalidated before reducer use, and correlated to expected Run/Hop/invocation. Invalid, truncated, missing-field, and identity-mismatched results fail closed. Real-adapter transport and resume generalization remain Slice-4-gated.

The Codex-local implementation commit left an empty `.git/HEAD.lock`. It was observed and retained as instructed. Independent implementation review is next; this record neither independently verifies nor maintainer-accepts Slice 3 and does not authorize Slice 4.
