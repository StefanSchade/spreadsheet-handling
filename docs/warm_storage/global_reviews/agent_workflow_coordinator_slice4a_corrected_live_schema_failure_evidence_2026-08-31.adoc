= Agent Workflow Coordinator -- Corrected Slice 4A Live Schema-Failure Evidence
:toc:
:toclevels: 3

Status:: *EVIDENCE ONLY. THE SECTION 11.4.4 ONE-SHOT LIVE AUTHORIZATION AT
BASELINE `16036a19` IS CONSUMED; THE HAPPY PATH REMAINS UNPROVEN. NO RETRY,
RESUME, NEW PROOF, PROVIDER INVOCATION, OR FIELD-LEVEL SCHEMA PATCH IS AUTHORIZED
BY THIS RECORD.*
Date:: 2026-08-31
Authorization baseline:: `16036a19` -- `docs(workflow): authorize corrected slice 4a live proof`.
Retained proof root:: `/home/stefan/wfc-proofs/slice4a-happy-16036a19` (inspected read-only).
Authority:: `docs/backlog/FTR-AGENT-WORKFLOW-COORDINATOR-P5.adoc`, section 11.4.4.

== 1. Scope and non-actions

This record preserves facts from the one authorized corrected disposable live proof.
It makes no provider call and changes neither coordinator code nor Project Memory.
The retained proof root was inspected read-only.

== 2. Identity, authorization consumption, and launch provenance

`proof-summary.json` records one NEW Run of `WFC-S4-PROOF`:

* `run_id=RUN-2aa11937d22d48fb80d160378bb337eb`;
* `invocation_id=INV-f74422b833f94eaf88a16f1caf81d7cf`;
* `hop_id=H001`, `hop_used=1`, and `max_autonomous_hops=1`.

The retained stdout contains `thread.started` and `turn.started` before the HTTP
400 provider error and `turn.failed`. The real provider boundary was therefore
reached. Under the section 11.4.4 one-shot rule, authorization `16036a19` is
consumed regardless of this failure outcome.

`launch-provenance.json` records the bounded, non-cryptographic host/operator
context `mosh-server -> bash -> python` (PIDs 1884581, 1884582, 1907961), with
`claude_related=false`, `codex_related=false`, and `openai_related=false`. Within
the stated provenance scope, this is consistent with the required ordinary,
non-nested maintainer mosh launch. It is not represented as cryptographic
attestation.

== 3. Retained coordinator and Git facts

The actual coordinator outcome is `process_returncode=1`, `timed_out=false`,
`acceptance=uncertain`, `vertical_success=false`, and
`final_run_status=reconcile_required`, with stop reason
`reconcile_requires_explicit_resume`. The dispatch marker remains present.

The disposable repository remains at its baseline and final HEAD
`835c0bc2fdbcd9fe62041edd47e3b74ce7ac3773` (`docs(workflow): WFC-S4-PROOF
baseline`). It has no workflow commits or changed paths. `PROOF.md` still reads
`WFC-S4-PROOF: pending marker`; `git status --porcelain` is empty; no remotes and
no Git lock files are recorded. The trusted checkout association identifies the
same repository and checkout ID `fea7378e-c78b-4130-a2dd-75ccb6f04932`.

== 4. Exact provider diagnostic and distinction from R10

The retained stdout reports HTTP 400 with:

* error type `invalid_request_error`;
* code `invalid_json_schema`;
* parameter `text.format.schema`;
* context `('properties', 'result', 'properties', 'schema_version')`; and
* message `schema must have a 'type' key`.

The accepted code emits the nested node equivalent to:

[source,json]
----
"schema_version": {"const": 1}
----

This is distinct from the consumed R10 failure: R10 rejected the insufficiently
specified `properties.result` object. Here the provider proceeds beyond that
corrected object and rejects its nested `schema_version` node because it lacks a
type. The evidence does not establish that merely adding `type=integer` would be
a complete correction.

== 5. Disposition boundary

The happy path is still unproven. No live authorization remains and no immediate
field-level retry is authorized. The factual architectural signal is limited to
this: the current end-to-end coordinator proof is discovering provider/CLI
structured-output compatibility rules incrementally. Provider-conformance discovery
should be examined at a lower integration layer before another live coordinator
proof. Maintainer disposition/design of that provider-integration boundary is
required before any later authorization.

No concern event was added: this tactical proof lifecycle belongs in the FTR, Git,
and retained proof evidence, not Project Memory.
