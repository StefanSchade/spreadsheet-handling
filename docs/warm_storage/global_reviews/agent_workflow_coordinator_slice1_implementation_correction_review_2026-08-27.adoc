= Agent Workflow Coordinator -- Independent Bounded Slice 1 Correction Re-Review
:toc:
:toclevels: 2

Review kind:: Independent *bounded correction re-review* (not a new implementation census).
Review status:: *COMPLETED*.
Verdict:: *ACCEPT -- SLICE 1 IMPLEMENTATION VERIFIED*.
Direct material regression:: *NONE*.
Architecture/FTR reopening:: *NO*.
Date:: 2026-08-27.
Reviewer continuity:: Same Opus 4.8 context that authored the broad implementation
review and assigned S1-R1..S1-R5; retained deliberately for stable Finding
identity and bounded disposition.

[IMPORTANT]
====
This artifact verifies the bounded correction only. It readies Slice 1 for
maintainer lifecycle acceptance; it does not itself accept Slice 1, does not
authorize Slice 2, no live agent adapter, no autonomous multi-Hop execution, and
no DMC Family Cycle 2. The corrected implementation was treated as read-only and
is unchanged by this review.
====

== 1. Scope and continuity

Bounded to the four corrected findings (S1-R1, S1-R2, S1-R4, S1-R5), direct
regressions caused by the correction, and confirmation that the deferred S1-R3 is
unchanged and legitimately non-blocking. The broad Slice-1 census, the accepted
architecture/FTR, and the already-accepted A--J rationale were not reopened.
Findings were re-verified by reading the diff and by executing the real
reducer/persistence code (read-only probes), not by trusting the correction
evidence.

== 2. Commits reviewed

[cols="1,3",options="header"]
|===
|Role |Commit
|Prior implementation review (finding authority) |`60bf6bb` docs(workflow): review coordinator slice 1 implementation
|Bounded correction (under review) |`a8474cb` fix(workflow): correct coordinator slice 1 review findings
|Correction evidence |`5f3b9b3` docs(workflow): record slice 1 correction evidence
|===

Verified: branch `main`, HEAD `5f3b9b3` (clean); all three are ancestors of HEAD.
`git diff 60bf6bb..a8474cb` touches only `scripts/workflow_coordinator/` and
`tests/` (reducer +47/model +4/persistence +2/serialization +4, plus tests); no
`src/spreadsheet_handling` change anywhere in `a93ed12..HEAD`; no Slice 2--5
machinery.

== 3. Disposition table

[cols="1,1,1,4",options="header"]
|===
|ID |Sev |Disposition |Basis

|S1-R1 |Important |*CLOSED*
|`require_reconcile` now enforces reason-specific entry states:
`interruption` in `{READY,RUNNING,AWAITING_SUPERVISOR}` (plus idempotent
same-reason `RECONCILE_REQUIRED` re-entry), `prerequisite_return` in
`{SUSPENDED}`. `AWAITING_HUMAN` is rejected for both, and `human_question` is
preserved (`run.human_question or <default>`). One function/state path retained.

|S1-R2 |Important |*CLOSED*
|`permitted_route_keys` returns `()` for `AWAITING_HUMAN`,
`AWAITING_HUMAN_BUDGET`, `RECONCILE_REQUIRED`, `SUSPENDED`, `COMPLETED`,
`STOPPED`; `AWAITING_SUPERVISOR` still yields only supervisor-declared routes;
`READY`/`RUNNING` retain the finite phase routes.

|S1-R4 |Minor |*CLOSED*
|`_finding_operation` uses one deliberate operation vocabulary for both creation
(`open`->none, `resolved`->`resolve`, `residual`->`residual`,
`superseded`->`supersede`) and transitions. Incidental string-equality
dependency removed.

|S1-R5 |Minor |*CLOSED*
|`finding_delta_ids` and `evidence_refs` now flow
`Hop -> compact() -> HopSummary -> strict Run JSON -> checkpoint per-Hop row` as
compact reference tuples. Deterministic roundtrip preserved; no log/event
subsystem added.

|S1-R3 |Minor |*DEFERRED (non-blocking)*
|`reduce_result` still never reads `RoutingResult.outcome`; no `failed`/`blocked`
route policy was invented. Contract-consistent for Slice 1; unchanged from the
original assessment. Remains a Slice-3/real-agent-result hardening item.
|===

== 4. Evidence reproduced (read-only probes)

* S1-R1: the exact original bypass now fails closed --
`require_reconcile(AWAITING_HUMAN, "interruption")` raises "invalid from Run
status awaiting_human"; direct `resume_after_reconcile` raises "does not require
reconciliation"; the escalation question ("Who may accept this contract?")
survives. A carried `human_question` is preserved through a legitimate
interruption reconcile; idempotent same-reason re-entry is allowed; a reason
switch on a `RECONCILE_REQUIRED` run is rejected; crash-recovery
`recover_uncertain_dispatch` remains charge-once idempotent (`hop_used==1` on
first and second recovery).
* S1-R2: full `RunStatus`->routes map matches the table above.
* S1-R4: `GATE` granted `resolve`/`supersede`/`residual` creates the matching
state; `AGENT` still cannot create a non-`open` Finding.
* S1-R5: checkpoint row carries `finding_delta_ids`/`evidence_refs` and
`run_from_json(run_to_json(run)) == run` holds with the new fields.

== 5. Direct-regression check -- NONE

* Legitimate interruption not over-rejected: crash recovery enters from `RUNNING`
(post-charge) or idempotent re-entry; replay B and the stale-reality test enter
from `READY`. All pass.
* Prerequisite return intact (`SUSPENDED` -> reconcile; replays B/E green).
* Idempotent reconcile grants no authority escape: it is confined to same-reason
`RECONCILE_REQUIRED` and cannot transform a human stop; resume still requires no
anomalies and unchanged HEAD.
* No active-state route hidden (`READY`/`RUNNING` retain routes); supervisor
route visibility intact.
* Finding-creation authority change is the intended S1-R4 unification, bounded to
gate/human with the granted operation (agent still blocked) -- not an unexpected
widening and consistent with WFC-FIND-03.
* `HopSummary` schema change roundtrips deterministically; no Slice-2+ machinery
introduced.

== 6. A--J regression

All 11 replay cases green within the 57-test focused suite. B still proves
`interruption` and `prerequisite_return` share one reconcile mechanism, now from
legitimate entry states (`READY` and `SUSPENDED`) rather than the S1-R1-invalid
human stop. I still proves `outcome=completed` + non-null `semantic_authority`
escalation mechanically stops with no autonomous route or supervisor invocation.

== 7. Human Readability

The S1-R4 correction resolves the concrete finding: authority behavior is now
reconstructable from the explicit `creation_operations`/`transition_operations`
maps without relying on incidental spelling, and the reason-specific S1-R1 guard
reads plainly. No unnecessary abstraction was added; the proportionate-complexity
and anti-overengineering conclusions of the broad review stand. No further
readability sweep is warranted.

== 8. Validation

* Focused suite `tests/unit/workflow_coordinator` + replays: *57 passed*
(reducer 20, serialization 12, persistence 14, replays 11), reproduced locally.
* `git diff --check` clean over `60bf6bb..a8474cb`; artifact renders under
asciidoctor with no warnings; correction JSON/PM valid.
* Source purity: no `src/spreadsheet_handling` change; nothing outside
`scripts/workflow_coordinator/` + `tests/` in the correction commit.
* Correction evidence `5f3b9b3` claims cross-checked and accurate.

== 9. Verdict and next lifecycle action

*ACCEPT -- SLICE 1 IMPLEMENTATION VERIFIED.* S1-R1 and S1-R2 (Important) are
closed; S1-R4 and S1-R5 (Minor) are closed; S1-R3 remains a legitimately deferred
non-blocking Minor; no direct material regression; A--J remain green;
complexity/readability remain proportionate. No Blocking or Important finding
remains.

Next lifecycle action: maintainer Slice-1 lifecycle acceptance and a separate
decision on Slice-2 authorization. This review authorizes neither Slice 2, any
live agent adapter, autonomous multi-Hop execution, nor DMC Family Cycle 2. The
FTR remains Open/active (Slice 1 only); nothing moves to cold storage.
