= Review 004 -- Open-Work Triage
:toc: left
:toclevels: 2

[NOTE]
====
Severity uses the project's existing assessment scale (the heat-map
`impact_0_5` normalization: `none`=0, `cosmetic`=1, `low`=2, `medium`=3,
`high`=4, `critical`=5). Where a row maps to a single finding, the finding's
raw severity label is given in parentheses. *Urgency is deliberately not
assessed here* -- urgency depends on current project context and is decided
separately. `proposed_state` is drawn only from the agreed disposition set:
`keep_open`, `mark_resolved`, `mark_historical`, `mark_superseded`,
`merge_duplicate`, `add_missing_anchor`, `needs_decision`,
`candidate_for_next_slice`.

This is a read-only triage. No canonical project-memory JSON was edited.
====

== Scope of open work surveyed

Counts from canonical data at review time (commit `266e4bb`, working tree):

* `findings.json`: 195 rows -- status `open`=27, `accepted`=21, `in_arbeit`=15,
  `resolved`=2, `done`=92, `superseded`=38; current_relevance `current`=45,
  `partial`=15, `historical`=135.
* `ftrs.json`: 251 rows (241 FTR, 10 BUG) -- free-text status, 58 empty.
* Derived `current_findings.json`: 54 rows (40 current, 14 partial).
* One executable sentinel: `tests/roundtrip/test_pathological_cell_contracts.py`
  (`1 xfailed`, confirmed at review time).

== Triage table

[cols="3,2,2,1,4,3", options="header"]
|===
| id | current_state | proposed_state | severity | evidence | recommended_action

| BUG-UNRESOLVED-HELPER-VALUE-NAN-SEMANTICS-P4A
| status=new (header), xfail-guarded
| keep_open + needs_decision + candidate_for_next_slice
| high (major via FIN-TOPIC-META-PSR-002)
| `docs/backlog/BUG-...P4A.adoc` lists 3 contract options; sentinel `1 xfailed`; tracked by current major finding FIN-TOPIC-META-PSR-002
| Make the contract decision (option 1 empty/null leaning per maintainer). Until then keep the BUG open and stop the header record reading as implemented (see PMC-007).

| FIN-TOPIC-META-PSR-002
| status=open, relevance=current
| keep_open
| high (major)
| Unresolved FK-helper values collapse to literal `nan`; tracks BUG-...P4A + FTR-PAYLOAD-SEMANTICS-RECOVERY-P5
| Keep as the steering signal for the helper-value contract; resolve with the BUG decision.

| FIN-TOPIC-META-PSR-001
| status=open, relevance=current
| keep_open + candidate_for_next_slice
| medium
| Payload-semantics recovery needs a load-bearing metadata classification; tracks FTR-PAYLOAD-SEMANTICS-RECOVERY-P5 + FTR-META-LIFECYCLE-INVENTORY-P5
| Treat as the entry point for a minimal meta-boundary contract slice.

| FIN-TOPIC-MODEL-CMB-005
| status=open, relevance=current
| keep_open
| medium
| `frame_lifecycle.derived_from` / `produced_by` straddle pipeline-runtime vs canonical meaning
| Concrete meta/persistence-boundary gap; feed into the meta-boundary slice scoping.

| FIN-TOPIC-MODEL-SBR-004
| status=open, relevance=current
| keep_open
| medium
| Semantic Model ch08 persistence-boundary subsection described as under-specified
| Documentation-side anchor for the persistence boundary; pair with CMB-005.

| FTR-REVIEW-001-META-BOUNDARY-CLEANUP-P5
| status=open
| keep_open + candidate_for_next_slice
| medium
| Open meta-boundary FTR; co-located with the meta-near finding cluster
| Candidate host for the minimal meta-boundary contract slice.

| FTR-PAYLOAD-SEMANTICS-RECOVERY-P5
| status=new
| keep_open + needs_decision
| medium
| Classify payload-semantics-bearing metadata; named by PSR-001/002 tracking
| Decision-dependent on the FK-helper contract; do not start before BUG-...P4A direction lands.

| 15x `in_arbeit` findings (FIN-REVIEW-001-D/IR/A-*, FIN-TOPIC-MODEL-SBR-003)
| status=in_arbeit (non-vocabulary), relevance=current/partial
| needs_decision
| medium (mostly should_fix / nice_to_have)
| `in_arbeit` is not in the documented status vocabulary (PMC-001)
| Re-label to a documented status (`open` or `accepted`); decide whether `in_arbeit` is adopted or rejected.

| 5x superseded-but-current (FIN-REVIEW-003-S1-F-A02-1/2/4, S3-F-RR-A02-1/3)
| status=superseded, relevance=current
| mark_historical
| cosmetic (nice_to_have)
| status and current_relevance disagree; derived `current_findings` already drops them (45->40)
| Set current_relevance=historical to match the superseded status, or re-open if still live.

| FIN-REVIEW-002-P2-006
| status=superseded, superseded_by=FTRs
| add_missing_anchor
| low
| `superseded_by` points to FTR-FK-HELPER-POLICY-LIFECYCLE-P4A; FTR-META-LIFECYCLE-INVENTORY-P5 (FTRs, not findings)
| `superseded_by` must reference a finding; move the FTR linkage to a tracking field (PMC-003).

| FIN-REVIEW-001-D-002 / D-003
| status, resolved_by_ftr_id multi-valued
| merge_duplicate / add_missing_anchor
| low
| `resolved_by_ftr_id` (singular) holds two ids; `tracking_ftr_ids` contains prose "FK Helper Consolidation Closure"
| Split into flat values; drop the prose token (PMC-004/PMC-005).

| FIN-REVIEW-002-P1-003 / P1-005
| status=open, relevance=current
| keep_open
| medium
| Architecture-guard inventory + ARCH-LAYERS stale-module findings; tracked by FTR-GUARDRAIL-AUDIT-P5 / FTR-CONCEPT-DOCS-CLEANUP-P5
| Maintainability-hygiene cluster; keep open as heat-map signal, not a forced task.

| FIN-REVIEW-002-P3-002
| status=open, relevance=current
| keep_open
| medium
| Workbook option vocabulary triplicated; only current finding under CONC-ADAPTER-SPREADSHEET-BOUNDARY
| Keep; sits behind the domain/meta slice per concern posture.

| FK-helper lifecycle findings (FIN-TOPIC-FK-HELPER-FKFM-003/005, FKPI-006/007/008)
| status=open, relevance=current
| keep_open
| low-medium
| All track FTR-FK-HELPER-POLICY-LIFECYCLE-P4A
| Keep clustered under the FK-helper lifecycle FTR; not individually actionable now.

| FTR-FK-HELPER-POLICY-LIFECYCLE-P4A
| status=new, no commit evidence
| keep_open
| medium
| Named as durable host for the unresolved-helper contract (BUG-...P4A adoc, Related items)
| Keep open; it is the likely landing place for the contract documentation.

| ODS FTR cluster (FTR-ODS-PRESENTATION-SUBMODULE-EXTRACTION-P4A, -PRESENTATION-CELL-WALK-CONSOLIDATION-P4A, -CAPABILITY-PROFILE-P5)
| status=new
| keep_open (gated)
| low-medium
| io_backends review: Mild hotspot, "Do Not Do Yet" gates new visible structures behind these
| Keep as behavior-preserving prep; gate new visible carrier structures behind them (see technical_debt_gates.adoc).

| FTR-ODS-CONSUMER-FEATURE-PARITY-P4A
| status="implemented (audit complete) -- independent review accepted"
| mark_resolved
| low
| Status text says audit complete + review accepted; still listed as a dependency target
| Normalize to a documented terminal status (`done`); it reads as effectively closed.

| BUG-CROSS-CARRIER-META-ROUNDTRIP
| status="narrow e2 + e6 slice implemented (intent-vs-resolution)"
| needs_decision
| low-medium
| Partial-slice status as free text; cross-carrier meta boundary is a named architecture follow-up (commit cae02d7)
| Decide whether residual scope is a kept-open BUG or a new FTR; normalize the status field.
|===

== Decisions / accepted / rejected / unresolved

* `decisions.json` holds 3 active decisions (xref dependency table, release pin
  0.2.0, visible-cell string substrate ADR). All three resolve to real targets;
  no dangling decision targets. *No triage action* -- consistent.
* No `rejected` decision records exist; the model has no explicit
  rejected/declined disposition table. This is a *gap to watch*, not a finding
  for this slice (the prompt forbids new ontology).

== Blockers / dependencies

* `ftr_blockers.json` / `ftr_dependency_edges.json`: 3 edges, all from the
  project-memory bootstrap FTRs; endpoints resolve. Context-report dependency
  diagnostics are clean. *No blocker drift.*
* The only substantive product dependency is sequencing:
  FTR-PAYLOAD-SEMANTICS-RECOVERY-P5 is decision-dependent on
  BUG-UNRESOLVED-HELPER-VALUE-NAN-SEMANTICS-P4A (the helper-value contract is a
  concrete instance of the broader payload-semantics question).

== Release-note candidates

* `release_note_candidates.json` is derived and rebuilds cleanly. The only
  release-relevant open item is the FK-helper unresolved-value contract, which
  the BUG adoc already designates as a *Known limitation* until the contract
  lands. *No new release-note action* required by this triage.
