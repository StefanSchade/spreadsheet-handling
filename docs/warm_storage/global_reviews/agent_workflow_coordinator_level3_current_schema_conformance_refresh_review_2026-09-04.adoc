= Agent Workflow Coordinator — Level-3 Current-Schema Provider-Conformance Refresh Review
Independent evidence review, 2026-09-04
:toc: macro
:sectnums:

toc::[]

== Verdict

*A -- ACCEPT LEVEL-3 CURRENT-SCHEMA CONFORMANCE REFRESH.*

The persisted evidence for the consumed one-shot at proof root
`/home/stefan/wfc-proofs/level3-codex-smoke-20260904-01` independently
establishes real-provider acceptance and successful semantic validation of the
*current* `RoutingResult` schema, including the required-nullable `commit_intent`
field. Every required success criterion in the section 11.4.5 refresh
authorization is present and internally consistent, and the persisted schema
SHA-256 is reproducible.

This review authorizes nothing. It is not a Slice-4B implementation review and
not a Level-4 proof. Choosing verdict A does not imply Level-4 readiness beyond
the ordinary lifecycle sequencing; Level-4 remains the next, separately
authorized stage.

== Authorization consumed

Authority: `docs/backlog/FTR-AGENT-WORKFLOW-COORDINATOR-P5.adoc`,
"Level-3 provider-conformance refresh authorization (current schema)" — *AUTHORIZED,
EXACTLY ONE* under section 11.4.5, following accepted Slice 4B (verdict
*A -- ACCEPT CORRECTED SLICE 4B*).

The authorization exists because `RoutingResult` now requires nullable
`commit_intent` (`null` or `{paths, subject}`); previously accepted Level-3
evidence did not prove real-provider acceptance of the current emitted schema.
The authorization permitted exactly one fresh adapter-level smoke against the
fixed root, with `commit_intent = null` explicitly declared sufficient. The
provider boundary has been reached; the authorization is therefore consumed. No
retry, alternate root, alternate executable/model/reasoning/timeout, Coordinator
Run, trusted commit, core mutation, or Level-4 proof was authorized by it.

This review performed *zero* provider invocations. `codex_adapter_smoke.py` was
not rerun; the proof root was inspected read-only and left intact.

== Persisted evidence

Reconstructed from the persisted files rather than trusting `smoke_status`:

`smoke-summary.json`::
* `invocation_attempted = true`, `returncode = 0`, `timed_out = false`
* `executable = codex`, `model = gpt-5.6-terra`, `reasoning_effort = medium`
* `candidate_result_present = true`, `semantic_validation_succeeded = true`,
  `diagnostics = []`
* `configured_executable_smoke_succeeded = true`
* `result_contract = routing-result-envelope/v1`, `schema_version = 1`

All eight required success elements of the refresh authorization
(single real invocation, rc 0, no timeout, candidate result, current-schema
provider acceptance, correlation, provider-neutral validation, configured-executable
smoke success, matching schema SHA-256, durable artifacts) are present.

=== Proof-root shape

The three durable artifacts exist:
`smoke-summary.json`, `result/result-message.json`,
`result/structured-result.schema.json`. The `workspace/` directory contains only
an initialized empty Git repository (`.git` scaffolding, no tracked files, no
commits, no worktree content), consistent with the runner's declared scope of
creating only the working-directory prerequisite. There is *no evidence of a
second provider attempt*: exactly one invocation id, one run id, one result
carrier. No provider-internal evidence beyond what the runner claims to own was
demanded.

== Schema identity and commit_intent contract

Independently computed:

  sha256sum result/structured-result.schema.json
  f421c3149a5cec2dbc79982cf29aa6d04aa8c350c623dd4705b937bc4beff912

This equals `smoke-summary.json.result_schema_sha256` exactly — *MATCH*.

The current `structured-result.schema.json` lists `commit_intent` in
`result.required`. It is defined as `anyOf`:

* `{ "type": "null" }`, or
* an object with `additionalProperties: false`, `required: [paths, subject]`,
  where `paths` is an array of strings and `subject` is a string.

The object arm exposes *only* a path list and a commit subject. There is *no*
arbitrary Git-command / argv / options surface — the required-nullable contract
is bounded exactly as specified. `commit_intent` is genuinely required (it must
be present) and nullable (it may be `null`).

== Correlation and semantic validation

Correlation identifiers are identical across `smoke-summary.json` and
`result/result-message.json`:

* `run_id = SMOKE-316b5718538b4f09b19629bb0f5238da`
* `hop_id = H001`
* `invocation_id = INV-632878f12b08495ab6fcfb07a40eb132`

Correlation: *PASS*.

The real provider result carrier (`result-message.json`) contains
`"commit_intent": null`, empirically exercising provider acceptance of the new
required-nullable field. The persisted carrier is accepted under the current
contract and reports the expected bounded smoke result:

* `outcome = completed`
* `requested_route = done`
* `requires_human = false`
* `scope_changed = false`
* `escalation = null`
* `commit_intent = null`

Semantic validation: *PASS*.

== Token-usage watch

Persisted usage (`stdout_tail` `turn.completed`): `input_tokens = 14712`,
`output_tokens = 130`, `reasoning_output_tokens = 0`. This is in the same
approximate range as the historical *L3LIVE-W1* WATCH (~14,670 input tokens for a
tiny conformance invocation). *L3LIVE-W1 is confirmed / repeated.* Per the
authorization it remains WATCH only and is *not* an acceptance blocker; it was not
investigated or optimized in this hop.

== S4B-R5 disposition

*S4B-R5 provider-conformance obligation: SATISFIED.*

S4B-R5 existed because the current required-nullable `commit_intent` schema had
not been proven through the real provider boundary. This consumed refresh proves
exactly that: the real provider accepted and returned the current strict schema
with `commit_intent = null`, semantic validation succeeded, and the emitted
schema SHA-256 is reproducible. The specific empirical provider-conformance
obligation is met.

Not closed by this proof: the *broader* S4B-R5 observations are conceptually
wider than this provider proof and remain as recorded in the FTR — namely that
`commit_intent` changed the strict provider schema while retaining
`schema_version = 1`, that old persisted result-message artifacts lacking the
field do not parse under the new parser, and that no migration framework is
introduced. Those old-artifact / schema-version compatibility concerns are
outside the scope of a single provider-conformance smoke and are deliberately not
disposed of here.

== Readiness

* Repository baseline confirmed at `9cff0014` before this review; worktree clean;
  no smoke-created runtime mutation to core.
* The consumed refresh satisfies the section 11.4.5 empirical gate for the
  current schema.
* Ready for maintainer conformance disposition: *YES*.
* Ready for Level-4 / 4B proof authorization *consideration*: *YES* (as the next
  lifecycle stage; this review authorizes no such proof).
* Level-4 proof authorized by this review: *NO*.
* Routine repository use authorized: *NO*.
