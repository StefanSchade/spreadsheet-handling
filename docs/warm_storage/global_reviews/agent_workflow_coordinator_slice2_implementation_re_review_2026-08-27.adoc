= Agent Workflow Coordinator -- Slice 2 Bounded Correction Re-Review (Independent)
:toc:

Status:: *ACCEPT -- SLICE 2 IMPLEMENTATION VERIFIED*
Date:: 2026-08-27
Reviewer context:: Same independent reviewer that authored S2-R1..S2-R5 in
`82f18d4`. Bounded correction re-review only; the broad Slice-2 review is not
repeated.

== 1. Scope

Bounded re-review of the maintainer-approved correction to the five findings of
`82f18d4`. Authority for findings: `82f18d4`. Correction under review:

* `bd38f28` -- "fix(workflow): correct slice 2 review findings" (code + FTR +
  evidence + tests).
* `a3b0433` -- "docs(workflow): record slice 2 correction evidence".
* `d7fb883` -- "docs(workflow): sync slice 2 implementation status" (docs-only;
  examined only as a lifecycle-truth consistency check).

Only direct correction regressions were checked. Broad Git design, Slice 1,
general architecture, and unrelated Minors were not reopened. Read-only: no
implementation modified.

== 2. Correction scope verification

`git diff 15fd6c3..HEAD` under `scripts/` touches only `git_facts.py` and
`__init__.py`; no `src/spreadsheet_handling/` change; no new prompt, adapter,
credential, autonomous-loop, or commit-subject implementation. The correction is
confined to the finding surface.

== 3. Finding dispositions

=== S2-R1 (IMPORTANT) -- CLOSED

Maintainer allocation verified against the accepted FTR:

* Slice 2 acceptance item 1 now reads "WFC-GIT-01..07 ... exercised" (was
  "01..08"). Verified in FTR 11.2.
* WFC-GIT-08 contract in section 6.2 is *intact and unchanged* (WorkItem/Hop
  identity projected into human-readable `git log --oneline`; validation is a
  coordinator postcondition, not a hook).
* Slice 3 (section 11.3) now explicitly owns WFC-GIT-08: its Scope adds
  "WFC-GIT-08 workflow-produced commit-subject projection" and a new acceptance
  item requires WFC-GIT-08 to be proven at the N=1 fake-adapter boundary.
* No Slice-3 implementation leaked: no subject read/validation code was added;
  the only code touched is the S2-R3/R4/R5 fixes.
* The original Slice-2 implementation evidence no longer overclaims -- its
  WFC-GIT-08 bullet now reads "formally deferred to Slice 3."

The rationale is sound: Slice 2 has no invocation/prompt/adapter boundary, so no
workflow-produced commit subjects exist to validate. No Blocking/Important
remains.

=== S2-R2 (MINOR) -- CLOSED

Evidence wording now truthfully distinguishes: "WFC-EVID-01/02/04: verified ...
WFC-EVID-03's trusted mechanism is proven; profile-selected test, architecture,
and source-style category population follows profile selection in Slice 3.
Mandatory Git facts remain direct observations from `git_facts.py`."

=== S2-R3 (MINOR) -- CLOSED

`observe_hop` now runs `git cat-file -e <base>^{commit}` first. Independently
reproduced in a temporary repo:

* non-existent name -> `('unresolved base anchor',)`;
* real blob object (valid, non-commit) -> `('unresolved base anchor',)`;
* real commit that is not an ancestor of HEAD -> `('divergent or rewritten
  ancestry',)`.

All cases fail closed (empty commits, anomaly present). New parametrized test
`test_invalid_base_anchor_is_not_reported_as_divergence` covers the name and blob
cases; the existing divergence test still passes.

=== S2-R4 (MINOR) -- CLOSED

`preflight_state_root` now additionally requires the tracked authority
`.gitignore` to be clean relative to `HEAD` (`git diff --quiet HEAD -- <source>`).
Independently reproduced:

* committed clean tracked rule -> accepted;
* unstaged uncommitted rule edit -> rejected ("must be committed and clean");
* staged uncommitted rule edit -> rejected ("must be committed and clean");
* `.git/info/exclude`-only -> still rejected ("local excludes do not qualify").

Authority is now taken only from committed tracked repository policy. New
parametrized test `test_state_root_rejects_uncommitted_tracked_ignore_authority`
covers staged and unstaged.

=== S2-R5 (MINOR) -- CLOSED

`__init__.py` holds a single docstring; `scripts.workflow_coordinator.__doc__`
is the one intended string. Dead duplicate removed.

== 4. Direct regression check

* Focused coordinator unit suite + A--J replays:
  `.venv/bin/python -m pytest tests/unit/workflow_coordinator/
  tests/architecture/semantic_invariants/test_workflow_coordinator_replays.py`
  -- *74 passed* (was 70; +4 targeted negative tests). `test_git_facts.py` alone:
  17 passed (was 13).
* `ruff check git_facts.py __init__.py` -- All checks passed.
* Corrected `observe_hop` control flow preserves 0/1/N and divergence/merge
  behavior (suite green; independently reproduced). The added state-root clean
  check does not regress the legitimate committed-rule path (accepted in
  reproduction). No direct material regression found.

== 5. Lifecycle-truth consistency (d7fb883)

The docs-only status-sync updates the FTR header, section 11 preamble, section
11.2 status, and the proposed-architecture authorization line from "SLICE 2
AUTHORIZED, NOT YET STARTED" to "IMPLEMENTED; BOUNDED REVIEW CORRECTION COMPLETE;
NOT YET INDEPENDENTLY VERIFIED OR MAINTAINER-ACCEPTED." This is truthful and does
not overclaim verification or acceptance. No architecture design or contract text
changed. Consistent with lifecycle state.

== 6. Verdict

*ACCEPT -- SLICE 2 IMPLEMENTATION VERIFIED.*

S2-R1 (Important) is CLOSED; S2-R2..S2-R5 (Minor) are CLOSED; no Blocking or
Important finding remains; no direct material regression was introduced. No new
finding escaped the bound.

This re-review authorizes no Slice 3 work, no live/fake agent adapter, and no DMC
Family Cycle 2. Maintainer acceptance of Slice 2 is the next lifecycle step.
