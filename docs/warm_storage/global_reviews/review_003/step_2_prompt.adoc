You are working in the `spreadsheet-handling` core repository.

Task: perform `REVIEW-003` step 2: public surface and onboarding readiness review.

This is a read-only review task. Do not modify runtime code, tests, documentation, registries, release notes, backlog items, or demo files. Only create the requested review artifact.

Before starting, read and follow:

`docs/ai_info/review_instructions.adoc`

== Context

`REVIEW-003` step 1 produced a Yellow verdict: the core architecture and runtime are broadly sound, and there are no blockers before credible real-world trial use. The remaining uncertainty for `0.2.0` readiness is concentrated on public surface, onboarding, demo path, user-facing documentation, and whether a technically inclined non-developer assisted by an LLM can follow the documented happy path.

Since step 1, the following cleanup has been performed:

* stale top-level public-surface scratch artifacts from REVIEW-003 finding F-0.2-1 were removed;
* `docs/release_notes/release_notes.adoc` was replaced with a planned `0.2.0` draft;
* the FTR workflow gained a conditional release-notes checkpoint;
* a dedicated review-agent instruction entry point was added at `docs/ai_info/review_instructions.adoc`.

Do not re-open general architecture questions unless they directly affect onboarding or public-surface reliability.

== Review Goal

Evaluate whether the repository's public-facing entry points are coherent enough for a realistic early adopter.

The target early adopter is:

* technically inclined;
* not necessarily a Python developer;
* comfortable with files, spreadsheets, scripts, and structured configuration;
* likely assisted by an LLM;
* interested in spreadsheet-based structured-data workflows.

The target adopter should not need to understand the internal runtime architecture to run the documented happy path.

== Scope

Inspect the public/onboarding surface of the core repository and, where referenced, the maintained demo path.

Focus on:

. README and first-click journey

* Does the root README clearly tell a new user what the project is?
* Does it point to the right documentation and demo surfaces?
* Are links release-safe and not misleading?

. Demo path

* Does the documented demo route still match the current core expectations?
* Is there a clear first-hour happy path?
* Are required commands understandable?
* Are expected outputs explained?
* Are stale demo assumptions visible from the core docs?

. User guide

* Does the user guide explain the workflow enough for an early adopter?
* Does the transformation catalog match the current registry/public step surface?
* Are FK-helper semantics and migration expectations described clearly enough?
* Are important gaps visible rather than hidden?

. CLI and YAML surface

* Does `sheets-run --help` align with the documented usage?
* Do examples and docs use the current YAML shape?
* Are there stale or contradictory config examples still visible?
* Can an LLM plausibly infer a working first pipeline from the docs?

. Release notes and public positioning

* Are the planned `0.2.0` release notes useful to an early adopter?
* Do they avoid over-promising stability?
* Do they identify migration-relevant behavior?
* Are they consistent with the README/user guide/demo posture?

. Public safety / repository hygiene

* Are committed review/onboarding artifacts free of private organization, customer, internal-product, or named-individual references?
* Are warm-storage review artifacts clearly separated from public user docs?

== Important Constraints

Do not score the project against ideal documentation completeness.

Do not require a full tutorial ecosystem, polished website, video walkthroughs, or beginner-level Python teaching before `0.2.0`.

Do not treat every missing parameter detail as a blocker.

Do not propose broad documentation rewrites unless the current docs mislead users or block the first-hour path.

Focus on concrete onboarding failure modes.

For every finding, explain what a realistic early adopter or LLM-assisted user would do wrong because of the current state.

== Required Review Artifact

Create:

`docs/warm_storage/code_review/review_003/step_2_public_surface_onboarding_review.adoc`

Use this structure:

## [source,asciidoc]

= REVIEW-003 -- Step 2 Public Surface and Onboarding Review

== Metadata

Include:

* review date;
* current HEAD commit;
* reviewer/model;
* scope;
* files modified;
* commands run.

== Executive Verdict

State one of:

* `Green`: public surface is good enough for early adopters and 0.2.0 readiness.
* `Yellow`: mostly ready, but specific onboarding/public-surface fixes are needed.
* `Red`: public surface is misleading or blocks credible early use.
* `Inconclusive`: evidence is insufficient.

== First-Hour User Journey Assessment

Describe the likely first-hour journey for a technically inclined non-developer assisted by an LLM.

Assess:

* entry from README;
* route into demo or user guide;
* setup commands;
* first workbook generation;
* edit-and-reimport path;
* how errors or confusion would likely appear.

== Public Surface Findings

Group findings into:

=== Blockers Before Real Use

Only issues that prevent credible first-hour trial use.

=== Before 0.2.0

Issues that should be fixed before a non-beta `0.2.0`.

=== Before Low-Touch Maintainer Period

Issues that matter because the maintainer may not be available to explain the project.

=== After 0.2.0

Useful documentation/onboarding improvements that should not block release.

=== Watch Only

Risks to observe during real use.

For every finding include:

* observation;
* evidence;
* realistic early-adopter failure mode;
* recommended action;
* priority rationale.

== Release Notes Check

Assess whether `docs/release_notes/release_notes.adoc` is good enough for planned `0.2.0` positioning.

Do not rewrite it. Record findings only.

== Demo Path Check

Assess whether the documented demo path is coherent from the core repository.

If the demo repository is available as a sibling checkout, inspect it enough to verify the documented route. If it is not available, state that limitation clearly.

Do not modify the demo repository.

== Non-Findings

List issues noticed but not treated as findings because they are aesthetic, speculative, too detailed for 0.2.0, or not relevant to first-hour onboarding.

== Recommended Next REVIEW-003 Stage

Choose one:

* 0.2 release readiness;
* maintainability/forkability;
* no further review needed before real use;
* another public-surface follow-up after fixes.

== Validation Performed

List commands run and results.

Suggested lightweight commands:

* `git status --short`
* `git rev-parse HEAD`
* `git diff --check`
* `python -m compileall -q src/spreadsheet_handling` if environment is active
* `sheets-run --help` if available
* documentation render commands if cheap
* demo commands only if the demo checkout is available and setup is already cheap

Do not spend excessive time on full test suites unless already configured and cheap.

== Constraints

This review must not mention any private organization, internal product, customer context, or named individual.

## This review must not modify runtime code, tests, registries, backlog items, release notes, user guide, README, or demo files.

== Review Behavior

Prefer concrete public-surface evidence over generic documentation criticism.

The central question is:

"Would a technically inclined non-developer, assisted by an LLM, be able to reach a successful first-hour spreadsheet roundtrip without private maintainer knowledge?"

Do not re-litigate architecture unless a public-surface issue exposes an architecture problem that blocks onboarding or `0.2.0`.
