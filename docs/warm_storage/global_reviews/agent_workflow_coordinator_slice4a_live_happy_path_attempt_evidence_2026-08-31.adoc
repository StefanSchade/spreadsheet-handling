= Agent Workflow Coordinator -- Slice 4A Live Happy-Path Attempt Evidence
:toc:
:toclevels: 2

Status:: *ONE-SHOT LIVE AUTHORIZATION CONSUMED; HAPPY PATH NOT PROVEN; NO RETRY AUTHORIZED.*
Date:: 2026-08-31
Authorization baseline:: `b29a500e` -- `docs(workflow): authorize slice 4a live happy-path proof`.
Authority:: `docs/backlog/FTR-AGENT-WORKFLOW-COORDINATOR-P5.adoc`, section 11.4.2.
Retained proof root:: `/home/stefan/wfc-proofs/slice4a-happy-b29a500e`.

This is an evidence-only, read-only inspection of the one live attempt authorized
by `b29a500e`.  It does not change the FTR, architecture, Project Memory,
Slice-4A implementation, or proof artifacts; it does not authorize or perform a
resume, retry, reproduction, or another provider invocation.

== Authorization consumption and attempt count

Section 11.4.2 authorized exactly one new disposable `N=1` proof, with a fresh
proof root, Run, invocation, repository, and trusted-state root.  The retained
state positively records a dispatch marker and one charged H001 for
`RUN-c2fe2ab13f224bbeb37f8cd9519ce0d1` / `INV-1c8e44c2a7e444aa9e2ff7dfa2ff2a06`.
The real provider boundary was reached: the retained stdout begins with
`thread.started` and `turn.started`, then records the provider's HTTP error.
Therefore the one-shot authorization is consumed regardless of the unsuccessful
outcome.

Read-only enumeration of `/home/stefan/wfc-proofs/` found only the authorized
`slice4a-happy-b29a500e` root.  It contains one disposable `repo/`, one
`trusted-state/` checkout, one Run ID, and one invocation ID.  A filesystem-wide
identifier search within that retained proof location found only the same Run and
invocation identities.  No additional `slice4a-happy-*` root, disposable
repository, trusted-state root, Run ID, or invocation ID was retained.  Thus no
retained evidence of a second real provider invocation exists.  This is a
bounded filesystem finding, not an assertion based on operator memory or a
cryptographic proof about unretained external activity.

The harness itself independently prevents an accidental second launch using the
same fixed root from reaching the provider: `run_disposable_proof()` resolves the
requested root and, before creating `repo/`, trusted state, a Run ID, or an
invocation ID, raises `Slice4Error("proof root must be a new path")` if that root
already exists.  The specified root already exists.  Accordingly, a second start
with exactly `/home/stefan/wfc-proofs/slice4a-happy-b29a500e` would fail locally
before the provider boundary.

== Retained identity, coordinator, and process facts

[cols="1,3",options="header"]
|===
|Fact |Retained value

|Work item / Hop
|`WFC-S4-PROOF` / `H001` (`sequence=1`, `max_autonomous_hops=1`)

|Run / invocation
|`RUN-c2fe2ab13f224bbeb37f8cd9519ce0d1` /
`INV-1c8e44c2a7e444aa9e2ff7dfa2ff2a06`

|Process outcome
|`process_returncode=1`; `timed_out=false`; `acceptance=uncertain`;
`vertical_success=false`

|Coordinator state
|`hop_used=1`, budget used `1` / remaining `0`,
`final_run_status=reconcile_required`,
`stop_reason=reconcile_requires_explicit_resume`

|Dispatch/result transport
|`dispatch_marker_present=true`; correlated `result-message.json` is zero bytes;
no structured result was produced
|===

The retained Run and checkpoint agree: H001 is attributed to
`uncertain_dispatch`, has `outcome=uncertain`, no applied route, and summary
`Invocation may have been accepted; charged without replay.`  They retain the
explicit human question `Validate current reality and explicitly authorize
resume.`  This evidence does not resume the Run, and no such action is
authorized.

== Bounded diagnostic and failure classification

The persisted bounded stdout tail records a provider `invalid_request_error`,
HTTP status `400`, code `invalid_json_schema`, parameter `text.format.schema`,
and the following message:

[source,text]
----
Invalid schema for response_format 'codex_output_schema': In
context=('properties', 'result'), 'additionalProperties' is required to be
supplied and to be false.
----

The same stdout shows `turn.failed` after that response.  This is the primary
terminal failure retained for the process.  It exposes an apparent
structured-output schema incompatibility requiring independent review.  This
evidence does not fix it, select an implementation correction, or assign a final
implementation-finding severity.

Separately, the bounded stderr tail records an earlier models-cache warning:

[source,text]
----
failed to load models cache: missing field `supports_parallel_tool_calls` at
line 97 column 5
----

Both observations are retained, but the cache warning is distinct from the later
provider HTTP/schema rejection.  The evidence does not establish that the cache
warning caused the terminal `invalid_json_schema` request failure.

== Host provenance

`launch-provenance.json` records the bounded ancestor chain
`mosh-server` -> `bash` -> `python` (harness process).  This is consistent with
the required ordinary non-nested host launch.  Its persisted bounded
environment-presence indicators are all false: `claude_related=false`,
`codex_related=false`, and `openai_related=false`.  The provenance declares its
scope as bounded host/operator launch context, with no argv or environment values
recorded, and explicitly is not cryptographic attestation.  Accordingly, this
evidence reports the non-nested-host condition as *consistent*, not proved beyond
that documented scope.

== Disposable Git and trusted-state inspection

Direct read-only inspection of the retained disposable repository found:

* initial and final `HEAD` are both
  `2390c6f4b1f16c2787e76f7e19f678a5942ed4e8`;
* the sole commit is `docs(workflow): WFC-S4-PROOF baseline`; the actual commit
  range and changed-path list are empty, so the expected workflow commit is
  absent;
* `PROOF.md` remains `WFC-S4-PROOF: pending marker`;
* the worktree is clean, there are no remotes, and no Git lock files are present.

The checkout association binds checkout ID
`6f387fb8-75a2-4143-9a91-28ed11e9520a` to the retained `repo/` and its `.git`
directory.  Its trusted state contains the Run snapshot, checkpoint, dispatch
marker, zero-byte result transport, and structured-result schema.  The dispatch
marker correlates the same Run, H001, invocation ID, baseline HEAD, sequence, and
reserved budget.  No Git mutation or unexpected anomaly fact is recorded.

== Scope conclusion

LIVE AUTHORIZATION CONSUMED:: YES
DISTINCT REAL RUNS FOUND:: 1
DISTINCT REAL INVOCATION IDS FOUND:: 1
SECOND REAL PROVIDER INVOCATION EVIDENCE:: NO
FIXED-ROOT SECOND START WOULD REACH PROVIDER:: NO
NON-NESTED HOST PROVENANCE:: CONSISTENT
PROCESS RETURN CODE:: 1
PRIMARY TERMINAL FAILURE:: Provider HTTP 400 `invalid_json_schema` rejection for the structured response schema.
MODELS-CACHE WARNING ALSO PRESENT:: YES
STRUCTURED RESULT PRODUCED:: NO
HOP USED:: 1
FINAL RUN STATUS:: `reconcile_required`
DISPATCH MARKER RETAINED:: YES
GIT MUTATION:: NO
HAPPY PATH PROVEN:: NO
REAL PROVIDER INVOCATIONS PERFORMED BY THIS EVIDENCE HOP:: 0
NEW LIVE INVOCATION AUTHORIZED:: NO
READY FOR INDEPENDENT LIVE-FAILURE REVIEW:: YES

The happy path required by section 11.4 remains unproven.  No retry, resume,
reproduction, or new proof is authorized.
