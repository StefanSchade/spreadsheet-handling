= Agent Workflow Coordinator -- Second Level-3 Live Smoke Review and Evidence
:toc:
:toclevels: 2

Status:: *SECOND LEVEL-3 AUTHORIZATION CONSUMED; SMOKE SUCCEEDED. THE INTENDED
TINY REAL ADAPTER CONTRACT IS TECHNICALLY PROVEN FOR EXACTLY ONE EMPIRICAL
INVOCATION. THIS IS BOUNDED INDEPENDENT EVIDENCE/REVIEW ONLY; MAINTAINER
LIFECYCLE DISPOSITION AND ANY LEVEL-4 DECISION FOLLOW SEPARATELY. LEVEL 4 IS NOT
AUTHORIZED.*
Date:: 2026-09-02
Baseline:: `343db09f` -- `docs(workflow): accept git workspace fix and authorize level 3 retry`
Authorization:: FTR-AGENT-WORKFLOW-COORDINATOR-P5, "Second Level-3 live authorization"
Smoke root:: `/home/stefan/wfc-proofs/level3-codex-smoke-20260902-02`
Real Codex invocations in this review hop:: 0
Real provider invocations in this review hop:: 0

== 1. Verdict

*A -- ACCEPT LEVEL-3 LIVE EVIDENCE.*

The second one-shot Level-3 smoke successfully establishes the intended
Level-3 real adapter contract:

----
AgentExecutionRequest
    -> CodexCliAdapter
    -> real Codex CLI
    -> real provider
    -> AgentExecutionOutcome
    -> provider-neutral validated_result()
----

for exactly this one empirical invocation. The persisted evidence is internally
consistent, correlates across all durable carriers, and is technically ready for
maintainer Level-3 disposition. This review does not itself perform maintainer
acceptance and does not authorize Level 4.

== 2. Authorization / execution identity

The authorized second smoke root was exactly
`/home/stefan/wfc-proofs/level3-codex-smoke-20260902-02` with pinned
configuration `executable=codex`, `model=gpt-5.6-terra`,
`reasoning_effort=medium`, `timeout_seconds=120`. The persisted
`smoke-summary.json` records exactly this pinned configuration.

Operator-reported shell result was `exit=0`. The apparent duplicate
command/output in copied terminal text was confirmed by the maintainer to be
duplicated pasted scrollback, not a second invocation. The persisted artifacts
are authoritative for invocation identity; they record a single invocation with
one `run_id`, one `hop_id`, and one `invocation_id`.

Governance state:

* first Level-3 authorization: *consumed / failed*
  (transport failure, prior evidence artifact, one-shot rule);
* second Level-3 authorization: *consumed / succeeded* (this hop's subject);
* further Level-3 invocation: *NOT AUTHORIZED*.

No further smoke was authorized or executed by this review.

== 3. Transport and provider evidence

`invocation_attempted=true`, `returncode=0`, `timed_out=false`,
`stderr_tail` empty, `diagnostics=[]`.

The persisted `stdout_tail` records a real Codex CLI event stream:

* `thread.started` with `thread_id=01a063b9-e9be-7921-943a-87ae537cf3cb`;
* `turn.started`;
* `item.completed` carrying an `agent_message` whose text is the structured
  routing envelope;
* `turn.completed` with a real usage block.

Unlike the first (failed) smoke, which stopped at a local trusted-directory
prerequisite before reaching the provider, this event stream demonstrates a
completed real thread/turn with an agent message and usage accounting. This is
sufficient evidence that the real Codex CLI/provider path was reached for this
single tested configuration. No generalization beyond this one invocation is
claimed.

Optional WATCH observation (usage from the CLI event stream):
`input_tokens=14670`, `output_tokens=124`, `reasoning_output_tokens=0`,
`cached_input_tokens=0`, `cache_write_input_tokens=0`. Token-efficiency analysis
is outside this Level-3 acceptance question; no finding is opened on token
consumption.

== 4. Schema / result correlation

Recorded identity in `smoke-summary.json`:

* `run_id = SMOKE-50017144de4946ff8d078891c172c36c`
* `hop_id = H001`
* `invocation_id = INV-c249d9107b4940b682066f8bf116b9bc`
* `result_contract = routing-result-envelope/v1`
* `result_schema_sha256 = 3d52c2cb86b446c39108da44dbbf59fcc159edabfb50f7e9b790b12904080b5a`

`result/result-message.json` carries the identical `run_id`, `hop_id`, and
`invocation_id`. The embedded `agent_message` text inside `stdout_tail` carries
the same three identifiers a third time. Required identity equality
(`run_id`, `hop_id`, `invocation_id`) holds across all three carriers.

Real schema-path check: deterministic SHA-256 of the persisted
`result/structured-result.schema.json` was computed independently:

----
3d52c2cb86b446c39108da44dbbf59fcc159edabfb50f7e9b790b12904080b5a
----

This matches `result_schema_sha256` in `smoke-summary.json` exactly. The
persisted schema is a strict routing envelope: top-level
`additionalProperties:false` requiring
`schema_version, run_id, hop_id, invocation_id, result`; the nested `result`
object is also `additionalProperties:false` requiring the routing fields
(`schema_version, outcome, requested_route, scope_changed, requires_human,
escalation, findings, claimed_commits, evidence_refs, summary`) with
`schema_version` constrained to `1`. The material empirical fact is established:
this exact emitted schema survived the real Codex CLI/provider structured-output
path and yielded a candidate accepted by existing provider-neutral semantic
validation. The schema is not redesigned or broadly re-reviewed here.

== 5. Semantic validation

`candidate_result_present=true`, `semantic_validation_succeeded=true`,
`configured_executable_smoke_succeeded=true`, `smoke_status=succeeded`.

The returned semantic envelope in `result-message.json` (the `result` object):

* `schema_version = 1`
* `outcome = completed`
* `requested_route = done`
* `scope_changed = false`
* `requires_human = false`
* `escalation = null`
* `findings = []`
* `claimed_commits = []`
* `evidence_refs = []`
* `summary = "Completed as requested."`

This is a valid result accepted by the existing provider-neutral
`validated_result()` path, as recorded by `semantic_validation_succeeded=true`.
The textual `summary` wording is not treated as material beyond schema validity.

== 6. Claim boundary

Level 3, as proven by this single invocation, establishes ONLY:

* the current `CodexCliAdapter` can launch the pinned real Codex CLI;
* the real CLI/provider path accepts the emitted strict structured-output schema;
* the invocation returns a structured candidate;
* the candidate correlates with `run_id`/`hop_id`/`invocation_id`;
* provider-neutral semantic validation accepts it;
* exactly this tiny adapter contract succeeded once.

It does NOT establish: full Coordinator Run lifecycle; multi-Hop operation;
reducer correctness as integrated E2E; dispatch/reconcile correctness as
integrated E2E; Git mutation correctness; commit authority; Finding convergence;
Slice-4A full happy path; routine repository operation; Slice 4B; Slice 5.
Slice-4A coordinator happy path remains *NOT YET PROVEN*.

Level 2 remains *NOT REQUIRED / NOT PURSUED BY DESIGN*, as stated in the FTR
conformance ladder. This successful real CLI/provider smoke is exactly the
Level-3 evidence that follows the intentionally omitted provider-internal
Level 2. Level 2 is not reopened.

== 7. Findings / watches

No BLOCKING, IMPORTANT, or MINOR findings.

[cols="1,1,3", options="header"]
|===
|ID |Severity |Note

|L3LIVE-W1
|WATCH
|The tiny smoke consumed `input_tokens=14670` for a `output_tokens=124`
structured result. Recorded as an empirical observation only; token-efficiency
is out of scope for Level-3 acceptance and is not a finding.
|===

== 8. Readiness / status table

[cols="2,1", options="header"]
|===
|Item |State

|SECOND LEVEL-3 AUTHORIZATION CONSUMED |YES
|SECOND LEVEL-3 SMOKE SUCCEEDED |YES
|RETURN CODE |0
|PROVIDER PATH REACHED |YES
|CANDIDATE PRESENT |YES
|CORRELATION VALID |YES
|SEMANTIC VALIDATION SUCCEEDED |YES
|REAL SCHEMA PATH SUCCEEDED |YES (SHA-256 match)
|LEVEL 3 TECHNICALLY PROVEN |YES
|LEVEL 4 NOT AUTHORIZED |CONFIRMED
|SLICE-4A FULL HAPPY PATH NOT YET PROVEN |CONFIRMED
|LEVEL 2 NOT REQUIRED / NOT PURSUED BY DESIGN |CONFIRMED
|REAL PROVIDER INVOCATIONS IN REVIEW HOP |0
|===

This artifact is bounded independent evidence/review. It does not perform
maintainer acceptance, does not update the FTR, and does not authorize Level 4.
Maintainer lifecycle disposition follows separately.

== Validation

* `git diff --check` -- clean.
* AsciiDoc rendered strictly with `asciidoctor --failure-level=WARNING`.
* Deterministic SHA-256 of the persisted schema recomputed and matched against
  `result_schema_sha256`.
* No Codex/provider invocation was performed.
