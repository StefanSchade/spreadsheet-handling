= Agent Workflow Coordinator -- Slice 4A Replacement Proof Evidence

Status:: *REPLACEMENT INVOCATION CONSUMED; FAILED/UNCERTAIN RESULT; INDEPENDENT REVIEW PENDING.*
Date:: 2026-08-30
Authorization baseline:: `20ded7b8` -- `docs(workflow): authorize slice 4a replacement proof`.
Retained proof root:: `/tmp/wfc-s4-replacement-9gmmz_3r`.

This is factual evidence for the exactly-one replacement disposable proof authorized
by `20ded7b8`. It does not accept a happy-path proof, change FTR or architecture
authorization, modify Project Memory, authorize a resume, or authorize another
invocation.

== Historical proof distinguished from this replacement

The historical first disposable proof is immutable evidence in
`agent_workflow_coordinator_slice4_disposable_proof_evidence_2026-08-30.adoc`.
That attempt ran in a nested-Codex environment and retained its own bounded
read-only-filesystem diagnostic. It is not this Run and was neither resumed nor
retried.

The retained replacement state names a different Run and invocation:

* `run_id=RUN-a9cc83d29171494795c0789f97571288`;
* `invocation_id=INV-683fbba33b634246b25a798a4b8fc11f`;
* `work_item_id=WFC-S4-PROOF`, `hop_id=H001`, `sequence=1`, and reserved budget
  `1`.

This establishes a new Run rather than reuse of the historical first Run. The
authorization required an ordinary non-nested host/SSH launch. The retained
replacement artifacts are consistent with that declared execution setting, but
they do not contain host/sandbox provenance sufficient to independently prove it.
Accordingly, this evidence records the non-nested-host condition as *consistent,
not durably proven*.

== Retained invocation and result facts

`proof-summary.json` records one process outcome for the above invocation:

* process return code: `1`;
* timeout: `false`;
* `vertical_success=false`;
* structured-result transport `result-message.json`: zero bytes; no structured
  result was available to validate;
* no stdout/stderr diagnostic tail was retained.

The exact process failure cause is therefore *unavailable from retained proof
evidence*. In particular, this evidence does not attribute exit code `1` to the
historical nested-Codex read-only-filesystem failure and does not classify an
implementation defect from that missing diagnostic.

The retained `run.json`, `checkpoint.json`, and `dispatch.json` contain one H001
only. `hop_used=1`, `budget.used=1`, `budget.remaining=0`, and
`next_permitted_route_keys=[]`. No second or retry invocation is present in the
retained state. This proves one recorded replacement dispatch/outcome; it is not
a basis to launch or reproduce another process.

== Failure-path containment observed

The corrected driver intentionally treats a started invocation with no valid
correlated result as uncertain: it independently observes Git, retains the
dispatch anchor, charges the Hop once, persists Run/checkpoint state, and does
not fabricate a result or replay the invocation. The retained replacement state
matches that path:

* H001 has `role=uncertain_dispatch`, `outcome=uncertain`, and summary
  `Invocation may have been accepted; charged without replay.`;
* its observed range is
  `2293010542d6ddb582811dbad78a4403db4f1ba1..2293010542d6ddb582811dbad78a4403db4f1ba1`;
* Run and checkpoint both record `status=reconcile_required`,
  `reconcile_reason=interruption`, and
  `stop_reason=reconcile_requires_explicit_resume`;
* the human question is `Validate current reality and explicitly authorize resume.`

Thus the real replacement observation positively supports the corrected
post-spawn no-result containment mechanics: one Hop charged, independent Git
reality retained, dispatch anchor retained, reconciliation required, explicit
resume required, and no retry observed. It does not prove the happy path.

== Independent disposable Git and state inspection

Direct read-only inspection of the retained disposable checkout found:

* initial and final `HEAD` both
  `2293010542d6ddb582811dbad78a4403db4f1ba1` (`docs(workflow): WFC-S4-PROOF baseline`);
* commit range empty and changed paths empty;
* no expected agent commit (expected subject:
  `docs(workflow): WFC-S4-PROOF H001 mark fixture`);
* tracked path only `PROOF.md`, whose retained content is
  `WFC-S4-PROOF: pending marker`;
* clean worktree and no configured remote;
* no `*.lock` file under the retained `.git` directory; no lock was removed.

The external checkout association binds checkout ID
`04e84cc7-2f29-41bf-b940-d721a2a7c3fc` to canonical path
`/tmp/wfc-s4-replacement-9gmmz_3r/repo` and to that checkout's `.git` directory
as both `git_dir` and `common_dir`. The external state contains the Run snapshot,
checkpoint, dispatch marker, empty result transport, and result schema. The
dispatch marker correlates the same Run, H001, invocation ID, base HEAD, and
budget reservation.

== Deterministic validation of the evidence change

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator
asciidoctor on this evidence artifact
git diff --check
----

No Codex or provider/model invocation was performed while creating this evidence.

== Scope conclusion

The one authorized replacement invocation is consumed. Its retained outcome is
an exit-1, non-timeout, no-structured-result uncertain outcome with no Git
mutation and a charged `reconcile_required` state. Happy-path proof remains
unproven. Further lifecycle disposition belongs to independent review and a
maintainer decision.
