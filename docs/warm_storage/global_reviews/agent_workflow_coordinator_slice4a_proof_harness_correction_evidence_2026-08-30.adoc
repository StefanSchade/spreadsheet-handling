= Agent Workflow Coordinator -- Slice 4A Proof-Harness Correction Evidence
:toc:

Status:: *S4H-R1 AND S4H-R2 CORRECTION EVIDENCE -- DETERMINISTIC FAKE-EXECUTABLE TESTING. NO LIVE CODEX/PROVIDER INVOCATION.*
Date:: 2026-08-30
Baseline:: `8cea13df` -- `docs(workflow): review slice 4a proof harness`
Implementation corrected:: `0b950dc4` -- `fix(workflow): correct slice 4a proof harness truthfulness`

== Scope and corrections

This bounded correction addresses only S4H-R1 and S4H-R2. The independent
review artifact remains unchanged.

=== S4H-R1 -- truthful diagnostic bound

The harness continues to retain Python string tails, with no change to accepted
Slice-4A runtime semantics. Harness names and durable summary evidence now state
the actual contract: `DIAGNOSTIC_LIMIT_CHARS`, `diagnostic_limit_chars`, and
bounded 16,384-character tails. The deterministic post-spawn fake emits non-ASCII
`ä` and `ß` tails and proves each persisted tail has exactly 16,384 characters
while its UTF-8 encoded length exceeds 16,384 bytes.

=== S4H-R2 -- truthful CLI exit status

`proof_exit_status()` returns shell zero only when the harness has no error and
the real hop is vertical, established, and completed. A successful fake proof
therefore returns zero. A post-spawn fake failure that reaches
`reconcile_required` / `uncertain`, and a pre-spawn missing-executable failure,
both return nonzero. This operator signal does not change coordinator or Run
semantics; durable files remain authoritative.

== Deliberately excluded residuals

S4H-R3, S4H-R4, and S4H-R5 were deliberately not addressed or modified. No
Slice-4A runtime semantic change was made. No live Codex/provider invocation was
performed or authorized.

== Validation

[source,text]
----
.venv/bin/pytest -q tests/unit/workflow_coordinator                 # 97 passed
.venv/bin/ruff check scripts/workflow_coordinator tests/unit/workflow_coordinator  # All checks passed
git diff --check                                                     # clean
asciidoctor -o /dev/null docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4a_proof_harness_evidence_2026-08-30.adoc
asciidoctor -o /dev/null docs/warm_storage/global_reviews/agent_workflow_coordinator_slice4a_proof_harness_correction_evidence_2026-08-30.adoc
----

REAL CODEX/PROVIDER INVOCATIONS PERFORMED:: 0
NEW LIVE INVOCATION AUTHORIZED:: NO
SLICE-4A RUNTIME SEMANTICS CHANGED:: NO
