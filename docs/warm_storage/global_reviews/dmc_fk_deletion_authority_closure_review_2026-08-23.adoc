= FK Deletion Authority Closure -- Independent Review
:revdate: 2026-08-23
:toc:

== 1. Scope

Independent, adversarial final closure review of

    6ea2421327a04ef6971b2c09723fdacc288ecde9
    docs(project-memory): close FK deletion authority subroutine

in `/home/stefan/repos/spreadsheet-handling/core`. This commit is intended
only to archive completed review evidence, preserve the DMC ->
FK/reference-helper -> deletion-authority nesting, write a factual sequence
summary, repair living evidence paths, curate Project Memory coarsely,
record recovered FK/Lookup genealogy in
`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5`, and restore the roadmap
return address to FK/reference-helper Family cycle 1. It must not itself
implement resumed Cycle-1 work.

Read first: `docs/ai_info/project_memory_workflow.adoc`,
`docs/ai_info/review_instructions.adoc`, `docs/ai_info/agent_rules.adoc`,
`project_memory/README.adoc`, `project_memory/contracts/review_sets.adoc`,
the Domain Maintainer Contract, the roadmap, and
`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5.adoc`. Inspected
`git show 6ea2421` in full (`-M` rename detection), the moved
Cycle-0/H5/deletion-authority evidence, and independently re-derived the
FK/Lookup genealogy from raw `git log`.

This review is read-only: no runtime, test, registry, or backlog code was
modified. This artifact is the only file this review adds or changes.

== 2. Archive topology

`git show -M --name-status 6ea2421` shows twelve clean `R098`-`R100` renames
from `docs/warm_storage/global_reviews/` into
`docs/cold_storage/reviews/topic_reviews/domain_reviews/dmc_hardening/program/`
(3 DMC-program files + their review) and
`docs/cold_storage/reviews/topic_reviews/fk_helper_reviews/dmc_hardening/{cycle_0,provenance_handoff,deletion_authority}/`
(the Cycle-0, H5, and deletion-authority evidence), plus two new files
(`deletion_authority/sequence_summary.adoc`, `dmc_hardening/index.adoc`) and
three `docs/backlog/*.adoc` content edits.

`REVSET-TOPIC-DOMAIN-REVIEWS` (`storage_path:
docs/cold_storage/reviews/topic_reviews/domain_reviews`) and
`REVSET-TOPIC-FK-HELPER-REVIEWS` (`storage_path:
docs/cold_storage/reviews/topic_reviews/fk_helper_reviews`) already existed
in `review_sets.json` with exactly these `storage_path` values; the commit
does not touch `review_sets.json`. The new `dmc_hardening/` subdirectories
are navigation nesting under the existing anchors, not a new review set or
DMC ontology, matching `review_sets.adoc`'s "Design Rule: Storage Topology."

All twelve moved artifacts represent work independently confirmed complete
before this commit: the DMC-program evidence pre-dates the DMC's own
acceptance/promotion (the DMC contract is the current normative replacement
for that evidence); Cycle 0 is independently accepted (ACCEPT WITH MINOR
CORRECTIONS); the H5 slice is SLICE COMPLETE; and deletion-authority Slices
1-5 are integrated/accepted and Slice 6 is accepted with all findings
closed (verified against `git log --oneline` for `683c16a`, `daf27bc`,
`4b4aeb9`, `a7f137c`, matching the sequence summary's own commit citations
exactly). No moved artifact is still active working material.

Unrelated pre-existing files in `domain_reviews/` (`domain_cleanup_wave_review.adoc`,
`domain_post_cleanup_state_review.adoc`) were correctly left untouched; no
opportunistic Warm Storage sweep occurred beyond the twelve DMC/FK-pilot
files the commit message names.

*Verdict: ACCEPT.*

== 3. Sequence-summary truthfulness

`deletion_authority/sequence_summary.adoc` was checked line-by-line against
`git log` and against the accepted design document's own text
(`derived_artifact_deletion_authority_design_2026-08-21.adoc`):

* All ten cited commit hashes/subjects (`3b50701`, `d66ae9c`, `c570db2`,
  `6d4caea`, `2afb9f4`, `86eebab`, `b52f8ca`, `683c16a`, `daf27bc`, `4b4aeb9`,
  `a7f137c`) match `git log --oneline` exactly.
* "Eight independently reproduced defect variants" matches the design
  document's own "Four runtime corrections... close all eight reproduced
  variants" (line 387-389) and its numbered Repro 1-4+ list.
* "Same-run transient FK provenance... is the FK-attributed deletion
  authority" matches the design document's repeated "same-run,
  materialized-only transient provenance" language (lines 114, 337, 838-839)
  -- not a strengthened or new claim.
* The non-goals section (no claim ledger, claimant, reliance, generation ID)
  matches the design document's explicit rejections.
* The full active suite ("2881 passed") matches the integration review's own
  reported figure.

The summary simplifies wording (e.g. condensing eight reproductions into one
sentence) but does not rewrite any accepted decision, add a new claim, or
soften/harden the accepted rule.

*Verdict: ACCEPT.*

== 4. Nested-subroutine relationship

`index.adoc` and `sequence_summary.adoc`'s "Return address" section both
represent the nesting identically:

    DMC hardening -> FK/reference-helper family (Phase F)
      -> H5 provenance handoff -> deletion-authority subroutine (CLOSED)

Both explicitly state the subroutine "is not a new Domain family," "does not
itself advance Family cycle 1," and that "Family cycles 1-7... have not
started" except Cycle 1 (IN PROGRESS). No document presents deletion
authority as a permanent architecture layer, a sibling roadmap program, or a
new Project Memory review set.

*Verdict: ACCEPT.*

== 5. Project Memory curation

Diff inspection of `project_memory/canonical/*.json`:

* `concern_events.json`: exactly one new event,
  `SIG-ACT-FK-DELETION-AUTHORITY-CLOSED` (one coarse row for the whole
  five-slice + two-review workstream, `commit_refs` lists all 23
  constituent commits rather than spawning 23 events -- matches the
  "represent completion coarsely" rule). Two pre-existing events' `notes`
  fields are edited *only* to substitute `warm_storage` paths for the new
  `cold_storage` paths; their summaries/verdicts are byte-identical
  otherwise.
* `concern_event_xrefs.json`: exactly one new xref row linking the new event
  to `CONC-DOMAIN-BOUNDARY-ROBUSTNESS` with `event_role:
  implementation_evidence`.
* `concerns.json`: `CONC-DOMAIN-BOUNDARY-ROBUSTNESS`'s `posture`,
  `current_summary`, `next_action`, `last_assessed_date/commit`, and `notes`
  are updated once. `next_action` correctly returns to "Family cycle 1...
  IN PROGRESS" and explicitly states this closure "does not itself authorize
  starting" resumed work.
* `reviews.json`, `review_sets.json`, `decisions.json`: confirmed
  byte-unchanged (`git show 6ea2421 --stat` lists none of the three). No
  canonical review rows were added for the moved artifacts (consistent with
  no rows existing for them beforehand), no new review set, and no decision
  row declaring `add_fk_helpers` a winner.

`last_assessed_commit` convention check: I inspected an earlier
project-memory curation precedent, `50022a9` ("record first DMC hardening
cycle"), which sets a concern's `last_assessed_commit` to `79bf0aa` -- a
*substantive* acceptance commit -- rather than to `50022a9`'s own hash. That
is the established convention: `last_assessed_commit` names the commit whose
facts are being recorded, not the curation commit doing the recording.
`6ea2421` follows the same convention, setting `last_assessed_commit:
a7f137c` (the Slice-6 documentation-closure acceptance commit) rather than
its own hash. This is correct, not an anomaly.

No stale `next_action` anywhere still instructs an agent to research
deletion authority; both `docs/ai_info/concepts_fk_helpers.adoc` and
`FTR-DOMAIN-MAINTAINER-CONTRACT-P4A_design.adoc`'s remaining
"deletion-authority" mentions describe the now-closed, current accepted
rule or a factual archival footnote, not open research.

*Verdict: ACCEPT.*

== 6. Current-context quality

Ran `make memory-context` (regenerates gitignored
`docs_generated/project_memory/current_context.adoc`; working tree remained
clean after the run). `CONC-DOMAIN-BOUNDARY-ROBUSTNESS`'s rendered section
states plainly: deletion authority is CLOSED; it is "not a new architecture
layer or continuing workstream"; FK/reference-helper hardening "resumes at
the exact point the H5 slice interrupted it: Family cycle 1... IN
PROGRESS"; F-002 and `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5` are
named as *candidates*; and the `add_fk_helpers` hypothesis is explicitly
labeled "working (not yet decided)." All six Area-6 comprehension points are
satisfied by a fresh read of the generated report alone. The health-report
diagnostics show no new missing-reference warnings introduced by this
commit (the only "Missing FTR Dependency References" rows are pre-existing
and untouched by this diff).

*Verdict: ACCEPT.*

== 7. Roadmap / return address

Independently re-derived from `roadmap_domain_hardening_sequence.adoc`'s own
pre-existing "Phase F" text (unchanged by this commit except for path fixups
and the new subroutine paragraph): Cycle 0 and the H5 slice were already
independently accepted before the deletion-authority subroutine began;
"Family cycles 1-7 below have not yet been executed for this family, so
Phase F itself is IN PROGRESS, not DONE" is pre-existing text, not
introduced by this commit. The new "Nested subroutine" paragraph and the
"STARTED: PHASE F" status block both correctly restate Family cycle 1 as
"IN PROGRESS" (unchanged) and explicitly state the subroutine "does not
authorize starting" resumed work. `git show 6ea2421` confirms no edit to any
`== Family cycle 1` through `== Family cycle 7` heading's own content --
only the two new paragraphs and path fixups were added. Family cycles 2-7
remain unstarted; the roadmap's own text for those sections is untouched.

F-002 and `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5` are
consistently presented as *candidates* ("next concrete candidates") in the
roadmap, the index, the sequence summary, and the concern's `next_action`
-- never as already-selected work.

*Verdict: ACCEPT.*

== 8. FK/Lookup genealogy verification -- MATERIAL FINDING

I independently re-derived the genealogy from raw `git log`, deliberately
not trusting the FTR's own citations:

[cols="2,1,4"]
|===
| Commit | Date | What it actually established

| `ee68eac`
| 2025-09-04
| Adds `core/fk.py` (163 lines) plus CLI pack/unpack FK round-trip logic.
  Not a Domain-layer module; not `add_fk_helpers`.

| `4e6cf15`
| 2025-09-19
| First commit registering an `"apply_fks"` pipeline step (the ancestor
  runtime name of today's `add_fk_helpers`).

| `fd8afa9` (FTR-FK-HELPER-REFACTOR-P3B)
| 2026-03-31
| Adds **only** `domain/validations/fk_helpers.py`
  (`check_duplicate_ids`, `check_unresolvable_fks`, ... ) and registers the
  `check_fk_helpers` pipeline step. Does **not** touch `apply_fks` /
  `add_fk_helpers`'s materialization logic at all. `check_fk_helpers` was
  later renamed to `validate_fk_helpers` (see `d9bbae8` below) -- a
  *different* public step than `add_fk_helpers`. `git log --grep`
  confirms `fd8afa9` is the *only* substantive commit under this FTR ID.

| `a83c29e` (FTR-FK-HELPER-DOMAIN-EXTRACTION)
| 2026-04-23
| "extract FK-helper logic from pipeline.steps into domain layer" -- creates
  `domain/transformations/fk_helpers.py` with `enrich_helpers()` /
  `drop_helpers()`. This is the commit that actually formalizes the
  `add_fk_helpers` materialization primitive as a Domain layer.

| `8e620b4` (FTR-EXPLICIT-HELPER-LOOKUP-POLICY-P4)
| 2026-04-27
| Introduces `enrich_lookup` directly under `domain/transformations/`.
  Commit message: "Replaces consumer-side plugins like ba-config
  `enrich_variable_usage_matrix_helpers` with declarative YAML config" --
  confirms the FTR's claim in Area 9 (not originally an FK alternative).

| `d9bbae8` (FTR-PIPELINE-STEP-NAMING-P4)
| 2026-04-28
| **Same commit** renames `apply_fks -> add_fk_helpers`,
  `check_fk_helpers -> validate_fk_helpers`, **and**
  `enrich_lookup -> add_lookup_helpers`. The literal current names
  `add_fk_helpers` and `add_lookup_helpers` were coined in the identical
  commit, one day apart from nothing.

| `c965431` (FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2)
| 2026-08-02
| Adds asymmetric `source_key`/`lookup_key`. Commit message: "Slice 1 only:
  no consumer change, no inbound binding, no project_by_role removal, no
  worldbuilding migration" -- confirms it did not itself prove canonical FK
  status, matching the FTR's Area-9 characterization.
|===

`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5.adoc` lines 68-74, in a
section explicitly labeled *"A. Genealogy (verified historical fact, from
`git log`)"*, states: `add_fk_helpers` "was formalized as a Domain layer in
`FTR-FK-HELPER-REFACTOR-P3B` (`fd8afa9`, 2026-03-31) -- a P3B priority,
markedly earlier than the P4A/P4A2/P5 priorities used for later work," and
concludes "Net verified genealogy: `add_fk_helpers` is the older,
FK-relation-native surface."

This citation is incorrect. `fd8afa9` formalized FK-helper *validation*
(what became `validate_fk_helpers`), not the `add_fk_helpers` materialization
primitive under review for consolidation. The commit that actually moved
`add_fk_helpers`'s own logic into the Domain layer is `a83c29e`
(2026-04-23) -- only **four days** before `enrich_lookup`/`add_lookup_helpers`
was born directly in the Domain layer (`8e620b4`, 2026-04-27), and the
current public *names* `add_fk_helpers`/`add_lookup_helpers` were coined in
the *identical* commit (`d9bbae8`, 2026-04-28), not months apart.

This does not make `add_fk_helpers` younger than `add_lookup_helpers`: the
underlying `apply_fks` pipeline step genuinely is older (registered
2025-09-19, ~7 months before `enrich_lookup` existed at all), which is a
defensible basis for the closure commit message's "seven months" framing if
read as "the FK pipeline-step capability," and I confirmed no living
artifact besides this one FTR passage overstates it. But the FTR's own
specific evidentiary chain -- citing `fd8afa9`/P3B as *when `add_fk_helpers`
was formalized as a Domain layer* -- names the wrong commit for that
specific claim, and does so inside a section that explicitly asserts
"verified historical fact." A future reader relying on this passage would
believe `add_fk_helpers` has been a stable Domain-layer citizen since March
2026 (a "P3B... markedly earlier" priority tier), when its actual
Domain-layer promotion was contemporaneous (four days apart) with Lookup's,
in the same P4A-era window.

Per `docs/ai_info/review_instructions.adoc`'s domain-boundary-robustness
guidance and this review's own Area-7/8 instructions ("Do not allow
inference to be labeled as verified genealogy" / "a false genealogy
materially biases the future consolidation" is a named MATERIAL example),
this is reportable at MATERIAL severity. Mitigating factors, which keep this
from invalidating the closure itself: (1) Section E of the same FTR
explicitly disclaims relying on historical age to justify the
`add_fk_helpers` working hypothesis ("must justify itself through coherent,
independent domain semantics... rather than through historical existence");
(2) no Project Memory decision, registry entry, or runtime behavior depends
on this citation; (3) `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5` is
explicitly not started by this commit, so nothing has yet been built on the
faulty premise; (4) it does not touch the deletion-authority archive,
sequence summary, Project Memory curation, or roadmap return address, all of
which independently verify clean (Sections 2-7 above).

*Recommended action:* correct
`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5.adoc`'s Section A before
that FTR is picked up for implementation: replace or supplement the
`fd8afa9` citation with `a83c29e` (the commit that actually formalizes
`add_fk_helpers`'s materialization primitive as a Domain layer), and soften
"markedly earlier than the P4A/P4A2/P5 priorities" to reflect that the
Domain-layer/public-name gap is days, not priority tiers, while the
*underlying pipeline-step capability* gap (`apply_fks` since 2025-09-19) is
genuinely close to seven months. This is a documentation-only correction; it
does not require reopening deletion authority or starting Cycle 1/FTR-P5.

== 9. Capability/consumer constraint review

Independently verified against current runtime (not just the FTR's prose):

* `classify_formula_helper_step`
  (`src/spreadsheet_handling/pipeline/execution_state/formula_helper.py:80`)
  resolves against `enrich_lookup` only (`expected_target=enrich_lookup`,
  line 92) -- confirms "Trusted Ingress certifies exactly the Lookup route"
  and that neither side is falsely described as a strict superset.
* `helper_prefix` exists in
  `domain/transformations/fk_helpers/enrich.py:134`; `_check_column_conflict`
  exists in `domain/transformations/enrich_lookup/operation.py:236` --
  confirms the FK-side alias/prefix capability and the Lookup-side collision
  check are both accurately described.
* Section C ("Compatibility constraint") is correctly labeled
  "maintainer-provided project constraint," phrased as "no independently
  governed external consumers... require preserving both historical public
  surfaces," and is explicitly bounded to "optimize for the simplest
  coherent domain model," with a following sentence clarifying this "is not
  a claim that consumer migration work is unnecessary." It is not conflated
  with a Git-derived fact and does not imply arbitrary breakage is
  automatically acceptable.
* Section E ("Working hypothesis") is correctly hedged: "This is recorded as
  a working hypothesis... not as a decision: no Project Memory decision
  record exists or should exist yet." Confirmed no such decision row exists
  in `decisions.json` (Section 5 above). Acceptance criteria (Scope,
  Acceptance sections) do not silently assume `add_fk_helpers` wins; the
  Acceptance section requires "A design decision exists naming exactly one
  canonical public helper-column surface" as future work, not a foregone
  conclusion.
* Worldbuilding acceptance case (`places.region_id -> regions.id`,
  `_regions_name`/`_regions_kind`, collision because `places` already owns
  `name`/`kind`) is preserved verbatim from the FTR's pre-existing Context
  section; this commit did not alter it. The "temporary values-mode"
  explanation ("Managed Formula/Trusted Ingress currently certifies only
  the Lookup route") is consistent with the `classify_formula_helper_step`
  code reading above.

*Verdict: ACCEPT*, subject to the Section 8 correction.

== 10. Path/navigation validation

`grep` for all twelve moved artifacts' old
`docs/warm_storage/global_reviews/<filename>` paths across the repository
found only four residual occurrences, all inside the moved review
documents' own "Lifecycle cleanup check" / "Historical-artifact
preservation" sections, describing where the review series lived *at the
time that review was written* (2026-08-21/22/23, before this archival
commit). These are frozen historical/scope statements, not live navigation,
and correctly remain unedited per the commit's own stated intent to
"preserve historical artifacts' own frozen point-in-time text." No living
(non-archived) document anywhere in the repository still points at any of
the twelve old paths.

`git diff --check 6ea2421^ 6ea2421` is clean (no whitespace errors). Working
tree is clean after `make memory-context`, `make test-file` x2, and the
architecture-guard run below (all of which write only gitignored
`docs_generated/` / `project_memory/derived/` output).

*Verdict: ACCEPT.*

== 11. Index / discoverability

`dmc_hardening/index.adoc` correctly points current normative authority at
the Domain Maintainer Contract and current Functional/Semantic/Technical
Model documents (not itself), labels its own contents "historical review
evidence... archived navigation material, not current authority," and its
diagram explicitly marks deletion authority CLOSED while Family cycles 1-7
remain ACTIVE -- correctly not implying FK-family closure. No edit to
`docs/cold_storage/reviews/topic_reviews/index.json` was made or appears
required by the existing review-set contract (review sets are keyed by
directory structure, not a separate manual index per
`review_sets.adoc`'s Design Rule).

*Verdict: ACCEPT.*

== 12. Validation results

[cols="3,1"]
|===
| Command | Result

| `make memory-context`
| PASS (regenerated gitignored outputs; no diagnostics regressions)

| `make test-file FILE=tests/integration/project_memory/test_concern_event_matrix_roundtrip.py`
| PASS (1 passed)

| `make test-file FILE=tests/integration/project_memory/test_render_context.py`
| PASS (2 passed)

| `.venv/bin/pytest -q tests/architecture/dependency_guards/test_project_memory_dependency_boundary.py`
| PASS

| `.venv/bin/pytest -q tests/unit/project_memory/ tests/integration/project_memory/`
| PASS (145 passed) -- full project-memory suite, run as additional
  canonical/review-set validation coverage

| `git diff --check 6ea2421^ 6ea2421`
| Clean

| `git status`
| Clean working tree throughout
|===

== 13. No premature runtime work

`git show 6ea2421 --stat` confirms the only non-doc/non-project_memory paths
touched are three `docs/backlog/*.adoc` files (content edits, not code) and
the twelve renamed review documents. `src/`, `tests/`, `registries/`,
Trusted Ingress behavior, `add_fk_helpers`, `add_lookup_helpers`, and
`enrich_lookup` runtime are all untouched. No formula certification or
consolidation implementation occurred.

*Verdict: ACCEPT.*

== 14. Findings

[cols="1,1,4"]
|===
| # | Severity | Finding

| 1
| MATERIAL
| `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5.adoc`'s "verified
  historical fact" genealogy section cites `fd8afa9`
  (FTR-FK-HELPER-REFACTOR-P3B, 2026-03-31) as the commit that formalized
  `add_fk_helpers` as a Domain layer. That commit formalized FK-helper
  *validation* only (later renamed `validate_fk_helpers`), not
  `add_fk_helpers`'s materialization primitive. The actual Domain-layer
  formalization of `add_fk_helpers` is `a83c29e` (2026-04-23), four days
  before Lookup's own Domain-layer birth (`8e620b4`, 2026-04-27); the
  current public names `add_fk_helpers`/`add_lookup_helpers` were coined in
  the identical commit (`d9bbae8`, 2026-04-28). See Section 8. Correction
  recommended before this FTR is next picked up; does not block this
  closure's archive/PM/return-address function.
|===

No other MATERIAL or MINOR findings survived verification. All eighteen
review areas otherwise check out clean against independently re-derived
evidence (git log, runtime source, and generated Project Memory context).

== 15. Final verdict

    MATERIAL CORRECTION REQUIRED

Scoped narrowly: the one MATERIAL finding is confined to a "recovered
context" genealogy citation this commit newly added to
`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5.adoc` (an FTR this commit
correctly does not start implementing). It does not implicate the
deletion-authority subroutine's own archive topology, sequence-summary
truthfulness, Project Memory curation, generated current-context quality,
roadmap return address, capability/consumer evidence, or path/navigation
integrity -- all independently verified clean in Sections 2-7 and 9-13
above.

== 16. Re-entry authorization

    Deletion-authority closure/archive: ACCEPTED
    Return to FK/reference-helper DMC Family cycle 1: AUTHORIZED

Family cycle 1 ("remove remaining ontology") is independent of the flawed
genealogy citation and of `FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5`
specifically, which remains an unselected candidate. This authorization
covers only resuming the FK/reference-helper DMC hardening cycle at Family
cycle 1; it does not select or authorize implementing F-002 or
`FTR-FK-REFERENCE-HELPER-SURFACE-CONSOLIDATION-P5`. That FTR's own Section A
genealogy should be corrected (Section 8/14 above) before it is next picked
up for design or implementation work.
