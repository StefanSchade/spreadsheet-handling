= Agent Workflow Coordinator -- Slice 1 Implementation Correction Evidence
:toc:
:toclevels: 2

Status:: *BOUNDED CORRECTION IMPLEMENTED; NOT INDEPENDENTLY ACCEPTED*
Date:: 2026-08-27
Correction authority:: Independent implementation review `60bf6bb8b4b5bd7ead8a10e90d806d75a926382a`
Corrected implementation:: `a8474cb6d91a50207b83bf27b5058d8d1d1e6f78`
Correction range:: `60bf6bb..a8474cb`

== Scope

This is the bounded correction authorized by
`agent_workflow_coordinator_slice1_implementation_review_2026-08-27.adoc`.
It corrects S1-R1, S1-R2, S1-R4, and the tiny contract-local S1-R5. It does not
reopen the architecture/FTR, change outcome routing for S1-R3, implement Slice
2, or begin a general Human Readability cleanup.

== Finding dispositions

S1-R1 -- CLOSED::
`require_reconcile` now validates reason-specific entry states.
`interruption` accepts only active mechanical states (or idempotent re-entry of
the same reconciliation), while `prerequisite_return` accepts a suspended
parent. An unresolved `AWAITING_HUMAN` semantic stop cannot enter reconciliation
or resume, and its original human question remains intact. The common reconcile
mechanism remains one function/state path.

S1-R2 -- CLOSED::
`permitted_route_keys` returns no immediately executable routes for
`AWAITING_HUMAN`, `AWAITING_HUMAN_BUDGET`, `RECONCILE_REQUIRED`, `SUSPENDED`,
`COMPLETED`, or `STOPPED`. `AWAITING_SUPERVISOR` still exposes only declared
supervisor routes; active states retain current finite phase routes. Checkpoint
`next_permitted_route_keys` is therefore truthful for current state.

S1-R4 -- CLOSED::
One explicit creation-operation mapping now maps `open` to no disposition,
`resolved` to `resolve`, `residual` to `residual`, and `superseded` to
`supersede`. Transition operations use the same operation vocabulary. Tests pin
that non-open creation requires the matching gate/human authority; residual and
superseded invariants remain unchanged.

S1-R5 -- CLOSED::
This was corrected because the accepted checkpoint contract explicitly includes
per-Hop Finding delta and evidence references, and both values already existed
on `Hop`. `HopSummary`, strict Run JSON, and checkpoint rows now carry those two
compact reference tuples. No richer log/event model or duplicated semantic
state was introduced.

S1-R3 -- DEFERRED MINOR::
Outcome/route consistency is unchanged. The observation remains non-blocking
future Slice-3/real-agent-result hardening evidence; no `failed`/`blocked`
route policy was invented without a reviewed contract.

== Tests and validation

* Focused model/reducer/persistence plus A--J replay suite: *57 passed*.
* A--J expected outcomes remain green. Replay B still proves that prerequisite
  return and interruption share the common reconcile behavior; its comparison
  now begins interruption from a legitimate active state instead of the
  S1-R1-invalid unresolved human stop.
* Repository unit suite: pass. Repository architecture suite: pass.
* Full active repository suite excluding the opt-in LibreOffice marker:
  *2,991 selected, all passed; 1 deselected*.
* Coordinator Ruff, compile, FTR marker collection, source-style guardrails
  (*0 errors*), AsciiDoc, JSON, and `git diff --check`: pass.
* No file under `src/spreadsheet_handling/` changed; no Slice 2--5 machinery,
  live adapter, provider invocation, autonomous multi-Hop loop, or DMC work was
  added.

== Readability and next authority

Human Readability work was limited to S1-R4 and the local clarity needed for the
reason-specific S1-R1 guard. The independent review's proportionate-complexity
and anti-overengineering conclusions remain intact; there was no cleanup wave.

Slice 1 remains *not independently accepted*. An independent bounded re-review
of the corrected implementation and this evidence is the next action. Slice 2
remains unauthorized.
