= Backlog Restructuring Prompt
:session: backlog-restructure-review-001
:model: Sonnet

== Context

----
Project: spreadsheet-handling — Python framework, hexagonal architecture,
PyPI beta. Sole dev / evening project.

This session has two goals:

1. REVIEW-001 FTR placement — the eight REVIEW-001 FTRs currently sit
   in an unnumbered section at the end of _ftrs_open.adoc with no phase
   assignment. They need to be placed into the correct phases with a
   clear execution order.

2. FTR-DOMAIN-CONSOLIDATION breakup — this FTR has been in Phase 6
   since before Phase 3 was complete. It mixes concerns of very
   different urgency. The FK-specific parts (C2/D-002) are now P3
   material (per REVIEW-001 supplement already added). The rest
   needs to be evaluated and either placed earlier or explicitly
   deferred with reasoning.

Backlog conventions (from _ftrs_open.adoc):
  - Completed phases collapse to a single archaeology comment.
  - FTRs move to cold_storage when complete.
  - Phases are numbered; P3x = near-term well-scoped,
    P4A = medium-term consumer-visible, P5 = post-beta quality,
    Phase 6+ = longer-term / speculative.
  - The REVIEW-001 section currently sits AFTER Phase 8 effectively —
    it has no phase wrapper.
  - _ftrs_open.adoc uses AsciiDoc include:: directives for FTR files;
    the index controls ordering and phase grouping.
----


== Step 1 — Read source material

Read completely:

----
docs/general_info/backlog/_ftrs_open.adoc
docs/general_info/backlog/FTR-DOMAIN-CONSOLIDATION.adoc
docs/internal_guide/code_review/review-consolidation.adoc
----

Also read these REVIEW-001 FTRs to understand their actual scope and
dependencies:

----
docs/general_info/backlog/FTR-REVIEW-001-QUICK-WINS-P3.adoc
docs/general_info/backlog/FTR-REVIEW-001-FORMULAS-CORE-MOVE-P3.adoc
docs/general_info/backlog/FTR-REVIEW-001-META-BLOB-JSON-P3.adoc
docs/general_info/backlog/FTR-REVIEW-001-META-REGISTRY-DERIVED-P3.adoc
docs/general_info/backlog/FTR-REVIEW-001-PASSES-CLEANUP-P3.adoc
docs/general_info/backlog/FTR-REVIEW-001-BACKEND-DISPATCH-P4A.adoc
docs/general_info/backlog/FTR-REVIEW-001-MODULARIZATION-P4A.adoc
docs/general_info/backlog/FTR-REVIEW-001-MODULE-HEADERS-P5.adoc
----

Do NOT write any files yet.


== Step 2 — Structural proposal (chat only, no files)

Produce a proposal covering both goals. Present it as a structured
chat response — not a file. Wait for confirmation before Step 3.

=== 2a — REVIEW-001 FTR placement

For each of the 8 REVIEW-001 FTRs, recommend:
- Which existing phase they belong in (or a new named sub-phase)
- Position within that phase (before/after which existing FTR)
- Reasoning (1 sentence)

Consider:
- The REVIEW-001 dependency order from review-consolidation.adoc
  "Abhängigkeitsgraph": META-REGISTRY-DERIVED before FK work;
  FORMULAS-CORE-MOVE before the new guard; PASSES-CLEANUP before
  the duplicate-list fix; __all__ before module splits.
- QUICK-WINS has no dependencies — it can go anywhere in P3.
- The current milestone gate "Beta 0.1.0b6": Phase 4a feature work
  complete. The P3 REVIEW-001 FTRs are architectural hygiene that
  should land *before* new feature work, not after.
- Phase 3g (CLI Cleanup) is the last active P3 phase. Should there
  be a new "Phase 3h-review" or should P3 REVIEW-001 FTRs go into
  an explicit slot before Phase 4a?
- BACKEND-DISPATCH (P4A) touches orchestrator and CLI — proximity
  to Phase 4a feature FTRs makes sense.
- MODULE-HEADERS (P5) fits naturally in Phase 5 alongside
  FTR-MAINTAINER-ENTRYPOINTS-AND-MODULE-BUDGETS-P5.

=== 2b — FTR-DOMAIN-CONSOLIDATION breakup

Analyze the current FTR-DOMAIN-CONSOLIDATION scope:
  - Coverage gate + CI
  - core/pk → domain/ migration (FK parts now in REVIEW-001 supplement)
  - Public API surface / internal marking
  - Deprecation shims
  - Dual pipeline-runner consolidation (note: 3b prerequisite is done)

For each sub-concern, recommend one of:
  (a) Extract as new standalone FTR with a specific phase placement
  (b) Absorb into an existing REVIEW-001 FTR (which one, and how)
  (c) Keep in FTR-DOMAIN-CONSOLIDATION but move it to an earlier phase
  (d) Explicitly defer to Phase 6+ with written reasoning

Then recommend what happens to FTR-DOMAIN-CONSOLIDATION itself:
  - Does it survive as a slimmed-down tracking FTR?
  - Or is it fully dissolved into the extracted pieces?
  - If it survives: what is its new scope, and which phase?

=== 2c — Proposed _ftrs_open.adoc diff (sketch)

Show a sketch of the phase structure changes — not the full file,
just the affected sections:
  - Where the REVIEW-001 FTRs get inserted
  - Where FTR-DOMAIN-CONSOLIDATION moves or splits
  - Any new phase headers needed

Format as a before/after sketch, clearly marked.

State clearly at the end: "Awaiting confirmation to proceed with
Step 3."


== Step 3 — Execute (after confirmation)

=== 3a — Update _ftrs_open.adoc

Modify docs/general_info/backlog/_ftrs_open.adoc:

Rules:
- Insert REVIEW-001 FTRs into their approved phase positions using
  include:: directives, consistent with the existing style.
- Remove the current unnumbered "=== REVIEW-001 Findings" section
  at the bottom — the table it contained served as a derivation
  record; replace it with a brief archaeology comment:
  `// REVIEW-001 FTRs integrated into phases above — 2026-05-11`
- If a new phase sub-header is needed (e.g. "Phase 3h — Architecture
  Hygiene (REVIEW-001)"), add it with a brief purpose line consistent
  with other phase headers.
- For FTR-DOMAIN-CONSOLIDATION: move the include:: to the approved
  new phase, or remove it if dissolved.
- Do NOT reorder or modify any existing FTR entries other than
  FTR-DOMAIN-CONSOLIDATION.
- Do NOT modify the milestone gate descriptions.
- Preserve all archaeology comments exactly.

=== 3b — Handle FTR-DOMAIN-CONSOLIDATION

Based on the approved proposal:

If dissolving:
- Add a deprecation header to FTR-DOMAIN-CONSOLIDATION.adoc:
  `// DISSOLVED 2026-05-11 — scope extracted into: <list of FTRs>`
- The REVIEW-001 supplement block stays in the file for traceability.
- Do not delete the file (cold_storage move happens on completion,
  not on dissolution of scope).

If slimming down:
- Edit FTR-DOMAIN-CONSOLIDATION.adoc to reflect the reduced scope.
- Mark extracted sub-concerns as:
  `// Extracted → <FTR-NAME> (2026-05-11)`
- Update the Purpose and Scope sections accordingly.

If extracting new standalone FTRs (only if approved in Step 2):
- Write new FTR files to docs/general_info/backlog/ following the
  surgical FTR format from the existing examples.
- Filename: FTR-<SLUG>-<PRIORITY>.adoc (no REVIEW-001 prefix —
  these originate from DOMAIN-CONSOLIDATION, not the review).


== Session close

After all edits, print a brief summary in chat:
- What changed in _ftrs_open.adoc (phase placements)
- What happened to FTR-DOMAIN-CONSOLIDATION
- Any new FTR files created
- Recommended first three FTRs to execute based on the
  Abhängigkeitsgraph and the new phase order
