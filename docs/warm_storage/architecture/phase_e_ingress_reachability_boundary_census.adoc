[[warm-phase-e-ingress-reachability-boundary-census]]
== Phase-E Ingress Reachability and Boundary Census

_Reconstructs the maintained paths by which payload and pipeline-description
input can currently reach ordinary Domain operations, before Phase-E Trusted
Ingress design begins._

Status: *Pre-design, pre-normative evidence artifact.* This is not an ADR, a
Technical Model amendment, an FTR, a Project Memory update, or a Phase-E
design. It authorizes no runtime or test change. In particular it does not
choose a Trusted Ingress mechanism, make `run_pipeline` an error, constrain
direct Python use, add scalar normalization or `scalar_category` wiring,
introduce Pydantic, select a family pilot, or start `D-T3`.

Date: 2026-08-11. Baseline: `60e0f261210110ab33be5bc94a30a0f3ed323a63`
(`docs(project_memory): record accepted domain architecture promotion`),
`main`, clean worktree. The independently accepted Durable Domain
Architecture Technical Model promotion precedes this census; Phase E itself
has not started. This artifact requires independent architecture review
before Phase-E design may treat it as accepted evidence. The next action is
therefore *independent reachability/boundary census review*.

=== 1. Baseline and method

At the baseline, `git log --oneline --decorate -40` contains the accepted
promotion at `60e0f261` and no later Phase-E design or implementation commit.
The rendered Project Memory concern `CONC-DOMAIN-BOUNDARY-ROBUSTNESS` records
the next action as this repository-wide reachability/boundary census, before
Phase-E design.

Normative authority was read before source tracing:

* link:../../technical_model/ch03_architecture_decisions/ADR-DOMAIN-BOUNDARY-ROBUSTNESS.adoc[ADR-DOMAIN-BOUNDARY-ROBUSTNESS];
* link:../../technical_model/ch03_architecture_decisions/ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY.adoc[ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY];
* link:../../technical_model/ch03_architecture_decisions/ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB.adoc[ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB];
* link:../../technical_model/ch03_architecture_decisions/ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE.adoc[ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE];
* link:../../technical_model/ch06_architectural_layers/architectural_layers.adoc[ch06 Architectural Layers], especially trust, authority, and validation responsibility;
* link:../../technical_model/ch08_architecture_details/internal_value_model.adoc[ch08 Internal Scalar Value Model],
  link:../../technical_model/ch08_architecture_details/pipeline_semantics.adoc[Pipeline Semantics], and
  link:../../technical_model/ch08_architecture_details/spreadsheet_parse_contract.adoc[Spreadsheet Parse Contract];
* link:../../backlog/roadmap_domain_hardening_sequence.adoc[Phase E, Domain Maintainer Contract, and Phase F roadmap sequence]; and
* the accepted link:domain_architecture_consolidation_inventory.adoc[Domain Architecture Consolidation Inventory] and
  link:durable_domain_architecture_technical_model_promotion_review.adoc[Technical Model promotion review].

The link:../../technical_model/ch04_concepts/trusted_ingress_and_runtime_security.adoc[ch04 Trusted Ingress concept]
was used only as provisional historical reasoning. It does not override the
accepted Technical Model or preselect an implementation.

The method combined definitions and call-site searches, package exports,
CLI dispatch, registry and dotted-resolution inspection, backend parsers,
supported-use tests, architecture guards, and disposable runtime probes.
Grep was not treated as a complete call graph. Dynamic modules remain open
where a configured import can name arbitrary installed code outside the one
excluded namespace.

Four responsibilities remain separate throughout this census:
description/control input, payload input, Domain invariants, and output
adaptation. They are responsibility views, not four newly asserted execution
layers. Runtime/operator trust is a fixed precondition; payload trust and
pipeline-description trust vary independently; output-sink capability is a
separately owned concern.

=== 2. Executive current-state graph

.Framework-managed payload flow
[source,text]
----
external carrier
  |  CSV / JSON / YAML / XML / XLSX / ODS
  v
router-selected loader/parser
  |  parser-local conversion + Frames/_meta construction
  v
application.orchestrator.orchestrate
  |
  +--> domain.ingress.run_domain_ingress       [mandatory; _meta.legend_blocks only]
  |
  +--> pipeline.execution.run_pipeline         [already-bound steps]
  |       |
  |       +--> bootstrap_meta/apply_overrides  [if selected; merge, then ingress again]
  |       +--> ordinary Domain operation(s)
  |
  +--> final Domain cleanup
  +--> persistable _meta projection            [not payload validation]
  v
router-selected saver -> render/backend/sink
----

.Description/control flow for `sheets-run`
[source,text]
----
YAML config / steps + CLI selections and overrides
  |
  v
safe_load + ad-hoc shape checks
  |
  v
build_steps_from_config / build_steps_from_yaml
  |
  +--> REGISTRY short name
  +--> centralized dotted/colon resolver
  |       `--> reject domain.ingress namespace; otherwise import + getattr
  +--> family-specific config binding
  v
already-bound callables ----------------------> orchestrate(payload path)

Direct Python may instead pass callables/BoundStep objects, bypassing the
configuration resolver.
----

.Programmatic bypasses
[source,text]
----
programmatic Frames --> run_pipeline --> supplied steps --> Domain

programmatic Frames --------------------------> direct Domain API

backend loader --> ExactTable / Frames --> run_pipeline or direct
                                          reconstruction / transformation
----

These bypasses are not automatically defects. `run_pipeline` is documented
as a trusted programmatic composition surface; direct Domain APIs have local
preconditions and invariants. The future coverage contract must state whether
such surfaces are excluded, gated, or covered rather than silently treating
Python-level reachability as framework-managed ingress.

=== 3. Entry-surface inventory

The census found *21 materially distinct path families*. The two grouped
reconstruction rows are specialized subpaths of the XLSX/ODS orchestrated
paths, counted separately because `ExactTable` construction adds a materially
different carrier and Domain boundary. Two further rows are cross-cutting
metadata-input participants which can run inside either an orchestrated or a
programmatic pipeline. Of the 21, 12 are framework-managed high-level
load/run/save paths, 7 are trusted-programmatic bypass families, and 2 are
those cross-cutting metadata participants; these counts deliberately describe
roles rather than pretending the path sets are disjoint call graphs.

Maintained initiating or participating surfaces are:

* installed CLIs `sheets-run` and `sheets-schema-maintain`;
* public `application.orchestrator.orchestrate`, plus the compatibility
  `spreadsheet_handling.orchestrator` re-export;
* public `pipeline.runner.run_app(AppConfig)`, config loaders, step builders,
  `pipeline.run_pipeline`, registry, and dotted-path resolver;
* the command-only `application.schema_maintenance.run_schema_maintenance`;
* package-root family exports and directly importable Domain registry targets;
* public backend convenience loaders/savers and backend classes;
* XLSX/ODS `ExactTable` projection, workbook-view remapping, grouped-xref
  reconstruction, and public grouped-xref construction/composites; and
* Domain pipeline writer steps for structured YAML, key-value resources, and
  artifact manifests, which materially participate by activating filesystem
  sinks during a pipeline.

The consumer-interface guide intentionally keeps the Python stability surface
small: `orchestrate`, `run_app`, config/step builders, and selected family
package roots are stronger public evidence than arbitrary importability.
Tests and examples establish supported call shapes but test-only direct calls
are not promoted to product surfaces merely because they import successfully.

=== 4. Concrete path matrix

Abbreviations: `LB` = `legend_blocks`-shape ingress only; `PB` = persistence
boundary; `CL` = final cleanup; `AR` = addressability resolver. No row invokes
the shared scalar classifier. Evidence paths are relative to `src/spreadsheet_handling/`
unless prefixed `tests/`.

[cols="1,2,1,2,2,2,2,2,1,1,2,2,2,2,2,3",options="header"]
|===
|Path ID |Entry |Surface |Payload |Description/control |Loader/parser |Frames/_meta made |Structural validation |Ingress? |Scalar? |Addressability gate |Domain reached |Output/sink |Trust now |Bypass |Evidence / Phase-E relevance

|PATH-CLI-XLSX-ORCHESTRATE |`sheets-run` |public |external workbook |YAML/CLI |XLSX openpyxl -> IR |spreadsheet contract; hidden meta |IO checks; workbook interpretation |yes, LB |no |registry/AR before load |configured operations |CL -> PB -> XLSX or other saver |operator trusted; payload/description independent |no |`cli/apps/run.py`, `xlsx/*`, `application/orchestrator.py`; external string payload has only metadata-shape ingress
|PATH-CLI-ODS-ORCHESTRATE |`sheets-run` |public |external ODS |YAML/CLI |ODF parser -> IR |spreadsheet contract; hidden meta |IO checks; ODS parser limits/geometry |yes, LB |no |registry/AR before load |configured operations |CL -> PB -> ODS or other saver |same |no |`ods/*`, `parser_limits.py`; only maintained backend with explicit parser extent limits
|PATH-CLI-CSV-ORCHESTRATE |`sheets-run` |public |external CSV directory |YAML/CLI |`load_csv_dir` |one DataFrame/file; no meta |IO checks; pandas CSV parse |yes, LB no-op absent meta |no |registry/AR before load |configured operations |CL -> PB -> saver |same |no |`csv_backend.py`; cells are string-bounded
|PATH-CLI-JSON-ORCHESTRATE |`sheets-run` |public |external JSON + optional meta sidecar |YAML/CLI |`read_json_dir` |DataFrames + dict `_meta` |IO checks; pandas JSON; sidecar dict check |yes, LB |no |registry/AR before load |configured operations |CL -> PB -> saver |same |no |`json_backend.py`; structured JSON payload is coerced by pandas `dtype=str`, sidecar remains structured
|PATH-CLI-YAML-ORCHESTRATE |`sheets-run` |public |external YAML directory |YAML/CLI |`load_yaml_dir` |one DataFrame/file, including `_meta` filename |safe load; list/dict/scalar-to-frame branching |yes, LB often no-op for `_meta` DataFrame |no |registry/AR before load |configured operations |CL -> PB -> saver |same |no |`yaml_backend.py`; safe YAML scalars/containers can survive in object cells
|PATH-CLI-XML-ORCHESTRATE |`sheets-run` |public |external XML directory |YAML/CLI |`read_xml_dir` |one DataFrame/file; no meta |ElementTree + flatten geometry |yes, LB no-op absent meta |no |registry/AR before load |configured operations |CL -> PB -> saver |same; source comments assume trusted local files |no |`xml_backend.py`; leaf text only
|PATH-PY-ORCHESTRATE |`orchestrate(...)` / shim |public |any router backend |direct trusted Python supplies IO and BoundSteps |router |backend-specific |IO descriptor checks; backend checks |yes, LB |no |optional: caller may build config or pass steps |supplied/configured operations |CL -> PB -> saver |trusted programmatic description; payload may be external |step binding bypass possible |`application/orchestrator.py`, root shim; public framework entry guarantees current limited ingress
|PATH-PY-RUN-APP |`run_app(AppConfig)` |public compatibility/consumer |any router backend |direct AppConfig, commonly safe-loaded YAML |router via orchestrate |backend-specific |dataclass construction + builder checks |yes, via orchestrate |no |registry/AR |configured operations |CL -> PB -> saver |same |no payload-flow bypass |`pipeline/runner.py`, consumer smoke tests; first configured input only
|PATH-CLI-SCHEMA-MAINTENANCE |`sheets-schema-maintain` |public command |external router backend |CLI arguments; fixed private operation |router via use case |backend-specific |argparse + request/Domain checks |yes, LB |no |not YAML-addressable; fixed private BoundStep |schema maintenance |CL -> PB -> saver/discard + report |trusted command, payload may be external |no |`cli/apps/schema_maintain.py`; framework path with private control plane
|PATH-PY-SCHEMA-MAINTENANCE |`run_schema_maintenance` |public use case |external router backend |typed request + private BoundStep |router via orchestrate |backend-specific |request/Domain checks |yes, LB |no |fixed by framework |schema maintenance |CL -> PB -> saver/discard |trusted programmatic control |no |`application/schema_maintenance.py`; current ingress precedes maintenance
|PATH-PY-RUN-PIPELINE-REGISTRY |builder + `run_pipeline` |public trusted programmatic |arbitrary Frames |registry specs supplied by caller |none |caller |builder/family checks only |no, except an explicitly selected merge step may self-run LB |no |registry/AR |registered Domain operations |none unless a step itself writes |trusted caller precondition |yes, intentional |`pipeline/build.py`, `execution.py`; future contract must classify/exclude/gate
|PATH-PY-RUN-PIPELINE-DOTTED |builder + `run_pipeline` |public trusted programmatic |arbitrary Frames |plugin/colon/nested target string |none |caller |resolver + binder checks |no |no |yes; import at bind time |arbitrary resolved callable |none or callable-owned capability |trusted caller; configured target may be less trusted |yes, intentional composition |`pipeline/steps.py`, `dotted_paths.py`; addressability is not payload validation
|PATH-PY-RUN-PIPELINE-PREBOUND |`run_pipeline(frames, steps)` |public trusted programmatic |arbitrary Frames/Python objects |direct callable or BoundStep |none |caller |none at runner |no |no |no |supplied callable |callable-owned |trusted caller |yes, explicit |`pipeline/execution.py`, `types.py`; no runtime Frames shape enforcement
|PATH-STEP-BOOTSTRAP-META |registered or direct `bootstrap_meta` |public cross-cutting participant |persisted `_meta` already in Frames |profile defaults + CLI/runtime overrides |none |merge candidate |recursive mapping merge; LB rule before set |yes, after merge |no |registry/AR only when config-built |next ordinary step receives merged meta |inherits enclosing path |inputs trusted by caller/operator today |enclosing path determines bypass |`domain/meta_bootstrap.py`; post-load metadata introduction is atomic but only LB-normalized
|PATH-STEP-YAML-OVERRIDES |registered `apply_overrides` or direct call |public cross-cutting participant |Frames plus external/inline override metadata |config selects path or inline mapping |`yaml.safe_load` for `overrides_path` |mapping merged into `_meta` |root mapping check; selected child mappings; LB before set |yes, after merge |no |registry/AR only when config-built |next ordinary step receives overrides |inherits enclosing path |external metadata possible under trusted caller/description |enclosing path determines bypass |`domain/yaml_overrides.py`; external metadata can enter after initial ingress, then receives only LB rule
|PATH-PY-DIRECT-DOMAIN |family package/direct module call |public/ambiguous by family |arbitrary Frames/DataFrames |fixed by Python call |none |caller |operation-local preconditions/invariants |no |no |no |chosen Domain transformation/validation |none unless chosen writer API |trusted programmatic |yes |family `__init__.py`, registry targets, docs/tests; public stability differs from importability
|PATH-PY-DIRECT-SCHEMA-MAINTENANCE |Domain API |public package root |arbitrary Frames |typed request |none |caller |schema-maintenance model/operation checks |no |no |no |`apply_schema_maintenance` |none |explicit stronger programmatic precondition |yes |`domain/schema_maintenance/__init__.py`; distinct from application use case
|PATH-PY-BACKEND-TO-DOMAIN |public loader then direct API |public composition |external carrier |direct Python operation selection |backend direct |backend-specific |parser + operation-local checks |no |no |no |any direct Domain API |caller-selected saver or none |trusted caller; external payload possible |yes |backend wrappers/classes plus Domain APIs; obvious supported composition seam
|PATH-ORCH-XLSX-GROUPED-RECONSTRUCT |orchestrated reverse config |public framework subpath |external edited workbook |registered fixed/reconstruction specs |XLSX exact projection |`ExactTable` + mapping meta |exact header option; reconstruction geometry/invariants |yes, LB before reconstruction |no |registry/AR |view remap -> reconstruct -> expand |CL -> PB -> saver |independent payload/description trust |no |`xlsx/parser_interpretation.py`, grouped reconstruction; local invariants are not ingress
|PATH-ORCH-ODS-GROUPED-RECONSTRUCT |orchestrated reverse config |public framework subpath |external edited ODS |registered fixed/reconstruction specs |ODS exact projection |`ExactTable` + mapping meta |ODS limits + exact/reconstruction checks |yes, LB before reconstruction |no |registry/AR |view remap -> reconstruct -> expand |CL -> PB -> saver |same |no |ODS exact tests and GX5 integration; same Domain carrier after different parser
|PATH-PY-GROUPED-ROUNDTRIP |loader/saver + builder + `run_pipeline` |supported programmatic/test evidence |external or generated workbook |config-built or direct steps |XLSX/ODS loader |`ExactTable`, meta, caller-added source |exact/reconstruction invariants |no |no |optional registry/AR |grouped reconstruction/expansion |direct saver/caller |trusted programmatic |yes, intentional |`tests/integration/pipeline/test_gx5_grouped_xref_public_roundtrip.py`; proves public reverse composition bypass
|===

=== 5. Payload-plane reachability

Externally originated payload reaches all CLI paths, `run_app`, any
router-backed `orchestrate`, both reconstruction paths, and any direct Python
composition that calls a public loader. Direct Python callers can additionally
place arbitrary Python objects in a DataFrame cell, arbitrary values under
`_meta`, or even pass a non-DataFrame frame value: `Frames` is a static alias,
not a runtime boundary.

Effective current scalar carriers differ:

* XLSX and ODS visible cells collapse to strings at spreadsheet parser
  interpretation; absent cells become `""`. XLSX uses cached formula results
  (`data_only=True`); ODS derives textual values from ODF value types.
* CSV requests `dtype=str`, disables default NA recognition, and preserves
  blank cells as `""`.
* JSON requests `dtype=str`, disables date conversion, and replaces pandas
  missing values with `""`. Nested JSON values are therefore subject to
  pandas string coercion; the private temporal decoder is not in the read path.
* YAML `safe_load` feeds list/dict/scalar structures to pandas before missing
  replacement. Safe YAML date/datetime and other structured built-ins can
  survive where pandas construction permits, including object cells; pandas
  may also infer/coerce column carriers.
* XML contributes flattened leaf text and `""` for absent text/missing fields.
* Direct programmatic Frames can carry arbitrary Python values.

No payload scalar grammar is enforced before ordinary Domain operations.
Parser-local conversion is not Domain ingress normalization; persistence-local
conversion is not ingress; and scalar classification is not conversion.

=== 6. Description/control-plane reachability

`sheets-run` reads config and ad-hoc steps with `yaml.safe_load`, selects a
named pipeline or top-level list, applies CLI IO overrides, and binds every
step before calling `orchestrate`. `load_app_config` similarly safe-loads YAML
into dataclasses; it explicitly requires a list for `pipeline`, while Python
constructor errors and family binders supply the remaining diffuse structural
checks. There is no Pydantic model or single config-schema boundary.

Current configurable target surfaces include registry short names, registry
entries whose target is a string, `plugin` dotted references, colon-style
factory references, and nested generic string targets. Runtime inspection at
the baseline found 44 registry entries: 38 `StepRegistration` string targets
and 6 bare factories. `resolve_configuration_callable` centralizes string
resolution, accepts colon or final-dot references, imports the module, obtains
the attribute, and requires it to be callable. Its sole namespace rule rejects
`spreadsheet_handling.domain.ingress` and all its submodules. There is no
general allowlist, operator policy object, or runtime representation of
trusted versus less-trusted pipeline descriptions.

Binding and therefore dotted import happen before payload loading for the CLI
and `run_app` paths. Safe parsing/preparation and capability activation are not
currently separated for arbitrary imports. This is an addressability fact,
not a payload-validation fact. Direct Python callable targets and prebuilt
steps bypass string resolution entirely under the trusted-operator/programmatic
precondition.

The four trust combinations are architecturally representable but not equally
protected:

[cols="2,2,2,3",options="header"]
|===
|Payload |Pipeline description |Runtime representability |Current intended/protected status
|trusted |trusted |ordinary configured or programmatic run |intended; protected only by local checks and current limited ingress
|externally originated / less trusted |trusted |external file + fixed or operator-chosen steps |intended motivation, but no closed scalar/general-meta ingress guarantee
|trusted |less trusted |operator loads a description containing selectable dotted targets |representable; no trust flag or operator allowlist, so not established as safely protected
|less trusted |less trusted |external carrier plus such a description |representable by separate inputs; not thereby claimed safely supported
|===

=== 7. `domain.ingress` call census

`domain/ingress/coordinator.py` defines frozen `IngressRule(name, apply)`, an
ordered `INGRESS_RULES` tuple, and `run_domain_ingress`, which applies the
rules to a shallow `dict[str, Any]` Frames carrier. The tuple currently has
one rule: `legend_blocks_shape`.

That rule touches only mapping-shaped `_meta.legend_blocks`. It converts
`None` to an empty mapping, copies mappings while dropping derived `resolved`,
and converts list-form authoring sugar into an identity mapping after checking
member shape/name uniqueness. If `_meta` is absent or not a mapping, it is a
no-op. It does not traverse DataFrame cells, `ExactTable.data`, any other
metadata root, or scalar values.

There are exactly three production call sites:

* `application/orchestrator.py`: mandatory immediately after every router
  load and before the first configured step;
* `domain/meta_bootstrap.py`: after the profile/persisted/CLI merge is prepared
  and before the merged frames are returned; and
* `domain/yaml_overrides.py`: after the override merge is prepared and before
  the merged frames are returned.

Thus bootstrap and override processing are not fixed orchestrator stages.
They are ordinary selectable steps; when selected, each re-runs ingress after
introducing metadata. Their merge is prepared on a copy, so a failing ingress
does not partially mutate the caller's input through that return path.

The coordinator is absent from the runtime and descriptive registries, cannot
be built by the YAML dialect, and is explicitly blocked by the centralized
dotted resolver. Architecture tests pin those properties. Nevertheless the
package exports its symbols for framework/test import. That export does not
make `IngressRule` the final Phase-E architecture: accepted promotion evidence
identifies it as one current implementation detail, not a design decision.

Obvious bypasses are `run_pipeline`, direct Domain calls, direct backend-to-
Domain composition, and reconstruction driven outside `orchestrate`. A caller
can explicitly invoke ingress, but no such call is mandatory on those paths.

=== 8. `scalar_values` call census

Production search found *zero imports or calls* of `scalar_category`,
`is_missing_carrier`, `is_supported_scalar`, or `UnsupportedScalarError`
outside `core/scalar_values.py`. Tests exercise the vocabulary. The ODS
renderer has a private, independently implemented `_is_missing_carrier`; it
does not import the shared function.

Consequently no maintained parser, loader, orchestrator, ingress rule,
Domain transformation, persistence boundary, or writer currently enforces
the accepted six-category vocabulary (String, Boolean, Number, Missing, Date,
DateTime). The classification vocabulary exists; mandatory ingress enforcement
does not. `scalar_category` classifies without narrowing and remains unwired.

=== 9. `orchestrate` versus `run_pipeline`

`orchestrate` expects IO descriptor mappings and already-bound optional steps.
Its exact fixed order is:

[source,text]
----
coerce input/output descriptors
  -> route and load backend payload
  -> mandatory current domain ingress
  -> run already-bound steps, if any
       -> bootstrap_meta/apply_overrides may occur here if configured
       -> each such metadata merge invokes ingress again
  -> final Domain cleanup
  -> persistable _meta projection, only when _meta is dict
  -> route and invoke saver
  -> return saved-shape Frames
----

`run_pipeline` expects a Frames-shaped object and an iterable of callables. It
sets `out = frames` and calls each step linearly, with optional diagnostic meta
snapshots. It does not resolve a registry entry, run ingress, perform cleanup,
project persistence metadata, or save. Resolution happens only if the caller
separately uses a builder. There is no runtime validation of the Frames alias.

The accepted Technical Model documents `run_pipeline` as a *trusted
programmatic composition surface*. Public exports, integration tests, and the
GX5 public roundtrip confirm plausible ordinary consumer use. Its bypass is
therefore category A/B below, not a bug finding. Whether Phase E preserves,
gates, or explicitly excludes it remains a design question.

A disposable monkeypatch probe against real public functions produced:

Invocation: `.venv/bin/python -` with an inline script which monkeypatched the
orchestrator load/save/ingress/cleanup/persistence seams, called public
`orchestrate` and `run_pipeline`, and installed a temporary in-memory module
to observe one plugin bind. No script file was retained.

[source,text]
----
ORCHESTRATE_ORDER=load > ingress:initial > step:bootstrap > ingress:profile > cleanup > persistence > save
RUN_PIPELINE_ORDER=step:direct
PLUGIN_BIND=import:census_probe_plugin > bound:plugin
----

The first trace used `orchestrate` with its load/save/cleanup/persistence seams
instrumented and a BoundStep calling real `bootstrap_meta`; the second passed
a direct step to `run_pipeline`; the third instrumented import during plugin
binding. The probe created no maintained file.

=== 10. Direct Domain API reachability

`domain/__init__.py` and `domain/transformations/__init__.py` do not provide a
universal barrel. Selected package roots do re-export operations and models,
including cell codec, discriminator split/merge, enrichment, foreign-key
helpers, grouped xref, xref contraction/expansion, validations, extractions,
tabular helpers, and schema maintenance. Registry target modules are also
directly importable. Grouped-xref documentation/tests explicitly demonstrate
public direct and builder-driven composition. The consumer guide cautions that
arbitrary internal barrels are not all stable public API.

A Python consumer can call these operations with arbitrary Frames/DataFrames.
Such calls bypass ingress, configurable addressability, final cleanup,
persistence projection, and backend output by construction. Individual
operations defend their own semantic invariants: required frames/columns,
geometry, canonical constructors, key uniqueness, metadata preconditions,
schema request validity, and similar family rules. Those are Domain invariants,
not evidence of a general payload ingress grammar.

Supported package-root APIs are therefore intentional trusted-programmatic
surfaces with explicit/local preconditions. Merely importable internal targets
remain ambiguous rather than automatically public. Phase E must define the
claim boundary; this census does not prohibit direct calls.

=== 11. Backend-by-backend ingress behavior

[cols="1,2,3,2,3,3",options="header"]
|===
|Backend |Parser/loader |Cell/value carrier into Frames |Missing/date/formula behavior |External `_meta` |Limits and local conversion
|XLSX |openpyxl parser -> WorkbookIR -> spreadsheet contract |visible headers/data stringified; optional `ExactTable` |`None -> ""`; dates/numbers/bools stringified; `data_only=True` uses cached formula value |hidden `_meta` key/value sheet; JSON blob with legacy `ast.literal_eval` fallback; mapping then canonicalized |no framework parser extent/resource limits found; presentation metadata interpretation and exact projection are parser-local
|ODS/Calc |odfpy parser -> WorkbookIR -> spreadsheet contract |ODF typed cells converted to text; optional `ExactTable` |empty -> `""`; boolean/float/date/time rendered textually; formula/cached display interpretation is parser-local |hidden meta blob, JSON with legacy literal fallback; mapping then canonicalized |`ParserLimits`: rows 1,048,576, columns 16,384, materialized cells 10,000,000, with repeat/merge checks; no broader archive/style/time ceiling established
|CSV/csv_dir |pandas `read_csv` |strings |`dtype=str`, `keep_default_na=False`, `na_values=[]`; blank `""`; no date/formula semantics |none; `_meta` is skipped on write |no framework file/row/column limit; header wrapping to MultiIndex is parser-local
|JSON/json_dir |pandas `read_json` |`dtype=str`; nested values subject to string coercion |`convert_dates=False`; pandas missing replaced by `""`; writer's temporal envelopes are not decoded on read |optional `_meta.yaml`, accepted only if safe-loaded value is dict |no framework resource/depth limit; write-side temporal encoder is persistence-local
|YAML/yaml_dir |PyYAML `safe_load` then pandas |list/dict/scalar values; safe structured built-ins can remain object carriers |pandas missing replaced by `""`; date/datetime may survive or be inferred; no formula semantics |a file named `_meta.yaml` becomes a DataFrame, not dict metadata; saver skips it |safe loader blocks arbitrary constructors but no framework alias/depth/size limit; Timestamp conversion is write-local
|XML/xml_dir |ElementTree -> flattened records |leaf strings |absent text/fields -> `""`; no date/formula semantics |none; `_meta` skipped on write |no framework resource/geometry limit; nested tags become dotted columns
|Direct Python |caller |arbitrary Python objects, arbitrary frame values |caller-defined |arbitrary mapping/non-mapping |no framework ingress unless caller chooses it
|===

The backends need not produce identical carriers under current authority.
Visible spreadsheet text collapse and CSV string-bounding are established
facts; YAML/programmatic carrier breadth is materially greater. None of the
parser-local conversions above is shared scalar classification.

=== 12. Metadata ingress

Metadata is independently externally reachable:

* XLSX/ODS parse a hidden carrier and recover a structured mapping from JSON
  or a legacy literal form; workbook presentation carrier facts may update
  canonical metadata before frames return.
* JSON accepts a safe-loaded `_meta.yaml` sidecar only when its root is a dict.
* YAML treats `_meta.yaml` as an ordinary tabular frame, so the current mapping
  ingress and later dict-only persistence projection are no-ops for it.
* CSV/XML create no metadata mapping.
* direct callers can supply any `_meta` value.
* `bootstrap_meta` and `apply_overrides` introduce profile, persisted, CLI, or
  YAML-derived metadata after the initial ingress, then invoke the LB rule.

Only `legend_blocks` has framework-owned ingress shape normalization. Other
metadata roots can reach ordinary Domain steps; family-specific resolvers and
transformations validate what they consume. Persistence projection removes or
projects derived/runtime roots (including cleanup commands and resolved
legend/xref data) immediately before saving, but otherwise preserves much
metadata. That output projection is not input validation.

Phase-E design must therefore reason about both cell payload and metadata
payload. This census does not decide whether they share a walker, policy, or
execution pass.

=== 13. Reconstruction and reverse paths

XLSX/ODS exact-read options identify per-frame header depths and construct
`ExactTable`. Its header grid is string-based; its data tuple preserves the
values supplied by the parser (strings on maintained spreadsheet paths).
Current ingress ignores `ExactTable`.

The registered reverse path applies persisted workbook-view sheet mappings,
requires an `ExactTable`, reconstructs a grouped matrix, and expands it to a
relation. Reconstruction verifies rectangular geometry, configured header
depth and row keys, visible/logical mapping, source-axis lookup, and canonical
grouped-matrix construction. Those checks are strong local reconstruction
invariants, not a general ingress boundary.

Within `orchestrate`, the one LB ingress call precedes reconstruction. In the
public GX5 integration flow, backend loaders/savers, config builders, and
`run_pipeline` are composed directly; reconstructed values receive no ingress,
cleanup, or persistence boundary. Reconstruction does not create a new scalar
class on maintained spreadsheet paths, but direct callers can construct
`ExactTable` data with arbitrary Python carriers before invoking it.

Confidence is VERIFIED STATIC for the orchestrated call chain and VERIFIED
RUNTIME for the public XLSX/ODS roundtrip composition exercised by the focused
GX5 suite. No other maintained workbook projection/reconstruction entry was
found that changes this conclusion.

=== 14. Output and sink continuation

On `orchestrate`, final Domain cleanup precedes the persistence boundary; the
boundary projects only dict-shaped `_meta`. Router savers then serialize the
Frames they receive. CSV/YAML/XML skip `_meta`; JSON writes dict metadata to a
YAML sidecar; XLSX/ODS build render IR and embed their metadata carrier.

The persistence boundary does not traverse or validate payload cells. Writers
perform sink-local representation conversions, for example JSON temporal
envelopes and YAML Timestamp conversion. Formula capability differs by sink:
an ordinary XLSX string beginning with `=` can become an openpyxl formula,
whereas ODS formula emission follows its explicit `FormulaSpec` path. This is
output-sink capability, not payload trust.

Three registered Domain pipeline steps can activate additional filesystem
sinks before final save: `write_structured_yaml`,
`write_key_value_resources`, and `write_artifact_manifest`. Their configured
relative paths are checked against a resolved `output_dir`; absolute and
escaping paths are rejected. Those are sink-local containment and semantic
checks. They neither provide general ingress nor prove a less-trusted pipeline
description safe to activate. The main output descriptor is operator/config
selected and is not constrained to one framework root; `discard` is also a
maintained saver.

=== 15. Bypass classification

[cols="1,3,3,3",options="header"]
|===
|Category |Current examples |Evidence/current assumption |Phase-E treatment to decide (not chosen here)
|A. Intentional trusted-programmatic bypass |direct/prebound `run_pipeline`; direct family API; direct backend-to-Domain; programmatic grouped roundtrip |Technical Model trusted-composition statement, public exports, consumer/GX5 tests |preserve, gate, or explicitly exclude from mandatory claim
|B. Public surface with stronger precondition |`run_pipeline`; schema-maintenance Domain API; canonical grouped constructors |caller supplies valid Frames/requests and accepts local operation contracts |make precondition/coverage boundary explicit
|C. Historical/legacy bypass |none established by this census |root orchestrator shim is an alias into ingress, not a bypass |do not invent a legacy defect
|D. Internal-only bypass |private helpers/direct internal module calls; tests invoking ingress or operations in isolation |no consumer stability evidence |keep outside product claim or document if later promoted
|E. Potential coverage gap requiring design decision |all framework external paths have only LB ingress; general metadata/scalars are unchecked; less-trusted dotted descriptions have no policy gate |accepted architecture requires truthful coverage and independent trust axes, but mechanism is unimplemented |define coverage, policy ownership, ordering, and activation boundary
|F. Test-only reachability |unit tests calling private helpers or public functions solely to isolate semantics |test call alone is not product surface |do not count as framework entry
|===

No category A/B path is labelled a defect. Conversely, direct Python's ability
to bypass does not by itself excuse category E on framework-managed external
paths.

=== 16. Confidence and evidence register

[cols="2,1,4",options="header"]
|===
|Finding |Confidence |Evidence
|baseline and next action |VERIFIED STATIC |Git history/status and rendered Project Memory concern at baseline
|`orchestrate` order and mandatory initial ingress |VERIFIED RUNTIME |source plus disposable order probe
|`run_pipeline` is step-only |VERIFIED RUNTIME |source plus direct-step probe and public GX5 usage
|plugin import occurs during binding |VERIFIED RUNTIME |instrumented safe fake-module import plus builder tests
|three production ingress call sites |VERIFIED STATIC |repository-wide import/call search and source inspection
|zero production shared scalar calls |VERIFIED STATIC |repository-wide symbol/import search; focused scalar tests
|44 registry entries and target split |VERIFIED RUNTIME |registry object inspection: 38 string registrations, 6 bare factories
|backend carrier behavior |VERIFIED STATIC and RUNTIME |parser source and focused backend/roundtrip tests
|direct package/export reachability |VERIFIED STATIC |`__init__.py`, interface guide, consumer and family tests
|arbitrary installed dotted targets |DYNAMIC/OPEN |resolver is centralized but target module universe depends on installed environment/import path
|all possible calls from external plugins |DYNAMIC/OPEN |arbitrary plugin code can call further code; ordinary static analysis cannot close this graph
|consumer intent for every directly importable Domain module |INFERRED/OPEN |stability guide distinguishes documented/package-root API from mere importability
|===

Focused executable validation during the census ran architecture ingress,
dotted plugin, consumer smoke, shared scalar, and GX5 public roundtrip tests;
all passed. Runtime probes were disposable and left no source/test artifact.

[source,bash]
----
.venv/bin/pytest -q \
  tests/architecture/test_domain_ingress_boundary.py \
  tests/unit/pipeline/test_plugins_dotted_path.py \
  tests/integration/pipeline/test_consumer_smoke.py \
  tests/integration/pipeline/test_gx5_grouped_xref_public_roundtrip.py \
  tests/unit/core/test_scalar_values.py

.venv/bin/pytest -q \
  tests/unit/io_backends/test_csv_backend.py \
  tests/integration/io_backends/json/test_json_roundtrip.py \
  tests/integration/io_backends/yaml/test_yaml_roundtrip.py \
  tests/integration/io_backends/xlsx/test_workbook_meta_blob_carrier.py \
  tests/integration/io_backends/ods/test_ods_adapter_roundtrip.py \
  tests/unit/io_backends/test_xml_multiheader.py \
  tests/unit/io_backends/ods/test_ods_parser_repeated_cells.py
----

=== 17. Mandatory-ingress coverage test

Tested claim:

____
All framework-managed external payload reaches ordinary Domain operations only
after Trusted Ingress.
____

The current routing shape is close but the substantive claim is *not yet
truthful*. All 12 framework-managed matrix paths converge on
`orchestrate` and therefore call the current coordinator before their first
ordinary configured/private Domain operation. However that coordinator
establishes only one metadata-shape assumption. It establishes no scalar
grammar, no general Frames/carrier contract, no `ExactTable` contract, and no
general metadata contract. Later bootstrap/override steps add metadata and do
re-run only that same LB rule.

For a future truthful claim, Phase E would have to classify the current paths
as follows, without this census choosing how:

* *wire/strengthen*: the 12 framework-managed paths need a successful boundary
  that actually establishes the assumptions later Domain code is promised;
  their existing convergence point may be relevant evidence but is not a
  preselected final mechanism;
* *explicitly exclude, gate, or reclassify*: the 7 trusted-programmatic path
  families (`run_pipeline`, direct Domain/schema APIs, direct backend
  composition, and programmatic reconstruction) need a stated public contract;
* *separately guard or redesign*: metadata introduced after initial load,
  `ExactTable`/non-DataFrame carriers, and filesystem-capable pipeline steps
  require explicit treatment if they fall inside the claim; and
* *keep distinct but coordinate*: pipeline-description addressability and
  authorization must not be mistaken for payload ingress. Dotted import can
  activate code before payload loading today, so a less-trusted-description
  claim requires its own authority/activation answer.

The claim should name the entries and assumptions it covers. Merely renaming
the current coordinator would not satisfy it.

=== 18. Phase-E design questions

==== Must decide in Phase-E design

The census leaves *12 bounded mandatory design questions*:

. What exact framework-managed entries and carrier variants are inside the
  mandatory Trusted Ingress coverage claim?
. What guarantee do `sheets-run`, schema maintenance, `run_app`, `orchestrate`,
  and the compatibility shim each publish?
. Does `run_pipeline` remain explicitly trusted and outside the guarantee, or
  is it gated/reclassified in some way?
. Which documented direct Domain/package APIs are explicitly trusted surfaces,
  and how are merely importable internals distinguished?
. What scalar locations/carriers are traversed, and what does successful
  six-category classification establish without prematurely choosing
  normalization?
. What metadata roots and externally authored metadata carriers are traversed;
  do metadata and cells share any mechanism or only a lifecycle boundary?
. Where is mandatory ingress ordered relative to parser conversion, initial
  load, bootstrap/profile merge, overrides, and reconstruction?
. How are DataFrame, `ExactTable`, grouped/canonical composites, and future
  structured values included or explicitly excluded?
. What normalization, if any, is permitted, who owns it, and how is that kept
  distinct from parser and persistence conversion?
. Where does operator-owned policy enter, and how are payload and
  pipeline-description trust represented independently?
. How does pipeline addressability/authorization coordinate with ingress while
  preserving addressability != payload validation and preparation !=
  capability activation?
. What rejection, diagnostic, and failure-atomicity guarantees apply when a
  boundary or later metadata introduction fails?

==== Already decided by ADR/Technical Model

* Runtime/operator trust is a fixed precondition; payload and pipeline
  description trust are independent variables.
* Operator-owned policy is outside a less-trusted description; restrictions
  narrow by payload/operation kind; rejection precedes capability activation.
* Output-sink capability is separately owned.
* Domain owns semantic invariants and final cleanup; persistence projects
  metadata; writers serialize rather than becoming payload validators.
* XLSX/ODS visible cells use the string substrate unless explicit typing is
  separately reopened.
* The six scalar categories classify without conversion; no general
  `normalize_scalar` contract has been accepted.
* Reconstruction invariants and ingress validation are different
  responsibilities.

==== Deferred to later work

* family-by-family hardening, the Domain Maintainer Contract application, the
  first family pilot, and `D-T3` selection/start;
* `List[Scalar]` or other structured composite policy beyond leaving a hook;
* sink-specific formula/output policy changes;
* broad per-backend resource-limit projects outside the Phase-E boundary
  decision; and
* Pydantic adoption or a replacement config-model architecture, which would
  require its own design authority.

=== 19. Facts, gaps, and design questions

*Facts:* every router-backed framework execution converges on `orchestrate`;
current ingress is mandatory there and metadata-only; `run_pipeline` and direct
Domain use bypass the macro flow; shared scalar classification is unused;
string/config resolution is centralized except for trusted direct callables;
backend carrier behavior and metadata carriers differ.

*Gaps:* the accepted durable architecture's eventual mandatory-ingress
assumption is not universally implemented; less-trusted description policy and
activation separation are not represented in runtime; general metadata and
scalar assumptions are not established.

*Design questions:* the 12 questions above determine coverage, contracts,
sequencing, policy, traversal, and atomicity. They are not converted here into
BUG/FTR items or implementation choices.

=== 20. Explicit non-decisions

This census does *not* conclude that all public Python APIs must route through
Trusted Ingress, that `run_pipeline` must be removed, that direct Domain calls
must be forbidden, that Pydantic should validate configuration, or that the
current `domain.ingress` coordinator is the final Phase-E mechanism. It does
not choose native-Python scalar normalization, a shared metadata/cell walker,
Domain ownership of pipeline addressability, identical backend carriers,
`List[Scalar]`, or a first family pilot. It does not write an ADR, update the
roadmap or Project Memory, authorize guards, or start implementation.

The only authorized next step is independent review of this reachability and
boundary census.
