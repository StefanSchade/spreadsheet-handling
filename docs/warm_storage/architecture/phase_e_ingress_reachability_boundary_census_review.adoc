[[warm-phase-e-ingress-reachability-boundary-census-review]]
== Independent Architecture Review -- Phase-E Ingress Reachability and Boundary Census

_Fresh-context evidence review of the pre-design census. This review does not
design or implement Phase E._

Status: *Accepted with corrections -- 0 Blocking / 3 Important / 1 Minor.*

Date: 2026-08-11.

Reviewed commit: `7a8f4600bf7b7149ffab337f3e57057687ce93a8`
(`docs(architecture): census Phase-E ingress reachability`).

Census lifecycle baseline: `60e0f261210110ab33be5bc94a30a0f3ed323a63`
(`docs(project_memory): record accepted domain architecture promotion`).

Review scope: documentation and evidence only. No Trusted Ingress design,
runtime, test, registry, ADR, Technical Model, roadmap, Project Memory, family
pilot, `D-T3`, Pydantic, guard, or release-note change is authorized by this
review.

=== 1. Baseline and independence

The review began on `main` at the reviewed commit with a clean worktree.
`git rev-parse 7a8f460^` returned the exact lifecycle baseline `60e0f261`.
The complete `60e0f261..7a8f460` delta contains only:

* the new 674-line census; and
* its two-line include in `_architecture.adoc`.

No runtime, test, registry, ADR, Technical Model, roadmap, or Project Memory
change is hidden in the census commit.

The census was read in full before verification, but only as a claim index.
The review then reconstructed authority, public entry surfaces, call chains,
registries, backends, metadata carriers, reverse paths, and output continuation
from maintained source and disposable probes.

=== 2. Governing authority reconstructed independently

The accepted authority establishes the following constraints without selecting
a Phase-E mechanism:

* `ADR-DOMAIN-BOUNDARY-ROBUSTNESS` and the normative Domain Boundary Robustness
  concept require proportionate validation for documented pandas, NumPy,
  configuration, metadata, and artifact contracts. They do not promise a
  sandbox for arbitrary Python already running in the trusted process.
* `ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY` makes runtime/operator trust a
  fixed precondition, keeps payload and pipeline-description trust independent,
  places less-trusted-input policy outside the less-trusted description, and
  requires rejection before capability activation. Importing an untrusted-
  selected Python module is explicitly capability activation.
* `ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB` assigns semantic cleanup and
  validation to Domain flow. Persistence projects metadata; writers serialize
  rather than becoming payload validators.
* `ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE` fixes maintained XLSX/ODS visible
  payload as strings unless that decision is separately reopened.
* ch06 keeps structural validation, payload ingress validity, Domain semantic
  invariants, and sink capability separately nameable.
* ch08 Internal Scalar Value Model owns the implemented six-category
  classification vocabulary but explicitly says no maintained ingress point
  enforces it. Classification does not normalize.
* ch08 Pipeline Semantics makes ingress macro flow rather than a configurable
  step, and keeps cleanup and persistence projection separate.
* the Spreadsheet Parse Contract ends format-specific parsing at `WorkbookIR`
  and generic projection at Frames plus persisted metadata; it does not claim
  that parsing is general Domain ingress validation.
* the provisional ch04 Trusted Ingress concept is useful historical evidence,
  including its description of `run_pipeline`, but is not mechanism authority.

The roadmap order is Phase E, then the Domain Maintainer Contract stage, then
Phase F family hardening. `D-T3` is a still-unselected family-pilot candidate
gated behind both Phase E and the Maintainer Contract.

At the lifecycle baseline, canonical Project Memory independently confirms the
handoff. `CONC-DOMAIN-BOUNDARY-ROBUSTNESS.next_action` calls for the repository-
wide Phase-E ingress/reachability/boundary census before design, explicitly
naming CLI/orchestration, `run_pipeline`, direct APIs, loaders, dotted
resolution, reconstruction, and bypasses. The accepted promotion event
`SIG-ACT-DOMAIN-TECHNICAL-MODEL-PROMOTION-ACCEPTED` says Phase E and `D-T3`
remain unstarted. The census therefore was the recorded next dependency-ready
action.

=== 3. Independent grouping rule and path map

This review calls a path family materially distinct when at least one of these
changes:

* the supported initiating interface;
* the external carrier/parser and therefore the value or resource behavior;
* the description-binding/capability-activation route;
* the automatic macro-flow guarantees; or
* a structural carrier crossing the Domain boundary.

Aliases with the same implementation (`ods`/`calc`, `json`/`json_dir`,
`yaml`/`yaml_dir`, `xml`/`xml_dir`) are one family. A configured reverse path
using `ExactTable` is kept separate from its ordinary spreadsheet path because
the carrier and reconstruction invariants materially change. Cross-cutting
steps are participants, not new top-level call graphs.

Under that rule, source reconstruction produces the census's 21 rows plus one
missing cross-cutting participant: post-ingress payload introduction by a
plugin, arbitrary resolved callable, or caller-supplied `BoundStep`. The review
count is therefore *22 role-granularity families/participants*. This is not a
claim that the repository has 22 disjoint call graphs.

The independently reconstructed macro map is:

[source,text]
----
sheets-run
  -> safe YAML parse / selection
  -> build every configured step (string target import can happen here)
  -> orchestrate

run_app
  -> build configured steps
  -> orchestrate

sheets-schema-maintain
  -> run_schema_maintenance
  -> private fixed BoundStep
  -> orchestrate

orchestrate
  -> coerce descriptors
  -> router loader
  -> run_domain_ingress                         [legend_blocks only]
  -> run_pipeline(already-bound steps)
       -> bootstrap_meta/apply_overrides may merge metadata and re-run ingress
       -> arbitrary plugin/BoundStep may also replace payload without re-ingress
  -> execute_final_domain_cleanup
  -> project_meta_to_persistable_contract       [dict _meta only]
  -> router saver

run_pipeline
  -> call each supplied callable in order
  -> no automatic loader, binding, ingress, cleanup, projection, or saver

direct Python
  -> package-public or internal loader / caller Frames
  -> selected package-public or internal Domain callable
  -> only that callable's local contract
----

Maintained loader/carrier branches are XLSX, ODS/Calc, CSV directory, JSON
directory, YAML directory, and XML directory. The reverse spreadsheet branch
can additionally publish `ExactTable` when exact-header options are selected.

=== 4. Explicit path-verdict matrix

[cols="2,2,4",options="header"]
|===
| Census path ID | Verdict | Independent assessment

| `PATH-CLI-XLSX-ORCHESTRATE`
| VERIFIED AS WRITTEN
| Public `sheets-run`; binding precedes `orchestrate`; XLSX load, initial LB
  ingress, configured steps, cleanup, projection, and saver are verified.

| `PATH-CLI-ODS-ORCHESTRATE`
| VERIFIED AS WRITTEN
| Same macro flow with the ODS parser and its explicit repeat/extent limits.
  `calc` is an alias, not another path.

| `PATH-CLI-CSV-ORCHESTRATE`
| VERIFIED AS WRITTEN
| `csv_dir` uses the string-bounded directory loader and converges on the same
  orchestration seam.

| `PATH-CLI-JSON-ORCHESTRATE`
| VERIFIED AS WRITTEN
| JSON tables and the optional dict-only YAML metadata sidecar converge on
  orchestration.

| `PATH-CLI-YAML-ORCHESTRATE`
| VERIFIED AS WRITTEN
| Safe-loaded YAML retains non-missing rich standard carriers where pandas
  permits; `_meta.yaml` is an ordinary DataFrame-valued frame.

| `PATH-CLI-XML-ORCHESTRATE`
| VERIFIED AS WRITTEN
| ElementTree flattened leaf text converges on orchestration. The source's
  trusted-local-file comment is not a general security guarantee.

| `PATH-PY-ORCHESTRATE`
| VERIFIED AS WRITTEN
| `application.orchestrator.orchestrate` and the root compatibility shim are
  intentional imports. The caller supplies already-bound steps.

| `PATH-PY-RUN-APP`
| VERIFIED AS WRITTEN
| `pipeline.run_app` is a package re-export and compatibility adapter over
  `orchestrate`; it selects only the first configured input.

| `PATH-CLI-SCHEMA-MAINTENANCE`
| VERIFIED AS WRITTEN
| Public command with a fixed application-owned private step and no YAML step
  addressability.

| `PATH-PY-SCHEMA-MAINTENANCE`
| VERIFIED WITH RECLASSIFICATION
| The callable is a deliberate application use-case seam used by the CLI and
  has module `__all__`, but it is not named by the small consumer-facing Python
  import guide. Classify it as package-public/undocumented, not an unqualified
  supported consumer API.

| `PATH-PY-RUN-PIPELINE-REGISTRY`
| VERIFIED AS WRITTEN
| Public builder plus public `run_pipeline`; binding occurs only because the
  caller explicitly asks the builder to do it.

| `PATH-PY-RUN-PIPELINE-DOTTED`
| VERIFIED AS WRITTEN
| The shared resolver imports at binding; `run_pipeline` itself performs no
  resolution or authorization.

| `PATH-PY-RUN-PIPELINE-PREBOUND`
| VERIFIED AS WRITTEN
| The public execution function accepts callables and performs only linear
  execution.

| `PATH-STEP-BOOTSTRAP-META`
| VERIFIED AS WRITTEN
| Registered infrastructure participant; post-merge LB ingress executes before
  the merged metadata is set.

| `PATH-STEP-YAML-OVERRIDES`
| VERIFIED AS WRITTEN
| Registered infrastructure participant; external or inline overrides merge,
  then post-merge LB ingress executes.

| `PATH-PY-DIRECT-DOMAIN`
| VERIFIED WITH RECLASSIFICATION
| Reachability is real, but this row aggregates explicit package APIs, package-
  public undocumented functions, registry-target modules, and internal/test
  helpers. It cannot uniformly be called public, supported, or intentional.

| `PATH-PY-DIRECT-SCHEMA-MAINTENANCE`
| VERIFIED AS WRITTEN
| `domain.schema_maintenance` explicitly re-exports the request/result model and
  operations. This is a supported lower-level primitive with a stronger caller
  precondition.

| `PATH-PY-BACKEND-TO-DOMAIN`
| VERIFIED WITH RECLASSIFICATION
| The composition exists, and `io_backends` explicitly exports `make_backend`
  plus selected backend classes. Individual convenience loaders are not
  uniform: some are package exports, some router-facing, and some explicitly
  test-facing or module-only. Support must be stated per backend surface.

| `PATH-ORCH-XLSX-GROUPED-RECONSTRUCT`
| VERIFIED AS WRITTEN
| Exact XLSX projection produces `ExactTable`; reconstruction and grouped
  expansion run after initial ingress and enforce local geometry/mapping rules.

| `PATH-ORCH-ODS-GROUPED-RECONSTRUCT`
| VERIFIED AS WRITTEN
| Same structural carrier and Domain reconstruction after the bounded ODS
  parser.

| `PATH-PY-GROUPED-ROUNDTRIP`
| VERIFIED AS WRITTEN
| User documentation, package exports, registry entries, and the public GX5
  integration flow positively support this configured lower-level composition.

| `PATH-XCUT-POST-INGRESS-STEP-PAYLOAD`
| MISSING PATH FOUND
| A configured plugin/arbitrary target or direct `BoundStep` may create or
  replace Frames after initial ingress. `run_pipeline` forwards its return value
  directly to the next step. No automatic ingress re-runs unless the callable
  itself invokes it. The target universe and resulting payload are DYNAMIC/OPEN.
|===

No census row is a dead or impossible product path. The test-facing
`write_xlsx` helper and arbitrary private helper imports were correctly not
counted. Single-file CSV backend methods, `make_backend`, custom
`SpreadsheetParser`, and format aliases are lower-level variants grouped under
backend-to-Domain rather than separate inflated rows.

=== 5. `orchestrate` order and runtime probes

Source and an independent disposable probe establish this fixed order:

[source,text]
----
_coerce_io(input), _coerce_io(output)
-> get_loader/load payload
-> domain.ingress.run_domain_ingress
-> run_pipeline(already-bound steps), when present
-> execute_final_domain_cleanup
-> project_meta_to_persistable_contract, only when _meta is dict
-> get_saver/save
-> return the saved-shape Frames
----

`bootstrap_meta` and `apply_overrides` are not fixed stages. If selected as
ordinary bound steps, each prepares a metadata merge, runs ingress on a
temporary `{"_meta": candidate}` mapping, and only then sets the metadata.
They therefore add later ingress calls without changing the orchestrator's one
fixed initial call.

Step binding is outside `orchestrate`. `sheets-run` and `run_app` bind every
configured step before entering it; a configured module import therefore occurs
before backend payload load. Direct `orchestrate` callers already possess
callables/`BoundStep` objects, so no addressability/import work is performed by
the function. Backend selection does not change this order.

Independent probe output:

[source,text]
----
ORCHESTRATE load > ingress > step > ingress > cleanup > persistence > save
RUN_PIPELINE step-only
CLI_BIND import:review_probe_plugin > orchestrate-entry
POST_INGRESS_REPLACEMENT ingress > custom-introduce > next-step-saw-opaque:True
----

The first probe used the real public orchestration function with load/save,
cleanup, and projection seams replaced only to record calls; the selected step
called real `bootstrap_meta`. The binding probe ran the real CLI `main` and real
step builder with a temporary YAML file and in-memory module. The final probe
used public `orchestrate` plus two real `BoundStep` objects to demonstrate that
a replacement payload introduced after initial ingress reaches the next step.
No probe script or fixture was retained.

=== 6. Independent `run_pipeline` classification

Verdict: *INTENTIONAL TRUSTED PROGRAMMATIC SURFACE*.

Evidence:

* `pipeline.execution.run_pipeline` sets `out = frames`, calls every supplied
  callable in order, and returns the final value.
* `pipeline.__init__` explicitly re-exports it.
* ch08 Internal Scalar Value Model normatively calls it the documented trusted-
  programmatic-composition surface.
* the accepted/provisional trust history, integration uses, and GX5 user-facing
  roundtrip establish non-test-only consumer use.
* expected steps are pre-bound callables. Registry, dotted resolution, and
  factory binding happen only if the caller separately uses a builder.

It does not perform ingress, persistence projection, final cleanup, save,
registry lookup, addressability checks, or runtime Frames validation. “Trusted”
is therefore a stronger caller/operator precondition, not a property the
function constructs. Whether Phase E preserves, gates, splits, or covers this
surface remains a design question.

=== 7. Direct Domain and backend API classification

`domain.__init__` and `domain.transformations.__init__` are empty, so there is
no universal Domain barrel. Representative classification is:

[cols="2,2,4",options="header"]
|===
| Representative surface | Class | Can an external library consumer reasonably call it?

| `domain.transformations.grouped_xref`
| A/E -- explicitly supported public pipeline/composite and deliberate lower-
  level constructors
| Yes for the composites, reconstruction, carrier, and documented constructor;
  the package docstring marks some model/projection primitives feature-internal.

| `domain.schema_maintenance`
| A/B -- explicit package-root API with stronger typed preconditions
| Yes. Models and operations are deliberately re-exported.

| `domain.transformations.cell_codec`, discriminator split, FK helpers,
  enrich lookup, and XRef crosstable
| A/B/E by exported symbol
| Yes for package-root exports used by the registry/documentation; no for
  underscore/private siblings merely reachable by module import.

| `domain.validations`, `domain.extractions`, `domain.tabular`
| B/E -- package-public lower-level primitives
| Reasonable for exported names, subject to local contracts; not a promise that
  every sibling module is stable.

| arbitrary registry target module or private helper
| C/D -- importable internal or test convenience
| No supported-public inference follows from importability alone.

| `io_backends.make_backend` and root-exported backend classes
| B/E -- explicit package-public lower-level adapter API
| Yes, with caller-owned composition and no automatic ingress.

| module convenience loaders/savers
| B/C/D by backend
| ODS package exports and documented/router-facing helpers have stronger
  evidence; XLSX/JSON/CSV/YAML/XML helper export and documentation strength is
  not uniform. `write_xlsx` is explicitly test-facing and not product reachability.
|===

Every direct call bypasses automatic addressability, ingress, final cleanup,
and persistence projection unless the selected callable itself is one of those
mechanisms. Local transformation, constructor, or reconstruction invariants do
not become a general payload grammar.

=== 8. `domain.ingress` production census

Repository-wide symbol and import tracing confirms exactly three production
source call sites and three semantic uses:

. `application/orchestrator.py` -- one initial call after every router load;
. `domain/meta_bootstrap.py` -- one post-merge call before setting metadata; and
. `domain/yaml_overrides.py` -- one post-merge call before setting metadata.

Tests invoke ingress directly, but those are not production call sites. The
bootstrap/override calls operate on a temporary mapping containing only the
candidate metadata; the orchestrator call operates on the complete loaded
Frames mapping. They share the coordinator but are not the same state shape.

`INGRESS_RULES` contains exactly one `IngressRule`, `legend_blocks_shape`.
It:

* is a no-op without mapping-shaped `_meta.legend_blocks`;
* maps `None` to `{}`;
* converts list authoring sugar to a mapping after member/identity checks;
* copies mapping form; and
* drops each block's render-derived `resolved` facet.

It does not inspect DataFrame cells, `ExactTable.data`, other metadata roots,
schema, policy, grouped carriers, or scalar values. “Only legend_blocks is
normalized” is confirmed specifically as a claim about the current
`domain.ingress` rule set. Parser-local workbook metadata canonicalization,
missing replacement, presentation-carrier reconciliation, family configuration,
cleanup, and persistence projection are separate operations and must not be
silently renamed ingress.

=== 9. Shared scalar production reachability

The four symbols were checked separately:

[cols="2,2,3",options="header"]
|===
| Symbol | Production callers outside `core/scalar_values.py` | Result

| `scalar_category` | 0 | No maintained ingress enforcement.
| `is_supported_scalar` | 0 | No maintained yes/no scalar gate.
| `is_missing_carrier` | 0 | Local missing predicates remain independent.
| `UnsupportedScalarError` | 0 | Raised only by the shared classifier module.
|===

The ODS renderer's `_is_missing_carrier` and other local pandas-missing checks
do not import the shared helper. No maintained ingress path enforces the six-
category String/Boolean/Number/Missing/Date/DateTime vocabulary today.

=== 10. Description/control-plane binding

All maintained *configuration-derived callable strings* pass through
`pipeline.dotted_paths.resolve_configuration_callable`:

* a colon-style `step:` factory through `resolve_registration`;
* the public `plugin` step's `dotted` value;
* the 38 string targets among the 44 registry entries; and
* nested string targets accepted by the generic frames-first/builder binders.

The resolver accepts colon or last-dot syntax, rejects only the complete
`spreadsheet_handling.domain.ingress` namespace, imports the module, reads the
attribute, and checks callability. The other six registry entries are bare
factories. Direct callable targets and prebuilt `BoundStep` objects intentionally
bypass string resolution.

“Centralized” is therefore confirmed only for configured strings, not for all
ways Python code can become a step. The resolver is an ownership/addressability
boundary, not authorization. Safe syntax parsing and namespace checking precede
import, but there is no operator allowlist or less-trusted-description policy.
For CLI and `run_app`, module import occurs during binding before payload load;
that is capability activation under the accepted ADR.

=== 11. Backend carrier audit

[cols="1,3,3,3,3",options="header"]
|===
| Backend | Parser / Frames construction | Visible-cell behavior | Missing/date/formula | Metadata and limits

| XLSX
| OpenPyXL `data_only=True` -> `WorkbookIR` -> generic projection
| Headers and data are stringified; exact mode publishes `ExactTable` whose
  maintained parser data is still strings
| `None -> ""`; numbers, booleans, and dates become strings; formulas contribute
  cached results, or `""` when no cached value exists
| Hidden metadata blob is JSON with legacy literal fallback and canonical
  projection. No framework parser ceiling comparable to ODS was found.

| ODS/Calc
| odfpy -> bounded parsed grid -> `WorkbookIR` -> generic projection
| ODF value types/display text collapse to strings; exact mode publishes
  `ExactTable`
| Empty -> `""`; boolean/float/date/time are rendered text; formula/display
  interpretation is parser-local
| Hidden metadata uses JSON with legacy literal fallback. `ParserLimits`
  enforce 1,048,576 rows, 16,384 columns, and 10,000,000 materialized cells.

| CSV directory
| pandas `read_csv`
| String-bounded by `dtype=str`, `keep_default_na=False`, `na_values=[]`
| Blank is `""`; no date or formula interpretation
| No metadata sidecar and no framework row/file/resource limit.

| JSON directory
| pandas `read_json(dtype=str, convert_dates=False)` then missing replacement
| Numbers, booleans, nested mappings, and lists are stringified by the maintained
  read path
| pandas missing -> `""`; private temporal object hook is not called
| Optional `_meta.yaml` is safe-loaded and accepted only when its root is dict;
  no framework size/depth limit.

| YAML directory
| `yaml.safe_load`, branch to pandas construction, then missing replacement
| Rich safe-loaded non-missing values can remain: date, Timestamp, bytes, list,
  and dict were reproduced in object cells
| pandas-recognized null -> `""`; date/datetime may survive or be inferred
| `_meta.yaml` is an ordinary DataFrame-valued frame; safe load blocks arbitrary
  constructors but no framework size/alias/depth limit is imposed.

| XML directory
| ElementTree -> flattened records -> DataFrame
| Leaf text only, with dotted columns for nested elements
| Absent text/fields -> `""`; no date/formula interpretation
| No metadata sidecar and no framework node/size/depth limit.
|===

The review reproduced representative carriers at runtime, including JSON nested
stringification, YAML rich object cells, CSV leading-zero preservation, XML
flattened text, and typed XLSX/ODS values collapsing to strings. These facts do
not imply one identical carrier contract across spreadsheet, structured, and
direct-programmatic Frames.

=== 12. Metadata input and normalization

Independent metadata flows are:

* XLSX/ODS hidden carriers -> structured mapping plus parser-owned carrier
  reconciliation -> Frames `_meta` -> initial LB ingress;
* JSON `_meta.yaml` -> safe-loaded dict only -> initial LB ingress;
* YAML `_meta.yaml` -> ordinary DataFrame frame -> LB ingress no-op;
* CSV/XML -> no external metadata mapping;
* direct Frames -> arbitrary `_meta` value and no automatic ingress unless the
  chosen high-level surface is `orchestrate`;
* `bootstrap_meta` -> profile defaults, persisted metadata, and runtime/CLI
  overrides -> merge -> LB ingress;
* `apply_overrides` -> inline or safe-loaded YAML metadata -> selected merges ->
  LB ingress; and
* ordinary Domain steps -> internally generated policy, provenance, cleanup,
  workbook-view, or family metadata under their local contracts.

Metadata and cell ingress are independent in current code. Only
`_meta.legend_blocks` receives `domain.ingress` shape normalization. Other
parser/bootstrap/configuration transformations are real, but semantically
different. Internally generated metadata is not external ingress merely because
it lives under `_meta`.

The census's “two metadata paths” is confirmed only as “two cross-cutting
post-load metadata-introduction steps” (`bootstrap_meta`, `apply_overrides`), not
as a total census of metadata origins.

=== 13. Reconstruction and reverse paths

XLSX/ODS exact-header options attach a lossless string header grid and publish
`ExactTable`. `ExactTable` owns tuple/string carrier conversion but does not by
itself validate rectangular geometry. `reconstruct_grouped_matrix` then checks:

* exact carrier identity;
* configured header depth and rectangular header/data widths;
* unique row-key positions and blank upper row-key cells;
* complete/unique visible dynamic tuples;
* live source-axis resolution; and
* validated `GroupedMatrix` construction before atomic publication.

These are strong local reconstruction and canonical-carrier invariants, not a
general scalar, Frames, or metadata ingress contract. The registered workbook-
view mapping and reconstruction steps run after initial orchestrator ingress.
The documented/public GX5 composition can instead call loaders, builders,
`run_pipeline`, and savers directly; it bypasses automatic ingress, cleanup, and
persistence projection. A reconstructed carrier can therefore reach ordinary
Domain transformations without `domain.ingress` on that programmatic route.

=== 14. Framework-managed, bypass, public, and trusted wording

The evidence supports these review meanings:

[cols="2,4",options="header"]
|===
| Term | Evidence-backed meaning

| framework-managed
| A high-level entry whose fixed application macro flow owns loader, initial
  ingress, final cleanup, persistence projection, and saver. It does not mean
  every action or new payload created inside an arbitrary configured callable is
  framework validated.

| public
| Explicit CLI/YAML surface or intentional Python import named by exports,
  interface guidance, user documentation, or a feature contract. Ordinary
  module importability is insufficient.

| supported
| Positive stability/use evidence exists. Package-public but undocumented and
  internal/test surfaces must remain distinguishable.

| intentional bypass
| Positive evidence establishes a lower-level composition contract with a
  stronger caller precondition. Absence of ingress alone is not intent evidence.

| trusted
| A runtime/operator/caller precondition for programmatic composition, not a
  validation result manufactured by `run_pipeline` or direct Domain APIs.

| mandatory
| Automatically executed for every path explicitly inside a named guarantee;
  it cannot mean every Python-reachable path.
|===

The census generally uses these distinctions, but “7 intentional trusted-
programmatic bypass families” drifts across aggregate direct-Domain and backend
rows whose public/support evidence varies internally. That wording must be
corrected before it becomes a Phase-E contract premise.

=== 15. Trust-combination audit

[cols="2,2,2,2,4",options="header"]
|===
| Surface | Trust combination | Representable | Intended | Protected / undecided now

| `sheets-run` / `run_app`
| trusted payload / trusted description
| yes
| yes
| Local parser/binder/Domain checks and LB ingress only; no general trusted-
  payload construction.

| fixed schema-maintenance entry or trusted configured pipeline
| less-trusted payload / trusted description
| yes
| yes, as Phase-E motivation
| Not protected by a closed scalar/general-metadata ingress contract.

| configured dotted target
| trusted payload / less-trusted description
| yes
| architecture requires representability, not present safe support
| No trust flag/operator policy; import activates before payload load.

| configured dotted target plus external carrier
| less-trusted payload / less-trusted description
| yes
| not established as safely intended today
| Neither general payload ingress nor description authorization is present.

| `run_pipeline` / direct Domain / direct backend composition
| trusted payload / trusted programmatic description
| yes
| yes where the particular public surface has positive evidence
| Caller precondition plus operation-local checks; bypass contract remains a
  Phase-E decision.

| direct programmatic surface with a less-trusted description
| either payload state / less-trusted description
| Python can represent it
| not implied by the trusted-programmatic contract
| Undecided; direct callables bypass configured-string classification entirely.
|===

All four trust combinations are architecturally representable. The runtime does
not safely protect all four. “Less-trusted pipeline” is currently representation,
not a protection claim.

=== 16. Output continuation

On `orchestrate`, final Domain cleanup precedes the dict-only metadata
persistence projection, which precedes the router saver. The projection prunes
runtime metadata and Resolution facets; it does not traverse payload cells.
CSV/YAML/XML skip `_meta`; JSON writes dict metadata to a YAML sidecar; XLSX/ODS
render and embed metadata.

The XLSX renderer returns ordinary string values unchanged to OpenPyXL, so a
string beginning `=` is interpreted as a formula by that sink. ODS formula
emission uses its explicit intent path. This is sink capability, not a reason to
globally reject formula-looking payload text.

The three registry-exposed filesystem writers (`write_structured_yaml`,
`write_key_value_resources`, `write_artifact_manifest`) validate relative target
paths against resolved output roots. Those sink-local checks neither validate
payload generally nor protect activation selected by a less-trusted description.
The census preserves the accepted writer/persistence/Domain responsibility
separation.

=== 17. Dynamic/open evidence and probe reproducibility

The arbitrary installed module universe, plugin side effects, direct callables,
Python library consumers, and monkeypatched/custom step behavior are genuinely
DYNAMIC/OPEN. Static search can verify maintained resolver call sites and
registry objects, but cannot close calls made by imported external code.

Confidence labels are justified as follows:

* orchestrator order, `run_pipeline` step-only behavior, configured import
  timing, backend carriers, and post-ingress replacement were reproduced at
  runtime;
* ingress/scalar call-site counts, exports, and fixed call chains are VERIFIED
  STATIC, with focused tests as supporting—not defining—evidence;
* intent for every importable internal Domain/helper module remains
  INFERRED/OPEN; and
* external plugin behavior remains DYNAMIC/OPEN.

The probes invoked real public functions. Monkeypatching replaced only I/O or
observation seams and did not replace the behavior being classified. Temporary
YAML and workbook artifacts lived under temporary directories. The probes are
deterministic for the maintained APIs and require no project-private fixture.

=== 18. Findings

==== PHASE-E-CENSUS-REVIEW-F1 -- Aggregate Python rows overstate intentional/public support

Severity: *Important*.

Census sections/paths: sections 3, 4, 10, and 15;
`PATH-PY-SCHEMA-MAINTENANCE`, `PATH-PY-DIRECT-DOMAIN`, and
`PATH-PY-BACKEND-TO-DOMAIN`; the “7 intentional trusted-programmatic bypass
families” total.

Observation and evidence: the consumer-interface guide says the Python surface
is intentionally small and explicitly warns against treating every module or
barrel export as public. `domain.__init__` and `domain.transformations.__init__`
export nothing. Selected family roots and `domain.schema_maintenance` do export
deliberate APIs; arbitrary registry targets/private helpers do not. Likewise,
`io_backends` exports `make_backend` and selected classes, while convenience
loader/saver visibility and documentation differ by backend; `write_xlsx` is
explicitly test-facing. The aggregate rows acknowledge some ambiguity but the
headline count still calls all seven intentional.

Why it matters: Phase-E design must not impose, exclude, or preserve a public
contract based on ordinary Python reachability. The stronger caller precondition
is load-bearing only for surfaces the repository actually intends consumers to
use.

Smallest documentation-only correction: label the application schema callable
package-public/undocumented; split the direct-Domain and backend-composition
rows' Surface/Trust verdict by evidence class; replace “7 intentional” with
“7 bypass rows: 5 positively intentional/supported at row granularity and 2
mixed aggregates requiring per-surface classification.” No runtime ticket is
created.

==== PHASE-E-CENSUS-REVIEW-F2 -- Post-ingress payload introduction by executable steps is not mapped

Severity: *Important*.

Census sections/paths: executive graph; sections 4, 6, 15, 17, and 19; all
orchestrated configured-operation rows.

Observation and evidence: `run_pipeline` assigns every callable's return value
to `out` and supplies it to the next step. `make_plugin_step` permits a plugin to
return replacement Frames; direct `BoundStep` has the same ability. The accepted
Pipeline Semantics explicitly says plugins may create, replace, drop, or
materialize frames. `orchestrate` invokes automatic ingress only before
`run_pipeline`; only the two known metadata merge steps voluntarily re-run it.
The independent runtime probe observed
`ingress > custom-introduce > next-step-saw-opaque:True`. The census correctly
marks arbitrary plugin calls DYNAMIC/OPEN, but does not state the resulting
post-ingress payload path in the payload matrix or mandatory-coverage analysis.

Why it matters: a future claim about all framework-managed external payload
cannot be inferred from initial loader convergence alone. Phase-E design must
explicitly define whether step-introduced payload is outside framework-managed
coverage, requires a separate contract, or participates in a future boundary.
This is independent of sandboxing arbitrary Python.

Smallest documentation-only correction: add one DYNAMIC/OPEN cross-cutting row
for plugin/custom-step payload introduction; qualify the 12-path convergence
claim as covering router-loaded payload up to the first configured step; add an
unresolved coverage question for payload introduced/replaced after that seam.
No runtime ticket is created.

==== PHASE-E-CENSUS-REVIEW-F3 -- The 12-question design inventory is not yet sufficient

Severity: *Important*.

Census section: section 18, “Phase-E design questions”; core claim 17.

Observation and evidence: the 12 questions cover high-level entries, public
guarantees, `run_pipeline`, direct APIs, scalar/metadata traversal, ordering,
structural carriers, normalization, operator policy, capability activation, and
failure atomicity. Three load-bearing questions are only implicit or absent:

* the operational definition of “framework-managed” used by the guarantee;
* treatment of payload created/replaced by a plugin, arbitrary dotted target,
  or caller-supplied step after initial ingress; and
* the required future bounded-composite extension hook (`List[Scalar]` or an
  equivalent bounded composite), which the census currently places only in a
  deferred-work bullet.

Why it matters: the first two determine the truthful coverage boundary; the
third prevents a scalar-only Phase-E design from accidentally foreclosing the
already accepted open composite candidate.

Smallest documentation-only correction: expand the mandatory design-question
inventory from 12 to 15 with those questions, without answering any of them or
changing the already-decided/deferred lists. No design or runtime ticket is
created.

==== PHASE-E-CENSUS-REVIEW-F4 -- Numeric totals are role-granularity summaries, not stable architecture facts

Severity: *Minor*.

Census sections: 3, 4, 15, 17, and 19.

Observation and evidence: backend variants are split where carriers differ,
while entry wrappers (`sheets-run`, `run_app`, direct `orchestrate`) overlap;
schema CLI/application are two entry surfaces over one use case;
reconstruction rows are subpaths of XLSX/ODS; backend-to-Domain composes two
other families; metadata rows are participants rather than initiating paths.
The census acknowledges role-based, non-disjoint counting, but repeatedly
presents 21/12/7/2 as if exact totals were architecture properties. Adding the
missing dynamic participant yields 22 without changing the central map.

Why it matters: later design review could mistake a harmless regrouping for a
coverage regression or force new paths into misleading bins.

Smallest documentation-only correction: make the matrix and grouping rule
authoritative; label all totals illustrative role-granularity summaries and
report mixed classifications rather than forcing them into “intentional.”

=== 19. Count and coverage reconciliation

[cols="3,1,1,1,5",options="header"]
|===
| Category | Census count | Review count | Match? | Explanation

| materially distinct path families/participants
| 21
| 22
| no
| The review adds the DYNAMIC/OPEN post-ingress payload-introduction
  participant. Both totals use role granularity rather than disjoint graphs.

| framework-managed high-level families
| 12
| 12
| yes, qualified
| The same six CLI backend branches, direct orchestrate, run_app, two schema
  entries, and two exact-reconstruction branches are present. “Managed” covers
  their fixed macro flow, not arbitrary payload created by a step.

| intentional trusted-programmatic bypass families
| 7
| 5 positive + 2 mixed aggregates
| no
| All seven bypass rows are reachable. `run_pipeline`'s three variants, direct
  schema Domain API, and grouped roundtrip have positive row-level intent
  evidence. Aggregate direct-Domain and backend-to-Domain rows require per-
  surface support classification.

| metadata cross-cutting paths
| 2
| 2
| yes, qualified
| Exactly bootstrap and YAML overrides are post-load metadata-introduction
  participants. They are not the complete metadata-origin count.

| unresolved mandatory Phase-E design questions
| 12
| 15
| no
| Add framework-managed definition, post-ingress step-introduced payload, and
  the bounded-composite extension hook.
|===

=== 20. Phase-E design-question assessment

[cols="1,5,3",options="header"]
|===
| # | Census question | Assessment

| 1
| Exact covered framework entries and carrier variants
| REQUIRED DURING PHASE-E DESIGN

| 2
| Guarantees of CLI, schema maintenance, `run_app`, `orchestrate`, and shim
| REQUIRED DURING PHASE-E DESIGN

| 3
| `run_pipeline` contract/treatment
| REQUIRED DURING PHASE-E DESIGN

| 4
| Direct documented Domain/package APIs versus internals
| REQUIRED DURING PHASE-E DESIGN

| 5
| Scalar traversal and what successful six-category classification establishes
| REQUIRED DURING PHASE-E DESIGN

| 6
| Metadata roots/carriers and relation to cell traversal
| REQUIRED DURING PHASE-E DESIGN

| 7
| Ordering relative to parse/load/bootstrap/overrides/reconstruction
| REQUIRED DURING PHASE-E DESIGN

| 8
| DataFrame, `ExactTable`, grouped/canonical, and future structured carriers
| REQUIRED DURING PHASE-E DESIGN

| 9
| Permitted normalization and ownership
| REQUIRED DURING PHASE-E DESIGN

| 10
| Operator policy injection and independent trust axes
| REQUIRED DURING PHASE-E DESIGN; policy ownership and trust independence are
  ALREADY DECIDED BY ADR/TM, concrete transport/representation is not.

| 11
| Addressability/authorization coordination and capability activation
| REQUIRED DURING PHASE-E DESIGN; rejection-before-activation is ALREADY
  DECIDED BY ADR/TM, mechanism is not.

| 12
| Rejection, diagnostics, and failure atomicity
| REQUIRED DURING PHASE-E DESIGN

| 13 (missing)
| Operational definition of “framework-managed”
| REQUIRED BEFORE PHASE-E DESIGN uses that term as its coverage premise.

| 14 (missing)
| Payload introduced/replaced by post-ingress executable steps
| REQUIRED DURING PHASE-E DESIGN

| 15 (missing)
| Future bounded-composite extension hook without deciding `List[Scalar]`
| REQUIRED DURING PHASE-E DESIGN
|===

The census correctly classifies these as already decided: fixed trusted
operator precondition; independent payload/description trust; operator-owned
policy and narrowing-only combination; rejection before activation; separate
sink capability; Domain semantic ownership; writer/persistence separation;
spreadsheet string substrate; classification without conversion; and
reconstruction versus ingress.

Family hardening, first-pilot selection, `D-T3`, Pydantic adoption, concrete
sink policy, and broad backend resource-limit projects remain LATER / OUT OF
SCOPE. The bounded-composite *choice* remains later/open, but preserving the
extension hook is a Phase-E design constraint and should not be deferred out of
the design inventory.

=== 21. Core-claim verdict matrix

[cols="1,5,3",options="header"]
|===
| # | Census claim | Verdict

| 1 | 21 materially distinct path families | REJECTED -- 22 under the stated
role granularity after adding post-ingress step payload introduction; totals are
not disjoint architecture facts.

| 2 | 12 framework-managed families | CONFIRMED WITH QUALIFICATION -- fixed
macro-flow entries/subpaths, not arbitrary step-created payload.

| 3 | 7 intentional trusted-programmatic bypass families | REJECTED -- seven
bypass rows exist, but only five have positive row-level intent/support evidence;
two aggregate mixed surfaces.

| 4 | 2 metadata cross-cutting paths | CONFIRMED WITH QUALIFICATION -- two
post-load metadata steps, not all metadata origins.

| 5 | `orchestrate` invokes ingress as framework execution | CONFIRMED.

| 6 | `run_pipeline` is an intended trusted-programmatic composition surface
| CONFIRMED -- caller precondition, not constructed trust.

| 7 | Direct Domain API support varies by family | CONFIRMED.

| 8 | `run_domain_ingress` has three production call sites | CONFIRMED.

| 9 | Ingress currently normalizes only `_meta.legend_blocks` | CONFIRMED --
specifically the `domain.ingress` rule set.

| 10 | `scalar_category` has no production ingress wiring | CONFIRMED.

| 11 | Configured dotted-target resolution is centralized | CONFIRMED WITH
QUALIFICATION -- all configured strings, not direct callables/BoundSteps; it is
addressability, not authorization.

| 12 | Dotted target import occurs during binding before payload load
| CONFIRMED WITH QUALIFICATION -- CLI and `run_app` configured flows;
`orchestrate` receives already-bound steps and does no binding.

| 13 | Backend carrier behaviors differ as stated | CONFIRMED.

| 14 | Metadata ingress is independent of cell ingress | CONFIRMED WITH
QUALIFICATION -- metadata has multiple origins and only LB receives the current
shared ingress rule.

| 15 | Reconstruction invariants are not general ingress validation
| CONFIRMED.

| 16 | Current mandatory Trusted Ingress is not substantively established
| CONFIRMED.

| 17 | The listed design questions are sufficient after census acceptance
| REJECTED -- three questions must be made explicit before design begins.
|===

=== 22. Mandatory-ingress truth test and architecture implications

Claim A -- “framework-managed orchestration invokes a function named
`domain.ingress`” -- is true for all router-loaded `orchestrate` flows. The
initial call occurs after load and before the first configured/private step.

Claim B -- “framework-managed external payload is validated/normalized into a
trusted Domain payload contract before ordinary transformations” -- is false.
The function establishes only the Legend Blocks container shape and removes one
derived facet. It establishes no scalar grammar, general Frames shape,
`ExactTable` contract, general metadata vocabulary, resource-neutral trusted
carrier, or policy result. In addition, executable steps can introduce new
payload after the initial seam.

The evidence therefore proves only that:

* a framework-managed orchestration seam exists and is a plausible place to
  consider a future stronger contract;
* current ingress does not establish that contract;
* `run_pipeline` and selected lower-level/programmatic APIs exist under stronger
  caller preconditions and require explicit Phase-E contract treatment;
* direct-Domain support must be decided per actual public surface, not inferred
  from importability;
* payload ingress and pipeline-description authorization are independent
  coverage problems;
* metadata and cell payload have distinct current flows;
* reconstruction invariants do not replace general ingress; and
* an orchestrator guarantee cannot silently cover direct calls or payload
  introduced by arbitrary executable steps.

This means Phase E contains semantic normalization/validation, wiring/ordering,
and coverage-contract questions. This review does not choose their solution.

=== 23. Verdict

*ACCEPTED WITH CORRECTIONS -- CENTRAL MAP IS SOUND; BOUNDED DOCUMENTATION
CORRECTIONS ARE REQUIRED BEFORE PHASE-E DESIGN.*

The census accurately identifies the maintained high-level seam, the current
coordinator's narrow behavior, `run_pipeline` bypass, backend carrier
differences, metadata separation, resolver timing, reverse-path invariants, and
the absence of substantive mandatory Trusted Ingress. It is not yet sufficient
as-is because the post-ingress executable-step payload path is missing, aggregate
Python surfaces are over-classified, and the mandatory design-question list is
incomplete. Correcting those points requires census documentation only; it does
not authorize Phase-E implementation.

=== 24. Validation and residual risk

Validation performed:

[source,bash]
----
git status --short
git rev-parse HEAD
git log --oneline --decorate -30
git show --stat 7a8f460
git show 7a8f460
git rev-parse 7a8f460^
git diff 60e0f261..7a8f460 --stat
git diff 60e0f261..7a8f460

rg production call sites, exports, registry targets, loaders, parser projection,
reconstruction, cleanup, persistence, writers, and scalar symbols

.venv/bin/python -    # disposable ordering/binding/post-ingress probes
.venv/bin/python -    # disposable CSV/JSON/YAML/XML carrier probes
.venv/bin/python -    # disposable XLSX/ODS typed-carrier probes

asciidoctor census
asciidoctor review
asciidoctor containing architecture document
git diff --check
----

Residual risk is intentionally bounded: arbitrary installed plugins and unknown
external Python consumers cannot be statically enumerated. They remain
DYNAMIC/OPEN and must be handled by explicit coverage wording, not by pretending
the graph is closed. No lifecycle cleanup, release-note change, or Project
Memory update is authorized by this review.

=== Review 001 Disposition

This disposition records only the bounded documentation corrections requested
by Review 001. It does not change the original review verdict, accept the
corrected census, or introduce a Phase-E design/runtime decision. A fresh-
context follow-up review determines final acceptance.

==== PHASE-E-CENSUS-REVIEW-F1 — RESOLVED

The census now separates Python reachability, package-public export,
documented consumer API, deliberately supported lower-level API, and
internal/test-facing reachability. `PATH-PY-SCHEMA-MAINTENANCE` is labelled a
package-public deliberate application-use-case seam that is undocumented as a
general consumer API. `PATH-PY-DIRECT-DOMAIN` and
`PATH-PY-BACKEND-TO-DOMAIN` are MIXED aggregates requiring per-surface
classification. The seven bypass/reachability rows are reconciled as five with
positive row-level intent/support evidence and two mixed aggregates;
`run_pipeline` remains an intentional trusted-programmatic surface. No future
public/support or coverage contract was selected.

==== PHASE-E-CENSUS-REVIEW-F2 — RESOLVED

The census adds `PATH-XCUT-POST-INGRESS-STEP-PAYLOAD` to the executive graph,
matrix, payload and orchestration analysis, bypass coverage, mandatory-ingress
truth test, and design inventory. It records that `run_pipeline` forwards each
step's return directly, that plugin/custom/`BoundStep` code may create or
replace Frames, and that only an explicitly ingress-calling step receives a
later pass. Step-created provenance remains DYNAMIC/OPEN; no plugin-output
trust rule, post-step ingress, or second contract was chosen.

==== PHASE-E-CENSUS-REVIEW-F3 — RESOLVED

The illustrative design-question inventory now contains 15 explicit questions,
adding the operational definition of framework-managed, treatment of payload
created/replaced after initial ingress, and the future bounded-composite
extension seam. The actual bounded-composite category, codec, schema, and
representation remain deferred, and none of the questions is answered by the
census correction.

==== PHASE-E-CENSUS-REVIEW-F4 — RESOLVED

The census now defines its role-granularity grouping rule and makes the matrix
authoritative. The illustrative summary is 22 paths/participants: 12 qualified
framework-managed high-level families, 7 bypass/reachability rows (5 positive
at row level and 2 mixed aggregates), 2 post-load metadata-introduction
participants, and 1 post-ingress executable-step payload participant. The
counts are explicitly non-disjoint and non-normative, so no architecture or
runtime decision follows from the regrouping.

=== Independent Follow-up Review 002

_Fresh-context follow-up evidence review of the corrected census. This section
does not design or implement Phase E._

Date: 2026-08-11.

Correction commit reviewed:
`4151eaea1a80c84f5e515d7a736e6f196075be3c`
(`docs(architecture): resolve Phase-E census review findings`).

Review 001 commit:
`7c2152ec34dd1ccbff260f962d30f1dfb929d3a6`
(`docs(architecture): review Phase-E ingress reachability census`).

Original census commit:
`7a8f4600bf7b7149ffab337f3e57057687ce93a8`
(`docs(architecture): census Phase-E ingress reachability`).

Status: *ACCEPTED — ALL REVIEW 001 FINDINGS ARE RESOLVED; THE PHASE-E
REACHABILITY CENSUS IS ACCEPTED AS THE EVIDENCE BASIS FOR PHASE-E DESIGN.*

==== Baseline, independence, and correction scope

The follow-up began on `main` at exact HEAD `4151eaea`, with a clean worktree.
`git rev-parse 4151eaea^` returned exact Review 001 commit `7c2152ec`. The
complete correction delta modifies only this review artifact and
`phase_e_ingress_reachability_boundary_census.adoc`: 240 insertions and 66
deletions across those two documentation files. No runtime, test, registry,
ADR, Technical Model, roadmap, Project Memory, Phase-E design, family-pilot,
or `D-T3` change is hidden in the correction.

Both corrected documents were read in full. The appended Review 001
dispositions were treated only as claims. Each original defect was
reconstructed from Review 001 and tested against maintained source, exports,
interface guidance, accepted Technical Model evidence, and a new disposable
runtime probe.

==== Original finding dispositions

[cols="3,2,7",options="header"]
|===
|Original Finding |Disposition |Independent evidence

|`PHASE-E-CENSUS-REVIEW-F1`
|RESOLVED
|The corrected matrix and sections 3, 10, and 15 separate Python reachability,
package-public export, documented consumer API, deliberately supported lower-
level API, and internal/test-facing reachability. The application schema-
maintenance callable is package-public and deliberately used by its CLI but is
not promoted into the small documented consumer import surface. Direct-Domain
and backend-to-Domain remain visibly MIXED aggregates. The seven reachability
rows reconcile to five with positive row-level support/intent evidence and two
mixed aggregates.

|`PHASE-E-CENSUS-REVIEW-F2`
|RESOLVED
|The new `PATH-XCUT-POST-INGRESS-STEP-PAYLOAD` participant is present in the
executive graph, authoritative matrix, payload analysis, orchestration order,
bypass classification, confidence register, mandatory-ingress truth test,
design inventory, facts/gaps summary, and explicit non-decisions.
`run_pipeline` assigns each step return directly to the next input. A fresh
runtime probe demonstrated replacement by a dotted plugin, generic dotted-
target forwarding, and caller-supplied `BoundStep` forwarding with no
automatic post-step ingress. Provenance remains DYNAMIC/OPEN and callable-
dependent rather than automatically external or untrusted.

|`PHASE-E-CENSUS-REVIEW-F3`
|RESOLVED
|The design inventory now contains all 15 questions, including an operational
future definition of framework-managed, future treatment of payload created or
replaced by executable steps after initial ingress, and preservation of a
future bounded-composite extension seam. They are written as questions. No
post-step ingress rule, plugin trust classification, composite codec/schema,
or `List[Scalar]` representation is selected.

|`PHASE-E-CENSUS-REVIEW-F4`
|RESOLVED
|Section 3 now states the grouping rule, makes the path matrix authoritative,
and labels totals illustrative, overlapping, non-disjoint, and non-normative.
The matrix contains 22 role-granularity rows/participants and reconciles as
12 qualified framework-managed high-level families, 7 bypass/reachability
rows, 2 post-load metadata-introduction participants, and 1 post-ingress
executable-step payload participant. Counts do not drive the future contract.
|===

==== F1 — support and reachability reconstruction

The original defect was the census's ability to turn real Python reachability
into a uniform public/support/intent conclusion, especially in
`PATH-PY-SCHEMA-MAINTENANCE`, `PATH-PY-DIRECT-DOMAIN`, and
`PATH-PY-BACKEND-TO-DOMAIN`.

Primary evidence still shows:

* `docs/ai_info/interfaces_and_gates.adoc` keeps the intended Python import
  surface small and explicitly rejects treating every internal module or
  barrel export as public API;
* `application/schema_maintenance.py` declares `run_schema_maintenance` in
  module `__all__`, and the public schema-maintenance CLI calls it, while the
  consumer import guide does not name it as a general library surface;
* `pipeline.__init__` deliberately exports `run_pipeline`, and the accepted
  Internal Scalar Value Model calls it the documented trusted-programmatic-
  composition surface;
* `domain.__init__` and `domain.transformations.__init__` provide no universal
  public barrel, while selected family package roots—including
  `domain.schema_maintenance` and grouped xref—do provide deliberate exports;
  and
* `io_backends.__init__` exports `make_backend` and selected backend classes,
  but convenience-loader, module-only, router-facing, and test-facing evidence
  is not uniform.

The exact correction replaces the uniform classification with explicit
evidence classes, relabels the application use case, keeps the two aggregates
MIXED, and narrows the seven-row summary to five positive rows plus two mixed
aggregates. It does not establish a new public contract. The incorrect
inference that anything importable below `domain/` or `io_backends/` is a
supported trusted-programmatic API is contradicted explicitly and cannot
reasonably be derived from the corrected artifact.

==== F2 — post-ingress executable-step payload reconstruction

The original defect was omission of values introduced after orchestrator
ingress by executable pipeline code. Maintained runtime behavior is direct:

* `application.orchestrator.orchestrate` loads through the router, invokes
  `domain_ingress.run_domain_ingress` once on that loader result, and then
  delegates already-bound steps to `run_pipeline`;
* `pipeline.execution.run_pipeline` sets `out = frames`, assigns
  `out = step(out)` for every callable, and passes that return directly to the
  next callable;
* `BoundStep.__call__` invokes `fn` directly and has no ingress hook;
* plugin and generic frames-target binders preserve a non-`None` callable
  return as the replacement payload;
* configured dotted targets resolve/import during binding, whereas direct
  callables and pre-bound steps require no resolver; and
* `bootstrap_meta` and `apply_overrides` are maintained exceptions only because
  they explicitly call `run_domain_ingress` on their candidate metadata merge
  before publication.

The independent disposable probe used the public orchestrator, real plugin and
generic dotted binders, public `run_pipeline`, and real `BoundStep` objects.
Only I/O, cleanup, persistence, and ingress observation seams were replaced.
It produced:

[source,text]
----
ORCHESTRATED_PLUGIN=ingress:initial > replace:plugin > next:same-replacement > save
GENERIC_DOTTED=generic:same
PREBOUND=prebound:same
AUTO_POST_STEP_INGRESS=0
----

The plugin-created DataFrame contained an opaque object, and the following
step observed the identical replacement mapping and object. No tracked probe
file or fixture was created.

The correction maps this behavior without calling all step-created payload
external, without calling plugin output necessarily untrusted, and without
selecting post-step ingress. It also qualifies the 12-family convergence claim
as initial router-loaded payload reaching the initial seam. Consequently the
incorrect inference that orchestrator ownership already covers plugin-created
Frames is no longer derivable.

==== F3 — design-question inventory reconstruction

The original defect was that three load-bearing coverage questions were absent
or only implicit. The corrected inventory now asks explicitly:

. what operational definition of framework-managed the future coverage
  contract will use;
. how, if at all, payload created or replaced after initial ingress by a
  plugin, arbitrary dotted target, caller-supplied `BoundStep`, or other
  executable step participates in the future guarantee; and
. what extensibility seam must remain so a later explicitly declared bounded
  composite such as `List[Scalar]` can be added without replacing or bypassing
  the trust grammar, while deferring its actual category, codec, schema, and
  representation.

The wording is interrogative and solution-neutral. The other 12 questions
still cover exact entries/carriers, published entry guarantees,
`run_pipeline`, documented direct APIs versus internals, scalar traversal,
metadata traversal, ordering, structural carriers, normalization, operator
policy and independent trust axes, addressability/authorization and activation,
and rejection/diagnostic/atomicity behavior. Read with the explicit current
facts and deferred-work boundaries, this is sufficiently complete to begin
design. No additional omission was found that is load-bearing for a truthful
future coverage contract.

==== F4 — numeric grouping reconstruction

The original defect was presentation of role-level totals as if they were
stable architecture facts. The corrected grouping rule separates a row when
the initiating supported interface, external carrier/parser behavior,
description/capability route, automatic macro-flow guarantee, or structural
Domain-boundary carrier changes. The matrix—not a number—is authoritative.

The current illustrative reconciliation is:

[cols="5,1",options="header"]
|===
|Role-granularity category |Count
|Qualified framework-managed high-level families |12
|Bypass/reachability rows: 5 positive and 2 mixed |7
|Post-load metadata-introduction participants |2
|Post-ingress executable-step payload participant |1
|Total matrix rows/participants |22
|===

The census explicitly states that these roles overlap, are not disjoint call
graphs, are not architecture invariants, and may be harmlessly regrouped.
Nothing requires Phase E to preserve exactly 22 paths.

==== Framework-managed definition assessment

The bounded current-census definition is materially consistent with runtime.
`sheets-run` binds configured steps and delegates to `orchestrate`; `run_app`
does the same through its compatibility adapter; direct `orchestrate` owns the
router loader, initial ingress, final cleanup, dict-only persistence
projection, and router saver; and the schema-maintenance CLI/application use
case supplies a private fixed step to that same macro flow. The two exact-
reconstruction rows are carrier-specific subpaths within the same owned flow.

The definition accurately limits framework management to the fixed macro flow.
It does not claim that arbitrary executable code or the values that code
returns are framework validated. Delegation through `orchestrate` is
compatible with application macro-flow ownership; no material mismatch was
found.

==== Mandatory-ingress truth test

*Claim A*—framework-managed orchestration invokes a function named
`domain.ingress`—is TRUE for the router-loaded orchestrate flows. Static source
still shows exactly three production `run_domain_ingress` call sites:
orchestrator initial payload, bootstrap metadata merge, and YAML override
metadata merge.

*Claim B*—that invocation thereby transforms framework-managed external
payload into a trusted Domain payload contract before ordinary
transformations—is FALSE today. `INGRESS_RULES` still contains only
`legend_blocks_shape`. It establishes no scalar grammar, general Frames
contract, general metadata contract, `ExactTable` contract, or general
normalization. The shared `scalar_category`, `is_missing_carrier`,
`is_supported_scalar`, and `UnsupportedScalarError` still have zero production
callers outside `core/scalar_values.py`. Executable steps can also introduce or
replace payload after the initial seam.

The corrected artifact now distinguishes initial/framework-loaded coverage
from step-introduced/replaced coverage and asks how the future contract treats
both. It does not pre-decide that they receive the same mechanism.

==== Cross-cutting taxonomy and terminology

`bootstrap_meta`, `apply_overrides`, and post-ingress executable-step payload
are correctly represented as participants that can occur within orchestrated
or programmatic pipelines, not independent disjoint application call graphs.
Only the two maintained metadata merge participants classify their profile,
persisted, CLI/runtime, inline, or YAML inputs as post-load metadata
introduction. Metadata created by ordinary Domain or arbitrary executable code
is not called external merely because it is stored below `_meta`; provenance
remains callable-dependent.

Global wording is consistent enough for design:

* Python reachability alone establishes neither public support nor intent;
* package-public, documented consumer, supported lower-level, internal, and
  test-facing evidence remain distinct;
* trusted denotes the runtime/operator/caller precondition, not a validation
  result;
* bypass denotes a path around the high-level macro flow and is not
  automatically a defect;
* framework-managed denotes the fixed macro-flow guarantee, not arbitrary
  executable output; and
* mixed aggregates remain visibly mixed.

No material terminology drift remains.

==== Gating and core-evidence regression

The correction remains pre-design and pre-normative. It does not decide the
exact mandatory coverage set; future inclusion or exclusion of `run_pipeline`
or direct Domain surfaces; plugin-output trust; post-step ingress;
normalization API; canonical scalar carriers; a metadata/cell common walker;
policy representation; Pydantic; dotted-target authorization mechanism;
`List[Scalar]` representation or implementation; resource-limit architecture;
`D-T3`; or a family pilot.

Lightweight reinspection also preserved the central evidence:

* orchestrator order remains load, initial ingress, linear steps, final Domain
  cleanup, persistence projection, and save;
* CLI and `run_app` binding/import occur before the orchestrator load, while
  direct orchestrator steps are already bound;
* `run_pipeline` remains an intentional trusted-programmatic composition
  surface and performs no automatic ingress;
* there are three production ingress call sites, and the sole rule remains
  Legend Blocks shape normalization;
* shared scalar helpers remain unwired in production;
* configured-string resolution remains centralized through
  `resolve_configuration_callable`, qualified as addressability rather than
  authorization, while direct callables/BoundSteps bypass string resolution;
* XLSX/ODS, CSV, JSON, YAML, XML, and direct-Python carrier differences remain
  as stated;
* metadata and cell flows remain separate;
* grouped reconstruction still enforces local exact-carrier, geometry,
  mapping, and canonical-construction invariants rather than general ingress;
  and
* Claim A remains true while Claim B remains false.

==== Negative implication test

None of the prohibited conclusions can reasonably be derived from the
corrected census:

* all seven bypass rows are uniformly supported and importability means public
  support are contradicted by the evidence classes and two MIXED rows;
* all payload inside `orchestrate` has passed Trusted Ingress, arbitrary step
  output automatically receives ingress, and framework-managed means
  configured code is framework validated are contradicted by the new
  cross-cutting row and initial-payload qualification;
* plugin output is necessarily external/untrusted and Phase E must ingress
  after every step are explicitly left undecided;
* permanent exemption of `run_pipeline` and prohibition of direct Domain APIs
  are explicitly left undecided;
* exactly 22 paths is explicitly non-normative;
* `List[Scalar]`, Pydantic, scalar normalization, a family pilot, and `D-T3`
  remain unselected and unauthorized.

==== New findings

No `PHASE-E-CENSUS-FOLLOWUP-*` finding is created. All four original findings
are fully resolved, and correction `4151eaea` introduced no new material
evidence or design-readiness problem.

==== Phase-E design readiness

*READY FOR PHASE-E DESIGN.*

The corrected census now supplies a truthful current entry/reachability map;
evidence-based public/support distinctions; explicit initial-versus-step-
introduced payload treatment; trustworthy current ingress coverage; separate
payload and description/control paths; metadata, backend, and reconstruction
coverage; explicit DYNAMIC/OPEN uncertainty for plugins and external callers;
a sufficiently complete 15-question design inventory; and explicit
non-decisions that prevent hidden solution selection.

This readiness authorizes only a separate Phase-E design task. It authorizes no
implementation, runtime/test change, guard, ADR, Technical Model or Project
Memory edit, `D-T3`, or family pilot.

==== Follow-up validation

Validation performed for this follow-up:

[source,bash]
----
git status --short
git rev-parse HEAD
git log --oneline --decorate -15
git show --stat 4151eaea
git show 4151eaea
git diff 7c2152ec..4151eaea --stat
git diff 7c2152ec..4151eaea

rg source, exports, interface guidance, ingress/scalar call sites,
   pipeline binding/execution, backend carriers, and reconstruction invariants

.venv/bin/python -    # disposable independent post-ingress payload probe

asciidoctor --failure-level=ERROR corrected census
asciidoctor --failure-level=ERROR complete review artifact
asciidoctor --failure-level=ERROR containing architecture document
git diff --check
git status --short
----

The three AsciiDoc renders completed without errors. The disposable runtime
probe was rerun independently and left no tracked artifact. `git diff --check`
passed. Before the review commit, the only worktree change was this appended
review section.

==== Final verdict

*ACCEPTED — ALL REVIEW 001 FINDINGS ARE RESOLVED; THE PHASE-E REACHABILITY
CENSUS IS ACCEPTED AS THE EVIDENCE BASIS FOR PHASE-E DESIGN.*

Acceptance authorizes only the next design step. It does not authorize
implementation.
