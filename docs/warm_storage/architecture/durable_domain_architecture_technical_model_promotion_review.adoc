== Independent Technical Model Architecture Review -- Durable Domain Architecture Promotion

Review date: 2026-08-10

Review type: fresh-context, independent technical-model architecture review

Promotion baseline: `60a26cb9ecb1abef4117ba9ec70b4c8fb2a531f6`
(`docs(project_memory): record independently accepted domain consolidation inventory`)

Reviewed commits:

* `10fa78e9fca6666229615cf1c5458778f669cab2` --
  `docs(technical_model): promote domain value and trust contracts`
* `a27e3770fbf951b030ebca62ff45276db8f7a87d` --
  `docs(technical_model): add glossary terms for promoted value/trust vocabulary`

Reviewed HEAD: `a27e3770fbf951b030ebca62ff45276db8f7a87d` on `main`

Initial repository state: clean.

=== Verdict

[source,text]
----
CHANGES REQUIRED --
TECHNICAL MODEL PROMOTION MUST BE CORRECTED BEFORE ACCEPTANCE.
----

The promotion is directionally sound and correctly leaves the major Phase-E,
Pydantic, and family-pilot decisions open. The accepted scalar categories,
carrier behavior, classification-without-narrowing contract, trust/authority
model, output-sink separation, configuration-step responsibility, and most of
the transformation vocabulary are supported by primary authority.

It is not yet safe to treat the whole promotion as settled durable
architecture. Four Important findings remain:

* Duration's deferred semantic-category status is blurred with the current
  classifier's carrier rejection;
* Domain-invariant ownership is stated too broadly as `domain/validations/`
  and the not-yet-implemented payload-ingress responsibility is called a
  different current owner;
* the glossary reserves `projection` more narrowly than maintained source and
  existing Technical Model authority actually use it;
* the provisional Trusted Ingress concept still contains current-state claims
  that directly predate and contradict the promoted scalar/trust contracts.

One Minor classification-definition precision issue also remains. These are
documentation corrections only. No runtime, test, ADR, guard, Project Memory,
Phase-E, D-T3, or family-pilot work is required to resolve this review.

=== 1. Exact scope and baseline

The commit graph is linear at the reviewed range:

[source,text]
----
60a26cb -> 10fa78e -> a27e377
----

`git rev-parse 10fa78e^` returned the exact baseline
`60a26cb9ecb1abef4117ba9ec70b4c8fb2a531f6`.
`git rev-parse a27e377^` returned `10fa78e9fca6666229615cf1c5458778f669cab2`.
`git merge-base 60a26cb a27e377` returned the exact baseline.

The full `60a26cb..a27e377` range changes exactly five Technical Model files:

* `docs/technical_model/ch04_concepts/trusted_ingress_and_runtime_security.adoc`
* `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`
* `docs/technical_model/ch08_architecture_details/_chapter.adoc`
* `docs/technical_model/ch08_architecture_details/internal_value_model.adoc`
* `docs/technical_model/ch09_glossary/01_terms.adoc`

Range size: 656 insertions and 53 deletions. No source, test, ADR, Project
Memory, roadmap, guard, registry, release-note, Phase-E, or D-T3 file is in the
range. `10fa78e` and `a27e377` are documentation-only. No hidden Pydantic
dependency, package move, family skeleton, or runtime wiring exists in the
range.

All five promoted files were preserved verbatim during this review. Their
pre-artifact SHA-256 values were:

[source,text]
----
be8bb5c7f7e8df6c4459d391b7b8b2f7c7b5e2bea4fb306f70b404669fe0253a  ch04 trusted ingress concept
25826fee7801310bf0ea717a435a6f2256f068b1a87754cf388799f45849b17b  ch06 architectural layers
b5a65d7f3a83ac47af6c5124f6f92e2f29bccb7af2b49753ff7ccf0ccd2488ae  ch08 chapter include
774b2b3e600d0f95f4ae65a744bdea2a0a20d1b4d92caabee08bf536ff41ed7d  ch08 internal value model
340a657b486f4860c1a48c6736419e4f8512aba1d6adcf4eeae1882fb1548389  ch09 glossary terms
----

=== 2. Authority reconstructed independently

The consolidation inventory was treated as a pre-normative routing artifact,
not as normative authority. Its accepted review and disposition were used to
identify claims to challenge, not to prove those claims.

Primary and corroborating authority independently reconstructed:

* ADR-DOMAIN-BOUNDARY-ROBUSTNESS -- proportionate robustness, no hostile
  in-process-code sandbox, narrow representations preferred, and future
  hardening requiring fresh evidence;
* ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY, including accepted Reviews 001
  and 002 -- fixed trusted-runtime/operator precondition, independent payload
  and pipeline-description authority, operator-owned policy, per-kind
  narrowing combination, capability-activation timing, and separately owned
  output-sink capability;
* ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE -- visible spreadsheet payload is
  deliberately collapsed to strings, with typing remaining opt-in rather than
  inferred from carrier metadata;
* ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB -- Domain pipeline owns
  semantic cleanup/validation while writers serialize what they receive;
* FTR-MINIMAL-INTERNAL-VALUE-MODEL-P4A and its accepted implementation and
  follow-up reviews -- String/Boolean/Number/Missing, bounded Missing
  recognition, classification without narrowing, no general normalization,
  Error deferred, Formula separate from payload scalar semantics;
* FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A and the accepted D-T1/D-T2 design,
  correction, implementation, and follow-up reviews -- Date/DateTime,
  temporal ordering, uniform `numpy.datetime64` handling, Duration/Time
  deferral, and persistence-local temporal codecs;
* current `src/spreadsheet_handling/core/scalar_values.py` and its focused
  tests;
* current YAML and JSON backend code and focused temporal persistence tests;
* current pipeline/configuration, validation, transformation, workbook-view,
  persistence-projection, extraction, and projection source where relevant;
* `roadmap_domain_hardening_sequence.adoc`, especially Phase D, Phase E, the
  Domain Maintainer Contract stage, family-cycle gates, and D-T3 sequencing;
* the corrected Domain Architecture Consolidation Inventory and Independent
  Architecture Review 001 plus disposition.

=== 3. Independently reconstructed scalar contract

The following current implemented semantic categories are accepted and mature:

[cols="2,3,4",options="header"]
|===
| Category | Accepted carrier behavior | Independent conclusion

| String
| `str`, including `numpy.str_` through subclass behavior; `""` is routed to
  Missing first
| Accurate

| Boolean
| `bool` and explicit `numpy.bool_`; checked before Number because `bool` is
  an `int` subclass
| Accurate

| Number
| `int`, `float`, `numpy.integer`, `numpy.floating`; Integer/Float remains a
  bounded deferral
| Accurate

| Missing
| Recognition over `None`, `""`, Python/NumPy NaN carriers, `pandas.NA`,
  `pandas.NaT`, and `numpy.datetime64('NaT')`; not a sentinel or wrapper
| Accurate

| Date
| `datetime.date`, after DateTime
| Accurate

| DateTime
| `datetime.datetime`, `pandas.Timestamp`, and every non-NaT
  `numpy.datetime64` regardless of unit
| Accurate
|===

`scalar_category` first recognizes Missing, then Boolean, String, the explicit
`numpy.timedelta64` exclusion, Number, DateTime, and Date. That order correctly
prevents Boolean-as-Number, Duration-as-Number, DateTime-as-Date, and NaT-as-
DateTime mistakes.

`pandas.Timestamp` remains a `Timestamp`; `numpy.int64` remains a
`numpy.int64`; classification returns a label and does not return, mutate,
rewrap, or narrow the input. Day-unit `numpy.datetime64[D]` is DateTime, not
Date. Timezone-aware `datetime`/`Timestamp` values are classified as DateTime
without UTC conversion, `tzinfo` stripping, or carrier canonicalization.

`is_missing_carrier` bounds `pandas.isna` to known numeric/NumPy scalar
shapes, recognizes `pandas.NA`/`pandas.NaT` by identity, handles
`numpy.datetime64` through `numpy.isnat`, explicitly excludes every
`numpy.timedelta64`, and does not dispatch arbitrary unknown objects through
their NumPy array protocol. The arbitrary-object safety claim is therefore
supported by source and focused tests.

`""` is both a recognized Missing carrier in scalar classification and an
ordinary legitimate visible string whose higher-level meaning may be resolved
from authoritative context. The promoted Missing section states that
contextual distinction explicitly and does not introduce a sentinel.

No `normalize_scalar()`-, `normalize_value()`-, or
`canonicalize_scalar()`-shaped general primitive exists in maintained source.
The private `_yaml_safe_temporal`, `_json_temporal_default`, and
`_json_temporal_object_hook` functions remain backend-local codec operations.
They are not imported by Domain/Core, not wired into Trusted Ingress, and do
not define a general normalized carrier set.

=== 4. Findings

==== TM-PROMOTION-REVIEW-F1 -- Important -- Duration deferral is blurred with current carrier rejection

*Exact file/section:* `docs/technical_model/ch08_architecture_details/internal_value_model.adoc`,
"Semantic categories versus carrier variants," lines 99-114.

*Claim:* Time and Duration are described as "deferred/rejected,
respectively," even though the next paragraph says none of the deferrals is
a permanent prohibition.

*Primary authority:* FTR-DATE-TIME-INTERNAL-VALUE-MODEL-P4A sections 9-10,
20, and 23; accepted D-T1 review/evidence; current
`core/scalar_values.py`.

*Independent evidence:* The FTR separately says to defer the Time and
Duration semantic categories. It also records the current implementation fact
that `datetime.time`, `datetime.timedelta`, `pandas.Timedelta`, and every
`numpy.timedelta64` carrier raise `UnsupportedScalarError`. It explicitly
leaves a future Duration slice free to decide Duration's own Missing semantics.

*Impact:* The slash wording upgrades a current classifier rejection into what
can be read as the semantic-category disposition. The following non-
prohibition sentence reduces but does not remove the conflict inside a
normative document. This is exactly the distinction future value-model work
must be able to recover without reading FTR history.

*Smallest correction:* State that both Time and Duration semantic categories
are deferred, while their named carriers are currently unsupported/rejected by
`scalar_category`; retain the statement that this is not a permanent
architectural prohibition.

==== TM-PROMOTION-REVIEW-F2 -- Important -- Domain invariant ownership is over-centralized and payload ingress has no current owner

*Exact files/sections:*

* `docs/technical_model/ch08_architecture_details/internal_value_model.adoc`,
  "Structural, ingress, and Domain-invariant validation," lines 184-224;
* `docs/technical_model/ch06_architectural_layers/architectural_layers.adoc`,
  "Trust, Authority, and Validation Responsibility," lines 339-350;
* `docs/technical_model/ch09_glossary/01_terms.adoc`, "Domain invariant
  validation," lines 380-390.

*Claim:* Domain semantic/invariant validity is "cleanly owned" by
`domain/validations/`; `apply_severity_policy` is presented as its sole
control-flow decision point; the ch06 summary calls the three rows "three
different questions with three different current owners."

*Primary authority:* ADR-DOMAIN-OWNS-VALIDATION-WRITERS-STAY-DUMB;
ADR-DOMAIN-BOUNDARY-ROBUSTNESS; the consolidation inventory's own seven-part
validation taxonomy; maintained Domain source.

*Independent evidence:* `domain/validations/` does own reusable Finding-based
checks. It does not own every Domain invariant. Transformation-local and
reconstruction invariants are enforced inline throughout maintained source,
including `enrich_lookup/operation.py::_check_*`,
`xref_crosstable/primitives.py::_ensure_*`,
`grouped_xref/reconstruction.py::_validated_geometry`,
`xref_axis_mapping/resolver.py::_ensure_bijection`, and many family-specific
typed-error/raising paths. The corrected inventory explicitly distinguishes
Category C (reusable Domain invariant validation), Category D (transformation
preconditions), and Category E (reconstruction/roundtrip invariants). Payload
ingress enforcement, meanwhile, has no current runtime owner: only unwired
classification machinery exists and Phase E owns the future mechanism.

*Impact:* The promoted wording erases legitimate distributed invariant
ownership and overstates current implementation by turning a future owner into
a third current owner. A maintainer could wrongly move local preconditions to
`domain/validations/` or assume payload ingress is already owned/enforced.

*Smallest correction:* Limit `domain/validations/` ownership to reusable,
Finding-based cross-data/cross-reference checks; explicitly preserve
transformation-local and reconstruction-local invariant owners. Replace
"three different current owners" with wording that distinguishes two current
ownership shapes from the Phase-E-gated ingress responsibility.

==== TM-PROMOTION-REVIEW-F3 -- Important -- `projection` is reserved more narrowly than maintained authority supports

*Exact file/section:* `docs/technical_model/ch09_glossary/01_terms.adoc`,
"projection," lines 405-412.

*Claim:* "Projection is reserved for header-shape, axis-substitution, and
IR-to-Frames senses."

*Primary authority:* maintained source and existing Technical Model usage;
`pipeline_semantics.adoc`; corrected consolidation inventory section 6 and
its review.

*Independent evidence:* The inventory itself begins from the fact that
`projection` is overloaded and offers its narrowing as proposed vocabulary
for the Technical Model to adjudicate. Current valid uses extend beyond the
three reserved senses: `pipeline/persistence_boundary.py` owns persistable
metadata projection; `domain/workbook_views.py` uses workbook-view/source
projection; `pipeline_semantics.adoc` uses workbook projection and final
cleanup/projection contracts; `generic_ir.adoc` uses RenderPlan/meta/reverse
projection; `domain/extractions/frame_extract.py` and
`enrich_lookup/operation.py` still use column/helper projection in code and
docstrings. Persistence projection is itself accepted architecture, not an
accidental local variable name.

*Impact:* The glossary becomes competing normative authority that contradicts
existing durable documentation and maintained source. It also makes the
inventory's recommended future naming direction read as an already-observed
repository invariant.

*Smallest correction:* Recast the entry as a preferred vocabulary for new
Domain transformation prose: use `extraction`/`selection` for plain column
subsets to reduce ambiguity, while acknowledging existing qualified senses
such as persistence, workbook-view, header, axis, and IR projection. Do not
claim an exclusive reservation unless a separately reviewed migration removes
or reclassifies the other valid uses.

==== TM-PROMOTION-REVIEW-F4 -- Important -- the provisional ch04 concept remains internally stale after reconciliation

*Exact file/sections:* `docs/technical_model/ch04_concepts/trusted_ingress_and_runtime_security.adoc`,
sections 2, 3.1, 7, 13, and 15 (notably lines 177-192, 657-680, 1171-1181,
and 1331-1357).

*Claim:* The new introduction and section 15 correctly point to the adopted
trust ADR and normative ch08 scalar model, but retained "current state" prose
still says there is "no stated trust model," "no closed internal value
vocabulary," no closed internal scalar contract, and that CSV directory input
uses pandas default inference. Section 7 still introduces its provisional
taxonomy as though no shapes have yet been named. Section 15 says only the
formerly open NaN candidate has existing FTR implementation authority.

*Primary authority:* ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY; accepted
Phase-D FTRs/reviews; current `csv_backend.py` and `scalar_values.py`; the new
ch06/ch08 durable owners.

*Independent evidence:* The trust ADR now states the trust model; ch08 and
`ScalarCategory` now state a closed classification vocabulary; Slice D2 made
`csv_dir` use `dtype=str`, `keep_default_na=False`, and `na_values=[]`; the
NaN BUG and Phase-D D1-D3/D-T1/D-T2 authority history has advanced beyond the
concept's candidate map. What remains absent is a mandatory ingress boundary
and a canonical normalized carrier representation, not a classification
vocabulary or stated trust model.

*Impact:* A reader following the new cross-references encounters mutually
exclusive current-state claims inside one Technical Model. The concept is
loudly provisional, so it does not override ch06/ch08 normatively, but its
reconciliation is incomplete and materially impairs historical
understandability and authority ownership.

*Smallest correction:* Keep the concept provisional and historical, but
time-box or update the stale current-state sentences: distinguish the now-
accepted classification/trust vocabulary from the still-absent Phase-E
enforcement/normalized representation; correct the CSV fact; label the
candidate/authority map as historical or update its status. Do not promote
the five-boundary table or any mechanism while doing so.

==== TM-PROMOTION-REVIEW-F5 -- Minor -- classification is not literally content-free

*Exact files/sections:*

* `docs/technical_model/ch08_architecture_details/internal_value_model.adoc`,
  "Classification versus normalization," lines 126-131;
* `docs/technical_model/ch09_glossary/01_terms.adoc`, "classification," lines
  285-293.

*Claim:* Classification is described as occurring "without inspecting
content" / "without inspecting string content."

*Primary authority:* current `core/scalar_values.py::is_missing_carrier` and
`scalar_category`; accepted Missing semantics.

*Independent evidence:* Classification does not parse or heuristically coerce
string content, but it must test whether a string equals `""` to distinguish
Missing from String. It also performs bounded NaN/NA/NaT recognition. The
later Missing section correctly documents this, limiting the ambiguity.

*Impact:* Bounded internal wording inconsistency; no architecture is changed,
and the detailed Missing section prevents a broad wrong inference.

*Smallest correction:* Say classification does not parse, coerce, or
heuristically interpret non-missing content, while explicitly allowing its
bounded Missing-carrier recognition.

=== 5. Trust and authority assessment

The adopted architecture has one fixed precondition, two variable independent
input-authority dimensions, and one separately owned capability concern:

[cols="2,3,3",options="header"]
|===
| Element | Authority | Review result

| Runtime/operator trust
| ADR Decision 1: fixed normative precondition
| Accurate. The ch06 headline's phrase "three independent dimensions" is
  imperfect in isolation, but the immediately following row explicitly says
  fixed and non-variable. No finding is warranted.

| Payload trust
| ADR Decision 2, composed with Domain Boundary Robustness levels
| Accurate and orthogonal to pipeline-description trust.

| Pipeline-description trust
| ADR Decision 2
| Accurate; all four payload/pipeline combinations remain representable and
  no profile name is adopted.

| Operator policy and narrowing combination
| ADR Decisions 3-4
| Accurate. Operator policy is outside untrusted pipeline input; pipeline
  requests may only narrow; combination stays per-kind. Concrete policy API,
  schema, and enforcement remain undecided.

| Output-sink capability
| ADR Decision 5
| Accurate as a separately owned concern, not another authoring-trust source.
  Its glossary category `trust` does not create a material ambiguity because
  the definition and ch06 text both state the separation explicitly.
|===

The four-way description/control input, payload input, Domain invariant, and
output adaptation synthesis is explicitly labeled explanatory and not a new
runtime/package layer. It does not replace the existing layer model or create
four execution stages. Finding F2 applies only to its pointer for Domain
invariant ownership, not to the explanatory four-way synthesis itself.

=== 6. Configuration-step assessment

The distinction between configuration/policy production and execution-time
policy resolution is mature enough to name normatively. It already exists in
`pipeline_semantics.adoc` and is evidenced by multiple maintained paths:

* `infer_fk_relations` performs bounded heuristics and writes resolved v2 FK
  relation policy;
* `configure_fk_helpers` validates explicit configuration and writes the same
  resolved relation model;
* `configure_lookup_helpers` resolves `auto`, validates keys/helpers/order,
  and writes lookup-helper policy;
* `configure_workbook_view` validates and normalizes workbook-view intent;
* later FK-helper and lookup operations read/resolve existing policy rather
  than reproducing those heuristics.

The promoted section correctly refuses to derive a file-layout convention.
It requires neither `policy.py` nor `configuration.py` and adopts no universal
package skeleton.

=== 7. Projection, extraction, reconstruction, and bijection assessment

The durable, supported parts are:

* projection does not imply reversibility;
* `expand`/`contract` names paired directions but does not itself prove
  bijection;
* `bijection`/`bijective` is used only where the owning contract makes the
  losslessness claim;
* `pipeline_semantics.adoc` remains authoritative for inverse kinds:
  `bijective`, `conditionally_bijective`, `relation_preserving`,
  `projection_only`, `validation_only`, `extraction`, `unknown`;
* extraction intentionally describes an information-discarding result and
  does not claim an inverse;
* reconstruction currently names the impure externally-parsed
  `ExactTable`-consuming reverse path, while `expand` names ordinary
  in-pipeline reverse direction.

Finding F3 is limited to the exclusive `projection` reservation. It does not
reject the useful preference for `extraction`/`selection`, nor does it reopen
the inverse-kind vocabulary.

=== 8. Glossary audit

The request describes 22 added entries. The commit actually adds 18 table
rows; several rows combine lexical aliases (`extraction / selection`,
`expand / contract`, `bijection / bijective`). Every added row was audited.

[cols="3,3,5",options="header"]
|===
| Entry | Assessment | Reason

| semantic category
| Accurate
| Matches `ScalarCategory` and points to ch08 for detail.

| carrier variant
| Accurate
| Correctly separates classification from narrowing/canonicalization.

| classification
| Incomplete/misleading
| Correct on no conversion/mutation; "without inspecting content" needs the
  bounded Missing-recognition qualification (F5).

| normalization
| Accurate
| Defines the contrast while saying no API/carrier policy exists and leaving
  general type resolution to Phase E. It does not select native-Python or
  other canonical carriers.

| Missing (scalar)
| Accurate
| Recognition, not representation; correctly avoids a sentinel and identifies
  visible `""`.

| payload trust
| Accurate
| ADR-backed and orthogonal to pipeline trust.

| pipeline-description trust
| Accurate
| ADR-backed; all four combinations preserved.

| runtime/operator trust
| Accurate
| Correctly identified as a fixed precondition.

| output-sink capability
| Accurate
| Category placement under `trust` is acceptable because the explanation
  explicitly makes it a separate responsibility, not a trust source.

| structural validation
| Accurate
| Useful A-vs-C distinction; no central owner is claimed.

| payload ingress validation
| Accurate
| Correctly says Phase E and no mandatory mechanism today.

| Domain invariant validation
| Incomplete/misleading
| Overstates `domain/validations/` ownership and omits local invariant owners
  (F2).

| configuration step
| Accurate
| Existing normative step category and multiple-family evidence; no file
  layout implied.

| projection
| Accurate but over-normative
| Preferred narrowing is useful, but exclusive reservation contradicts
  maintained valid uses (F3).

| extraction / selection
| Accurate
| Aligns with the inverse-kind taxonomy and named extraction feature.

| expand / contract
| Accurate
| Pairing does not automatically imply bijection.

| reconstruction
| Accurate
| Describes the sole maintained identifier-level use and distinguishes it from
  ordinary in-pipeline `expand`; it does not say every reverse transform is a
  reconstruction.

| bijection / bijective
| Accurate
| Explicit-owning-contract requirement and inverse-kind cross-reference are
  correct.
|===

No added term duplicates an existing glossary row. `carrier variant` does not
conflict with the existing meta-system term `carrier`: the category and
definition distinguish runtime scalar carriers from persistence files.

=== 9. Phase-E, Pydantic, and family-pilot gating

No promoted normative passage adopts any of the following:

* the full five-boundary ownership/timing table;
* a normalization API or canonical normalized carrier set;
* an ingress traversal/coordinator mechanism;
* policy representation, profile names, or CLI/API transport;
* `run_pipeline` bypass resolution or mandatory ingress wiring;
* parser/resource limits;
* `List[Scalar]` implementation;
* Pydantic;
* family hardening, D-T3, or a selected family;
* a universal family file/package skeleton;
* a domain-module composite taxonomy;
* new guards.

The ch04 five-boundary table remains explicitly provisional. The ch08 scalar
document says classification exists but ingress enforcement does not. General
normalization/type resolution is correctly assigned to Phase E without
choosing an API or result carriers. `List[Scalar]` remains explicitly open.
`run_pipeline` remains a documented trusted programmatic path and its future
boundary treatment remains open.

Pydantic is neither adopted nor rejected. No package-location rule is adopted;
no claim permits Pydantic to define internal `PipelineSpec`, policy, or payload
scalar semantics. The durable principle is only that any future structural
validation mechanism must not collapse structural, payload-ingress, and Domain
semantic questions or silently redefine project-owned scalar semantics. An
actual Pydantic use site/location/translation decision remains ADR-gated.

No mandatory `model.py`, `policy.py`, `operation.py`, `validation.py`, package
skeleton, `__init__.py` rule, or repo-wide convergent family pattern is
promoted. Those remain family-pilot/Domain-Maintainer-Contract gated.

=== 10. Negative implication test

[cols="4,2,5",options="header"]
|===
| Wrong conclusion attempted | Derivable? | Assessment

| All accepted values become native Python
| No
| Carrier variants explicitly remain unchanged; no normalization exists.

| Missing is a new sentinel/type
| No
| Explicitly denied.

| All empty strings are always semantically Missing
| No
| `""` is both a Missing carrier and legitimate payload whose contextual
  meaning is resolved elsewhere.

| `numpy.datetime64[D]` is Date
| No
| Every non-NaT unit is explicitly DateTime.

| Timezone-aware values normalize to UTC
| No
| Category membership only; no canonicalization policy.

| Duration is permanently forbidden
| Plausibly, locally
| F1: "deferred/rejected, respectively" creates the local inference, though
  the following non-prohibition sentence counters it.

| Pydantic is forbidden in `domain/`
| No
| Explicitly denied.

| Trusted Ingress exists today
| No
| Explicitly Phase E and not started.

| `run_pipeline` is protected today
| No
| Explicitly denied.

| Projection always has an inverse
| No
| Extraction/projection and inverse-kind text deny this.

| Every expand/contract pair is bijective
| No
| Explicitly denied.

| Reconstruction means every reverse transform
| No
| It is explicitly distinguished from ordinary `expand`.

| Every family needs `policy.py`
| No
| Explicitly denied.

| Configuration steps must be separate files
| No
| Explicitly a family-local layout choice.

| Output-sink capability is a third untrusted author
| No
| It is explicitly a separate capability responsibility, not a trust source.

| D-T3 is next
| No
| Explicitly unselected and unauthorized.
|===

=== 11. Durable-owner and duplication assessment

[cols="3,3,5",options="header"]
|===
| Concept | Durable owner | Duplication assessment

| Proportionate boundary robustness
| ADR-DOMAIN-BOUNDARY-ROBUSTNESS plus its operational Technical Model concept
| No new competing decision.

| Trust/operator/payload/pipeline policy decisions
| ADR-TRUSTED-RUNTIME-AND-POLICY-AUTHORITY
| ch06 is an appropriate layer-map restatement; glossary is a summary. ch04
  should remain historical/provisional but needs F4 reconciliation.

| Scalar categories and carrier classification
| ch08 Internal Scalar Value Model, grounded by accepted FTR/runtime evidence
| FTR remains evidence/history; glossary should summarize, not supersede ch08.

| Visible spreadsheet string substrate
| ADR-VISIBLE-CELL-TYPING-STRING-SUBSTRATE
| ch08 Missing text composes with, rather than reopens, the ADR.

| General normalization and Trusted Ingress
| Phase E roadmap/future accepted design
| No current Technical Model passage becomes a mechanism owner.

| Reusable Finding/Severity validation
| ch06 validation section and `domain/validations/`
| F2 must stop this from being mistaken for ownership of every invariant.

| Configuration-step taxonomy
| ch08 Pipeline Step Semantics
| ch06 and glossary are useful summaries backed by multiple real families.

| Inverse-kind semantics
| ch08 Pipeline Step Semantics
| Glossary correctly cross-references rather than restating the full taxonomy.
|===

Cross-reference density is generally helpful: ch08 owns scalar detail, ch06
owns the layer-map restatement, ADRs own decisions/rationale, and the glossary
summarizes. The principal cycle/ambiguity is F4: the provisional ch04 concept
points to the new owners but retains contradictory current-state prose.

=== 12. Explicit disposition matrix

[cols="3,3,5",options="header"]
|===
| Promoted concept | Disposition | Basis / boundary

| Scalar categories
| ACCEPT AS NORMATIVE
| Accepted implemented String/Boolean/Number/Missing/Date/DateTime contract.

| Carrier variants
| ACCEPT AS NORMATIVE
| Verified directly against `scalar_values.py`.

| Classification
| ACCEPT WITH WORDING CORRECTION
| Contract is mature; fix F5's content-free overstatement.

| Normalization ownership
| ACCEPT AS NORMATIVE
| General normalization/type resolution belongs to Phase E.

| Normalization API/canonical carriers
| PHASE-E-GATED
| No API or normalized representation is selected.

| Missing
| ACCEPT AS NORMATIVE
| Recognition, not representation; bounded carrier set.

| Date / DateTime
| ACCEPT AS NORMATIVE
| D-T1 accepted and implemented, including ordering/unit/timezone boundaries.

| Error
| ACCEPT AS NORMATIVE
| Its deferred-category status is mature; no current carrier exists.

| Time / Duration
| ACCEPT WITH WORDING CORRECTION
| Semantic categories deferred; current carriers rejected (F1).

| Formula
| ACCEPT AS NORMATIVE
| `FormulaSpec` is output Intent, not payload scalar; future formula ingress/
  preservation stays with its own FTR.

| Structural validation
| ACCEPT AS NORMATIVE
| Distinction is mature; current implementation remains diffuse.

| Payload ingress validation
| PHASE-E-GATED
| The question/vocabulary is accepted; enforcement owner/mechanism is absent.

| Domain invariant validation
| ACCEPT WITH WORDING CORRECTION
| Preserve reusable and transformation-local/reconstruction-local owners (F2).

| Runtime/operator trust
| ACCEPT AS NORMATIVE
| Fixed ADR precondition.

| Payload trust
| ACCEPT AS NORMATIVE
| Independent ADR dimension.

| Pipeline-description trust
| ACCEPT AS NORMATIVE
| Independent ADR dimension; all four combinations retained.

| Output-sink capability
| ACCEPT AS NORMATIVE
| Separately owned ADR concern, not a third trust author.

| Four-way responsibility synthesis
| DEMOTE TO EXPLANATORY
| Already correctly labeled explanatory; keep it in that role, not a layer.

| Configuration step
| ACCEPT AS NORMATIVE
| Existing Pipeline Step Semantics plus multiple real producer/consumer cases.

| Projection
| ACCEPT WITH WORDING CORRECTION
| Prefer narrowed usage without claiming exclusive reservation (F3).

| Extraction / selection
| ACCEPT AS NORMATIVE
| Named one-way information-discarding operation/inverse kind.

| Expand / contract
| ACCEPT AS NORMATIVE
| Paired directions; no automatic bijection claim.

| Reconstruction
| ACCEPT AS NORMATIVE
| Current precise externally-parsed reverse-step vocabulary.

| Bijection / bijective
| ACCEPT AS NORMATIVE
| Explicit owning-contract claim only.

| Five-boundary ownership/timing mechanism
| PHASE-E-GATED
| ch04 table remains provisional beyond ADR Decision 5 separation.

| Pydantic adoption/location/translation
| ADR-GATED
| No decision or package rule exists.

| Family conventions / universal skeleton
| FAMILY-PILOT-GATED
| Domain Maintainer Contract and broader family sampling remain prerequisites.
|===

=== 13. Documentation, links, and executable verification

Executed independently:

[source,text]
----
git diff --check 60a26cb..a27e377
asciidoctor internal_value_model.adoc
asciidoctor architectural_layers.adoc
asciidoctor trusted_ingress_and_runtime_security.adoc
asciidoctor 01_terms.adoc
asciidoctor docs/technical_model/technical_model.adoc
make docs-html DOC_BUILD_DIR=/tmp/tm-promotion-review-docs-html
.venv/bin/pytest -q tests/unit/core/test_scalar_values.py
.venv/bin/pytest -q \
  tests/integration/io_backends/json/test_json_roundtrip.py \
  tests/integration/io_backends/yaml/test_yaml_roundtrip.py
----

Results:

* all four changed/new content files rendered individually with exit 0 and no
  warning;
* the full Technical Model rendered with exit 0 and no warning;
* the rendered glossary contains one structurally valid three-column table and
  all added rows, including semantic category, output-sink capability, and
  bijection/bijective;
* every newly added target file exists; anchors
  `trust-and-authority-dimensions`, `configuration-step-responsibility`,
  `internal-value-model-trust-authority-pointer`, and
  `validation-findings-severity` exist at their referenced owners;
* the ch08 chapter includes `internal_value_model.adoc` exactly once;
* `git diff --check` passed;
* scalar tests: 74 passed;
* focused JSON/YAML temporal persistence tests: 28 passed.

The repository-wide `make docs-html` target completed and rendered the full
Technical Model. It also emitted pre-existing diagnostics in unrelated
backlog/cold-storage/developer-guide inputs (including heading-level errors,
an unrelated missing cold-storage include, and existing heading warnings).
None names a promoted file, and the directly built promoted files plus full
Technical Model are clean. This review does not widen scope to repair those
baseline documentation issues.

=== 14. Final assessment

The core promotion does not prematurely start Phase E, D-T3, Pydantic, family
hardening, an ADR, or a guard. The scalar runtime contract is reconstructed
accurately in nearly every load-bearing detail, and the trust/authority model
faithfully preserves the ADR's fixed precondition, two independent input
dimensions, narrowing-only operator policy, and separate sink responsibility.

Acceptance is withheld because durable current architecture must not contain
the F1-F4 material ambiguities and contradictions. The smallest next action is
a documentation-only correction pass limited to those findings (and the Minor
F5 precision fix), followed by an independent follow-up review. That pass must
not alter runtime/tests, begin Phase E or D-T3, introduce Pydantic, add an ADR
or guard, prescribe family structure, curate Project Memory, or push.

[source,text]
----
DURABLE DOMAIN ARCHITECTURE TECHNICAL MODEL PROMOTION
INDEPENDENTLY REVIEWED.
----
