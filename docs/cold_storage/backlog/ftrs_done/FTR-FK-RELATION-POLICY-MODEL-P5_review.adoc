= FTR-FK-RELATION-POLICY-MODEL-P5 -- Implementation Review

== Metadata

[cols="1,3", options="header"]
|===
| Field | Value

| Reviewed FTR
| `FTR-FK-RELATION-POLICY-MODEL-P5`

| Review verdict
| READY WITH NITS

| Reviewer
| Codex

| Scope reviewed
| Documentation / model-contract slice only. The review checked that the v2
  FK relation policy is documented as target-state under
  `_meta.helper_policies.fk`, that current runtime and registry behavior remain
  truthfully v1, and that the follow-up implementation FTRs own runtime and
  registry changes.
|===

== Verdict

READY WITH NITS.

The implementation is a documentation-only model-contract slice. It does not
change source, runtime, tests, demos, or registry artifacts. The modified
documents consistently distinguish current runtime behavior, the v2 target
model, and the later implementation FTRs.

== Blocking Findings

None.

== Non-Blocking Findings

. `docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc` around the relation
  identity wording slightly muddies identity by listing `target_frame` and
  `target_key` inside "relation identity". The model elsewhere correctly says
  the key is `(source_frame, source_column)`.
+
Suggested wording:
+
----
The policy distinguishes the relation key `(source_frame, source_column)` and
target resolution `(target_frame, target_key)` from materialized helper columns.
----

. `docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc` says
  `schema_version: 2` "or equivalent planning marker". Since the examples and
  acceptance pin `schema_version: 2`, remove the escape hatch if this is meant
  to be the contract shape.

. `docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc` has no trailing newline.
  Fix before committing the implementation docs.

== Validation

Validation commands reported by Opus:

[cols="2,1", options="header"]
|===
| Command | Result

| `asciidoctor -o /dev/null docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc`
| Pass

| `asciidoctor -o /dev/null docs/backlog/_ftrs_open.adoc`
| Pass

| `asciidoctor -o /dev/null docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`
| Pass

| `asciidoctor -o /dev/null docs/ai_info/concepts_fk_helpers.adoc`
| Pass

| `asciidoctor --safe-mode=unsafe -o /tmp/backlog.html docs/backlog/backlog.adoc`
| Pass

| `git diff --check`
| Pass
|===

Additional validation / inspection run by Codex:

[cols="2,2", options="header"]
|===
| Command or inspection | Result

| `git status --short`
| Exactly the three expected modified documentation files; no untracked files
  before the review artifact was created.

| `git diff --stat`
| Only the three expected documentation files were modified.

| Targeted diff inspection of
  `docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc`,
  `docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`, and
  `docs/ai_info/concepts_fk_helpers.adoc`
| Confirmed this is a model/documentation slice and that v2 is presented as
  target-state, not current runtime behavior.

| Registry grep / inspection of
  `docs/technical_model/ch05_registries/meta_registry/meta_registry.yaml` and
  `docs/technical_model/ch05_registries/pipeline_step_registry/pipeline_step_registry.json`
| Confirmed no `schema_version: 2` token and no registry claim that v2 is
  current runtime behavior. The registries remain current-state artifacts.

| `git diff --check -- docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc docs/technical_model/ch04_concepts/fk_helper_metadata.adoc docs/ai_info/concepts_fk_helpers.adoc`
| Pass.

| `asciidoctor -o /dev/null docs/backlog/FTR-FK-RELATION-POLICY-MODEL-P5.adoc`
| Pass.

| `asciidoctor -o /dev/null docs/backlog/_ftrs_open.adoc`
| Pass.

| `asciidoctor -o /dev/null docs/technical_model/ch04_concepts/fk_helper_metadata.adoc`
| Pass.

| `asciidoctor --safe-mode=unsafe -o /tmp/backlog.html docs/backlog/backlog.adoc`
| Pass.
|===

== Disposition

Follow-up fixes are required for the nits before closure.

== Residual Risks

* The implementation FTRs may still refine non-invariant field names when the
  runtime shape is implemented.
* `meta_registry.yaml`, `pipeline_step_registry.json`, and the public user
  guide remain v1-truthful until the later implementation FTRs update runtime
  behavior and the corresponding documented contracts.
