===== FTR-YAML-OVERRIDES — Merge per-sheet config from YAML files

Purpose: Allow analysts to define sheet-specific options (id_field, helper_prefix, fks) in YAML instead of CLI.
Acceptance: CLI args < YAML < Defaults precedence; end-to-end tests for merge behavior.
