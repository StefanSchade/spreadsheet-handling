===== FTR-SEMANTIC-PROBLEM-STATEMENT-P5 -- Elaborate the problem statement and ambiguity policy

*Status:* New
*Priority:* P5
*Area:* Semantic model / documentation

==== Goal

Elaborate the chapters that motivate the semantic model: why semantic
friction remains after structural discipline, how human / AI / legacy
project stages handle ambiguity differently, and which ambiguities the
project treats as defects versus tolerated.

==== Scope

* Refine the Problem Statement, Human / AI / Legacy ambiguity, and
  Ambiguity Policy chapters in
  `docs/semantic_model/semantic_model.adoc`.
* Anchor each claim to a concrete project example where possible
  (FK-helper review, missing-policy ambiguity, stale registry consumer
  lists). Reference existing review notes / FTRs rather than
  reconstructing history inline.
* Keep the "no customer identity as semantics" guidance and replace
  ad-hoc YAML examples with project-aligned naming where possible.

==== Out of scope

* No new contract matrices, lifecycle rules, or guard proposals.
* No reorganization of the seed's chapter layout (owned by
  `FTR-SEMANTIC-MODEL-SKELETON-P5`).
* No changes to runtime behavior or registry artifacts.

==== Seed sections elaborated

* Problem Statement
* Human / AI / Legacy semantic ambiguity
* Ambiguity Policy

==== Acceptance (rough)

* Each "ambiguity becomes explicit" / "ambiguity may remain tolerated"
  bullet is paired with at least one concrete project reference.
* The semantic-mode-vs-identity-branch guidance is recorded with
  YAML-style examples that match current project naming conventions.
* The document still renders cleanly via `make docs-html`.

==== Code and guards

* No production code. No tests. No guards. Documentation only.
