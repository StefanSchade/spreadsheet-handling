===== FTR-PREHEX-TEST-RETIRE — Remove pre-hex test suite

_Superseded by <<FTR-PREHEX-TEST-CONSOLIDATION-P3C#,FTR-PREHEX-TEST-CONSOLIDATION-P3C>> (Phase 3c), which adds retire/rewrite/promote classification instead of blanket deletion. Kept for traceability._

*Purpose:*::
Remove tests that target pre-hex logic (old `core`/pack/unpack flows) to reduce noise and maintenance.

*Scope:* `tests/legacy/**` (renamed `tests/pre_hex/**` if we choose to rename now).
Excludes: `test_plugins_dotted_path.py` (moved to unit suite).

*Plan:*::
- Keep quarantined (excluded by default) until domain consolidation ships with coverage gates.
- After consolidation, delete folder and references.

*Acceptance:*::
- CI passes with configured coverage floors.
- No references to the pre-hex suite remain.

*Refenreces:*::
- to be done after <<FTR-DOMAIN-CONSOLIDATION — Fold scattered domain code into a tidy layer,FTR-DOMAIN-CONSOLIDATION)>>
