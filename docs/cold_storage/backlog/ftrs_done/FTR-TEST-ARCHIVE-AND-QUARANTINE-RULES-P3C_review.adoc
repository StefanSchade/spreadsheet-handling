===== Review: FTR-TEST-ARCHIVE-AND-QUARANTINE-RULES-P3C

*Review-Datum:* 2026-04-30
*Reviewer:* (post-hoc review)
*Verdict:* Umgesetzt — leichtgewichtige Lifecycle-Regeln etabliert

'''

====== Zusammenfassung

Das FTR definiert klare Zustände (Active, Quarantined, Archived) und
Übergangsregeln für Tests, damit keine ambigen Grauzone-Tests im Repository
verbleiben. Die Überprüfung bestätigt, dass der im Delivery-Scope genannte
Umfang umgesetzt ist.

'''

====== Prüfpunkte

[cols="1,3,1", options="header"]
|===
| # | Kriterium | Status

| 1
| Lifecycle-States (Active, Quarantined, Archived) in `tests/README.adoc`
  dokumentiert
| ✓ vorhanden

| 2
| Übergangsregeln (Quarantined→Active, Quarantined→Archived, Active→Quarantined)
  explizit definiert
| ✓ vorhanden

| 3
| `tests/legacy_pre_hex/README.adoc` als expliziter Quarantine-Carrier
  dokumentiert mit Klassifikation und Status
| ✓ vorhanden

| 4
| `make test-prehex` Target für explizites Ausführen der Quarantine-Slice
| ✓ vorhanden (+ Aliase `test-legacy`, `test-legacy-try`)

| 5
| Default-Test-Run schließt Quarantine aus
  (`not legacy and not prehex and not slow`)
| ✓ im Makefile dokumentiert

| 6
| Carrier-Boundary-Guard mit FTR-Marker
  (`test_carrier_boundary_guards.py` referenziert
  `FTR-TEST-ARCHIVE-AND-QUARANTINE-RULES-P3C`)
| ✓ vorhanden

| 7
| Kein komplexes Lifecycle-Tooling eingeführt
| ✓ wie beabsichtigt
|===

'''

====== Offene Beobachtungen (informell, kein Blocker)

* Das FTR liegt weiterhin als `include` in `_ftrs_open.adoc` (Zeile 325),
  obwohl es auch in `ftrs/done/` und `_ftrs_done.adoc` geführt wird. Das ist
  ein kosmetisches Backlog-Artefakt — der Open-Include zeigt den verbleibenden
  Kontext der „Group C"-Sektion, nicht einen offenen Arbeitsstatus.
* `tests/legacy_pre_hex/` ist aktuell leer (keine `test_*.py` Dateien). Die
  Quarantine-Infrastruktur steht aber korrekt bereit und `make test-prehex`
  meldet das erwartete „deferred/no-tests"-Ergebnis.
* Der Guard in `test_carrier_boundary_guards.py` prüft, dass Support-Carrier
  keine Tests definieren — der explizite Quarantine-Status-Check für neue
  Dateien unter `legacy_pre_hex/` ist über die README-Klassifikationspflicht
  abgedeckt, nicht über automatische Tooling-Erzwingung.

'''

====== Fazit

Das FTR ist inhaltlich abgeschlossen. Lifecycle-Zustände sind dokumentiert,
Quarantine ist physisch isoliert, der Default-Test-Run ist sauber, und die
Boundary-Guards schützen die Topologie. Kein weiterer Handlungsbedarf.
