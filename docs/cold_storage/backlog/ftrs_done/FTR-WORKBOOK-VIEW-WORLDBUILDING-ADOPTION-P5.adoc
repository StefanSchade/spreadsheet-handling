= FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5 -- Adopt explicit sheet names and ordering in the worldbuilding consumer

*Status:* Done
*Priority:* P5
*Area:* Consumer ergonomics / workbook view / roundtrip
*Kind:* Adoption and validation slice

== Purpose

Apply the existing workbook-view capability to the worldbuilding consumer so
visible spreadsheet sheet names may be shorter and more user-oriented than the
canonical JSON frame names, while preserving explicit sheet ordering and safe
roundtrip reconstruction.

The worldbuilding repository is the first realistic proof point. The public
demo follows only after the behavior and configuration shape are understood.

== Context

Canonical frame names are optimized for stable data modeling and configuration.
Visible spreadsheet sheet names are a user-interface concern and may benefit
from shorter labels and an intentional order.

The core already provides:

* `configure_workbook_view`;
* explicit sheet-to-frame mappings;
* workbook-view sheet ordering;
* `apply_workbook_view_sheet_mappings` for reimport;
* XLSX and ODS metadata carriers.

This slice validates that these capabilities work together in a real consumer.

== Scope

* Select a representative set of worldbuilding frames.
* Configure explicit visible sheet names.
* Configure a deliberate sheet order that differs from lexical frame ordering.
* Export the consumer workbook as XLSX and ODS.
* Reimport both carriers through the maintained consumer workflow.
* Verify that visible sheet names resolve back to canonical frame names.
* Verify that canonical JSON frame names remain unchanged.
* Record any configuration, documentation, backend, or roundtrip gaps found.

== Non-Goals

* No new workbook-view model unless the existing contract proves insufficient.
* No heuristic inference of frame names from visible sheet names.
* No renaming of canonical JSON frame keys.
* No demo-repository update in the first slice.
* No addition of new presentation-meta families.
* No unrelated worldbuilding pipeline cleanup.

== Acceptance

* The exported workbook uses the configured visible sheet names.
* Sheet order follows explicit configuration rather than lexical frame order.
* Canonical JSON and internal frame names remain unchanged.
* XLSX reimport restores the declared logical frame names.
* ODS reimport restores the declared logical frame names or exposes a narrowly
  documented backend gap.
* Reimport does not depend on the current physical order of sheets.
* Existing worldbuilding roundtrip checks remain green.
* Any discovered defect is tracked as a separate `BUG-*` item rather than being
  silently absorbed into this adoption FTR.
* The resulting configuration and lessons are suitable as input for a later
  demo-oriented documentation slice.

== Initial Assessment

The unchanged maintained worldbuilding integration was verified first against
the current editable core checkout. Both `make export-stories check-reimport`
and `make export-stories-ods check-reimport-ods` passed before adoption edits,
so no compatibility repair or intermediate commit was required.

The maintained workbook surface contained these logical frames:
`stories`, `characters`, `events`, `groups`, `regions`, `places`,
`story_group_matrix_view`, `story_cast_role_matrix_view`,
`story_cast_notes_matrix_view`,
`story_cast_character_development_matrix_view`,
`story_cast_crystal_matrix_view`, and `story_locations`.

`configure_workbook_view` was already called near the end of
`pipelines/dino_insel/json_to_spreadsheet.yaml`, after frame preparation and
before validation metadata was added. `apply_workbook_view_sheet_mappings` was
already the first step of `pipelines/dino_insel/spreadsheet_to_json.yaml`,
before steps requiring logical frame names. XLSX and ODS already shared these
same maintained pipelines through the Makefile's `SHEET_FORMAT` override.

Before adoption, both carriers physically ordered the visible sheets as
`stories`, `characters`, `events`, `groups`, `regions`, `places`,
`story_groups`, `story_cast_roles`, `story_cast_notes`,
`story_cast_development`, `story_cast_crystals`, `story_locations`, followed
by the metadata sheet. The long `story_cast_*_matrix_view` and
`story_group_matrix_view` names were the clearest candidates for shorter
labels; all adopted labels use existing English worldbuilding vocabulary.

== Delivery

Worldbuilding commit:
`b4c441f89ab8a08078b9e75f7ce5d72b553bab36`.

The exact declaration and physical visible-sheet order is:

[cols="2,2,1"]
|===
|Logical frame |Visible sheet |Order

|`stories` |`Stories` |1
|`story_locations` |`Story Locations` |2
|`story_group_matrix_view` |`Story Groups` |3
|`story_cast_role_matrix_view` |`Cast Roles` |4
|`story_cast_notes_matrix_view` |`Cast Notes` |5
|`story_cast_character_development_matrix_view` |`Cast Development` |6
|`story_cast_crystal_matrix_view` |`Cast Crystals` |7
|`characters` |`Characters` |8
|`groups` |`Groups` |9
|`places` |`Places` |10
|`regions` |`Regions` |11
|`events` |`Events` |12
|===

The consumer change also realigned existing validation and legend placement
metadata with the declared physical names. The reverse pipeline was retained
unchanged because its explicit mapping step was already correctly positioned.
Canonical JSON filenames and final frame keys remain `characters`, `events`,
`groups`, `places`, `regions`, `stories`, `story_cast_cells`, `story_groups`,
and `story_locations`.

== Acceptance Evidence

[cols="2,3"]
|===
|Criterion |Evidence

|Configured visible sheet names in XLSX and ODS
|The focused consumer test reads physical sheet names from both produced
carriers and asserts the complete declared list.

|Explicit nonlexical order in both carriers
|The same test asserts declaration order for XLSX and ODS and asserts that the
logical-frame declaration is not lexical.

|Canonical JSON/frame identity unchanged
|Reimport asserts the exact nine canonical JSON filenames and the maintained
semantic comparison passes for both carriers.

|XLSX and ODS reimport restore logical identity
|Both reverse runs complete through the real maintained workflow; no visible
alias becomes a JSON filename, and the final canonical data matches.

|Physical position independence
|The test swaps the first two visible sheets in each produced carrier while
retaining the metadata sheet, confirms the new physical order, then reimports
successfully through the persisted mappings.

|Payload data survives
|The full canonical/reimport semantic comparison passes, with additional exact
record assertions across stories, story locations, characters, and cast cells.

|Maintained roundtrips remain green
|The normal XLSX and ODS export/check-reimport paths both pass after adoption.

|No silent capability gap
|No core, XLSX, ODS, or cross-carrier defect was found; no BUG ticket is
required.
|===

Executed validation:

[source,text]
----
make test-workbook-view
# Ran 2 tests in 6.014s -- OK

make export-stories check-reimport
# Reimport matches canonical data after normalization.

make export-stories-ods check-reimport-ods
# Reimport matches canonical data after normalization.
----

Two consumer gaps were encountered and closed inside the slice:

* *consumer configuration gap* -- legend placement metadata still named the
  former physical `story_groups` sheet; it now targets `Story Groups`;
* *consumer documentation gap* -- the maintained pipeline had no concise
  workbook-view mapping/verification guide; one now lives beside the pipeline.

No core documentation gap, XLSX backend defect, ODS backend defect,
cross-carrier roundtrip defect, or missing capability was found.

== Follow-up

After this FTR closes:

. decide whether the current workbook-view API is sufficiently ergonomic;
. close any surfaced defects;
. specify one new presentation-meta roundtrip family;
. adapt the public demo with simpler, didactic vocabulary.
