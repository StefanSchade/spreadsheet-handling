= Record Closure: FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2
:toc:

This artifact records the post-acceptance record-management closure (archival
hygiene) for `FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2`. It is *not* an
implementation slice. No runtime code, tests, Worldbuilding data, pipeline
configuration, or accepted architecture were changed. Closure performed
2026-08-03.

== 1. Baseline (verified)

* Core HEAD at start: `8a86bd633af1671b4c78247c32bae72affc35315`
  -- `docs(FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2): review Slice 4 correction`.
* Worldbuilding HEAD at start: `692c087dc0633737e3345f7363267f92cf523652` (clean).
* Core working tree at start contained only the known protected concurrent pair
  (see section 20).

== 2. Final acceptance authority

* `docs/cold_storage/backlog/ftrs_done/FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice4_review_002.adoc`
  (Slice-4 independent acceptance review 002).
* Final acceptance commit: `8a86bd6`
  (`docs(FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2): review Slice 4 correction`).
* The implementation was not reopened; all four slices are independently accepted.

== 3. Complete archived family inventory (22 artifacts)

All 22 active-backlog artifacts of the FTR family were archived together:

[source,text]
----
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2.adoc                       (parent FTR)
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_assessment_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_confirmation_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_confirmation_review_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_implementation_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_review_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_review_002.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_review_003.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_review_004.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_correction_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_correction_002.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice1_correction_003.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice2_implementation_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice2_review_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice2_review_002.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice2_correction_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice3_implementation_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice3_review_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice4_implementation_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice4_review_001.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice4_review_002.adoc
FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_slice4_correction_001.adoc
----

Classification: 1 parent FTR, 1 assessment, 1 confirmation, 1 confirmation
review; Slice 1 (implementation, four reviews, three corrections); Slice 2
(implementation, two reviews, one correction); Slice 3 (implementation, one
review); Slice 4 (implementation, two reviews, one correction). The parent FTR
states the whole FTR is implemented and independently accepted.

== 4. Source and destination paths

* Source: `docs/backlog/`.
* Destination: `docs/cold_storage/backlog/ftrs_done/` (the repository's
  established completed-FTR destination; the whole family kept together, not
  split into `docs/cold_storage/reviews/`). Moved with `git mv`; content
  unchanged. Verified: parent absent from active backlog, all 22 present under
  the destination.

== 5. Active-index cleanup

* `docs/backlog/_ftrs_open.adoc`: removed the live
  `include::FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2.adoc[]` and the Phase-3d
  open-tracking bullet that described Slice 4 as "pending independent review
  002". Replaced with a short completed-state note that links the archival
  destination. The existing Phase-3d "Current state" note recording that
  `project_by_role` was retired by this FTR (and that `resolve_column_roles` is
  retained) was kept as accurate architectural context.
* `docs/backlog/roadmap_domain_hardening_sequence.adoc`: corrected two stale
  "pending that FTR's independent review 002" phrasings (Phase B status and the
  current-sequence-summary line) to record acceptance in review 002 plus the
  archival destination.

== 6. Cross-reference updates

* The only live include pointing at the old active path was in `_ftrs_open.adoc`
  (removed above).
* Architectural references that name the FTR by id only (no path) were preserved
  unchanged, as they remain useful and accurate:
  `docs/functional_model/ch04_use_case_concepts/xref_crosstable.adoc`,
  `docs/release_notes/release_notes.adoc`,
  `docs/developer_guide/ch10_project_evolution/01_project_evolution.adoc`,
  `docs/warm_storage/global_reviews/architecture_health_review.adoc`, and
  `docs/backlog/FTR-FUNCTIONAL-MODEL-VIEW-DERIVATION-FAMILIES-P5.adoc` (whose own
  remaining view-derivation scope legitimately stays open).
* `docs/backlog/...` path strings that appear inside the moved family artifacts
  are historical point-in-time records and were left unchanged (they are prose /
  code references, not live includes).

== 7. Project Memory FTR-row decisions

* Added `FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2` to
  `project_memory/canonical/ftrs.json` (it was absent): status `done (accepted)`,
  `priority: P4A2`, `current_relevance: current`, `source_path` pointing at the
  archived parent under `ftrs_done/`, with a coarse whole-FTR summary and
  Git-verified `created_commit`/`created_date`, `implementation_commits`,
  `review_commits`, and `closed_date`. Field shape follows the nearby
  `done (accepted)` P4A2 sibling `FTR-XREF-AXIS-MAPPINGS-P4A2`.

== 8. Historical dynamic-projection row decisions

* `FTR-DYNAMIC-FRAME-PROJECTION-IMPL-P5` was stale (`status: new`, active
  `docs/backlog/...` source path). Curated truthfully without deletion: status
  `superseded`, `current_relevance: historical`, `source_path` corrected to its
  actual current archival location
  `docs/cold_storage/backlog/ftrs_done/FTR-DYNAMIC-FRAME-PROJECTION-IMPL-P5.adoc`
  (verified on disk), `closed_date: 2026-08-03`. Summary and implementation
  notes preserve that the step was historically implemented and accepted (commit
  `4bedcf5`) and make clear that `project_by_role` was later retired by this FTR
  and is no longer part of the current pipeline surface. Historical
  implementation commit/date fields were left intact.
* Parent `FTR-DYNAMIC-FRAME-PROJECTION-P5`: left unchanged. Its `source_path` is
  already the `ftrs_done/` location and its `closed_date` is set; its residual
  `status: new` is a pre-existing generic hygiene detail not caused by the
  `project_by_role` retirement, so it was deliberately not normalized here (to
  avoid broad unrelated Project Memory normalization).

== 9. Closure-event decision

* Added exactly one coarse steering event to
  `project_memory/canonical/concern_events.json`:
  `SIG-ACT-XREF-LOOKUP-HELPER-SUBSTITUTION-ACCEPTED`
  (`source_type: activity`, `source_id: ACT-XREF-LOOKUP-HELPER-SUBSTITUTION-ACCEPTED`,
  `weight: high`, `event_date: 2026-08-03`). The summary covers the whole
  closure at steering altitude (asymmetric lookup support, durable
  workbook-boundary cleanup settled, ten Worldbuilding sites migrated,
  `project_by_role` removed, shared role resolver retained for validation, XLSX
  and ODS parity preserved) without enumerating individual slices or commits.
  `commit_refs` carries coarse anchors only.

== 10. Concern links

* Added two `concern_event_xrefs.json` rows linking the closure event to the
  smallest defensible set of genuinely-affected existing concerns:
** `CONC-PROJECTION-ERGONOMICS` -- role `implementation_evidence` (the explicit
   lookup/helper composition replaced role-based projection).
** `CONC-MAINTAINABILITY-HYGIENE` -- role `mitigation` (a redundant public
   pipeline step and its registry / implementation-unit records were removed).
* `CONC-USER-SURFACE-ADOPTION` and `CONC-DOMAIN-META-SEMANTICS` were considered
  and intentionally excluded to keep the set minimal. No concern status was
  changed and no new concern was created. Xref ids use the contract-documented,
  roundtrip-stable `CTSX-<event>--<concern>` form.

== 11. Review-record decision

* No new `reviews.json` row was added. This matches the established convention
  for recently-completed multi-slice FTR families (for example
  `FTR-XREF-AXIS-MAPPINGS-P4A2` and the grouped-XRef adoption), which record
  acceptance only through the coarse activity event and reference the acceptance
  review in prose, and it matches the Project Memory bounded-growth rule that a
  green acceptance review needs no separate canonical record when it neither
  changed the contract nor left an open finding. Slice-4 Review 002 was a clean
  acceptance and is referenced from the closure event and this artifact.

== 12. Canonical files changed

* `project_memory/canonical/ftrs.json` (added one row; curated one stale row).
* `project_memory/canonical/concern_events.json` (added one event).
* `project_memory/canonical/concern_event_xrefs.json` (added two xrefs).

== 13. Regenerated outputs

Derived and generated Project Memory outputs are `.gitignore`d rebuildable
artifacts and are not committed. Regenerated and inspected:

* `project_memory.ods` (export), `project_memory/staging/` (reimport).
* `project_memory/derived/` query views.
* `project_memory/derived/memory_health_report.{json,adoc}`.
* `docs_generated/project_memory/current_context.adoc`.

== 14. Exact commands and results

[source,shell]
----
make memory-setup          # OK (installed -e .[project-memory])
make memory-export         # OK (canonical JSON -> project_memory.ods)
make memory-check          # reimport + diff: FAILS on pre-existing roundtrip
                           #   normalization only (see below)
make memory-query          # OK (derived views rebuilt)
make memory-context        # OK (current_context.adoc rebuilt)
make memory-extract        # OK (1031 commit signals)
make memory-health-report  # OK (see section 15)
make domain-contracts-check# OK (health report written, no error)
----

`make memory-check` reports a diff between canonical JSON and the ODS reimport.
This discrepancy is *pre-existing and not introduced by this closure*: with all
three canonical edits stashed, the untouched baseline produces the same class of
diff (row reordering, legacy `CTSX`/`XREF` xref-id inconsistency in other rows,
and empty-optional-field injection on reimport). All five rows added or changed
by this closure were verified to roundtrip cleanly (identical in
`project_memory/staging/` after export+reimport). This is an explained,
pre-existing normalization gap, not an unexplained canonical difference.

== 15. Health-report result

* No new health errors were introduced. Holding extracted commit signals
  constant, the `ftrs.json` edit *improved* the report:
  `unknown_extracted_artifact_ids` dropped from 36 (baseline) to 35 (adding the
  FTR as a known canonical row resolved one previously-unknown id); all other
  summary counts were unchanged (`ambiguous_commit_links=48`,
  `recent_unlinked_commits=22`, `canonical_events_without_commit_refs=3`,
  `known_artifacts_without_commit_evidence=63`). The health report does not read
  `concern_events` / `concern_event_xrefs`, so those additions cannot affect it.
* Generated context: the completed FTR is correctly excluded from *Active FTRs*;
  the stale `FTR-DYNAMIC-FRAME-PROJECTION-IMPL-P5` row no longer appears as
  current/new; the closure event renders under both
  `CONC-PROJECTION-ERGONOMICS` and `CONC-MAINTAINABILITY-HYGIENE`; and the
  context does not advertise `project_by_role` as current (it appears only in
  past-tense removal prose).

== 16. Stale-path / open-state search result

* `rg 'include::.*FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2'` over `docs` and
  `project_memory`: no matches (the only live include was removed).
* No current backlog/index surface lists the FTR as open / pending /
  in_progress. Remaining `docs/backlog/...` path strings and any "pending"
  wording occur only inside archived point-in-time family artifacts and are
  preserved as historical truth.

== 17. Changed files (working tree)

[source,text]
----
R  docs/backlog/FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2*.adoc  (22 -> ftrs_done/)
A  docs/cold_storage/backlog/ftrs_done/FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2_record_closure_001.adoc
M  docs/backlog/_ftrs_open.adoc
M  docs/backlog/roadmap_domain_hardening_sequence.adoc
M  project_memory/canonical/ftrs.json
M  project_memory/canonical/concern_events.json
M  project_memory/canonical/concern_event_xrefs.json
----

No `src/`, registry, runtime-test, setup/install, Worldbuilding, or pipeline-YAML
file was changed.

== 18. Commits

Two path-specific Core commits (no push, no branch switch). Exact hashes are
recorded in the untracked resumability checkpoint
`/tmp/xref_lookup_helper_substitution_record_closure_2026-08-03.md` and in the
closure terminal summary.

* Commit 1 -- `docs(FTR-XREF-LOOKUP-HELPER-SUBSTITUTION-P4A2): archive accepted
  FTR family`: the 22 `git mv` renames, the `_ftrs_open.adoc` and
  `roadmap_domain_hardening_sequence.adoc` active-index edits, and this record
  closure artifact.
* Commit 2 -- `docs(project_memory): record projection helper substitution
  closure`: the three canonical Project Memory files.

== 19. Repository statuses (final)

* Core: FTR-owned changes committed in the two commits above; Worldbuilding not
  touched. The only remaining uncommitted entries are the protected concurrent
  pair (section 20).
* Worldbuilding: unchanged, HEAD `692c087`, clean.

== 20. Protected concurrent-path status

The known unrelated concurrent pair was left entirely untouched -- not restored,
edited, moved, renamed, staged, or committed; no `git add -A` / `git commit -a`
was used:

* `D docs/warm_storage/global_reviews/next_program_sequencing_review.adoc`
  (deletion, unstaged) -- unchanged start and end.
* `?? tests/next_program_sequencing_review.adoc` (untracked) -- unchanged;
  content hash `4dc22bbdb28a8e8d33ee8fd0c41cb484b5541a74` at both start and end.

== 21. Protected committed file status

* `docs/warm_storage/global_reviews/review_001_fk_helpers_human_readability.adoc`
  (commit `7448902`) was not modified, moved, or included in any commit.

XREF LOOKUP HELPER SUBSTITUTION RECORD CLOSURE COMPLETE
