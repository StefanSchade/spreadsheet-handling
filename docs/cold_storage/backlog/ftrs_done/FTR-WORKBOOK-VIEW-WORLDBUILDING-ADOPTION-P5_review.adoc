===== FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5 -- Implementation review

*Reviewed FTR:* `docs/cold_storage/backlog/ftrs_done/FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5.adoc`

*Worldbuilding implementation:* `b4c441f89ab8a08078b9e75f7ce5d72b553bab36`

*Review type:* Implementation self-review and closure.

*Verdict:* GREEN.

The consumer adoption stays within the FTR. It changes only the maintained
Dino-Insel workbook-view declaration, presentation-name-dependent validation
and legend routing, focused consumer tests, and adjacent usage documentation.
It does not change core product code, infer identities from labels, rename
canonical JSON, add presentation metadata families, or touch the demo.

==== Architectural Assessment

The implementation uses the existing workbook-view boundary as designed:
prepared logical frames are declared once with visible names and order;
persisted sheet mappings are authoritative on reimport; and the existing
inbound mapping step remains ahead of validation, projection, and
recomposition. The carrier reorder test demonstrates identity by metadata
rather than position.

No configuration ergonomics issue justified a core API change. The only
configuration issue was expected consumer-owned presentation routing: the
existing legend placement had to follow the newly declared physical sheet
name.

==== Acceptance Review

All acceptance criteria are met for both XLSX and ODS:

* configured names and the declared narrative-first, nonlexical order are
  asserted from each physical workbook;
* reimport restores canonical filenames/frame identities for both carriers;
* the test physically swaps representative sheets while retaining embedded
  metadata and then proves successful reimport;
* full semantic comparison and representative record checks prove payload, not
  names alone, survives;
* the pre-change baseline and post-change maintained roundtrips pass;
* consumer mapping and verification documentation records the adopted shape.

The consumer uses `unittest` because it has no pytest dependency or prior
pytest suite. The focused Make target runs two integration tests, each covering
both carriers through the real CLI workflows.

==== Findings and Gap Classification

No open findings.

* *Consumer configuration gap -- resolved:* legend placement still targeted
  the old physical `story_groups` name and was updated to `Story Groups`.
* *Consumer documentation gap -- resolved:* a pipeline-local mapping and
  verification guide was added.
* *Core documentation gap:* none.
* *XLSX backend defect:* none.
* *ODS backend defect:* none.
* *Cross-carrier roundtrip defect:* none.
* *Missing capability:* none.

No BUG backlog item is required.

==== Validation

Worldbuilding validation recorded by the implementation:

[source,text]
----
make test-workbook-view
# Ran 2 tests in 6.014s -- OK

make export-stories check-reimport
# Reimport matches canonical data after normalization.

make export-stories-ods check-reimport-ods
# Reimport matches canonical data after normalization.
----

Core closure validation:

[source,text]
----
.venv/bin/pytest -q tests/unit/domain/test_workbook_views.py \
  tests/unit/rendering/test_frame_selection.py \
  tests/integration/pipeline/test_configured_workbook_view_reimport_roundtrip.py
# 25 passed

make source-style-guards
# exit 0; 0 errors, 78 warnings, 2 allowlisted findings

make test-arch
# 134 passed

make docs-html
# exit 0; existing unrelated missing-attributes and section-level diagnostics remain

asciidoctor --failure-level=ERROR -o /tmp/ftrs_open.html \
  docs/backlog/_ftrs_open.adoc
asciidoctor --failure-level=ERROR -o /tmp/FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5.html \
  docs/cold_storage/backlog/ftrs_done/FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5.adoc
asciidoctor --failure-level=ERROR -o /tmp/FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5_review.html \
  docs/cold_storage/backlog/ftrs_done/FTR-WORKBOOK-VIEW-WORLDBUILDING-ADOPTION-P5_review.adoc
# all exit 0

git diff --check
# exit 0
----

An additional direct Ruff check is not green on the authoritative pre-change
core source: it reports nine existing findings in `core/refs.py`,
`domain/artifact_manifest.py`, `domain/validations/findings.py`, and
`rendering/workbook_projection.py`. None of those files changed in this slice.
This is unrelated baseline quality debt, not a workbook-view, documentation,
carrier, or cross-carrier gap, and broadening this adoption closure to edit
those files would violate scope.

==== Release Notes

No core release-note entry is needed. The core API, YAML contract, carrier
behavior, and documented capability are unchanged; this FTR validates an
existing contract in a sibling consumer. The consumer-owned visible names and
configuration are documented beside its maintained pipeline, and the consumer
has no release-notes artifact.

==== Lifecycle cleanup check

* The FTR status is `Done`.
* Its include was removed from `docs/backlog/_ftrs_open.adoc`, whose phase note
  now records the completed parity result.
* The FTR and this review move together to
  `docs/cold_storage/backlog/ftrs_done/`.
* No additional companion artifacts exist.
* No residual scope or accepted backend gap remains, so no follow-up FTR is
  required by this workstream.
* Cleanup is performed in the core evidence/closure change.
