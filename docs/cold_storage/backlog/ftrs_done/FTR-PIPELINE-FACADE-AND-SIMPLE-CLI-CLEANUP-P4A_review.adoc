===== Review: FTR-PIPELINE-FACADE-AND-SIMPLE-CLI-CLEANUP-P4A

*Review-Datum:* 2026-05-06
*Reviewer:* Codex
*Verdict:* Akzeptiert; Review-Findings in der Abschlussnacharbeit erledigt

'''

====== Initial Findings

Die erste Review des Implementierungsstands fand drei Punkte:

* *High:* `src/spreadsheet_handling/pipeline/pipeline.py` war weiterhin eine
  breite Backward-Compatibility-Fassade und re-exportierte Types,
  Step-Factories und Registry-Funktionen.
* *Medium:* Aktive Tests importierten weiterhin direkt aus
  `spreadsheet_handling.pipeline.pipeline`, obwohl direkte owning-module
  Imports existierten.
* *Low:* Ein CLI-End-to-End-Test liess die neu eingefuehrte
  `DeprecationWarning` uncaptured durchlaufen.

'''

====== Nacharbeit

* Die alte `pipeline.pipeline`-Fassade wurde entfernt.
* Maintained Imports wurden auf `pipeline.registry`, `pipeline.steps`,
  `pipeline.types` oder die bewusste Package-API umgestellt.
* `pipeline.__init__` bleibt als kleiner intentionaler Public Surface erhalten.
* `tests/unit/pipeline/test_public_import_surface.py` sichert ab, dass die alte
  Fassade nicht erneut auftaucht und die Package-API eng bleibt.
* Der offene Warning-Test erwartet die `sheets-pack` Deprecation jetzt
  explizit.
* `sheets-pack` und `sheets-unpack` bleiben funktional, geben aber
  Deprecation-Warnings aus und sind in der User-Doku als legacy example shims
  markiert.

'''

====== Testsignal

In dieser Umgebung erfolgreich ausgefuehrt:

* Fokussierter Pytest-Slice fuer CLI, Pipeline-Registry, Import-Surface,
  Domain-Registry-User und Pipeline-Integration:
  `114 passed`
* Ruff auf den geaenderten CLI-, Pipeline- und Testdateien:
  `All checks passed`

Hinweis: Der erste WSL-Lauf war im Sandbox-Kontext mit `E_ACCESSDENIED`
blockiert; der gleiche Test-/Ruff-Slice lief danach mit genehmigtem WSL-Aufruf
erfolgreich.

'''

====== Fazit

Die FTR-Akzeptanz ist nach der Review-Nacharbeit erfuellt: Die breite
Compatibility-Fassade ist weg, aktive Code- und Testpfade nutzen die
intendierte Import-Oberflaeche, die CLI-Shims sind sichtbar deprecated, und der
Shortcut-Mechanismus bleibt als bewusstes Muster dokumentiert.
