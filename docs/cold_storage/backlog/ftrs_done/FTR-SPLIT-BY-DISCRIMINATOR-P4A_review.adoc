===== Review: FTR-SPLIT-BY-DISCRIMINATOR-P4A

*Review-Datum:* 2026-04-30
*Reviewer:* (post-hoc review)
*Verdict:* Vollständig umgesetzt

'''

====== Zusammenfassung

Das FTR liefert ein bijektives Split/Merge-Transformationspaar für die
Aufspaltung einer Tabelle nach Diskriminator-Spalte. Die Überprüfung bestätigt,
dass alle im Delivery-Scope und in den Acceptance Criteria genannten Punkte
umgesetzt sind.

'''

====== Prüfpunkte

[cols="1,3,1", options="header"]
|===
| # | Kriterium | Status

| 1
| `split_by_discriminator` Step implementiert
  (`domain/transformations/discriminator_split.py`)
| ✓

| 2
| `merge_by_discriminator` Step implementiert (gleiche Datei)
| ✓

| 3
| Meta-Contract: Metadata unter `_meta.split_by_discriminator`
  (values, column_order, split_columns, row_order, preserve_row_order)
| ✓

| 4
| Meta-Registry-Eintrag: `meta_canonical` / `canonical_meta`
  (in `meta_registry.yaml`)
| ✓

| 5
| Pipeline-Step-Registry: beide Steps registriert mit
  `kind: "conditionally_bijective"` und Inverse-Verknüpfung
| ✓

| 6
| `target_pattern` / `source_pattern` mit `{value}` Placeholder
| ✓

| 7
| Optionale `value_map` für explizites Naming
| ✓

| 8
| Row-Order-Preservation via Metadata
| ✓

| 9
| Roundtrip-Test: `merge(split(df)) == df`
| ✓

| 10
| Guard: Non-uniform columns → `ValueError`
| ✓

| 11
| Guard: Duplicate generated frame names → `ValueError`
| ✓

| 12
| Guard: Non-invertible Pattern (fehlendes `{value}`) → `ValueError`
| ✓

| 13
| Guard: Unsafe discriminator values → `ValueError` mit Hinweis auf `value_map`
| ✓

| 14
| Guard: Existing target frame collision → `ValueError`
| ✓

| 15
| Guard: Source enthält bereits Discriminator-Spalte → `ValueError`
| ✓
|===

'''

====== Testabdeckung

[cols="2,3", options="header"]
|===
| Testdatei | Abdeckung

| `tests/unit/domain/transformations/test_discriminator_split.py`
| 10 Tests: Split, Merge, Roundtrip, value_map, 5 Fehlerfälle

| `tests/integration/pipeline/test_discriminator_split_pipeline.py`
| YAML-Pipeline Split+Merge End-to-End

| `tests/unit/pipeline/test_step_registration.py`
| Registry-Auflösung beider Steps

| `tests/architecture/semantic_invariants/test_meta_registry_hardening.py`
| Meta-Klassifikation als `canonical_meta`
|===

'''

====== Beobachtungen

* Die FTR-Spezifikation wurde gegenüber dem ursprünglichen Entwurf sinnvoll
  erweitert: Row-Order-Metadata, explizite Naming-Contracts, und detaillierte
  Fehlerfälle sind dazugekommen.
* Die Trennung zwischen metadata-gestütztem Roundtrip und rein
  konfigurationsgetriebenem Ad-hoc-Merge ist sauber gelöst.
* Die Guard-Abdeckung ist umfassend — alle spezifizierten Fehlerfälle haben
  dedizierte Tests.

'''

====== Fazit

Kein Handlungsbedarf. Feature ist vollständig, gut getestet, und im Registry
korrekt verankert.
