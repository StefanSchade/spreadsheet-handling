===== FTR-ODS-CONSUMER-FEATURE-PARITY-P4A -- Track ODS parity for consumer-driven workbook features

*Status:* Implemented (audit complete) — independent review accepted
*Priority:* P4A
*Area:* Spreadsheet adapters / parity
*Kind:* Cross-cutting tracking FTR

==== Purpose

Keep ODS/Calc support visible while consumer-driven workbook features are
added.

The ODS adapter is not just a secondary output format. It is also the practical
test that the adapter architecture carries the spreadsheet-neutral contract
rather than hard-coding XLSX assumptions into new features.

This is a tracking FTR, not a standalone feature. Each spreadsheet-facing FTR
in this group should document its ODS disposition. This FTR verifies that the
group has no implicit XLSX-only behavior.

==== Scope

For the pre-beta prototype-consumer-derived feature set, maintain a small
parity matrix and focused tests. The scope is split between post-wave items
already implemented (audit only, no rebuild) and feature work that lands
during the active P4A wave (parity must be covered while the feature lands).

===== Audit existing parity

The following capabilities are already implemented in the post-REVIEW-001 wave
state. The task here is to record their ODS disposition explicitly
(implemented, not applicable, or accepted gap), not to re-implement them:

* declarative workbook views (closed by `FTR-DECLARATIVE-WORKBOOK-VIEWS-P4A`)
* explicit helper-column styling (closed by `FTR-HELPER-COLUMN-STYLE-METADATA-P4A`)
* editable/read-only column protection (closed by `FTR-EDITABLE-COLUMNS-AND-PROTECTION-P4A`)
* lookup helper formula mode (closed by the formula-providers / formulas-core-move FTR chain)
* validation dropdowns from legend metadata, where existing ODS parity tests
  do not already cover the case
* column-width behavior, including accepted XLSX-only gaps

===== Coverage during the active wave

The following active P4A FTRs must each document an explicit ODS disposition
(implemented, not applicable, or accepted gap) in their own acceptance text;
this tracking FTR verifies that disposition is present:

* `FTR-LOCALIZED-RESOURCE-MATRIX-ROUNDTRIP-P4A`
* `FTR-RESOURCE-FALLBACK-OVERRIDE-SEMANTICS-P4A`
* `FTR-KEY-VALUE-RESOURCE-WRITER-P4A` (ODS disposition typically "not
  applicable" since the writer emits text resource files, not spreadsheets)
* `FTR-GENERATED-ARTIFACT-MANIFEST-P4A` (same: typically "not applicable")
* `FTR-WORKBOOK-REIMPORT-VIEW-MAPPING-P4A`
* `FTR-WORKBOOK-REIMPORT-DERIVED-COLUMN-POLICY-P4A`
* `FTR-WORKBOOK-VIEW-ROUNDTRIP-RECOMPOSITION-P4A`

`FTR-LOCALIZED-RESOURCE-MATRIX-ROUNDTRIP-P4A` disposition: implemented in the
minimal slice with the same explicit configured pipeline exercised through
XLSX and ODS roundtrip readback.

===== Post-wave invariants to protect

* Backend dispatch now runs centrally through `io_backends/router.py`. New
  spreadsheet-facing features automatically reach both XLSX and ODS through
  the router; new code paths must not reintroduce parallel dispatch.
* The lazy-optional-import invariant for openpyxl and odfpy must be preserved.
  The regression guard `test_package_import_does_not_load_spreadsheet_backend_modules`
  stays green: bare `import spreadsheet_handling` must not load `openpyxl`,
  `odf`, or the `io_backends.xlsx` / `io_backends.ods` submodules.

==== Non-goals

* Do not force fake ODS parity where the backend model is materially different.
* Do not block a feature forever if the correct outcome is an explicit
  documented adapter gap.
* Do not duplicate every XLSX integration test one-to-one.

==== Acceptance

* Every prototype-consumer-derived pre-beta workbook feature has an explicit
  ODS disposition: implemented, not applicable, or accepted gap.
* At least one ODS integration or roundtrip test exercises the maintained
  declarative workbook path.
* Any accepted ODS gap is documented in the relevant FTR and in a compact
  adapter capability note.
* No new generic workbook feature lands with only implicit XLSX behavior.

==== Pre-beta ODS parity matrix

Disposition vocabulary: *implemented* (proven on both backends), *not
applicable* (no spreadsheet render/parse path; nothing to reconcile),
*accepted gap* (backend model differs materially; documented, not simulated).

[cols="3,2,5",options="header"]
|===
| Feature (owning FTR) | ODS disposition | Evidence

| Declarative workbook views (`FTR-DECLARATIVE-WORKBOOK-VIEWS-P4A`)
| implemented
| `test_ods_xlsx_parity.py::test_configured_workbook_view_renders_portably_across_xlsx_and_ods`

| Helper-column styling (`FTR-HELPER-COLUMN-STYLE-METADATA-P4A`)
| implemented
| `test_ods_xlsx_parity.py::test_explicit_helper_column_style_metadata_is_portable`

| Editable/read-only column protection (`FTR-EDITABLE-COLUMNS-AND-PROTECTION-P4A`)
| accepted gap (XLSX-only)
| `test_ods_xlsx_parity.py::test_protection_is_xlsx_only_ods_documented_gap`; capability profile

| Lookup helper formula mode (formulas-core chain)
| implemented
| `tests/unit/io_backends/ods/test_ods_formula_translation.py`

| Validation dropdowns from legend metadata
| implemented
| `test_legend_blocks.py::test_ods_legend_block_roundtrips_as_metadata_not_data_frame`

| Column-width behavior (`FTR-COLUMN-WIDTH-ROUNDTRIP-P4`, archived)
| accepted gap (XLSX-only / partial)
| archived FTR closure note + capability profile

| Freeze panes / freeze header (`FTR-ODS-FREEZE-READBACK-P3K`)
| accepted gap (write best-effort; read hint-only)
| `test_ods_xlsx_parity.py::test_freeze_parse_hint_difference_is_an_explicit_accepted_gap`; capability profile

| Localized resource matrix roundtrip (`FTR-LOCALIZED-RESOURCE-MATRIX-ROUNDTRIP-P4A`)
| implemented
| `test_localized_resource_matrix_roundtrip.py::test_generic_discriminator_context_matrix_roundtrip_across_xlsx_and_ods`

| Resource fallback/override semantics (`FTR-RESOURCE-FALLBACK-OVERRIDE-SEMANTICS-P4A`)
| not applicable (backend-neutral domain step)
| disposition note in its FTR; output covered by generic frame/`_meta` parity

| Key-value resource writer (`FTR-KEY-VALUE-RESOURCE-WRITER-P4A`)
| not applicable (text writer)
| disposition note in its FTR

| Generated artifact manifest (`FTR-GENERATED-ARTIFACT-MANIFEST-P4A`)
| not applicable (YAML/JSON manifest)
| disposition note in its FTR

| Workbook reimport view mapping (`FTR-WORKBOOK-REIMPORT-VIEW-MAPPING-P4A`)
| implemented
| `test_ods_xlsx_parity.py::test_workbook_view_reverse_mapping_roundtrips_portably_across_xlsx_and_ods`

| Workbook reimport derived-column policy (`FTR-WORKBOOK-REIMPORT-DERIVED-COLUMN-POLICY-P4A`)
| not applicable (in-pipeline; does not parse XLSX/ODS)
| explicit in that FTR's acceptance (slice-1 boundary)

| Workbook view roundtrip recomposition — current slices (`FTR-WORKBOOK-VIEW-ROUNDTRIP-RECOMPOSITION-P4A`)
| implemented
| `test_configured_workbook_view_reimport_roundtrip.py` (XLSX + ODS, incl. identity roundtrip)
|===

==== Delivery status

This section records implementation/audit status. The independent review is
recorded in `FTR-ODS-CONSUMER-FEATURE-PARITY-P4A_review.adoc`.

* Audit complete. Every listed pre-beta workbook feature now has an explicit
  ODS disposition recorded above and in its owning FTR text (added where
  previously missing for the key-value writer, artifact manifest, and
  resource fallback/override FTRs).
* No production code changed and no new tests were added: existing coverage
  already proves every *implemented* disposition, and the *not applicable*
  items are backend-neutral by construction. Adding spreadsheet parity tests
  for in-pipeline / text-writer steps would be fake parity and is deliberately
  avoided.
* Accepted gaps (protection, column-width, freeze readback) remain documented
  in their FTRs and in
  `docs/internal_guide/architecture/spreadsheet_backend_capability_profiles.adoc`;
  none are simulated.
* Post-wave invariants verified intact: backend dispatch stays centralized
  through `io_backends/router.py`, the lazy-optional-import guard
  (`test_package_import_does_not_load_spreadsheet_backend_modules`) stays
  green, and no parallel XLSX-only dispatch was introduced.
* Acceptance is met and independently reviewed. The FTR remains in the active
  backlog as an implemented tracking FTR until the enclosing pre-beta backlog
  phase is closed.
