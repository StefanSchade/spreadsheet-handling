===== Review: FTR-SOURCE-STYLE-GUARDRAILS-P4

*Review-Datum:* 2026-05-06
*Reviewer:* Codex (post-hoc review)
*Verdict:* Akzeptiert; keine offenen Findings

'''

====== Findings

Keine offenen funktionalen oder strukturellen Findings gegen den umgesetzten
Delivery-Scope.

Die FTR-Acceptance ist im aktuellen Stand nachvollziehbar erfuellt:

* Ein separater Source-Style-Guard-Command ist vorhanden und lokal lauffaehig.
* Der Command ist als eigener Make-Target verdrahtet.
* Source-Style-Guardrails laufen getrennt von Architektur-Guardrails.
* Nested conditional expressions in Production-Code werden als Error erzwungen.
* Overlong-Function- und Complexity-Checks liefern stabiles, reviewbares Output.
* AI-Primer und Repo-Agent-Instruktionen wurden explizit um Readability-Regeln
  ergaenzt.
* Eine knappe, strukturierte Allowlist-Policy ist vorhanden.
* CI-Integration ist bewusst deferred und im FTR begruendet dokumentiert.

'''

====== Positiv bestaetigt

* Tooling-Slice geliefert:
  - `tools/check_source_style.py` implementiert AST- und Kommentar-Guards fuer
    function-length, branch-complexity, nested-conditional-expression,
    dense-comprehension, german-source-comment und german-source-string.
  - `tools/source_style_allowlist.yaml` ist vorhanden und enthaelt enge,
    begruendete Ausnahmen mit rule/path/symbol/rationale/owner/review_date.

* Stage-A-Verhalten korrekt modelliert:
  - Fail: nested-conditional-expression, german-source-comment, parse-error,
    allowlist-invalid.
  - Warn: function-length, branch-complexity, dense-comprehension,
    german-source-string, allowlist-stale.

* Separationsprinzip umgesetzt:
  - `make source-style-guards` als eigener Entry-Point.
  - Architektur-Guardrails bleiben separat ueber `make test-arch`.

* Dokumentation und Agent-Regeln aktualisiert:
  - `docs/internal_guide/dev_man/source_style_guardrails.adoc` dokumentiert
    Ziel, Stage-A-Verhalten, Allowlist-Policy und CI-Status.
  - `docs/internal_guide/ai_policy/ai_primer_prompt.adoc` und
    `docs/internal_guide/ai_policy/repo_committing_agents.adoc` enthalten
    explizite Vorgaben zu nested conditionals, benannten Zwischenwerten,
    Function-Size und Sprachkonsistenz.

* Traceability vorhanden:
  - Implementierung in Commit `0b9c184` mit FTR-Referenz im Commit-Body.

'''

====== Testsignal

In dieser Umgebung erfolgreich ausgefuehrt:

* `python tools/check_source_style.py`
  - Ergebnis: `0 error(s), 38 warning(s), 3 allowlisted finding(s)`
  - Das passt zum dokumentierten Stage-A-Modell (warn-first mit enger Error-Menge).

Einschraenkung der lokalen Verifizierung:

* `python -m pytest` ist in der aktiven Windows-Shell nicht verfuegbar
  (`No module named pytest`), daher konnte `tests/unit/tools/test_check_source_style.py`
  hier nicht direkt erneut ausgefuehrt werden.
* Der Delivery-Commit enthaelt jedoch dedizierte Unit-Tests fuer den neuen
  Guard-Tool-Slice.

'''

====== Fazit

Der FTR ist konsistent und praxisnah umgesetzt: eigener Guardrail-Pfad,
explizite Stage-A-Signale, enge Allowlist-Disziplin und klare Agent-Policy-
Verankerung. Das reduziert Readability-Drift bei AI-assistierten Aenderungen,
ohne Architektur-Guardrails zu vermischen oder sofort in noisy Hard-Fails zu
kippen.
