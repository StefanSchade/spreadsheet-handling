===== FTR-WORKBOOK-VIEW-ROUNDTRIP-RECOMPOSITION-P4A -- Recompose edited workbook views into canonical frames

*Status:* Done
*Priority:* P4A
*Area:* Pipeline / roundtrip semantics
*Prerequisites:* FTR-WORKBOOK-REIMPORT-VIEW-MAPPING-P4A, FTR-WORKBOOK-REIMPORT-DERIVED-COLUMN-POLICY-P4A

==== Purpose

Make the workbook readback path declarative enough to turn edited spreadsheet
views back into canonical source frames.

Generating a friendly workbook is only half of the business-editing use case.
The important roundtrip is:

`canonical JSON -> editable workbook views -> edited workbook -> canonical JSON`

A prototypical consumer reimport pipeline currently only reads the workbook
back to JSON. Mapping visible sheets and removing helper columns are handled by
the prerequisite reimport FTRs. This FTR focuses on recomposing edited view
payloads, especially matrix-shaped views, into canonical relation frames.

Note: `FTR-LOCALIZED-RESOURCE-MATRIX-ROUNDTRIP-P4A` proves recomposition by an
explicit test-local pipeline. Reusable workbook-view recomposition policy,
conflict handling, and sheet/frame readback mapping remain in this FTR.

==== Goals

* Use explicit sheet/frame mapping from the reimport mapping FTR.
* Use derived/helper-column policy from the reimport derived-column FTR.
* Support reverse compact-matrix or xref expansion from an editable matrix view
  back to the canonical relation.
* Preserve or deliberately ignore derived/read-only columns according to
  lifecycle/editability metadata.
* Keep payload-conflict / precedence handling for overlapping editable views
  explicit and configuration-driven; that concern is tracked separately in
  `FTR-WORKBOOK-VIEW-PAYLOAD-CONFLICT-PRECEDENCE-P6`.
* Keep the readback pipeline inspectable and runnable through `sheets-run`.

==== Non-goals

* Do not require automatic conflict resolution in this slice.
* Do not infer canonical targets from arbitrary sheet names without
  configuration.
* Do not trust protected/read-only cells as a security boundary.
* Do not own helper-column mismatch policy; that belongs to
  `FTR-WORKBOOK-REIMPORT-DERIVED-COLUMN-POLICY-P4A`.
* Do not implement multi-user merge reconciliation here.

==== Acceptance

* A business-facing workbook can be read back through a configured pipeline
  that produces canonical relation frames from workbook-view projections.
* Matrix cells can be expanded back into explicit relation rows through the
  configured path using existing declared transform configuration and without
  heuristic target inference.
* Payload-conflict / precedence handling for overlapping editable views is
  explicitly deferred to
  `FTR-WORKBOOK-VIEW-PAYLOAD-CONFLICT-PRECEDENCE-P6`.
* The roundtrip has focused tests for edited matrix values and unchanged
  matrix values on the configured path.
* ODS and XLSX readback dispositions are explicit for the same configured
  roundtrip path.

==== Delivery status

This section records the delivered closure scope for this FTR. Final review
details live in the sibling `_review.adoc`.

* Slice A -- sheet remap -- *Implemented*. `apply_workbook_view_sheet_mappings`
  (`spreadsheet_handling.domain.workbook_views`) re-keys visible workbook
  sheets to logical view frames using the persisted
  `_meta.workbook_view.sheet_mappings`. It delegates identity resolution to
  `resolve_workbook_view_sheet_mappings` (no inference from sheet names,
  order, normalized labels, or columns), preserves `_meta` unchanged, returns
  only mapped frames plus `_meta`, and is registered as a YAML-callable
  pipeline step (mirrored in `pipeline_step_registry.json`).
* Slice B -- configured reference reimport roundtrip -- *Implemented*. A
  configured pipeline takes an edited workbook view written under a *renamed*
  visible sheet back to canonical relation frames using only existing
  machinery: forward `contract_xref` + `configure_workbook_view`, the Slice-A
  `apply_workbook_view_sheet_mappings` remap step, and existing `expand_xref`
  recomposition. Covered by
  `tests/integration/pipeline/test_configured_workbook_view_reimport_roundtrip.py`
  for both XLSX and ODS (renamed sheet -> logical frame -> canonical relation,
  edited matrix values reflected, unchanged values identity roundtrip). No new
  matrix logic was added.
* Conflict/precedence handling is *delegated* to
  `FTR-WORKBOOK-VIEW-PAYLOAD-CONFLICT-PRECEDENCE-P6` (Phase 6). It is
  intentionally out of the beta-critical recomposition path. Execution steps
  must remain explicit/configuration-driven and must not guess winners.
* Final disposition: this FTR closes on the configured workbook-view
  reimport/recomposition slice proven through the existing generic XRef
  machinery (`apply_workbook_view_sheet_mappings` + `expand_xref`) for XLSX
  and ODS. No new production matrix logic was added in the closing slice.
  Conflict/precedence behavior is intentionally no longer owned here and is
  tracked by the dedicated P6 FTR above.
