== Event–FTR Links

Concern events are cross-referenced to FTRs via shared commit references. This is
a mechanical, rebuildable join and is not rendered in full in the default
context to keep it focused on current state.

{% if event_ftr_link_count %}
{{ event_ftr_link_count }} event–FTR commit links are available in the derived
table `project_memory/derived/event_ftr_links.json` (rebuilt by `make
memory-query`). Consult it when tracing which commits connect a concern event to
an FTR.
{% else %}
No event–FTR links were derived from the current commit reference data.
{% endif %}
