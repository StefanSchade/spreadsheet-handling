[
  {
    "id": "GAP-META-REF-DECLARATIONS-IN-CODE",
    "subject_kind": "implementation_unit",
    "subject_id": "IMPL-DOMAIN-SCHEMA-COLUMNS",
    "finding": "Reference-detection conventions (FRAME_KEYS/COLUMN_KEYS/COLUMN_LIST_KEYS) and per-root maintenance policies are hardcoded frozensets in meta_refs.py. The registry (meta_reference_surfaces) now mirrors them, but the code does not read the registry; consistency is enforced only by tests/architecture/semantic_invariants/test_meta_reference_surfaces.py.",
    "disposition": "accepted",
    "source_refs": "src/spreadsheet_handling/domain/schema_maintenance/meta_refs.py; docs/cold_storage/reviews/global_reviews/review_005_domain_contract_registry/review.adoc (Slice 1)",
    "notes": "mirror-plus-guard is the deliberate Slice 1 design; generating the frozensets from the registry is a possible later step"
  },
  {
    "id": "GAP-META-REF-CONVENTION-ESCAPE",
    "subject_kind": "meta_reference_surface",
    "subject_id": "MRS-UNKNOWN-ROOT-DEFAULT",
    "finding": "A new meta attribute holding a column name escapes rename/drop propagation silently unless it lies on an explicitly handled path of a supported root, or matches the FRAME_KEYS+COLUMN_KEYS/COLUMN_LIST_KEYS convention inside a scanned root. Supported roots are exempt from the scan, so unhandled paths inside them escape even when they use conventional key names.",
    "disposition": "open",
    "source_refs": "src/spreadsheet_handling/domain/schema_maintenance/meta_update.py:_handle_plugin_roots; src/spreadsheet_handling/domain/schema_maintenance/meta_refs.py:is_known_schema_maintenance_root",
    "notes": "escapes produce no report entry; the operator cannot see them in a dry run. The concrete helper_policies.lookup instance was fixed in Slice 1a (GAP-HELPER-LOOKUP-POLICY-ESCAPES resolved); the class remains open for other unhandled supported-root paths and non-conventional key names"
  },
  {
    "id": "GAP-HELPER-LOOKUP-POLICY-ESCAPES",
    "subject_kind": "meta_reference_surface",
    "subject_id": "MRS-FK-RELATIONS-SOURCE-COLUMN",
    "finding": "RESOLVED (Slice 1a): _meta.helper_policies.lookup column references (key, allowed_helpers, default_helpers, order.sort_by) previously escaped rename/drop propagation because _handle_helper_policies maintained only the fk subtree and supported roots are exempt from the unknown-root scan. Schema maintenance now updates lookup references on rename of the lookup frame's columns and blocks referenced drops (prune included); malformed lookup shapes block.",
    "disposition": "resolved",
    "source_refs": "src/spreadsheet_handling/domain/schema_maintenance/meta_update.py:_handle_lookup_policies; tests/unit/domain/schema_maintenance/test_metadata_refs.py (lookup policy cases); registries/domain_contracts/canonical/meta_reference_surfaces.json (MRS-LOOKUP-POLICY-*)",
    "notes": "fixed 2026-07-06 by maintaining helper_policies.lookup alongside fk; the broader silent-escape class (unhandled paths in supported roots, non-conventional key names) remains open as GAP-META-REF-CONVENTION-ESCAPE"
  },
  {
    "id": "GAP-SCHEMA-TABLE-KEY-UNIMPLEMENTED",
    "subject_kind": "transformation",
    "subject_id": "TRANS-RENAME-KEY",
    "finding": "Table/key maintenance rows (TRANS-ADD-TABLE, TRANS-REMOVE-TABLE, TRANS-RENAME-TABLE, TRANS-RENAME-KEY) remain unimplemented candidates; the implemented schema maintenance is column-level only (TRANS-SCHEMA-COLUMN-MAINTENANCE). The column implementation does not discharge these rows.",
    "disposition": "open",
    "source_refs": "src/spreadsheet_handling/domain/schema_maintenance/operations.py; registries/domain_contracts/canonical/transformations.json",
    "notes": "kept as candidates on purpose; promoting or retiring them is a semantic decision outside Slice 1"
  }
]
